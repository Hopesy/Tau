# 质量评分

用这份文档按产品区域和架构层次记录当前质量水位，方便持续知道最薄弱的地方在哪。

## 评分标准

- `A`：覆盖完整、行为稳定、文档清楚、运行风险低。
- `B`：整体可接受，但还有明确短板。
- `C`：能用，但需要针对性补强。
- `D`：脆弱、缺少规范，或很多行为尚未定义。

## 当前基线（2026-05-02）

| 区域 | 评分 | 原因 | 下一步 |
| --- | --- | --- | --- |
| 产品定义 | C | 已有 `docs/ARCHITECTURE.md`、`docs/product-specs/tau-port-overview.md` 和完整移植总 plan `docs/exec-plans/active/2026-05-10-tau-complete-pi-mono-port.md`，并且不再只围绕 CLI 说话，`WebUi / Mom / Pods` 也已经进入真实产品切片。当前问题不再是“有没有模块”，而是每个模块距离上游 parity closure 还有明确缺口。 | 按完整移植总 plan 继续把每个应用面的剩余能力写实：Mom Slack/workspace/sandbox，CodingAgent session tree，Ai OAuth/provider e2e，WebUi 流式/附件，Pods lifecycle。 |
| Tau.Ai | C | 已补上 `ModelCatalog`、内建模型表、provider auth resolver、auth.json 读取、OAuth provider registry，以及 Azure OpenAI Responses / OpenAI Codex / Mistral / Vertex / Gemini CLI / Bedrock 的第一轮接线；当前已把 `openai-responses`、`openai-codex-responses` 与 `azure-openai-responses` 从 `OpenAiCompatibleProvider` 兜底提升为专用 SSE provider，把 Codex Responses 扩展到 WebSocket/auto transport 与 session socket cache，并补齐 Responses/Codex service-tier usage 与 flex/priority cost multiplier、GitHub Copilot Responses 动态 headers / vision 行为与 tool-result image payload，把 `mistral-conversations` 切到专用 Mistral provider，把 Bedrock 从 placeholder 推进到 ConverseStream 专用 provider，把 Vertex ADC 从 placeholder 推进到 service account / authorized user token exchange，并把 Gemini CLI / Antigravity 请求、headers、fallback 与 retry 行为补到更接近上游；这轮又补了 generated model seed/generator/catalog 接缝、typed/default selection、`Model.Compat` 元数据和 models.json custom model/provider 配置入口和 request auth/header 合并，OpenAI-compatible provider 已能按显式 compat 消费 stream usage、max tokens field、reasoning format/map、z.ai tool stream、tool strict mode、OpenRouter routing 与 Vercel AI Gateway routing，`ModelCatalog` 也能启动时合并 `TAU_MODELS_FILE` / `.tau/models.json` / `~/.tau/models.json`。仍缺 AWS SSO/AssumeRole/IMDS/ECS/web identity credential chain、真实云端 e2e、OAuth login/refresh 与 models.json 动态 provider API 注册。 | 继续把剩余 provider 从“可发现/可配置/可序列化”推进到“可完整调用”，优先补 OAuth login/refresh、AWS SSO/AssumeRole/IMDS/ECS/web identity credential chain、dynamic provider 注册与 provider e2e。 |
| Tau.Agent | C | 双层循环、工具执行器、状态与事件骨架已具备，继续稳定承接各应用面；本轮还承担了 Mom 结构化委派测试的宿主复用角色。但高层 Agent API、行为回归和对外 façade 仍不完整。 | 在支撑 WebUi / Mom 的过程中继续补 façade 与行为测试。 |
| Tau.CodingAgent | C | 已具备最小 CLI 宿主、可测试主循环、基础工具链、本地 session 持久化、最小 session lifecycle、最小 settings/model selection、auth 状态入口和最小手动 compaction；运行时默认 model/provider 已接到 `ModelCatalog`，独立 `csproj` 的 build / run 已恢复，并且 `Tau.CodingAgent` / `Tau.WebUi` / `Tau.Mom` / `Tau.CodingAgent.Tests` / `Tau.Agent.Tests` 已从 DLL `HintPath` 收回到正常 `ProjectReference`。显式 `Create(provider, model, history)` 宿主工厂继续作为 WebUi/Mom 共享 runtime 边界；当前 `CodingAgentSessionStore` 支持 `TAU_CODING_AGENT_SESSION_FILE` 或 `./.tau/coding-agent-session.json`，并持久化 provider/model/messages/display name，`CodingAgentSettingsStore` 支持 `TAU_CODING_AGENT_SETTINGS_FILE` 或 `./.tau/coding-agent-settings.json`，CLI 支持 `/help`、`/name`、`/copy`、`/export`、`/import`、`/new`、`/session`、`/quit`、`/model`、`/provider`、`/models`、`/providers`、`/auth`、`/login` 和 `/compact`；`/session` 当前只报告 Tau 平面 session 的 display name、model、消息计数、tool call 数和 session 文件路径；`/export` 当前导出 Tau snapshot JSON，`/import` 当前严格导入 Tau snapshot JSON 并恢复当前平面 session；slash command 解析已抽到 `CodingAgentCommandRouter`，clipboard 写入已通过 `ICodingAgentClipboard` 隔离，避免 `CodingAgentHost` 继续膨胀。 | 继续补真实 OAuth/device login、更完整的 compaction（auto-compaction、compaction metadata、branch/tree 语义）、richer session 管理（resume/tree/full stats）和更高保真的 copy/export/import/share 等 session 操作。 |
| Tau.Tui | C | 已经有最小终端抽象、transcript、输入缓冲和会话层，并承载 `Tau.CodingAgent` 主路径；但还没有真正的组件系统、编辑器、渲染层和键盘体系。 | 从最小输入编辑与消息渲染开始继续向真实 TUI 推进。 |
| Tau.WebUi | C | 已经从 ASP.NET Core `Hello World` 提升为第二层 Web 宿主：首页聊天页、status/catalog/session/messages API、provider/model 选择和 `output/webui-sessions.json` 持久化都已可跑；同时新增了最小 runner/store 测试。仍然缺流式前端、附件、auth/settings UX 和 richer rendering。 | 继续补流式消息绑定、会话恢复体验、附件和更完整的 auth/settings 入口。 |
| Tau.Mom | C | 已经从 worker 模板提升到结构化本地委派宿主：`--once`、inbox/outbox/archive/events、`.txt/.md/.json` 输入、`provider/model/workingDirectory/title/metadata/attachments` 字段和结果落盘都已现场跑通；前一轮把 runner / result schema 收口为结构化 `DelegationToolEvent`、stop reason、`DelegationUsage`（含 token + 可选总成本），并把 `RuntimeDelegationAgentRunner` 改为可注入 `ICodingAgentRunner` 工厂；这一轮继续把 `title`、`metadata`、`attachments` 真正接进运行时上下文和 outbox 契约，并让本地存在的 attachment staging 到 `workingDirectory/attachments/`，通过 `attachments/attachments.jsonl` 和 `log.jsonl` 保留 `original/local` 元数据；runner 输入现在还会注入 `<mom_runtime_context>`，明确本地 workspace/channel layout、events 文件格式、attachment manifest、memory/log/status 路径和 `[SILENT]` 事件响应约定；`ChannelWorkspaceLayout` 会创建 `scratch/`、workspace/channel `skills/`、`attachments/` 和 `events/`，并把 `SYSTEM.md` 与 skill docs inventory 注入 prompt context；`workingDirectory/context.json` 会恢复/保存 Tau-native channel session snapshot，让同一 workdir 的 delegation 继承上一轮 runtime messages；调用 runner 前会写 `workingDirectory/last_prompt.jsonl`，记录 mom runtime context、delegation context、实际 runner input、恢复的 session messages、当前 prompt 和 attachment/image attachment count；`workingDirectory/MEMORY.md`、父目录 `MEMORY.md` 与 `workingDirectory/log.jsonl` 的最近非 bot channel history 会进入 prompt context；本地 file delegation 现在也会把用户请求和 bot 结果追加到同一个 `log.jsonl`，把当前或最近一次运行状态写到 `workingDirectory/status.json`，并基于未过期 `running` 状态跳过同一 workdir 的新请求；新增 `MomEventProcessor` 会把本地 `events/*.json` 的 `immediate/one-shot/periodic` 唤醒转换成 inbox 委派请求，复用相同 channel/workspace seam；新增 `MomChannelMessage` / `MomChannelAttachment` 作为 Slack-compatible envelope，file requests 和 local events 已经先统一映射 channel/user/ts/thread/attachments/provider/model/title/metadata，再生成 `DelegationRequest`；新增 `IMomChannelTransport` / `IMomChannelResponder` / `MomChannelMessageProcessor`，先固定 channel transport/responder contract、busy-state、stop placeholder、typing、thread response、attachment staging、status/log writeback 与 runner 调用。仍未接 Slack SDK、真实 Slack queue、skill runtime loader 和完整 sandbox。 | 下一步接 Slack Socket Mode/Web API adapter、backfill/queue/cancellable stop/file download、workspace/sandbox/tool delegation 与更高层 delegation flow。 |
| Tau.Pods | C | 已从控制台占位提升为具备主动运维入口的 CLI：config init/list/validate/status、`probe`、`exec`、sample config、probe/exec service 和 source-gen JSON 已具备，并有 7 个测试通过。仍缺 deploy、restart、lifecycle 和更高层 pod orchestration。 | 继续进入 deploy / stop / restart / health 与更高层 lifecycle。 |
| 测试 | C | `Tau.Ai.Tests`、`Tau.CodingAgent.Tests`、`Tau.Agent.Tests`、`Tau.Pods.Tests` 当前都能真实通过；`Tau.Agent.Tests` 已扩到 20 个 Mom 结构化委派测试，覆盖 request/outbox/log/status、attachment staging、busy-state guard、runner context、`<mom_runtime_context>` 注入、`context.json` channel session restore/save、`last_prompt.jsonl` prompt debug snapshot、workspace layout bootstrap、`SYSTEM.md` 注入和 skill docs inventory、Slack-compatible channel message envelope、channel processor 的 busy/stop/typing/thread response/status/log 行为，以及 immediate/one-shot/periodic/invalid events 转 inbox 行为；`Tau.CodingAgent.Tests` 已扩到 54 个测试，覆盖 CLI host 渲染/错误处理、runner rehydrate/model switch/reset/restore/compact、WebChatStore、CodingAgent session/settings store、slash command router/catalog、clipboard seam、session snapshot export/import、`LoadStrict()`、`/new`、`/model`、`/auth`、`/login`、`/compact`、`/session`、`/quit`、`/help`、`/name`、`/copy`、`/export`、`/import`；`Tau.Ai.Tests` 已扩展到 79 个测试，覆盖 OpenAI chat-completions serialization、OpenAI-compatible compat/routing payload、custom models.json merge/override/request auth、ProviderAuthResolver status、Responses SSE/service-tier cost、GitHub Copilot Responses static/dynamic headers 与 tool-result image body、Codex headers/retry/WebSocket/auto fallback/session reuse/service-tier fallback、generated model catalog merge 与新增模型查询、Azure Responses URL/header/body/reasoning/deployment map/error、Mistral payload/stream、Bedrock bearer/SigV4/shared profile/eventstream、Vertex API key/ADC token exchange、Gemini CLI/Antigravity headers/fallback/retry/empty-stream 和 auth/registry 行为。 | 继续把高价值 runtime smoke 自动化，尤其是 provider e2e、WebUi、Mom、Pods 的端到端行为。 |
| CI/CD | C | 主 CI 已改为 `scripts/verify-dotnet.sh`，按显式项目顺序 restore / build / test，形成了真实可重复的项目级门禁；现在也已经覆盖到 `WebUi / Mom / Pods` 三个应用面。`Tau.CodingAgent`、`Tau.WebUi`、`Tau.Mom`、`Tau.CodingAgent.Tests` 和 `Tau.Agent.Tests` 已不再依赖预置 DLL `HintPath`，降低了 stale assembly 和并行构建顺序风险；`dotnet build Tau.slnx --verbosity minimal` 当前也已通过。当前本机 `bash scripts/verify-dotnet.sh --skip-restore` 仍因 WSL 缺少 `/bin/bash` 失败，属于本机 shell 环境问题，验证可退回等价 dotnet 顺序命令。release 产物仍不是 Tau 的真实可执行交付件。 | 让 release 链开始对应真实 Tau 构建产物，并继续把运行态 smoke 纳入验证脚本或 PowerShell 兜底脚本。 |
| 可观测性 | D | 当前主要依赖控制台输出、Web API 返回、Mom outbox JSON 和 Pods probe/exec 输出，没有统一日志/trace 约定。虽然已经能看见 session store、delegation output、probe result、exec summary，但还没有系统化可观测协议。 | 先为 provider 调用、tool execution、session / delegation / pod operation 阶段补最小日志。 |
| 安全与配置 | C | 当前已经补了环境变量矩阵、auth.json、OAuth 凭证读取骨架和 Vertex ADC token exchange，配置边界比之前清楚；WebUi 已具备会话级 provider/model 配置，Mom 已具备结构化 provider/model/workdir 请求，Pods 已具备 probe/exec 配置。但 secrets 存储策略、OAuth login UX、token migration 和多运行态配置仍未完整定义。 | 在继续推进 `Tau.CodingAgent` / `WebUi` / `Mom` / `Pods` 的设置层时，把 auth/config 的持久化与迁移规则一起落仓库。 |
