# 质量评分

用这份文档按产品区域和架构层次记录当前质量水位，方便持续知道最薄弱的地方在哪。

## 评分标准

- `A`：覆盖完整、行为稳定、文档清楚、运行风险低。
- `B`：整体可接受，但还有明确短板。
- `C`：能用，但需要针对性补强。
- `D`：脆弱、缺少规范，或很多行为尚未定义。

## 最新增量（2026-05-26）

- `Tau.WebUi` 生产默认 message runner 已把同一个 `ITauLogSink` 透传到 `WebUiRunnerFactory.Create(...) -> RuntimeCodingAgentRunner.Create(..., logSink)`；`/api/sessions/{id}/messages/stream` 不再只覆盖 fake-runner endpoint 行为，而是能在默认 runner 路径写出 `agent/run.*` runtime events，并为后续真实 tool call 复用 `AgentLoopConfig.LogSink -> tool/execution.*` 打开路径。
- 本轮继续补 `TauRuntimeLogContext` cross-module correlation baseline：`RuntimeCodingAgentRunner` 每次 run 生成/继承 `correlationId`，并把 `sessionId` / `messageId` 透传到 `agent/run.*`；`AgentLoopConfig.LogContext -> ToolExecutor` 让 `tool/execution.*` 继承同一组字段；WebUi message stream 每次发送用 Web session id + transient message id 注入默认 runner。当前不改变 JSONL 顶层格式，也不持久化 WebChat message id。
- 本轮继续把同一 runtime log context 扩到 `Tau.CodingAgent` CLI/RPC 宿主层：`ICodingAgentRunner.RunAsync(...)` 增加 per-run `TauRuntimeLogContext` 入口，CLI 普通 turn 生成稳定 turn correlation/message id 并在 retry attempts 间复用，RPC prompt 用 command id 作为 `correlationId/messageId`，同时从 JSONL tree summary 注入 `sessionId`。当前不提前生成 JSONL message entry id，不改变 retry/rollback 持久化时序。
- 本轮继续把同一 context 接到 `Tau.Mom` delegation path：`RuntimeDelegationAgentRunner` 从 `DelegationRequest.Metadata` 的 `requestId` / Slack `ts` 生成 `correlationId/messageId`，从 `channel:threadTs|ts` 生成 `sessionId`，并把这些字段写入 Mom `delegation/response/tool/usage` runtime events，同时传给内部 CodingAgent runner，使 Mom delegation 与 `agent/run.*` / `tool/execution.*` 可关联。当前仍不把本地工作目录当作 session id，也不改变 `status.json` / `log.jsonl` / outbox 合同。
- 本轮补 `Tau.Pods` setup plan/run baseline：`PodDefinition.VllmVersion` 记录 `release/nightly/gpt-oss`，`PodDefinition.Gpus` 持久化 setup detect 结果，`PodSetupPlanner` 生成 `/tmp/pod_setup.sh` setup command，CLI 暴露 `setup plan [--json] [--mount <command>] [--models-path <path>] [--vllm release|nightly|gpt-oss] [path] <id>`；`PodSetupService` 与 `setup run [--json] [--script <path>] ... [path] <id>` 固定 SSH test、SCP script、远端 setup 执行和 `nvidia-smi` GPU detect 链路。`setup run` 成功后会写回当前 pod 的 `modelsPath`、`vllmVersion` 和 detected `gpus`，失败不写 config；JSON/text 输出显式暴露 `configUpdated` 与 `configPath`，让调用方不用重新 diff 配置文件判断是否持久化。输出和 config 都不保存真实 token，真实 `HF_TOKEN` / `PI_API_KEY` 通过 stdin 传给远端 shell，display command / JSON / text output 只保留 placeholder 或 redacted stdout/stderr。当前只做 fake process runner focused coverage，不宣称真实 SSH/HF/GPU pod smoke。
- 本轮继续补 `Tau.Pods` registration / active config baseline：`PodsConfig` 持久化上游兼容 `active` 字段，`list/status --json` 暴露 `activePodId` 与 per-pod `active`，CLI 新增 `active/remove`，并新增本地 `setup <id> "ssh [-p port] <host>"` 注册入口。注册只写本地 config，不执行 SSH/SCP/setup run；复杂 SSH options 暂显式拒绝，以免扩散到当前只支持 `SshHost/SshPort` 的远端执行链。
- 本轮继续把 `Tau.Pods` active pod 语义接到 model 命令消费面：`model list [--json] [path]` 默认使用 config `active`，`model pull/remove/status [--json] [path] <model-id>` 的单尾随参数按 model id 解释并走 active pod；旧显式 `<pod-id> <model-id>` 用法保留。active 缺失时返回 `No active pod. Use 'active <pod-id>' to set one.`。
- 本轮继续把 `Tau.Pods` active pod 语义接到 deployments 与 vLLM 控制类消费面：`deployments [--json] [path] [pod-id]` 以及 `vllm status/health/stop [--json] [path] [pod-id] <deployment-name>` 现在都可在缺省 pod-id 时使用 config `active`，但仍保留显式 pod-id override。
- 本轮继续把 `Tau.Pods` active pod 语义接到 serve 类 vLLM 命令：`vllm plan/preflight/deploy [--config path] [--pod id] <model-id> [--name deployment-name]` 会在省略 `--pod` 时使用 config `active`，旧 `[path] <pod-id> <model-id> [deployment-name]` positional 用法仍兼容；deployment name 在 active 默认路径上通过 `--name` 显式传入，避免 Hugging Face `org/model` 和旧 path heuristic / pod-id slot 形成歧义。
- 本轮继续把 `Tau.Pods` 的显式 option 入口再扩一层：`model list/pull/remove/status`、`deployments` 以及 `vllm status/health/stop` 现在也支持 `--config path` / `--pod id`，同时保留旧 positional 形式；这样 model 名、deployment 名和 config path 都能用 option 显式消歧，不再只依赖 path heuristic。
- 本轮继续把 `Tau.Pods` 的 vLLM snapshot 选择再收口一层：`vllm plan/preflight/deploy` 现在支持 `--revision` / `--snapshot`，preflight 会优先解析 `refs/<revision>` 或 `snapshots/<revision>`，并把 `requestedRevision` / `revision` 暴露到 plan 和 preflight 输出里。随后又补 `model pull --revision` / `--snapshot`，两条 Hugging Face 下载路径都会带同一 revision；`vllm deploy --prefetch` 会在模型 cache/snapshot/revision 缺口时先复用 `model pull`，再二次 preflight 和 deploy；当前仍缺真实远端 revision/HF download smoke。
- 本轮 targeted 验证继续新增通过：`dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`，`PullAsync_|ModelPull_|VllmPlan_MissingModel_PrintsUsage` focused tests 7/7，以及 Pods vLLM prefetch/revision focused tests 7/7。
- 本轮 targeted 验证新增通过：`dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`，以及 `VllmPlan_WithConfigPodAndNameOptions|VllmPlan_WithoutPodOption|VllmPreflight_WithoutPodOption|VllmDeploy_WithoutPodOption|VllmPlan_WithJsonOption|VllmPlan_WithVllmExtraArgs|VllmDeploy_WithVllmExtraArgs|VllmPreflight_WithJsonOutput|VllmDeploy_WithJsonOutput` focused tests 10/10。
- 本轮 targeted 验证继续新增通过：`Deployments_WithConfigPodOptions|ModelList_WithConfigPodOptions|ModelStatus_WithConfigPodOptions|VllmStatus_WithConfigPodOptions|Deployments_WithoutPodId|ModelList_WithoutPodId|ModelStatus_WithoutPodId|VllmStatusHealthStop_WithoutPodId|ModelList_WithJsonOutput|VllmStatus_WithJsonOutput` focused tests 12/12，以及 `dotnet build src\\Tau.Pods\\Tau.Pods.csproj --no-restore --verbosity minimal`。
- 本轮 targeted 验证通过：`AgentRuntimeToolTraceTests` 2/2、`RuntimeCodingAgentRunnerTests.RunAsync_EmitsRun*` 2/2、`RuntimeCodingAgentRunnerTests.RunAsync_PerRunLogContextOverridesConfiguredContext` 1/1、`CodingAgentRpcHostTests.RunAsync_PromptPassesCommandAndTreeSessionLogContextToRunner` 1/1、`CodingAgentHostTests.RunAsync_RetryableTurnPassesStableTreeSessionLogContextToRunner` 1/1、`RuntimeDelegationAgentRunnerTests.ExecuteAsync_AggregatesUsage_StopReason_ToolEventsWithDuration` 1/1、`WebUiEndpointTests.MessageStreamEndpoint_DefaultRunnerLogsAgentRunEvents` 1/1、Pods setup plan targeted tests `PodSetup|SetupPlan` 5/5、Pods setup run focused tests `PodSetup|SetupRun` 7/7（含 config writeback/token-not-persisted 断言），追加 `FullyQualifiedName~SetupRun` 2/2（含 `configUpdated/configPath` 和 SSH failure no-writeback 断言），追加 Pods model active fallback focused tests 7/7、Pods deployments/vllm active fallback focused tests 7/7，并通过 `dotnet build src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-restore --verbosity minimal`、`dotnet build src\Tau.Mom\Tau.Mom.csproj --no-restore --verbosity minimal` 与 `dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`。验证过程中曾因并行 build 触发 Windows/Roslyn `Tau.Ai.dll` 文件锁，已 `dotnet build-server shutdown` 后改回串行验证；没有重跑全仓 gate。

## 最新增量（2026-05-24）

- 本轮按 `GOAL.md` 继续完整移植链路，收口多个互不重叠 baseline：`Tau.Pods` vLLM SSH command/service orchestration + health retry/backoff、`Tau.Mom` delegation runtime response/tool/usage log events 与 Slack runtime response update/delete seam、`Tau.WebUi` CodingAgent JSONL preview tree metadata/filter/audit、`Tau.Tui` transcript viewport host + Home scroll-top、`Tau.Ai` Bedrock SSO cache token refresh、`Tau.CodingAgent` HTML nested Markdown list structure。
- `Tau.Pods` 已新增 `vllm deploy/status/health/stop [--json]`：deploy 会通过 SSH 写远端 service/metadata，优先 `systemctl --user enable --now`，systemd user command 不可用或失败时 fallback 到 `nohup` + `.pid` / `.log`；deploy 默认按 12 次、5 秒 backoff 做远端 `/health` readiness 窗口，`starting/unknown/not-ready` 会继续等待，`unhealthy/dead/not-found`、SSH 启动失败、runner failure 和 cancelled 会归类为终止失败；部署命令失败或 health 最终非 ready 时尝试 rollback cleanup；`vllm health` 默认单次探测，也可用 `--health-attempts` / `--health-backoff-ms` 调整；status 拉取 systemd/fallback pid/metadata 文本；health 返回 `ready/unhealthy/dead/starting/unknown`、`failureKind` 和 attempts；stop disable/remove unit 或 kill pid 并清理文件。该能力仍只是 SSH command/service baseline，成功不代表真实 GPU/HF/vLLM 环境长期健康，metadata 仍是 `planned-vllm`，rollback 是 cleanup，不是多版本 rollout 状态机。
- `Tau.Mom` 已把 `RuntimeDelegationAgentRunner` 接入生产 `ITauLogSink`，并记录 `delegation.start` / `response.start` / `tool.start` / `tool.end` / `response.end` / `usage` / `delegation.end`；字段包含 provider/model/workingDirectory、tool id/name、duration、isError、response preview/characters、usage tokens/service tier/cost。取消路径会写 `delegation.end stopReason=cancelled` 后 rethrow。当前仍是本地文本摘要级 observability baseline，不是完整 trace/correlation 协议。
- `Tau.Mom` 已补 Slack runtime response update/delete seam：`MomChannelMessageProcessor` 在 responder 支持 runtime response 时先创建 `_Thinking_ ...` 占位消息，完成后用 `chat.update` 更新同一条消息；runner 返回 `[SILENT]` 时通过 `chat.delete` 删除占位且不再追加最终回复。该能力固定 Slack 消息生命周期合同，但仍未做真实 Slack smoke 或真实 Slack session sync。
- `Tau.WebUi` CodingAgent JSONL preview 现在返回 tree metadata 和 filter metadata：leaf/root/branch/message/label counts、entry type histogram、current branch ids 和 per-entry id/type/parent/depth/child/current-branch/current-leaf/label metadata；entry id、parent id 和 label target resolution 按 case-insensitive tree-store 语义处理；preview endpoint 支持 `search` 与 `currentBranchOnly` 只过滤返回的 preview messages，conservative import 响应会带回 source tree/source audit/warnings。preview tree 不被 conservative import 持久化，import 仍按 timeline-derived messages 生成 WebChat DTO，不保留 branch tree。
- `Tau.Tui` 已新增 `TuiTranscriptViewportHost` 和 `TuiTranscriptSession`，前者把 `TuiTranscriptViewport`、`ITuiRenderSurface` 和 `TuiDiffRenderer` 包成可复用 render/apply wrapper，后者提供 start/stop、auto-render mutation、PageUp/PageDown/Home/End 滚动输入和可注入 key reader seam；测试固定首帧/增量 diff、scroll stable viewport、status row diff、resize full redraw、force/reset frame、apply 失败不推进 previous frame、Home scroll-top、session mutation/input/stop/read-key 行为。当前仍不是完整 terminal host、overlay compositor、hardware cursor 或 CodingAgent 主屏。
- `Tau.Ai` 已补 Bedrock SSO cache token refresh baseline：access token 过期且 AWS CLI cache 中已有 `clientId` / `clientSecret` / `refreshToken` / 未过期 `registrationExpiresAt` 时，会调用 SSO OIDC `CreateToken`，用刷新后的 access token 调 SSO Portal，并 best-effort 写回 cache；缺 refresh metadata、client registration 过期或 OIDC 失败时返回明确 `aws sso login --profile <profile>` 诊断。当前仍缺 OIDC `RegisterClient`、cache 并发边界和真实 AWS 云端 e2e。
- `Tau.CodingAgent` 已修正 HTML transcript 嵌套 Markdown list 结构：缩进升级时子 `<ul>/<ol>` 会留在父 `<li>` 内，indent 回退时按 open list item stack 关闭，避免 nested list 被导出成父项外的兄弟列表。当前仍是 Tau 轻量 Markdown subset，不是完整 Markdown/highlight renderer。
- 本轮 targeted 验证通过：`Tau.Pods.Tests` 89、`Tau.Tui.Tests` 144、`Tau.Agent.Tests` 81、`Tau.WebUi.Tests` 41、`Tau.Ai.Tests` 220、`Tau.CodingAgent.Tests` 347。当前工作树项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 与 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke` 均已通过，smoke 覆盖 WebUi 和 Mom `--once`，Mom smoke 处理 1 个 due event 并写出 `delegation.start/end` runtime log。

## 最新增量（2026-05-23）

- 本轮继续沿 JSONL 脱敏收口 WebUi 面：`GET /api/sessions/{id}/export.jsonl`、`POST /api/sessions/import.jsonl`、CodingAgent JSONL preview 与 conservative import 现在默认复用 `JsonlSecretRedactor`，递归只处理 JSON string value，保留 field key、number、bool 和 null；`TAU_WEBUI_REDACT_SECRETS=0` 可关闭。
- `Tau.WebUi.Tests` 新增 3 个 redaction 回归，覆盖 WebUi-local JSONL export、WebUi-local JSONL import、CodingAgent JSONL preview/import 的 string-value redaction 与 disabled redactor opt-out；当前 targeted WebUi 测试数提升到 32。
- 本轮在上一提交 `c77e5ad` 之后继续做并行收口，但新开的多名 worker 被网关 `403 Forbidden` 阻断；最终只把已实际落地且通过定向测试的三条切片纳入本次提交：Ai runtime log JSONL 脱敏统一、CodingAgent JSONL tree session 写出脱敏、Pods `vllm plan --json`。没有把 Tui/WebUi/Mom 新能力写成已完成。
- `Tau.Ai` 已把 `JsonlTauLogSink` 从逐字段手工 redaction 改为完整 JSONL 行写出后复用 `JsonlSecretRedactor`：默认只递归脱敏 string value，覆盖 category、event 和 field value，保留 field key、number、bool 和 null；`TAU_LOG_REDACT_SECRETS=0` 继续作为 opt-out。
- `Tau.CodingAgent` 已把 JSONL tree session 写出接入 `JsonlSecretRedactor`：`CodingAgentTreeSessionStore` 写 session header、append entry、rewrite/export current branch 时默认按 `TAU_CODING_AGENT_REDACT_SECRETS` 脱敏 string value，object key/number/bool/null 保留，`0/false` 可关闭。该能力会改变持久化内容，不是可恢复加密。
- `Tau.Pods` 已给 `vllm plan` 增加 `--json` 输出：默认文本 plan 保持，`--json` 输出 pod/deployment/model/modelPath/port/servedModel/unit/serveCommand/systemdUnit/metadata/metadataJson/remoteCommand，仍不执行 SSH、不调用 `systemctl`、不写远端状态。
- 当前新增定向验证已串行通过：`Tau.Ai.Tests` 205、`Tau.CodingAgent.Tests` 346、`Tau.Pods.Tests` 66；全仓 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1` 在恢复缺失 NuGet 缓存后通过。
- 本轮“继续继续继续”在既有 6-agent 并行基础上继续收口；Tui/Mom worker 返回成功，WebUi/Pods 新 worker 成功，Ai/CodingAgent 由主控本地补齐。主控继续保持 docs/history/验证/commit 串行收口，避免 Windows/Roslyn 文件锁。
- `Tau.Ai` 已补 `JsonlSecretRedactor` 通用 JSONL 行级脱敏 foundation：递归只处理 string value，保留 object key 和 number/bool/null，invalid JSON fallback 到整行 redaction，disabled redactor 原样返回。该能力是基础 helper，不自动覆盖全部 JSONL 文件。
- `Tau.CodingAgent` 已把 `/tree --interactive` fold state 从 settings fallback 推进到 append-only `tree_state` session metadata baseline：navigator 返回 folded snapshot 后写入当前 JSONL session，启动时优先恢复 session-scoped collapsed ids；settings `treeCollapsedEntryIds` 仍保留兼容 fallback。
- `Tau.Tui` 已补 `TuiTranscriptViewport` 组合 viewport foundation，复用 `TuiMessageArea` / `TuiStatusBar` / `TuiScrollbackBuffer`，固定最后一行 status，支持 append/set/clear、滚动、resize rewrap 和 bottom-follow；当前未接 terminal host/overlay/hardware cursor。
- `Tau.WebUi` 已从 CodingAgent JSONL 只读 preview 推进到 conservative import baseline：`POST /api/sessions/import.coding-agent-jsonl` 复用 preview parser，把 timeline 转成 WebChat DTO 并持久化；tool call/result/thinking/image 只落可审计文本标记，不保留完整 branch/tree 结构。
- `Tau.Mom` 已补 `log.jsonl` / `last_prompt.jsonl` 默认 secret redaction baseline：写 log、读 history 注入 prompt 和写 prompt debug 时默认复用 `JsonlSecretRedactor` / `TauSecretRedactor`，`TAU_MOM_REDACT_SECRETS=0` 可关闭。
- `Tau.Pods` 已把 `PodVllmCommandPlanner` 接到 `vllm plan [path] <id> <model> [name]` CLI 预览入口，输出 systemd/metadata/remote command plan，不执行 SSH、不调用 `systemctl`、不写远端状态。
- 上一轮定向验证已串行通过：`Tau.Ai.Tests` 204、`Tau.CodingAgent.Tests` 342、`Tau.Tui.Tests` 110、`Tau.Agent.Tests` 76、`Tau.Pods.Tests` 65、`Tau.WebUi.Tests` 29；本轮后续全仓验证已重新覆盖最新测试基线。
- 6-agent 并行轮次已从干净基线 `f80153d` 同时推进 `Tau.Ai`、`Tau.CodingAgent`、`Tau.Tui`、`Tau.WebUi`、`Tau.Mom` 和 `Tau.Pods` 六个互斥切片；主控统一完成串行验证，避免并行 build/test 的 Roslyn 文件锁风险。
- `Tau.Ai` 已补 models.json dynamic OpenAI-compatible provider registration：未预注册 provider 只有在 `apiKind` / `apiType` 显式标记为 `openai-compatible`、`openai-completions` 或 `openai-chat-completions` 时才运行时注册；未知或拼错 api 不静默兜底。`EnvironmentVariableScope` 测试 helper 改用 `SemaphoreSlim`，避免 async test 跨线程 dispose 触发 `SynchronizationLockException`。
- `Tau.CodingAgent` 已补 `/metadata [entry-id]` JSONL session/entry inspector baseline，可查看 session header、cwd/parent/leaf、entry/message/branch/label 统计、最近 metadata entries，或指定 entry 的 id/type/parent/timestamp/path/depth/children/label/message preview 与 compaction/branch-summary/retry 等关键字段。
- `Tau.Tui` 已补 `TuiMessageArea` 和 `TuiStatusBar` 库层 foundation，覆盖 role prefix、wrap、bottom-anchored transcript、left/right status segment、右侧状态保留和窄宽截断；当前仍未接完整 terminal host/viewport。
- `Tau.WebUi` 已补 JSONL import problem-details contract 和前端 `.jsonl` import 分流：unsupported content type 返回 `415 application/problem+json`，parser error 返回 `400 application/problem+json` 且包含稳定 `code` 和可选 `line`。
- `Tau.Mom` 已补 `MomLocalDelegationFlow`，让 `Worker` 后台轮询和 `Program --once` 复用同一条 event -> inbox -> runner -> outbox/status/log/archive 本地 flow。
- `Tau.Pods` 已补 model lifecycle CLI baseline：`model list/pull/remove/status`、pod-level `modelsPath`、Hugging Face CLI/python fallback 和 SSH keepalive；当前仍未做真实 SSH/HF/vLLM smoke。
- 上一轮 6-agent 串行验证当时通过：`Tau.Ai.Tests` 196、`Tau.Agent.Tests` 68、`Tau.Tui.Tests` 92、`Tau.CodingAgent.Tests` 336、`Tau.Pods.Tests` 55；额外 `Tau.WebUi.Tests` 17/17 和 Mom host sandbox validation smoke 也通过。本轮最新 targeted counts 见本节顶部。
- `Tau.WebUi` 已补 WebUi-local JSONL import baseline：新增 `WebChatJsonlImporter` 和 `POST /api/sessions/import.jsonl`，对 `type=session` header、`type=message` entries、版本、必填字段、重复 id 和线性 `parentId` 做校验，并覆盖 JSONL export -> import roundtrip；当前仍不是 CodingAgent JSONL branch/session-tree 语义对齐。
- `Tau.Mom` 已补 `--validate-sandbox` 显式 validation 入口：`MomCommandLine` 会剥离 `--once` / `--validate-sandbox` 并把其他 host args 透传给配置系统，`MomSandboxValidator` 复用 host/Docker validate seam；host sandbox smoke 可本地直接通过，真实 Docker container smoke 仍缺。
- `Tau.Pods` 已补 ssh process failure/cancellation hardening：本地 `ssh` 启动失败、injected runner 异常、`Process.Start` 返回 null 和 cancellation 都会返回结构化 `PodExecResult` failure，默认进程取消时 best-effort kill，lifecycle summary 继续透传底层 failure。
- `Tau.CodingAgent` 已补 thinking 模型能力 clamp baseline：新增 `CodingAgentThinkingLevels` 共享 helper，统一 `/thinking`、selector、settings selector、启动恢复、显式 `/model` / `/provider` / Ctrl+L、Ctrl+P/Ctrl+Shift+P、RPC `set_model` / `cycle_model` / `set_thinking_level` / `cycle_thinking_level` / `update_settings` 的 effective thinking 规则；非 reasoning 模型归一 off，不支持 xhigh 的 reasoning 模型把 xhigh 归一 high，支持 xhigh 的模型保留 xhigh。
- `Tau.CodingAgent` 已补 scoped model thinking-level per-entry baseline：settings `enabledModels` 字符串条目支持 `provider/model:off|minimal|low|medium|high|xhigh`；`/scoped-models` 命令可解析、展示和保存 suffix，TUI multi-select selector 保存时保留既有 suffix，Ctrl+P/Ctrl+Shift+P 与 RPC `cycle_model` 切到带 suffix 的 scoped model 时会同步 runner/default thinking，并按目标模型能力 clamp。当前仍不是完整上游 per-entry thinking 编辑 UI。
- `Tau.CodingAgent` 已补 model selector search/chrome baseline：`CodingAgentModelSelectorComponent` 在 `/model select`、交互式裸 `/model` 和 Ctrl+L 的模型选择器顶部渲染 `Model Selector` / `Search:` 轻量 chrome，普通字符输入会交互式更新过滤，Backspace 会回退过滤；搜索、scope toggle、selected model detail 和 auth filtering footer 现在在同一个 focused component 中闭环。当前仍不是完整上游 theme/dynamic-border/terminal-host parity；scoped model thinking-level per-entry baseline 已在后续同日切片补齐。
- `Tau.CodingAgent` 已补 model selector scope/detail baseline：`CodingAgentModelSelectorComponent` 在 `/model select`、交互式裸 `/model` 和 Ctrl+L 的模型选择器中支持 Tab 在 `scoped` / `all` auth-configured 候选之间切换；有 scoped 候选时默认显示 scoped scope，切到 all scope 后仍优先定位当前模型，并在列表下方显示 `Model Name: ...`。search/chrome 和 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。
- `Tau.CodingAgent` 已补 model selector footer hint baseline：`TuiSelectListLayout` 新增通用 `FooterHint`，单选列表会在普通列表、滚动提示之后以及无匹配结果时渲染 footer；`CodingAgentModelSelector` 在模型 selector 底部显示 `Only showing models with configured auth`，对齐上游“只展示已配置凭证模型”的提示意图。scope toggle、selected model detail、search/chrome、scoped model thinking-level per-entry 和模型能力 clamp baseline 已在同日补齐；完整上游 theme/dynamic-border/terminal-host parity 与 per-entry thinking 编辑 UI 仍后续补齐。
- `Tau.CodingAgent` 已补 model selector / model cycle / RPC model switching 的 provider auth filtering baseline：新增 `CodingAgentModelAvailability` 共享 helper，CLI router 和 RPC host 统一按 provider auth status 过滤实际可用模型；`/model select`、交互式裸 `/model`、Ctrl+L、Ctrl+P / Ctrl+Shift+P、显式 `/model`、`/provider`、RPC `get_available_models` / `set_model` / `cycle_model` / `update_settings.settings.model` 都只展示、循环或接受已配置凭证的 provider/model。`/scoped-models` 仍基于全部注册模型维护 settings `enabledModels`，作为配置入口允许预先加入尚未登录的 provider。search/chrome、scoped model thinking-level per-entry 和模型能力 clamp baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity 或 per-entry thinking 编辑 UI parity。
- `Tau.CodingAgent` 已补 `/model select` / Ctrl+L model selector baseline：新增 `CodingAgentModelSelector`，真实交互式 editor 会话中裸 `/model` 或 `/model select [search]` 会复用当前 TUI selector foundation 打开模型单选列表，优先使用 settings `enabledModels` scope 中已配置凭证的模型，scope 缺失时展示全部已配置凭证模型；`select [search]` 支持初始过滤，选择后更新 runner model、保存 default provider/model，并同步 tree session。无 selector 的裸 `/model` 保留 current status，Ctrl+L 触发同一 selector 并保留当前输入 draft。search/chrome 和 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。
- `Tau.CodingAgent` 已补 Ctrl+P / Ctrl+Shift+P model cycling interactive baseline：`Tau.Tui` 默认 keybinding 新增 `CycleModelForward` / `CycleModelBackward`，`InteractiveInputEditor` 可把 app action 返回给 `CodingAgentHost` 并保留当前 draft；host 调用 `CodingAgentCommandRouter.CycleModel()`，按 settings `enabledModels` 有序 scope 或全部可用模型切换到下一个/上一个模型，保存默认 provider/model，并在候选不足两个时显示 `Only one model in scope` / `Only one model available`。search/chrome 和 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。
- `Tau.CodingAgent` 已补 `/thinking select` TUI selector baseline：新增 `CodingAgentThinkingSelector`，真实交互式 editor 会话中 `/thinking select` 会复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示当前模型可用的 reasoning level 和说明文案，选择后立即更新 runner `ThinkingLevel` 并写回 settings `defaultThinkingLevel`；裸 `/thinking` 保留 current status 语义，取消或无 selector 不修改 settings。当前仍不是完整上游全 settings UI parity。
- `Tau.CodingAgent` 已补 `/logout` OAuth provider selector baseline：真实交互式 editor 会话中裸 `/logout` 或 `/logout select` 会复用 `CodingAgentAuthSelector`，只列出当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后删除对应 `auth.json` credential entry；无 selector 的裸 `/logout` 继续使用当前 provider，显式 `/logout <provider>` 保持兼容。环境变量和 `models.json` credential 配置不会被修改，当前仍不是完整上游 OAuth login dialog/session parity、credential refresh UX 或真实外部 OAuth e2e。
- `Tau.CodingAgent` 已补 `/login` OAuth provider selector baseline：真实交互式 editor 会话中裸 `/login` 或 `/login select` 会复用 `CodingAgentAuthSelector` 列出当前注册且有 OAuth provider 的 provider，选择后调用现有 OAuth login flow 并保存到 `auth.json`；无 selector 的裸 `/login` 继续使用当前 provider，显式 `/login <provider>` 保持兼容。当前仍不是完整上游 OAuth login dialog/session parity。
- `Tau.CodingAgent` 已补 `/auth select` TUI provider status selector baseline：新增 `CodingAgentAuthSelector`，`/auth [current|select|provider]` 可查看当前 provider、交互式选择 provider 或显式查询 provider 的 auth 状态；真实交互式 editor 会话中 `/auth select` 复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示 configured/missing、source、OAuth/login capability，选择后只输出所选 provider status。当前 selector 只检查状态，不写 `auth.json`、不执行 OAuth login，也不是完整上游 OAuth login-session parity。
- `Tau.CodingAgent` 已补 `/scoped-models` TUI selector baseline：新增 `CodingAgentScopedModelsSelector`，裸 `/scoped-models` 或 `/scoped-models select` 在真实交互式 editor 会话中打开 `TuiMultiSelectList` / `TuiMultiSelectSession`，支持过滤、模型 toggle、provider toggle、enable all、clear、reorder、Ctrl+S save 和 Esc cancel；无 selector 的会话继续输出当前 scope 摘要，命令式 `set/add/remove/clear/all` 保持兼容，并已支持 `provider/model:thinking` suffix。`/scoped-models` 作为配置入口仍维护全部注册模型 scope；空闲输入 prompt 的 Ctrl+P / Ctrl+Shift+P、Ctrl+L / `/model select` 会在同一 scope 上再按 auth-configured 模型过滤实际候选；model selector search/chrome、scoped per-entry thinking 和模型能力 clamp baseline 已完成，完整上游 theme/dynamic-border/terminal-host parity 与 per-entry thinking 编辑 UI 仍未完成。
- `Tau.Tui` 已补多选 selector foundation：新增 `TuiMultiSelectList` / `TuiMultiSelectSession`，固定 null=all enabled、显式有序选择、filter、provider 分组切换、Alt+Up/Down reorder 和 save/cancel result；当前已被 `/scoped-models` 接线复用，但仍不是完整上游 terminal host、message/status area 或全局 selector framework。
- `Tau.CodingAgent` 已补 `/settings` TUI selector baseline：新增 `CodingAgentSettingsSelector`，裸 `/settings` 在真实交互式 editor 会话中打开 `TuiSelectList` / `TuiSelectorSession`，可选择 auto-compaction、steering/follow-up mode、tree filter、thinking level、scoped models 或 theme 并写回 settings；`/settings current` / `/settings path` 保留摘要和路径查询，非交互会话裸 `/settings` 继续输出摘要。当前仍不是完整上游 SettingsList/submenu、images/terminal/transport/packages 等全量配置面或完整 TUI edit parity。
- `Tau.CodingAgent` 已补 `/theme select` TUI selector baseline：新增 `CodingAgentThemeSelector`，复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 显示 theme list；`CodingAgentCommandRouter` 通过可注入 selector 保存选中的 theme，取消时不改 settings，生产入口只在真实交互式 editor 存在时启用。当前仍不是完整上游 theme rendering、theme file watcher 或 TypeScript extension runtime parity。
- `Tau.Tui` 已补最小 ANSI render surface：新增 `TuiAnsiRenderSurface`，把 `TuiRenderDiff` 翻译为 synchronized output buffer；full redraw 会 clear/home 后写全量行，line diff 会定位行、清行并写 replacement text。当前仍不是完整 viewport/scrollback renderer、overlay compositor、hardware cursor 或 message/status area。
- `Tau.Tui` 已补 selector session / overlay host baseline：新增 `ITuiRenderSurface` render sink 合同、`TuiOverlayHost` 单组件 render/input loop 和 `TuiSelectorSession`，把 `TuiSelectList` 放进可测试的打开、按键、diff apply、选择/取消返回流程。当前仍不是完整上游 terminal host、overlay compositing、ANSI synchronized output、viewport/scrollback、message/status area 或 CodingAgent selector 接线。
- `Tau.Tui` 已补组件/render/selector foundation：新增 `ITuiComponent` / `ITuiInputComponent` 抽象、`TuiContainer` / `TuiBox` / `TuiTextBlock` 组件、`TuiText` 宽度/截断/wrap helper、带 footer hint 的 `TuiSelectList` 单选列表基础层和 `TuiDiffRenderer` 纯函数式差分渲染计划器。当前这是库内可测试 foundation，不等于完整上游 terminal host、overlay compositing、message/status area、ANSI terminal renderer或 theme rendering。
- `Tau.CodingAgent` 已补 `/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline：命令会读取当前 settings `theme`，复用 `CodingAgentThemeStore` 列出 built-in、用户、项目、env/CLI 和 extension-contributed themes；`select` 会在交互式会话中打开 TUI selector 并保存所选 theme，`set` 会校验主题存在后写回 settings，`clear` 清空字段并回到默认 `dark`。完整上游 theme rendering、theme file watcher 和 TypeScript extension runtime 仍未完成。
- `Tau.CodingAgent` 已补 context files baseline：启动时默认先加载用户目录 `~/.tau/AGENTS.md` 或 `~/.tau/CLAUDE.md`，再按 parent-to-cwd 顺序加载项目链上的 `AGENTS.md` 或 `CLAUDE.md`；同一目录优先 `AGENTS.md`。生成 system prompt 会追加 `# Project Context`；`--no-context-files` / `-nc` 可禁用默认加载，`/reload` 会重读 context files，并在 runner 使用生成 system prompt 时刷新 skill/context inventory。完整 theme selector、TUI theme rendering、theme file watcher、TypeScript extension runtime 和 full resource selector 仍未完成。
- `Tau.CodingAgent` 已补 RPC `new_session.parentSession` metadata baseline：`--mode rpc` 的 `new_session` 会读取上游同名可选字段，把它写入 JSONL session header 的 `parentSession`，并继续追加 `session_info action=new`；后续 `/session`、`/tree` 和 HTML transcript export 可复用现有 parent metadata 展示链路。CLI `/new` 默认行为保持不变。
- `Tau.CodingAgent` 已补 RPC `get_settings` / `update_settings` baseline：`--mode rpc` 可读取当前 settings 文件路径和 Tau 已支持 settings 字段，并可批量更新默认模型、tree filter、retry、default thinking、enabledModels、steering/follow-up mode 和 auto-compaction enabled；更新后会同步当前 runner model/thinking/queue mode、retry options 与 auto-compaction 状态。完整上游 settings selector UI、theme / terminal / packages 等全量配置面、streamed bash output 和 extension UI 仍未完成。
- `Tau.CodingAgent` 已补 RPC `bash` / `abort_bash` baseline：`--mode rpc` 可通过可注入 `ICodingAgentShellRunner` 后台执行一次 shell 命令，返回 `output`、`exitCode`、`cancelled`、`truncated` 和可选 `fullOutputPath`；`abort_bash` 只取消当前 shell 命令，并发 `bash` 会被拒绝。完整上游 streamed bash output、extension UI 和更完整终端子系统仍未完成。
- `Tau.CodingAgent` 已补 RPC queue / auto-compaction settings controls baseline：`--mode rpc` 支持 `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction`，`get_state` 返回 runner/settings backed `steeringMode`、`followUpMode` 和 `autoCompactionEnabled`；settings 新增 `steeringMode`、`followUpMode`、`autoCompactionEnabled`，并兼容旧 `queueMode` 读取。完整上游 extension UI、streamed bash output 和 settings selector UI 仍未完成。
- `Tau.CodingAgent` 已补 RPC auto retry baseline：`--mode rpc` 支持 `set_auto_retry` / `abort_retry`，复用现有 retry policy、settings retry 字段和 JSONL `auto_retry_start` / `auto_retry_end` audit；headless prompt 失败后会按同一 rollback 语义 retry，`abort_retry` 可取消 pending retry delay。完整上游 settings selector UI、bash/extension UI 仍未完成。
- `Tau.CodingAgent` 已补 RPC session switch / fork messages baseline：`--mode rpc` 支持 `switch_session` 恢复指定 JSONL tree session，并支持 `get_fork_messages` 返回可供 headless fork selector 使用的 user message `{ entryId, text }` 列表。完整上游 settings selector UI、bash/extension UI 仍未完成。
- `Tau.CodingAgent` 已补 RPC cycle model baseline：`--mode rpc` 支持 `cycle_model`，按 settings `enabledModels` 有序 scope 或全部可用模型切换到下一个模型，遇到 `provider/model:thinking` suffix 时同步 runner/default thinking，保存默认模型，返回 `{ model, thinkingLevel, isScoped }`；候选不足两个时返回显式 `data: null`，active prompt 期间拒绝切换。完整上游 settings selector UI、bash/extension UI 仍未完成。
- `Tau.CodingAgent` 已补 RPC thinking controls baseline：`--mode rpc` 支持 `set_thinking_level` 与 `cycle_thinking_level`，复用现有 runner `ThinkingLevel` 和 settings `defaultThinkingLevel` 语义；active prompt 期间拒绝改 thinking，`cycle` 到 off 时返回显式 `data: null`。完整上游 settings selector UI、bash/extension UI 仍未完成。
- `Tau.CodingAgent` 的 settings 命令已从只读摘要推进到 `/settings [current|path|select]`：交互式 selector 可写回 auto-compaction、steering/follow-up mode、tree filter、thinking level、scoped models 和 theme；`current/path` 仍保留可脚本化查询。完整上游 SettingsList/submenu、images/terminal/transport/packages 等全量配置面仍未完成。
- `Tau.CodingAgent` 已补 `/changelog [count|all]` baseline：命令读取 Tau 本地 release notes 表（默认 `docs/releases/feature-release-notes.md`，可由 `TAU_CODING_AGENT_CHANGELOG_FILE` 覆盖），输出日期、功能域、用户价值和变更摘要。完整上游启动 changelog 渲染、`collapseChangelog` 设置和 install/update telemetry 仍未完成。
- `Tau.CodingAgent` 已补 `/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline：命令查看或维护 settings `enabledModels` 有序模型 scope，并支持 `provider/model:thinking` per-entry suffix；`clear` / `all` 回到 all enabled / no filter；交互式 selector 可多选、过滤、按 provider 批量切换并重排 scope，保存时保留既有 suffix；该配置入口仍基于全部注册模型，实际 Ctrl+P / Ctrl+Shift+P、Ctrl+L 和 `/model select [search]` 会按同一 scope 取已配置凭证模型做循环或单选，并按目标模型能力 clamp effective thinking。model selector search/chrome、per-entry thinking 与模型能力 clamp baseline 已完成，完整上游 theme/dynamic-border/terminal-host parity 和 per-entry thinking 编辑 UI 仍未完成。
- `Tau.CodingAgent` 已补 `/logout [select|provider]` baseline：命令会删除本地 `auth.json` 中对应 provider 的 credential entry；交互式 selector 会筛选当前有本地 OAuth credential 且注册了 OAuth provider 的 provider；无 selector 时未传 provider 仍使用当前 runner provider；环境变量和 `models.json` credential 配置保持不变，不回显 secret。完整上游 OAuth login/session parity 仍未完成。
- `Tau.CodingAgent` 已补 `/reload` baseline：命令会重读 settings 并同步 retry policy / default thinking level / queue mode，重读 JSON extension commands/resources 并刷新 extension-contributed prompt/skill/theme paths，重载 prompts、skills、context files、theme status 和交互式 editor keybindings；runner 使用生成 system prompt 时会刷新 skill/context inventory。完整上游 theme selector、TUI theme rendering、theme file watcher、TypeScript extension runtime 和 full resource selector 仍未完成。
- `Tau.CodingAgent` 已补 `/hotkeys` 当前 editor keybinding listing baseline：命令只读取 host 注入的 `IKeyBindingMap`，会反映自定义 keybindings 和 `EditorAction.None` 禁用默认绑定；print/RPC/redirected 模式没有 editor 时返回 unavailable。完整上游 app/session/tree/extension shortcut registry 仍未完成。
- 上一轮 6-agent 截止时，`Tau.CodingAgent.Tests` 已提升到 336 个测试：覆盖 scoped model pattern 解析、settings suffix round-trip、`/scoped-models` thinking suffix 展示/保存、selector suffix 保留、CLI cycle thinking override、RPC cycle thinking override、thinking 能力 clamp、model selector search/chrome、scope/detail、auth footer hint、auth-configured filtering、model cycle、显式 `/model` / `/provider`、`/metadata` session/entry inspector 和 RPC `get_available_models` / `set_model` / `cycle_model` 回归。`Tau.Tui.Tests` 已提升到 92 个测试，覆盖 `TuiSelectList` footer hint 与 `TuiMessageArea` / `TuiStatusBar` foundation；`Tau.Agent.Tests` 已提升到 68 个测试，覆盖 steering/follow-up `all` 与 `one-at-a-time` drain 语义、Mom sandbox validation 入口和 `MomLocalDelegationFlow`；`Tau.Ai.Tests` 已提升到 196 个测试，覆盖 dynamic OpenAI-compatible provider registration。本轮最新 counts 见本节顶部和测试评分行。

## 当前基线（2026-05-02 起持续增量）

| 区域 | 评分 | 原因 | 下一步 |
| --- | --- | --- | --- |
| 产品定义 | C | 已有 `docs/ARCHITECTURE.md`、`next.md` 和完整移植总 plan `docs/exec-plans/active/2026-05-10-tau-complete-pi-mono-port.md`，并且不再只围绕 CLI 说话，`WebUi / Mom / Pods` 也已经进入真实产品切片。当前问题不再是“有没有模块”，而是每个模块距离上游 parity closure 还有明确缺口。 | 按完整移植总 plan 继续把每个应用面的剩余能力写实：Mom Slack/workspace/sandbox，CodingAgent session tree，Ai OAuth/provider e2e，WebUi 流式/附件，Pods lifecycle。 |
| Tau.Ai | C | 已补上 `ModelCatalog`、内建模型表、provider auth resolver、auth.json 读取、OAuth provider registry，以及 Azure OpenAI Responses / OpenAI Codex / Mistral / Vertex / Gemini CLI / Bedrock 的第一轮接线；当前已把 `openai-responses`、`openai-codex-responses` 与 `azure-openai-responses` 从 `OpenAiCompatibleProvider` 兜底提升为专用 SSE provider，把 Codex Responses 扩展到 WebSocket/auto transport 与 session socket cache，并补齐 Responses/Codex service-tier usage 与 flex/priority cost multiplier、GitHub Copilot Responses 动态 headers / vision 行为与 tool-result image payload，把 `mistral-conversations` 切到专用 Mistral provider，把 Bedrock 从 placeholder 推进到 ConverseStream 专用 provider，把 Vertex ADC 从 placeholder 推进到 service account / authorized user token exchange，并把 Gemini CLI / Antigravity 请求、headers、fallback 与 retry 行为补到更接近上游；这轮又补了 generated model seed/generator/catalog 接缝、typed/default selection、`Model.Compat` 元数据、models.json custom model/provider 配置入口、request auth/header 合并、显式 OpenAI-compatible dynamic provider registration，以及 Bedrock SSO cache token refresh baseline（过期 access token + cache refresh metadata -> OIDC `CreateToken` + best-effort cache writeback）；OpenAI-compatible provider 已能按显式 compat 消费 stream usage、max tokens field、reasoning format/map、z.ai tool stream、tool strict mode、OpenRouter routing 与 Vercel AI Gateway routing，`ModelCatalog` 也能启动时合并 `TAU_MODELS_FILE` / `.tau/models.json` / `~/.tau/models.json`。仍缺真实云端 e2e、SSO OIDC client registration renewal、cache 并发/多 cache 边界、更完整 runtime config UX 与更广义的 dynamic provider API。 | 继续把剩余 provider 从“可发现/可配置/可序列化”推进到“可完整调用”，优先补 provider e2e、SSO registration renewal 边界和 runtime config UX。 |
| Tau.Agent | C | 双层循环、工具执行器、状态与事件骨架已具备，继续稳定承接各应用面；本轮还承担了 Mom 结构化委派测试的宿主复用角色。但高层 Agent API、行为回归和对外 façade 仍不完整。 | 在支撑 WebUi / Mom 的过程中继续补 façade 与行为测试。 |
| Tau.CodingAgent | C | 已具备最小 CLI 宿主、可测试主循环、基础工具链、本地 session 持久化、settings/model selection、auth 状态入口和手动 compaction；运行时默认 model/provider 已接到 `ModelCatalog`，独立 `csproj` 的 build / run 已恢复，并且 `Tau.CodingAgent` / `Tau.WebUi` / `Tau.Mom` / `Tau.CodingAgent.Tests` / `Tau.Agent.Tests` 已从 DLL `HintPath` 收回到正常 `ProjectReference`。显式 `Create(provider, model, history)` 宿主工厂继续作为 WebUi/Mom 共享 runtime 边界；当前继续保留 `CodingAgentSessionStore` flat snapshot JSON（`TAU_CODING_AGENT_SESSION_FILE` 或 `./.tau/coding-agent-session.json`），同时新增 `CodingAgentTreeSessionStore` JSONL tree（默认 `./.tau/coding-agent-session.jsonl`，`TAU_CODING_AGENT_TREE_SESSION_FILE` 或 `.jsonl` 形式的 `TAU_CODING_AGENT_SESSION_FILE` 可覆盖），支持 append-only `session` header、`message` / `model_change` / `session_info` / `label` / `compaction` / `branch_summary` / `auto_retry_start` / `auto_retry_end` / `tree_state` entries、entry `id/parentId/timestamp`、header `cwd/parentSession` summary、label timestamp、runner message diff 同步、当前 branch restore、session-scoped collapsed entry ids、settings `treeFilterMode` 默认过滤、settings `treeCollapsedEntryIds` 兼容 fallback、settings retry fields/default thinking level/steeringMode/followUpMode/autoCompactionEnabled/theme/enabledModels scoped model list、运行中 steering/follow-up CLI baseline、`--mode rpc` JSONL baseline（`prompt` / `steer` / `follow_up` / `abort` / `new_session` / `get_state` / `set_model` / `cycle_model` / `get_available_models` / `set_thinking_level` / `cycle_thinking_level` / `set_auto_retry` / `abort_retry` / `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` / `switch_session` / `get_fork_messages` / `compact` / `fork` / `clone` / `get_session_stats` / `get_messages` / `get_commands` / `export_html` / `get_last_assistant_text` / `set_session_name`）、`/session` 估算 token/context usage、auto-compaction threshold budget 与 retry policy、`/tree` filter/search modes、`/tree --interactive` 移动/选择/取消/overlay search/filter cycle/next-prev match/Left-Right page/Ctrl-Alt branch navigation/Space fold/selected metadata/session fold persistence、`/label`、`/fork`、`/fork --summarize [instructions]`、`/clone`、`/resume`、`.jsonl` export/import、branch summary restore context baseline、手动 `/compact` compaction metadata baseline、token-budget recent-message retention baseline、split-turn prefix summary metadata/text baseline、opt-in auto-compaction threshold baseline、context overflow compact-and-retry baseline、普通回合失败/取消 rollback baseline、host-level retryable error auto-retry baseline、Tau-native retry audit event baseline、Tau-native `/retry` settings command baseline、RPC `set_auto_retry` / `abort_retry` baseline、RPC `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` baseline、RPC `switch_session` / `get_fork_messages` baseline、Tau-native `/thinking [current|select|cycle|off|minimal|low|medium|high|xhigh]` settings/TUI selector baseline、`/settings [current|path|select]` CLI/settings/TUI selector baseline、`/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline、`/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline、Ctrl+P / Ctrl+Shift+P model cycle baseline、`/model select [search]` / Ctrl+L model selector baseline、model selector auth filtering footer/scope/detail/search chrome baseline、scoped model thinking-level per-entry baseline、thinking 模型能力 clamp baseline、retry cancellation visibility baseline、`/export` 默认 HTML、`.html/.htm` standalone transcript export、HTML branch outline、HTML branch filter/search、HTML cwd/parent metadata、HTML current-branch JSONL timeline、label/model/compaction/branch-summary/retry/tree-state event rendering、HTML split-turn prefix rendering/search、HTML branch summary read/modified file rendering、HTML 内嵌 JSONL download、message deep-link/copy-link baseline、HTML code fence rendering baseline、HTML inline code span rendering baseline、HTML plaintext link rendering baseline、HTML Markdown heading/list/blockquote block rendering baseline、HTML Markdown strong/emphasis span rendering baseline、HTML Markdown pipe table rendering baseline、HTML Markdown task list rendering baseline、HTML image metadata caption baseline、HTML long tool result folding baseline、HTML tool call JSON argument rendering baseline、HTML long tool call arguments folding baseline、`/share` secret Gist baseline、prompt template discovery/expansion baseline、skill command discovery/expansion baseline、context files baseline、theme loader/status baseline、JSON extension command discovery/execution/resource/load diagnostics baseline、`/hotkeys` 当前 editor keybinding listing baseline、`/reload` settings/resources/prompts/skills/context-files/theme-status/keybindings baseline、`/auth [current|select|provider]` provider status selector baseline、`/login [select|provider]` OAuth provider selector baseline、`/logout [select|provider]` OAuth provider selector/auth.json credential removal baseline、`/changelog [count|all]` release notes baseline 和 `/session` tree stats；CLI 当前支持 `/help`、`/reload`、`/hotkeys`、`/settings`、`/theme`、`/name`、`/copy`、`/files`、`/export`、`/share`、`/import`、`/new`、`/session`、`/tree`、`/label`、`/fork`、`/clone`、`/resume`、`/quit`、`/model`、`/provider`、`/models`、`/providers`、`/scoped-models`、`/prompts`、`/skills`、`/extensions`、`/auth`、`/login`、`/logout`、`/changelog`、`/retry`、`/thinking`、`/history`、`/find`、`/clear` 和 `/compact`。仍缺完整上游 TreeSelector、自动 branch switching summarization hooks、多选、完整 metadata inspector、LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI、完整 compaction extension events/cancellation UI、完整 settings selector UI、完整 model selector theme/dynamic-border/terminal-host parity/per-entry thinking UI editor、完整 OAuth login/session parity、credential refresh UX、完整 TUI 运行中输入 overlay / keybinding hints、完整 RPC extension UI / bash / full settings parity、完整 TypeScript extension runtime/custom tools/events、完整上游 theme selector parity、TUI theme rendering、theme file watcher、full resource selector、启动 changelog/collapse/telemetry parity、完整 Markdown/highlight renderer、custom tool renderer、richer HTML template 和 Tau 专属 share viewer。 | 继续补上游 TreeSelector 剩余交互、自动 branch switching summarization hooks、上游 LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI、完整 compaction event/cancellation parity、完整 settings selector UI、完整 model selector theme/dynamic-border/terminal-host parity/per-entry thinking UI editor、完整 OAuth login/session parity、credential refresh UX、完整 RPC extension UI / bash / full settings parity、完整 TypeScript extension runtime、theme rendering/watcher、resource selector、完整 Markdown/highlight renderer、richer HTML template、share viewer 和 richer rendering。 |
| Tau.Tui | C | 已经有最小终端抽象、transcript、输入缓冲和会话层，并承载 `Tau.CodingAgent` 主路径；输入编辑器、持久化 history、自定义 keybinding、组件树基础层、文本宽度/截断/wrap helper、单选/多选选择列表基础层、纯函数式 diff 计划器、render surface 合同、最小 ANSI diff sink、单组件 selector session / overlay host baseline、`TuiMessageArea` message transcript foundation、`TuiStatusBar` status line foundation、`TuiScrollbackBuffer` 纯内存 viewport/scrollback foundation、`TuiTranscriptViewport` 组合 viewport foundation、`TuiTranscriptViewportHost` viewport host/diff apply wrapper 和 `TuiTranscriptSession` runtime seam 已经落地。当前已接回 CodingAgent theme/settings/scoped-model/auth status selector、`/login` OAuth provider selector 和 `/logout` OAuth provider selector baseline，但 scrollback/viewport/viewport host/transcript session 仍未接管 terminal host、selector overlay 或 CodingAgent 主屏，仍缺完整 overlay compositing、硬件 cursor 和 theme rendering。 | 下一步把 message/status/scrollback/viewport foundation 接入真实 terminal host，或继续把完整 OAuth login dialog/session 和 resource selector 接入同一 selector foundation，避免 CLI 命令面继续绕过 TUI。 |
| Tau.WebUi | C | 已经从 ASP.NET Core `Hello World` 提升为第二层 Web 宿主：首页聊天页、status/catalog/session/messages API、provider/model 选择、`output/webui-sessions.json` 持久化、NDJSON 流式消息、附件 upload/preview/send、tool timeline、session delete/export/import、title rename、last-opened session restore baseline、auth status 和 client-side Markdown rendering 都已可跑；当前还补了 `WebChatService` fake-runner 行为测试，覆盖流式 assistant 写回、附件 prompt 构造和 session 持久化，并把 Minimal API endpoint 映射抽出后补了 HTTP 级 NDJSON streaming、session 持久化、export/import/delete、rename/restore、400/404 合约回归；默认 message runner 现在也会把 WebUi 生产 log sink 传进 `RuntimeCodingAgentRunner`，让 `/api/sessions/{id}/messages/stream` 真实 runner 路径写出 `agent/run.*` runtime events；现在又新增 `GET /api/sessions/{id}/export.jsonl` 和 `POST /api/sessions/import.jsonl`，以 WebUi-local 线性 JSONL transcript 暴露并导入 session header/message entries，unsupported content type 与 parser error 会返回 `application/problem+json` 稳定错误 code/line，前端也会按 `.jsonl` 文件分流到 JSONL endpoint；WebUi-local JSONL export/import 与 CodingAgent JSONL preview/import 默认复用 `JsonlSecretRedactor` 做 string-value secret redaction，`TAU_WEBUI_REDACT_SECRETS=0` 可关闭；`POST /api/sessions/import.coding-agent-jsonl/preview` 已返回只读 tree metadata（leaf/root/branch/label counts、entry type histogram、current branch ids 和 per-entry branch/label/depth metadata），entry id/parent/label target 按 case-insensitive tree-store 语义解析；`POST /api/sessions/import.coding-agent-jsonl` conservative import baseline 仍只消费 timeline-derived `preview.Messages`，把 CodingAgent timeline 保守持久化为 WebChat DTO，不替换 `WebChatStore`，也不保留完整 branch/tree 结构。仍缺完整 CodingAgent JSONL branch/session-tree 导入与语义对齐。 | 继续把 WebUi session 从 WebChat DTO baseline 向 CodingAgent branch/tree session 语义对齐，下一步应继续强化可审计导入策略，而不是直接覆盖 WebChatStore。 |
| Tau.Mom | C | 已经从 worker 模板提升到结构化本地委派宿主：`--once`、inbox/outbox/archive/events、`.txt/.md/.json` 输入、`provider/model/workingDirectory/title/metadata/attachments` 字段和结果落盘都已现场跑通；前一轮把 runner / result schema 收口为结构化 `DelegationToolEvent`、stop reason、`DelegationUsage`（含 token + 可选总成本），并把 `RuntimeDelegationAgentRunner` 改为可注入 `ICodingAgentRunner` 工厂；本轮又把生产入口接到 `ITauLogSink`，并记录 delegation/response/tool/usage/end runtime log events，取消路径会先写 `delegation.end stopReason=cancelled` 再 rethrow；这一轮继续把 `title`、`metadata`、`attachments` 真正接进运行时上下文和 outbox 契约，并让本地存在的 attachment staging 到 `workingDirectory/attachments/`，通过 `attachments/attachments.jsonl` 和 `log.jsonl` 保留 `original/local` 元数据；runner 输入现在还会注入 `<mom_runtime_context>`，明确本地 workspace/channel layout、events 文件格式、attachment manifest、memory/log/status 路径和 `[SILENT]` 事件响应约定；`ChannelWorkspaceLayout` 会创建 `scratch/`、workspace/channel `skills/`、`attachments/` 和 `events/`，并把 `SYSTEM.md` 与 Agent Skills prompt inventory 注入 prompt context；`workingDirectory/context.json` 会恢复/保存 Tau-native channel session snapshot，让同一 workdir 的 delegation 继承上一轮 runtime messages；调用 runner 前会写 `workingDirectory/last_prompt.jsonl`，记录 mom runtime context、delegation context、实际 runner input、恢复的 session messages、当前 prompt 和 attachment/image attachment count；`workingDirectory/MEMORY.md`、父目录 `MEMORY.md` 与 `workingDirectory/log.jsonl` 的最近非 bot channel history 会进入 prompt context；本地 file delegation 现在也会把用户请求和 bot 结果追加到同一个 `log.jsonl`，把当前或最近一次运行状态写到 `workingDirectory/status.json`，并基于未过期 `running` 状态跳过同一 workdir 的新请求；新增 `MomEventProcessor` 会把本地 `events/*.json` 的 `immediate/one-shot/periodic` 唤醒转换成 inbox 委派请求，复用相同 channel/workspace seam；新增 `MomChannelMessage` / `MomChannelAttachment` 作为 Slack-compatible envelope，file requests 和 local events 已经先统一映射 channel/user/ts/thread/attachments/provider/model/title/metadata，再生成 `DelegationRequest`；新增 `IMomChannelTransport` / `IMomChannelResponder` / `MomChannelMessageProcessor`，先固定 channel transport/responder contract、busy-state、true cancellable stop、typing、thread response、attachment staging、status/log writeback 与 runner 调用；新增 `SlackEventMapper`，固定 Socket Mode `app_mention`、DM、bot/self/subtype skip、mention stripping 和 file metadata 到 `MomChannelMessage` 的 receive-side 映射规则；新增 `SlackSocketModeTransport` / `SlackSocketModeWorker`，固定 `auth.test`、`apps.connections.open`、WebSocket text frame、envelope ack 和 `SlackSocketModeEnabled` 配置开关；新增 `SlackBackfillService`，在 Socket Mode worker 启动前对已有 `log.jsonl` 的 channel 调 `conversations.history` 做 old-message log-only 回填；新增 `SlackWebApiResponder`，用 `HttpClient` 固定 `chat.postMessage`、thread message 与 `files.uploadV2` 的真实 Slack Web API 契约，并保持 token 不进入异常消息；新增 `SlackAttachmentDownloader`，用 bot token 下载 Slack private file URL 并复用 attachment manifest；新增 `MomChannelQueueDispatcher`，固定同频道顺序处理、不同频道独立推进、pending queue limit 和 stop bypass；新增 `MomChannelRunRegistry`，用 linked cancellation token 跟踪当前进程内 active channel run，stop 命令会取消 runner、写 `cancelled` status 并回复 `_Stopped_`；新增 `MomSandboxConfig` / `IMomSandboxExecutor` / `MomToolSet`，默认 host sandbox，配置层支持 `docker:<container>`，Docker validate/exec 路径已通过 `IMomSandboxProcessRunner` 可注入 seam 固定本地测试，并把 runner 默认工具切到上游同名 `bash/read/write/edit/attach`；skills 按上游方式作为 XML `<available_skills>` 暴露，脚本通过 Mom tools 使用，不另注册成 direct tool。runtime 日志仍是文本摘要级 baseline，不是完整 trace/correlation 协议；仍未接真实 Slack smoke 和真实 Docker container smoke。 | 下一步接真实 Slack smoke、Docker sandbox smoke 与更高层 delegation flow。 |
| Tau.Pods | C | 已从控制台占位提升为具备主动运维入口的 CLI：config init/list/validate/status、`probe`、`exec`、`health`、`deploy`、`stop`、`restart`、`model list/pull/remove/status`、`setup plan/run`、`vllm plan/deploy/status/health/stop`、sample config、probe/exec/lifecycle/model/setup/vLLM orchestration service 和 source-gen JSON 已具备；target-command 支持默认 `tau.pods.json` 或显式 config path，SSH lifecycle metadata 命令会规范化 deployment name 并做 shell quoting；`PodExecService` 现在用 `ProcessStartInfo.ArgumentList` 构造系统 `ssh` argv，避免本地参数拼接和 quoting 漂移，并把本地 ssh 启动失败、runner 异常、`Process.Start` 返回 null 和 cancellation 转成结构化 failure；model lifecycle baseline 支持 pod-level `modelsPath`、remote HF cache list/status/remove、`huggingface-cli` pull、python fallback、`model pull --revision` / `--snapshot` 和长耗时 SSH keepalive；`setup plan/run` 已固定 setup command、`HF_TOKEN` / `PI_API_KEY` 配置状态、`release/nightly/gpt-oss` vLLM version metadata、token stdin transport、display/JSON/text output 不回显 secret、stdout/stderr redaction、SSH test、SCP script、remote setup 和 GPU detect 执行链；`setup run` 成功后会把 `modelsPath`、`vllmVersion` 和 detected `gpus` 写回 pod config，失败不写 config 且不保存 token，JSON/text 输出会返回 `configUpdated` 与 `configPath`；`PodVllmCommandPlanner` 纯本地 vLLM serve command planner baseline 已接到 `vllm plan [--json] [--config path] [--pod id] <model> [--name name] [--revision rev] [--snapshot rev]` CLI 预览入口，且支持 revision-aware snapshot discovery（旧 `[path] <id> <model> [name]` 兼容）；本轮 `PodVllmOrchestrationService` 又把 `vllm deploy/status/health/stop [--json]` 接到 SSH command/service baseline：deploy 写 service/metadata并优先 systemd user unit，systemd 不可用或失败时 fallback 到 nohup pid/log；deploy 默认按 12 次、5 秒 backoff 做远端 `/health` readiness 窗口，可用 `--no-health` 跳过，也可用 `--health-attempts` / `--health-backoff-ms` 调整；preflight 现在还能按 `--revision` / `--snapshot` 解析 `refs/<revision>` 或 `snapshots/<revision>`，并把 `requestedRevision` 暴露到 plan/preflight 输出里；`vllm deploy --prefetch` 会在模型 cache/snapshot/revision 缺口时复用 `model pull` 后二次 preflight，成功才写 service，pull 失败不会触发 deploy 或 rollback；部署命令失败或 health 最终非 ready 时尝试 rollback cleanup；status/health 拉远端状态文本并返回 failureKind/attempts；stop 做 idempotent cleanup；`vllm plan/preflight/deploy` 已支持 `--config/--pod/--name/--revision/--snapshot` active fallback，`model list/pull/remove/status`、`deployments` 和 `vllm status/health/stop` 也支持 `--config/--pod` 显式入口。`Tau.Pods.Tests` 上一轮记录覆盖 94 个 pod config/probe/exec/lifecycle/model/setup/vLLM planner/orchestration/CLI 回归，本轮新增 revision/prefetch targeted coverage 但未重算全量 count。仍缺真实 SSH/HF download、真实 setup run/SCP/GPU smoke、vLLM startup health smoke、多版本回滚、更完整远端 transport hardening 和真实运维 smoke。 | 继续补真实 SSH/HTTP/HF smoke、真实 setup run smoke、vLLM runner 编排、health 判定、多版本回滚、更完整远端 transport hardening 和更高层 pod orchestration。 |
| 测试 | C | 当前六个测试项目都已有真实 targeted 覆盖，并继续按模块固定关键合同：`Tau.Ai.Tests` 覆盖 provider/auth/OAuth/models/observability/JSONL redaction/Bedrock SSO refresh；`Tau.CodingAgent.Tests` 覆盖 CLI、session/settings、JSONL tree、compaction/retry/export/share、RPC、selectors、HTML nested Markdown list；`Tau.Agent.Tests` 覆盖 Mom delegation、Slack-compatible channel runtime、sandbox/tool seam、runtime response update/delete、runtime log events 和 AgentRuntime queue mode；`Tau.Tui.Tests` 覆盖 input/editor/history/keybinding/components/selectors/diff/ANSI/scrollback/transcript viewport/viewport host/transcript session，并包含 Home scroll-top；`Tau.WebUi.Tests` 覆盖 browser flow、session/export/import、WebUi-local JSONL、CodingAgent JSONL preview tree/filter/audit、conservative import source audit 和默认 runner runtime log sink 透传；`Tau.Pods.Tests` 覆盖 config/probe/exec/lifecycle/model/setup/vLLM planner、setup plan/run token redaction/version metadata、fake-runner SSH/SCP/setup/GPU execution chain、setup result config writeback、setup run `configUpdated/configPath` 输出、SSH failure no-writeback、vLLM deploy/status/health/stop orchestration、health retry/backoff、failure classification、rollback cleanup 和 deploy prefetch/revision 闭环。当前 targeted counts：`Tau.Ai.Tests` 220、`Tau.CodingAgent.Tests` 347、`Tau.WebUi.Tests` 42、`Tau.Agent.Tests` 81、`Tau.Pods.Tests` 上一轮记录 94、本轮未重算全量、`Tau.Tui.Tests` 144；上一轮记录的工作树 `verify-dotnet.ps1 -SkipRestore` 与 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 均已通过，后者包含 WebUi smoke 和 Mom `--once` smoke；本轮重跑 runtime log correlation targeted tests、Pods setup plan targeted tests `PodSetup|SetupPlan` 5/5、setup run focused tests `PodSetup|SetupRun` 7/7、追加 `FullyQualifiedName~SetupRun` 2/2，Pods vLLM prefetch/revision focused tests 7/7，并通过 `Tau.CodingAgent.csproj`、`Tau.Mom.csproj`、`Tau.Pods.csproj` build；未重跑全仓 gate。 | 继续把高价值 runtime smoke 自动化，尤其是 provider e2e、WebUi、Mom、Pods 的端到端行为。 |
| CI/CD | C | 主 CI 已改为 `scripts/verify-dotnet.sh`，按显式项目顺序 restore / build / test，形成了真实可重复的项目级门禁；现在也已经覆盖到 `WebUi / Mom / Pods` 三个应用面。`Tau.CodingAgent`、`Tau.WebUi`、`Tau.Mom`、`Tau.CodingAgent.Tests` 和 `Tau.Agent.Tests` 已不再依赖预置 DLL `HintPath`，降低了 stale assembly 和并行构建顺序风险；`Tau.slnx` 已具备 solution-level build 入口。当前本机 `bash scripts/verify-dotnet.sh --skip-restore` 仍因 WSL 缺少 `/bin/bash` 失败，属于本机 shell 环境问题；当前工作树已用等价 PowerShell 入口通过 `verify-dotnet.ps1 -SkipRestore` 与 `verify-dotnet.ps1 -SkipRestore -RunSmoke`，后者覆盖 WebUi smoke 和 Mom `--once` smoke。release 产物仍不是 Tau 的真实可执行交付件。 | 让 release 链开始对应真实 Tau 构建产物，并继续把运行态 smoke 纳入验证脚本或 PowerShell 兜底脚本。 |
| 可观测性 | C | 已有 `ITauLogSink` / `JsonlTauLogSink` / `NullTauLogSink` baseline，runtime event JSONL 默认做 string-value redaction；`TauRuntimeLogContext` 已提供最小 cross-module correlation 字段 baseline；`RuntimeCodingAgentRunner` 已写 `agent/run.start|run.end|run.cancel|run.error` 并生成/继承 `correlationId`；`ProviderAuthResolver.GetStatus` 已写 `auth/status.checked`，CodingAgent/WebUi auth status 生产路径已接入 runtime sink；WebUi message stream 默认 runner 现在也会把同一个 sink 和 `sessionId` / transient `messageId` 传给 `RuntimeCodingAgentRunner`，因此生产聊天路径可写带 correlation 字段的 `agent/run.*`；CodingAgent CLI 普通 turn 会把同一 per-turn context 传入 runner 并在 retry attempts 间复用，RPC prompt 会用 command id 作为 `correlationId/messageId` 并注入 tree `sessionId`；`ToolExecutor` 已通过 `AgentLoopConfig.LogSink` 写共享 `tool/execution.start|end` trace，并通过 `AgentLoopConfig.LogContext` 继承同一个 `correlationId/sessionId/messageId`，覆盖 tool id/name、execution mode、argument/result 尺寸、duration、success/isError 和轻量 failure kind，且不写完整 arguments/result；`RuntimeDelegationAgentRunner` 已写 Mom delegation/response/tool/usage/end events，并且生产 `Tau.Mom` 会从环境创建 log sink；Tau.Pods probe、vLLM、普通 lifecycle 和 model lifecycle 也已有摘要事件。当前仍主要是本地 JSONL 与文本摘要级观测，不是完整 trace/correlation 协议；仍缺 Mom/Pods 与真实 e2e trace 的统一 correlation 贯通。 | 继续把 Mom/Pods/真实 e2e trace 接到同一 correlation 语义，再把高价值 runtime smoke 纳入验证链。 |
| 安全与配置 | C | 当前已经补了环境变量矩阵、auth.json schema、OAuth login/refresh、OAuth 凭证读取/刷新写回和 Vertex ADC token exchange，配置边界比之前清楚；本轮又把默认 `./.tau/auth.json` / `./.tau/models.json` 加入忽略、让 Unix `auth.json` 写入权限收紧到 0600，并过滤 OAuth metadata 里的 credential 保留字段，避免 metadata shadow 密钥语义。`/logout [select|provider]` 已能删除本地 `auth.json` 对应 provider credential entry，交互式 selector 只列出当前本地 OAuth credential，不修改环境变量或 `models.json` credential 配置。models.json 状态检查现在只识别 credential 配置，不解析 env、不执行 `!command`、不回显 `apiKey` 或 credential header 值。`JsonlTauLogSink` runtime event log、`CodingAgentTreeSessionStore` JSONL tree session 写出、WebUi-local JSONL export/import、CodingAgent JSONL preview/import 和 Mom channel `log.jsonl` / `last_prompt.jsonl` 都已默认复用 `JsonlSecretRedactor` 做 string-value 脱敏；field key 保持不改，非标准 secret pattern 和真实云端凭证链仍未完整定义。WebUi 已具备会话级 provider/model 配置，Mom 已具备结构化 provider/model/workdir 请求，Pods 已具备 probe/exec 配置。 | 继续梳理非标准 secret pattern、AWS credential chain 与跨运行态 auth/config 迁移规则。 |
