# 质量评分

用这份文档按产品区域和架构层次记录当前质量水位，方便持续知道最薄弱的地方在哪。

## 评分标准

- `A`：覆盖完整、行为稳定、文档清楚、运行风险低。
- `B`：整体可接受，但还有明确短板。
- `C`：能用，但需要针对性补强。
- `D`：脆弱、缺少规范，或很多行为尚未定义。

## 最新增量（2026-06-09）

- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `CodingAgent RPC extension UI and streamed bash parity` 候选领取 RPC assistant stream event schema 子切片。对照上游 `packages/coding-agent/src/modes/rpc/rpc-types.ts`、`rpc-mode.ts`、`docs/rpc.md` 和 `packages/ai/src/types.ts`，Tau RPC `message_update` 现在输出上游字段 `assistantMessageEvent`，不再输出 Tau 私有 `streamEvent`；assistant stream event payload 现在补齐 `partial`、`text_end/thinking_end.content`、`toolcall_end.toolCall` 和 `done/error.reason` baseline。当前已通过 focused `CodingAgentRpcHostTests` gate 57/57、RPC gate 64/64、完整 `Tau.CodingAgent.Tests` 490/490 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 120、Tui 251、CodingAgent 490、WebUi 61、Pods 216）。该切片关闭本地 RPC `message_update` assistant event shape baseline；完整 TypeScript extension runtime、package-loaded extensions、custom tools、真实 extension UI 调用、全量 RPC response/message schema、toolCall arguments object shape、settings/package/terminal contracts 和最终 RPC `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `CodingAgent image and clipboard ingestion` 候选继续领取 image resize/EXIF 子切片。`CodingAgentImagePreprocessor` 现在被 CLI `@file` 和 `read_file` 共用：`images.autoResize=true` 时覆盖本地 PNG resize 到上游默认 2000x2000 / 4.5MiB base64 headroom、尺寸说明、JPEG/WebP EXIF orientation 尺寸解析；`RuntimeCodingAgentRunner.Create(...)` 会把启动 settings `images.autoResize` 传给默认 `ReadFileTool`，HTML transcript read_file metadata 会显示 resized/dimensions。当前已通过 focused `ImagePreprocessor|InitialMessageBuilder|ReadFileTool` gate 24/24、完整 `Tau.CodingAgent.Tests` 489/489 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 120、Tui 251、CodingAgent 489、WebUi 61、Pods 216）。该切片关闭本地 PNG resize + EXIF dimension metadata + read_file auto-resize wiring baseline；desktop/OSC52 image clipboard、完整 Photon JPEG/GIF/WebP resize/convert quality parity、terminal PNG conversion、provider vision e2e、完整 TUI attachment/editor path 和最终 image ingestion `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 CodingAgent assistant usage cost persistence 后续缺口领取 session stats/display 子切片。`CodingAgentSessionStats` 现在暴露 `tokens.input/output/cacheRead/cacheWrite/total`、`cost` 和 `costRecords`；CLI `/session` 会显示 persisted usage/cost，RPC `get_session_stats` 返回同一 JSON stats；JSONL tree session 模式会从当前 branch 持久化 assistant entries 汇总 usage/cost，覆盖 compaction 后 runtime snapshot 不再携带旧 assistant message 的场景。当前已通过 focused CodingAgent usage-cost stats/display gate 248/248、完整 `Tau.CodingAgent.Tests` 484/484 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 120、Tui 251、CodingAgent 484、WebUi 61、Pods 216）。该切片关闭本地 `/session` / RPC stats usage-cost display baseline；真实 provider/e2e cost samples、older-session backfill、exact upstream session root/path semantics、richer upstream footer/TUI rendering 和最终 `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 CodingAgent `package manager/install telemetry/changelog` partial 面继续领取 startup changelog/version + install telemetry runtime 子切片。对照上游 interactive startup 的 `getChangelogForDisplay()` / `reportInstallTelemetry()` 行为，Tau 新增 `CodingAgentStartupNoticeService`，settings snapshot/document/RPC surface 持久化并暴露 `lastChangelogVersion`；交互式 host 在 fresh session 启动时检查版本状态，fresh install 只记录当前版本并 best-effort telemetry，版本变化时按 `collapseChangelog` 显示折叠或完整 notice，恢复已有 messages 的 session 不弹 notice；`PI_OFFLINE` 禁用 telemetry，`PI_TELEMETRY` 覆盖 settings。当前已通过 focused `StartupNotice|SettingsStore|Changelog|GetSettings|UpdateSettings` gate 20/20、candidate `Cli|Session|Settings|PackageManager|Changelog|StartupNotice` gate 138/138、完整 `Tau.CodingAgent.Tests` 478/478 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 478、WebUi 61、Pods 216）。第一次项目级 gate 曾在既有 WebUi browser-flow 长轮询测试上 30s 超时；排查确认当前无 WebUi diff，失败项单独重跑 1/1 通过，第二次项目级 gate 完整通过。该切片关闭本地 startup changelog/version state 与 install telemetry runtime baseline；interactive config selector、package-loaded TypeScript extension runtime、最终 `pi` package/bin identity、真实 npm/git network smoke、真实 package consumer smoke、真实 telemetry backend e2e 和完整 TUI changelog rendering parity 仍保持 open。
- 上一轮继续 `GOAL.md` 100% pi-mono parity 主线，从 CodingAgent `package manager/install telemetry/changelog` partial 面继续领取 package resource filter 子切片。对照上游 `packages/coding-agent/src/core/package-manager.ts` 的 `applyPatterns()` / manifest collection 语义，Tau 的 `CodingAgentPackageManager` 现在会解析 package manifest 和 package object filters 中的 plain include、glob include、`!` exclude、`+` exact force-include、`-` exact force-exclude；manifest glob/negation 与 settings filter 都有本地临时包目录测试，空数组继续表示显式禁用该资源类型。上一刀已落地的 npm/git command execution、source settings persistence、本地 package manifest/convention resource discovery、extension/prompt/skill/theme store 合并和 `/reload` package resource refresh 继续保留。该切片已通过 focused `PackageManager|SettingsStore` gate 21/21、candidate `Cli|Session|Settings|PackageManager` gate 128/128、完整 `Tau.CodingAgent.Tests` 472/472 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 472、WebUi 61、Pods 216）。该切片关闭本地 package resource glob/negation filter baseline；后续 startup changelog/install telemetry runtime 已继续收口，interactive config selector、package-loaded TypeScript extension runtime、最终 `pi` package/bin identity、真实 npm/git network smoke 和真实 package consumer smoke 仍保持 open。
- 上一轮继续 `GOAL.md` 100% pi-mono parity 主线，从 CodingAgent `package manager/install telemetry/changelog` partial 面继续领取 package manager execution 子切片。对照上游 `packages/coding-agent/src/package-manager-cli.ts`、`core/package-manager.ts`、`core/settings-manager.ts` 和 `utils/git.ts`，Tau 的 `CodingAgentPackageManager` / `CodingAgentPackageCli` 现在不仅处理 source settings，还会执行 npm/git install/update/remove：npm package 通过可注入 command runner 执行全局 `npm install -g`、project `.tau/npm --prefix`、settings `npmCommand` 和 update 的 `@latest` 安装；git package 支持常见 `git:` / HTTPS / SSH source 的 clone/checkout/pull/remove 到 user/project `.tau/git` install root；`PI_OFFLINE` 会跳过 update；命令失败会返回 CLI error 且不写入 settings；错误输出走现有 secret redaction。上一刀已落地的上游 string/object `packages` persistence、本地 package manifest/convention resource discovery、extension/prompt/skill/theme store 合并和 `/reload` package resource refresh 继续保留。该切片已通过 focused `PackageManager|SettingsStore` gate 17/17、candidate `Cli|Session|Settings|PackageManager` gate 124/124、完整 `Tau.CodingAgent.Tests` 468/468 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 468、WebUi 61、Pods 216）。该切片关闭本地 package source/list/config + local package resource discovery + npm/git command execution baseline；后续 startup changelog/install telemetry runtime 已继续收口，interactive config selector、package-loaded TypeScript extension runtime、最终 `pi` package/bin identity、真实 npm/git network smoke 和真实 package consumer smoke 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Pods command/config/known-model compatibility` 候选领取 pod-agent runtime seam 子切片。对照上游 `packages/pods/src/commands/prompt.ts` 当前只构造 coding-agent 参数后抛出 `Not implemented` 的事实，Tau 按产品 runtime 缺口推进：`agent [--config path] [--pod id] <name> [message/options...]` 现在保留 redacted upstream-style argument plan，并通过新增 `PodAgentRunner` 生成临时 Tau.Ai `models.json` provider、设置 `TAU_MODELS_FILE` / `TAU_PROVIDER` / `TAU_MODEL`、启动 Tau.CodingAgent；临时 config 只保存 `PI_API_KEY` env var name 或 `dummy`，不落明文密钥。`Tau.CodingAgent` CLI 现在实际消费 `--provider`、`--model` 和 `--system-prompt`，并把 `--json` 当 boolean option 处理，避免吞掉后续 prompt 文本。当前已通过 focused Pods `Agent|PodsCli` gate 96/96、CodingAgent `CodingAgentInitialMessageBuilderTests` 6/6、完整 `Tau.Pods.Tests` 216/216、完整 `Tau.CodingAgent.Tests` 456/456 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 61、Pods 216）。该切片关闭本地可执行 pod-agent process runner 合同；真实 pod endpoint prompt e2e、真实 SSH/HF/GPU/vLLM smoke、完整 upstream JSON output parity 和最终 Pods `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `WebUi sandbox/artifact runtime bridge` 候选继续领取 LLM-facing JavaScript REPL active-browser bridge 子切片。对照上游 `packages/web-ui/src/tools/javascript-repl.ts`，Tau 新增 `WebUiJavaScriptReplBridge` 和 `/api/sessions/{id}/javascript-repl/next` / `/result` endpoints：server-side `javascript_repl` AgentTool 入队 session-bound request，活动 WebUi 页面长轮询领取后调用现有 `window.executeJavaScriptRepl(code)` sandbox host，再回传 output/error 与 `fileName/mimeType/size/contentBase64` files；生产 `WebChatService` / `WebUiRunnerFactory` 现在默认注册 `artifacts + javascript_repl` session tools，无活动页面时返回明确 timeout error，不伪造 server-side JS evaluator。当前已通过 focused `WebUiTools|JavaScriptReplBridgeEndpoints|WebUiPageTests` 10/10、真实 Chromium `JavaScriptReplBridge` browser flow 2/2、完整 `Tau.WebUi.Tests` 61/61 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 61、Pods 215）。该切片关闭可用 LLM-facing `javascript_repl` active browser bridge execution + tool result files details/base64 本地合同；full artifact message reconstruction、Docx/Excel rich viewers、pdfjs renderer/toggles、reusable Lit component package、extension sandbox URL mode、release/static browser smoke 和最终 WebUi `verified` 状态仍保持 open。
- 上一轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `WebUi sandbox/artifact runtime bridge` 候选领取 LLM-facing artifacts tool 子切片。对照上游 `packages/web-ui/src/tools/artifacts/artifacts.ts`、`tools/javascript-repl.ts`、`prompts/prompts.ts` 和 `ChatPanel.ts`，Tau 新增 internal `WebUiTools`：`WebUiArtifactsTool` 暴露 `command/filename/content/old_str/new_str` schema，并通过 session-bound `WebArtifactService` 真实执行 create/update/rewrite/get/delete/logs；生产 `WebChatService` 在有 `WebArtifactService` 时把 CodingAgent 默认工具与 WebUi session tools 合并，前端在 streaming `tool_end: artifacts` 后刷新 artifact pane。`WebUiToolPrompts` 现在承载 artifacts、javascript_repl、attachments、artifacts runtime 和 file-download provider description baseline；该 artifacts 子切片当时保留 `javascript_repl` execution gap，后续已由本轮 active-browser bridge 子切片关闭。该 artifacts 子切片已通过 focused `WebUiTools|WebUiPageTests` 7/7、完整 `Tau.WebUi.Tests` 57/57 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 57、Pods 215），关闭 LLM-facing artifacts AgentTool + prompt/provider description baseline；full artifact message reconstruction、Docx/Excel rich viewers、pdfjs renderer/toggles、reusable Lit component package、extension sandbox URL mode、release/static browser smoke 和最终 WebUi `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `WebUi sandbox/artifact runtime bridge` 候选继续领取 JavaScript REPL execution host 子切片。对照上游 `packages/web-ui/src/tools/javascript-repl.ts`、`components/SandboxedIframe.ts` 和 `components/sandbox/*RuntimeProvider.ts`，Tau 的 `WebUiPage` 现在暴露 browser-side `window.executeJavaScriptRepl(code)`：创建隐藏 sandbox iframe，注入现有 runtime bridge、session attachment snapshot、artifact helper、`returnDownloadableFile` 和 console capture，执行 ES module async code 后汇总 console output、return value、execution error 与 returned files；REPL sandbox 会复用 `/api/sessions/{id}/runtime/messages` 读写 artifact store，并在 mutating artifact action 后刷新 artifact pane。console runtime 现在会等待 pending console sends 再 completion，避免 completion 先于日志 ack。当前已通过 focused `JavaScriptRepl|WebUiPageTests` 4/4 和完整 `Tau.WebUi.Tests` 53/53。该切片只关闭本地 browser sandbox execution host baseline；LLM-facing `javascript_repl` AgentTool 注册、prompt constants、完整 artifact message reconstruction、Docx/Excel rich viewers、pdfjs page renderer/view-code toggles、reusable Lit component package、extension sandbox URL mode、release/static browser smoke 和最终 WebUi `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `WebUi sandbox/artifact runtime bridge` 候选继续领取 specialized viewer baseline 子切片。对照上游 `packages/web-ui/src/tools/artifacts/artifacts.ts`、`MarkdownArtifact.ts`、`SvgArtifact.ts`、`PdfArtifact.ts`、`TextArtifact.ts`、`ImageArtifact.ts` 和 `GenericArtifact.ts`，Tau 的 artifact pane 现在按 MIME / extension 分流 Markdown、SVG、image、PDF、text/code 和 generic files：Markdown 复用现有 client-side `renderText`，SVG 用 sandboxed iframe preview + download，image / PDF / generic file 都提供 data-url download，text/code 扩展名集合补齐到更接近上游 `TextArtifact` 覆盖面，generic fallback 显示不可预览提示。浏览器回归也显式重选本测试创建的 session 后刷新 artifact pane，固定整套测试时后台 session load 竞态。当前已通过 focused `ArtifactsPane_RendersSpecializedViewerBaselines|WebUiPageTests` 3/3、完整 `Tau.WebUi.Tests` 51/51、跨项目 `Tau.CodingAgent.Tests` WebUi endpoint fixture 11/11 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 51、Pods 215）。该切片只关闭轻量 viewer routing/render/download baseline；LLM-facing `javascript_repl` AgentTool、完整 LLM-facing artifact message reconstruction、Docx/Excel rich viewers、pdfjs page renderer/view-code toggles、可复用 Lit component package、release/static browser smoke 和最终 WebUi `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `WebUi sandbox/artifact runtime bridge` 候选继续领取 artifact operation semantics 子切片。对照上游 `packages/web-ui/src/tools/artifacts/artifacts.ts` 和 `prompts/prompts.ts`，Tau 的 runtime message contract 现在新增上游式 `create`、`update`、`rewrite`、`logs` / `htmlArtifactLogs` action，并保留旧 `createOrUpdate` 兼容入口；`WebRuntimeMessageRequest` 新增上游命名的 `old_str` / `new_str` JSON 字段。服务端 `create` 拒绝覆盖既有文件，`update` 要求 `old_str` / `new_str` 并只替换首个匹配，找不到字符串时返回完整内容帮助重试，`rewrite` 要求文件存在并整文件替换，`htmlArtifactLogs` 读取当前 HTML artifact sandbox 通过 console runtime message 收集的 bounded in-memory log。HTML iframe bridge 新增 `createArtifact`、`updateArtifact`、`rewriteArtifact` 和 `htmlArtifactLogs` helper，并在 create/update/rewrite/delete 后刷新 artifact pane。当前已通过 focused `ArtifactEndpoints|WebUiPageTests` 5/5、完整 `Tau.WebUi.Tests` 50/50、跨项目 `Tau.CodingAgent.Tests` WebUi endpoint fixture 11/11、`git diff --cached --check`、项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 50、Pods 215），以及临时 WebUi + Playwright 页面/DOM 检查（页面加载到 `Tau Web UI`，并确认新 iframe bridge helper 和 `old_str/new_str` 已注入）。该切片只关闭 artifact operation semantics 的本地 runtime/tool-contract baseline；LLM-facing `javascript_repl` AgentTool、完整 LLM-facing artifact message reconstruction、specialized viewers、reusable Lit component package、release/static browser smoke 和最终 WebUi `verified` 状态仍保持 open。
- 前一 WebUi provider 子切片关闭了 HTML artifact attachments + file-return local baseline：iframe 会从当前 session user messages 注入只读 attachment snapshot，暴露 `listAttachments()`、`readTextAttachment(id)`、`readBinaryAttachment(id)`，并通过 `returnDownloadableFile(fileName, content, mimeType?)` 发送上游 `fileName` casing 的一次性 `file-returned` metadata。该子切片当时通过 focused 4/4、Chromium iframe provider flow 1/1、完整 WebUi 49/49、跨项目 WebUi endpoint fixture 11/11 和项目级 gate；本轮 operation 子切片已在其基础上把 artifact update/rewrite/log 本地语义继续补上。
- 前一 server-backed artifact/runtime 子切片新增了 per-session `WebArtifactStore` / `WebArtifactService`、artifact CRUD endpoints、`/api/sessions/{id}/runtime/messages` bridge、artifact pane 与 HTML sandbox `listArtifacts` / `getArtifact` / `createOrUpdateArtifact` / `deleteArtifact` helper，并在 session 删除时清理 artifact state。attachments/file-return、artifact operation semantics、lightweight specialized viewer baseline 和 browser-side JavaScript REPL execution host 已由后续 2026-06-09 子切片继续补齐；LLM-facing `javascript_repl` AgentTool、full renderer registry、Docx/Excel rich viewers、pdfjs page renderer/view-code toggles、reusable Lit component package 和 release/static browser smoke 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `CodingAgent RPC extension UI and streamed bash parity` 候选继续领取 RPC client helper 子切片。对照上游 `packages/coding-agent/src/modes/rpc/rpc-client.ts`，Tau 新增 `CodingAgentRpcClient` / `CodingAgentRpcProcessTransport`，支持启动 `--mode rpc` 进程、追加 provider/model/agent args/env、生成 `req_N`、LF JSONL send、pending response correlation、event subscription、stderr-backed timeout、`promptAndWait` / `collectEvents` / `waitForIdle`、typed bash result 和 stop-time pending rejection；同时修正 helper 等待超时 token 生命周期，避免等待 helper 返回未完成 task 后 timeout source 被提前释放。当前已通过 CodingAgent build、focused `CodingAgentRpcClientTests` 7/7、完整 `Tau.CodingAgent.Tests` 456/456 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 44、Pods 215）。该切片只关闭上游 `rpc-client.ts` 的 .NET-native client helper baseline；exact TypeScript response/event schema、TypeScript extension runtime、package-loaded extensions、custom tools、真实 extension UI 调用、full settings/package/terminal contracts 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `CodingAgent RPC extension UI and streamed bash parity` 候选领取 RPC extension UI protocol 子切片。对照上游 `packages/coding-agent/src/modes/rpc/rpc-types.ts` 与 `rpc-mode.ts`，Tau 新增 `CodingAgentRpcExtensionUiBridge`，并让 `CodingAgentRpcHost` 识别 stdin `extension_ui_response`，支持输出上游同形 `extension_ui_request`：`select`、`confirm`、`input`、`editor`、`notify`、`setStatus`、`setWidget`、`setTitle`、`set_editor_text`；等待型请求可由 value/confirmed/cancelled response resolve，fire-and-forget 请求只输出事件，可选字段未传时按上游 optional shape 省略，预取消请求返回默认值且不输出 request。当前 focused `CodingAgentRpcHostTests` gate 54/54 已通过。该切片只关闭 headless RPC extension UI bridge 和状态机 baseline；TypeScript extension runtime、package-loaded extensions、custom tools、真实 extension UI 调用和 exact schema parity 仍保持 open。
- 验证补充：该 RPC extension UI 子切片还通过了 `dotnet build src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-restore --verbosity minimal`（0 warning / 0 error）、完整 `Tau.CodingAgent.Tests` 449/449、`git diff --check`（仅既有 docs CRLF 规范化 warning）和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 449、WebUi 44、Pods 215）。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `CodingAgent image and clipboard ingestion` 候选领取 CLI `@file` / initial prompt 子切片。对照上游 `packages/coding-agent/src/cli/file-processor.ts`、`cli/initial-message.ts`、`main.ts`、`utils/mime.ts` 和 `core/tools/path-utils.ts`，Tau 新增 `CodingAgentInitialMessageBuilder` 与 `CodingAgentCliArguments`，把 redirected stdin、`@file` 文本、PNG/JPEG/GIF/WebP 图片和第一条 CLI message 合并为初始 prompt；print mode 与 interactive startup 在含图片时走 `ContentBlock` runner 路径，RPC 模式拒绝 `@file`，空白 stdin 不触发 print mode。当前已通过 CodingAgent build、focused initial-message/print gate 11/11、完整 `Tau.CodingAgent.Tests` 445/445、`git diff --check` 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 445、WebUi 44、Pods 215）。该切片只关闭 CLI 初始输入 `@file` text/image MIME baseline；clipboard image paste、Photon resize/convert/EXIF、真实 provider vision e2e、完整 TUI attachment/editor path 和最终 image ingestion `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` `startModel()` 配置时序缺口领取 save-before-watch / delete-config 子切片。对照上游 `packages/pods/src/commands/models.ts` 在 remote start 返回 PID 后立即保存 `config.pods[podName].models[name]`、再监控 startup log、启动失败后删除配置的行为，`PodVllmOrchestrationService.DeployAsync` 新增 `onRemoteStarted` seam；`PodsCli` 现在在远端启动成功后、`startupWatch` / `/health` 之前写 configured model state，并在 startup watcher 或 health 失败 rollback 后删除该 state。新增 service seam 测试证明回调发生在 startup watcher 前，CLI 测试直接在 watcher/health fake SSH 命令执行时读取 config，证明中间状态可观察。当前已通过 `git diff --check`、focused ordering gate 4/4、完整 `Tau.Pods.Tests` 215/215 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 215）。该切片只关闭本地 save-before-watch/delete-on-failure ordering contract；真实 SSH/HF/GPU/vLLM startup smoke、多版本 rollback、transport hardening 和 Pods final `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` model runner 缺口领取 HF download / PI API key command 子切片。对照上游 `packages/pods/scripts/model_run.sh`，`PodVllmCommandPlanner` 生成的 `model_run_<name>.sh` 现在会在 runner header 后执行 `HF_HUB_ENABLE_HF_TRANSFER=1 hf download "$MODEL_ID"`，失败时输出稳定 `ERROR: Failed to download model` 并退出，成功时输出 `Model download complete`；Tau-planned `vllm serve` 命令固定携带 `--api-key "$PI_API_KEY"`，再追加 served model name 与额外 vLLM args。当前已通过 focused runner/deploy/plan/rollback gate 65/65、完整 `Tau.Pods.Tests` 214/214、`git diff --check` 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 214）。该切片只关闭本地上传脚本/远端命令文本合同；save-before-watch/delete-config ordering baseline 已由后续独立切片收口，真实远端 HF download、PI API key 环境、SSH/GPU/vLLM startup smoke、多版本 rollback 和 Pods final `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` model runner wrapper 缺口领取 `model_run.sh` / pseudo-TTY `script` 子切片。对照上游 `packages/pods/scripts/model_run.sh` 和 `packages/pods/src/commands/models.ts` 中上传 runner、创建 wrapper、用 `script -q -f -c ... ~/.vllm_logs/<name>.log` 启动并保存 wrapper PID 的行为，`PodVllmCommandPlanner` / `PodVllmOrchestrationService` 现在会生成并上传 `~/.tau_pods/model_run_<name>.sh` 与 `~/.tau_pods/model_wrapper_<name>.sh`，systemd 与 fallback 都启动同一 wrapper；runner 保留 `Model runner exiting with code`，wrapper 追加 `Script exited with code`，systemd 不再重复 append wrapper stdout/stderr，stop/rollback cleanup 会删除 runner/wrapper 脚本，plan/deploy text/JSON 与 metadata 暴露 `runnerScriptPath`、`wrapperScriptPath` 和 `usesPseudoTtyWrapper`。当前已通过 `dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`、focused wrapper/deploy/plan/rollback gate 65/65、完整 `Tau.Pods.Tests` 214/214 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 214）。该切片本身只关闭本地 runner/wrapper/pseudo-TTY 合同；HF download / PI API key command baseline 和 save-before-watch/delete-config ordering baseline 已由后续独立切片收口，真实 SSH/HF/GPU/vLLM smoke、多版本 rollback 和 Pods final `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` deploy-time startup watcher 缺口领取 `startModel()` 启动等待子切片。对照上游 `packages/pods/src/commands/models.ts` 在启动后监控 `~/.vllm_logs/<name>.log`、看到 `Application startup complete` / failure marker 后停止的行为，`PodVllmOrchestrationService.DeployAsync` 现在会在 remote deploy command 成功后先运行结构化 `WatchStartupAsync`，输出 `startupWatch` JSON/text；watcher ready 后继续现有 `/health` readiness，watcher 识别 `Model runner exiting with code`、`Script exited with code`、OOM 和 engine initialization failure 时返回 `failureKind=startup-failed` 并触发 rollback。当前已通过 `dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`、focused deploy gate 34/34、完整 `Tau.Pods.Tests` 214/214 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 214）。该切片只关闭 deploy-time startup marker wait / failure rollback baseline；configured model save/delete ordering、model runner wrapper、pseudo-TTY `script`、HF download / PI API key command 已由后续独立切片收口，真实 SSH/HF/GPU/vLLM smoke、多版本 rollback 和 Pods final `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` realtime log follow 缺口领取 `logs --follow` 子切片。对照上游 `packages/pods/src/commands/models.ts` 的 `viewLogs()`，Tau 现在新增 `PodExecService.OpenCommandAsync` 继承 stdout/stderr 执行 SSH remote command，并让 `logs --follow` / `logs -f` 在 active/指定 pod 上先检查 configured model state，再流式执行 `tail -f ~/.vllm_logs/<name>.log`；missing model 返回 `failureKind=model-not-found` 且不执行 SSH，`--json --follow` 明确拒绝，普通 `logs --json` 快照合同保持不变。当前已通过 `dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`、focused gate 7/7、完整 `Tau.Pods.Tests` 212/212 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 212）。该切片只关闭 top-level realtime log viewing baseline；deploy-time startup watcher 自动停止、watcher 失败后 config 清理、exact `model_run.sh` wrapper、真实 SSH/HF/GPU/vLLM smoke 和多版本 rollback 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` `startModel` 配置防护和 operation failure schema 缺口领取 duplicate configured deployment guard 子切片。对照上游 `packages/pods/src/commands/models.ts` 在 `pod.models[name]` 已存在时拒绝启动的合同，`vllm deploy` 和 top-level `start` 现在会在远端 preflight/deploy 前用 planner 规范化 deployment name，若 configured model state 已存在则返回 `failureKind=model-already-exists`，不执行 SSH、不覆盖原 state；`PodVllmOperationResult`、JSON 和 text 输出新增 top-level `failureKind`，现有 deploy/stop failure 也会给出稳定分类。当前已通过 Pods build、focused gate 5/5、完整 `Tau.Pods.Tests` 207/207、`git diff --check` 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 207）。该切片只关闭 duplicate-start guard 与 operation failure kind baseline；上游 watcher 失败后的配置删除流程、exact `model_run.sh` wrapper、真实 SSH/HF/GPU/vLLM smoke 和多版本 rollback 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` remote process/log schema 缺口继续领取 startup log marker 子切片。对照上游 `packages/pods/src/commands/models.ts` 中 `startModel` 的 log watcher，`vllm health/status` 现在在 curl `/health` 不 ready 后优先扫描 `~/.vllm_logs/<name>.log` 最近日志：`Application startup complete` 会判定 ready，`Model runner exiting with code`、`Script exited with code`、`torch.OutOfMemoryError`、`CUDA out of memory` 和 engine initialization failure 会判定 unhealthy；旧 `.tau_pods` 日志仍作为 fallback。`ParseState` 也能从 raw startup marker 直接映射 ready/unhealthy。当前已通过 Pods build、focused gate 7/7、完整 `Tau.Pods.Tests` 205/205 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 205）；`git diff --check` 也已通过。该切片只关闭本地 startup marker 判定 baseline；deploy-time startup watcher、失败后 config 自动删除、exact `model_run.sh` wrapper、真实 SSH/HF/GPU/vLLM smoke 和多版本 rollback 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` remote process/log schema 缺口领取 vLLM log path 子切片。对照上游 `packages/pods/src/commands/models.ts` 与 `scripts/model_run.sh` 使用 `~/.vllm_logs/<name>.log` 的日志路径合同，`PodVllmCommandPlanner` 现在在 plan/metadata JSON 暴露 `logPath`，top-level `logs` 快照会先 tail 该上游路径再回退 journal/旧 `.tau_pods` 日志；后续 model runner wrapper baseline 已把实际 deploy 输出改为 wrapper 通过 `script` 写入同一路径，systemd 不再重复 append wrapper stdout/stderr。当前已通过 Pods build、focused gate 19/19、完整 `Tau.Pods.Tests` 204/204 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 204）；`git diff --check` 也已通过。该切片只关闭本地 upstream log path baseline；startup watcher、startup complete/failure line parsing、runner wrapper 已由后续独立 baseline 收口，真实 SSH/HF/GPU/vLLM smoke 和多版本 rollback 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` remote process state 缺口领取本地 PID writeback 子切片。对照上游 `packages/pods/src/commands/models.ts` 成功启动后保存 `pid` 的配置合同，`PodVllmOrchestrationService` 现在会让 systemd 路径 best-effort 查询 `MainPID`，fallback `nohup` 路径回显 `.pid` 文件中的进程号，并把 deploy stdout 中的 `pid=<n>` 解析到 `PodVllmOperationResult.ProcessId`；`PodsCli` JSON 输出新增 `processId`，`PodsConfigStore.ApplyVllmDeploymentResult` 在有正整数 PID 时写入 configured model `pid`，否则继续写 `0`。当前已通过 focused Pods gate 2/2、完整 `Tau.Pods.Tests` 204/204 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 204）；`git diff --check` 也已通过。该切片只关闭本地 deploy PID state writeback baseline；exact upstream `model_run.sh` wrapper、`~/.vllm_logs` startup streaming、真实 SSH/HF/GPU/vLLM smoke 和多版本 rollback 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` GPU allocation state 缺口领取本地合同子切片。对照上游 `packages/pods/src/commands/models.ts` 的 `selectGPUs` / `startModel` / `stopModel`，`PodVllmCommandPlanner` 现在基于 configured `pod.Models[*].Gpu` usage 选择 least-used GPU，成功 `vllm deploy` / top-level `start` 会写回 configured model 的 model/port/gpu state，成功 `vllm stop` / top-level `stop` / no-arg stop-all 会删除对应 state，stop-all 只删除成功停止的 deployment。当前已通过 focused Pods gate 29/29、完整 `Tau.Pods.Tests` 204/204 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 438、WebUi 44、Pods 204）。该切片只关闭本地 usage-aware allocation state baseline；`Pid=0`、exact upstream PID/log-tail/startup streaming、可用 pod-agent runtime、真实 SSH/HF/GPU/vLLM smoke 和多版本 rollback 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` command compatibility 剩余缺口领取 `shell` / `agent` prompt-mapping 子切片。对照上游 `packages/pods/src/cli.ts` 的 `pi shell [<name>]` 与 `packages/pods/src/commands/prompt.ts`，`PodsCli` 现在支持 `shell [--json] [--config path] [--pod id] [pod-id]`，解析 active/指定 pod 后通过无远端命令 SSH 进程打开 shell；同时支持 `agent [--config path] [--pod id] <name> [message/options...]` 的上游 prompt 参数映射 baseline，读取 `pod.models[modelName]`，构造 `--base-url` / `--model` / redacted `--api-key` / `--api` / `--system-prompt` 和 passthrough user args，并保持稳定 not-implemented failure，不泄漏 `PI_API_KEY`。当前已通过 `dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --filter "PodsCli" --no-restore --verbosity minimal` 89/89 和完整 `Tau.Pods.Tests` 201/201。该切片只关闭 shell 入口和 agent 参数映射合同；usage-aware GPU allocation state 已由后续独立子切片关闭本地合同，可用 pod-agent runtime、本地 PID/log-tail exact flow 和真实 SSH/HF/GPU/vLLM smoke 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 `Tau.Pods` command compatibility 剩余缺口继续领取 no-arg stop-all 子切片。对照上游 `packages/pods/src/cli.ts` / `commands/models.ts` 的 `pi stop [<name>]`，`PodsCli` 现在支持 `stop [--json] [--config path] [--pod id]` 无 deployment name：先列出 active/指定 pod deployments，再逐个复用现有 `vllm stop` 停止，并输出 `operation=stop-all` 聚合 JSON/text；`stop <name>`、`stop --pod <pod> <name>` 和旧 Tau 可消歧 positional stop 入口继续保留。当前已通过 `dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --filter "PodsCli" --no-restore --verbosity minimal` 86/86 和完整 `Tau.Pods.Tests` 198/198。该切片只关闭 no-arg stop-all 的本地合同；usage-aware GPU allocation state 已由后续独立子切片关闭本地合同，可用 pod-agent runtime、本地 PID/log-tail exact flow 和真实 SSH/HF/GPU/vLLM smoke 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 Phase 2 Candidate Queue 领取 `Tau.Pods` top-level command compatibility 子切片。对照上游 `packages/pods/src/cli.ts`，`PodsCli` 现在接受上游式 `pods` group（`pods` / `pods setup` / `pods active` / `pods remove`）、`start <model> --name <name>`、`list` 和 `stop <name>`；其中 `pods` group 复用现有配置列表/注册/active/remove 路径，`start` 映射到 `vllm deploy` 并强制上游 `--name` 要求，`list` 映射到 active/指定 pod 的 `deployments`，`stop <name>` 映射到 `vllm stop`。旧 Tau `deployments`、`vllm *` 和可消歧旧 stop 入口继续保留。当前已通过 `dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --filter "PodsCli" --no-restore --verbosity minimal` 85/85 和完整 `Tau.Pods.Tests` 197/197。该切片只关闭 top-level shim 的本地合同；无参 stop-all、shell/agent prompt mapping 和 usage-aware GPU allocation state 已由后续子切片关闭本地合同，本地 PID/log-tail exact flow 和真实 SSH/HF/GPU/vLLM smoke 仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 Phase 2 Candidate Queue 领取 `Tau.Pods` record-shaped config/schema 子切片。对照上游 `packages/pods/src/config.ts` / `types.ts`，`PodsConfigStore` 现在可读取上游 `pods: Record<string, Pod>` / `models: Record<string, Model>` 配置，保留原始 `ssh` command、GPU inventory、modelsPath、vLLM version 和每个 pod 的 configured model state；保存时写回上游 record shape，同时继续兼容旧 Tau list-shaped config load。当前已通过 `dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --no-restore --verbosity minimal` 192/192。该切片关闭本地 config shape/schema migration baseline；usage-aware GPU allocation state 已由后续独立子切片关闭本地合同，真实 SSH/HF/GPU/vLLM smoke 和 Pods final `verified` 状态仍保持 open。
- 本轮继续 `GOAL.md` 100% pi-mono parity 主线，从 Phase 2 Candidate Queue 领取 `Tau.Pods` config path/env compatibility 子切片。对照上游 `packages/pods/src/config.ts`，`PodsCli` 现在在没有显式 config path 时支持 `PI_CONFIG_DIR/pods.json` 和无 env 默认 `~/.pi/pods.json`；显式 `--config path` 与旧 positional config path 继续优先，help 文案更新为 `<config-dir>/pods.json`。`PodsConfigStore.Load` 缺文件返回空 `PodsConfig`，因此配置列表入口 `pods --json` / `validate --json` 对 missing default config 不再报 `Config not found`。该切片关闭 config path/env 与 missing-file 子合同；record-shaped schema/state migration 已由后续子切片关闭，真实 SSH/HF/GPU/vLLM smoke 仍保持 open。
- 本轮继续 `GOAL.md` Pass 0 状态校准：本轮开始时工作树在 `origin/main` 上保持干净，上游 package 目录仍为 `agent`、`ai`、`coding-agent`、`mom`、`pods`、`tui`、`web-ui`，没有新增未知 package。`next.md` 和 100% active plan 已从旧 dirty WIP 入口切到 Pass 0 -> Phase 2 Candidate Queue 路线；Pods matrix 已修正 stale 文字，明确本地 `--gpus` / `--memory` / `--context` planning 已完成，但 upstream usage-aware `pod.models[*].gpu` round-robin allocation、top-level `start` command compatibility、真实 SSH/HF/setup/GPU/vLLM smoke 仍保持 open。该轮是 docs-only 状态校准，不新增 runtime 能力。
- 本轮继续执行恢复后的 100% pi-mono parity 主线，从 Phase 2 Candidate Queue 领取 `Pods command/config/known-model compatibility` 切片。对照上游 `packages/pods/src/models.json`、`model-configs.ts` 和 `commands/models.ts`，`Tau.Pods` 新增随项目输出复制的 `src/Tau.Pods/Models/models.json`、`PodKnownModelRegistry` 和 source-gen JSON 类型，固定 known model list/name/config lookup、GPU type + GPU count fallback，并让 `PodVllmCommandPlanner` 在没有显式 `--vllm` extra args 时自动把匹配配置的 args/env 注入 `vllm plan/deploy` serve command、metadata 和 CLI JSON/text 输出；显式 `--vllm` 仍覆盖 known-model 自动配置。随后本地 option planning 子切片又补上 `--gpus` / `--memory` / `--context` 规划语义，写入 serve command、metadata、JSON/text 输出，并通过最新 Pods gate 108/108、完整 `Tau.Pods.Tests` 181/181 和项目级 `verify-dotnet.ps1 -SkipRestore`。后续本轮已补 top-level `start/list/stop` shim；该 known-model 切片本身仍不关闭 upstream round-robin GPU allocation、上游本地 PID/log-tail/startup streaming exact flow 或真实 SSH/HF/GPU/vLLM smoke。
- 本轮按用户明确要求把当前目标从已完成的 Agent Platform Baseline 恢复为所有能力 100% pi-mono parity。`GOAL.md` 已重新指向 `docs/exec-plans/active/2026-05-28-tau-100-percent-pi-mono-parity.md`，`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 继续作为 Phase 1 inventory freeze 和 Phase 2 Candidate Queue 的权威 backlog；`docs/exec-plans/completed/2026-06-07-tau-agent-platform-baseline.md` 只作为已完成前置能力和 shared Agent foundation，不再作为当前主线目标，也不代表完整 product parity 完成。后续质量评估重新以 product parity、真实外部 e2e、release/package/CI final closure、`next.md` parity backlog 清零和 matrix 全部 `verified` / 用户确认 `non-goal` 为标准；旧 dirty WIP 边界已经按 Agent platform baseline、`/settings select` UI parity 和 docs-only goal restore 分别收口，后续不再作为当前未提交边界处理。
- 本轮继续 Agent Platform Baseline 的首版运行时代码验收：`Tau.Agent` 新增 `src/Tau.Agent/Platform/**`，提供 `AgentApplication` / `AgentApplicationBuilder` / `AgentRunResult` / `DelegateAgentTool` / `AgentToolContext` / `IAgentSessionStore` / `AgentSessionSnapshot` / `InMemoryAgentSessionStore`。外部 .NET 应用现在可以用薄 builder 配置 provider registry、model、system prompt、delegate tools、session id、session store 和 log sink，运行 prompt 后读取 assistant text、final messages、usage、stop reason、error/cancel 状态、tool events 与 log context。`AgentApplication` 在失败/取消时不会保存 session，并会把内存 conversation 恢复到回合前消息，避免失败回合污染应用会话；`AgentRuntime` 现在会写 `provider/run.start|run.end` 摘要字段，覆盖 provider/model/api、message/tool count、transport、usage/cost、stopReason、failureKind、duration 和 correlation/session/message id，不写 prompt、headers、api key、metadata、完整 messages、tool arguments 或 tool result；tool runtime log 继续只写 argument/result bytes、failureKind、correlation/session/message id 等摘要字段，不写完整参数或结果。新增 `examples/Tau.Agent.ConsoleExample` 和 `examples/Tau.Agent.HttpExample`，并新增 `scripts/verify-agent-platform-examples.ps1`；该 smoke 已接入 `scripts/verify-dotnet.ps1 -RunSmoke`。本轮验收已通过 targeted 验证 `dotnet test tests\Tau.Agent.Tests\Tau.Agent.Tests.csproj --filter "AgentPublicApiCompileSampleTests|AgentPlatform" --no-restore --verbosity minimal` 5/5、example smoke、`Tau.Agent.Tests` 119/119、`Tau.Ai.Tests` 280/280、仓库级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 和 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke`。Agent platform baseline 的本地 completion audit 已收口；真实 provider/OAuth e2e、真实 NuGet/package registry 发布演练、真实 signing/provenance rehearsal 和后续 product parity 接回仍是长期后续边界，不因 fake provider/platform smoke 而关闭。
- 本轮曾按用户明确要求临时把主目标从 UI parity 切到 Tau Agent 应用底座，方便后续基于 Tau 搭建 Agent 应用。`GOAL.md` 当时从 `Tau 100% pi-mono parity /goal` 切换为 `Tau Agent Platform /goal`；`docs/exec-plans/completed/2026-06-07-tau-agent-platform-baseline.md` 已归档本次 Agent platform baseline 计划；`next.md` 顶部已记录 Agent platform P0 完成态；旧 `docs/exec-plans/active/2026-05-28-tau-100-percent-pi-mono-parity.md` 当时保留为长期 parity 审计路线。本次是计划和质量口径切换，不新增运行时代码能力；`/settings select` UI parity 后续已作为独立切片收口，不应混入 Agent SDK/API 提交边界。随后已进一步把 `GOAL.md` 补成可执行验收合同：记录 `Tau.Agent` / `Tau.Ai` 当前内核事实面、完成证据和后续路线，并在 completed plan 中记录 platform surface checklist 与 WP1-WP5 工作包；本次又把 current checkpoint 和第一版 API 骨架候选收紧为 WP1/WP2 可执行入口，默认优先实现 `AgentApplication` / `AgentApplicationBuilder` / `AgentRunResult` / `DelegateAgentTool` / `IAgentSessionStore` / `AgentSessionSnapshot` / `InMemoryAgentSessionStore` 或等价 UI-free 类型。当前主线已恢复为 100% product parity；Agent platform 仅作为 shared foundation。
- 本轮继续 CodingAgent `/settings select` product-level SettingsList adoption：对照上游 `packages/coding-agent/src/modes/interactive/components/settings-selector.ts` 和 `packages/tui/src/components/settings-list.ts`，把 `CodingAgentSettingsSelector` 的生产入口从 Tau-native 7 项 `TuiSelectList` 主菜单改为 `TuiSettingsList` 主设置面。当前可从 `/settings select` 直接写回 auto-compaction、terminal show images / clear on shrink、image auto-resize / block images、show hardware cursor、editor padding、autocomplete max visible、steering/follow-up mode、tree filter、thinking level、quiet startup、collapse changelog 和 install telemetry，并保留 scoped models / theme 既有子 selector 入口；旧 action-id selector payload 仍保留给既有注入测试和兼容路径。`Tau.Tui` 新增 `TuiSettingsListSession` 与 composition overlay runner，固定 settings-list change/cancel 的 session result。当前验证已通过 `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter "CodingAgentSettingsSelectorTests|TryHandleAsync_SettingsCommand" --no-restore --verbosity minimal` 9/9、`dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 251/251、`dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --no-restore --verbosity minimal` 438/438，以及项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 251、`Tau.CodingAgent.Tests` 438、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。完整 settings submenus、package/transport/shell path/npm settings、部分 persisted terminal/editor/image/changelog 字段的真实 runtime wiring、完整 focus stack/theme rendering 和真实 TTY/PTY smoke 仍保留为后续缺口。

## 最新增量（2026-06-06）

- 本轮继续 Tau.Tui settings-list component parity：对照上游 `packages/tui/src/components/settings-list.ts`，新增 `TuiSettingsList` / `TuiSettingItem` / `TuiSettingsListTheme` / `TuiSettingsListOptions`。当前覆盖 settings item 的 label/value 对齐、description wrapping、可选搜索、Enter/Space value cycle、submenu delegate / done callback、Esc/Ctrl-C cancel、滚动提示和宽度截断；`Tau.Tui.Tests` 新增 5 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 249/249，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 249、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭 `components/settings-list.ts` 的库层 foundation；完整 CodingAgent settings 产品面、images/terminal/transport/packages 等全量配置项、完整 focus stack、theme rendering 和真实 TTY/PTY smoke 仍保留为后续缺口。
- 本轮继续 Tau.Tui box component parity：对照上游 `packages/tui/src/components/box.ts`，扩展 `TuiBox`。当前覆盖 child rendering、水平/垂直 padding、optional background formatter、`SetBackgroundFormatter(...)` 运行期切换，以及 bg-sample/child-line cache invalidation；`Tau.Tui.Tests` 新增 1 个 targeted test，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 244/244，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 244、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭 `components/text.ts` / `truncated-text.ts` / `box.ts` / `spacer.ts` 组件组的本地库层 baseline，不关闭完整 TUI host/focus stack、theme rendering、硬件 cursor 或真实 TTY/PTY smoke。
- 本轮继续 Tau.Tui text/truncated-text component parity：对照上游 `packages/tui/src/components/text.ts` 与 `components/truncated-text.ts`，扩展 `TuiTextBlock` 并新增 `TuiTruncatedText`。当前覆盖 Text 的空白文本不渲染、tab-normalized wrap、水平/垂直 padding、full-line background formatter、`SetCustomBackgroundFormatter(...)` cache invalidation，以及 TruncatedText 的首行截断、padding、空文本仍输出 padded line 和 width-aware ellipsis；`TuiText` 新增 `ApplyBackgroundToLine(...)` helper 复用同一 visible-width padding 语义；`Tau.Tui.Tests` 新增 3 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 243/243，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 243、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭 `text.ts` / `truncated-text.ts` 的库层组件 baseline，不关闭 `box.ts` custom background/cache、完整 TUI host/focus stack、theme rendering、硬件 cursor 或真实 TTY/PTY smoke。
- 本轮继续 Tau.Tui spacer component parity：对照上游 `packages/tui/src/components/spacer.ts`，新增 `TuiSpacer`。当前覆盖默认 1 行空输出、`SetLines(...)` 动态更新、负数 line count 等价空输出，以及在 `TuiContainer` 中夹在其它组件之间的组合渲染；`Tau.Tui.Tests` 新增 3 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 240/240，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 240、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭独立 spacer component parity，不关闭 broader text/truncated-text exact widget parity、完整 TUI host/focus stack、硬件 cursor 或真实 TTY/PTY smoke。
- 本轮继续 Tau.Tui ProcessTerminal lifecycle foundation：对照上游 `packages/tui/src/terminal.ts`，新增 `TuiProcessTerminal`、`ITuiProcessTerminalTransport`、`ITuiTerminalTimer` 和 `TuiTerminalWriteLog`。当前覆盖 raw mode 保存/恢复、UTF-8 input、stdin resume/pause、bracketed paste enable/disable、resize subscription、Unix dimension refresh seam、Windows VT input seam、`TuiInputSequenceBuffer` 拆包转发与 paste re-wrap、Kitty keyboard protocol query/response/enable/disable、modifyOtherKeys fallback/disable、exit 前 input drain、ANSI move/cursor/clear/title 操作，以及 `TAU_TUI_WRITE_LOG` / `PI_TUI_WRITE_LOG` 诊断写日志路径（file 或 directory -> timestamped file，写日志失败不影响 terminal output）。`Tau.Tui.Tests` 新增 12 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 237/237，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 237、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭 `terminal.ts` 的本地 lifecycle seam，不关闭真实 TTY/PTY smoke、真实 Kitty/iTerm/Windows terminal 行为、硬件 cursor、完整 generic TUI app host 或 CodingAgent 主屏接管。
- 本轮继续 Tau.Tui stdin-buffer foundation：对照上游 `packages/tui/src/stdin-buffer.ts`，新增 `TuiInputSequenceBuffer`。当前覆盖普通字符拆分、跨 chunk CSI/SGR mouse buffering、OSC/DCS/APC string sequence 终止符识别、SS3/meta escape、高字节 meta 转换、bracketed paste content event 与 remainder 处理、manual flush、clear、timeout flush 和 destroy 后拒绝复用；`Tau.Tui.Tests` 新增 11 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 225/225，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 225、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭上游 `stdin-buffer.ts` 的库层输入序列缓冲 foundation，不关闭真实 `ProcessTerminal` raw mode、Kitty keyboard protocol enable/disable、modifyOtherKeys fallback、stdin drain、Windows VT input、硬件 cursor 或 live TTY/PTY smoke。
- 本轮继续 Tau.Tui terminal image foundation：对照上游 `packages/tui/src/terminal-image.ts` 与 `components/image.ts`，新增 `TuiTerminalImage`、`TuiImage`、terminal capability/image dimension/render option/result 类型和 image component options/theme。当前覆盖 tmux/screen/Kitty/Ghostty/WezTerm/iTerm2/VS Code/Alacritty/unknown capability detection、kitty single/chunked escape encoding、kitty delete escape、iTerm2 file escape、PNG/JPEG/GIF/WebP dimension sniffing、row calculation、protocol render、OSC 8 hyperlink、fallback text、image component protocol/fallback render 和 cache 行为；`Tau.Tui.Tests` 新增 8 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 214/214，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 214、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭 terminal image 与 image component 的库层 foundation，不关闭真实 Kitty/iTerm2 terminal smoke、完整 terminal lifecycle、CodingAgent tool image integration、硬件 cursor 或 TTY/PTY smoke。
- 本轮继续 Tau.Tui rich Markdown foundation：对照上游真实 `packages/tui/src/components/markdown.ts`，新增 `TuiMarkdown`、`TuiMarkdownTheme` 和 `TuiDefaultTextStyle`。当前覆盖 heading、paragraph、inline code/bold/italic/strike/link、fenced code、list、table、blockquote、horizontal rule、padding/background/cache、terminal image escape line no-wrap 和可选 OSC 8 hyperlink 输出；`Tau.Tui.Tests` 新增 8 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 206/206，`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 也通过，计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 206、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166。本切片关闭 Markdown 组件库层 foundation，不关闭 terminal image、Marked exact tokenizer、syntax highlight/theme integration、完整 TUI host/focus stack、硬件 cursor 或真实 TTY/PTY smoke。
- 本轮继续 Tau.Tui loader/cancellable-loader foundation：对照上游 `packages/tui/src/components/loader.ts` 与 `components/cancellable-loader.ts`，新增 `TuiLoader` 和 `TuiCancellableLoader`。`TuiLoader` 固定上游 10 帧 braille spinner、默认 `Loading...` message、前置空行、message 更新、formatter hook、可选 80ms timer 和可注入 render request callback；同时提供手动 `AdvanceFrame()` 以便 deterministic tests。`TuiCancellableLoader` 在 `Escape` / `Ctrl+C` 上触发 abort signal 与单次 abort callback，并复用 loader render/stop/dispose 行为。`Tau.Tui.Tests` 新增 8 个 targeted tests，当前 `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal` 通过 198/198。本切片关闭 loader / cancellable-loader 库层 foundation，不关闭 terminal image、rich markdown、full TUI app host/focus stack、硬件 cursor 或真实 TTY/PTY smoke。
- 本轮继续 root / AI test utility parity：新增 `scripts/generate-ai-test-image.ps1` 与 `scripts/verify-ai-test-image.ps1`。对照上游 `packages/ai/scripts/generate-test-image.ts`，Tau 现在有 PowerShell-first 的 200x200 白底红圆 PNG fixture generator；默认输出 `tests/Tau.Ai.Tests/Data/red-circle.png`，也支持 `-OutputPath` 临时路径和 `-Json` 摘要。实现不依赖 node-canvas 或 System.Drawing，而是纯 PowerShell 写 PNG chunks 与 zlib stored blocks。`verify-ai-test-image.ps1` 在临时目录生成 fixture，并解析 PNG signature/chunks、IHDR、zlib/adler32、scanline filter 和关键像素，当前 30 assertions 通过；`plan-release.ps1`、`verify-release-contracts.ps1` 与 `.github/workflows/tau-ci.yml` 已纳入 `ai-test-image-smoke`。本切片关闭上游 AI test image fixture generator 的 Tau-native 本地 smoke baseline，不关闭 broader image/clipboard ingestion、provider vision e2e 或 upstream canvas antialias byte-for-byte parity。
- 本轮继续 Phase 5 package signing/provenance hardening baseline：新增 `scripts/generate-release-provenance.ps1`、`scripts/sign-release-packages.ps1` 与 `scripts/verify-release-provenance.ps1`。`generate-release-provenance.ps1` 默认 dry-run，扫描 release archives 与 NuGet packages，记录 git commit/branch/worktree、MSBuild version source、每个制品的 path、format、size 和 SHA256；显式 `-Apply` 才写入 `artifacts/provenance/release-provenance.json`，默认要求 clean worktree。`sign-release-packages.ps1` 默认 dry-run，按 `dotnet nuget sign` 预览 NuGet package signing；显式 `-Apply` 才执行，证书必须通过 `-CertificatePath` 或 `-CertificateFingerprint` 指定，timestamp/hash/output/overwrite 可配置，证书密码只从指定 env 读取且 JSON、命令和子进程 output preview 都脱敏。`verify-release-provenance.ps1` 用临时 Git 仓库、fixture archive/package 和 fake `dotnet.ps1` 固定 provenance JSON、SHA256/size/path、symbols 包排除、dirty apply 阻断、signing dry-run command shape、证书路径/指纹入口、timestamp 参数、fake signing apply、无证书 apply 阻断和密码不泄漏，当前 34 assertions 通过。`plan-release.ps1`、`verify-release-contracts.ps1` 与 `.github/workflows/tau-ci.yml` 已纳入 `release-provenance-smoke`。本切片关闭本地 provenance manifest 与 NuGet signing guarded smoke baseline，不关闭真实 code-signing certificate rehearsal、真实 NuGet/package registry 发布演练、release archive signing、外部供应链 attestation、非宿主 executable smoke 或真实外部 e2e release smoke。

## 最新增量（2026-06-03）

- 本轮继续 Phase 5 release finalization parity：新增 `scripts/finalize-release.ps1` 与 `scripts/verify-release-finalize.ps1`，对照上游 `scripts/release.mjs` 后段 `npm run publish` / `git push origin main` / `git push origin v<version>`，把 Tau 的本地 release commit/tag 后续远端步骤拆成受控 finalization 层。`finalize-release.ps1` 默认 dry-run，只预览 branch/tag push 和可选 GitHub Release archive upload；显式 `-Apply` 才在 release tag 格式、git 可用性、clean worktree、remote、branch、local tag、tag 指向 branch tip、release archive 存在等预检通过后执行 `git push <remote> <branch>` 与 `git push <remote> <tag>`。显式 `-CreateGitHubRelease` 时额外要求 release notes 与 `gh` CLI，并用已验证 archives 调用 `gh release create`；`-Draft` / `-Prerelease` 在 dry-run 预览和实际 fake-gh 执行中都固定为 `--draft` / `--prerelease`。`verify-release-finalize.ps1` 使用临时 Git 仓库、临时 bare remote、fake archive 和 fake `gh.cmd` 固定 dry-run JSON、真实 temp remote branch/tag push、GitHub Release create 参数、dirty worktree apply 阻断和 draft/prerelease flag 一致性，当前 22 assertions 通过；`verify-release-contracts.ps1` 已纳入该 smoke，当前 46 assertions 通过；`.github/workflows/tau-ci.yml` 也独立运行 release finalize smoke。本切片关闭 guarded branch/tag push 与 GitHub Release archive upload baseline；真实远端发布演练、非宿主平台 executable smoke、真实外部 e2e release smoke 或上游第二个 `[Unreleased]` changelog commit 形态仍缺。
- 本轮继续 Phase 5 package registry publish synchronization baseline：新增 `scripts/publish-release-packages.ps1` 与 `scripts/verify-release-package-publish.ps1`，把上游 `npm run publish` 对应的 registry publish 段拆成 Tau-native 的受控 NuGet/package publish 层。`publish-release-packages.ps1` 默认 dry-run，只预览 `dotnet pack` 与 `dotnet nuget push`；显式 `-Apply` 才执行。默认项目边界只覆盖 `Tau.Ai`、`Tau.Agent`、`Tau.Tui` 三个库包，应用项目继续默认通过 release archives 交付，显式传入应用项目时会给出 warning。脚本从 `Directory.Build.props` 读取版本，默认输出到 `artifacts/nuget`，默认 NuGet v3 source，只从 `-ApiKeyEnv` 指定的环境变量读取 API key；JSON、planned command、command result 和子进程 output preview 只报告 env 名称与 present 状态，不回显密钥值。`verify-release-package-publish.ps1` 使用临时 Git 仓库、临时 library/app project fixture 和 fake `dotnet.ps1` 固定默认库包边界、dry-run JSON、pack/push 参数、API key redaction、子进程 output preview 脱敏、应用项目 warning 和 dirty worktree apply 阻断，当前 21 assertions 通过；`verify-release-contracts.ps1` 和 `.github/workflows/tau-ci.yml` 已纳入该 smoke。本切片关闭 guarded package publish dry-run/apply contract baseline，不关闭真实 NuGet/package registry 发布演练、应用项目包策略决策、symbol package publishing、真实 package signing/provenance rehearsal 或真实外部 e2e release smoke。

## 最新增量（2026-05-30）
- 本轮继续 CodingAgent auth migration helper parity：新增 `scripts/migrate-coding-agent-auth.ps1` 与 `scripts/verify-coding-agent-auth-migration.ps1`，对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateAuthToAuthJson()` 职责，把 legacy `oauth.json` provider credentials 与 `settings.json.apiKeys` 迁移到 `auth.json`。Tau 版本默认使用用户 `~/.tau`，也支持显式 `-AgentDirectory` / `-AuthPath` / `-OAuthPath` / `-SettingsPath`；默认 dry-run，显式 `-Apply` 才写 `auth.json`、把 `oauth.json` rename 为 `oauth.json.migrated` 并从 `settings.json` 删除 `apiKeys`；`auth.json` 已存在时跳过所有 legacy 文件；OAuth 与 api key 同 provider 冲突时 OAuth 优先；JSON 输出只报告 provider 与 credential kind，不回显 secret。fixture smoke 固定 dry-run、apply、二次 apply 幂等、existing auth skip、invalid oauth/settings skip、OAuth winner、secret redaction 和 remaining gap audit，当前 25 assertions 通过。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `coding-agent-auth-migration-smoke`。本切片只关闭 legacy `oauth.json` + `settings.json.apiKeys` -> `auth.json` helper baseline，不迁移 Tau runtime `coding-agent-settings.json`，也不关闭 full settings/runtime auth parity、credential refresh UX、actual hooks/custom tools migration 或真实 OAuth e2e。

## 最新增量（2026-05-29）
- 本轮继续 CodingAgent keybindings migration helper parity：新增 `scripts/migrate-coding-agent-keybindings.ps1` 与 `scripts/verify-coding-agent-keybindings-migration.ps1`，对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateKeybindingsConfigFile()` / `core/keybindings.ts` `KEYBINDING_NAME_MIGRATIONS` 职责，把 upstream object-style `keybindings.json` 中的旧键名迁移为 canonical `tui.*` / `app.*` keybinding ids。Tau 版本默认使用用户 `~/.tau/keybindings.json`，也支持显式 `-AgentDirectory` / `-KeybindingsPath`；默认 dry-run，显式 `-Apply` 才写回；canonical 键已存在时保留 canonical 值并丢弃冲突 legacy 键；未知 custom 键保留；invalid JSON、非 object JSON、缺失文件、目录 source 都跳过而不误写。fixture smoke 固定 dry-run、apply、冲突 canonical winner、custom key preserved、Tau array-style `coding-agent-keybindings.json` no-op、invalid/missing skip 和二次 apply 幂等，当前 23 assertions 通过。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `coding-agent-keybindings-migration-smoke`。本切片只关闭 legacy keybinding id migration helper baseline，不转换 Tau 当前 array-style runtime keybinding 文件，也不关闭 auth/settings migration、legacy `oauth.json` migration、actual hooks/custom tools migration to `extensions/`、package/extension shortcut migration 或完整 keybinding runtime parity。
- 本轮继续 CodingAgent deprecated extension dirs audit helper parity：新增 `scripts/audit-coding-agent-deprecated-extension-dirs.ps1` 与 `scripts/verify-coding-agent-deprecated-extension-dirs-audit.ps1`，对照上游 `packages/coding-agent/src/migrations.ts` 的 `checkDeprecatedExtensionDirs(...)` warning audit，默认检查用户 `~/.tau` 与当前项目 `./.tau`，也支持显式 `-BaseDirectory` / `-Label`。脚本只审计不改文件：发现 `hooks/` 时报告 hooks renamed warning；发现 `tools/` 目录中除 managed `fd`、`rg`、`fd.exe`、`rg.exe` 和隐藏 entry 以外的 custom entries 时报告 custom tools merged warning；`tools` 是文件或不可读时不产生 custom tools warning。fixture smoke 固定 hooks warning、custom tools warning、managed binary ignore、hidden entry ignore、file tools path skip、missing base 和 no-mutation boundary，当前 20 assertions 通过。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `coding-agent-deprecated-extension-dirs-audit-smoke`。本切片只关闭 deprecated hooks/tools warning audit helper baseline，不迁移 hooks 或 custom tools 到 `extensions/`，也不关闭 auth/settings/session/keybindings migrations、general custom tool migration parity 或 TypeScript extension runtime。
- 本轮继续 CodingAgent tools-to-bin migration helper parity：新增 `scripts/migrate-coding-agent-tools-to-bin.ps1` 与 `scripts/verify-coding-agent-tools-to-bin-migration.ps1`，对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateToolsToBin()` 职责，只扫描 agent `tools/` 下的 managed binaries `fd`、`rg`、`fd.exe`、`rg.exe`。Tau 版本默认使用用户 `~/.tau`，也支持显式 `-AgentDirectory`；默认 dry-run，显式 `-Apply` 才创建 `bin/` 并 move，目标 `bin/<name>` 已存在时删除旧 `tools/<name>` 副本，不覆盖目标；source 不存在、`tools/` 不存在或 source 是目录时跳过；非托管 custom tools 保持不动。fixture smoke 固定 dry-run、apply、duplicate removal、content preservation、custom tool preserved、directory source skip、missing tools dir、二次 apply 幂等和 remaining gap audit，当前 25 assertions 通过。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `coding-agent-tools-to-bin-migration-smoke`。本切片只关闭 managed fd/rg binary relocation helper baseline，不关闭 auth/settings/session/keybindings migrations、actual hooks/custom tools migration to extensions 或 general custom tool migration parity。
- 本轮继续 CodingAgent commands-to-prompts migration helper parity：新增 `scripts/migrate-coding-agent-commands.ps1` 与 `scripts/verify-coding-agent-commands-migration.ps1`，对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateCommandsToPrompts(...)` / `migrateExtensionSystem(...)` 职责，把 configured base directory 下旧 `commands/` 在 `prompts/` 不存在时 rename 为 `prompts/`。Tau 版本默认检查用户 `~/.tau` 与当前项目 `./.tau`，也支持显式 `-BaseDirectory`；默认 dry-run，显式 `-Apply` 才执行 rename；`prompts/` 已存在、没有 `commands/`、base directory 不存在或 `commands` 不是目录时跳过；`-Json` 输出 base directory summary。fixture smoke 固定 dry-run 不移动、apply rename、content preserved、target-exists/source preserved、no-commands、file source、missing base、二次 apply 幂等和 remaining gap audit，当前 22 assertions 通过。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `coding-agent-commands-migration-smoke`。本切片只关闭 legacy commands-to-prompts helper baseline，不关闭 auth/settings/session/keybindings/tools-to-bin migration、actual hooks/custom tools migration to extensions 或 general custom tool migration parity。
- 本轮继续 CodingAgent session migration helper parity：新增 `scripts/migrate-coding-agent-sessions.ps1` 与 `scripts/verify-coding-agent-session-migration.ps1`，对照上游 `packages/coding-agent/scripts/migrate-sessions.sh` 与 `migrations.ts` 的 misplaced root JSONL relocation 职责，把 agent 根目录直接子 `.jsonl` 中第一行 `type=session` 且带 `cwd` 的 session，按上游 cwd 编码规则搬到 Tau 当前 `/clone` / `/resume` 可发现的 `coding-agent-sessions/<encoded-cwd>/<filename>`。Tau 版本默认 dry-run，显式 `-Apply` 才移动文件；目标已存在、坏 JSON、无 `cwd`、非 session header 和 nested JSONL 均跳过；`-Json` 输出 scan/file summary。fixture smoke 固定 dry-run 不移动、apply 迁移、Windows cwd 编码、target conflict、坏 header 跳过、nested 不扫描和二次 apply 幂等。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `coding-agent-session-migration-smoke`。本切片只关闭 Tau-native 可发现的 root JSONL relocation helper，不关闭 upstream auth/settings/extensions/keybindings/tools migrations 或 exact session schema parity。
- 本轮继续 Mom timestamp migration helper parity：新增 `scripts/migrate-mom-timestamps.ps1` 与 `scripts/verify-mom-timestamp-migration.ps1`，对照上游 `packages/mom/scripts/migrate-timestamps.ts` 的本地 `log.jsonl` timestamp 迁移职责，把 `<data-dir>/<channel>/log.jsonl` 中历史毫秒 Unix timestamp 转成 Slack `seconds.microseconds` 形态。Tau 版本默认 dry-run，显式 `-Apply` 才写回；`-Json` 输出 scan/migration summary；fixture smoke 固定 dry-run 不写入、apply 迁移、已有 Slack timestamp 保留、malformed 行保留和二次 apply 幂等。为保护 Tau 当前本地 bot log 语义，脚本不会迁移带 `-bot` 等后缀的 Tau-native timestamp。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `mom-timestamp-migration-smoke`。本切片关闭 Mom timestamp migration helper baseline，不关闭真实 Slack smoke、Slack session sync 或 Docker sandbox smoke。
- 本轮继续 Phase 5 edit tool stats script parity：新增 `scripts/report-edit-tool-stats.ps1` 与 `scripts/verify-edit-tool-stats.ps1`，对照上游 `scripts/edit-tool-stats.mjs` 的 JSONL edit tool usage audit 建立 Tau-native 等价。脚本默认扫描 Tau JSONL session，识别 Tau `edit_file` 与上游 `edit` tool call/result，统计成功率、失败类型、single/multi-edit、参数风格、provider/model、扩展名、same-file cluster、上下文 inflation、巨大 replacement 和 worst examples；支持 `-Json`、`-IncludeRecords`、`-FailedOnly`、`-Model`、`-Extension`、`-Since` 与显式 `-SessionPath` / `-SessionsDirectory`。fixture smoke 固定 Tau/upstream 两种工具名、multi-edit、not_found_exact_text 分类、same-file cluster、过滤和坏 JSONL 行跳过；`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `edit-tool-stats-smoke`。本切片关闭 root edit stats audit baseline，不改变 edit tool runtime，也不关闭真实 provider/tool e2e。
- 本轮继续 Phase 5 release version sync parity：新增 `scripts/sync-release-versions.ps1` 与 `scripts/verify-release-version-sync.ps1`，对照上游 `scripts/sync-versions.js` 的 lockstep version audit 建立 Tau-native 等价。上游脚本同步 `packages/*/package.json` 的版本和内部 `@mariozechner/*` dependency ranges；Tau 没有 npm workspace，版本事实源收敛为 `Directory.Build.props` 的单一 MSBuild `VersionPrefix=0.1.0`，`src/**/*.csproj` 默认继承该版本。新脚本会审计显式项目版本属性是否漂移，dry-run 发现漂移时返回非零，`-Apply` 可把显式项目版本同步到中心版本；smoke 用临时 fixture 固定 drift detection、ProjectReference 审计和 apply 修复，同时验证当前仓库 8 个 src 项目无漂移。`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `release-version-sync-smoke`。本切片关闭 Tau 的版本同步/漂移审计 baseline，不关闭 NuGet/package publish synchronization 或 publish/push automation。
- 本轮继续 Phase 5 CodingAgent startup profile script parity：新增 `scripts/profile-coding-agent-startup.ps1` 与 `scripts/verify-coding-agent-startup-profile.ps1`，对照上游 `scripts/profile-coding-agent-node.mjs` 的 RPC startup benchmark 建立 Tau PowerShell-first 等价。Profiler 运行 `dotnet <Tau.CodingAgent.dll> --mode rpc --no-context-files --no-themes`，在隔离 Tau auth/models/session/settings/history/keybindings/log 路径下发送 `get_state` JSONL，计时到 successful response 并输出 min/median/average/max、`METRIC startup_time_ms=<median>` 或 JSON summary。脚本用无 BOM JSONL input file + shell redirection 规避 Windows PowerShell/.NET redirected stdin BOM 破坏 RPC JSONL 的问题；smoke 固定 warmup/measured run shape、positive median、assembly path 和 remaining gap audit。`plan-release.ps1`、`verify-release-contracts.ps1` 与 `.github/workflows/tau-ci.yml` 已纳入 `coding-agent-startup-profile-smoke`。本切片只关闭 RPC startup profiler baseline，不关闭上游 TUI first-frame startup timing、CPU profile、Node/Bun runtime 对比或 `PI_STARTUP_BENCHMARK` 等价 hook。
- 本轮继续 CodingAgent assistant usage cost persistence：`Usage` 现在可携带可选 `UsageCost`，`AgentRuntime` 在 provider done/error/failure assistant message 进入 state 和事件前补齐 provider/model/api/timestamp，并在模型有 pricing 时按 `ModelCatalog.CalculateCost` 写入 `usage.cost`；`CodingAgentSessionStore` 会把 assistant usage、cost、provider/model/api/timestamp 持久化到 flat session 和 JSONL tree session message。`scripts/report-session-costs.ps1` 的 no-cost warning 已改为只说明不反推 older sessions / missing persisted cost，`verify-session-audit-scripts.ps1` 的固定 fixture 日期窗口也已修正，避免 smoke 随时间过期。当前已通过 focused Agent/Proxy gate 18/18、focused CodingAgent session/tree gate 10/10、session audit script smoke 15 assertions、完整 Agent 120/120、完整 CodingAgent 480/480 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 120、Tui 251、CodingAgent 480、WebUi 61、Pods 216）。本切片关闭本地 `message.usage.cost` 默认持久化，不关闭真实 provider/e2e cost samples、usage cost UI 展示、older-session backfill 或完整 share/export transcript parity。
- 本轮继续 Phase 5 guarded local release execution baseline：新增 `scripts/execute-release.ps1`，对照上游 `scripts/release.mjs` 的本地 version/notes/commit/tag 执行段建立受控 PowerShell 流程。脚本默认 dry-run；显式 `-Apply` 且开始时工作树干净才执行 `verify-release-contracts.ps1`、`prepare-release.ps1 -Apply`、可选 `validate-release.ps1 -Run -AllowDirty`，随后只 stage `Directory.Build.props` 与 `docs/releases/feature-release-notes.md`，创建 `Release v<version>` commit 并打 `v<version>` tag；同名 tag、dirty worktree 或 preparation 产生额外变更都会阻断。`plan-release.ps1` 已把 `execute-release.ps1` 列入 required scripts 和 planned commands，`verify-release-contracts.ps1` 已把 execute dry-run contract 纳入短 smoke。本切片仍不 publish、不 push，不声明非宿主 executable smoke、真实外部 e2e release smoke、examples/Photon/interactive raster assets/external export-html payload parity 或 exact Unix wrapper/auth-backup parity 完成；Tau 也不生成上游第二个 `Add [Unreleased] section` commit，因为当前 release notes 是日期表格而非每包 CHANGELOG section。
- 本轮继续 Phase 5 release dry-run contract smoke baseline：新增 `scripts/verify-release-contracts.ps1`，只调用 `plan-release.ps1`、`prepare-release.ps1` 与 `validate-release.ps1` 的 `-Json` dry-run 输出，不跑 `dotnet build/test`，不生成 artifact，也不写版本或 release notes。脚本固定 dry-run boundary、下一版本、planned command names、non-executed commit/tag/publish/push mutation boundary、preparation changed files 和 validation coverage metadata；`validate-release.ps1` 现在会输出 `validationLevel`、enabled/skipped validation count 与 validation name 列表，并在同时跳过 no-env 和 matrix 时给出 coverage warning，避免把只跑 `git diff --check` 误读成完整 release validation。CI 已在长 no-env/release artifact 步骤前运行该短 smoke。本切片仍不执行 release preparation apply、commit、tag、publish、push、真实外部 e2e 或非宿主 executable smoke。
- 本轮继续 Phase 5 guarded release validation baseline：新增 `scripts/validate-release.ps1`，把 `git diff --check`、`verify-no-env.ps1` 和 `build-release-matrix.ps1` 串成一个本地 release validation flow。脚本默认 dry-run，只列命令；显式 `-Run` 才执行本地验证，且默认要求工作树干净。`scripts/plan-release.ps1` 现在把该 validation flow 纳入 required scripts 和 planned commands。本切片仍不执行 `prepare-release.ps1 -Apply`，不修改版本或 release notes，不声明 release commit/tag、publish、push、真实外部 e2e 或非宿主 executable smoke 完成。
- 本轮继续 Phase 5 guarded release preparation baseline：新增 `scripts/prepare-release.ps1`，对照上游 `scripts/release.mjs` 的 version + changelog 准备段，把现有 `update-release-version.ps1` 和 `update-release-notes.ps1` 串成一个本地 release preparation flow。脚本默认 dry-run；显式 `-Apply` 且工作树干净时才写回 `Directory.Build.props` 和 `docs/releases/feature-release-notes.md`；`-Apply` 前会先执行两个 helper 的 dry-run 预检，降低半写入风险。`scripts/plan-release.ps1` 现在把该 preparation flow 纳入 required scripts 和 planned commands。本切片仍不声明 no-env gate、release matrix build/package、release commit/tag、publish、push、非宿主 executable smoke 或真实外部 e2e release smoke 完成。
- 本轮继续 Phase 5 release notes mutation helper baseline：新增 `scripts/update-release-notes.ps1`，读取 `docs/releases/feature-release-notes.md`，按 `v<version>`、日期、功能域、用户价值和摘要生成当前月份 release notes 表格行；默认 dry-run，仅显式 `-Apply` 时写回 release notes。`scripts/plan-release.ps1` 的 planned commands 现在同时列出 version update preview 和 release notes update preview，非执行 mutation 也明确指向未来 release execution flow 中的显式 `-Apply`。本切片关闭 release notes helper baseline，不声明 release commit/tag/publish/push execution automation 完成。
- 本轮继续 Phase 5 repo-owned version source baseline：`Directory.Build.props` 现在定义 Tau 产品版本 `VersionPrefix=0.1.0`，`scripts/plan-release.ps1` 可以直接从 MSBuild version source 计算 `major|minor|patch` 下一版本，不再需要常规传 `-CurrentVersion`。`scripts/build-release-artifacts.ps1` 会把同一版本和版本来源写入 release `manifest.json`，`scripts/smoke-release-artifacts.ps1` 会验证 manifest version 与 `Directory.Build.props` 一致。本切片关闭 repo-owned version source + release manifest version audit baseline，不声明真实 version bump 写回、release notes 修改、commit/tag/publish/push execution automation、非宿主 executable smoke 或真实外部 e2e release smoke 完成。
- 本轮继续 Phase 5 version writeback preview baseline：新增 `scripts/update-release-version.ps1`，读取 `Directory.Build.props` 中唯一的 `Version` / `VersionPrefix` / `PackageVersion`，计算 `major|minor|patch` 或显式 `x.y.z` 下一版本；默认 dry-run，仅显式 `-Apply` 时写回该 MSBuild 属性。`scripts/plan-release.ps1` 的 planned commands 现在列出同一脚本的 dry-run 命令，非执行 mutation 改为未来 release execution flow 中显式 `-Apply`。本切片关闭 version writeback helper baseline，不声明 release notes、commit/tag/publish/push execution automation 完成。
- 本轮继续 Phase 5 release automation dry-run baseline：新增 `scripts/plan-release.ps1`，对照上游 `scripts/release.mjs` 的 clean worktree、version bump / explicit semver、changelog release section、commit/tag、publish 和 push 流程，生成 Tau 的 dry-run 发布计划。脚本会检查 git worktree、release notes、release scripts，读取 `Directory.Build.props` 的 repo-owned `VersionPrefix`，计算 `major|minor|patch` 或显式 `x.y.z` 的下一版本，并列出 no-env gate、release matrix build/package 命令；同时明确列出不会执行的版本写入、release notes 修改、history/commit/tag/publish/push 操作。本切片关闭的是 release planning / dry-run audit baseline，不声明 version/changelog/tag/publish execution automation、真实发布执行、非宿主 executable smoke 或真实外部 e2e release smoke 完成。

- 本轮继续 Phase 5 release payload manifest baseline：对照上游 `scripts/build-binaries.sh` 在每个平台目录复制 `package.json`、`README.md`、`CHANGELOG.md`、Photon wasm、theme JSON、interactive assets、export-html、docs、examples 和 Windows koffi native module 的清单，`scripts/build-release-artifacts.ps1` 现在会把当前 Tau 的 `README.md`、`LICENSE` 和完整 `docs/` 复制进 `artifacts/tau-<rid>/`，并在 `manifest.json.releasePayload` 中审计每个 release payload 的 Tau 状态。`changelog` 记录为 Tau-native `docs/releases/feature-release-notes.md`，`package-json` 记录为 Tau-native `manifest.json`，`theme` 与 `export-html` 记录为编译进 `CodingAgentThemeStore` / `CodingAgentHtmlSessionExporter` 的 inline 实现；当前没有 root `examples/`、Photon wasm/image resize pipeline、交互式 raster assets 或 Bun/koffi native module 依赖，因此以 `missing` / `not-applicable` 留在 manifest 中。`scripts/smoke-release-artifacts.ps1` 已把这些 manifest entry 和 `docs/releases/feature-release-notes.md` 纳入 artifact smoke。本切片关闭的是当前 Tau docs payload copy + release payload audit baseline，不声明 examples、Photon image pipeline、interactive raster assets、external export-html vendor/template、非宿主 executable smoke、version/tag/publish automation 或真实外部 e2e 完成。
- 本轮继续 Phase 5 cross-platform archive matrix baseline：`scripts/package-release-artifacts.ps1` 新增 `-ArchiveFormat auto|zip|tar.gz` 与宿主 RID smoke 判定，`auto` 对齐上游 `build-binaries.sh` 的格式规则：Windows RID 生成 zip，Linux/macOS RID 生成 tar.gz；归档后统一解压到 clean temp 校验 `tau-<rid>` 顶层目录，只有 archive RID 等于宿主 RID 时默认运行 executable smoke，非宿主 RID 只做解压结构校验，除非显式 `-ForceSmoke`。新增 `scripts/package-release-matrix.ps1` 用于批量归档已存在的 `artifacts/tau-<rid>/`，新增 `scripts/build-release-matrix.ps1` 用于按 `osx-arm64/osx-x64/linux-x64/linux-arm64/win-x64` 逐个 restore/build/package。本切片把 Phase 5 从 current-RID zip baseline 推进到上游风格 RID/格式矩阵 baseline；仍不声明非宿主平台已完成本机 executable smoke、Unix wrapper 权限语义已完全验证、examples/Photon/interactive raster assets/external export-html payload parity、version/changelog/tag/publish automation 或真实外部 e2e release smoke 完成。
- 本轮继续 Phase 5 CI/release artifact baseline：新增 `.github/workflows/tau-ci.yml`，把当前权威 Windows PowerShell 链路接到 GitHub Actions。workflow 在 `push main`、`pull_request` 和手动触发时运行，按 `global.json` 安装 .NET SDK，先 `dotnet restore Tau.slnx`，再执行 `scripts/verify-no-env.ps1 -SkipRestore -RunSmoke`，随后构建 Release artifact、打包 `artifacts/releases/tau-win-x64.zip`，并在上传前由 `package-release-artifacts.ps1` 解压到 clean temp 后复用 artifact smoke 验证。该切片关闭 Windows current-RID CI + release zip artifact upload baseline；仍不声明全平台 archive matrix、Unix wrapper parity、exact auth-backup parity、version/changelog/tag/publish automation 或真实外部 e2e release smoke 完成。
- 本轮继续 Phase 5 release archive baseline：新增 `scripts/package-release-artifacts.ps1`，对照上游 `scripts/build-binaries.sh` 的“生成归档并解压测试”职责，为当前 RID artifact 生成 `artifacts/releases/tau-<rid>.zip`，并解压到全新临时目录后复用 `scripts/smoke-release-artifacts.ps1` 验证 extracted copy。该脚本只消费已经存在的 `artifacts/tau-<rid>/`，校验 `manifest.json`，保留 `tau-<rid>` 顶层目录进 zip，使用 .NET `System.IO.Compression` 避免依赖外部 `zip`，并在删除临时解压目录前确认路径位于系统 temp 根下。当前关闭的是 current-RID zip archive + extraction smoke baseline；仍不声明全平台 `tar.gz/zip` 矩阵、Unix wrapper parity、exact auth-backup parity、version/changelog/tag/publish automation、CI 接入或真实外部 e2e release smoke 完成。
- 本轮继续 Phase 5 `pi-test.sh --no-env` wrapper parity：新增 `scripts/pi-test.ps1`，作为 PowerShell-first 的 CodingAgent 直达 wrapper。默认路径直接运行 `Tau.CodingAgent`；`--no-env` 时复用 `scripts/invoke-no-env.ps1`，只对子进程清理 provider/AWS/Azure/HF/Slack/Tau auth-config env，并把 Tau auth/models/session/settings/history/keybindings/log 指到临时状态目录；`--input-file` 通过 shell redirection 传 stdin，避免 Windows PowerShell / .NET 写 stdin 时引入 BOM 破坏 RPC JSONL；`--argument-list-base64` 给验证脚本提供不依赖 PowerShell `--` 透传的参数数组入口。`verify-no-env.ps1 -RunSmoke` 的 CodingAgent RPC smoke 现在实际通过 `pi-test.ps1 --no-env --no-build` 运行，从而把 no-env gate 与上游 `pi-test.sh` 的“直达 CodingAgent CLI”职责接起来。该切片仍不移动或备份用户真实 `auth.json`，也不声明 Unix shell wrapper、exact auth-backup parity、CI 接入或 release automation 完成。

## 最新增量（2026-05-28）

- 本轮继续 Phase 5 no-env wrapper baseline：新增 `scripts/invoke-no-env.ps1` 和 `scripts/verify-no-env.ps1`，对照上游 root `test.sh` / `pi-test.sh --no-env` 的隔离意图，建立 PowerShell-first 子进程环境清理入口。`invoke-no-env.ps1` 使用 `ProcessStartInfo.EnvironmentVariables` 只对子进程清除 OpenAI / Anthropic / Google / Azure / AWS / HF / OpenAI-compatible aliases / Slack / Tau auth-config-session-log 相关变量，设置 `PI_NO_LOCAL_LLM=1` 与 `TAU_NO_LOCAL_LLM=1`，并可把 `TAU_AUTH_FILE`、`TAU_MODELS_FILE`、CodingAgent session/settings/history/keybindings 和 `TAU_LOG_FILE` 指到临时目录；dry-run 只列变量名，不回显 secret 值；input file 通过 shell redirection 进入子进程，避免 Windows PowerShell / .NET stdin BOM 破坏 RPC JSONL。`verify-no-env.ps1 -SkipRestore -RunSmoke` 已在隔离环境真实运行全仓 gate 并通过，覆盖 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 115、`Tau.Tui.Tests` 190、`Tau.CodingAgent.Tests` 435、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166、既有 `tau-ai` / WebUi / Mom smoke，以及 no-env `tau-ai list` 和 CodingAgent RPC `get_state`。该切片不移动或备份用户真实 `auth.json`，因此 exact upstream auth backup / Unix wrapper parity、CI 接入、cross-platform archives 和 release automation 仍未完成。
- 本轮启动 Phase 5 release artifact baseline：新增 `scripts/build-release-artifacts.ps1` 与 `scripts/smoke-release-artifacts.ps1`，对照上游 `scripts/build-binaries.sh` 先建立当前 RID 的真实 Tau 可执行交付物。当前 Windows artifact 输出到 `artifacts/tau-win-x64/`，包含 `Tau.CodingAgent`、`Tau.Ai.Cli`、`Tau.Mom`、`Tau.Pods`、`Tau.WebUi` publish 输出，wrapper 命令 `pi`、`tau-ai`、`pi-ai`、`mom`、`pi-pods`、`tau-web-ui`，以及 `manifest.json`。release smoke 已验证 `tau-ai list`、`pi-ai list`、`pi --mode rpc get_state`、`pi-pods --help`、WebUi `/healthz` / `/api/status` / `/api/catalog` / session store 写入，以及 Mom `--once` 本地 event/inbox/outbox/status/log/runtime-log 链路；`verify-dotnet.ps1 -SkipRestore` 也通过。该切片不声明 cross-platform archives、no-env/auth-backup wrapper、version/changelog/tag/publish automation、CI 接入或真实外部 e2e 完成。
- 本轮继续 Phase 2 AI public API/bin closure：对照上游 `packages/ai/src/cli.ts` 与 `package.json` `bin.pi-ai`，新增 `Tau.Ai.Cli` standalone executable baseline，支持 `help`、`list`、`login [provider]`、交互式 provider selection、unknown-provider error、默认 Tau auth store 写入，以及 `--auth-file auth.json` 显式复刻上游 cwd `auth.json` 写入语义；`auth.json` 已加入 `.gitignore`，避免兼容测试路径产生的凭证文件入仓。focused validation `AiCliRunnerTests` 7/7、`Tau.Ai.Tests` 280/280 通过，`dotnet run --project src\Tau.Ai.Cli\Tau.Ai.Cli.csproj --no-build -- list` smoke 可列出 5 个内置 OAuth provider。该切片不声明发布层 `pi-ai` alias、真实 OAuth e2e、完整 TypeBox/AJV 或 exact TypeScript export/subpath shape 完成。
- 本轮继续 Phase 2 AI provider parser adoption closure：对照上游 `parseStreamingJson(...)` 在 OpenAI completions、OpenAI Responses shared、Anthropic、Bedrock 和 Mistral streaming tool-call argument path 的调用方式，把 Tau 的 OpenAI-style / OpenAI Responses / Anthropic / Mistral / Bedrock provider streaming tool-call arguments 接入 `StreamingJsonParser` object raw-text helper；`ToolCallContent.Arguments` 在 partial/done message 中保持合法 JSON object 字符串，`ToolCallDeltaEvent.Delta` 保留 provider 原始增量。focused validation `OpenAiProvider_ParsesPartialStreamingToolArguments|AnthropicProvider_ParsesPartialStreamingToolArguments|Stream_ParsesPartialStreamingToolArguments|Stream_TranslatesToolCallEvents|Stream_ConvertsToolsAndParsesToolCallEvents|AiUtilityHelpersTests|AiPublicApiCompileSampleTests` 34/34 通过，`Tau.Ai.Tests` 273/273 通过。该切片关闭 primary provider streaming tool-argument parser adoption baseline，但不声明真实 provider e2e、standalone `pi-ai` bin、完整 TypeBox/AJV 或 exact TypeScript export/subpath shape 完成。
- 本轮继续 Phase 2 AI public helper closure：新增并校准 `StreamingJsonParser.ParseStreamingJson(...)`，对照上游 `packages/ai/src/utils/json-parse.ts` 和 `partial-json@0.1.7` 的实际行为，固定 complete JSON fast path、空/非法输入返回 `{}`、incomplete nested object/array/string/literal recovery、incomplete decimal drop 和 incomplete exponent base recovery；`AiPublicApiCompileSampleTests` 已覆盖外部消费者调用入口。focused validation `AiUtilityHelpersTests|AiPublicApiCompileSampleTests` 29/29 通过。该切片关闭 public incomplete JSON parser baseline，但不声明 standalone `pi-ai` bin、完整 TypeBox/AJV、exact TypeScript export/subpath shape，或所有 provider 内部 streaming tool-argument parser 已统一改用该 helper。
- 本轮继续 Phase 2 AI public provider / auth status seam closure：新增 `Tau.Ai.Providers.Faux` public scripted provider baseline，对照上游 `packages/ai/src/providers/faux.ts` 固定注册/注销、queued/factory responses、text/thinking/tool-call helper、usage/cache estimate、streamed deltas、terminal error/aborted message 和 public API sample 覆盖；同时 `ProviderAuthResolver` 的 models.json status source 改为构造期可注入 `ModelConfigurationStore`，测试不再依赖进程级 `TAU_MODELS_FILE`，避免 Windows/xUnit 并行下临时目录删除碰到文件占用。validation `Tau.Ai.Tests` 259/259、`verify-dotnet.ps1 -SkipRestore` 和 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 均通过，后者覆盖 WebUi 和 Mom `--once` smoke。该切片仍不声明 standalone `pi-ai` bin、完整 TypeBox/AJV、incomplete JSON parser、option-level signal/onPayload/onResponse 或 exact TypeScript export/subpath shape 完成。
- 本轮继续 Phase 2 AI public helper closure：新增 `AiHeaderUtilities`、`ShortHash`、`ContextOverflowDetector` 和 `JsonSchemaHelpers.StringEnum`，对照上游 `packages/ai/src/utils/headers.ts`、`hash.ts`、`overflow.ts`、`typebox-helpers.ts` 固定 .NET-native public helper baseline；`ContextOverflowDetector` 覆盖上游 provider overflow pattern、non-overflow exclusion 和 silent usage overflow 判断，并被 `Tau.CodingAgent` retry/overflow 分类复用。该当时切片不声明 standalone `pi-ai` bin、public faux provider、完整 TypeBox/AJV、incomplete JSON parser 或 exact TS export shape 完成；后续本轮 provider lane 已补 Faux provider baseline。focused validation `AiUtilityHelpersTests|AiPublicApiCompileSampleTests` 17/17、`CodingAgentRetryClassifierTests` 2/2 通过。
- 本轮继续 Phase 2 AI public API surface closure：新增 `AiPublicApiCompileSampleTests`，用外部消费者样例固定 `Tau.Ai` 当前 public surface：message/content/tool/model/options/usage、`ProviderRegistry`、`StreamFunctions`、`AssistantMessageStream` / stream events、`ModelCatalog` cost/model helper、`ProviderAuthResolver`、OAuth registry 入口和 `ToolArgumentValidator`。该切片不声明 `pi-ai` standalone bin、TypeBox/faux/helper export 全量 parity 完成；剩余缺口仍保留在 matrix。focused validation `AiPublicApiCompileSampleTests` 1/1、`Tau.Ai.Tests` 226/226、`verify-dotnet.ps1 -SkipRestore` 通过。
- 本轮继续 Phase 2 AI public helper closure：把原本只存在于 `Tau.Agent` 内部的 tool argument schema validation 上移为 `Tau.Ai.ToolArgumentValidator` 公共 helper，暴露 `ValidateToolCall(...)` / `ValidateToolArguments(...)`，覆盖 Tau 当前消费的 object/array/required/properties/items/type coercion/enum/anyOf 子集；`Tau.Agent` 继续复用同一 helper 保留 error-as-tool-result 行为。focused validation `ToolArgumentValidatorTests` 4/4、`AgentRuntimeContractTests` 15/15 通过；随后串行复核 `Tau.Ai.Tests` 225/225、`Tau.Agent.Tests` 115/115、`verify-dotnet.ps1 -SkipRestore` 与 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 均通过。
- 本轮继续 Phase 2 Agent TransformContext cancellation closure：`ContextTransformer` 现在把 `TransformContextAsync` / `TransformContext` 统一接入 cancellation-aware build flow，`AgentRuntime` 在 transform 阶段被取消时会产出 aborted assistant、`turn_end` 和 `agent_end`，并跳过 provider 调用。focused validation `AgentRuntimeContractTests` 15/15、`Tau.Agent.Tests` 115/115 通过，随后仓库级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke` 也通过。Agent loop/event contract 本地已闭合，剩余更大范围缺口转为 facade 选项级 pass-through、public export 形状和真实 proxy/e2e 验证。
- 本轮继续 Phase 2 Agent tool cancellation cleanup：对照上游 `agent-loop.ts` 的 catch-as-tool-result 语义，Tau 现在把 tool prepare/before/execute/after 阶段由运行 token 取消触发的 `OperationCanceledException` 收敛为 `Operation canceled.` error tool result，不再打断 runtime 枚举；`AgentRuntime` 会在 finally 中清空 pending tool calls；parallel cancellation 不会再让 update wait 提前抛出，sibling success/cancel 结果仍按 assistant source order 进入 `turn_end.toolResults`。focused validation `AgentRuntimeContractTests` 14/14、`Tau.Agent.Tests` 114/114 通过。Agent 剩余 gap 收窄为 `TransformContext` cancellation parity。
- 本轮继续 Phase 2 Agent parallel timing / low-level cancellation closure：对照上游 `agent-loop.ts`，Tau parallel tool execution 现在先按 assistant source order 发 start/prepare，再并发执行，tool update 可在前序 tool 未完成时实时流出，最终 end/result 仍按 source order 发出；low-level `RunAsync` / `RunStream` 在 assistant stream cancellation 时会产出 aborted assistant、`turn_end` 和 `agent_end`，`RunStream.ResultAsync` 正常返回 `agent_end.messages`，并保留 aborted `ErrorEvent` partial stop reason。focused validation `AgentRuntimeContractTests` 12/12、`Tau.Agent.Tests` 112/112 通过。该增量当时把 Agent 剩余 gap 收窄为 tool cancellation cleanup、parallel sibling cancellation result preservation 和 `TransformContext` cancellation parity；本轮后续 tool cancellation cleanup 已继续移除前两项。
- 本轮继续 Phase 2 Agent low-level stream closure：对照上游 `agent-loop.ts createAgentStream()`，新增 `AgentRuntime.RunStream(...)`，返回 Tau-native `EventStream<AgentEvent, ChatMessage[]>`，并从 `agent_end.messages` 提取 `ResultAsync`；`AgentRuntimeContractTests` 与 `AgentPublicApiCompileSampleTests` 已覆盖该入口。focused validation `AgentRuntimeContractTests|AgentPublicApiCompileSampleTests` 8/8、`Tau.Agent.Tests` 107/107 通过。该增量当时把 Agent 剩余 gap 收窄为 parallel event timing 和更细的 runtime cancellation edge parity；本轮后续 parallel/cancellation closure 已继续移除 parallel timing 和 assistant-stream cancellation gap。
- 本轮继续 Phase 2 Agent failure/cancel closure：对照上游 `agent.ts handleRunFailure`，`Agent` facade 现在会在 stream fault 时追加 stopReason=`error` 的 synthetic assistant failure message，在 cancellation 时追加 stopReason=`aborted` 的 synthetic assistant failure message，并把该 failure message 放进 `agent_end.messages`；同时新增 `StopReason.Aborted`，接通 proxy stop reason、OpenAI Responses `cancelled/aborted` 映射和 Mom stop reason normalization。focused validation `AgentFacadeTests|ProxyStreamProviderTests` 10/10、`Tau.Agent.Tests` 106/106 通过，并通过 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`。该增量当时把 Agent 剩余 gap 收窄为 parallel event timing、low-level `EventStream<AgentEvent, AgentMessage[]>` wrapper shape 和更细的 runtime cancellation edge parity；本轮后续 low-level stream closure 已继续移除 wrapper gap。
- 本轮继续 Phase 2 Agent public API closure：新增 `AgentPublicApiCompileSampleTests`，以外部消费者样例编译并运行 `AgentOptions` / `Agent` facade、`AgentRuntime` / `AgentLoopConfig`、`ProxyStreamProvider` / `ProxyStreamOptions` 和新增 Agent event payload 字段；focused validation `AgentPublicApiCompileSampleTests` 1/1 通过。该增量当时把 Agent 剩余缺口从“还缺 public API compile-sample”收窄为 parallel event timing 与完整 failure/cancel assistant message 语义。
- 本轮继续 Phase 2 Agent critical contract closure：`Tau.Agent` 现在补上 `agent_end.messages`、`turn_end.message/toolResults`、tool update partial result、schema validation 和 error-as-tool-result baseline；`Tau.Agent.Tests` 全量 103/103，`Tau.CodingAgent.Tests` 433/433，`Tau.WebUi.Tests` 44/44，并通过 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`。该增量不改变 Tau 的外部发布物，但把 Agent loop/schema contract 从“仍缺关键 payload/validation/error 收口”推进到“已能作为 Phase 2 继续分派的稳定基线”。
- 本轮完成 Phase 1 final normalization（docs-only）：`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 现在新增 normalization result 与 Phase 2 Candidate Queue，明确 capability rows 只作摘要、file-level rows 作 ownership/source 映射、surface rows 作 public contract/e2e 映射；当前上游 `packages` 目录集合已核对为 `agent`、`ai`、`coding-agent`、`mom`、`pods`、`tui`、`web-ui`，没有未知 package 目录留在 matrix 之外。该增量不改变运行时实现，但把 Phase 1 从“等待 final normalization”推进到“按 candidate queue 进入 Phase 2 worker 分派”。
- 本轮继续 Phase 1 inventory freeze（docs-only）：并行 explorer 只读对照 `packages/coding-agent/src/**`、`packages/tui/src/**`、`packages/web-ui/src/**`、`packages/mom/src/**` / `packages/mom/scripts/**` 与 Tau 当前实现后，`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 已补上 CodingAgent、Tui、WebUi、Mom grouped file-level matrix 以及对应 CLI/RPC/terminal/HTTP/component/Slack/session/sandbox/config/log/schema surface。该增量不改变运行时实现，但把 Phase 1 从“还缺四个产品模块清点”推进到“七个 package + root scripts 的主要 inventory 已冻结，下一步做 final normalization 和 Phase 2 候选抽取”。
- 本轮继续 Phase 1 inventory freeze（docs-only）：`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 现在补上 AI command/API/env/config/log/schema surface，覆盖 `pi-ai` CLI/bin、KnownApi/KnownProvider、StreamOptions/Provider options、auth.json/models.json、runtime log hooks、tool schema validation、OAuth registry、helper utilities、generated model coverage 和 error/abort contract 的上游- Tau 对照。该增量不改变运行时实现，但把 AI 从“已冻 file-level mapping”推进到后续 worker 可直接领任务的公共合同粒度。
- 本轮继续 Phase 1 inventory freeze（docs-only）：`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 现在补上 `packages/ai/src/**` file-level mapping，覆盖 barrel/package export、registry、stream facade、types/models/env/OAuth utilities、provider implementations、faux provider 和 utility helpers。该增量不改变运行时实现，但明确保留 `pi-ai` standalone CLI/bin、exact TypeScript export/API-name parity、AI-level tool schema validation helper、faux provider public API、provider real e2e、OAuth real e2e、generated model full coverage、OpenAI/Google API string divergence 和 library-level context-overflow helper 等缺口。
- 本轮继续 Phase 1 inventory freeze（docs-only）：`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 现在补上 `packages/pods/src/**` file-level mapping、Pods command/env/config/log surface，以及 root scripts/manifests file matrix。该增量不改变运行时实现，但把 Pods/root 从 capability-level inventory 推进到后续 worker 可直接领任务的审计粒度：明确保留 real SSH/SCP/setup/HF/GPU/vLLM e2e、known-model/GPU allocation、`PI_CONFIG_DIR` config path、top-level command alias、`pods agent` prompt path、release artifact 和 no-env wrapper 等缺口。
- 本轮继续 Phase 2 Agent critical contract closure：新增 `Tau.Agent.Agent` high-level facade 和 `AgentOptions`，对齐上游 `packages/agent/src/agent.ts` 的 stateful wrapper baseline。该 facade 持有 model/provider registry/system prompt/tools/interceptors/options，暴露 `PromptAsync`、`ContinueAsync`、`Subscribe`、`Steer`、`FollowUp`、queue clear、`HasQueuedMessages`、`Abort`、`WaitForIdleAsync` 和 `Reset`。
- `AgentState` 现在除 messages/streaming/pending/error 外，也暴露 system prompt、model 和 tools；`AgentRuntime` 运行时会按 config 同步这些状态字段，方便 facade 和直接 runtime 调用共享同一事实源。
- `MessageStartEvent` / `MessageEndEvent` 现在承载任意 `ChatMessage`，并保留 `Partial` 兼容属性；facade 会为 prompt user message emit message lifecycle，runtime 在 tool execution end 后会为 `ToolResultMessage` emit message lifecycle，使事件序列更接近上游 `agent-loop.ts`。
- `ContinueAsync` 已固定 assistant-tail 语义：没有 queued steering/follow-up 时拒绝继续；有 queued steering 时先 drain steering 并跳过 runtime 初始 steering poll，避免重复注入；queued follow-up 则作为新 prompt message 继续执行。
- 本轮 focused validation 通过：`dotnet test tests\Tau.Agent.Tests\Tau.Agent.Tests.csproj --filter AgentFacadeTests --verbosity minimal` 6/6；完整 `Tau.Agent.Tests` 97/97。随后项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 和 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke` 均通过，覆盖 `Tau.Ai.Tests` 221、`Tau.Agent.Tests` 97、`Tau.Tui.Tests` 190、`Tau.CodingAgent.Tests` 433、`Tau.WebUi.Tests` 44、`Tau.Pods.Tests` 166，并完成 WebUi 与 Mom `--once` smoke。该 facade 切片当时尚未标为完整 Agent parity；本轮后续 Agent loop closure 已补 payload/schema/error-result 第一层，剩余缺口收窄到 parallel event timing、完整 failure/cancel assistant message 语义和 public API compile-sample。

## 最新增量（2026-05-26）

- `Tau.WebUi` 生产默认 message runner 已把同一个 `ITauLogSink` 透传到 `WebUiRunnerFactory.Create(...) -> RuntimeCodingAgentRunner.Create(..., logSink)`；`/api/sessions/{id}/messages/stream` 不再只覆盖 fake-runner endpoint 行为，而是能在默认 runner 路径写出 `agent/run.*` runtime events，并为后续真实 tool call 复用 `AgentLoopConfig.LogSink -> tool/execution.*` 打开路径。
- 本轮继续补 `TauRuntimeLogContext` cross-module correlation baseline：`RuntimeCodingAgentRunner` 每次 run 生成/继承 `correlationId`，并把 `sessionId` / `messageId` 透传到 `agent/run.*`；`AgentLoopConfig.LogContext -> ToolExecutor` 让 `tool/execution.*` 继承同一组字段；WebUi message stream 每次发送用 Web session id + transient message id 注入默认 runner。当前不改变 JSONL 顶层格式，也不持久化 WebChat message id。
- 本轮继续把同一 runtime log context 扩到 `Tau.CodingAgent` CLI/RPC 宿主层：`ICodingAgentRunner.RunAsync(...)` 增加 per-run `TauRuntimeLogContext` 入口，CLI 普通 turn 生成稳定 turn correlation/message id 并在 retry attempts 间复用，RPC prompt 用 command id 作为 `correlationId/messageId`，同时从 JSONL tree summary 注入 `sessionId`。当前不提前生成 JSONL message entry id，不改变 retry/rollback 持久化时序。
- 本轮继续把同一 context 接到 `Tau.Mom` delegation path：`RuntimeDelegationAgentRunner` 从 `DelegationRequest.Metadata` 的 `requestId` / Slack `ts` 生成 `correlationId/messageId`，从 `channel:threadTs|ts` 生成 `sessionId`，并把这些字段写入 Mom `delegation/response/tool/usage` runtime events，同时传给内部 CodingAgent runner，使 Mom delegation 与 `agent/run.*` / `tool/execution.*` 可关联。当前仍不把本地工作目录当作 session id，也不改变 `status.json` / `log.jsonl` / outbox 合同。
- 本轮补 `Tau.Pods` setup plan/run baseline：`PodDefinition.VllmVersion` 记录 `release/nightly/gpt-oss`，`PodDefinition.Gpus` 持久化 setup detect 结果，`PodSetupPlanner` 生成 `/tmp/pod_setup.sh` setup command，CLI 暴露 `setup plan [--json] [--mount <command>] [--models-path <path>] [--vllm release|nightly|gpt-oss] [path] <id>`；`PodSetupService` 与 `setup run [--json] [--script <path>] ... [path] <id>` 固定 SSH test、SCP script、远端 setup 执行和 `nvidia-smi` GPU detect 链路。`setup run` 成功后会写回当前 pod 的 `modelsPath`、`vllmVersion` 和 detected `gpus`，失败不写 config；JSON/text 输出显式暴露 `configUpdated` 与 `configPath`，让调用方不用重新 diff 配置文件判断是否持久化。输出和 config 都不保存真实 token，真实 `HF_TOKEN` / `PI_API_KEY` 通过 stdin 传给远端 shell，display command / JSON / text output 只保留 placeholder 或 redacted stdout/stderr。当前只做 fake process runner focused coverage，不宣称真实 SSH/HF/GPU pod smoke。
- 本轮继续补 `Tau.Pods` registration / active config baseline：`PodsConfig` 持久化上游兼容 `active` 字段，`list/status --json` 暴露 `activePodId` 与 per-pod `active`，CLI 新增 `active/remove`，并新增本地 `setup <id> "ssh [-p port] <host>"` 注册入口。注册只写本地 config，不执行 SSH/SCP/setup run；复杂 SSH options 暂显式拒绝，以免扩散到当前只支持 `SshHost/SshPort` 的远端执行链。
- 本轮继续把 `Tau.Pods` active pod 语义接到 model 命令消费面：`model list [--json] [path]` 默认使用 config `active`，`model pull/remove/status [--json] [path] <model-id>` 的单尾随参数按 model id 解释并走 active pod；旧显式 `<pod-id> <model-id>` 用法保留。active 缺失时返回 `No active pod. Use 'active <pod-id>' to set one.`。
- 本轮继续把 `Tau.Pods` active pod 语义接到 deployments 与 vLLM 控制类消费面：`deployments [--json] [path] [pod-id]` 以及 `vllm status/health/stop [--json] [path] [pod-id] <deployment-name>` 现在都可在缺省 pod-id 时使用 config `active`，但仍保留显式 pod-id override。
- 本轮继续把 `Tau.Pods` active pod 语义接到 serve 类 vLLM 命令：`vllm plan/preflight/deploy [--config path] [--pod id] <model-id> [--name deployment-name]` 会在省略 `--pod` 时使用 config `active`，旧 `[path] <pod-id> <model-id> [deployment-name]` positional 用法仍兼容；deployment name 在 active 默认路径上通过 `--name` 显式传入，避免 Hugging Face `org/model` 和旧 path heuristic / pod-id slot 形成歧义。
- 本轮继续把 `Tau.Pods` 的显式 option 入口再扩一层：`model list/pull/remove/status`、`deployments` 以及 `vllm status/health/stop` 现在也支持 `--config path` / `--pod id`，同时保留旧 positional 形式；这样 model 名、deployment 名和 config path 都能用 option 显式消歧，不再只依赖 path heuristic。
- 本轮继续把 `Tau.Pods` 的 vLLM snapshot 选择再收口一层：`vllm plan/preflight/deploy` 现在支持 `--revision` / `--snapshot`，preflight 会优先解析 `refs/<revision>` 或 `snapshots/<revision>`，并把 `requestedRevision` / `revision` 暴露到 plan 和 preflight 输出里。随后又补 `model pull --revision` / `--snapshot`，两条 Hugging Face 下载路径都会带同一 revision；`vllm deploy --prefetch` 会在模型 cache/snapshot/revision 缺口时先复用 `model pull`，再二次 preflight 和 deploy；当前仍缺真实远端 revision/HF download smoke。
- 本轮 targeted 验证继续新增通过：`dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`，`PullAsync_|ModelPull_|VllmPlan_MissingModel_PrintsUsage` focused tests 7/7，以及 Pods vLLM prefetch/revision focused tests 7/7。
- 本轮 targeted 验证新增通过：`dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`，以及 `VllmPlan_WithConfigPodAndNameOptions|VllmPlan_WithoutPodOption|VllmPreflight_WithoutPodOption|VllmDeploy_WithoutPodOption|VllmPlan_WithJsonOption|VllmPlan_WithVllmExtraArgs|VllmDeploy_WithVllmExtraArgs|VllmPreflight_WithJsonOutput|VllmDeploy_WithJsonOutput` focused tests 10/10。
- 本轮 targeted 验证继续新增通过：`Deployments_WithConfigPodOptions|ModelList_WithConfigPodOptions|ModelStatus_WithConfigPodOptions|VllmStatus_WithConfigPodOptions|Deployments_WithoutPodId|ModelList_WithoutPodId|ModelStatus_WithoutPodId|VllmStatusHealthStop_WithoutPodId|ModelList_WithJsonOutput|VllmStatus_WithJsonOutput` focused tests 12/12，以及 `dotnet build src\\Tau.Pods\\Tau.Pods.csproj --no-restore --verbosity minimal`。
- 本轮 targeted 验证通过：`AgentRuntimeToolTraceTests` 2/2、`RuntimeCodingAgentRunnerTests.RunAsync_EmitsRun*` 2/2、`RuntimeCodingAgentRunnerTests.RunAsync_PerRunLogContextOverridesConfiguredContext` 1/1、`CodingAgentRpcHostTests.RunAsync_PromptPassesCommandAndTreeSessionLogContextToRunner` 1/1、`CodingAgentHostTests.RunAsync_RetryableTurnPassesStableTreeSessionLogContextToRunner` 1/1、`RuntimeDelegationAgentRunnerTests.ExecuteAsync_AggregatesUsage_StopReason_ToolEventsWithDuration` 1/1、`WebUiEndpointTests.MessageStreamEndpoint_DefaultRunnerLogsAgentRunEvents` 1/1、Pods setup plan targeted tests `PodSetup|SetupPlan` 5/5、Pods setup run focused tests `PodSetup|SetupRun` 7/7（含 config writeback/token-not-persisted 断言），追加 `FullyQualifiedName~SetupRun` 2/2（含 `configUpdated/configPath` 和 SSH failure no-writeback 断言），追加 Pods model active fallback focused tests 7/7、Pods deployments/vllm active fallback focused tests 7/7，并通过 `dotnet build src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-restore --verbosity minimal`、`dotnet build src\Tau.Mom\Tau.Mom.csproj --no-restore --verbosity minimal` 与 `dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`。验证过程中曾因并行 build 触发 Windows/Roslyn `Tau.Ai.dll` 文件锁，已 `dotnet build-server shutdown` 后改回串行验证；没有重跑全仓 gate。

## 最新增量（2026-05-24）

- 本轮按 `GOAL.md` 继续完整移植链路，收口多个互不重叠 baseline：`Tau.Pods` vLLM SSH command/service orchestration + health retry/backoff、`Tau.Mom` delegation runtime response/tool/usage log events 与 Slack runtime response update/delete seam、`Tau.WebUi` CodingAgent JSONL preview tree metadata/filter/audit、`Tau.Tui` transcript viewport host + Home scroll-top、`Tau.Ai` Bedrock SSO cache token refresh、`Tau.CodingAgent` HTML nested Markdown list structure。
- `Tau.Pods` 已新增 `vllm deploy/status/health/stop [--json]`：deploy 会通过 SSH 写远端 service/metadata，优先 `systemctl --user enable --now`，systemd user command 不可用或失败时 fallback 到 `nohup` + `.pid` / `.log`；deploy 默认按 12 次、5 秒 backoff 做远端 `/health` readiness 窗口，`starting/unknown/not-ready` 会继续等待，`unhealthy/dead/not-found`、SSH 启动失败、runner failure 和 cancelled 会归类为终止失败；部署命令失败或 health 最终非 ready 时尝试 rollback cleanup；`vllm health` 默认单次探测，也可用 `--health-attempts` / `--health-backoff-ms` 调整；status 拉取 systemd/fallback pid/metadata 文本；health 返回 `ready/unhealthy/dead/starting/unknown`、`failureKind` 和 attempts；stop disable/remove unit 或 kill pid 并清理文件。该能力仍只是 SSH command/service baseline，成功不代表真实 GPU/HF/vLLM 环境长期健康，metadata 仍是 `planned-vllm`，rollback 是 cleanup，不是多版本 rollout 状态机。
- `Tau.Mom` 已把 `RuntimeDelegationAgentRunner` 接入生产 `ITauLogSink`，并记录 `delegation.start` / `response.start` / `tool.start` / `tool.end` / `response.end` / `usage` / `delegation.end`；字段包含 provider/model/workingDirectory、tool id/name、duration、isError、response preview/characters、usage tokens/service tier/cost。取消路径会写 `delegation.end stopReason=cancelled` 后 rethrow。当前仍是本地文本摘要级 observability baseline，不是完整 trace/correlation 协议。
- `Tau.Mom` 已补 Slack runtime response update/delete seam：`MomChannelMessageProcessor` 在 responder 支持 runtime response 时先创建 `_Thinking_ ...` 占位消息，完成后用 `chat.update` 更新同一条消息；runner 返回 `[SILENT]` 时通过 `chat.delete` 删除占位且不再追加最终回复。该能力固定 Slack 消息生命周期合同，但仍未做真实 Slack smoke 或真实 Slack session sync。
- `Tau.WebUi` CodingAgent JSONL preview 现在返回 tree metadata 和 filter metadata：leaf/root/branch/message/label counts、entry type histogram、current branch ids 和 per-entry id/type/parent/depth/child/current-branch/current-leaf/label metadata；entry id、parent id 和 label target resolution 按 case-insensitive tree-store 语义处理；preview endpoint 支持 `search` 与 `currentBranchOnly` 只过滤返回的 preview messages，conservative import 响应会带回 source tree/source audit/warnings。preview tree 不被 conservative import 持久化，import 仍按 timeline-derived messages 生成 WebChat DTO，不保留 branch tree。
- `Tau.Tui` 已新增 `TuiTranscriptViewportHost` 和 `TuiTranscriptSession`，前者把 `TuiTranscriptViewport`、`ITuiRenderSurface` 和 `TuiDiffRenderer` 包成可复用 render/apply wrapper，后者提供 start/stop、auto-render mutation、PageUp/PageDown/Home/End 滚动输入和可注入 key reader seam；测试固定首帧/增量 diff、scroll stable viewport、status row diff、resize full redraw、force/reset frame、apply 失败不推进 previous frame、Home scroll-top、session mutation/input/stop/read-key 行为。当前仍不是完整 terminal host、overlay compositor、hardware cursor 或 CodingAgent 主屏。
- `Tau.Ai` 已补 Bedrock SSO cache token refresh baseline：access token 过期且 AWS CLI cache 中已有 `clientId` / `clientSecret` / `refreshToken` / 未过期 `registrationExpiresAt` 时，会调用 SSO OIDC `CreateToken`，用刷新后的 access token 调 SSO Portal，并 best-effort 写回 cache；缺 refresh metadata、client registration 过期或 OIDC 失败时返回明确 `aws sso login --profile <profile>` 诊断。当前仍缺 OIDC `RegisterClient`、cache 并发边界和真实 AWS 云端 e2e。
- `Tau.CodingAgent` 已修正 HTML transcript 嵌套 Markdown list 结构：缩进升级时子 `<ul>/<ol>` 会留在父 `<li>` 内，indent 回退时按 open list item stack 关闭，避免 nested list 被导出成父项外的兄弟列表。当前仍是 Tau 轻量 Markdown subset，不是完整 Markdown/highlight renderer。
- 本轮 targeted 验证通过：`Tau.Pods.Tests` 89、`Tau.Tui.Tests` 144、`Tau.Agent.Tests` 81、`Tau.WebUi.Tests` 41、`Tau.Ai.Tests` 220、`Tau.CodingAgent.Tests` 347。当前工作树项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore` 与 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke` 均已通过，smoke 覆盖 WebUi 和 Mom `--once`，Mom smoke 处理 1 个 due event 并写出 `delegation.start/end` runtime log。

## 最新增量（2026-05-23）

- 本轮继续沿 JSONL 脱敏收口 WebUi 面：`GET /api/sessions/{id}/export.jsonl`、`POST /api/sessions/import.jsonl`、CodingAgent JSONL preview 与 conservative import 现在默认复用 `JsonlSecretRedactor`，递归只处理 JSON string value，保留 field key、number、bool 和 null；`TAU_WEBUI_REDACT_SECRETS=0` 可关闭。
- `Tau.WebUi.Tests` 新增 3 个 redaction 回归，覆盖 WebUi-local JSONL export、WebUi-local JSONL import、CodingAgent JSONL preview/import 的 string-value redaction 与 disabled redactor opt-out；当前 targeted WebUi 测试数提升到 32。
- 本轮在上一提交 `c77e5ad` 之后继续做并行收口，但新开的多名 worker 被网关 `403 Forbidden` 阻断；最终只把已实际落地且通过定向测试的三条切片纳入本次提交：Ai runtime log JSONL 脱敏统一、CodingAgent JSONL tree session 写出脱敏、Pods `vllm plan --json`。没有把 Tui/WebUi/Mom 新能力写成已完成。
- `Tau.Ai` 已把 `JsonlTauLogSink` 从逐字段手工 redaction 改为完整 JSONL 行写出后复用 `JsonlSecretRedactor`：默认只递归脱敏 string value，覆盖 category、event 和 field value，保留 field key、number、bool 和 null；`TAU_LOG_REDACT_SECRETS=0` 继续作为 opt-out。
- `Tau.CodingAgent` 已把 JSONL tree session 写出接入 `JsonlSecretRedactor`：`CodingAgentTreeSessionStore` 写 session header、append entry、rewrite/export current branch 时默认按 `TAU_CODING_AGENT_REDACT_SECRETS` 脱敏 string value，object key/number/bool/null 保留，`0/false` 可关闭。该能力会改变持久化内容，不是可恢复加密。
- `Tau.Pods` 已给 `vllm plan` 增加 `--json` 输出：默认文本 plan 保持，`--json` 输出 pod/deployment/model/modelPath/port/servedModel/unit/serveCommand/systemdUnit/metadata/metadataJson/remoteCommand，仍不执行 SSH、不调用 `systemctl`、不写远端状态。
- 当前新增定向验证已串行通过：`Tau.Ai.Tests` 205、`Tau.CodingAgent.Tests` 346、`Tau.Pods.Tests` 66；全仓 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1` 在恢复缺失 NuGet 缓存后通过。
- 本轮“继续继续继续”在既有 6-agent 并行基础上继续收口；Tui/Mom worker 返回成功，WebUi/Pods 新 worker 成功，Ai/CodingAgent 由主控本地补齐。主控继续保持 docs/history/验证/commit 串行收口，避免 Windows/Roslyn 文件锁。
- `Tau.Ai` 已补 `JsonlSecretRedactor` 通用 JSONL 行级脱敏 foundation：递归只处理 string value，保留 object key 和 number/bool/null，invalid JSON fallback 到整行 redaction，disabled redactor 原样返回。该能力是基础 helper，不自动覆盖全部 JSONL 文件。
- `Tau.CodingAgent` 已把 `/tree --interactive` fold state 从 settings fallback 推进到 append-only `tree_state` session metadata baseline：navigator 返回 folded snapshot 后写入当前 JSONL session，启动时优先恢复 session-scoped collapsed ids；settings `treeCollapsedEntryIds` 仍保留兼容 fallback。
- `Tau.Tui` 已补 `TuiTranscriptViewport` 组合 viewport foundation，复用 `TuiMessageArea` / `TuiStatusBar` / `TuiScrollbackBuffer`，固定最后一行 status，支持 append/set/clear、滚动、resize rewrap 和 bottom-follow；当前未接 terminal host/overlay/hardware cursor。
- `Tau.WebUi` 已从 CodingAgent JSONL 只读 preview 推进到 conservative import baseline：`POST /api/sessions/import.coding-agent-jsonl` 复用 preview parser，把 timeline 转成 WebChat DTO 并持久化；tool call/result/thinking/image 只落可审计文本标记，不保留完整 branch/tree 结构。
- `Tau.Mom` 已补 `log.jsonl` / `last_prompt.jsonl` 默认 secret redaction baseline：写 log、读 history 注入 prompt 和写 prompt debug 时默认复用 `JsonlSecretRedactor` / `TauSecretRedactor`，`TAU_MOM_REDACT_SECRETS=0` 可关闭。
- `Tau.Pods` 已把 `PodVllmCommandPlanner` 接到 `vllm plan [path] <id> <model> [name]` CLI 预览入口，输出 systemd/metadata/remote command plan，不执行 SSH、不调用 `systemctl`、不写远端状态。
- 本轮继续补 `Tau.Pods` vLLM known-model option planning：`vllm plan/deploy` 现在支持 `--gpus n`、`--memory percent`、`--context size`。非 `--vllm` 路径会为 known model 按请求 GPU count 取配置、选择本地 `PodDefinition.Gpus` 中的 GPU id、单 GPU时注入 `CUDA_VISIBLE_DEVICES`，并把 memory/context 映射成 `--gpu-memory-utilization` 与 `--max-model-len`；JSON/text plan 和 metadata 暴露 `requestedGpuCount`、`selectedGpus`、`memoryUtilization`、`contextTokens`。显式 `--vllm` 仍覆盖 known-model/GPU/memory/context 自动规划；unknown model 使用 `--gpus` 会报错。usage-aware GPU allocation state 已由后续独立子切片关闭本地 baseline；直接 `start/stop/list/logs` PID/log-tail 流程或真实 SSH/HF/GPU/vLLM smoke 仍未完成。
- 本轮 targeted/full Pods 验证通过：`dotnet build src\Tau.Pods\Tau.Pods.csproj --no-restore --verbosity minimal`，`dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --filter "PodsCli|Model|Config|PodVllmCommandPlanner" --no-restore --verbosity minimal` 108/108，`dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --no-restore --verbosity minimal` 181/181。
- 上一轮定向验证已串行通过：`Tau.Ai.Tests` 204、`Tau.CodingAgent.Tests` 342、`Tau.Tui.Tests` 110、`Tau.Agent.Tests` 76、`Tau.Pods.Tests` 65、`Tau.WebUi.Tests` 29；本轮后续全仓验证已重新覆盖最新测试基线。
- 6-agent 并行轮次已从干净基线 `f80153d` 同时推进 `Tau.Ai`、`Tau.CodingAgent`、`Tau.Tui`、`Tau.WebUi`、`Tau.Mom` 和 `Tau.Pods` 六个互斥切片；主控统一完成串行验证，避免并行 build/test 的 Roslyn 文件锁风险。
- `Tau.Ai` 已补 models.json dynamic OpenAI-compatible provider registration：未预注册 provider 只有在 `apiKind` / `apiType` 显式标记为 `openai-compatible`、`openai-completions` 或 `openai-chat-completions` 时才运行时注册；未知或拼错 api 不静默兜底。`EnvironmentVariableScope` 测试 helper 改用 `SemaphoreSlim`，避免 async test 跨线程 dispose 触发 `SynchronizationLockException`。
- `Tau.CodingAgent` 已补 `/metadata [entry-id]` JSONL session/entry inspector baseline，可查看 session header、cwd/parent/leaf、entry/message/branch/label 统计、最近 metadata entries，或指定 entry 的 id/type/parent/timestamp/path/depth/children/label/message preview 与 compaction/branch-summary/retry 等关键字段。
- `Tau.Tui` 已补 `TuiMessageArea` 和 `TuiStatusBar` 库层 foundation，覆盖 role prefix、wrap、bottom-anchored transcript、left/right status segment、右侧状态保留和窄宽截断；当前仍未接完整 terminal host/viewport。
- `Tau.WebUi` 已补 JSONL import problem-details contract 和前端 `.jsonl` import 分流：unsupported content type 返回 `415 application/problem+json`，parser error 返回 `400 application/problem+json` 且包含稳定 `code` 和可选 `line`。
- `Tau.Mom` 已补 `MomLocalDelegationFlow`，让 `Worker` 后台轮询和 `Program --once` 复用同一条 event -> inbox -> runner -> outbox/status/log/archive 本地 flow。
- `Tau.Pods` 已补 model lifecycle CLI baseline：`model list/pull/remove/status`、pod-level `modelsPath`、Hugging Face CLI/python fallback 和 SSH keepalive；当前仍未做真实 SSH/HF/vLLM smoke。
- 上一轮 6-agent 串行验证当时通过：`Tau.Ai.Tests` 196、`Tau.Agent.Tests` 68、`Tau.Tui.Tests` 92、`Tau.CodingAgent.Tests` 336、`Tau.Pods.Tests` 55；额外 `Tau.WebUi.Tests` 17/17 和 Mom host sandbox validation smoke 也通过。本轮最新 targeted counts 见本节顶部。
- `Tau.WebUi` 已补 WebUi-local JSONL import baseline：新增 `WebChatJsonlImporter` 和 `POST /api/sessions/import.jsonl`，对 `type=session` header、`type=message` entries、版本、必填字段、重复 id 和线性 `parentId` 做校验，并覆盖 JSONL export -> import roundtrip；当前仍不是 CodingAgent JSONL branch/session-tree 语义对齐。
- `Tau.Mom` 已补 `--validate-sandbox` 显式 validation 入口：`MomCommandLine` 会剥离 `--once` / `--validate-sandbox` 并把其他 host args 透传给配置系统，`MomSandboxValidator` 复用 host/Docker validate seam；host sandbox smoke 可本地直接通过，真实 Docker container smoke 仍缺。
- `Tau.Pods` 已补 ssh process failure/cancellation hardening：本地 `ssh` 启动失败、injected runner 异常、`Process.Start` 返回 null 和 cancellation 都会返回结构化 `PodExecResult` failure，默认进程取消时 best-effort kill，lifecycle summary 继续透传底层 failure。
- `Tau.CodingAgent` 已补 thinking 模型能力 clamp baseline：新增 `CodingAgentThinkingLevels` 共享 helper，统一 `/thinking`、selector、settings selector、启动恢复、显式 `/model` / `/provider` / Ctrl+L、Ctrl+P/Ctrl+Shift+P、RPC `set_model` / `cycle_model` / `set_thinking_level` / `cycle_thinking_level` / `update_settings` 的 effective thinking 规则；非 reasoning 模型归一 off，不支持 xhigh 的 reasoning 模型把 xhigh 归一 high，支持 xhigh 的模型保留 xhigh。
- `Tau.CodingAgent` 已补 scoped model thinking-level per-entry baseline：settings `enabledModels` 字符串条目支持 `provider/model:off|minimal|low|medium|high|xhigh`；`/scoped-models` 命令可解析、展示和保存 suffix，TUI multi-select selector 保存时保留既有 suffix，Ctrl+P/Ctrl+Shift+P 与 RPC `cycle_model` 切到带 suffix 的 scoped model 时会同步 runner/default thinking，并按目标模型能力 clamp。当前仍不是完整上游 per-entry thinking 编辑 UI。
- `Tau.CodingAgent` 已补 model selector search/chrome baseline：`CodingAgentModelSelectorComponent` 在 `/model select`、交互式裸 `/model` 和 Ctrl+L 的模型选择器顶部渲染 `Model Selector` / `Search:` 轻量 chrome，普通字符输入会交互式更新过滤，Backspace 会回退过滤；搜索、scope toggle、selected model detail 和 auth filtering footer 现在在同一个 focused component 中闭环。当前仍不是完整上游 theme/dynamic-border/terminal-host parity；scoped model thinking-level per-entry baseline 已在后续同日切片补齐。
- `Tau.CodingAgent` 已补 model selector scope/detail baseline：`CodingAgentModelSelectorComponent` 在 `/model select`、交互式裸 `/model` 和 Ctrl+L 的模型选择器中支持 Tab 在 `scoped` / `all` auth-configured 候选之间切换；有 scoped 候选时默认显示 scoped scope，切到 all scope 后仍优先定位当前模型，并在列表下方显示 `Model Name: ...`。search/chrome 和 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。
- `Tau.CodingAgent` 已补 model selector footer hint baseline：`TuiSelectListLayout` 新增通用 `FooterHint`，单选列表会在普通列表、滚动提示之后以及无匹配结果时渲染 footer；`CodingAgentModelSelector` 在模型 selector 底部显示 `Only showing models with configured auth`，对齐上游“只展示已配置凭证模型”的提示意图。scope toggle、selected model detail、search/chrome、scoped model thinking-level per-entry 和模型能力 clamp baseline 已在同日补齐；完整上游 theme/dynamic-border/terminal-host parity 与 per-entry thinking 编辑 UI 仍后续补齐。
- `Tau.CodingAgent` 已补 model selector / model cycle / RPC model switching 的 provider auth filtering baseline：新增 `CodingAgentModelAvailability` 共享 helper，CLI router 和 RPC host 统一按 provider auth status 过滤实际可用模型；`/model select`、交互式裸 `/model`、Ctrl+L、Ctrl+P / Ctrl+Shift+P、显式 `/model`、`/provider`、RPC `get_available_models` / `set_model` / `cycle_model` / `update_settings.settings.model` 都只展示、循环或接受已配置凭证的 provider/model。`/scoped-models` 仍基于全部注册模型维护 settings `enabledModels`，作为配置入口允许预先加入尚未登录的 provider。search/chrome、scoped model thinking-level per-entry 和模型能力 clamp baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity 或 per-entry thinking 编辑 UI parity。
- `Tau.CodingAgent` 已补 `/model select` / Ctrl+L model selector baseline：新增 `CodingAgentModelSelector`，真实交互式 editor 会话中裸 `/model` 或 `/model select [search]` 会复用当前 TUI selector foundation 打开模型单选列表，优先使用 settings `enabledModels` scope 中已配置凭证的模型，scope 缺失时展示全部已配置凭证模型；`select [search]` 支持初始过滤，选择后更新 runner model、保存 default provider/model，并同步 tree session。无 selector 的裸 `/model` 保留 current status，Ctrl+L 触发同一 selector 并保留当前输入 draft。search/chrome 和 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。
- `Tau.CodingAgent` 已补 Ctrl+P / Ctrl+Shift+P model cycling interactive baseline：`Tau.Tui` 默认 keybinding 新增 `CycleModelForward` / `CycleModelBackward`，`InteractiveInputEditor` 可把 app action 返回给 `CodingAgentHost` 并保留当前 draft；host 调用 `CodingAgentCommandRouter.CycleModel()`，按 settings `enabledModels` 有序 scope 或全部可用模型切换到下一个/上一个模型，保存默认 provider/model，并在候选不足两个时显示 `Only one model in scope` / `Only one model available`。search/chrome 和 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。
- `Tau.CodingAgent` 已补 `/thinking select` TUI selector baseline：新增 `CodingAgentThinkingSelector`，真实交互式 editor 会话中 `/thinking select` 会复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示当前模型可用的 reasoning level 和说明文案，选择后立即更新 runner `ThinkingLevel` 并写回 settings `defaultThinkingLevel`；裸 `/thinking` 保留 current status 语义，取消或无 selector 不修改 settings。当前仍不是完整上游全 settings UI parity。
- `Tau.CodingAgent` 已补 `/logout` OAuth provider selector baseline：真实交互式 editor 会话中裸 `/logout` 或 `/logout select` 会复用 `CodingAgentAuthSelector`，只列出当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后删除对应 `auth.json` credential entry；无 selector 的裸 `/logout` 继续使用当前 provider，显式 `/logout <provider>` 保持兼容。环境变量和 `models.json` credential 配置不会被修改，当前仍不是完整上游 OAuth login dialog/session parity、credential refresh UX 或真实外部 OAuth e2e。
- `Tau.CodingAgent` 已补 `/login` OAuth provider selector baseline：真实交互式 editor 会话中裸 `/login` 或 `/login select` 会复用 `CodingAgentAuthSelector` 列出当前注册且有 OAuth provider 的 provider，选择后调用现有 OAuth login flow 并保存到 `auth.json`；无 selector 的裸 `/login` 继续使用当前 provider，显式 `/login <provider>` 保持兼容。当前仍不是完整上游 OAuth login dialog/session parity。
- `Tau.CodingAgent` 已补 `/auth select` TUI provider status selector baseline：新增 `CodingAgentAuthSelector`，`/auth [current|select|provider]` 可查看当前 provider、交互式选择 provider 或显式查询 provider 的 auth 状态；真实交互式 editor 会话中 `/auth select` 复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示 configured/missing、source、OAuth/login capability，选择后只输出所选 provider status。当前 selector 只检查状态，不写 `auth.json`、不执行 OAuth login，也不是完整上游 OAuth login-session parity。
- `Tau.CodingAgent` 已补 `/scoped-models` TUI selector baseline：新增 `CodingAgentScopedModelsSelector`，裸 `/scoped-models` 或 `/scoped-models select` 在真实交互式 editor 会话中打开 `TuiMultiSelectList` / `TuiMultiSelectSession`，支持过滤、模型 toggle、provider toggle、enable all、clear、reorder、Ctrl+S save 和 Esc cancel；无 selector 的会话继续输出当前 scope 摘要，命令式 `set/add/remove/clear/all` 保持兼容，并已支持 `provider/model:thinking` suffix。`/scoped-models` 作为配置入口仍维护全部注册模型 scope；空闲输入 prompt 的 Ctrl+P / Ctrl+Shift+P、Ctrl+L / `/model select` 会在同一 scope 上再按 auth-configured 模型过滤实际候选；model selector search/chrome、scoped per-entry thinking 和模型能力 clamp baseline 已完成，完整上游 theme/dynamic-border/terminal-host parity 与 per-entry thinking 编辑 UI 仍未完成。
- `Tau.Tui` 已补多选 selector foundation：新增 `TuiMultiSelectList` / `TuiMultiSelectSession`，固定 null=all enabled、显式有序选择、filter、provider 分组切换、Alt+Up/Down reorder 和 save/cancel result；当前已被 `/scoped-models` 接线复用，但仍不是完整上游 terminal host、message/status area 或全局 selector framework。
- `Tau.CodingAgent` 已补 `/settings` TUI settings-list baseline：`CodingAgentSettingsSelector` 在真实交互式 editor 会话中打开 `TuiSettingsList` / `TuiSettingsListSession` 主设置面，可写回 auto-compaction、terminal show images / clear on shrink、image auto-resize / block images、show hardware cursor、editor padding、autocomplete max visible、steering/follow-up mode、tree filter、thinking level、quiet startup、collapse changelog、install telemetry，并保留 scoped models 或 theme 既有子 selector 入口；`/settings current` / `/settings path` 保留摘要和路径查询，非交互会话裸 `/settings` 继续输出摘要。package source/list/local resource baseline 已补；当前仍不是完整上游 submenus、transport/shell path/npm command runtime wiring、部分设置字段真实 runtime wiring 或完整 TUI edit parity。
- `Tau.CodingAgent` 已补 `/theme select` TUI selector baseline：新增 `CodingAgentThemeSelector`，复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 显示 theme list；`CodingAgentCommandRouter` 通过可注入 selector 保存选中的 theme，取消时不改 settings，生产入口只在真实交互式 editor 存在时启用。当前仍不是完整上游 theme rendering、theme file watcher 或 TypeScript extension runtime parity。
- `Tau.Tui` 已补最小 ANSI render surface：新增 `TuiAnsiRenderSurface`，把 `TuiRenderDiff` 翻译为 synchronized output buffer；full redraw 会 clear/home 后写全量行，line diff 会定位行、清行并写 replacement text。当前仍不是完整 viewport/scrollback renderer、overlay compositor、hardware cursor 或 message/status area。
- `Tau.Tui` 已补 selector session / overlay host baseline：新增 `ITuiRenderSurface` render sink 合同、`TuiOverlayHost` 单组件 render/input loop 和 `TuiSelectorSession`，把 `TuiSelectList` 放进可测试的打开、按键、diff apply、选择/取消返回流程。当前仍不是完整上游 terminal host、overlay compositing、ANSI synchronized output、viewport/scrollback、message/status area 或 CodingAgent selector 接线。
- `Tau.Tui` 已补组件/render/selector foundation：新增 `ITuiComponent` / `ITuiInputComponent` 抽象、`TuiContainer` / `TuiBox` Box background/cache baseline、`TuiTextBlock` Text background/cache baseline、`TuiTruncatedText` 首行截断组件、`TuiText` 宽度/截断/wrap/background helper、带 footer hint 的 `TuiSelectList` 单选列表基础层和 `TuiDiffRenderer` 纯函数式差分渲染计划器。当前这是库内可测试 foundation，不等于完整上游 terminal host、overlay compositing、message/status area、ANSI terminal renderer或 theme rendering。
- `Tau.CodingAgent` 已补 `/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline：命令会读取当前 settings `theme`，复用 `CodingAgentThemeStore` 列出 built-in、用户、项目、env/CLI 和 extension-contributed themes；`select` 会在交互式会话中打开 TUI selector 并保存所选 theme，`set` 会校验主题存在后写回 settings，`clear` 清空字段并回到默认 `dark`。完整上游 theme rendering、theme file watcher 和 TypeScript extension runtime 仍未完成。
- `Tau.CodingAgent` 已补 context files baseline：启动时默认先加载用户目录 `~/.tau/AGENTS.md` 或 `~/.tau/CLAUDE.md`，再按 parent-to-cwd 顺序加载项目链上的 `AGENTS.md` 或 `CLAUDE.md`；同一目录优先 `AGENTS.md`。生成 system prompt 会追加 `# Project Context`；`--no-context-files` / `-nc` 可禁用默认加载，`/reload` 会重读 context files，并在 runner 使用生成 system prompt 时刷新 skill/context inventory。完整 theme selector、TUI theme rendering、theme file watcher、TypeScript extension runtime 和 full resource selector 仍未完成。
- `Tau.CodingAgent` 已补 RPC `new_session.parentSession` metadata baseline：`--mode rpc` 的 `new_session` 会读取上游同名可选字段，把它写入 JSONL session header 的 `parentSession`，并继续追加 `session_info action=new`；后续 `/session`、`/tree` 和 HTML transcript export 可复用现有 parent metadata 展示链路。CLI `/new` 默认行为保持不变。
- `Tau.CodingAgent` 已补 RPC `get_settings` / `update_settings` baseline：`--mode rpc` 可读取当前 settings 文件路径和 Tau 已支持 settings 字段，并可批量更新默认模型、tree filter、retry、default thinking、enabledModels、steering/follow-up mode 和 auto-compaction enabled；更新后会同步当前 runner model/thinking/queue mode、retry options 与 auto-compaction 状态。完整上游 settings selector UI、theme / terminal / packages 等全量配置面、TypeScript extension runtime integration 和 exact schema parity 仍未完成。
- `Tau.CodingAgent` 已补 RPC `bash` / `abort_bash` baseline：`--mode rpc` 可通过可注入 `ICodingAgentShellRunner` 后台执行一次 shell 命令，返回 `output`、`exitCode`、`cancelled`、`truncated` 和可选 `fullOutputPath`；`abort_bash` 只取消当前 shell 命令，并发 `bash` 会被拒绝；后续切片已补 `bash_output` chunk 和 `bash_event` lifecycle baseline，本轮又补上 .NET RPC client helper baseline。exact bash schema parity 和更完整终端子系统仍未完成。
- `Tau.CodingAgent` 已补 RPC queue / auto-compaction settings controls baseline：`--mode rpc` 支持 `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction`，`get_state` 返回 runner/settings backed `steeringMode`、`followUpMode` 和 `autoCompactionEnabled`；settings 新增 `steeringMode`、`followUpMode`、`autoCompactionEnabled`，并兼容旧 `queueMode` 读取。完整上游 settings selector UI、TypeScript extension runtime integration 和 exact schema parity 仍未完成。
- `Tau.CodingAgent` 已补 RPC auto retry baseline：`--mode rpc` 支持 `set_auto_retry` / `abort_retry`，复用现有 retry policy、settings retry 字段和 JSONL `auto_retry_start` / `auto_retry_end` audit；headless prompt 失败后会按同一 rollback 语义 retry，`abort_retry` 可取消 pending retry delay。完整上游 settings selector UI、TypeScript extension runtime integration 和 exact schema parity 仍未完成。
- `Tau.CodingAgent` 已补 RPC session switch / fork messages baseline：`--mode rpc` 支持 `switch_session` 恢复指定 JSONL tree session，并支持 `get_fork_messages` 返回可供 headless fork selector 使用的 user message `{ entryId, text }` 列表。完整上游 settings selector UI、TypeScript extension runtime integration 和 exact schema parity 仍未完成。
- `Tau.CodingAgent` 已补 RPC cycle model baseline：`--mode rpc` 支持 `cycle_model`，按 settings `enabledModels` 有序 scope 或全部可用模型切换到下一个模型，遇到 `provider/model:thinking` suffix 时同步 runner/default thinking，保存默认模型，返回 `{ model, thinkingLevel, isScoped }`；候选不足两个时返回显式 `data: null`，active prompt 期间拒绝切换。完整上游 settings selector UI、TypeScript extension runtime integration、exact schema parity 和更完整终端子系统仍未完成。
- `Tau.CodingAgent` 已补 RPC thinking controls baseline：`--mode rpc` 支持 `set_thinking_level` 与 `cycle_thinking_level`，复用现有 runner `ThinkingLevel` 和 settings `defaultThinkingLevel` 语义；active prompt 期间拒绝改 thinking，`cycle` 到 off 时返回显式 `data: null`。完整上游 settings selector UI、TypeScript extension runtime integration、exact schema parity 和更完整终端子系统仍未完成。
- `Tau.CodingAgent` 的 settings 命令已从只读摘要推进到 `/settings [current|path|select]`：交互式 `select` 当前使用 `TuiSettingsList` 主设置面，可写回 auto-compaction、terminal show images / clear on shrink、image auto-resize / block images、show hardware cursor、editor padding、autocomplete max visible、steering/follow-up mode、tree filter、thinking level、quiet startup、collapse changelog、install telemetry，并保留 scoped models 和 theme 子 selector 入口；`current/path` 仍保留可脚本化查询。package source/list/local resource baseline 已补；完整上游 submenus、transport/shell path/npm command runtime wiring、部分设置字段真实 runtime wiring 和完整 TUI edit parity 仍未完成。
- `Tau.CodingAgent` 已补 `/changelog [count|all]` baseline 与 startup notice baseline：命令读取 Tau 本地 release notes 表（默认 `docs/releases/feature-release-notes.md`，可由 `TAU_CODING_AGENT_CHANGELOG_FILE` 覆盖），输出日期、功能域、用户价值和变更摘要；交互式 fresh session 启动现在会按 `lastChangelogVersion`、`collapseChangelog`、`enableInstallTelemetry`、`PI_OFFLINE` 和 `PI_TELEMETRY` 处理版本记录、折叠/完整 notice 和 best-effort telemetry。完整上游 CHANGELOG entry slicing、真实安装/更新 telemetry backend e2e 和完整 TUI changelog rendering 仍未完成。
- `Tau.CodingAgent` 已补 `/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline：命令查看或维护 settings `enabledModels` 有序模型 scope，并支持 `provider/model:thinking` per-entry suffix；`clear` / `all` 回到 all enabled / no filter；交互式 selector 可多选、过滤、按 provider 批量切换并重排 scope，保存时保留既有 suffix；该配置入口仍基于全部注册模型，实际 Ctrl+P / Ctrl+Shift+P、Ctrl+L 和 `/model select [search]` 会按同一 scope 取已配置凭证模型做循环或单选，并按目标模型能力 clamp effective thinking。model selector search/chrome、per-entry thinking 与模型能力 clamp baseline 已完成，完整上游 theme/dynamic-border/terminal-host parity 和 per-entry thinking 编辑 UI 仍未完成。
- `Tau.CodingAgent` 已补 `/logout [select|provider]` baseline：命令会删除本地 `auth.json` 中对应 provider 的 credential entry；交互式 selector 会筛选当前有本地 OAuth credential 且注册了 OAuth provider 的 provider；无 selector 时未传 provider 仍使用当前 runner provider；环境变量和 `models.json` credential 配置保持不变，不回显 secret。完整上游 OAuth login/session parity 仍未完成。
- `Tau.CodingAgent` 已补 `/reload` baseline：命令会重读 settings 并同步 retry policy / default thinking level / queue mode，重读 JSON extension commands/resources 并刷新 extension-contributed prompt/skill/theme paths，重载 prompts、skills、context files、theme status 和交互式 editor keybindings；runner 使用生成 system prompt 时会刷新 skill/context inventory。完整上游 theme selector、TUI theme rendering、theme file watcher、TypeScript extension runtime 和 full resource selector 仍未完成。
- `Tau.CodingAgent` 已补 `/hotkeys` 当前 editor keybinding listing baseline：命令只读取 host 注入的 `IKeyBindingMap`，会反映自定义 keybindings 和 `EditorAction.None` 禁用默认绑定；print/RPC/redirected 模式没有 editor 时返回 unavailable。完整上游 app/session/tree/extension shortcut registry 仍未完成。
- 上一轮 6-agent 截止时，`Tau.CodingAgent.Tests` 已提升到 336 个测试：覆盖 scoped model pattern 解析、settings suffix round-trip、`/scoped-models` thinking suffix 展示/保存、selector suffix 保留、CLI cycle thinking override、RPC cycle thinking override、thinking 能力 clamp、model selector search/chrome、scope/detail、auth footer hint、auth-configured filtering、model cycle、显式 `/model` / `/provider`、`/metadata` session/entry inspector 和 RPC `get_available_models` / `set_model` / `cycle_model` 回归。`Tau.Tui.Tests` 已提升到 92 个测试，覆盖 `TuiSelectList` footer hint 与 `TuiMessageArea` / `TuiStatusBar` foundation；`Tau.Agent.Tests` 已提升到 68 个测试，覆盖 steering/follow-up `all` 与 `one-at-a-time` drain 语义、Mom sandbox validation 入口和 `MomLocalDelegationFlow`；`Tau.Ai.Tests` 已提升到 196 个测试，覆盖 dynamic OpenAI-compatible provider registration。本轮最新 counts 见本节顶部和测试评分行。

## 当前基线（2026-05-02 起持续增量）

| 区域 | 评分 | 原因 | 下一步 |
| --- | --- | --- | --- |
| 产品定义 | C | 已有 `docs/ARCHITECTURE.md`、`next.md` 和完整移植总 plan `docs/exec-plans/active/2026-05-10-tau-complete-pi-mono-port.md`，并且不再只围绕 CLI 说话，`WebUi / Mom / Pods` 也已经进入真实产品切片。当前问题不再是“有没有模块”，而是每个模块距离上游 parity closure 还有明确缺口。 | 按完整移植总 plan 继续把每个应用面的剩余能力写实：Mom Slack/workspace/sandbox，CodingAgent session tree，Ai OAuth/provider e2e，WebUi 流式/附件，Pods lifecycle。 |
| Tau.Ai | C | 已补上 `ModelCatalog`、内建模型表、provider auth resolver、auth.json 读取、OAuth provider registry，以及 Azure OpenAI Responses / OpenAI Codex / Mistral / Vertex / Gemini CLI / Bedrock 的第一轮接线；当前已把 `openai-responses`、`openai-codex-responses` 与 `azure-openai-responses` 从 `OpenAiCompatibleProvider` 兜底提升为专用 SSE provider，把 Codex Responses 扩展到 WebSocket/auto transport 与 session socket cache，并补齐 Responses/Codex service-tier usage 与 flex/priority cost multiplier、GitHub Copilot Responses 动态 headers / vision 行为与 tool-result image payload，把 `mistral-conversations` 切到专用 Mistral provider，把 Bedrock 从 placeholder 推进到 ConverseStream 专用 provider，把 Vertex ADC 从 placeholder 推进到 service account / authorized user token exchange，并把 Gemini CLI / Antigravity 请求、headers、fallback 与 retry 行为补到更接近上游；这轮又补了 generated model seed/generator/catalog 接缝、typed/default selection、`Model.Compat` 元数据、models.json custom model/provider 配置入口、request auth/header 合并、显式 OpenAI-compatible dynamic provider registration、AI-level tool argument schema validation helper、Bedrock SSO cache token refresh baseline（过期 access token + cache refresh metadata -> OIDC `CreateToken` + best-effort cache writeback），以及 `Tau.Ai.Cli` standalone help/list/login baseline（含 `--auth-file auth.json` cwd auth 兼容入口）；本轮 release artifact 已补 `pi-ai` wrapper alias 并用 smoke 验证 `pi-ai list`。OpenAI-compatible provider 已能按显式 compat 消费 stream usage、max tokens field、reasoning format/map、z.ai tool stream、tool strict mode、OpenRouter routing 与 Vercel AI Gateway routing，`ModelCatalog` 也能启动时合并 `TAU_MODELS_FILE` / `.tau/models.json` / `~/.tau/models.json`。仍缺真实云端/OAuth e2e、package/global install alias parity、SSO OIDC client registration renewal、cache 并发/多 cache 边界、更完整 runtime config UX、完整 AJV/TypeBox validation keyword parity 与更广义的 dynamic provider API。 | 继续把剩余 provider 从“可发现/可配置/可序列化”推进到“可完整调用”，优先补 provider/OAuth e2e、package/global install alias parity、SSO registration renewal 边界和 runtime config UX。 |
| Tau.Agent | B | 双层循环、工具执行器、状态与事件骨架已具备，继续稳定承接各应用面；当前已新增 `Agent` / `AgentOptions` 高层 facade baseline，覆盖 prompt、continue、subscribe、steering/follow-up queue、idle settlement、reset 和 prompt/tool-result message lifecycle；本轮又补上 `agent_end.messages`、`turn_end.message/toolResults`、tool update partial result、schema validation、error-as-tool-result baseline、facade/runtime/proxy/event public API compile-sample、high-level facade failure/cancel synthetic assistant message baseline、low-level `AgentRuntime.RunStream(...)` / `EventStream<AgentEvent, ChatMessage[]>` wrapper、parallel tool start/update timing、assistant stream cancellation terminal event、aborted `ErrorEvent` stop reason preservation、tool cancellation cleanup、parallel sibling cancellation result preservation 和 `TransformContext` cancellation；当前 Agent platform 首版 public surface 也已落地到 `src/Tau.Agent/Platform/**`：`AgentApplication` / builder / run result / delegate tool / UI-free session store，使外部 .NET 应用可以用最小代码创建 Agent、注册 tools、保存/恢复 conversation、接入 provider/tool runtime log 并读取 usage/stop reason。`AgentRuntime` 已写 `provider/run.start|run.end` 摘要 trace，`AgentPlatformTests` 固定 fake provider、delegate tool、session save/restore、provider run + tool execution runtime log 摘要和 cancellation rollback；Console / HTTP examples 已提供源码模板和 smoke，并已通过全仓 gate 与 `verify-dotnet.ps1 -RunSmoke`。剩余更大范围缺口转为真实 provider/OAuth e2e、public export 形状决策和真实 proxy/e2e 验证。 | Agent platform baseline 已完成；下一步如继续 Agent platform，应做真实 provider/OAuth e2e、NuGet consumer smoke 或 package registry 发布演练。 |
| Tau.CodingAgent | C | 已具备最小 CLI 宿主、可测试主循环、基础工具链、本地 session 持久化、settings/model selection、auth 状态入口和手动 compaction；运行时默认 model/provider 已接到 `ModelCatalog`，独立 `csproj` 的 build / run 已恢复，并且 `Tau.CodingAgent` / `Tau.WebUi` / `Tau.Mom` / `Tau.CodingAgent.Tests` / `Tau.Agent.Tests` 已从 DLL `HintPath` 收回到正常 `ProjectReference`。显式 `Create(provider, model, history)` 宿主工厂继续作为 WebUi/Mom 共享 runtime 边界；当前继续保留 `CodingAgentSessionStore` flat snapshot JSON（`TAU_CODING_AGENT_SESSION_FILE` 或 `./.tau/coding-agent-session.json`），同时新增 `CodingAgentTreeSessionStore` JSONL tree（默认 `./.tau/coding-agent-session.jsonl`，`TAU_CODING_AGENT_TREE_SESSION_FILE` 或 `.jsonl` 形式的 `TAU_CODING_AGENT_SESSION_FILE` 可覆盖），支持 append-only `session` header、`message` / `model_change` / `session_info` / `label` / `compaction` / `branch_summary` / `auto_retry_start` / `auto_retry_end` / `tree_state` entries、entry `id/parentId/timestamp`、header `cwd/parentSession` summary、label timestamp、runner message diff 同步、当前 branch restore、session-scoped collapsed entry ids、settings `treeFilterMode` 默认过滤、settings `treeCollapsedEntryIds` 兼容 fallback、settings retry fields/default thinking level/steeringMode/followUpMode/autoCompactionEnabled/theme/enabledModels scoped model list、运行中 steering/follow-up CLI baseline、`--mode rpc` JSONL baseline（`prompt` / `steer` / `follow_up` / `abort` / `new_session` / `get_state` / `set_model` / `cycle_model` / `get_available_models` / `set_thinking_level` / `cycle_thinking_level` / `set_auto_retry` / `abort_retry` / `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` / `switch_session` / `get_fork_messages` / `compact` / `fork` / `clone` / `get_session_stats` / `get_messages` / `get_commands` / `export_html` / `get_last_assistant_text` / `set_session_name`）、`/session` 估算 token/context usage、auto-compaction threshold budget 与 retry policy、`/tree` filter/search modes、`/tree --interactive` 移动/选择/取消/overlay search/filter cycle/next-prev match/Left-Right page/Ctrl-Alt branch navigation/Space fold/selected metadata/session fold persistence、`/label`、`/fork`、`/fork --summarize [instructions]`、`/clone`、`/resume`、`.jsonl` export/import、branch summary restore context baseline、手动 `/compact` compaction metadata baseline、token-budget recent-message retention baseline、split-turn prefix summary metadata/text baseline、opt-in auto-compaction threshold baseline、context overflow compact-and-retry baseline、普通回合失败/取消 rollback baseline、host-level retryable error auto-retry baseline、Tau-native retry audit event baseline、Tau-native `/retry` settings command baseline、RPC `set_auto_retry` / `abort_retry` baseline、RPC `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` baseline、RPC `switch_session` / `get_fork_messages` baseline、Tau-native `/thinking [current|select|cycle|off|minimal|low|medium|high|xhigh]` settings/TUI selector baseline、`/settings [current|path|select]` CLI/settings/TUI selector baseline、`/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline、`/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline、Ctrl+P / Ctrl+Shift+P model cycle baseline、`/model select [search]` / Ctrl+L model selector baseline、model selector auth filtering footer/scope/detail/search chrome baseline、scoped model thinking-level per-entry baseline、thinking 模型能力 clamp baseline、retry cancellation visibility baseline、`/export` 默认 HTML、`.html/.htm` standalone transcript export、HTML branch outline、HTML branch filter/search、HTML cwd/parent metadata、HTML current-branch JSONL timeline、label/model/compaction/branch-summary/retry/tree-state event rendering、HTML split-turn prefix rendering/search、HTML branch summary read/modified file rendering、HTML 内嵌 JSONL download、message deep-link/copy-link baseline、HTML code fence rendering baseline、HTML inline code span rendering baseline、HTML plaintext link rendering baseline、HTML Markdown heading/list/blockquote block rendering baseline、HTML Markdown strong/emphasis span rendering baseline、HTML Markdown pipe table rendering baseline、HTML Markdown task list rendering baseline、HTML image metadata caption baseline、HTML long tool result folding baseline、HTML tool call JSON argument rendering baseline、HTML long tool call arguments folding baseline、`/share` secret Gist baseline、prompt template discovery/expansion baseline、skill command discovery/expansion baseline、context files baseline、theme loader/status baseline、JSON extension command discovery/execution/resource/load diagnostics baseline、`/hotkeys` 当前 editor keybinding listing baseline、`/reload` settings/resources/prompts/skills/context-files/theme-status/keybindings baseline、`/auth [current|select|provider]` provider status selector baseline、`/login [select|provider]` OAuth provider selector baseline、`/logout [select|provider]` OAuth provider selector/auth.json credential removal baseline、`/changelog [count|all]` release notes baseline 和 `/session` tree stats；CLI 当前支持 `/help`、`/reload`、`/hotkeys`、`/settings`、`/theme`、`/name`、`/copy`、`/files`、`/export`、`/share`、`/import`、`/new`、`/session`、`/tree`、`/label`、`/fork`、`/clone`、`/resume`、`/quit`、`/model`、`/provider`、`/models`、`/providers`、`/scoped-models`、`/prompts`、`/skills`、`/extensions`、`/auth`、`/login`、`/logout`、`/changelog`、`/retry`、`/thinking`、`/history`、`/find`、`/clear` 和 `/compact`。仍缺完整上游 TreeSelector、自动 branch switching summarization hooks、多选、完整 metadata inspector、LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI、完整 compaction extension events/cancellation UI、完整 settings selector UI、完整 model selector theme/dynamic-border/terminal-host parity/per-entry thinking UI editor、完整 OAuth login/session parity、credential refresh UX、完整 TUI 运行中输入 overlay / keybinding hints、完整 RPC extension UI / bash / full settings parity、完整 TypeScript extension runtime/custom tools/events、完整上游 theme selector parity、TUI theme rendering、theme file watcher、full resource selector、启动 changelog/collapse/telemetry parity、完整 Markdown/highlight renderer、custom tool renderer、richer HTML template 和 Tau 专属 share viewer。 | 继续补上游 TreeSelector 剩余交互、自动 branch switching summarization hooks、上游 LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI、完整 compaction event/cancellation parity、完整 settings selector UI、完整 model selector theme/dynamic-border/terminal-host parity/per-entry thinking UI editor、完整 OAuth login/session parity、credential refresh UX、完整 RPC extension UI / bash / full settings parity、完整 TypeScript extension runtime、theme rendering/watcher、resource selector、完整 Markdown/highlight renderer、richer HTML template、share viewer 和 richer rendering。 |
| Tau.Tui | C | 已经有最小终端抽象、transcript、输入缓冲和会话层，并承载 `Tau.CodingAgent` 主路径；输入编辑器、持久化 history、自定义 keybinding、组件树基础层、`TuiBox` Box background/cache baseline、`TuiTextBlock` Text background/cache baseline、`TuiTruncatedText` 首行截断组件、文本宽度/截断/wrap/background helper、`TuiSpacer` 空行组件、单选/多选选择列表基础层、`TuiSettingsList` settings-list foundation、`TuiSettingsListSession` session wrapper、纯函数式 diff 计划器、render surface 合同、最小 ANSI diff sink、单组件 selector session / overlay host baseline、`TuiMessageArea` message transcript foundation、`TuiStatusBar` status line foundation、`TuiScrollbackBuffer` 纯内存 viewport/scrollback foundation、`TuiTranscriptViewport` 组合 viewport foundation、`TuiTranscriptViewportHost` viewport host/diff apply wrapper、`TuiTranscriptSession` runtime seam、terminal image/image component foundation、独立 `TuiInputSequenceBuffer` stdin-buffer foundation 和 `TuiProcessTerminal` lifecycle seam 已经落地。当前已接回 CodingAgent theme/settings/scoped-model/auth status selector、`/login` OAuth provider selector 和 `/logout` OAuth provider selector baseline；`TuiSettingsList` 已替换产品级 `/settings select` 主设置面，但完整上游 settings submenus、focus stack、theme rendering 和真实 runtime terminal/editor/image wiring 仍未补齐。ProcessTerminal 已有 raw/bracketed paste/Kitty/modifyOtherKeys/drain/diagnostic write-log 本地合同测试，但仍未有真实 TTY/PTY smoke，也未接管 CodingAgent 主屏，仍缺完整 overlay compositing、硬件 cursor 和 theme rendering。 | 下一步把 message/status/scrollback/viewport/input-sequence foundation 接入真实 terminal host并补 live TTY/PTY smoke，或继续补完整 settings/OAuth/resource selector 子菜单与 runtime wiring。 |
| Tau.WebUi | C | 已经从 ASP.NET Core `Hello World` 提升为第二层 Web 宿主：首页聊天页、status/catalog/session/messages API、provider/model 选择、`output/webui-sessions.json` 持久化、NDJSON 流式消息、附件 upload/preview/send、tool timeline、session delete/export/import、title rename、last-opened session restore baseline、auth status 和 client-side Markdown rendering 都已可跑；`WebChatService` fake-runner、Minimal API endpoint 和 browser flow 已覆盖流式 assistant 写回、附件 prompt、session 持久化、NDJSON streaming、export/import/delete/rename/restore、默认 runner log sink 透传、CodingAgent JSONL preview/import 审计和错误合同。当前又补了 server-backed artifact/runtime baseline、HTML artifact provider/operation baseline、lightweight viewer baseline、browser-side JavaScript REPL execution host、LLM-facing artifacts tool baseline 和 LLM-facing JavaScript REPL active-browser bridge baseline：`WebArtifactStore` / `WebArtifactService` 默认写 `output/webui-artifacts.json`，提供 per-session artifact CRUD endpoints、`/api/sessions/{id}/runtime/messages` runtime bridge、artifact pane、text/image/html/Markdown/SVG/PDF/generic preview、HTML sandbox helper `listArtifacts/getArtifact/createArtifact/updateArtifact/rewriteArtifact/createOrUpdateArtifact/deleteArtifact/htmlArtifactLogs`，并在 session 删除时同步清理 artifact state；HTML artifact iframe 现在还能通过 `listAttachments/readTextAttachment/readBinaryAttachment` 读取当前 session user attachment snapshot，并通过 `returnDownloadableFile(fileName, content, mimeType?)` 发送一次性 `file-returned` metadata，不写入 artifact store。artifact runtime action 现在本地对齐上游 create no-overwrite、update `old_str/new_str` 首个匹配替换、rewrite 整文件替换、delete/get 和 `htmlArtifactLogs`/`logs` console log 读取语义；artifact viewer 现在本地覆盖 Markdown/SVG/PDF/image/text/generic 的 routing/render/download baseline；`window.executeJavaScriptRepl(code)` 会创建隐藏 sandbox iframe，复用同一 attachment/artifact/file-return/console runtime bridge，执行 async browser JS 后返回 console、return value、error 和 returned files，并通过 Playwright 回归证明可读取附件、写 artifact 和返回文件。默认 WebUi runner 现在注册 session-bound `artifacts` 和 `javascript_repl` AgentTools：`artifacts` 用同一 artifact store 执行 create/update/rewrite/get/delete/logs，`javascript_repl` 通过 `WebUiJavaScriptReplBridge` 把 tool request 交给活动浏览器长轮询执行，并返回 output/error 与 `details.files[]` base64 metadata；前端在 `tool_end: artifacts/javascript_repl` 后刷新 artifact pane。`WebUiToolPrompts` 已移植 artifacts、javascript_repl、attachments、artifacts runtime 和 file-download provider description baseline。`POST /api/sessions/import.coding-agent-jsonl` conservative import baseline 仍只消费 timeline-derived `preview.Messages`，把 CodingAgent timeline 保守持久化为 WebChat DTO，不替换 `WebChatStore`，也不保留完整 branch/tree 结构。仍缺完整 CodingAgent JSONL branch/session-tree 导入与语义对齐、full artifact message reconstruction、Docx/Excel rich viewers、pdfjs page renderer/view-code toggles、reusable Lit component package、extension sandbox URL mode 和 release/static browser smoke。 | 继续把 WebUi session 从 WebChat DTO baseline 向 CodingAgent branch/tree session 语义对齐；artifact lane 下一步应补 full renderers、artifact message reconstruction 或真实 release/static browser smoke，不能把当前 JS REPL bridge baseline 误标为完整 upstream package parity。 |
| Tau.Mom | C | 已经从 worker 模板提升到结构化本地委派宿主：`--once`、inbox/outbox/archive/events、`.txt/.md/.json` 输入、`provider/model/workingDirectory/title/metadata/attachments` 字段和结果落盘都已现场跑通；前一轮把 runner / result schema 收口为结构化 `DelegationToolEvent`、stop reason、`DelegationUsage`（含 token + 可选总成本），并把 `RuntimeDelegationAgentRunner` 改为可注入 `ICodingAgentRunner` 工厂；本轮又把生产入口接到 `ITauLogSink`，并记录 delegation/response/tool/usage/end runtime log events，取消路径会先写 `delegation.end stopReason=cancelled` 再 rethrow；这一轮继续把 `title`、`metadata`、`attachments` 真正接进运行时上下文和 outbox 契约，并让本地存在的 attachment staging 到 `workingDirectory/attachments/`，通过 `attachments/attachments.jsonl` 和 `log.jsonl` 保留 `original/local` 元数据；runner 输入现在还会注入 `<mom_runtime_context>`，明确本地 workspace/channel layout、events 文件格式、attachment manifest、memory/log/status 路径和 `[SILENT]` 事件响应约定；`ChannelWorkspaceLayout` 会创建 `scratch/`、workspace/channel `skills/`、`attachments/` 和 `events/`，并把 `SYSTEM.md` 与 Agent Skills prompt inventory 注入 prompt context；`workingDirectory/context.json` 会恢复/保存 Tau-native channel session snapshot，让同一 workdir 的 delegation 继承上一轮 runtime messages；调用 runner 前会写 `workingDirectory/last_prompt.jsonl`，记录 mom runtime context、delegation context、实际 runner input、恢复的 session messages、当前 prompt 和 attachment/image attachment count；`workingDirectory/MEMORY.md`、父目录 `MEMORY.md` 与 `workingDirectory/log.jsonl` 的最近非 bot channel history 会进入 prompt context；本地 file delegation 现在也会把用户请求和 bot 结果追加到同一个 `log.jsonl`，把当前或最近一次运行状态写到 `workingDirectory/status.json`，并基于未过期 `running` 状态跳过同一 workdir 的新请求；新增 `MomEventProcessor` 会把本地 `events/*.json` 的 `immediate/one-shot/periodic` 唤醒转换成 inbox 委派请求，复用相同 channel/workspace seam；新增 `MomChannelMessage` / `MomChannelAttachment` 作为 Slack-compatible envelope，file requests 和 local events 已经先统一映射 channel/user/ts/thread/attachments/provider/model/title/metadata，再生成 `DelegationRequest`；新增 `IMomChannelTransport` / `IMomChannelResponder` / `MomChannelMessageProcessor`，先固定 channel transport/responder contract、busy-state、true cancellable stop、typing、thread response、attachment staging、status/log writeback 与 runner 调用；新增 `SlackEventMapper`，固定 Socket Mode `app_mention`、DM、bot/self/subtype skip、mention stripping 和 file metadata 到 `MomChannelMessage` 的 receive-side 映射规则；新增 `SlackSocketModeTransport` / `SlackSocketModeWorker`，固定 `auth.test`、`apps.connections.open`、WebSocket text frame、envelope ack 和 `SlackSocketModeEnabled` 配置开关；新增 `SlackBackfillService`，在 Socket Mode worker 启动前对已有 `log.jsonl` 的 channel 调 `conversations.history` 做 old-message log-only 回填；新增 `SlackWebApiResponder`，用 `HttpClient` 固定 `chat.postMessage`、thread message 与 `files.uploadV2` 的真实 Slack Web API 契约，并保持 token 不进入异常消息；新增 `SlackAttachmentDownloader`，用 bot token 下载 Slack private file URL 并复用 attachment manifest；新增 `MomChannelQueueDispatcher`，固定同频道顺序处理、不同频道独立推进、pending queue limit 和 stop bypass；新增 `MomChannelRunRegistry`，用 linked cancellation token 跟踪当前进程内 active channel run，stop 命令会取消 runner、写 `cancelled` status 并回复 `_Stopped_`；新增 `MomSandboxConfig` / `IMomSandboxExecutor` / `MomToolSet`，默认 host sandbox，配置层支持 `docker:<container>`，Docker validate/exec 路径已通过 `IMomSandboxProcessRunner` 可注入 seam 固定本地测试，并把 runner 默认工具切到上游同名 `bash/read/write/edit/attach`；skills 按上游方式作为 XML `<available_skills>` 暴露，脚本通过 Mom tools 使用，不另注册成 direct tool。runtime 日志仍是文本摘要级 baseline，不是完整 trace/correlation 协议；仍未接真实 Slack smoke 和真实 Docker container smoke。 | 下一步接真实 Slack smoke、Docker sandbox smoke 与更高层 delegation flow。 |
| Tau.Pods | C | 已从控制台占位提升为具备主动运维入口的 CLI：config init/validate/status、上游式 `pods` group、top-level `start/list/stop/logs/logs --follow/ssh/shell/agent` shim、no-arg `stop` stop-all 聚合、agent prompt 参数映射 + Tau.CodingAgent process runner baseline、probe/exec/lifecycle/model/setup/vLLM planner/orchestration service、known-model planning、usage-aware GPU allocation state baseline、deploy PID state writeback baseline、upstream `~/.vllm_logs/<name>.log` log path baseline、realtime log follow baseline、startup marker health/status baseline、deploy-time startup watcher baseline、save-before-watch/delete-config ordering baseline、model runner wrapper / pseudo-TTY script baseline、runner HF download / PI API key command baseline、duplicate configured deployment guard、operation `failureKind` baseline、active pod fallback、path/env default、missing config 空对象语义和上游 record-shaped config read/write 都已有本地合同。配置保存现在输出上游 `pods` / `models` record shape，旧 Tau list-shaped config 仍可 load 迁移；`Tau.Pods.Tests` 已有 pod config/probe/exec/shell/agent/lifecycle/model/setup/vLLM planner/orchestration/CLI/GPU allocation/PID/log-path/startup-marker/startup-watch/config-ordering/runner-wrapper/HF-download/API-key/duplicate-guard/log-follow 和 pod-agent process runner 回归。仍缺真实 pod prompt e2e、真实 SSH/HF download、真实 setup run/SCP/GPU smoke、真实 vLLM startup health smoke、多版本回滚、更完整远端 transport hardening和真实运维 smoke。 | 继续补真实 SSH/HTTP/HF smoke、真实 setup run smoke、真实 pod prompt e2e、多版本回滚、更完整远端 transport hardening和更高层 pod orchestration。 |
| 测试 | C | 当前六个测试项目都已有真实 targeted 覆盖。`Tau.CodingAgent.Tests` 现在覆盖 CLI initial-message builder / print image content-block、image preprocessor PNG resize / EXIF dimension metadata、read_file image resize metadata、RPC client helper、WebUi shared endpoint fixture 注册回归、assistant usage/cost flat + JSONL tree session persistence，以及 `/session` 和 RPC stats usage-cost display，当前完整 `Tau.CodingAgent.Tests` 为 489/489；`Tau.Agent.Tests` 现在覆盖 assistant usage cost enrichment、provider metadata preservation 和 proxy usage cost roundtrip，当前完整 `Tau.Agent.Tests` 为 120/120。`Tau.WebUi.Tests` 现在覆盖 artifact CRUD/runtime endpoint、unsafe filename/session cleanup、file-return metadata endpoint、artifact create/update/rewrite/delete/get/htmlArtifactLogs operation semantics、browser artifact pane refresh、Markdown/SVG/PDF/generic viewer baseline、HTML runtime bridge 静态回归，以及真实 headless Chromium 中 HTML artifact 读取 session attachment 并调用 `returnDownloadableFile` 的 provider flow；上一轮新增 LLM-facing `javascript_repl` bridge 回归，证明 session-bound JS REPL tool 会入队 request、HTTP long poll endpoint 可领取/完成 request、无活动浏览器返回 timeout error、runner 可通过 `javascript_repl` 取得 output 和 base64 file details，真实 Chromium 页面也能 polling pending tool request、执行 sandbox 并 POST result。当前完整 `Tau.WebUi.Tests` 为 61/61。`Tau.Pods.Tests` 现在覆盖 config path/env、missing config、record-shaped upstream config load/save、legacy list-shaped load、top-level `pods/start/list/stop/logs/logs --follow/shell/agent` shim、no-arg `stop` stop-all、agent prompt argument redaction/passthrough、pod-agent process runner argv/env/config/redaction/exit-code contract、usage-aware GPU allocation state、deploy processId/config pid state、save-before-watch/delete-config ordering、duplicate configured deployment guard、operation failure kind 输出、upstream vLLM log path plan/unit/logs command contract、realtime follow command contract、deploy-time startup watcher ready/failure/rollback、model runner wrapper/pseudo-TTY path/metadata/text/JSON/cleanup contract、runner HF download / PI API key command contract、startup complete/failure marker health/status 判定、probe/exec/lifecycle/model/setup/vLLM planner/orchestration、known-model planning、deploy health retry/backoff、failure classification、rollback cleanup 和 deploy prefetch/revision 闭环；本轮 focused CodingAgent image resize/EXIF gate 24/24、完整 CodingAgent 489/489、项目级 `verify-dotnet.ps1 -SkipRestore` 均已通过，项目级计数为 `Tau.Ai.Tests` 280、`Tau.Agent.Tests` 120、`Tau.Tui.Tests` 251、`Tau.CodingAgent.Tests` 489、`Tau.WebUi.Tests` 61、`Tau.Pods.Tests` 216；上一轮 focused CodingAgent usage-cost stats/display gate 248/248、完整 CodingAgent 484/484、focused Agent/Proxy gate 18/18、focused CodingAgent session/tree gate 10/10、session audit script smoke 15 assertions、完整 Agent 120/120、完整 CodingAgent 480/480 已通过。上一轮 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 已通过，并覆盖 tau-ai、Agent platform examples、WebUi 和 Mom smoke。 | 后续测试重点转向 clipboard desktop smoke、provider vision e2e、真实 provider/OAuth e2e、真实 Slack/Docker/Pods/WebUi release/static smoke、NuGet consumer smoke、真实 registry/signing rehearsal 和长期 product parity。 |
| CI/CD | C | 主 CI 已改为 `scripts/verify-dotnet.sh`，按显式项目顺序 restore / build / test，形成了真实可重复的项目级门禁；现在也已经覆盖到 `WebUi / Mom / Pods` 三个应用面。`Tau.CodingAgent`、`Tau.WebUi`、`Tau.Mom`、`Tau.CodingAgent.Tests` 和 `Tau.Agent.Tests` 已不再依赖预置 DLL `HintPath`，降低了 stale assembly 和并行构建顺序风险；`Tau.slnx` 已具备 solution-level build 入口，当前也纳入 `examples/Tau.Agent.ConsoleExample` 和 `examples/Tau.Agent.HttpExample`。当前本机 `bash scripts/verify-dotnet.sh --skip-restore` 仍因 WSL 缺少 `/bin/bash` 失败，属于本机 shell 环境问题；Windows PowerShell 入口继续作为本机权威验证链。`scripts/verify-dotnet.ps1 -RunSmoke` 现在除 `tau-ai`、WebUi、Mom smoke 外，也会调用 `verify-agent-platform-examples.ps1 -SkipRestore`，覆盖 Agent platform Console / HTTP examples，且本轮已通过。release 链现在能产出 current-RID 真实 Tau 可执行交付件，并已有 package publish/finalize/provenance/signing dry-run smoke；但 Agent platform examples 仍是源码模板，不是独立 app artifact。仍缺非宿主平台 executable smoke、examples/Photon/interactive raster assets/external export-html vendor parity、exact Unix wrapper/auth-backup parity、真实 NuGet/package registry 发布演练、真实 package signing/provenance rehearsal 和真实外部 e2e release smoke。 | 保持 Windows PowerShell gate 为本机权威链；下一步补真实 package registry 发布演练、NuGet consumer smoke 与 signing/provenance 实战。 |
| 可观测性 | C | 已有 `ITauLogSink` / `JsonlTauLogSink` / `NullTauLogSink` baseline，runtime event JSONL 默认做 string-value redaction；`TauRuntimeLogContext` 已提供最小 cross-module correlation 字段 baseline；`RuntimeCodingAgentRunner` 已写 `agent/run.start|run.end|run.cancel|run.error` 并生成/继承 `correlationId`；`AgentRuntime` 已写共享 `provider/run.start|run.end` trace，字段限于 provider/model/api、message/tool count、transport、usage/cost、stopReason、failureKind、duration 和 correlation/session/message id，不写 prompt、headers、api key、metadata、完整 messages、tool arguments 或 tool result；`ProviderAuthResolver.GetStatus` 已写 `auth/status.checked`，CodingAgent/WebUi auth status 生产路径已接入 runtime sink；WebUi message stream 默认 runner 现在也会把同一个 sink 和 `sessionId` / transient `messageId` 传给 `RuntimeCodingAgentRunner`，因此生产聊天路径可写带 correlation 字段的 `agent/run.*`；CodingAgent CLI 普通 turn 会把同一 per-turn context 传入 runner 并在 retry attempts 间复用，RPC prompt 会用 command id 作为 `correlationId/messageId` 并注入 tree `sessionId`；`ToolExecutor` 已通过 `AgentLoopConfig.LogSink` 写共享 `tool/execution.start|end` trace，并通过 `AgentLoopConfig.LogContext` 继承同一个 `correlationId/sessionId/messageId`，覆盖 tool id/name、execution mode、argument/result 尺寸、duration、success/isError 和轻量 failure kind，且不写完整 arguments/result；`RuntimeDelegationAgentRunner` 已写 Mom delegation/response/tool/usage/end events，并且生产 `Tau.Mom` 会从环境创建 log sink；Tau.Pods probe、vLLM、普通 lifecycle 和 model lifecycle 也已有摘要事件。当前仍主要是本地 JSONL 与文本摘要级观测，不是完整 trace/correlation 协议；仍缺 Mom/Pods 与真实 e2e trace 的统一 correlation 贯通。 | 继续把 Mom/Pods/真实 e2e trace 接到同一 correlation 语义，再把高价值 runtime smoke 纳入验证链。 |
| 安全与配置 | C | 当前已经补了环境变量矩阵、auth.json schema、OAuth login/refresh、OAuth 凭证读取/刷新写回和 Vertex ADC token exchange，配置边界比之前清楚；本轮又把默认 `./.tau/auth.json` / `./.tau/models.json` 加入忽略、让 Unix `auth.json` 写入权限收紧到 0600，并过滤 OAuth metadata 里的 credential 保留字段，避免 metadata shadow 密钥语义。`/logout [select|provider]` 已能删除本地 `auth.json` 对应 provider credential entry，交互式 selector 只列出当前本地 OAuth credential，不修改环境变量或 `models.json` credential 配置。models.json 状态检查现在只识别 credential 配置，不解析 env、不执行 `!command`、不回显 `apiKey` 或 credential header 值。`JsonlTauLogSink` runtime event log、`CodingAgentTreeSessionStore` JSONL tree session 写出、WebUi-local JSONL export/import、CodingAgent JSONL preview/import 和 Mom channel `log.jsonl` / `last_prompt.jsonl` 都已默认复用 `JsonlSecretRedactor` 做 string-value 脱敏；field key 保持不改，非标准 secret pattern 和真实云端凭证链仍未完整定义。WebUi 已具备会话级 provider/model 配置，Mom 已具备结构化 provider/model/workdir 请求，Pods 已具备 probe/exec 配置。 | 继续梳理非标准 secret pattern、AWS credential chain 与跨运行态 auth/config 迁移规则。 |
