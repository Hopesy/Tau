# 功能发布记录

## 2026-05

| 日期 | 功能域 | 用户价值 | 变更摘要 |
| --- | --- | --- | --- |
| 2026-05-23 | WebUi | Web 会话现在可以导出为可审计的 JSONL transcript，后续更容易向 CodingAgent session/tree 语义靠拢。 | 新增 `WebChatJsonlExporter` 和 `GET /api/sessions/{id}/export.jsonl`，首行输出 `type=session` header，后续按当前 WebChat DTO 输出线性 `type=message` entries，使用稳定 `message-000001` id 与线性 `parentId`。这是 WebUi-local baseline，不等于完整 CodingAgent branch/session-tree parity。 |
| 2026-05-23 | Mom | Docker sandbox 路径可以用本地 fake runner 验证，不再只能等真实 Docker 环境暴露命令构造问题。 | 新增 `IMomSandboxProcessRunner` seam，`MomSandboxExecutorFactory.ValidateAsync` 和 Docker executor 支持注入 runner；测试固定 `docker --version`、`docker inspect -f {{.State.Running}} <container>`、stopped/missing container、missing docker 以及 `docker exec -w /workspace <container> sh -c <command>` 构造。真实 Docker container smoke 仍后续补齐。 |
| 2026-05-23 | Pods | SSH exec 的本地进程参数构造更稳，不再依赖拼接后的 `ssh` 命令行字符串。 | `PodExecService` 改用 `ProcessStartInfo.ArgumentList` 构造系统 `ssh` argv，把 `-p`、端口、`-o BatchMode=yes`、`-o ConnectTimeout=5`、host 和 remote command 作为独立参数传入；测试覆盖精确参数顺序、复杂 remote command 作为单一参数传递，以及 lifecycle tests 继续检查生成的远端命令。 |
| 2026-05-23 | CodingAgent | thinking/reasoning level 会按当前模型能力自动降级，用户切到不支持 reasoning 或不支持 xhigh 的模型时不再把无效档位写进运行态。 | 新增 `CodingAgentThinkingLevels` 共享 helper，统一 `/thinking`、`/thinking select`、settings selector、启动恢复、显式 `/model` / `/provider` / Ctrl+L、Ctrl+P/Ctrl+Shift+P、RPC `set_model` / `cycle_model` / `set_thinking_level` / `cycle_thinking_level` / `update_settings` 的 thinking clamp；非 reasoning 模型强制 off，不支持 xhigh 的 reasoning 模型把 xhigh clamp 到 high，支持 xhigh 的模型保留 xhigh。 |
| 2026-05-23 | CodingAgent | scoped model 列表可以为单个模型指定 reasoning/thinking level，快捷切换和 RPC 切换会自动带上该模型偏好的思考档位。 | `enabledModels` 字符串条目现在支持 `provider/model:off|minimal|low|medium|high|xhigh`；`/scoped-models set/add/remove/current` 会解析、展示并持久化 per-entry thinking suffix，Ctrl+P/Ctrl+Shift+P 和 RPC `cycle_model` 切到带 suffix 的 scoped model 时会更新 runner thinking level 并同步默认 thinking setting；交互式 scoped-models selector 暂只保留既有 suffix，不提供逐项编辑，模型能力 clamp 已在后续同日切片补齐。 |
| 2026-05-23 | CodingAgent | 模型选择器可以直接输入搜索词并看到明确的搜索框状态，不再只依赖 `/model select [search]` 的初始过滤参数。 | `CodingAgentModelSelectorComponent` 新增 `Model Selector` / `Search:` 轻量 chrome；普通字符输入会更新模型过滤，Backspace 会回退过滤，搜索结果仍保留 scoped/all 切换、selected `Model Name: ...` 和 `Only showing models with configured auth` footer。当前仍不是完整上游 theme/dynamic-border/terminal-host parity；scoped model thinking-level per-entry baseline 已在后续同日切片补齐。 |
| 2026-05-23 | CodingAgent | 模型选择器可以在当前 scoped 候选和全部已配置凭证模型之间切换，并显示当前选中模型名称，减少“为什么看不到其他模型”和“当前行到底是哪一个模型”的判断成本。 | 新增 `CodingAgentModelSelectorComponent`，在 `/model select`、交互式裸 `/model` 和 Ctrl+L 的 model selector 中支持 Tab 在 `scoped` / `all` scope 之间切换；有 scoped 候选时默认进入 `scoped`，切到 `all` 后仍优先定位当前模型，并在列表下方显示 `Model Name: ...`。search/chrome 与 scoped model thinking-level per-entry baseline 已在后续同日切片补齐；当前仍不是完整上游 theme/dynamic-border/terminal-host parity。 |
| 2026-05-23 | CodingAgent | 模型选择器会明确提示当前只显示已配置凭证的模型，用户不会误以为模型列表缺项或随机过滤。 | `TuiSelectList` 新增通用 footer hint 行，`CodingAgentModelSelector` 在 `/model select`、交互式裸 `/model` 和 Ctrl+L 的单选列表底部显示 `Only showing models with configured auth`；该 footer 在普通列表、滚动提示之后和 no-match 状态都会稳定渲染。scope toggle、selected model detail、search/chrome、scoped model thinking-level per-entry baseline 和模型能力 clamp 已在同日补齐；完整上游 theme/dynamic-border/terminal-host parity 与 per-entry thinking UI editor 仍后续补齐。 |
| 2026-05-23 | CodingAgent | 模型选择、快捷切换和 RPC 切换现在不会把用户带到未配置凭证的 provider，减少运行时才失败的切换路径。 | 新增 `CodingAgentModelAvailability` 共享 helper，并把 `/model select`、交互式裸 `/model`、Ctrl+L、Ctrl+P / Ctrl+Shift+P model cycle、显式 `/model`、`/provider`、RPC `get_available_models` / `set_model` / `cycle_model` / settings model update 收口到 auth-configured provider/model。`/scoped-models` 仍维护全部注册模型 scope，未配置 auth 的模型不会在实际 selector/cycle/switch 中出现。footer、scope/detail、search/chrome、scoped model thinking-level per-entry baseline 和模型能力 clamp 已在同日补齐；完整 theme/dynamic-border/terminal-host parity、per-entry thinking UI editor 和 OAuth login session parity 后续补齐。 |
| 2026-05-22 | CodingAgent | 用户可以用 `/model select`、交互式裸 `/model` 或 Ctrl+L 打开模型选择器，不必记住完整 provider/model 名称。 | 新增 `CodingAgentModelSelector`，复用 `TuiSelectList` / `TuiSelectorSession`，按当前 scoped model list 或全部可用模型选择，支持 `/model select [search]` 初始过滤，选择后保存 default provider/model；Ctrl+L 保留当前输入 draft。本切片当时不含 provider auth filtering；auth filtering、footer hint、scope toggle、selected model detail、search/chrome、scoped model thinking-level per-entry baseline 和模型能力 clamp 已在 2026-05-23 补齐，完整上游 theme/dynamic-border/terminal-host parity 与 per-entry thinking UI editor 仍后续补齐。 |
| 2026-05-22 | CodingAgent | 用户可以在交互式输入中用 Ctrl+P / Ctrl+Shift+P 按当前模型 scope 快速切换模型，不必输入 `/model` 或手改 settings。 | 新增 `CycleModelForward` / `CycleModelBackward` editor action、默认 keybinding、host/router model cycle baseline；空闲输入 prompt 会复用 settings `enabledModels` 或全部可用模型切到下一个/上一个模型，保存默认 provider/model，并保留当前输入 draft。auth filtering、model selector footer hint、scope toggle、selected model detail、search/chrome、scoped model thinking-level per-entry baseline 和模型能力 clamp 已在 2026-05-23 补齐，完整 theme/dynamic-border/terminal-host parity 和 per-entry thinking UI editor 后续补齐。 |
| 2026-05-22 | CodingAgent | 用户可以在交互式会话里用 selector 选择 thinking/reasoning level，不必反复执行 `/thinking cycle` 或手写 settings。 | 新增 `CodingAgentThinkingSelector` 和 `/thinking [current\|select\|cycle\|off\|minimal\|low\|medium\|high\|xhigh]` baseline；`/thinking select` 在真实交互式 editor 中复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示 `off/minimal/low/medium/high/xhigh`，选择后更新 runner `ThinkingLevel` 并写回 settings `defaultThinkingLevel`；裸 `/thinking` 继续只显示当前状态。 |
| 2026-05-22 | CodingAgent | 用户在交互式会话里执行裸 `/logout` 时可以先选择当前已 OAuth 登录的 provider，不必手动输入 provider id。 | `/logout [select\|provider]` 现在复用 `CodingAgentAuthSelector` 的 provider list seam；真实交互式 editor 会话中裸 `/logout` 或 `/logout select` 会列出当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后删除对应 `auth.json` entry；无 selector 的裸 `/logout` 仍沿用当前 provider，显式 `/logout <provider>` 保持兼容，环境变量和 `models.json` credential 不会被修改。 |
| 2026-05-22 | CodingAgent | 用户在交互式会话里执行裸 `/login` 时可以先选择 OAuth provider，不必手动输入 provider id。 | `/login [select\|provider]` 现在复用 `CodingAgentAuthSelector` 的 provider list seam；真实交互式 editor 会话中裸 `/login` 或 `/login select` 会列出当前注册且有 OAuth provider 的 provider，选择后调用现有 OAuth login flow 并保存到 `auth.json`；无 selector 的裸 `/login` 仍沿用当前 provider，显式 `/login <provider>` 保持兼容。 |
| 2026-05-22 | CodingAgent | 用户可以在交互式会话里用键盘切换查看各 provider 的 auth 状态，不必逐个输入 provider 名。 | 新增 `CodingAgentAuthSelector` 和 `/auth [current\|select\|provider]` baseline，`/auth select` 在真实交互式 editor 中复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示 configured/missing、source、OAuth/login capability；选择后只输出所选 provider status，不写凭证、不启动 OAuth login。 |
| 2026-05-22 | CodingAgent | 用户可以在交互式会话里用多选 UI 管理模型切换 scope，不必手写 `enabledModels` 数组。 | 新增 `TuiMultiSelectList` / `TuiMultiSelectSession` 与 `CodingAgentScopedModelsSelector`，裸 `/scoped-models` 或 `/scoped-models select` 在交互式 editor 中打开 selector，支持过滤、toggle、provider toggle、enable all、clear、reorder、保存和取消；非交互会话继续保留摘要/命令式 set/add/remove/clear/all。 |
| 2026-05-22 | CodingAgent | 用户在真实交互式会话里只靠键盘就能把 settings 里最常改的几项直接切掉，不必手写 JSON。 | 新增 `/settings [current|path|select]` baseline，裸 `/settings` 在交互式 editor 会话中打开 TUI selector，可切换 auto-compaction、steering/follow-up mode、tree filter、thinking level、scoped models 和 theme；`current/path` 继续保留可脚本化摘要和路径查询，非交互会话继续输出摘要。 |
| 2026-05-22 | CodingAgent | `/theme` 现在有真实 TUI selector 路径，用户不必记主题名或手写 settings。 | 新增 `CodingAgentThemeSelector` 和 `/theme select`，在交互式会话中复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 选择主题；取消不修改 settings，非交互会话返回 selector unavailable。 |
| 2026-05-22 | Tui | TUI diff 计划现在可以落到真实 ANSI 输出，不再只停留在测试里的内存 sink。 | 新增 `TuiAnsiRenderSurface`，把 full redraw 渲染为 synchronized output + clear/home + 全量行，把 line diff 渲染为 cursor-position + clear-line + replacement text；当前仍未实现 viewport/scrollback、overlay compositing 或硬件 cursor。 |
| 2026-05-22 | Tui | Selector foundation 现在可以在确定性的输入/渲染 loop 里运行，后续接 OAuth/resource selector 时不需要重新发明按键循环。 | 新增 `ITuiRenderSurface`、`TuiOverlayHost` 和 `TuiSelectorSession`，覆盖初始渲染、按键读取、diff apply、Enter 选择和 Esc/Ctrl-C 取消；当前已被 `/theme select`、交互式 `/settings`、`/scoped-models`、`/auth select`、`/login` 和 `/logout` selector 复用，仍未实现完整 ANSI terminal host、overlay compositing 或 message/status area。 |
| 2026-05-22 | Tui | 后续 selector 和消息/状态区可以复用同一套组件、选择列表和 diff 合同，而不是继续写一次性 console 输出。 | 新增 `ITuiComponent` / `ITuiInputComponent`、`TuiContainer` / `TuiBox` / `TuiTextBlock`、`TuiText`、`TuiSelectList` 和 `TuiDiffRenderer` foundation；当前已接入 `/theme select`、交互式 `/settings`、`/scoped-models`、`/auth select`、`/login` OAuth provider selector 和 `/logout` OAuth provider selector baseline，仍未接入完整 terminal host、overlay compositing、message/status area 或 full OAuth login-session / resource selector UI。 |
| 2026-05-22 | CodingAgent | 用户可以在 CLI 内查看、列出、选择或清除当前主题，不必手改 settings JSON。 | 新增 `/theme [current\|list\|set\|clear] [name]` baseline，复用 theme loader/status 发现 built-in、用户、项目、显式和 extension-contributed themes，`set` 会校验主题存在后写入 settings `theme`，`clear` 回到默认 `dark`。 |
| 2026-05-21 | CodingAgent | CLI 启动时会自动带入项目级 `AGENTS.md` / `CLAUDE.md` 指令，不需要用户手动复制仓库协作规则。 | 新增 context files baseline：默认发现 `~/.tau/AGENTS.md` 或 `CLAUDE.md`，再按父目录到当前目录加载项目链；同目录优先 `AGENTS.md`；生成 system prompt 会注入 `# Project Context`；`--no-context-files` / `-nc` 可禁用，`/reload` 会刷新当前 context file 列表。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 新建会话时可以保留父会话来源，后续 `/session`、`/tree` 和 HTML export 能追溯分叉来源。 | RPC `new_session` 现在读取可选 `parentSession`，写入 JSONL session header 的 `parentSession` metadata，并继续追加 `session_info action=new`；CLI `/new` 行为保持不变。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以读取并批量更新 Tau 当前支持的 settings，不必逐个调用零散控制命令。 | 新增 RPC `get_settings` / `update_settings` baseline，覆盖默认模型、tree filter、retry、default thinking、enabledModels、steering/follow-up mode 和 auto-compaction enabled；更新后会同步当前 runner 状态，active prompt 期间拒绝修改。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以直接执行一次 shell 命令，并能取消运行中的 shell 命令。 | 新增 RPC `bash` / `abort_bash` baseline，`bash` 后台执行并返回 `output`、`exitCode`、`cancelled`、`truncated` 和可选 `fullOutputPath`，`abort_bash` 只取消当前 shell 命令；并发 `bash` 会被拒绝。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以切换 steering/follow-up 队列模式，并持久化 auto-compaction 开关。 | 新增 RPC `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` baseline；`get_state` 现在返回 runner/settings backed `steeringMode`、`followUpMode` 和 `autoCompactionEnabled`，settings 支持 `steeringMode`、`followUpMode`、`autoCompactionEnabled` 并读取旧 `queueMode`。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以切换 JSONL session，并展示可 fork 的用户消息列表。 | 新增 RPC `switch_session` / `get_fork_messages` baseline，复用 JSONL tree session restore 与 user message extraction；active prompt 期间拒绝切换，切换后同步 flat snapshot。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以开关自动重试，并取消等待中的 retry delay。 | 新增 RPC `set_auto_retry` / `abort_retry` baseline，复用 settings retry policy、rollback 和 JSONL retry audit；取消等待时写 `Retry cancelled`，失败输入不落盘。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以按 scope 切换模型，不需要交互式 CLI。 | 新增 RPC `cycle_model` baseline，复用 settings `enabledModels` 或全部可用模型，保存默认模型，候选不足时返回显式 `data:null`，active prompt 期间拒绝切换。 |
| 2026-05-21 | CodingAgent | Headless / RPC client 可以调整 thinking level，不需要交互式 CLI。 | 新增 RPC `set_thinking_level` / `cycle_thinking_level` baseline，持久化 settings `defaultThinkingLevel`，active prompt 期间拒绝变更。 |
| 2026-05-21 | CodingAgent | 用户可以在 CLI 内查看当前 settings 路径和有效配置摘要，不必打开 JSON 文件核对。 | 新增 `/settings [current\|path]` read-only baseline，展示当前 model/thinking、默认 model、tree filter、retry、default thinking 和 enabledModels scope。 |
| 2026-05-21 | CodingAgent | 用户可以在 CLI 内管理模型切换 scope，不必手改 settings 文件。 | 新增 `/scoped-models [set\|add\|remove\|clear\|all]` baseline，写入 settings `enabledModels`，`clear/all` 回到全模型。 |
| 2026-05-21 | CodingAgent | 用户可以在 CLI 内查看 Tau 最近的本地发布记录，不需要离开当前会话去翻文档。 | 新增 `/changelog [count\|all]` baseline，默认读取 `docs/releases/feature-release-notes.md`，并支持 `TAU_CODING_AGENT_CHANGELOG_FILE` 覆盖来源。 |
