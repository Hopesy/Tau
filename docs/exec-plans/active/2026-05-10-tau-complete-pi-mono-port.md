# Tau 完整 pi-mono 移植计划

## 目标

把 Tau 从“按模块补关键切片”升级为“以 `pi-mono-main` 为参考源，逐模块追到可评审的功能等价层”。这份 plan 是完整移植的总路线图，现有 `2026-04-23-tau-port-baseline.md` 继续作为已完成基线和决策历史，本文件负责后续 parity closure。

“完整”在这里不是一次性把所有代码机械翻译到 C#，而是每个上游能力都要在 Tau 中落成下列三件事：

- 有明确的 Tau-native 边界和数据模型。
- 有本地可验证的 test 或 smoke。
- 文档、history、quality、next 同步说明已完成/未完成，不把 seam 伪装成完整功能。

## 当前事实

- `Tau.Ai` 已经有多 provider 专用路径、generated model catalog、models.json custom config、provider auth resolver、OAuth login/refresh、auth.json schema、auth.json secret persistence baseline、auth.json provider credential removal baseline、models.json secret status boundary baseline、`JsonlTauLogSink` runtime event log 默认脱敏 baseline、`JsonlSecretRedactor` 通用 JSONL string value redaction foundation、Bedrock credential chain baseline 和 models.json 显式 OpenAI-compatible dynamic provider registration；当前 `JsonlTauLogSink` 已改为完整 JSONL 行写出后复用 `JsonlSecretRedactor` 递归处理 string value，保留 field key / number / bool / null；WebUi-local JSONL export/import 与 CodingAgent JSONL preview/import 也已接入同一 helper；但 SSO token 自动刷新、更广义 dynamic provider API / runtime config UX、非标准 secret pattern 和真实 e2e 仍未闭合。
- `Tau.CodingAgent` 已有平面 session store、JSONL session tree 基线、settings、slash command router、基础命令、手动 compaction 和 opt-in auto-compaction threshold；当前已支持 append-only JSONL entries、labels、label timestamp、header `cwd/parentSession` summary、JSONL tree session 写出按 `TAU_CODING_AGENT_REDACT_SECRETS` 默认脱敏 string value、`branch_summary` entry baseline、`/fork --summarize [instructions]` branch summary、branch summary restore context、compaction entry metadata baseline、token-budget recent-message retention baseline、split-turn prefix summary metadata/text baseline、失败普通回合 rollback baseline、host-level retryable error auto-retry baseline、Tau-native `auto_retry_start` / `auto_retry_end` audit entries、Tau-native `/retry` settings command baseline、RPC `set_auto_retry` / `abort_retry` baseline、RPC `bash` / `abort_bash` baseline、RPC `set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` settings controls baseline、RPC `switch_session` / `get_fork_messages` session switch/fork selector baseline、Tau-native `/thinking [current|select|cycle|off|minimal|low|medium|high|xhigh]` settings/TUI selector baseline、thinking 模型能力 clamp baseline（非 reasoning 模型 off、不支持 xhigh 的 reasoning 模型 xhigh -> high）、Tau-native `/settings [current|path|select]` CLI/settings/TUI selector baseline、Tau-native `/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline、Tau-native `/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline、scoped model per-entry thinking-level baseline（`enabledModels` 支持 `provider/model:off|minimal|low|medium|high|xhigh`，Ctrl+P/Ctrl+Shift+P 与 RPC `cycle_model` 会应用 override 并按模型能力 clamp）、Tau-native `/model select [search]` / Ctrl+L model selector baseline、Ctrl+P/Ctrl+Shift+P model cycle baseline、model selector provider auth filtering/footer/scope/detail/search chrome baseline、运行中 steering/follow-up CLI baseline、`--mode rpc` LF JSONL headless baseline（含 `set_auto_retry` / `abort_retry` auto retry controls、`bash` / `abort_bash` shell controls、`set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` queue/auto-compaction controls、`switch_session` / `get_fork_messages` session utilities、`export_html` / `get_last_assistant_text` / `set_session_name` session utilities）、retry delay cancellation visibility baseline、context overflow compact-and-retry baseline、resume/fork/clone/tree、`/tree` filter/search modes、settings `treeFilterMode` 默认过滤、`/tree --interactive` navigator（移动、选择、overlay search、filter cycle、next/prev match、Left/Right page、Ctrl/Alt branch navigation、Space fold、selected metadata）、settings retry fields/default thinking level/steeringMode/followUpMode/autoCompactionEnabled/theme/enabledModels scoped model list、theme loader/status baseline（built-in、用户、项目、env/CLI 和 extension-contributed theme paths + diagnostics）、`.jsonl` branch export/import、`.html/.htm` standalone transcript export、HTML branch outline、HTML branch filter/search、HTML cwd/parent metadata、HTML current-branch JSONL timeline、label/model/compaction/branch-summary/retry event rendering、HTML split-turn prefix display/search、HTML branch summary read/modified file rendering、HTML 内嵌 JSONL 下载、message deep-link/copy-link baseline、HTML text code fence rendering baseline、HTML inline code span rendering baseline、HTML plaintext link rendering baseline、HTML Markdown heading/list/blockquote block rendering baseline、HTML Markdown strong/emphasis span rendering baseline、HTML Markdown pipe table rendering baseline、HTML Markdown task list rendering baseline、HTML image metadata caption baseline、HTML long tool result folding baseline、HTML tool call JSON argument rendering baseline、HTML long tool call arguments folding baseline、`/share` secret Gist baseline、基础 tree stats、`/session` 估算 token/context usage、auto threshold budget 与 retry policy、prompt template baseline、skill command baseline、context files baseline（`~/.tau/AGENTS.md|CLAUDE.md` + parent-to-cwd `AGENTS.md|CLAUDE.md` + `--no-context-files` / `-nc` + `/reload` refresh）、JSON extension command/resource/load diagnostics baseline、`/hotkeys` 当前 editor keybinding listing baseline、`/reload` settings/resources/prompts/skills/context-files/theme-status/keybindings baseline、`/auth [current|select|provider]` provider status selector baseline、`/login [select|provider]` OAuth provider selector baseline、`/logout [select|provider]` OAuth provider selector/auth.json credential removal baseline、`/changelog [count|all]` release notes baseline、`TAU_CODING_AGENT_AUTO_COMPACT_TOKENS` / `TAU_CODING_AGENT_AUTO_COMPACT_INSTRUCTIONS` / `TAU_CODING_AGENT_COMPACT_KEEP_RECENT_TOKENS` / `TAU_CODING_AGENT_COMPACT_KEEP_RECENT_MESSAGES` / `TAU_CODING_AGENT_AUTO_RETRY_ATTEMPTS` / `TAU_CODING_AGENT_AUTO_RETRY_BASE_DELAY_MS`、settings `defaultThinkingLevel` / `steeringMode` / `followUpMode` / `autoCompactionEnabled` / `theme` / `enabledModels` 和 flat JSON 兼容，但仍缺完整上游 TreeSelector、自动 branch switching summarization hooks、LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI、完整 compaction event/cancellation parity、完整 settings selector UI、完整 model selector theme/dynamic-border/terminal-host parity/per-entry thinking UI editor、完整 OAuth login/session parity、credential refresh UX、完整 TUI 运行中输入 overlay、完整 RPC extension UI / streamed bash output / full settings parity、完整 TypeScript extension runtime/custom tools/events、完整上游 theme selector parity、TUI theme rendering、theme file watcher、full resource selector、完整启动 changelog / collapse / telemetry parity、完整 Markdown/highlight renderer、custom tool renderer、Tau 专属 share viewer 和 richer HTML template。
- `Tau.CodingAgent` auth/login/logout 面已新增 `/auth [current|select|provider]` provider status selector baseline、`/login [select|provider]` OAuth provider selector baseline 和 `/logout [select|provider]` OAuth provider selector baseline：交互式 `/auth select` 会复用 TUI 单选 selector 展示各 provider configured/missing、source、OAuth/login capability；交互式裸 `/login` 或 `/login select` 会列出当前注册且有 OAuth provider 的 provider，选择后调用现有 OAuth login flow 并保存到 `auth.json`；交互式裸 `/logout` 或 `/logout select` 会列出当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后删除对应 `auth.json` entry；完整 OAuth login dialog/session parity、credential refresh UX 和真实 OAuth e2e 继续单独跟踪。
- `Tau.WebUi` 已有可持久化 session、provider/model 选择、NDJSON streaming endpoint、前端增量渲染、file attachments（upload/preview/send）、tool call timeline cards、session delete/export/import、title rename、last-opened session restore baseline、auth status 查询、client-side Markdown rendering（code fences、inline code、links、headings/lists/blockquotes、strong/emphasis、tables、task lists）、WebUi-local 线性 JSONL transcript export/import roundtrip、WebUi-local JSONL export/import 默认 string-value secret redaction、CodingAgent JSONL session 只读 preview endpoint、preview-derived conservative import endpoint、CodingAgent JSONL preview/import 默认 string-value secret redaction、fake-runner service-level streaming/attachment persistence 行为测试和 Minimal API endpoint NDJSON/session/export-import-delete/error/rename-restore/JSONL export/import/preview/import contract 回归；仍缺完整 WebUi parity（与 CodingAgent JSONL branch/session-tree 导入和持久化语义对齐）。
- `Tau.Mom` 已有本地 inbox/outbox/events、runtime context、attachment staging、workspace layout、channel log/status/context/last_prompt seams，并已补 `log.jsonl` / `last_prompt.jsonl` 默认 JSON string value secret redaction、Slack Socket Mode/Web API seam、startup backfill seam、Slack private file download seam、per-channel queue seam、true cancellable stop seam、Agent Skills prompt inventory、Mom host sandbox/tool delegation seam、Docker sandbox validate/exec 可注入 process runner seam 和 `--validate-sandbox` 显式 validation 入口；仍缺真实 Docker sandbox smoke 和端到端 Slack flow。
- `Tau.Pods` 已有 config/probe/exec/health/deploy/stop/restart、model list/pull/remove/status、`vllm plan` plan-only CLI 和 `vllm plan --json` 机器可读 plan 输出，target-command 支持默认 `tau.pods.json` 或显式 config path，SSH lifecycle metadata 命令会规范化 deployment name 并做 shell quoting，`PodExecService` 已用 `ProcessStartInfo.ArgumentList` 构造系统 `ssh` argv，并把本地 ssh 启动失败、runner 异常和 cancellation 固定为结构化 failure；仍缺真实 SSH/HF download/vLLM orchestration、更完整远端 transport hardening 和真实运维 smoke。
- 2026-05-23 的后续 6-agent 并行收口又补齐六个 baseline：`Tau.Ai` `JsonlSecretRedactor` foundation、`Tau.CodingAgent` append-only `tree_state` fold metadata、`Tau.Tui` `TuiTranscriptViewport`、`Tau.WebUi` CodingAgent JSONL conservative import、`Tau.Mom` channel log/prompt debug 默认脱敏和 `Tau.Pods` `vllm plan` CLI。
- 2026-05-23 的 6-agent 并行切片又补齐了六个 baseline：`Tau.CodingAgent` `/metadata [entry-id]` JSONL session/entry inspector、`Tau.Tui` `TuiMessageArea` / `TuiStatusBar` message/status foundation、`Tau.WebUi` JSONL import `application/problem+json` content-type/error 合同与前端 `.jsonl` 分流、`Tau.Mom` `MomLocalDelegationFlow` 本地委派 flow、`Tau.Pods` `model list/pull/remove/status` model lifecycle baseline，以及 `Tau.Ai` models.json OpenAI-compatible runtime provider registration。
- 2026-05-23 在 `c77e5ad` 之后继续尝试 6-agent 并行，但新开 worker 多数被网关 `403 Forbidden` 阻断；本轮只收口已实际落地并验证的三条切片：`Tau.Ai` `JsonlTauLogSink` 统一复用 `JsonlSecretRedactor`、`Tau.CodingAgent` JSONL tree session 写出默认脱敏、`Tau.Pods` `vllm plan --json`。定向测试串行通过 `Tau.Ai.Tests` 205、`Tau.CodingAgent.Tests` 346、`Tau.Pods.Tests` 66；Tui/WebUi/Mom 没有新增完成项。
- Windows 本机验证入口以 `powershell -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke` 为准；不要把本机 bash/WSL 缺失误判为 Tau 失败。

## 总体顺序

完整移植按“可运行 seam -> adapter/transport -> e2e parity”的顺序推进，不再只按文件数量补代码：

1. **P0：收拢当前 dirty chain**
   - 保持当前 Tau.Mom 迁移链可 build/test/smoke。
   - 所有新 seam 都先走 fake/local transport 测试，再接真实外部 SDK。
   - 本阶段退出标准：`Tau.Mom` 独立 build、`Tau.Agent.Tests`、`verify-dotnet.ps1 -SkipRestore -RunSmoke` 全绿。

2. **P1：Tau.Mom full parity**
   - 建立 Slack-compatible channel message envelope，让 file/events/Slack/backfill 共享同一消息事实。
   - 接 Slack Socket Mode / Web API adapter：mention、DM、message logging、bot message logging、thread response、typing/update/delete/upload。
   - 已接 channel queue、busy-state、true cancellable stop、queue limit、startup backfill 和 old-message logging。
   - 已接 Slack file download 到 `ChannelAttachmentStore`，保留 `original/local/url` 语义。
   - 接 workspace/sandbox/tool delegation：`bash/read/write/edit/attach`，并保证 `scratch/`、`attachments/`、`SYSTEM.md`、`skills/` 的路径 authority 不分叉。
   - skill docs 对齐上游 Agent Skills prompt inventory；Mom skills 通过 `read/bash` 使用脚本，不额外注册为直接 tool 名。
   - 端到端验证先用 fake Slack transport，再用可配置真实 Slack smoke。

3. **P2：Tau.CodingAgent session / command / extension parity**
   - 已移植 JSONL SessionManager tree 的最小可用基线：session header、message/model/session-info/label/branch_summary entries、branch/fork/clone/resume、tree output、基础 tree stats、`.jsonl` branch export/import。
   - 已补命令行 `/tree` filter/search modes、settings `treeFilterMode`、label timestamp、`/tree --interactive` navigator、overlay search、filter cycle、next/prev match、Left/Right page、Ctrl/Alt branch navigation、Space fold、selected metadata、settings-backed `treeCollapsedEntryIds` fold persistence 和 `/metadata [entry-id]` CLI inspector；继续补多选、session-scoped richer fold state、完整 TUI metadata inspector 和上游 TreeSelector parity。
   - 已补手动 compaction 的 JSONL entry metadata baseline、token-budget recent-message retention baseline、split-turn prefix summary metadata/text baseline、opt-in token-threshold auto-compaction、settings/RPC auto-compaction boolean control、context overflow compact-and-retry、失败普通回合 rollback、host-level retryable error auto-retry、Tau-native retry audit event baseline、`/retry` settings command baseline、RPC auto retry baseline、retry delay cancellation visibility baseline 和 `/session` token budget/context/retry usage；继续补上游 LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI 和完整 compaction event/cancellation parity。
   - 已补 prompt template discovery/expansion baseline、skill command discovery/expansion baseline、context files baseline、theme loader/status baseline 与 Tau-native JSON extension command/resource/load diagnostics baseline；已补 `/reload` 对 settings、extension resources、prompts、skills、context files、theme status 和 keybindings 的当前进程重载 baseline；继续补完整 TypeScript extension runtime、custom tools/events、theme selector、TUI theme rendering、theme file watcher、interactive/full resource selector 和 richer runtime diagnostics。
   - 已补运行中 steering/follow-up CLI baseline：runner 暴露 `Steer` / `FollowUp`，host 在 active turn 期间消费可注入 input source；已补 `/hotkeys` 当前 editor keybinding listing baseline；继续补完整 TUI overlay、keybinding hints 和上游完整 shortcut registry。
   - 已补 `--mode rpc` LF JSONL headless baseline：覆盖 `prompt`、`steer`、`follow_up`、`abort`、`new_session`（含可选 `parentSession` JSONL header metadata）、`get_state`、`get_settings`、`update_settings`、`set_model`、`cycle_model`、`get_available_models`、`set_thinking_level`、`cycle_thinking_level`、`set_auto_retry`、`abort_retry`、`bash`、`abort_bash`、`set_steering_mode`、`set_follow_up_mode`、`set_auto_compaction`、`switch_session`、`get_fork_messages`、`compact`、`fork`、`clone`、`get_session_stats`、`get_messages`、`get_commands`、`export_html`、`get_last_assistant_text` 和 `set_session_name`；继续补完整上游 extension UI sub-protocol、streamed bash output、settings selector / theme / terminal / packages 等全量配置面和 full command parity。
   - 已补 `/settings [current|path|select]` CLI/settings/TUI selector baseline：展示 settings 路径/摘要，或在交互式会话中打开 TUI selector 写回 auto-compaction、steering/follow-up mode、tree filter、thinking level、scoped models 和 theme；继续补完整上游 SettingsList/submenu、images/terminal/transport/packages 等全量配置面。
   - 已补 `/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline：复用 theme loader/status 列表和校验主题，`select` 通过 `TuiSelectorSession` 交互式选择并写 settings `theme`，`set` 写 settings `theme`，`clear` 回到默认 `dark`；继续补完整上游 theme selector parity、TUI theme rendering 和 theme file watcher。
   - 已补 `/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline：settings `enabledModels` 显式数组表示有序模型 scope，缺失/null 表示 all enabled / no filter；条目可写成 `provider/model:off|minimal|low|medium|high|xhigh` 为单个 scoped model 指定 thinking override；真实交互式 editor 会话中裸 `/scoped-models` 或 `/scoped-models select` 通过 `TuiMultiSelectList` / `TuiMultiSelectSession` 选择、过滤、按 provider 批量切换、重排并保存，保存时保留既有 per-entry thinking suffix；`/model select [search]`、交互式裸 `/model` 和 Ctrl+L 已通过 `CodingAgentModelSelectorComponent` 按同一 scope 或全部已配置凭证模型做单选模型切换，有 scoped 候选时默认 scoped，可用 Tab 切换 scoped/all，顶部显示 `Model Selector` / `Search:` 轻量 chrome，普通字符输入会更新过滤，Backspace 会回退过滤，列表下方显示 `Model Name: ...`，底部提示只显示已配置凭证模型；空闲输入 prompt 的 Ctrl+P / Ctrl+Shift+P 和 RPC `cycle_model` 已按同一 scope 或全部可用模型做循环切换，并在命中 per-entry thinking override 时同步 runner/default thinking 且按目标模型能力 clamp。完整上游 theme/dynamic-border/terminal-host parity 和 per-entry thinking UI editor 继续后续补齐。
   - 已补 `/auth [current|select|provider]` provider auth status selector baseline：真实交互式 editor 会话中 `/auth select` 通过 `TuiSelectList` / `TuiSelectorSession` 展示各 provider configured/missing、credential source、OAuth/login capability；当前只输出状态，不写凭证、不执行 OAuth login。
   - 已补 `/login [select|provider]` OAuth provider selector baseline：真实交互式 editor 会话中裸 `/login` 或 `/login select` 会筛选当前注册且有 OAuth provider 的 provider，选择后调用现有 OAuth login flow 并保存到 `auth.json`；无 selector 的裸 `/login` 继续沿用当前 provider，显式 `/login <provider>` 保持兼容；继续补完整上游 OAuth login dialog/session parity。
   - 已补 `/logout [select|provider]` OAuth provider selector baseline：真实交互式 editor 会话中裸 `/logout` 或 `/logout select` 会筛选当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后只删除对应 `auth.json` entry；无 selector 的裸 `/logout` 继续沿用当前 provider，显式 `/logout <provider>` 保持兼容；环境变量和 `models.json` credential 配置不被修改。
   - 已补 standalone HTML transcript export、本地 JSONL download baseline、branch outline、branch filter/search、current-branch JSONL timeline、labels/tree metadata、branch_summary timeline/read files/modified files、message deep-link/copy-link、文本 code fence rendering baseline、inline code span rendering baseline、plaintext link rendering baseline、Markdown heading/list/blockquote block rendering baseline、Markdown strong/emphasis span rendering baseline、Markdown pipe table rendering baseline、Markdown task list rendering baseline、image metadata caption baseline、长 tool result folding baseline、tool call JSON argument rendering baseline、长 tool call arguments folding baseline 和 `/share` secret Gist baseline；继续补 export/import parity：clipboard/rich content、完整 Markdown/highlight renderer、custom tool renderer、richer HTML template、Tau 专属 share viewer 与真实 `gh` smoke。
   - 保持当前 Tau snapshot store 作为迁移/兼容入口，不直接破坏已有平面 session。

4. **P3：Tau.Ai provider/auth/model parity**
   - OAuth/device login/refresh：Anthropic、GitHub Copilot、OpenAI Codex、Gemini CLI/Antigravity。
   - AWS credential chain：SSO、AssumeRole、credential_process、IMDS、ECS、web identity。
   - dynamic provider API 注册：models.json 中未预注册 provider 的显式 OpenAI-compatible runtime registration 已完成；更广义 provider API 与 runtime config UX 继续后置。
   - generated model seed 持续同步到 Tau 已支持 API 家族；模型表不能领先于 provider 行为。
   - 建立 provider e2e matrix，把 stub tests 与真实云端 smoke 分层。

5. **P4：Tau.WebUi parity**
   - Streaming message UI，和 `AssistantMessageStream` / agent events 对齐。
   - Attachments upload/download、image/tool result rendering。
   - Thinking/tool timeline/rich markdown/code block rendering。
   - Auth/settings UX：provider status、login entry、models.json/settings 管理。
   - Web session restore、rename、delete、export/import 与 CodingAgent session 语义对齐。

6. **P5：Tau.Tui parity**
   - 真正输入编辑器、选择/历史/快捷键体系。
   - 组件树、selector foundation、selector session host、差分渲染、message area、status area、tool timeline。
   - 与 CodingAgent richer rendering 共享必要抽象，不把 UI 状态塞回 runtime。

7. **P6：Tau.Pods parity**
   - deploy/stop/restart/health lifecycle。
   - model lifecycle（`model list/pull/remove/status` baseline 已完成）、remote command output、failure classification。
   - SSH/HTTP transport hardening，配置校验与安全边界。

8. **P7：release / CI / docs parity**
   - release 产物改成真实 Tau executable/package。
   - CI 接入 PowerShell 或 bash 等价 smoke，覆盖 WebUi/Mom/Pods 高价值运行态。
   - 文档、references、security、supply-chain、release notes 以可审计方式同步。

## 当前执行切片

当前执行切片已推进到 `Tau.CodingAgent` 的 JSONL session tree、Branch Summarization baseline、interactive tree navigator fold/metadata baseline、HTML transcript export、HTML text code fence rendering、HTML inline code span rendering、HTML plaintext link rendering、HTML Markdown heading/list/blockquote block rendering、HTML Markdown strong/emphasis span rendering、HTML Markdown pipe table rendering、HTML Markdown table rendering、HTML Markdown task list rendering、HTML image metadata caption、HTML long tool result folding、HTML tool call JSON argument rendering、HTML long tool call arguments folding、prompt/skill、context files、theme loader/status、theme command baseline、JSON extension command/resource/diagnostics、失败普通回合 rollback、host-level retryable error auto-retry、retry audit event、Tau-native retry settings command、RPC auto retry baseline、RPC session switch / fork messages baseline、Thinking Level settings command、Thinking selector baseline、Settings selector baseline、Scoped Models TUI selector baseline、Ctrl+P/Ctrl+Shift+P model cycle interactive baseline、`/model select [search]` / Ctrl+L model selector baseline、运行中 steering/follow-up CLI baseline、`/hotkeys` 当前 editor keybinding listing baseline、`/reload` settings/resources/prompts/skills/context-files/theme-status/keybindings baseline、`/auth` status selector baseline、`/login [select|provider]` OAuth provider selector baseline、`/logout [select|provider]` OAuth provider selector/auth.json credential removal baseline、`/changelog [count|all]` release notes baseline、RPC mode baseline、RPC thinking controls baseline、RPC cycle model baseline、RPC session utility baseline 和 context overflow compact-and-retry baseline：

- 新增 `CodingAgentTreeSessionStore` / `CodingAgentTreeSessionController`，保留 flat JSON snapshot 兼容入口，同时默认维护 `.tau/coding-agent-session.jsonl`；`TAU_CODING_AGENT_TREE_SESSION_FILE` 或 `.jsonl` 形式的 `TAU_CODING_AGENT_SESSION_FILE` 可指定 tree session 文件。
- JSONL 第一行写 `type=session` header，后续 append-only 写 `message`、`model_change`、`session_info`、`label` entries，每个 entry 带 `id`、`parentId`、`timestamp`；host 通过 runner message count diff 追加新消息，避免重写旧 history。
- `/session` 同时报告 flat stats、估算 token/context usage、auto-compaction threshold budget、当前 retry policy 和 tree file/leaf/entry/message/branch/label/cwd/parent 信息；`/tree [max entries] [default|no-tools|user-only|labeled-only|all] [--label-time] [--search query]` 输出短 id、parent、entry 摘要、label、label timestamp、当前 branch 与 leaf 标记，并在 header 显示 cwd / parent metadata，按上游 tree selector 的核心过滤语义隐藏 bookkeeping / tool-only assistant / tool result；`--search` 会按空格分词匹配 entry text、label、branch summary 和基础 metadata；未显式传 filter 时读取 settings `treeFilterMode`，无效值回退 `default`；`/label <entry-id> [label | clear]` 追加 label change；`/fork <entry-id>` 从历史 entry 切出新 branch 并恢复当前 runner messages；`/fork <entry-id> --summarize [instructions]` 会先摘要被离开的 branch，追加 `branch_summary` entry，并在恢复目标 branch 时注入 summary context；`/clone` 把当前 active branch 导出成新的 JSONL session 并切换当前 runner/session controller；`/resume [latest | path.jsonl]` 恢复 JSONL session。
- `/export` 默认导出 standalone HTML transcript；`/export <path.html|path.htm>` 显式导出 HTML；HTML 提供 branch outline、`default/no-tools/user-only/labeled-only/all` filter/search、cwd/parent metadata、内嵌当前 branch JSONL 并提供 Download JSONL 按钮，按 current branch JSONL 渲染 message / session_info / model_change / label / compaction / branch_summary timeline，branch summary 会显示 summary、source entry、read files 和 modified files；消息文本里的 fenced code block 会拆成独立 `code-block` / `<code>` 区块，普通文本里的 backtick inline code span 会渲染为 `<code class="inline-code">`，普通文本段会把 Markdown-style `[label](http/https...)` 和裸 `http(s)` URL 渲染成外链，code fence 和 inline code 内不链接，非 http(s) scheme 保持文本安全输出；普通文本段检测到 heading/list/blockquote block marker 时会改用轻量 rich-text block rendering，渲染 `h1`-`h6`、`ul/ol/li` 和 `blockquote`，并继续复用 inline code/link 安全输出；普通文本里的 `**strong**` / `__strong__` 和 `*emphasis*` / `_emphasis_` 会分别渲染为 `<strong>` / `<em>`，并复用 inline code/link 安全输出；inline code、code fence 和单词内部下划线不会触发 emphasis；普通文本段中的 Markdown pipe table（header + separator + data rows）会渲染为可横向滚动的 `<table>`，单元格继续复用 inline code/link/strong/emphasis 安全输出，code fence 内表格文本保持代码块；普通文本列表项里的 `[ ]` / `[x]` task marker 会渲染为 disabled checkbox，任务文本继续复用 inline code/link/strong/emphasis 安全输出，code fence 内任务列表文本保持代码块；图片内容继续以内嵌 data URI 渲染，并在 caption 显示 mime type 和估算字节数；长 tool result 文本会默认折叠为 `<details class="tool-result-fold">` 并保留完整输出；tool call arguments 中可解析 JSON 会格式化为 `code-block` / `<code data-language="json">`，不可解析参数仍按原始 `<pre>` 安全转义，超长参数会默认折叠为 `<details class="tool-call-arguments-fold">` 并保留全文；消息节点绑定 JSONL entry id 和 label badge，支持 copy link 和 `targetId` deep link 自动定位；`/export <path.jsonl>` 导出当前 branch 为独立 JSONL session；`/share` 会复用同一 HTML transcript exporter，检查 GitHub CLI 登录状态并创建 secret gist，预览 URL 支持 `TAU_SHARE_VIEWER_URL` 覆盖；`/import <path.jsonl>` 等价于 resume；其他非 `.jsonl/.html/.htm` 的 `/export` / `/import` 继续走 Tau flat snapshot JSON，避免破坏旧 session。
- prompt template discovery/expansion baseline 已接入：用户目录 `~/.tau/prompts`、项目目录 `./.tau/prompts` 和 `TAU_CODING_AGENT_PROMPT_PATHS` 指定路径可提供 `.md` prompt template；`/prompts` 列表展示模板，非内置 slash 输入命中模板时会在 runner 调用前展开参数占位。
- skill command discovery/expansion baseline 已接入：用户目录 `~/.tau/skills`、项目目录 `./.tau/skills` 和 `TAU_CODING_AGENT_SKILL_PATHS` 指定路径可提供 `SKILL.md`；`/skills` 列表展示 `/skill:<name>` 命令，`/skill:<name> args` 会把 skill body 包装成上游风格 `<skill>` block 后发送给 runner；可见 skill 会进入默认 system prompt inventory，`disable-model-invocation: true` 只保留显式命令调用。
- context files baseline 已接入：启动时默认先加载用户目录 `~/.tau/AGENTS.md` 或 `~/.tau/CLAUDE.md`，再按 parent-to-cwd 顺序加载项目链上的 `AGENTS.md` 或 `CLAUDE.md`；同一目录优先 `AGENTS.md`，不可读文件跳过。生成 system prompt 会追加 `# Project Context`；`--no-context-files` / `-nc` 会禁用默认 context file 加载，`/reload` 会重读 context files 并在 runner 使用生成 system prompt 时刷新 context inventory。
- JSON extension command/resource/diagnostics baseline 已接入：用户目录 `~/.tau/extensions`、项目目录 `./.tau/extensions` 和 `TAU_CODING_AGENT_EXTENSION_PATHS` 指定路径可提供 `.json` command definitions；`/extensions` 列表展示命令、extension JSON 文件路径、每个文件的 command/prompt/skill/theme resource 计数、重复 command 的解析名、prompt/skill/theme resource paths，以及坏 JSON、不可读文件、缺失显式路径等 load diagnostics；非内置 slash 输入先查 extension command，再查 skill/prompt；status-only command 直接写 UI，`sendToRunner=true` command 会展开 `prompt/response` 后发送给 runner，重复 name 按上游规则解析成 `name:1`、`name:2`；同一 JSON 可通过 `promptPaths/skillPaths/themePaths` 或 `resources.promptPaths/resources.skillPaths/resources.themePaths` 贡献 prompt/skill/theme 发现路径，路径相对 extension JSON 所在目录解析。
- `/reload` settings/resources/prompts/skills/context-files/theme-status/keybindings baseline 已接入：命令会重读 settings 并立即同步 host retry policy、runner thinking level 与 queue mode，重读 JSON extension status/resources 后更新 extension-contributed prompt/skill/theme paths，再重载 prompt templates、skills、context files、theme status 和交互式 editor keybindings；runner 使用生成 system prompt 时会按最新 skills/context files 刷新 inventory。当前不把完整 TypeScript extension runtime lifecycle、完整上游 theme selector、TUI theme rendering、theme file watcher 或 full resource selector 写成已完成。
- `/logout [select|provider]` OAuth provider selector/auth.json credential removal baseline 已接入：命令复用 `/auth` 的 provider 解析语义；真实交互式 editor 会话中裸 `/logout` 或 `/logout select` 会打开同一 provider selector，只列出当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后删除对应 `auth.json` entry；无 selector 时未传 provider 仍使用当前 runner provider，显式 provider 不走 selector；删除范围限定为本地 `auth.json` 中匹配 provider 的 credential entry，环境变量和 `models.json` credential 配置保持不变；缺失 auth file、malformed auth file 或 provider 未命中时返回友好状态并不重写文件。
- `/changelog [count|all]` release notes baseline 已接入：命令读取 Tau 本地 release notes 表，默认从当前目录向上查找 `docs/releases/feature-release-notes.md`，可通过 `TAU_CODING_AGENT_CHANGELOG_FILE` 覆盖；输出日期、功能域、用户价值和变更摘要。当前不处理启动时 changelog 展示、`lastChangelogVersion`、`collapseChangelog` 或 install/update telemetry。
- `/settings [current|path|select]` CLI/settings/TUI selector baseline 已接入：命令读取当前 settings store 和 runner 状态；裸 `/settings` 在交互式会话中打开 `CodingAgentSettingsSelector`，可选择 auto-compaction、steering/follow-up mode、tree filter、thinking level、scoped models 或 theme 并写回 settings；`/settings current` 输出 settings 文件路径、当前 provider/model 与 thinking、默认 provider/model、tree filter、retry policy、default thinking、steering/follow-up mode、auto-compaction 设置、theme 和 `enabledModels` scope；`/settings path` 只输出 settings 文件路径。当前仍不实现完整上游 SettingsList/submenu、images/terminal/transport/packages 等全量配置面或完整 TUI edit parity。
- `/thinking [current|select|cycle|off|minimal|low|medium|high|xhigh]` CLI/settings/TUI selector baseline 已接入：裸 `/thinking` 和 `/thinking current` 保留状态查询语义，显式 level/off/cycle 保持现有 settings 持久化合同；`/thinking select` 在真实交互式 editor 会话中打开 `CodingAgentThinkingSelector`，复用 `TuiSelectList` / `TuiSelectorSession` / `TuiAnsiRenderSurface` 展示当前模型可用的 thinking levels 和说明文案，选择后立即更新 runner `ThinkingLevel` 并写回 settings `defaultThinkingLevel`；无 selector 或取消时不修改 settings。模型能力 clamp 已接入：非 reasoning 模型只允许 off，不支持 xhigh 的 reasoning 模型把 xhigh 降到 high。当前仍不实现 full settings UI parity。
- `/theme [current|list|select|set|clear] [name]` CLI/settings/TUI selector baseline 已接入：命令读取 settings `theme`，复用 `CodingAgentThemeStore.LoadStatus()` 列出 built-in、用户、项目、env/CLI 和 extension-contributed themes；`select` 会在交互式 editor 会话中通过 `CodingAgentThemeSelector` 打开 `TuiSelectList`，选择后写回 settings，取消时保持 settings 不变；`set` 会大小写不敏感校验主题存在后写回 settings，并保留 settings 其他字段；`clear` 清空 settings `theme` 并显示默认 `dark`。当前不实现完整上游 theme rendering 或 theme file watcher。
- `/scoped-models [current|select|set|add|remove|clear|all]` CLI/settings/TUI selector baseline 已接入：命令读取当前 runner 注册 provider/model 列表，支持 `provider/model`、唯一 model id，以及 `provider/model:off|minimal|low|medium|high|xhigh` per-entry thinking suffix 解析；显式 scope 写入 settings `enabledModels` 并保留顺序，`clear` / `all` 或等于全量且无 thinking override 时保存为 `null`，表示 all enabled / no filter；真实交互式 editor 会话中裸 `/scoped-models` 或 `/scoped-models select` 打开 `CodingAgentScopedModelsSelector`，复用 `TuiMultiSelectList` / `TuiMultiSelectSession` 支持过滤、模型 toggle、provider toggle、enable all、clear、Alt+Up/Down reorder、Ctrl+S save 和 Esc/Ctrl-C cancel，并在保存时保留旧 suffix metadata；`/model select [search]`、交互式裸 `/model` 和 Ctrl+L 打开 `CodingAgentModelSelectorComponent`，空闲输入 prompt 的 Ctrl+P / Ctrl+Shift+P 和 RPC `cycle_model` 也复用同一 scope，切到带 suffix 的 scoped model 时会应用 thinking override 并按目标模型能力 clamp；实际模型使用入口已经按 provider auth status 收口，只有 auth-configured provider/model 会进入 selector/cycle/switch/RPC availability；模型 selector 有 scoped 候选时默认 scoped，可用 Tab 切换 scoped/all，顶部显示 `Model Selector` / `Search:` 轻量 chrome，普通字符输入会更新过滤，Backspace 会回退过滤，列表下方显示 `Model Name: ...`，底部显示 `Only showing models with configured auth`。当前不实现完整上游 theme/dynamic-border/terminal-host parity 或 per-entry thinking UI editor。
- `/auth [current|select|provider]` provider auth status selector baseline 已接入：命令复用 runner/provider registry 和 `ProviderAuthResolver` status 事实源；裸 `/auth` / `/auth current` 输出当前 provider status，显式 `/auth <provider>` 输出指定 provider status，真实交互式 editor 会话中 `/auth select` 打开 `CodingAgentAuthSelector`，选择后输出所选 provider 的 configured/missing、source、OAuth/login capability 和状态消息。当前不写 `auth.json`，不执行 OAuth login，不回显 secret。
- `/login [select|provider]` OAuth provider selector baseline 已接入：真实交互式 editor 会话中裸 `/login` 或 `/login select` 会打开同一 provider selector，只列出当前注册且有 OAuth provider 的 provider；选择后调用现有 OAuth login flow 并通过 runner 保存 credentials 到 `auth.json`。没有 selector 的裸 `/login` 继续沿用当前 provider，显式 `/login <provider>` 不走 selector。当前不等于完整上游 OAuth login dialog/session parity。
- Compaction recent-message retention baseline 已接入：manual / auto compaction 写 `firstKeptEntryId` 时默认先按 `TAU_CODING_AGENT_COMPACT_KEEP_RECENT_TOKENS` 的 token budget 从尾部选择 cut-point，未命中时再保留当前 compaction boundary 后最近 4 条 message，可通过 `TAU_CODING_AGENT_COMPACT_KEEP_RECENT_MESSAGES` 调整 fallback；如果 cut point 落在一个 user turn 中间，Tau 会在 compaction entry 上写 `isSplitTurn=true` 和确定性 `turnPrefixSummary`，branch restore / resume / clone export 按 summary + split-turn prefix context + retained messages + post-compaction messages 重建 runtime，并对导出的 JSONL session remap `firstKeptEntryId`。
- Context overflow compact-and-retry baseline 已接入：host 遇到 context overflow 错误型 `AgentEndEvent` 或 runner exception 时，会恢复失败回合前 snapshot，调用 `CompactAsync()`，把结果记录为 `fromHook=true` compaction entry，更新回滚基线后重试同一输入；如果 compaction 失败，则回滚到原始 snapshot，不写失败输入。
- 失败普通回合 rollback baseline 已接入：host 在普通消息调用 runner 前记录回合前 snapshot，runner exception、取消或错误型 `AgentEndEvent` 后恢复 snapshot，flat JSON 和 JSONL tree 只持久化回滚后的 messages/provider/model/name。
- Host-level retryable error auto-retry baseline 已接入：生产入口优先从 settings retry fields 读取配置，未配置时回退 `TAU_CODING_AGENT_AUTO_RETRY_ATTEMPTS` 和 `TAU_CODING_AGENT_AUTO_RETRY_BASE_DELAY_MS`，retryable runner exception 或错误型 `AgentEndEvent` 会先恢复回合前 snapshot，再按 exponential delay 重试同一输入；成功后只持久化成功 attempt，非 retryable 错误或重试耗尽继续按 rollback baseline 收口。
- Tau-native retry audit event baseline 已接入：JSONL tree 会为普通 transient retry 写 `auto_retry_start` 和 `auto_retry_end` entries，记录 attempt/maxAttempts/delay/error/success/finalError；成功恢复时先同步成功 attempt 的 user/assistant messages，再追加 retry end；失败或耗尽时保留 retry audit，但不写失败输入或 partial assistant。`/tree` search 和 HTML transcript timeline/branch outline 已能显示/search retry entries。
- Tau-native retry settings command baseline 已接入：`/retry [current|default|off|<max attempts> [base delay ms]]` 可查看、关闭、恢复 env/default 或设置 attempts/base delay，写回现有 settings JSON，并通过 host callback 让同进程下一轮普通输入立即使用新策略；retry delay 被 cancellation token 中断时会显示明确 `auto-retry cancelled during delay` 状态，写 `auto_retry_end(success=false, finalError="Retry cancelled")`，并回滚失败输入；这仍不是完整上游 settings UI/cancellation parity。
- RPC auto retry baseline 已接入：`set_auto_retry` 复用 settings retry fields 和 Tau 默认 retry policy，开启时让 headless prompt 按同一 rollback/retry/audit 语义处理 retryable failure，关闭时写 settings retry `0/0`；`abort_retry` 只取消 pending retry delay，不等同于 `abort` 当前 agent turn，取消时写 `auto_retry_end(success=false, finalError="Retry cancelled")` 并保持失败输入不落盘。
- RPC queue / auto-compaction settings controls baseline 已接入：`set_steering_mode` / `set_follow_up_mode` 会更新 runner queue mode 并写入 settings；`set_auto_compaction` 写入 settings-backed boolean 状态；`get_state` 返回真实 `steeringMode`、`followUpMode` 和 `autoCompactionEnabled`。Tau 的实际自动 compaction 仍需要 `TAU_CODING_AGENT_AUTO_COMPACT_TOKENS` 提供 threshold budget。
- RPC session switch / fork messages baseline 已接入：`switch_session` 复用 JSONL tree session `Resume()` 恢复指定 session，并把恢复后的 branch 同步到 runner 和 flat session；`get_fork_messages` 先从 runner 同步 tree，再返回所有 user message 的 `{ entryId, text }` 列表供 headless fork selector 使用；active prompt 期间拒绝切换 session。
- 运行中 steering/follow-up CLI baseline 已接入：`ICodingAgentRunner` 暴露 `Steer(string)` / `FollowUp(string)`，`RuntimeCodingAgentRunner` 复用 `AgentRuntime` 现有 queue；`CodingAgentHost` 在每个 active runner turn 期间消费 `ICodingAgentTurnInputSource`，生产入口仅在交互式 editor 启用时注入非阻塞 console source，Enter 提交 steering，Alt+Enter 提交 follow-up；这仍不是完整上游 TUI overlay / keybinding hint / RPC mode parity。
- `/hotkeys` 当前 editor keybinding listing baseline 已接入：`CodingAgentCommandRouter` 只读取 host 注入的 `IKeyBindingMap`，通过 `CodingAgentHotkeysFormatter` 按 action 分组输出当前有效按键组合和说明；自定义 keybindings 与 `EditorAction.None` 禁用默认绑定会反映到输出；没有 editor 的 print/RPC/redirected 模式返回 unavailable；这仍不是完整上游 app/session/tree/extension shortcut registry。
- RPC mode baseline 已接入：`--mode rpc` 不创建交互式 editor，不写 welcome/status/TUI 文本到 stdout，`CodingAgentRpcHost` 直接复用 runner、flat session、JSONL tree、settings 和 compaction seam，stdin/stdout 采用 LF-delimited JSONL；当前覆盖 prompt acceptance + async agent events、`steer` / `follow_up` / `abort`、state/settings/model/cycle_model/thinking/auto-retry/bash/queue/auto-compaction/messages/commands 查询或控制、compact/fork/clone/session stats、`new_session`（可选 `parentSession` 会写入 JSONL header 并进入 `/session`、`/tree` 和 HTML metadata）、`get_available_models`、`get_settings` / `update_settings`、`set_thinking_level` / `cycle_thinking_level`、`set_auto_retry` / `abort_retry`、`bash` / `abort_bash`、`set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction`、`switch_session` / `get_fork_messages`、`export_html`、`get_last_assistant_text`、`set_session_name` 和 invalid JSON 错误响应；这仍不是完整上游 extension UI sub-protocol、streamed bash output、settings selector / theme / terminal / packages 等全量配置面或 full command parity。
- 当前仍不把完整上游 TreeSelector、自动 branch switching summarization hooks、Tau 专属 share viewer、完整 Markdown/highlight renderer、完整上游 richer HTML template/vendor renderer、上游 LLM-generated split-turn summarization、auto-retry settings UI parity、完整 retry cancellation UI、完整 compaction extension events/cancellation UI、完整 settings selector UI、完整 OAuth login/session parity、credential refresh UX、真实 OAuth e2e、完整 RPC parity、完整 TypeScript extension runtime、custom tools/events、完整上游 theme selector、TUI theme rendering、theme file watcher、interactive/full resource selector、richer runtime diagnostics 和完整 RPC settings/bash/extension UI 写成已完成。

## 风险与约束

- 不把“本地 seam 已完成”写成“真实 Slack/Sandbox 已完成”。
- 不为追求 1:1 文件数量而牺牲 Tau 当前 AOT/source-gen/零 provider SDK 的边界。
- 外部服务能力必须先有 fake transport/stub handler 测试，再接真实 e2e。
- Windows 本机验证串行执行；不要并行跑会写相同 `bin/obj/output` 的 build/test。

## 验证方式

- `dotnet build src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-restore --verbosity minimal`
- `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --no-restore --verbosity minimal`
- targeted tests 覆盖 `TAU_CODING_AGENT_AUTO_COMPACT_TOKENS` / `TAU_CODING_AGENT_AUTO_COMPACT_INSTRUCTIONS`、pending input token estimate、普通消息前自动 compaction、flat session 持久化和 JSONL `fromHook=true`
- targeted tests 覆盖 `/tree` filter modes、settings `treeFilterMode`、search、bookkeeping/tool-only assistant hiding、`no-tools`、`labeled-only` 和 `--label-time`
- targeted tests 覆盖 `/tree --interactive` 的 j/k/g/G/Home/End/Enter/q/Esc 移动/选择/取消、overlay search、filter cycle、next/prev match、Left/Right page、Ctrl/Alt+Left fold-or-up、Ctrl/Alt+Right unfold-or-down、Space fold/expand、selected metadata 展示和 no-match Enter 空选择保护
- targeted tests 覆盖 `/clone` 成功复制当前 branch 到新 JSONL session、空 branch 返回 `Nothing to clone yet`、参数错误走 command catalog usage
- targeted tests 覆盖 JSONL header `cwd` / `parentSession` 出现在 `/session` 和 `/tree` metadata 输出中
- targeted tests 覆盖 tree session HTML transcript export 显示 cwd / parent metadata、message entry id、label badge、model change、label change、compaction timeline entry 和 branch outline filter/search 控件
- targeted tests 覆盖 HTML transcript text fenced code block 会渲染为 `code-block` / `<code>`，并继续对代码内容做 HTML escape
- targeted tests 覆盖 HTML transcript 普通文本 backtick inline code span 会渲染为 `<code class="inline-code">`，并确认 inline code 内的 URL 不被链接化
- targeted tests 覆盖 HTML transcript plaintext link rendering 会把 Markdown-style `[label](http/https...)` 和裸 `http(s)` URL 安全渲染为外链，同时不链接 code fence 内 URL 或非 http(s) scheme
- targeted tests 覆盖 HTML transcript 普通文本 heading/list/blockquote block rendering 会输出轻量 rich-text block，并继续对 heading/list/quote 内容复用 inline code/link 安全渲染；code fence 内的同类 marker 不参与 block rendering
- targeted tests 覆盖 HTML transcript 普通文本 strong/emphasis span rendering 会输出 `<strong>` / `<em>`，并继续复用 inline code/link 安全渲染；inline code、code fence 和单词内部下划线不参与 emphasis rendering
- targeted tests 覆盖 HTML transcript 普通文本 Markdown pipe table rendering 会输出可横向滚动的 `<table>`，单元格继续复用 inline code/link/strong/emphasis 安全渲染；code fence 内的 pipe table 文本不参与 table rendering
- targeted tests 覆盖 HTML transcript 普通文本 Markdown task list rendering 会把列表项里的 `[ ]` / `[x]` 输出为 disabled checkbox，任务文本继续复用 inline code/link/strong/emphasis 安全渲染；code fence 内的 task list 文本不参与 task list rendering
- targeted tests 覆盖 HTML transcript image content 会继续渲染 data URI，并显示 mime type 与估算字节数 caption
- targeted tests 覆盖 HTML transcript 长 tool result 文本会默认折叠为 `<details class="tool-result-fold">`，summary 显示字符数，内容保留全文并继续安全转义和复用 code fence rendering；普通 assistant 长文本不折叠
- targeted tests 覆盖 HTML transcript 中可解析 tool call arguments JSON 会格式化为 `code-block` / `<code data-language="json">`，保留 `<tau>` 等字符的可读形式并在最终 HTML 层安全转义；不可解析 arguments 保持原始 `<pre>` fallback
- targeted tests 覆盖 HTML transcript 中超长 tool call arguments 会默认折叠为 `<details class="tool-call-arguments-fold">`，summary 显示原始字符数，折叠内继续保留格式化 JSON 或 raw `<pre>` fallback 全文
- targeted tests 覆盖 `/share` 复用临时 HTML transcript、调用可注入 gist client、空 session 不触发 share，并在完成后清理临时 HTML
- targeted tests 覆盖 compaction `firstKeptEntryId` recent-message retention、token-budget cut-point、branch restore 的 summary + retained + post-compaction message 顺序、导出 JSONL 时的 `firstKeptEntryId` remap，以及无效 `firstKeptEntryId` 不导致 session restore 崩溃
- targeted tests 覆盖 retained cut point 落在一个 user turn 中间时，JSONL 写 `isSplitTurn` / `turnPrefixSummary`、branch restore 把 split-turn prefix context 拼进 summary message、HTML transcript 展示该 prefix
- targeted tests 覆盖 `/extensions` 的 extension JSON 文件明细、resource path 明细、重复命令解析名、坏 JSON 诊断和缺失显式路径诊断
- targeted tests 覆盖 runner exception 和错误型 `AgentEndEvent` 后回滚到回合前 snapshot，并确认 flat JSON / JSONL tree 不持久化失败输入
- targeted tests 覆盖 retryable 错误型 `AgentEndEvent` 成功重试后只持久化成功 attempt、non-retryable error 不重试、retryable exception 耗尽重试后回滚到回合前 snapshot
- targeted tests 覆盖 JSONL `auto_retry_start` / `auto_retry_end` 字段、成功重试时 start -> 成功 user/assistant -> end 的审计顺序、重试耗尽时失败输入不落盘、`/tree --search` 能搜到 retry event，以及 HTML export 能渲染 retry timeline entries
- targeted tests 覆盖 `/retry` current/off/default/numeric 设置、settings retry fields 保存/清空、host 同进程 retry policy 热更新、`/session` retry policy 展示和 invalid argument usage
- targeted tests 覆盖 `/thinking` current/high/cycle/off/invalid 设置、`/thinking select` selected/off/cancel/unavailable/invalid、selector state/description、settings `DefaultThinkingLevel` 保存/清空、host selector 接线，以及 `/help` command catalog 同步
- targeted tests 覆盖 `/metadata [entry-id]`：session-level metadata summary、entry-level detail、无 tree session 时 unavailable、额外参数 usage 和 `/help` command catalog 同步
- targeted tests 覆盖 `/hotkeys` 使用当前 editor keybinding map 输出 bindings、自定义 binding 覆盖、`EditorAction.None` 禁用默认 binding、无 editor 返回 unavailable，以及附加参数返回 usage
- targeted tests 覆盖 `/logout [select|provider]` 默认 provider、显式 provider、selector selected/cancel/unavailable/no-OAuth、host selector 接线、missing auth.json entry、额外参数 usage、help/catalog 同步、`OAuthCredentialStore.Remove` 删除匹配 provider 并保留其他 entries、provider 缺失不改写文件，以及 `ProviderAuthResolver.Logout` 删除 auth file credential 且不回显 secret
- targeted tests 覆盖 `/changelog [count|all]` 从 release notes table 输出最近条目、`all` 输出全部条目、无效参数返回 usage、host 渲染状态并且不调用 runner，以及 `/help` command catalog 同步
- targeted tests 覆盖 `/settings [current|path|select]` 输出 settings path/摘要、selector 可用/不可用、selector action 写回 auto-compaction、steering/follow-up mode、tree filter、thinking level、nested theme selector、取消不改 settings、host 裸 `/settings` selector 接线，以及 `/help` command catalog 同步
- targeted tests 覆盖 `/theme` current/list/set/clear、settings `theme` 持久化、保留 settings 其他字段、无 settings/theme store、missing theme 和 usage 错误，以及 `/help` command catalog / host help 同步
- targeted tests 覆盖 `/scoped-models` 默认 all enabled 展示、`set/add/remove/clear` 更新 settings `enabledModels`、保留同一 settings 文件里的默认模型/tree filter/retry/thinking 字段、无效 action/空 action/多余参数/unknown model 的 usage 或错误输出，以及 `/help` command catalog 同步
- targeted tests 覆盖 active runner turn 期间 `ICodingAgentTurnInputSource` 的 `Steering` / `FollowUp` 输入会分别转发到 runner `Steer` / `FollowUp`
- targeted tests 覆盖 RPC mode prompt response + event stream、严格 LF JSONL framing、active prompt 下 `steer` / `follow_up` / `abort`、`get_state` / `get_settings` / `update_settings` / `set_model` / `get_messages` / `get_commands`、`bash` / `abort_bash` shell result / 并发拒绝 / 取消 / 参数错误、`set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` settings 状态控制、`compact` 持久化 flat/tree session、`export_html` HTML transcript 输出、`get_last_assistant_text` null/text 响应、`set_session_name` flat/tree 持久化，以及 invalid JSON 错误响应
- targeted tests 覆盖 RPC `set_auto_retry` / `abort_retry`：settings retry fields 持久化、`get_state.autoRetryEnabled`、retryable prompt rollback/retry audit、pending retry delay cancellation 和失败输入不落盘
- targeted tests 覆盖 AgentRuntime queue modes：steering/follow-up `all` 与 `one-at-a-time` drain 语义，确保 all 模式不会因为本轮 drain 过消息而触发空转 LLM turn
- targeted tests 覆盖 retry delay cancellation 输出明确状态、写 retry end audit、回滚失败输入且不污染 flat/tree session
- targeted tests 覆盖 context overflow 错误触发 compaction 后重试同一输入、成功时只持久化 compacted session + 成功 attempt，以及 compaction failure 时回滚原始 snapshot
- targeted tests 覆盖 `auth.json` OAuth metadata 保留字段过滤、auth status 不回显密钥，以及 Unix 下保存文件为 owner-only mode
- targeted tests 覆盖 models.json command-backed `apiKey` 在 auth status 中不执行 `!command`、不回显 command stdout，并覆盖 credential-like header 能被识别为 models.json auth status
- targeted tests 覆盖 Bedrock `credential_process` quoted-argv tokenize、不完整引号失败、`Version=1` 校验、`Expiration` 过期拒绝、缺失 AccessKey/SecretKey、非零退出 + stderr 透传、超时识别、空命令 not-configured，以及通过 `BedrockOptions.CredentialProcess` 与 profile `credential_process` 兜底走 SigV4 签名（含 SessionToken 与 region）的 provider 集成与错误透传
- targeted tests 覆盖 Bedrock web identity baseline：`ParseAssumeRoleResponse` 读取 `Credentials` 子节点、缺失 `Credentials` 返回 null、`Expiration` 过期拒绝；`ExtractStsError` 解析 STS `ErrorResponse` 的 `Code`/`Message`；`ResolveAsync` POST `Action=AssumeRoleWithWebIdentity` 到 regional STS 端点并 URL-encode `RoleArn/RoleSessionName/WebIdentityToken`、缺 token/role 返回 not-configured、缺失 token 文件、空 token 文件、STS 4xx + `<Error>` 错误透传、`StsEndpoint` 覆盖；以及 provider 集成（env/options 触发 -> STS XML -> SigV4 签名，含 `x-amz-security-token`）和 STS 失败时不调用 Bedrock
- targeted tests 覆盖 Bedrock ECS container baseline：`ResolveCredentialUri` 把 `_RELATIVE_URI` 拼到 `http://169.254.170.2`、补齐缺失前导斜杠、`_FULL_URI` 优先覆盖、非法 URI 报错；`IsAllowedContainerEndpoint` 接受 169.254.170.2/23、loopback、HTTPS，拒绝其他 HTTP host 与非 http/https scheme；`ParseEcsCredentialsJson` 读取 `AccessKeyId/SecretAccessKey/Token/Expiration` 并标记 `Source=ecs`、过期拒绝、缺字段失败；`ResolveAsync` 走相对/绝对 URI 的 HTTP GET、Authorization token (env 或 file) 注入、非 loopback HTTP 端点拒绝、4xx body 透传、未配置返回 not-configured；以及 provider 集成（RELATIVE_URI + bearer token -> ECS JSON -> SigV4 签名，含 `x-amz-security-token`）
- targeted tests 覆盖 Bedrock IMDS v2 baseline：`ParseInstanceCredentialsJson` 读取 `Code=Success` + 凭证字段并标记 `Source=ec2_instance_metadata`、`Code != Success` 报错；`ResolveAsync` 默认 opt-out、`AWS_EC2_METADATA_DISABLED=false` 启用 v2 PUT token + role list + credentials JSON 流程并附带 `X-aws-ec2-metadata-token`、token 4xx + v1 fallback 直接 GET 不带 token、`AWS_EC2_METADATA_V1_DISABLED=true` 时拒绝 v1 fallback、`Ec2MetadataDisabled=true` option 覆盖 env、`Ec2MetadataServiceEndpoint` 切到 loopback、非允许 host 拒绝；以及 provider 集成（`Ec2MetadataDisabled=false` -> v2 三步 -> SigV4 签名）
- targeted tests 覆盖 Bedrock AssumeRole baseline：未设 `role_arn` 时 not-configured；`role_arn` 无 `source_profile` 时返回未支持错误；标准 source_profile + 静态凭证 + `external_id` 时通过 STS POST + 泛化 SigV4 (service=sts) 签名 + 解析 `<Credentials>` 返回 `assume_role` 来源凭证；source_profile 缺静态凭证 / STS 错误透传；以及 provider 集成（profile `dev` -> 用 base 静态凭证签 STS -> 用 STS 返回的 `ASIAASSUMED` 签 Bedrock）
- targeted tests 覆盖 Bedrock SSO baseline：缺 `sso_account_id/sso_role_name` 时 not-configured；缺 `sso_start_url/sso_region` 时报错；token cache 文件缺失/过期时报错；token cache 解析（`accessToken`、`expiresAt`）；legacy inline SSO 通过 SHA1(start_url) 命名命中 cache、GET portal `federation/credentials`、`x-amz-sso_bearer_token` 头、`role_name/account_id` 查询参数；`sso_session` 形态通过 SHA1(session name) 命名命中 cache；portal 401 错误透传
- targeted tests 覆盖 `Tau.Tui` `InteractiveInputEditor` baseline：char append + Enter 提交、Backspace、Delete + 左右导航、Home/End、Ctrl+Left/Right 词级跳转、Ctrl+Backspace / Ctrl+Delete 词级删除、Ctrl-A/E 行首行尾跳转、Ctrl-K kill-to-end、Ctrl-U kill-to-start、Ctrl-R 反向 history 搜索（Enter 提交、Ctrl-R 切到更旧匹配、Esc 取消恢复原 buffer、Backspace 缩短 pattern）、`FindPrevious/NextWordBoundary` 边界情形（空串、纯空白、连续空格、行尾）、Ctrl-C 取消（不写入 history）、Up/Down history walk 与回到空 buffer、提交时写入 history、history duplicate-drop / capacity 截断
- targeted tests 覆盖 HTML transcript 普通文本 Markdown horizontal rule（`---` / `***` / `___`）渲染为 `<hr>`，三种 marker 都能识别，附近段落继续输出，code fence 内的 `---` 仍按 fenced code 文本输出
- targeted tests 覆盖 HTML transcript 普通文本 Markdown strikethrough (`~~text~~`) -> `<del>`：内嵌 `**strong**` 时同时输出 `<del><strong>...</strong></del>`、紧邻空格的 `~~` 不闭合、inline code 内 `~~` 保持字面量、fenced code 内 `~~` 保持代码块，且 `~~...~~` 不跨行
- targeted tests 覆盖 HTML transcript 普通文本 angle-bracket autolink `<http(s)://...>`：渲染为 `target=_blank rel=noreferrer noopener` 的外链；inline code 与 fenced code 内仍保持 `&lt;...&gt;` 字面量
- targeted tests 覆盖 `Tau.Tui` 持久化 history：`IInputHistoryStore` 加载已有 entries 到 `InputHistory`、新提交追加、连续重复 deduped 不写盘；`FileInputHistoryStore` 真实落盘 → 再次构造 `InputHistory` 时正确恢复顺序、超过 `maxEntries` 时尾部截断、缺失文件不抛错
- targeted tests 覆盖 `Tau.Tui` selector session host：初始渲染、按键移动后 diff apply、Enter 返回选中项、Esc 取消、surface width 变化触发 full redraw、未消费输入不触发新 render
- targeted tests 覆盖 `Tau.Tui` ANSI render surface：full redraw 输出 synchronized output + clear/home + 全量行，line diff 输出 cursor-position + clear-line + replacement text，empty diff 不写输出，尺寸最小 clamp 为 1
- targeted tests 覆盖 HTML transcript 嵌套列表：缩进升级时打开新 `<ul>` / `<ol>`、indent 回到外层时关闭嵌套、同 indent 切换 ordered/unordered 时关闭并重开正确 list，嵌套内项目顺序保持正确
- targeted tests 覆盖 `Tau.Pods` `LogsAsync`：journalctl 命令构造（含 `tau-pod-<name>` unit、`-n <tail>`、回退 `~/.tau_pods/<name>.log`）、缺省 100 行、非 SSH pod 直接拒绝、exec 失败时 surface 错误 summary；CLI 端 `logs` 子命令未 ssh pod 报错、tail 参数非数值时拒绝
- targeted tests 覆盖 `Tau.Pods` model lifecycle：非 SSH pod 拒绝、remote HF cache list/status/remove、`huggingface-cli` pull、python fallback、keepalive argv、显式/默认 config path、未知 pod 和未知 model subcommand
- targeted tests 覆盖 `Tau.Tui` `TuiMessageArea` / `TuiStatusBar`：role prefix、wrap、bottom-anchored visible lines、right segment alignment 和窄宽截断
- targeted tests 覆盖 `Tau.WebUi` JSONL import problem-details：unsupported content type、parser code/line、safe filename、前端 `.jsonl` import content type 和 endpoint contract
- targeted tests 覆盖 `Tau.Mom` `MomLocalDelegationFlow`：due event 入队、provider/model normalize、attachment staging、fake runner call、outbox/status/log/archive 写入和 inbox cleanup
- targeted tests 覆盖 `/history [count|all]` 命令：未提供 snapshot provider 时返回 not-available 错误、`/history` 默认 20 条 newest-first、显式 count 透传给 provider、非数值 argument 报 usage、provider 返回空时显示 empty status
- targeted tests 覆盖 `/clear` 命令：clearScreen action 被显式调用、未注入 action 时返回 not-supported 错误、附加参数报 usage
- targeted tests 覆盖 `/find <pattern>` 命令：UserMessage / AssistantMessage / ToolCallContent 都参与搜索、大小写不敏感、无匹配返回 friendly status、缺 pattern 报 usage、匹配输出包含 role + 序号 + 上下文截断
- targeted tests 覆盖 `Tau.WebUi POST /api/sessions/{id}/clear`：清空 messages 后保留 Title/Provider/Model；session 不存在返回 404
- targeted tests 覆盖 `TauSecretRedactor`（Tau.Ai）：AWS access key（AKIA/ASIA/AROA）/ GitHub token / Slack token / Anthropic `sk-ant-` / OpenAI `sk-` / `Bearer …` / JWT 三段式都替换为 `[redacted]`、非 secret 文本不动、`enabled=false` 跳过、`IsEnabledFromEnvironment` truth table；并覆盖 `Tau.WebUi GET /api/sessions/{id}/export.html` 与 `export.md` 默认对消息正文做替换、返回 `text/html` / `text/markdown`；不存在的 session 返回 404
- targeted tests 覆盖 `Tau.WebUi GET /api/sessions/search?q=...`：大小写不敏感 substring 命中按 UpdatedAt 排序、无匹配返回空数组、缺 `q` 或空 `q` 报 400
- targeted tests 覆盖 `Tau.WebUi POST /api/sessions/{id}/clone`：克隆 messages / provider / model、Title 加 `Copy of ` 前缀、新 Id；源 session 不存在返回 404
- targeted tests 覆盖 `CodingAgentSecretRedactor`：AWS access key（AKIA/ASIA/AROA）/ GitHub token / Slack token / Anthropic `sk-ant-` / OpenAI `sk-` / `Bearer …` / JWT 三段式都替换为 `[redacted]`、非 secret 文本不动、`enabled=false` 跳过、`IsEnabledFromEnvironment` 对 `0/false/1/true/null/""` 的判定；并覆盖 `/export <html>` 默认对常见 secret pattern 做替换，且非 secret 内容保持不变
- 真实 CLI smoke：临时 `TAU_CODING_AGENT_SKILL_PATHS` 下执行 `/skills`，确认真实进程能发现并列出 `/skill:<name>`
- 真实 CLI smoke：临时 `TAU_CODING_AGENT_EXTENSION_PATHS` 下执行 `/extensions` 与 status-only extension command，确认真实进程能发现并执行 JSON extension command；再通过同一 extension JSON 的 `resources.promptPaths/resources.skillPaths` 确认 `/prompts` 和 `/skills` 能发现 extension-contributed resources
- 真实 CLI smoke：`/export` 默认 HTML、`/import <snapshot.json>` -> `/export <session.html>`，检查 HTML 包含 user/assistant/thinking/tool call/tool result 内容
- `dotnet build src\Tau.Mom\Tau.Mom.csproj --no-restore`
- `dotnet test tests\Tau.Agent.Tests\Tau.Agent.Tests.csproj --no-restore`
- `powershell -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke`

## 进度记录

- [x] 完整移植总路线图落到 active plan。
- [x] `Tau.Mom` Slack-compatible channel message envelope seam。
- [x] `Tau.Mom` fake Slack transport / responder seam（transport/responder interface + channel processor + fake responder tests）。
- [x] `Tau.Mom` Slack Web API responder seam（`chat.postMessage` / thread response / `files.uploadV2` + fake HTTP tests）。
- [x] `Tau.Mom` Slack event mapper seam（Socket Mode JSON `app_mention` / DM / skip / file metadata -> `MomChannelMessage` + mapper tests）。
- [x] `Tau.Mom` Slack Socket Mode transport seam（auth/open/read/ack/reconnect + config-gated worker）。
- [x] `Tau.Mom` Slack startup backfill seam（existing log channels + conversations.history + old-message log-only writeback）。
- [x] `Tau.Mom` Slack private file download seam（bot-token HTTP download + attachment manifest）。
- [x] `Tau.Mom` Slack per-channel queue seam（sequential same-channel processing + queue limit + stop bypass）。
- [x] `Tau.Mom` true cancellable stop seam（in-process run registry + linked cancellation token + cancelled status）。
- [x] `Tau.Mom` Agent Skills prompt inventory parity（XML `<available_skills>` + sandbox path mapping + channel override + disable-model-invocation）。
- [x] `Tau.Mom` sandbox/tool delegation seam（host sandbox + docker path/command construction + `bash/read/write/edit/attach` Mom tools）。
- [x] `Tau.Mom` local delegation flow seam（`MomLocalDelegationFlow` 统一 Worker 和 `--once` 的 event -> inbox -> runner -> outbox/status/log/archive 流程，并用 fake runner e2e 固定 attachment staging 和 cleanup）。
- [ ] `Tau.Mom` real Slack smoke。
- [~] `Tau.Mom` Docker sandbox smoke（Docker validate/exec command construction + `--validate-sandbox` 显式入口已可用 fake process runner / host smoke 本地验证；真实容器 smoke 仍缺）。
- [x] `Tau.CodingAgent` JSONL SessionManager tree baseline（header + message/model/session-info/label entries + branch/fork/clone/resume/tree + stats + `.jsonl` export/import）。
- [x] `Tau.CodingAgent` standalone HTML transcript export baseline（`/export` 默认 HTML + `.html/.htm` export + text/thinking/tool/image rendering + text code fence rendering + inline code span rendering + plaintext link rendering + Markdown heading/list/blockquote block rendering + Markdown strong/emphasis span rendering + Markdown pipe table rendering + Markdown task list rendering + image metadata caption + long tool result folding + tool call JSON argument rendering + long tool call arguments folding + branch outline + branch filter/search + current-branch JSONL timeline + label/model/compaction events + 内嵌 JSONL 下载 + message deep-link/copy-link）。
- [x] `Tau.CodingAgent` share/Gist export baseline（`/share` + 临时 HTML transcript + GitHub CLI `gh auth status` / `gh gist create --public=false` + `TAU_SHARE_VIEWER_URL`）。
- [x] `Tau.CodingAgent` manual compaction entry metadata baseline（`compaction` entry + `summary/firstKeptEntryId/tokensBefore/fromHook` + branch restore summary / retained / post-compaction messages）。
- [x] `Tau.CodingAgent` opt-in auto-compaction threshold baseline（`TAU_CODING_AGENT_AUTO_COMPACT_TOKENS` + pending input estimate + JSONL `fromHook=true`）。
- [x] `Tau.CodingAgent` `/session` token budget/context usage baseline（flat/tree session stats + model context window + auto-compaction threshold remaining）。
- [x] `Tau.CodingAgent` prompt template discovery/expansion baseline（`~/.tau/prompts` + `./.tau/prompts` + `TAU_CODING_AGENT_PROMPT_PATHS` + `/prompts` + 参数替换）。
- [x] `Tau.CodingAgent` skill command discovery/expansion baseline（`~/.tau/skills` + `./.tau/skills` + `TAU_CODING_AGENT_SKILL_PATHS` + `/skills` + `/skill:<name>` + system prompt inventory）。
- [x] `Tau.CodingAgent` context files baseline（`~/.tau/AGENTS.md|CLAUDE.md` + parent-to-cwd `AGENTS.md|CLAUDE.md` + generated system prompt `# Project Context` + `--no-context-files` / `-nc` + `/reload` context refresh）。
- [x] `Tau.CodingAgent` JSON extension command/resource/diagnostics baseline（`~/.tau/extensions` + `./.tau/extensions` + `TAU_CODING_AGENT_EXTENSION_PATHS` + `/extensions` + status/runner command expansion + duplicate command invocation names + prompt/skill resource paths + extension file/resource/load diagnostics）。
- [x] `Tau.CodingAgent` reload command baseline（`/reload` + settings retry/thinking/queue reload + extension resource reload + prompt/skill/context-file/keybinding reload + generated system prompt skill/context refresh）。
- [x] `Tau.CodingAgent` failed-turn rollback baseline（runner exception、取消或错误型 `AgentEndEvent` 恢复回合前 snapshot，flat JSON / JSONL tree 不持久化失败输入）。
- [x] `Tau.CodingAgent` host-level retryable error auto-retry baseline（retryable runner exception / 错误型 `AgentEndEvent` 恢复 snapshot 后重试，成功只持久化成功 attempt，耗尽后 rollback）。
- [x] `Tau.CodingAgent` retry audit event baseline（普通 transient retry 写 JSONL `auto_retry_start` / `auto_retry_end` entries，`/tree` search 与 HTML transcript 可显示 retry start/end；成功恢复时 retry end 排在成功 attempt messages 之后）。
- [x] `Tau.CodingAgent` retry settings command baseline（`/retry` current/off/default/numeric 设置 + settings JSON 持久化 + host 同进程热更新 + `/session` retry policy）。
- [x] `Tau.CodingAgent` thinking level command baseline（`/thinking` current/select/cycle/off/minimal/low/medium/high/xhigh + settings JSON 持久化 + 启动恢复到 runner reasoning level + 交互式 TUI selector baseline + 模型能力 clamp）。
- [x] `Tau.CodingAgent` steering/follow-up CLI baseline（runner `Steer/FollowUp` seam + host active-turn input source + Enter/Alt+Enter console mapping）。
- [x] `Tau.CodingAgent` hotkeys listing baseline（`/hotkeys` + 当前 editor `IKeyBindingMap` 注入 + 自定义/禁用 binding 输出 + targeted tests）。
- [x] `Tau.CodingAgent` RPC mode baseline（`--mode rpc` LF JSONL stdin/stdout + prompt/steer/follow_up/abort/new_session parent metadata/state/settings/model/cycle_model/bash/abort_bash/compact/fork/clone/messages/commands/export_html/get_last_assistant_text/set_session_name targeted tests）。
- [x] `Tau.CodingAgent` RPC thinking controls baseline（`set_thinking_level` / `cycle_thinking_level` + settings `defaultThinkingLevel` 持久化 + active prompt 拒绝 + explicit `data:null` response + targeted tests）。
- [x] `Tau.CodingAgent` RPC cycle model baseline（`cycle_model` + settings `enabledModels` scope + 默认模型持久化 + explicit `data:null` response + active prompt 拒绝 + targeted tests）。
- [x] `Tau.CodingAgent` RPC queue / auto-compaction settings controls baseline（`set_steering_mode` / `set_follow_up_mode` / `set_auto_compaction` + settings-backed `get_state` + AgentRuntime queue drain tests）。
- [x] `Tau.CodingAgent` branch summarization baseline（`/fork <entry-id> --summarize [instructions]` + JSONL `branch_summary` entry + branch restore summary context + HTML timeline/readFiles/modifiedFiles + targeted test）。
- [x] `Tau.CodingAgent` logout command baseline（`/logout [provider]` + `auth.json` provider credential removal + env/models.json unchanged status + Tau.Ai/CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` logout OAuth provider selector baseline（`/logout [select|provider]` + OAuth credential provider selector + selector selected/cancel/unavailable/no-OAuth + host selector 接线 + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` changelog command baseline（`/changelog [count|all]` + release notes table parser + `TAU_CODING_AGENT_CHANGELOG_FILE` override + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` settings selector baseline（`/settings [current|path|select]` + settings path/effective summary + TUI selector action dispatch + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` theme command baseline（`/theme [current|list|select|set|clear] [name]` + theme loader/status list + TUI selector + settings `theme` persistence + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` scoped models command + TUI selector baseline（`/scoped-models [current|select|set|add|remove|clear|all]` + settings `enabledModels` 有序 scope + all enabled/no filter 语义 + `TuiMultiSelectList` / `TuiMultiSelectSession` + CodingAgent/Tui targeted tests）。
- [x] `Tau.CodingAgent` Ctrl+P/Ctrl+Shift+P model cycle baseline（`EditorAction.CycleModelForward/Backward` + idle prompt app action result + settings `enabledModels` scope 或全部可用模型循环 + 默认模型持久化 + CodingAgent/Tui targeted tests）。
- [x] `Tau.CodingAgent` /model select + Ctrl+L model selector baseline（`/model select [search]` + 交互式裸 `/model` + Ctrl+L + `CodingAgentModelSelector` + settings 默认模型持久化 + CodingAgent/Tui targeted tests）。
- [x] `Tau.CodingAgent` model auth filtering baseline（`CodingAgentModelAvailability` + CLI router / RPC host 共享模型可用性过滤；实际 `/model select`、裸 `/model`、Ctrl+L、Ctrl+P/Ctrl+Shift+P、显式 `/model`、`/provider`、RPC `get_available_models` / `set_model` / `cycle_model` / settings model update 只展示或接受 auth-configured provider/model；`/scoped-models` 继续维护全部注册模型 scope；CodingAgent/Tui targeted tests）。
- [x] `Tau.CodingAgent` model selector footer/scope/detail/search chrome baseline（`CodingAgentModelSelectorComponent` + `/model select` / 裸 `/model` / Ctrl+L selector 的 auth filtering footer、Tab scoped/all 切换、selected `Model Name: ...`、顶部 `Model Selector` / `Search:` 和普通字符/Backspace 过滤；CodingAgent/Tui targeted tests）。
- [x] `Tau.CodingAgent` auth provider status selector baseline（`/auth [current|select|provider]` + `CodingAgentAuthSelector` + provider status select/cancel/unavailable + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` login OAuth provider selector baseline（`/login [select|provider]` + `CodingAgentAuthSelector` provider list reuse + existing OAuth login seam + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` retry cancellation visibility baseline（retry delay cancellation 明确状态 + JSONL retry end audit + failed input rollback）。
- [x] `Tau.CodingAgent` RPC auto retry baseline（`set_auto_retry` / `abort_retry` + settings retry policy + prompt rollback/retry audit + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` RPC session switch / fork messages baseline（`switch_session` / `get_fork_messages` + JSONL session restore + fork selector user message extraction + CodingAgent targeted tests）。
- [x] `Tau.CodingAgent` context overflow compact-and-retry baseline（context overflow 错误恢复 snapshot 后 compact，记录 `fromHook=true` compaction entry，再重试同一输入；compaction failure 回滚原始 snapshot）。
- [~] `Tau.CodingAgent` interactive tree navigator / richer session metadata（已补命令行 `/tree` filter/search modes、settings `treeFilterMode`、label timestamp、`/tree --interactive` navigator、overlay search、filter cycle、next/prev match、Left/Right page、Ctrl/Alt branch navigation、Space fold、selected metadata、settings-backed `treeCollapsedEntryIds` fold persistence 和 `/metadata [entry-id]` session/entry inspector；多选、session-scoped richer fold state 和完整 TUI metadata inspector 仍未完成）。
- [~] `Tau.CodingAgent` auto-compaction / token threshold / retained-message cut-point（threshold、`/session` budget baseline、Tau-native token-budget recent-message retention baseline、split-turn prefix summary metadata/text baseline 与 context overflow compact-and-retry baseline 已完成，上游 LLM-generated split-turn summarization、compaction extension events 和 cancellation UI 仍缺）。
- [~] `Tau.CodingAgent` retry / recovery parity（host-level retryable error auto-retry、Tau-native retry audit event baseline、`/retry` settings command baseline、RPC `set_auto_retry` / `abort_retry` baseline 和 retry cancellation visibility baseline 已完成，完整 settings UI 控制和完整 retry cancellation UI 仍缺）。
- [~] `Tau.CodingAgent` RPC mode parity（headless JSONL baseline 已完成，并已覆盖 `new_session.parentSession` metadata；extension UI sub-protocol、streamed bash output、full settings 控制和 full command parity 仍缺）。
- [~] `Tau.CodingAgent` settings selector parity（CLI/settings summary、交互式 TUI selector baseline、scoped models nested selector action 与 RPC queue/auto-compaction boolean controls 已完成；完整上游 SettingsList/submenu、images/terminal/transport/packages 等全量配置面仍缺）。
- [~] `Tau.CodingAgent` theme selector parity（theme loader/status、CLI/settings selection baseline 和 `/theme select` TUI selector baseline 已完成；完整上游 theme selector parity、TUI theme rendering、theme file watcher 仍缺）。
- [~] `Tau.CodingAgent` scoped model / selector / cycle parity（CLI/settings `enabledModels` baseline、per-entry thinking-level baseline、模型能力 clamp、交互式 TUI multi-select selector baseline、`/model select [search]` / Ctrl+L model selector baseline、Ctrl+P/Ctrl+Shift+P model cycle baseline、provider auth filtering baseline 和 model selector footer/scope/detail/search chrome baseline 已完成；完整上游 theme/dynamic-border/terminal-host parity 和 per-entry thinking UI editor 仍缺）。
- [~] `Tau.CodingAgent` auth/login/logout selector parity（`/auth [current|select|provider]` provider status selector baseline、`/login [select|provider]` OAuth provider selector baseline 与 `/logout [select|provider]` OAuth provider selector baseline 已完成；完整 OAuth login dialog/session parity、credential refresh UX 和真实 e2e 仍缺）。
- [~] `Tau.Ai` OAuth/device/provider config parity（Anthropic authorization code + PKCE、GitHub Copilot device flow、OpenAI Codex authorization code + PKCE、Gemini CLI authorization code + PKCE + project discovery、Antigravity authorization code + PKCE + project discovery、auth.json schema、refresh 写回、默认本地 auth/models/session 忽略、Unix 0600 写入、OAuth metadata 保留字段过滤、models.json auth status 非解析检查、credential header 状态识别、models.json 显式 OpenAI-compatible dynamic provider registration、`JsonlTauLogSink` runtime event log 默认脱敏 baseline 和 `JsonlSecretRedactor` JSONL string value redaction foundation 已完成；仍缺 SSO token 自动刷新、更广义 dynamic provider API / runtime config UX、CodingAgent/WebUi 等非 `JsonlTauLogSink` JSONL 面脱敏和真实 e2e）。
- [x] `Tau.Ai` Bedrock credential chain（六源全部 baseline 已完成：`BedrockOptions.CredentialProcess` 或 profile `credential_process` 启用 credential helper；`AWS_WEB_IDENTITY_TOKEN_FILE` + `AWS_ROLE_ARN` 或 profile `web_identity_token_file/role_arn` 触发 STS AssumeRoleWithWebIdentity；profile `role_arn` + `source_profile` 触发 STS AssumeRole（用 source profile 静态凭证按泛化后的 SigV4 签 STS 请求）；`AWS_CONTAINER_CREDENTIALS_RELATIVE_URI/FULL_URI` + 可选 token/token-file 触发 ECS task-role 拉取（SSRF 校验）；`AWS_EC2_METADATA_DISABLED=false` opt-in 走 IMDSv2 流程；profile `sso_session` 或 legacy inline (`sso_start_url/sso_region/sso_account_id/sso_role_name`) 触发 SSO portal `federation/credentials`（token cache 路径按 SHA1(sso_session) 或 SHA1(sso_start_url) 计算，可被 `BedrockOptions.SsoTokenCacheFile/SsoTokenCacheDirectory` / `AWS_ENDPOINT_URL_SSO` / `SsoPortalEndpoint` 覆盖）；`BedrockSigV4Signer` 已支持任意 service，STS XML 解析通过 `BedrockStsResponseParser` 共享；来源标记 `static`/`profile:<n>`/`web_identity`/`assume_role`/`credential_process`/`ecs`/`ec2_instance_metadata`/`sso`，错误 verbatim；剩余增量：`credential_source`-based AssumeRole、SSO token 自动刷新和真实云端 e2e）。
- [~] `Tau.WebUi` streaming/attachments/rich rendering/session export（NDJSON streaming endpoint + 前端增量渲染、attachments upload/preview/send、tool call timeline cards、session delete/export/import、title rename、last-opened session restore baseline、auth status、client-side Markdown rendering、WebUi-local 线性 JSONL transcript export/import roundtrip、WebUi-local JSONL export/import 默认 string-value secret redaction、JSONL import content-type / `application/problem+json` code+line 错误合同、前端 `.jsonl` import 分流、CodingAgent JSONL session 只读 preview endpoint、preview-derived conservative import endpoint、CodingAgent JSONL preview/import 默认 string-value secret redaction、fake-runner service-level 行为测试和 Minimal API endpoint NDJSON/session/export-import-delete/error/rename-restore/JSONL export/import/preview/import contract 回归已完成；仍缺完整 WebUi parity 和 CodingAgent JSONL branch/session-tree 导入/持久化语义对齐）。
- [~] `Tau.Tui` input editor baseline（`InteractiveInputEditor` + `IConsoleKeyReader` / `IInteractiveRenderer` seam + `InputHistory`：char append、backspace、delete、左/右、Home/End、Enter、Ctrl-C、Up/Down history、duplicate-drop、capacity 截断；`SystemConsoleKeyReader` + `SystemConsoleInteractiveRenderer` 提供 `Console.ReadKey` / `Console.CursorPosition` 实现；`InteractiveConsoleSession.ReadInputAsync` 在 editor 可用时优先使用，否则回退 `ITerminal.PromptAsync`；`Tau.CodingAgent.Program` 在交互式 console 下默认挂上 editor，redirected stdin/stdout 或 `TAU_CODING_AGENT_DISABLE_INPUT_EDITOR=1` 时回退 `Console.ReadLine`；多行/wrap 编辑仍缺）。
- [~] `Tau.Tui` message/status foundation（`TuiMessageArea` 和 `TuiStatusBar` 已完成库层 role prefix、wrap、bottom-anchored transcript、left/right status segment 和窄宽截断；`TuiScrollbackBuffer` 已完成纯内存 viewport/scrollback foundation；`TuiTranscriptViewport` 已完成 message/status/scrollback 组合 viewport foundation；仍缺接入完整 terminal host、overlay compositing 和硬件 cursor）。
- [~] `Tau.Tui` component/render/selector/message-status foundation（已补 `ITuiComponent` / `ITuiInputComponent`、`TuiContainer`、`TuiBox`、`TuiTextBlock`、`TuiText`、带 footer hint 的 `TuiSelectList`、`TuiMultiSelectList`、`TuiDiffRenderer`、`ITuiRenderSurface`、`TuiAnsiRenderSurface`、`TuiOverlayHost`、`TuiSelectorSession`、`TuiMultiSelectSession`、`TuiMessageArea`、`TuiStatusBar`、`TuiScrollbackBuffer` 和 `TuiTranscriptViewport`；已接入 `Tau.CodingAgent` `/theme select`、交互式 `/settings`、`/scoped-models`、`/model select`、`/auth select` status selector、`/login` OAuth provider selector 和 `/logout` OAuth provider selector baseline；完整 terminal host、overlay compositing、硬件 cursor、theme rendering、完整 OAuth login dialog/session 和 resource selector UI 仍缺）。
- [x] `Tau.Pods` deploy/stop/restart/health/logs（含 CLI target-command 参数解析、SSH lifecycle metadata 命令转义、SSH exec `ArgumentList` argv hardening、本地 ssh 启动失败/runner 异常/cancellation 结构化返回、logs 走 journalctl `tau-pod-<name>` 单元并回退 `~/.tau_pods/<name>.log`，可配置 tail）。
- [~] `Tau.Pods` model lifecycle / vLLM planner（`model list/pull/remove/status`、pod-level `modelsPath`、remote HF cache parsing、`huggingface-cli` pull、python fallback、SSH keepalive、`PodVllmCommandPlanner` 本地 serve/systemd/metadata/remote command plan baseline 和 `vllm plan [path] <id> <model> [name]` plan-only CLI 已完成；真实 SSH/HF download、token handling、vLLM orchestration、systemd install/start/status 和 startup smoke 仍缺）。

## 决策记录

- 2026-05-10：决定新增完整移植总 plan，而不是继续只在 baseline plan 里追加零散条目。原因是用户目标已经明确升级为“完完整整的移植”，需要一个跨模块、跨阶段、可持续评审的 parity closure 路线图；旧 baseline plan 保留为已完成基线和历史决策来源。
- 2026-05-10：决定先把 `Tau.Mom` 的 Slack event 事实抽成 `MomChannelMessage`，而不是直接接 Slack SDK。原因是上游 `SlackEvent`、backfill message、local event 和 file delegation 都会汇入同一 channel log/session/status/attachment 语义；先固定 Tau-native envelope，可以让 file/events/fake Slack/real Slack 共用映射和测试。
- 2026-05-10：决定先落 `IMomChannelTransport` / `IMomChannelResponder` / `MomChannelMessageProcessor`，而不是让真实 Slack adapter 直接调用 `IDelegationAgentRunner`。原因是 busy-state、stop、typing、thread response、status/log writeback 和 attachment staging 都属于 Mom channel runtime 语义；Slack SDK adapter 应只负责收消息、发消息、上传/下载文件。

- 2026-05-10：决定先把 Slack Web API responder 单独落成 SlackWebApiResponder，不和 Socket Mode transport 混在同一切片。原因是发消息、thread 回复和文件上传的 HTTP 契约可以用 fake handler 本地验证，且不需要真实 Slack token；Socket Mode 收消息、用户/频道缓存、backfill 和文件下载继续作为下一切片。
- 2026-05-10：决定先把 Slack receive-side 规则落成 `SlackEventMapper`，仍不引入 Slack SDK。原因是上游 `app_mention` / `message` 的过滤、mention stripping、DM 触发和 file metadata 语义可以用纯 JSON 本地验证；真实 Socket Mode transport 后续只负责连接、ack 和用户/频道缓存。
- 2026-05-10：决定把 Socket Mode transport 做成 SlackSocketModeTransport + ISlackSocketModeConnector + config-gated SlackSocketModeWorker，而不是直接把 ClientWebSocket 写进 worker。原因是 open/ack/read/error 脱敏可以用 fake HTTP/WebSocket 本地验证，默认不开启也不会影响现有 inbox/events worker；backfill、文件下载、queue limit 和 stop cancellation 当时继续后置，后续已拆成独立切片补齐。
- 2026-05-11：决定把 Slack private file download 做成 `SlackAttachmentDownloader`，接在 `MomChannelMessageProcessor` 生成 `DelegationRequest` 之前，而不是塞进 `SlackEventMapper`。原因是 mapper 只负责 Slack JSON 到 Tau-native envelope 的纯映射，authenticated download 需要 bot token、HTTP、workingDirectory 和 attachment manifest，属于 channel runtime 语义。
- 2026-05-11：决定把 Slack queue 做成 `MomChannelQueueDispatcher`，由 `SlackSocketModeWorker` enqueue 后立即继续读 Socket Mode 消息，而不是继续在 worker 中 await processor。原因是上游 mom 的 `ChannelQueue` 是 per-channel sequential work queue；Tau 也需要同频道顺序处理、不同频道独立推进、pending queue limit，并让 `stop` 不排在已有 pending work 后面。
- 2026-05-11：决定把 Slack startup backfill 做成 `SlackBackfillService`，在 Socket Mode worker 启动读消息前对已有 `log.jsonl` 的 channel 执行 `conversations.history`，只写 `log.jsonl` 不触发 delegation。原因是上游 backfill 的目标是把启动前消息补进 channel history，而不是重放旧消息；Tau 保持 mapper/processor/queue 各自边界清楚，backfill 只复用 attachment download 和 log store。
- 2026-05-11：决定把 Slack stop cancellation 做成 `MomChannelRunRegistry`，只取消当前进程内 active channel run 的 linked `CancellationToken`，而不是把 `status.json` 当作可跨进程取消句柄。原因是 `status.json` 只能证明某个 workdir 最近处于 running，不能安全取消已经不存在的 runner；真实 stop 必须落在 in-memory runner token 上，状态文件只负责记录最终 `cancelled` 结果。
- 2026-05-11：决定先把 Mom sandbox/tool delegation 做成本地可验证 seam，而不是直接跑真实 Docker e2e。原因是上游 mom 的核心边界是 executor + `bash/read/write/edit/attach` tool set；Tau 先固定 `host` executor、`docker:<container>` 配置解析、workspace path translation、tool names 和 attach result contract，真实 Docker container smoke 后置，避免把不可用外部环境误写成迁移完成。
- 2026-05-11：决定把 Mom skills 收口为 Agent Skills prompt inventory，而不是实现一套“每个 skill 自动注册成 tool”的 runtime loader。原因是上游 mom 只把 `SKILL.md` 加载进 system prompt，skill 里的脚本仍通过 `bash/read/write/edit` 使用；Tau 因此对齐 XML `<available_skills>`、sandbox path mapping、channel override 和 `disable-model-invocation`，不重复造一个上游没有的工具注册层。
- 2026-05-12：决定先把 `Tau.CodingAgent` JSONL session tree 做成可运行 baseline，而不是一次性迁入上游所有 session-manager 细节。原因是 branch/fork/resume/tree/label 和 `.jsonl` export/import 是用户可见的核心缺口；share/Gist export、richer HTML template、interactive navigator 与 auto-compaction/token threshold 可以在同一 tree store 上继续补，不需要先破坏现有 flat JSON session 兼容。
- 2026-05-12：决定把 HTML export 先落成 Tau-native standalone transcript，而不是直接搬上游带模板 JS、theme、share/Gist 的完整导出系统。原因是当前 Tau 还没有上游 TUI renderer、extension tool renderer 和 share 服务；先保证 `/export` 默认路径和 `/export <path.html>` 能从当前 branch/session 真实导出可打开 transcript，并在 HTML 内提供 branch outline 和本地 JSONL download，再继续补 richer template 和 Gist/share。
- 2026-05-12：决定把手动 `/compact` 先落成 JSONL `compaction` entry metadata baseline，而不是等待 auto-compaction 一起实现。原因是当前 Tau manual compaction 已经会改变 runtime state，如果继续用普通 message 同步到 tree，会丢失上游 session-manager 的 compaction audit boundary；先写 `summary/firstKeptEntryId/tokensBefore/fromHook` 并在 branch restore 时重建 summary message，可以为后续 token threshold、retained-message cut-point 和 retry/rollback 相关恢复接缝复用同一 entry 语义。
- 2026-05-12：决定把 auto-compaction 先做成 opt-in token threshold，而不是默认开启。原因是当前摘要仍需要真实 provider 调用，默认启用会让无凭证本地 CLI 在普通消息前先失败；用 `TAU_CODING_AGENT_AUTO_COMPACT_TOKENS` 明确开启，并把自动触发写成 JSONL `fromHook=true`，可以先固定 audit boundary，retention、retry audit、settings/RPC 和 compaction event/cancellation parity 后续分切片补。
- 2026-05-12：决定先把 token budget UI 落在 `/session` 单行状态里，而不是新增新的 dashboard 或实时状态区。原因是当前 Tau TUI 还没有完整组件/status area，`/session` 已经是 flat/tree session stats 的事实源；复用 `CodingAgentTokenEstimator` 能让手动 compaction、auto-compaction threshold 和用户可见 context usage 使用同一估算语义。
- 2026-05-12：决定先移植文件型 prompt template baseline，而不是直接实现完整 extension runtime。原因是上游 prompt template 是用户可见的动态 slash command 面，且可以完全本地验证；Tau 先对齐 `.md` frontmatter、默认/显式 prompt 目录和参数替换，让 `/template args` 能真正进入 runner，extension command、skill command 和 resource selector 后续在同一发现层继续补。
- 2026-05-12：决定把 CodingAgent skill commands 做成独立 `CodingAgentSkillStore` baseline，而不是等待完整 extension/resource loader。原因是上游 `/skill:<name>` 本质是本地 `SKILL.md` 发现和 prompt expansion，Tau 可以先对齐默认/显式 skill 目录、frontmatter、`disable-model-invocation`、`/skills` 列表、`/skill:<name>` block expansion 和 system prompt inventory；extension command registry、resource selector 与 diagnostics 后续继续补。
- 2026-05-12：决定把 CodingAgent extension commands 先做成 Tau-native JSON 声明式 command/resource baseline，而不是直接嵌入上游 TypeScript/Jiti extension runtime。原因是上游 extension runtime 能执行任意代码并覆盖 commands/tools/events/UI/resource discovery；Tau 当前先交付可审计、可本地验证的 command surface 和 prompt/skill resource path 贡献，按上游顺序让 extension command 先于 skill/prompt 执行，并保留重复命令 `name:1/name:2` 解析规则；完整 TS runtime、custom tools/events、theme selector/TUI rendering/watcher、resource selector 和 diagnostics 后续单独收口。
- 2026-05-13：决定先把 `/tree` 命令推进到可过滤、可搜索、可显示 label timestamp 的命令行 tree viewer，而不是直接实现完整上游 TUI `TreeSelector`。原因是 Tau 当前 TUI 还没有 overlay/select-list/search/fold 组件栈；先把 `default/no-tools/user-only/labeled-only/all` 过滤语义、entry text / label / metadata search、bookkeeping 隐藏、tool-only assistant 隐藏和 label timestamp 固定在可测试命令面，可以减少 JSONL session tree 和后续 interactive navigator 的语义漂移。
- 2026-05-13：决定把上游 `treeFilterMode` 先落到 Tau 现有 settings JSON，而不是等待完整 settings selector UI。原因是当前 Tau 已有 `CodingAgentSettingsStore` 和 `/model` / `/provider` 默认模型持久化，直接扩展同一 JSON 字段即可让 `/tree` 默认过滤模式可配置；同时修正保存默认模型时保留非模型设置，避免后续 settings 字段互相覆盖。
- 2026-05-13：决定把 `/clone` 实现为当前 active branch 的独立 JSONL session export + resume，而不是复用 `/fork` 在同一文件追加 branch marker。原因是上游 `/clone` 的用户语义是“保留当前位置并进入新 session 文件”，而 Tau 已有 `WriteCurrentBranch` 可复用同一序列化/ID remap 路径，能避免在 clone/export/import 三条路径里重复造 session writer。
- 2026-05-13：决定先把 JSONL header 的 `cwd` / `parentSession` 暴露在 `/session` 和 `/tree`，而不是单独新增 session metadata 命令。原因是这两个命令已经是 Tau 当前 session 事实源；把 clone/export 产生的父 session 来源显示出来，可以让用户不用手动打开 JSONL header 就能确认当前会话从哪里分叉。
- 2026-05-13：决定把 HTML transcript export 复用现有 `CodingAgentTreeSessionSummary` 显示 cwd / parent metadata，而不是解析内嵌 JSONL 文本。原因是 router 在 HTML export 前已经同步 tree 并取得 summary；直接使用 summary 可以避免 HTML exporter 和 JSONL serializer 之间形成第二套 metadata 解析逻辑。
- 2026-05-13：决定把 `/share` 先做成 GitHub CLI secret Gist baseline，而不是直接接 GitHub REST API 或自建 Tau share service。原因是上游当前也是依赖 `gh auth status` 和 `gh gist create --public=false`，Tau 可以复用现有 HTML transcript exporter 和一个可注入 share client 固定命令面；真实 `gh` smoke、Tau 专属 viewer 和 richer HTML template 后续单独推进。
- 2026-05-14：决定先给 Tau-native HTML transcript 补 message deep-link / copy-link，而不是直接搬上游完整 `template.js` / `template.css`。原因是 Tau 还没有上游 renderer/theme/custom tool HTML 体系；先把 JSONL entry id、`leafId` / `targetId` URL 语义和定位高亮固定在当前单文件 exporter 上，可以让 `/share` 生成的 secret gist 具备最小可用的消息级引用能力。
- 2026-05-14：决定继续让 Tau-native HTML transcript 直接消费 current branch JSONL，渲染 `session_info`、`model_change`、`label` 和 `compaction` entries，而不是只显示 flat snapshot messages。原因是上游 HTML viewer 的核心价值是可审计的 session tree；Tau 先把 entry timeline、label badge、branch outline 滚动和 compaction 摘要固定在无 vendor 的单文件 exporter 上，完整 Markdown/highlight/theme/custom tool renderer 继续后置。
- 2026-05-14：决定给 Tau-native HTML branch outline 补 `default/no-tools/user-only/labeled-only/all` filter 和 search baseline，而不是等待完整上游 sidebar/tree renderer。原因是 `/tree` 命令面已经固定了这些过滤语义，HTML share/export 应该复用同一用户心智；这仍只是单文件 viewer 的交互基线，不代表上游完整虚拟树、折叠、resizer 和 theme parity 已完成。
- 2026-05-14：决定先把 compaction retained-message 做成 Tau-native token-budget + message-count fallback baseline，而不是一次性迁入上游 split-turn prefix summary、retry/rollback。原因是 Tau 已有 JSONL `compaction` entry 和 branch restore seam，但 `firstKeptEntryId` 还只是占位；先让 manual / auto compaction 按 recent token budget 选择 cut-point，未命中时默认保留最近 4 条 message，并让 restore / clone export 统一消费同一 `firstKeptEntryId`，可以先消除 session tree 的语义空洞，完整 split-turn prefix summary、retry settings/RPC 和 compaction event/cancellation parity 后续再补。
- 2026-05-14：决定把 JSON extension diagnostics 做成 `CodingAgentExtensionCommandStore.LoadStatus()` 的 Tau-native loader status，而不是直接移植上游完整 TypeScript extension runner diagnostics。原因是当前 Tau extension baseline 仍是声明式 JSON，最先要解决的是坏 JSON、缺失显式路径、resource path 和重复 command 解析名不再静默消失；custom tools/events/theme selector/TUI rendering/watcher、interactive resource selector 和更完整 runtime diagnostics 继续保留为后续 parity。
- 2026-05-14：决定先把 retry/rollback 缺口拆成失败普通回合 rollback baseline，而不是直接实现上游完整 auto-retry 编排。原因是 Tau 当前 `RuntimeCodingAgentRunner.RunAsync()` 会先把 user message 写入 runtime，host 只报错再持久化会让 provider exception、取消或错误型 `AgentEndEvent` 污染 flat/tree session；先用回合前 snapshot 恢复 messages/provider/model/name，可以保证 session store 不写失败输入。随后已分切片补入 host-level retry、context overflow compact-and-retry 与 Tau-native retry audit event baseline。
- 2026-05-14：决定把 auto-retry 的第一步落在 `CodingAgentHost`，而不是直接移植上游 AgentSession retry queue / extension events。原因是 Tau 已经有 host-level pre-turn snapshot 和 flat/tree persistence seam，可以先保证 transient provider/runtime error 重试不会污染 session；`TAU_CODING_AGENT_AUTO_RETRY_ATTEMPTS` 和 `TAU_CODING_AGENT_AUTO_RETRY_BASE_DELAY_MS` 先覆盖生产入口，context overflow 单独走 compaction recovery。后续已补 Tau-native JSONL retry audit event；settings/RPC 控制和 retry cancellation UI 仍未完成。
- 2026-05-14：决定把 context overflow compact-and-retry 做在 host 层，并把成功 compaction 后的 session 作为新的 rollback baseline。原因是 Tau 的 JSONL tree 是 append-only，一旦写入 compaction entry 就不应该在后续 retry 失败时把 runner 恢复到 compaction 前却留下 tree compaction 记录；因此 overflow recovery 先恢复失败回合前 snapshot，compact 并记录 `fromHook=true`，随后重试同一输入。若 compaction 本身失败，则不写 compaction entry，直接回滚原始 snapshot。
- 2026-05-14：决定把普通 transient retry 的上游事件语义先落成 Tau-native JSONL audit entries，而不是等待完整 settings/RPC 或 extension event runtime。原因是 Tau 已经有 append-only tree session 和 host-level retry；先写 `auto_retry_start` / `auto_retry_end` 可以让 retry 行为在 `/tree`、HTML export 和 cloned session 中可审计。成功恢复时 retry end 必须排在成功 attempt 的 user/assistant messages 后面，避免审计时间线误导；retry 耗尽时则只保留 retry audit，不持久化失败输入。
- 2026-05-14：决定先把 retry settings 收口成 Tau-native `/retry` 命令和 settings JSON 字段，而不是宣称完成上游完整 RPC/settings UI。原因是 Tau 当前命令路由、settings store 和 host retry option callback 已足够支撑 CLI 可见、可持久化、同进程热更新的 retry 策略；上游 RPC、selector UI 和 cancellation UI 仍保留为后续 parity。
- 2026-05-21：决定把 `/settings` 第一刀限定为 read-only CLI/settings summary，而不是直接搬完整上游 settings selector UI。原因是 Tau 现有 settings store 已承载默认模型、tree filter、retry、default thinking、queue modes、auto-compaction boolean 和 enabledModels scope；先固定可 inspect 的当前合同，可以让后续 selector/TUI edit 复用同一 settings 事实源。
- 2026-05-14：决定先把 split-turn prefix summary 做成 Tau-native deterministic metadata/text baseline，而不是在同一切片增加第二次 LLM summarization 调用。原因是 Tau 现有 compaction 已经能基于 `firstKeptEntryId` 恢复 summary + retained messages；当 cut point 落在同一 user turn 中间时，先把被丢弃的 turn prefix 以 `isSplitTurn` / `turnPrefixSummary` 写入 JSONL，并在 branch restore / HTML transcript 中展示，可以消除 retained suffix 缺上下文的问题，同时继续把上游 LLM-generated split-turn summarization、extension events 和 cancellation UI 留给后续 parity。
- 2026-05-14：决定把 retry cancellation 先收口为 host delay cancellation visibility baseline，而不是直接补上游完整 settings/RPC UI。原因是 Tau 现有 retry delay 已受 `CancellationToken` 控制；补一条明确终端状态、保留 `auto_retry_end` 审计并验证 flat/tree session 不落失败输入，能先让取消行为可见可审计，完整上游 cancellation UI 继续后置。
- 2026-05-14：决定把 richer rendering 的下一步收口为 HTML transcript text code fence rendering baseline，而不是搬完整 Markdown/highlight/template renderer。原因是当前 Tau exporter 已是无外部依赖的 standalone HTML；先解析文本中的三反引号 fenced code block，输出安全转义的 `code-block` / `<code>` 区块，可以立即改善导出/分享里的代码可读性，同时把完整 Markdown、高亮、custom tool renderer 和上游 richer template 继续留给后续 parity。
- 2026-05-14：决定继续把 richer rendering 的相邻切片收口为 HTML transcript long tool result folding baseline，而不是搬完整上游 HTML renderer/template。原因是当前 exporter 里最容易影响可读性的相邻问题是大段工具输出淹没会话；先只对 `ToolResultMessage` 的长文本加 `<details class="tool-result-fold">` 默认折叠，保留全文并复用现有 text/code fence renderer，可以改善导出/分享阅读体验，同时不影响普通 user/assistant 文本，也不把完整 Markdown/highlight/custom tool renderer 写成完成。
- 2026-05-14：决定把 HTML richer rendering 的下一刀限定为 tool call JSON argument rendering baseline，而不是实现完整 custom tool renderer。原因是 `ToolCallContent.Arguments` 当前已经是 JSON 字符串事实源，先把可解析 JSON 格式化为 `code-block` / `<code data-language="json">`，不可解析时保留原始 `<pre>` fallback，能提高 transcript 可读性且不改变工具协议；`JsonSerializer` 格式化阶段使用 relaxed encoder，最终仍由 HTML escape 负责安全输出，避免把 `<tau>` 这类值变成不易读的 `\u003C...`。
- 2026-05-15：决定把 HTML richer rendering 的下一步限定为 plaintext link rendering baseline，而不是引入完整 Markdown renderer。原因是当前普通文本仍保留 `<pre>` 语义，先只在非 code fence 文本段识别 Markdown-style `[label](http/https...)` 和裸 `http(s)` URL，输出 `target="_blank"` / `rel="noreferrer noopener"` 安全外链，可以提高导出/分享可读性，同时避免把 images、tables、headings、emphasis、syntax highlight 或 custom tool renderer 写成已完成。
- 2026-05-15：决定把 image content metadata 收口为 `<figcaption>` baseline，而不是实现完整附件/图片 gallery。原因是 `ImageContent` 当前已经以内嵌 data URI 渲染，补 mime type 和估算字节数可以提升 transcript inspectability，同时不改变图片 payload、不引入附件下载协议，也不把 WebUi attachment parity 写成完成。
- 2026-05-15：决定把超长 tool call arguments 默认折叠成 `<details class="tool-call-arguments-fold">`，而不是引入完整 custom tool renderer。原因是大 JSON 参数会淹没 transcript，折叠可以改善阅读体验；折叠内仍复用已有 JSON `code-block` 格式化和 raw `<pre>` fallback，并保留全文，不改变 tool call 协议。
- 2026-05-15：决定把普通文本 backtick inline code span 收口为 `<code class="inline-code">` baseline，而不是引入完整 Markdown renderer。原因是命令、文件名和短代码片段是 transcript 可读性的高频需求；先在非 fenced code 文本段内识别 inline code，且不在 inline code 内继续做 URL linkification，可以提升可读性并保持渲染边界简单。
- 2026-05-15：决定把普通文本 heading/list/blockquote 收口为轻量 block rendering baseline，而不是引入完整 Markdown/highlight renderer。原因是导出/分享 transcript 里标题、步骤列表和引用是高频结构；Tau 先只识别 `#` heading、简单有序/无序列表和 `>` 引用，继续复用 inline code/link 安全输出，并保持 code fence 内 marker 不参与 block rendering。
- 2026-05-15：决定把普通文本 strong/emphasis 收口为轻量 span rendering baseline，而不是引入完整 Markdown parser。原因是加粗和斜体是 transcript 可读性的高频内联语义；Tau 先只支持 `**strong**` / `__strong__` 与 `*emphasis*` / `_emphasis_`，继续复用 inline code/link 安全输出，并明确 inline code、code fence 和单词内部下划线不触发 emphasis。
- 2026-05-15：决定把普通文本 Markdown pipe table 收口为轻量 table rendering baseline，而不是引入完整 Markdown parser 或表格对齐/扩展语法。原因是导出/分享 transcript 里状态表、对照表和检查表很常见；Tau 先只识别 header + separator + data rows 的常见 pipe table，输出可横向滚动的 `<table>`，单元格复用现有 inline code/link/strong/emphasis 安全渲染，并保持 code fence 内表格文本不参与解析。
- 2026-05-15：决定把普通文本 Markdown task list 收口为轻量 checkbox rendering baseline，而不是引入完整 GitHub Flavored Markdown parser。原因是导出/分享 transcript 里任务清单和迁移 checklist 很常见；Tau 先只识别 list item 开头的 `[ ]` / `[x]` marker，输出 disabled checkbox，任务文本复用现有 inline code/link/strong/emphasis 安全渲染，并保持 code fence 内任务列表文本不参与解析。
- 2026-05-16：决定把 secret 持久化边界先收口到 `auth.json` baseline，而不是一次性宣称覆盖所有配置密钥。原因是 OAuth login/refresh 已经会写回本地凭证，必须先保证默认本地 auth 文件不进 Git、Unix 写入权限为 0600，并过滤 metadata 中会 shadow credential 语义的保留字段；models.json secret、导出/share 和运行态日志脱敏继续作为后续安全配置切片推进。
- 2026-05-16：决定把 models.json secret 边界先收口为“默认本地文件不进 Git + status 非解析检查”，而不是禁止 `!command` 或把 models.json 当成 credential store。原因是上游形态需要 request-time env/literal/command value resolution，但 `/auth` 和 WebUi auth status 这类只读状态面不应该为了展示状态而执行本地命令；因此 runtime request dispatch 继续解析，status 只检查 `apiKey` / credential header 是否存在并保持脱敏。
- 2026-05-17：决定把 Bedrock AWS credential chain 的第一步切到 `credential_process`，而不是先做 SSO / AssumeRole / IMDS / ECS / web identity。原因是 `credential_process` 是 AWS credentials chain 中最自包含的环节（纯子进程 + JSON 输出），profile 解析层已经读取了 `credential_process` 字段，可以直接接 `IBedrockProcessRunner` seam 并通过 stub runner 在本地完整验证；后续 SSO / AssumeRole / IMDS / ECS / web identity 切片可以共用 `IBedrockProcessRunner` 和现有 SigV4 签名层，不需要在第一切片就把 chain 抽象铺满。
- 2026-05-17：决定让 Bedrock provider 在 credential_process 失败时把具体错误（exit code、stderr、Expiration 过期、JSON 解析等）透传到 `ErrorEvent`，而不是统一返回 missing-credentials 兜底文案。原因是 credential helper 失败时用户最需要的就是真实退出码和 stderr 来定位问题；而 missing-credentials 文案只在“静态凭证缺失且没有配置 credential_process”时才有意义。
- 2026-05-17：决定把 Bedrock AWS credential chain 的第二步切到 `web identity from env/profile`，而不是 IMDS / ECS / SSO / AssumeRole。原因是 STS AssumeRoleWithWebIdentity 是 chain 里唯一无需 SigV4（用 OIDC token 自证）也无需 AWS SDK 的 IAM call，能在本地完全用 stub HttpClient 验证；profile 解析层已经读取了 `web_identity_token_file`/`role_arn` 字段，可以直接接入。后续 ECS / IMDS / AssumeRole / SSO 切片可以共用同一个 STS XML 解析层和 STS endpoint 兜底逻辑。
- 2026-05-17：决定让 Bedrock 在 web identity 阶段把 STS `ErrorResponse` 的 `Code`/`Message` 抽出来透传，而不是直接 dump 原始 XML。原因是 STS 错误（`InvalidIdentityToken` 等）才是 OIDC pod 用户排查的关键信号；保留原始 XML 既污染日志又泄露 RequestId 等不必要细节。如果 XML 不是标准 STS 错误格式，再回退到 raw body 兜底。
- 2026-05-17：决定把 ECS container credentials 落成 Tau-native HTTP resolver，并强制 SSRF allowlist（169.254.170.2、169.254.170.23、loopback、HTTPS）。原因是上游 `AWS_CONTAINER_CREDENTIALS_FULL_URI` 是用户/容器编排定义的任意 URL；如果 Tau 不限制，攻击者可以让 coding agent 把进程的元数据 token 发到自己的 endpoint。allowlist 把链上的语义和 AWS SDK 行为对齐，又给 EKS pod identity / 自定义 loopback proxy 留出兼容路径。
- 2026-05-17：决定让 ECS resolver 接在 `credential_process` 之后、`fail` 之前，而不是放在 static/profile 之间。原因是 ECS 触发条件是 env vars（典型容器内部署），credential_process 是显式 profile 配置（用户主动开启）。优先让用户显式配置的 helper 起作用，再回退到容器自动注入的 task role 凭证，与“显式优先”的整体取舍一致；同时把 IMDS 留到这个位置之后，避免在显式 helper 失败时静默退到 metadata service。
- 2026-05-17：决定把 Bedrock IMDS 默认改为 **opt-in**（`AWS_EC2_METADATA_DISABLED=false` 或 `Ec2MetadataDisabled=false` 才启用），与 AWS SDK 默认 opt-out 行为相反。原因是 Tau 作为编码 agent 经常运行在开发机/本地容器/受限网络上，169.254.169.254 通常会触发 TCP 卡顿（不像 ECS endpoint 只在容器里出现）。把默认改为 opt-in 避免每条 Bedrock 请求都为不存在的 metadata service 等 1s+；EC2 实例上用户显式 `AWS_EC2_METADATA_DISABLED=false` 即可拿回 SDK 默认行为。
- 2026-05-17：决定让 IMDS 默认走 IMDSv2（PUT token + 后续 GET 携带 `X-aws-ec2-metadata-token`），仅在 token 请求 4xx 且 `AWS_EC2_METADATA_V1_DISABLED` 未启用时回退 v1。原因是 v2 是 AWS 推荐姿态，能避免 IMDS SSRF 旁路；v1 仅作为旧实例兼容路径，并允许通过 env / option 强制关闭以匹配安全合规需求。
- 2026-05-17：决定先把 AssumeRole 切到 `source_profile`-only（指向静态 profile），credential_source 与递归 source_profile 留后续。原因是大多数用户的 ~/.aws/config 是“base 静态 + dev role_arn + source_profile = base”形态；先打通该路径已经能消费泛化后的 SigV4 签名器和共享 STS XML 解析器，并避免一次性引入递归依赖、credential_source 的多分支组合（Environment / EcsContainer / Ec2InstanceMetadata）和潜在循环。
- 2026-05-17：决定补完 `credential_source` (Environment / EcsContainer / Ec2InstanceMetadata) 三种 AssumeRole 源，重用现有 ECS/IMDS resolver 的 HTTP 路径，并对 `credential_source=Ec2InstanceMetadata` 强制开启 IMDS（覆盖默认 opt-out）。原因是 profile 显式声明 `credential_source = Ec2InstanceMetadata` 已经表明用户在 EC2 上使用 IMDS 作为 AssumeRole 源；若仍受全局 opt-out 控制会产生静默失败。同时仍未支持递归 source_profile，因为递归依赖管理与防循环值得单独切片。
- 2026-05-18：决定把 HTML transcript 的 secret redaction 设为 export 路径**默认开启**，可以通过 `TAU_CODING_AGENT_REDACT_SECRETS=0` 关闭。原因是 `/export` 与 `/share` 是显式对外分享流程，未脱敏的 AWS / GitHub / Slack / Anthropic / OpenAI / Bearer / JWT pattern 一旦命中就会泄露真实凭证；为调试或本地存档需要原始内容时再显式 opt-out 更安全。默认走 message-level 预处理（创建带 redacted text 的新 record），保留 ToolCallContent 的 id/name 等结构字段不动，避免破坏 transcript 的可审计性。
- 2026-05-17：决定让 `ResolveStaticCredentials` 在 active profile 含 `role_arn` 时主动放弃 profile-static，与 AWS SDK 行为对齐。原因是 profile 同时配置 `role_arn` + 静态凭证时，静态凭证是 AssumeRole 的源凭证，不应该被当作 profile 的最终凭证返回，否则 chain 会在 AssumeRole 之前就用错凭证直接签 Bedrock 请求。
- 2026-05-17：决定把 SigV4 签名器泛化到 `(region, service)` 而不是再克隆一份 STS 专用签名器。原因是 STS / Bedrock / 后续可能的 SSO / IAM endpoint 都用同一套 v4 算法；service 进入 credential scope 与 signing key 派生即可，签名器其余逻辑没有任何分支差异。同时 STS XML 解析（成功/错误两条路径）抽到 `BedrockStsResponseParser` 共享，避免 web identity 与 AssumeRole 维护两套不同的解析器。
- 2026-05-17：决定让 Bedrock SSO 直接消费 `aws sso login` 写下的 `~/.aws/sso/cache/<hash>.json` token cache，而不是自己实现 SSO-OIDC token registration / refresh。原因是 Tau 当前没有交互式登录入口（OAuth login 是另一条线），而 `aws sso login` 已经是 AWS CLI 用户的标配。先把已有 token 转成 Bedrock 凭证就能解锁 99% 的 SSO 用例；token 自动刷新和真实 OIDC 注册留到后续切片，按需扩展同一 resolver。
- 2026-05-17：决定让 SSO resolver 同时识别 `sso_session = NAME` 与 legacy inline (`sso_start_url` 直接写在 profile 里) 两种形态，并按 AWS CLI 规则选择哈希输入（SHA1(session name) vs SHA1(start_url)）。原因是上游 AWS CLI 自 2.9 起推荐 `sso-session` 形态，但仍有大量旧 profile 写 inline；同时把 token cache 目录抽成 `BedrockOptions.SsoTokenCacheDirectory` / `SsoTokenCacheFile` 让单元测试不依赖 `~/.aws/sso/cache`。
- 2026-05-19：决定把 interactive tree navigator 的下一步限定为 Space fold/expand、selected metadata 和 no-match Enter 保护，而不是一次性搬完整上游 TreeSelector。原因是 Tau 当前 navigator 仍是纯 console baseline；先把 parent/depth/type metadata 写进 `CodingAgentTreeViewItem` 并在本地交互层实现可测试折叠，可以消除长 session tree 的基本浏览噪音，同时继续把多选、fold 持久化、metadata inspector 和完整上游 selector 后置。
- 2026-05-19：决定继续把 interactive tree navigator 的相邻键位限定为普通 Left/Right 分页和 Ctrl/Alt+Left/Right 的 fold-or-up / unfold-or-down 分支导航，而不是实现完整上游 `TreeSelectorComponent`。原因是上游当前键位语义已经明确，Tau 的纯 console navigator 可以先复用 visible parent/children map 固定分支段跳转行为；多选、fold 持久化和 metadata inspector 仍作为后续 parity 切片。
- 2026-05-20：决定把 Steering/FollowUp 先落为 Host 层可注入 input source baseline，而不是重写完整 TUI editor。原因是 `AgentRuntime` 已有 steering/follow-up queue，缺口在 runner seam 和 active turn 期间输入转发；`ICodingAgentTurnInputSource` 让测试 deterministic，生产端使用 `Console.KeyAvailable` 避免不可取消 `Console.ReadKey` 在 turn 结束后悬挂。完整 TUI overlay、快捷键提示和 RPC `steer/follow_up` 继续后置。
- 2026-05-21：决定把 `/hotkeys` 第一刀限定为当前交互式 editor 的 `IKeyBindingMap` listing，而不是宣称完成上游全局 shortcut registry。原因是 Tau 目前已有 `KeyBindingMap` / `KeyBindingFileStore`，最小用户价值是看到当前实际 editor 绑定；app/session/tree/extension shortcut registry 和 footer hints 依赖更完整 TUI/extension runtime，继续后置。
- 2026-05-21：决定把 `/reload` 第一刀限定为 settings、JSON extension resources、prompt templates、skills 和交互式 editor keybindings 的当前进程重载，而不是宣称完成上游完整 reload。原因是当时 Tau 还没有 theme/context file loader 或 TypeScript extension runtime lifecycle；先把已有 stores、extension resource state 和 generated system prompt skill inventory 做成可验证 reload，避免把未移植 runtime 写成完成。context files 和 theme loader/status baseline 已在后续独立切片补齐，完整 TypeScript extension runtime、theme selector/rendering/watcher 和 full resource selector 仍后置。
- 2026-05-21：决定把 CodingAgent context files 第一刀限定为 Tau-native `~/.tau` + ancestor `AGENTS.md` / `CLAUDE.md` baseline，而不是直接移植完整上游 theme/context/resource loader。原因是上游核心用户价值是把项目协作规则稳定注入 system prompt；Tau 用 `~/.tau/AGENTS.md|CLAUDE.md` 作为用户级目录，项目级按 parent-to-cwd 顺序读取 `AGENTS.md|CLAUDE.md`，并保留 `--no-context-files` / `-nc` 禁用开关。theme loader/status 后续已补，完整 TypeScript extension runtime、theme selector/rendering/watcher 和 full resource selector 继续后置。
- 2026-05-21：决定把 `/logout` 第一刀限定为删除本地 `auth.json` provider credential entry，而不是清理环境变量或 `models.json` credential 配置。原因是上游 `AuthStorage.logout(provider)` 的核心语义是删除 auth storage entry；env 和 `models.json` 是用户外部配置/模型配置来源，自动改写会越界并可能破坏可恢复性。2026-05-22 已补交互式 OAuth provider selector baseline，但完整 OAuth login/session parity、credential refresh UX 和真实 e2e 仍需继续推进。
- 2026-05-21：决定把 `/changelog` 第一刀限定为读取 Tau 本地 release notes 表，而不是新增根级 `CHANGELOG.md` 或移植上游启动 changelog/collapse/telemetry。原因是 Tau 已有 `docs/releases/feature-release-notes.md` 作为用户可感知变更记录；显式命令先接这个仓库事实源，避免引入启动副作用、网络遥测或未完成 TUI 渲染。
- 2026-05-21：决定把 `/scoped-models` 第一刀限定为 CLI/settings `enabledModels` baseline，而不是一次性搬完整 scoped model selector 或 Ctrl+P model cycling。原因是 Tau 当前尚未具备完整 TUI selector / model cycle UI 层；先固定持久化语义、provider/model 解析、all enabled/no filter 表达和错误边界，可以让后续 selector 直接复用同一 settings contract。
- 2026-05-20：决定把 RPC Mode 第一刀落成 Tau-native LF JSONL headless baseline，而不是一次性搬完整上游 RPC mode。原因是上游协议还包含 extension UI、bash、settings、session switch 和 export_html 等多个子协议；Tau 当前最需要的是让外部进程能稳定驱动 runner、session/tree/settings 和 compaction seam。先交付 prompt / steer / follow_up / abort / state / model / compact / fork / clone / messages / commands，可以解锁 IDE/WebUi 进程嵌入的最小合同，同时继续把完整上游 RPC parity 后置。
- 2026-05-21：决定把 RPC session utility 第二刀限定为 `export_html`、`get_last_assistant_text` 和 `set_session_name`。原因是三者都复用现有 HTML exporter、runner messages、session name 与 flat/tree store seam，能低风险补齐 headless client 常用能力；bash/abort_bash、settings RPC、session switch 和 extension UI 需要单独协议与安全边界，继续后置。
- 2026-05-21：决定把 RPC thinking controls 第三刀限定为 `set_thinking_level` / `cycle_thinking_level`。原因是 Tau 已有 `/thinking`、runner `ThinkingLevel` 与 settings `defaultThinkingLevel` 合同，RPC 只需复用同一事实源即可让 headless client 调整 reasoning 档位；settings RPC、session switch、bash/extension UI 继续按独立协议边界后置。
- 2026-05-21：决定把 RPC cycle model 第四刀限定为 `cycle_model`，复用 settings `enabledModels` 与 runner `SelectModel()`，不扩 runner interface。原因是 Tau 已有 `/model`、`/scoped-models` 和 settings scope 合同；headless client 只需要一个可审计的 next-model 切换入口。候选不足两个时显式返回 `data:null`，active prompt 期间拒绝切换，settings RPC、session switch、bash/extension UI 继续后置。
- 2026-05-21：决定把 RPC auto retry 第五刀限定为 `set_auto_retry` / `abort_retry`，复用现有 retry policy、settings retry fields、rollback snapshot 和 JSONL retry audit，不引入第二套 RPC retry 状态。原因是上游 RPC 对这两条命令也是委派给 session retry 开关和 retry abort；Tau 当前最小缺口是让 headless prompt 使用同一 retry/rollback/audit 语义，完整 settings selector/UI、bash/abort_bash、session switch 和 extension UI 继续后置。
- 2026-05-21：决定把 RPC session switch 第六刀限定为 `switch_session` / `get_fork_messages`，复用现有 JSONL tree session `Resume()`、flat snapshot persist 和 user message extraction，不扩展到 bash、full settings RPC 或 extension UI。原因是这两条都是上游 headless session utility 的低耦合入口：`switch_session` 只需要恢复指定 JSONL session，`get_fork_messages` 只需要列出可 fork 的 user messages；bash/abort_bash 和 full settings RPC 仍按独立边界后置。
- 2026-05-21：决定把 RPC queue / auto-compaction settings controls 第七刀限定为 `set_steering_mode`、`set_follow_up_mode`、`set_auto_compaction` 和 settings-backed `get_state`。原因是 queue mode 需要真实影响 `AgentRuntime` drain 语义，不能只写 RPC 响应；auto-compaction boolean 需要和 threshold budget 分离，`true` 不自动生成 `TAU_CODING_AGENT_AUTO_COMPACT_TOKENS`，`false` 则禁止生产入口自动触发。完整 RPC bash/extension UI/full settings selector 继续后置。
- 2026-05-21：决定把 RPC bash 第八刀限定为 `bash` / `abort_bash` 的阻塞式 result baseline，而不是一次性搬完整 terminal subsystem。原因是上游 RPC 的稳定 response 类型是 `BashResult`；Tau 先固定 `ICodingAgentShellRunner` 接缝、后台执行、并发拒绝、取消和 output/exitCode/cancelled/truncated/fullOutputPath 响应形态，streamed bash output、terminal UI 与 extension UI 继续后置。
- 2026-05-21：决定把 RPC settings 第九刀限定为 `get_settings` / `update_settings` 的 Tau-supported settings baseline，而不是宣称完整上游 settings runtime parity。原因是 Tau 当前 settings store 当时只承载默认模型、tree filter、retry、default thinking、enabledModels、queue mode 和 auto-compaction boolean；先固定 headless snapshot/update 合同并同步 runner runtime 状态，theme、terminal、packages、markdown、images、transport 等上游全量配置面继续按后续真实实现推进。后续 CLI/settings theme baseline 已把 `theme` 纳入同一 settings 事实源。
- 2026-05-22：决定把 `/theme` 第一刀限定为 CLI/settings-backed selection baseline，而不是直接搬完整上游 theme selector / TUI rendering。原因是 Tau 已有 `CodingAgentThemeStore` 发现 built-in、用户、项目、显式和 extension-contributed themes，也已有 settings `theme` 字段；先把 list/set/clear/current 的可审计命令面固定，能让用户选择主题并给后续 TUI selector/rendering/watcher 复用同一事实源。
- 2026-05-22：决定在 CodingAgent extension runtime 之前先补 `Tau.Tui` 组件/render/selector foundation。原因是 settings/theme/scoped-model/thinking/OAuth/resource selector 都依赖同一类 TUI 选择器和差分渲染能力；先把 `ITuiComponent`、基础组件、`TuiSelectList` 和 `TuiDiffRenderer` 做成可测试库层，可以避免每个 selector 在 CodingAgent 里继续手写一次性 console UI。完整 terminal host、overlay compositing、message/status area、ANSI renderer 和具体 CodingAgent selector 接线继续拆后续切片。
- 2026-05-22：决定继续把 selector host 收口为 `ITuiRenderSurface` + `TuiOverlayHost` + `TuiSelectorSession`，而不是直接实现完整 ANSI terminal host。原因是后续 CodingAgent settings/theme/scoped-model selector 最先需要的是 deterministic render/input loop 和 select/cancel result；真实 terminal synchronized output、viewport/scrollback、overlay compositing 和硬件 cursor 仍应作为独立渲染宿主切片，避免把终端副作用混入 selector 语义测试。
- 2026-05-22：决定把第一版真实输出层限定为 `TuiAnsiRenderSurface`，只消费 `TuiRenderDiff` 并写 ANSI buffer。原因是 selector host 已经能产生稳定 diff，最小下一步是让 diff 能落到 `Console.Out` 或任意 `TextWriter`；viewport/scrollback、overlay compositing 和硬件 cursor 需要更多终端状态跟踪，应独立推进，不和 ANSI sink 的纯翻译逻辑混在一起。
- 2026-05-21：决定把 RPC `new_session.parentSession` 先落到现有 JSONL header metadata，而不是引入新的 session registry。原因是 Tau 已经把 header `parentSession` 暴露到 `/session`、`/tree` 和 HTML export；headless client 新建会话时只需要把上游同名字段接到同一事实源，CLI `/new` 继续保持无 parent metadata 的本地行为。
- 2026-05-22：决定把第一条真实 CodingAgent selector 接到 `/theme select`，而不是先做完整 settings selector 或 theme rendering。原因是 theme store/status/settings 合同已经稳定，且 TUI foundation 已有可测试 selector session；先用可注入 `CodingAgentThemeSelector` 验证真实 command -> TUI selector -> settings persistence 路径，能降低后续 settings/scoped-model/OAuth selector 接线风险。theme file watcher、theme rendering 和完整上游 selector parity 继续后置。
- 2026-05-22：决定把 `/settings` selector baseline 限定在 Tau 已有真实 settings seam 上，并把 scoped models 作为 nested action 接入，而不是一次性搬完整上游 SettingsList/submenu。原因是 auto-compaction、steering/follow-up mode、tree filter、thinking level、theme 和 enabledModels 已经有持久化/运行态事实源；images、terminal、transport、packages 等字段尚未建模，不能在 selector UI 里伪装成可用配置。
- 2026-05-22：决定把 `/scoped-models` selector 做成 settings-backed 多选 baseline，而不是直接实现 Ctrl+P model cycling。原因是现有 `enabledModels` 合同已经能表达 all enabled 与有序 scope；先用 `TuiMultiSelectList` 固定 filter、toggle、provider toggle、enable all、clear、reorder 和 save/cancel，可让 settings selector 与裸 `/scoped-models` 复用同一选择器，同时把运行中模型快速循环继续留给独立切片。
- 2026-05-22：决定把 `/auth select` 第一刀限定为 provider auth status selector，而不是直接触发 OAuth login 或改写 credential store。原因是 Tau 当前已有 `ProviderAuthStatus` 和 `/auth` status-only 输出，先把 provider 选择与状态展示接进同一 `TuiSelectList` foundation，可以让用户检查各 provider 配置状态，同时避免 selector UI 产生凭证写入副作用；完整 OAuth login-session parity 继续作为独立 auth/login 切片推进。
- 2026-05-22：决定把 `/login` 的第一刀限定为 OAuth provider selector baseline，而不是完整搬上游 login dialog/session UI。原因是 Tau 已有真实 `IOAuthProvider.LoginAsync(...)` 和 `SaveOAuthCredentials(...)` seam，缺口是交互式 provider 选择；先复用 `CodingAgentAuthSelector` 只选择当前注册且有 OAuth provider 的 provider，选择后走既有 login/save 路径，可以把裸 `/login` 对齐上游 selector 入口，同时不新增第二套 credential store 或浏览器 UI。
- 2026-05-22：决定把 `/logout` selector 第一刀限定为 OAuth credential provider 选择 + 现有 `auth.json` removal seam，而不是完整上游 login/session 管理。原因是 Tau 已有 `ProviderAuthStatus.UsesOAuth` 和 `_runner.Logout(provider)`，selector 只需要列出当前有本地 OAuth credential 且注册了 OAuth provider 的 provider；显式 `/logout <provider>` 保持可清理任意本地 auth entry，环境变量和 `models.json` 仍不属于本命令改写范围。
- 2026-05-22：决定把 `/thinking select` 做成显式 Tau-native selector，而不是改变裸 `/thinking` 的查询语义。原因是 Tau 已有脚本友好的 `/thinking` status 行为，直接改成打开 UI 会破坏非交互/测试合同；`select` 复用上游 thinking selector 的 level 顺序和说明文案，先固定 settings/runner 同步路径，模型能力 clamp 后续再跟随 model metadata 补齐。
- 2026-05-22：决定把 Ctrl+P / Ctrl+Shift+P 第一刀限定为空闲输入 prompt 的 model cycle baseline，而不是直接搬完整上游 model selector overlay。原因是 Tau 已经有 `enabledModels` scope、RPC `cycle_model` 和 host/router 持久化 seam；先让 `InteractiveInputEditor` 返回 app action 并复用 router 切换模型，可以把最常用快捷键闭环落地，同时把 footer hints、model selector overlay、scoped model thinking-level per-entry 和更完整 auth filtering 后置。
- 2026-05-22：决定把显式 model selector 第一刀限定为 `/model select [search]`、交互式裸 `/model` 和 Ctrl+L baseline，而不是直接搬完整上游 `ModelSelectorComponent`。原因是 Tau 已有 `enabledModels` scope、`TuiSelectList` / `TuiSelectorSession` foundation 和 model cycle 持久化 seam；先固定显式选择模型并保存默认设置，完整 overlay/footer hint/provider auth filtering/scoped model thinking-level per-entry 后置。
- 2026-05-23：决定用共享 `CodingAgentModelAvailability` 收口 CLI router 与 RPC host 的模型可用性判断，而不是让 `/model`、model cycle 和 RPC 各自实现 provider auth filtering。原因是上游 `ModelRegistry.getAvailable()` / `AgentSession.setModel()` 的核心语义是只展示或切换已配置凭证的模型；共享 helper 可以避免 selector、cycle、显式切换和 headless RPC 之间漂移。
- 2026-05-23：决定 `/scoped-models` 不按 auth-configured 收窄，继续基于全部注册模型维护 settings `enabledModels`。原因是它是配置入口，用户需要能预先配置尚未登录的 provider/model；真正运行时入口才按 auth status 过滤，避免用户在 selector/cycle/switch/RPC 中被带到无法调用的 provider。
- 2026-05-23：决定把 model selector 的提示先收口为 `TuiSelectList` 通用 footer hint + `CodingAgentModelSelector` auth filtering 文案，而不是直接搬完整上游 `ModelSelectorComponent` overlay。原因是 Tau 当时 selector host 仍是单组件列表，没有 search input chrome；先把“只显示已配置凭证模型”的用户可见解释落到 footer，能和已完成的 auth filtering baseline 闭环。scope toggle、selected detail、search/chrome、scoped model thinking-level per-entry baseline 和模型能力 clamp 已在后续相邻切片补齐，完整 theme/dynamic-border/terminal-host parity 与 per-entry thinking UI editor 仍留给后续 TUI host / selector parity。
- 2026-05-23：决定把 model selector scope toggle / selected detail 做成 CodingAgent 专用 `CodingAgentModelSelectorComponent`，而不是把 `TuiSelectList` 泛化为带任意 header/detail 的复杂组件。原因是本切片只需要补齐上游 `ModelSelectorComponent` 中已清晰的 scoped/all 切换和 `Model Name` 明细；search/chrome 已在后续同日切片补齐，完整 theme/dynamic-border/terminal-host parity 仍依赖更完整 TUI host，不应该为了单个 selector 提前扩大基础组件合同。
- 2026-05-23：决定把 model selector search chrome 继续放在 `CodingAgentModelSelectorComponent`，而不是把 `TuiSelectList` 改成带标题、搜索框和 detail 区的复合组件。原因是当前基础列表已经被 theme/auth/thinking 等 selector 复用，过早扩张通用组件会牵连无关选择器；模型选择器自己的标题、搜索状态、scope、detail 和 footer 可以先作为专用 wrapper 闭环，完整 theme/dynamic-border/terminal-host parity 继续留给后续 TUI host。
- 2026-05-23：决定把 scoped model per-entry thinking 第一刀落在 `enabledModels` 字符串 pattern、命令/RPC 解析和 cycle 应用上，而不是引入新的 settings JSON 结构或扩展 multi-select UI。原因是上游 settings 本身使用 `provider/model:thinking` 字符串模式；保持同一字段能兼容已有配置和 RPC `update_settings`，同时用 selector 保存时保留旧 suffix 避免 UI 往返丢 metadata。完整逐项 thinking 编辑 UI 留给后续 selector/settings parity；模型能力 clamp 已在后续同日切片补齐。
- 2026-05-23：决定把 thinking 模型能力 clamp 放在 `Tau.CodingAgent` runtime 共享 helper，而不是只依赖 provider 请求层兜底。原因是 `/thinking`、settings selector、启动恢复、CLI/RPC model switch 和 scoped model override 都会在真正发请求前暴露当前 thinking 状态；统一在 `CodingAgentThinkingLevels` 里按 `Model.Reasoning` 和 `ModelCatalog.SupportsXhigh()` 归一，能让 settings/state/RPC 返回值和 provider payload 保持一致。
- 2026-05-23：决定在 `c6ed266` 干净提交之后开启 3 个并行 worker，分别推进 WebUi JSONL export baseline、Mom Docker sandbox validate/exec 可测试 seam、Pods SSH exec `ArgumentList` hardening，而不是继续让 worker 触碰刚大幅修改的 `Tau.CodingAgent` / `Tau.Tui` 热点路径。原因是这三个应用面写入目录和测试项目基本隔离，能获得并行收益；第四个以上 worker 会显著增加合并与验证成本。WebUi 本轮只声明 WebUi-local 线性 transcript，不宣称 CodingAgent branch/session-tree parity；Mom 本轮只固定 fake runner command seam，不宣称真实 Docker smoke；Pods 本轮只固定本地 ssh argv 构造，不宣称完整远端 transport hardening。
- 2026-05-23：决定下一轮继续沿同样的 3 worker 切片推进 WebUi/Mom/Pods，而不是扩大到 CodingAgent/Tui 热点路径。WebUi 先补 WebUi-local `import.jsonl` 线性 transcript roundtrip，不直接接 CodingAgent tree store；Mom 先补 `--validate-sandbox` 显式 validation 入口，但普通 `--once`/worker 不自动验证 Docker，避免缺 Docker 时误伤本地委派；Pods 先固定本地 ssh 启动失败、runner 异常和 cancellation 的结构化 failure，不新增全局 timeout 策略。原因是这些切片继续保持写入范围隔离，并能把上一轮 baseline 变成可导入、可诊断、可 smoke 的合同。
- 2026-05-23：用户要求直接扩大到 6 个 agent 后，决定从干净提交 `f80153d` 开启 6 个互斥 worker：`Tau.Ai`、`Tau.CodingAgent`、`Tau.Tui`、`Tau.WebUi`、`Tau.Mom`、`Tau.Pods` 各一个。原因是前两轮已经证明 WebUi/Mom/Pods 三面能隔离推进，本轮额外加入 Ai/CodingAgent/Tui 但要求每个 worker 只写自己的模块和测试，且不改 README/docs/scripts，由主控统一文档/history/验证/提交。代价是集成和全仓验证成本上升，因此全仓 build/test 仍必须由主控串行执行，避免 Roslyn 文件锁和跨模块语义漂移。
- 2026-05-23：决定把 `models.json` 的 dynamic provider registration 限定为显式 OpenAI-compatible marker，而不是把任意未知 `api` 都注册为 OpenAI-compatible。原因是 custom provider 入口需要降低改代码成本，但拼错 API 名不能静默变成可调用 provider；因此只接受 `openai-compatible`、`openai-completions` 和 `openai-chat-completions` 三个 marker。
- 2026-05-23：决定把本轮 `Tau.Tui` message/status 切片限定为库层 `TuiMessageArea` / `TuiStatusBar` foundation，不直接接 CodingAgent 主屏。原因是 terminal host、viewport/scrollback、overlay compositing 和 theme rendering 仍未闭合；先固定纯函数式渲染合同能让后续接主屏时不混入 runtime 语义。
- 2026-05-23：决定把 `Tau.Pods` model lifecycle 做成 SSH/Hugging Face cache baseline，而不是直接启动 vLLM runner 或真实下载 smoke。原因是 list/status/remove/pull 的远端命令合同和 cache path 解析可本地验证；真实 SSH server、HF token/download 和 vLLM orchestration 对环境依赖更重，应作为后续 smoke/orchestration 切片。
- 2026-05-23：用户要求“下一步继续并行”后，决定从干净提交 `bcba121` 继续开启 6 个互斥 worker：`Tau.Ai` 补 runtime 日志/JSONL export 脱敏边界，`Tau.CodingAgent` 补 tree metadata/fold 相邻 CLI seam，`Tau.Tui` 补 viewport/scrollback foundation，`Tau.WebUi` 补 CodingAgent JSONL branch session 只读 adapter baseline，`Tau.Mom` 补多消息 session sync/runtime flow 本地 seam，`Tau.Pods` 补 vLLM runner command planner baseline。原因是上一轮 6-agent 已验证模块级写入隔离可行；本轮继续要求 worker 不改 README/docs/scripts，由主控串行整合文档、history、验证和提交，避免并行 build/test 触发 Windows/Roslyn 文件锁。
- 2026-05-23：本轮后续执行时，Tui/Mom 原始 worker 成功返回，WebUi/Pods 补开 worker 成功返回，Ai/CodingAgent 切片由主控本地补齐。最终实现边界收口为：`JsonlSecretRedactor` 通用 JSONL string value redaction foundation、CodingAgent append-only `tree_state` session fold snapshot、`TuiTranscriptViewport` 组合 viewport、WebUi CodingAgent JSONL conservative import、Mom `log.jsonl` / `last_prompt.jsonl` 默认脱敏、Pods `vllm plan` plan-only CLI。定向测试串行通过 `Tau.Ai.Tests` 204、`Tau.CodingAgent.Tests` 342、`Tau.Tui.Tests` 110、`Tau.Agent.Tests` 76、`Tau.Pods.Tests` 65、`Tau.WebUi.Tests` 29；仍不宣称 full parity、真实 Slack/Docker/vLLM/SSH/HF/e2e 已完成。
- 2026-05-23：决定把 WebUi JSONL export/import 也接入 `JsonlSecretRedactor`，并让 CodingAgent JSONL preview/conservative import 在生成 preview text / WebChat DTO 前先脱敏 string value。原因是 WebUi-local JSONL transcript 已经能导出和再导入，CodingAgent JSONL 也能通过 WebUi 预览/保守导入；如果只保护 HTML/Markdown 或 CodingAgent tree writer，WebUi JSONL 文件仍会成为旁路泄漏面。规则保持和其它 JSONL writer 一致：默认开启、`TAU_WEBUI_REDACT_SECRETS=0` opt-out、只改 string value、field key/number/bool/null 保持原样。
