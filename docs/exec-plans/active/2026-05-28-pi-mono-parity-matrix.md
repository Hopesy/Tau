# pi-mono -> Tau parity matrix

Status values:

- `verified`: Tau implementation has direct behavior evidence from tests/smoke/e2e and docs are current.
- `ported`: Tau implementation exists, but evidence is not yet strong enough for final 100% acceptance.
- `partial`: Tau covers part of the upstream behavior, with known behavior gaps.
- `missing`: No meaningful Tau equivalent yet.
- `external-e2e-needed`: Contract/fake coverage may exist, but real provider/service/runtime validation is still required.
- `non-goal-proposed`: Candidate non-goal; only valid after explicit user confirmation.

This matrix is the Phase 1 inventory surface for `docs/exec-plans/active/2026-05-28-tau-100-percent-pi-mono-parity.md`. It is intentionally evidence-first: every status must point to upstream source, Tau target, and validation evidence or remaining proof needed.

## Inventory Progress

| Area | Upstream scope | Tau scope | Status | Notes |
| --- | --- | --- | --- | --- |
| AI + Agent | `packages/ai`, `packages/agent` | `src/Tau.Ai`, `src/Tau.Agent`, tests | partial | Capability inventory plus `packages/ai/src/**`, AI command/API/env/config/log/schema surface, and `packages/agent/src/**` file-level mapping are now frozen. Known final blockers include provider e2e, exact public API/bin/export shape, auth refresh/login e2e, broader config UX, and the remaining facade option/public export decisions around Agent. |
| CodingAgent + Tui | `packages/coding-agent`, `packages/tui` | `src/Tau.CodingAgent`, `src/Tau.Tui`, tests | partial | Capability inventory plus grouped `packages/coding-agent/src/**`, CodingAgent CLI/RPC/env/config/log/schema surface, `packages/tui/src/**`, and Tui terminal/input/render/theme surface are now frozen. Known final blockers include package/bin/config parity, full TreeSelector/session schema, TypeScript extension runtime, image/clipboard ingestion, terminal host, terminal image/loader/rich markdown and live TTY smoke. |
| WebUi | `packages/web-ui` | `src/Tau.WebUi`, tests | partial | Capability inventory plus grouped `packages/web-ui/src/**` and WebUi HTTP/component/env/config/log/schema/browser/release surface are now frozen. Known final blockers include reusable component package parity, IndexedDB/provider-key/custom-provider UI, CodingAgent branch/tree true persistence, artifact renderers, sandbox runtime providers, browser/release packaging parity. |
| Mom | `packages/mom` | `src/Tau.Mom`, Mom-related `tests/Tau.Agent.Tests` | partial | Capability inventory plus grouped `packages/mom/src/**`, `packages/mom/scripts/**`, and Mom Slack/event/session/sandbox/env/config/log/schema surface are now frozen. Known final blockers include real Slack smoke, Slack session sync/schema parity, Docker smoke, fs-watch event runtime, and timestamp migration helper parity. |
| Pods + root scripts | `packages/pods`, root scripts/manifests | `src/Tau.Pods`, `tests/Tau.Pods.Tests`, `scripts/*`, CI/release | partial | Capability inventory plus `packages/pods/src/**` file-level mapping and root script matrix are now frozen. Known final blockers include real SSH/HF/GPU/vLLM smoke, upstream GPU allocation/model prompt parity, rollout/rollback state, transport hardening and release artifact parity. |

## Upstream Package Surface

Initial file counts from `C:\Users\zhouh\Desktop\pi-mono-main`:

| Package | Total tracked source/doc/config files counted | `src/**` files counted | Tau target |
| --- | ---: | ---: | --- |
| `packages/ai` | 109 | 44 | `src/Tau.Ai`, `tests/Tau.Ai.Tests` |
| `packages/agent` | 15 | 5 | `src/Tau.Agent`, `tests/Tau.Agent.Tests` |
| `packages/coding-agent` | 411 | 137 | `src/Tau.CodingAgent`, `tests/Tau.CodingAgent.Tests` |
| `packages/tui` | 57 | 25 | `src/Tau.Tui`, `tests/Tau.Tui.Tests` |
| `packages/web-ui` | 85 | 75 | `src/Tau.WebUi`, `tests/Tau.WebUi.Tests` |
| `packages/mom` | 29 | 16 | `src/Tau.Mom`, Mom-related `tests/Tau.Agent.Tests` |
| `packages/pods` | 22 | 10 | `src/Tau.Pods`, `tests/Tau.Pods.Tests` |

Count method:

```powershell
$root='C:\Users\zhouh\Desktop\pi-mono-main'
$pkgs='ai','agent','coding-agent','tui','web-ui','mom','pods'
foreach($p in $pkgs){
  $src=Join-Path $root "packages\$p"
  $files=rg --files $src | Where-Object { $_ -match '\.(ts|tsx|js|mjs|json|md|sh|css)$' }
  $srcFiles=$files | Where-Object { $_ -match '\\src\\' }
  "${p}: total=$($files.Count) src=$($srcFiles.Count)"
}
```

## Root Build/Test/Release Surface

| Upstream item | Upstream behavior | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| `package.json` workspaces | Builds all packages in order: tui, ai, agent, coding-agent, mom, web-ui, pods. Runs root check, browser smoke, tests, version and publish scripts. | `Tau.slnx`, `scripts/verify-dotnet.ps1`, `scripts/verify-dotnet.sh`, `.github/workflows/tau-ci.yml` | partial | Tau has project-level build/test/smoke and Windows CI now runs no-env smoke plus release artifact/archive build. Release/publish/version parity is not complete. |
| `test.sh` | Temporarily backs up `~/.pi/agent/auth.json`, unsets provider env vars, sets `PI_NO_LOCAL_LLM=1`, runs `npm test`. | `scripts/verify-no-env.ps1 -SkipRestore`, `scripts/invoke-no-env.ps1`, `scripts/verify-dotnet.ps1` and module tests | partial | Tau now has PowerShell child-process no-env isolation for provider/AWS/Azure/HF/Slack/Tau auth/config env vars, plus isolated `TAU_AUTH_FILE` / `TAU_MODELS_FILE` / CodingAgent state paths. It does not move or back up user auth files, so exact auth-backup shell parity remains open. |
| `pi-test.sh` | Runs `packages/coding-agent/src/cli.ts`, supports `--no-env` to unset provider env vars before CLI execution. | `scripts/pi-test.ps1`, `scripts/invoke-no-env.ps1`, `scripts/verify-no-env.ps1`, `dotnet run --project src/Tau.CodingAgent/Tau.CodingAgent.csproj` | partial | Tau now has a PowerShell-first CodingAgent direct wrapper: `pi-test.ps1` runs `Tau.CodingAgent`, supports `--no-env`, isolated Tau state, stdin file redirection and base64 child argument arrays; `verify-no-env.ps1 -RunSmoke` uses it for CodingAgent RPC `get_state`. It is still not an exact Unix shell wrapper. |
| `scripts/check-browser-smoke.mjs` + `scripts/browser-smoke-entry.ts` | Browser smoke for web UI/example. | `scripts/verify-dotnet.ps1 -RunSmoke`, `tests/Tau.WebUi.Tests/WebUiBrowserFlowTests.cs` | partial | Tau has WebUi smoke and browser tests; release/static package browser smoke still needs closure. |
| `scripts/build-binaries.sh` | Builds distributable upstream binaries. | `scripts/build-release-artifacts.ps1`, `scripts/package-release-artifacts.ps1`, `scripts/smoke-release-artifacts.ps1`, `.github/workflows/tau-ci.yml` | partial | Tau now has a release artifact baseline under `artifacts/tau-<rid>/` with `pi`, `tau-ai` / `pi-ai`, `mom`, `pi-pods` and `tau-web-ui` wrappers plus manifest and host-RID smoke. Archive packaging supports `auto|zip|tar.gz`: Windows RIDs produce zip, Linux/macOS RIDs produce tar.gz, archives are extracted into a clean temp directory for structure validation, and host RID archives run executable smoke. `build-release-matrix.ps1` / `package-release-matrix.ps1` now encode the upstream five-platform matrix. `build-release-artifacts.ps1` also copies current `README.md`, `LICENSE` and full `docs/`, while `manifest.json.releasePayload` audits upstream payload items as included, tau-native inline/docs/manifest, missing or not-applicable. Remaining gap is non-host runner executable smoke, examples/Photon/interactive raster assets/external export-html payload parity and release automation. |
| `scripts/release.mjs` | Release/version/publish flow. | `Directory.Build.props`, `scripts/plan-release.ps1`, `scripts/prepare-release.ps1`, `scripts/update-release-version.ps1`, `scripts/update-release-notes.ps1`, `scripts/build-release-artifacts.ps1`, release matrix/package scripts | partial | Tau now has a repo-owned MSBuild `VersionPrefix`, a dry-run release planner, a guarded local preparation script, a narrowly scoped version writeback helper, and a narrowly scoped release notes helper; preparation only writes version and release notes when `-Apply` is explicit and the worktree is clean. Release artifacts record the same version source in `manifest.json` and smoke validates it. Tau still does not implement release commit/tag, publish or push execution automation. |
| `scripts/session-transcripts.ts` | Session transcript utility. | CodingAgent/WebUi export paths | partial | Tau has exports, but script parity/invocation surface is not inventoried as verified. |
| `scripts/profile-coding-agent-node.mjs` | TUI/RPC profiling entry. | no direct Tau script | missing | Need decide Tau profiling/smoke equivalent or non-goal. |
| `scripts/edit-tool-stats.mjs` | Edit tool statistics utility. | no direct Tau script | missing | Need inventory whether this is user-facing or release/dev-only. |
| `scripts/cost.ts` | Cost utility. | `Tau.Ai.Registry.ModelCatalog.CalculateCost` | partial | Library cost exists; script/CLI utility parity not verified. |
| `scripts/sync-versions.js` | Workspace package version sync. | no direct Tau equivalent | partial | Likely replaced by .NET packaging version flow, but needs Phase 5 decision. |

## AI + Agent Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| AI stream/event abstractions | `packages/ai/src/types.ts`, `stream.ts`, `utils/event-stream.ts` | `src/Tau.Ai/Abstractions/*`, `src/Tau.Ai/Streaming/*`, provider parser tests | ported | Tau has 13 stream-event records and channel-backed `EventStream`; final verification still depends on provider-family e2e. |
| Provider registry and built-ins | `api-registry.ts`, `providers/register-builtins.ts`, provider subpath exports in `packages/ai/package.json` | `src/Tau.Ai/Providers/ProviderRegistry.cs`, `BuiltInProviders.cs`, `StreamFunctions.cs`, `tests/Tau.Ai.Tests/BuiltInProvidersTests.cs` | partial | Tau has lazy registry and major built-ins, but parity must still prove every upstream exported provider/API surface and dynamic registration behavior. |
| Model catalog / generated models | `models.ts`, `models.generated.ts`, `scripts/generate-models` implied by package build | `src/Tau.Ai/Registry/ModelCatalog.cs`, `GeneratedBuiltInModels.g.cs`, `generated-models.seed.json`, `ModelCatalogTests.cs` | partial | Generated seed exists, but current docs only prove Tau-supported model families, not full upstream generated model coverage. |
| OpenAI / Responses / Codex / Azure / Copilot providers | `providers/openai-*.ts`, `azure-openai-responses.ts`, `github-copilot-headers.ts` | `src/Tau.Ai/Providers/OpenAi*`, `tests/Tau.Ai.Tests/OpenAi*`, `AzureOpenAiResponsesProviderTests.cs` | external-e2e-needed | Stub/serialization tests exist; real cloud Responses/Codex/Azure/Copilot behavior remains unverified. |
| Anthropic / Google / Vertex / Gemini CLI / Antigravity / Mistral | `providers/anthropic.ts`, `google*.ts`, `google-vertex.ts`, `mistral.ts` | `src/Tau.Ai/Providers/Anthropic`, `Google`, `Mistral`, corresponding tests | external-e2e-needed | Contract tests cover request and parser behavior; real provider e2e and auth refresh/error semantics remain final blockers. |
| Bedrock / AWS auth chain | `bedrock-provider.ts`, `providers/amazon-bedrock.ts` | `src/Tau.Ai/Providers/Bedrock/*`, `Bedrock*Tests.cs` | partial | Tau covers SigV4, bearer, shared profile, credential process, ECS/IMDS/web identity, AssumeRole and SSO refresh baseline; OIDC client registration renewal, multi-cache/concurrency and real AWS e2e remain open. |
| OAuth providers and credential store | `oauth.ts`, `utils/oauth/*` | `src/Tau.Ai/Auth/OAuth/*`, `OAuthProviderTests.cs`, `OAuthCredentialStoreTests.cs`, `ProviderAuthResolverTests.cs` | partial | Built-in OAuth providers exist for Anthropic, Copilot, Gemini CLI, Antigravity and OpenAI Codex; real OAuth e2e and UX-level refresh/login parity remain open. |
| Env/api key resolution and custom provider config | `env-api-keys.ts`, `api-registry.ts`, provider options | `EnvironmentApiKeyResolver.cs`, `ModelConfigurationStore.cs`, `ModelConfigurationStoreTests.cs` | partial | Tau supports env/literal/command-backed models.json subset and dynamic OpenAI-compatible registration; full upstream runtime config UX and every provider option still need mapping. |
| Secret redaction and runtime log foundation | Upstream package has auth/config surfaces; redaction is Tau hardening surface | `TauSecretRedactor.cs`, `JsonlSecretRedactor.cs`, `JsonlTauLogSink.cs`, redaction tests | ported | Tau has stronger string-value JSONL redaction foundation; remaining work is expanding patterns from real leaked samples and proving cross-module adoption. |
| Agent runtime loop, facade and tool events | `packages/agent/src/agent.ts`, `agent-loop.ts`, `types.ts` | `src/Tau.Agent/Agent.cs`, `Runtime/AgentRuntime.cs`, `Runtime/ToolExecutor.cs`, `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, `Abstractions/AgentEvents.cs`, `tests/Tau.Agent.Tests/AgentFacadeTests.cs`, `AgentRuntimeContractTests.cs`, `AgentPublicApiCompileSampleTests.cs`, queue/trace tests | partial | Tau now has a high-level `Agent` facade baseline plus `agent_end.messages`, `turn_end.message/toolResults`, tool update partial result, schema validation, error-as-tool-result closure, facade/runtime/proxy/event public API compile-sample coverage, high-level facade failure/cancel synthetic assistant message coverage, low-level `AgentRuntime.RunStream(...)` / `EventStream<AgentEvent, ChatMessage[]>` result wrapper coverage, parallel start/update timing parity, low-level assistant stream cancellation terminal coverage, tool cancellation cleanup, parallel sibling cancellation result preservation and `TransformContext` cancellation. Remaining gap has moved to broader facade option pass-through, export-shape decisions and real proxy/e2e validation. |
| Agent stream proxy | `packages/agent/src/proxy.ts` reconstructs partial assistant messages from stripped proxy events via `/api/stream` | `src/Tau.Agent/Proxy/ProxyStreamProvider.cs`, `tests/Tau.Agent.Tests/ProxyStreamProviderTests.cs` | ported | Tau now has a .NET proxy stream provider that posts the upstream request envelope, rebuilds stripped proxy events into Tau stream events, and has request/response tests. Real proxy-server e2e remains unverified. |
| Package/index export shape | `packages/agent/src/index.ts`, `packages/ai/package.json` subpath exports and bins | `Tau.Agent` assembly public API: `Agent`, `AgentRuntime`, tool/event abstractions, proxy provider | partial | Tau public API is .NET-native and not package-export equivalent; facade/runtime/proxy are now exposed from the assembly, but final matrix must still freeze public API samples and decide which TypeScript export names map to Tau-native equivalents. |

### AI File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/ai/src/index.ts` | `src/Tau.Ai/Tau.Ai.csproj`, public `Tau.Ai` namespaces, `tests/Tau.Ai.Tests/AiPublicApiCompileSampleTests.cs` | partial | Core types/providers/registry are public by namespace rather than a TypeScript barrel, and `AiPublicApiCompileSampleTests` now freezes the current .NET consumer surface. Final closure still needs an explicit mapping for upstream re-exports without direct Tau-native equivalents. |
| `packages/ai/package.json` exports / bin `pi-ai` | `Tau.Ai` class library, `src/Tau.Ai.Cli/Tau.Ai.Cli.csproj`, release artifact `bin/pi-ai.cmd`, plus `Tau.CodingAgent` auth commands | partial | `Tau.Ai.Cli` now provides a standalone `tau-ai` baseline for help/list/login, interactive provider selection, Tau default auth store writes, and explicit `--auth-file auth.json` cwd auth compatibility; current release artifact also includes `pi-ai` wrapper alias. Remaining gap is package-manager/global install alias parity plus exact package export/subpath shape. |
| `packages/ai/src/types.ts` | `src/Tau.Ai/Abstractions/Messages.cs`, `ContentBlocks.cs`, `StreamEvents.cs`, `Models.cs`, `Options.cs`, `Tools.cs` | partial | Tau ports message/content/tool/model/usage and 13 stream events as C# records. Upstream `KnownApi` names are not byte-equivalent (`openai-completions` vs Tau `openai-chat-completions`, `google-generative-ai` vs Tau `google-generative-language`), and `onPayload` / `onResponse` callback parity is not frozen. |
| `packages/ai/src/api-registry.ts` | `src/Tau.Ai/Providers/ProviderRegistry.cs`, `BuiltInProviders.cs` | ported | Registry supports lazy factories, source IDs, unregister-by-source and clear semantics. Exact TypeScript `ApiProvider` generic surface is .NET-native. |
| `packages/ai/src/stream.ts` | `src/Tau.Ai/Providers/StreamFunctions.cs`, provider tests | partial | `Stream`, `StreamSimple`, `CompleteAsync`, and `CompleteSimpleAsync` exist and resolve auth/models.json before dispatch. Upstream import-side-effect registration and exact thrown error text/API names still need public API samples. |
| `packages/ai/src/models.ts` | `src/Tau.Ai/Registry/ModelCatalog.cs`, `ModelCatalogTests.cs` | partial | Tau supports provider/model lookup, canonical references, default model selection, cost calculation, service-tier multipliers, `SupportsXhigh`, and model equality. Full upstream model set and default parity remain unproven. |
| `packages/ai/src/models.generated.ts` | `src/Tau.Ai/Registry/GeneratedBuiltInModels.g.cs`, `generated-models.seed.json`, `scripts/generate-tau-ai-models.ps1` | partial | Tau has a generated catalog pipeline for currently supported families. It is not yet proven equivalent to upstream generated model coverage. |
| `packages/ai/src/env-api-keys.ts` | `src/Tau.Ai/Auth/EnvironmentApiKeyResolver.cs`, `ProviderAuthResolver.cs` | partial | Tau maps the main env vars, OAuth-token precedence, GitHub token aliases, Vertex ADC marker, Bedrock ambient credentials and common OpenAI-compatible provider env vars. Browser/Vite dynamic-import behavior is irrelevant in .NET; exact env matrix still needs a dedicated contract table. |
| `packages/ai/src/cli.ts` | `src/Tau.Ai.Cli/AiCliRunner.cs`, `src/Tau.Ai.Cli/Program.cs`, `tests/Tau.Ai.Tests/AiCliRunnerTests.cs` | partial | Tau now has a standalone AI CLI baseline: help/list/login, interactive provider selection, unknown-provider errors, OAuth credential save, default Tau auth store, and explicit `--auth-file auth.json` for upstream cwd auth compatibility. Remaining gap is real OAuth e2e and final release/bin name packaging. |
| `packages/ai/src/oauth.ts` | `src/Tau.Ai/Auth/OAuth/*`, `ProviderAuthResolver.cs`, `OAuthCredentialStore.cs` | partial | OAuth registry/store/providers exist and integrate with auth resolution. Exact top-level export shape and real provider login/refresh e2e remain open. |
| `packages/ai/src/bedrock-provider.ts` | `src/Tau.Ai/Providers/Bedrock/*`, `BuiltInProviders.cs` | partial | Tau loads Bedrock directly as a built-in provider and has injectable seams/tests. Upstream node-only subpath export wrapper is not package-shape equivalent. |
| `packages/ai/src/utils/event-stream.ts` | `src/Tau.Ai/Streaming/EventStream.cs`, `AssistantMessageStream.cs` | ported | Channel-backed push/pull stream and final result semantics exist. Final acceptance still depends on every provider emitting the same terminal event contract. |
| `packages/ai/src/utils/json-parse.ts` | `src/Tau.Ai/Utilities/StreamingJsonParser.cs`, helper tests, public API sample; provider-specific parser/accumulator code under `src/Tau.Ai/Providers/**` | partial | Tau now exposes `StreamingJsonParser.ParseStreamingJson(...)`, calibrated against upstream `partial-json@0.1.7` for complete JSON, incomplete object/array/string/literal recovery, invalid/empty fallback `{}`, incomplete decimal drop and incomplete exponent base recovery. OpenAI-style, OpenAI Responses shared, Anthropic, Mistral and Bedrock streaming tool-call arguments now use the helper baseline for object raw-text partial/final arguments. Remaining gaps are exact TypeScript export naming, edge audit across future providers and real provider e2e. |
| `packages/ai/src/utils/sanitize-unicode.ts` | `src/Tau.Ai/Security/UnicodeTextSanitizer.cs`, request sanitizer tests | ported | Request builders remove unpaired surrogates across OpenAI, Responses, Anthropic, Google, Vertex, Gemini CLI, Mistral and Bedrock tests. |
| `packages/ai/src/utils/overflow.ts` | `src/Tau.Ai/Utilities/ContextOverflowDetector.cs`, `src/Tau.CodingAgent/Runtime/CodingAgentRetryOptions.cs`, helper/retry tests | partial | Tau now exposes an AI-level context overflow detector with upstream provider error patterns, non-overflow exclusions and silent usage overflow detection; CodingAgent retry/overflow classification reuses it. Remaining gap is exact public TypeScript helper shape and provider e2e evidence. |
| `packages/ai/src/utils/typebox-helpers.ts` | `src/Tau.Ai/Utilities/JsonSchemaHelpers.cs`, helper tests | partial | Tau now has a .NET-native `JsonSchemaHelpers.StringEnum(...)` helper that emits provider-compatible `{ type: "string", enum: [...] }` JSON schema. Full TypeBox export parity remains a non-.NET-shape decision. |
| `packages/ai/src/utils/validation.ts` | `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, `tests/Tau.Ai.Tests/ToolArgumentValidatorTests.cs`, Agent tool tests | partial | Tau now exposes `ToolArgumentValidator.ValidateToolCall(...)` / `ValidateToolArguments(...)` as an AI-level public helper and Agent reuses it for schema/error-result behavior. It covers the subset Tau currently consumes: object/array/required/properties/items/type coercion/enum/anyOf. Full AJV/TypeBox keyword and CSP fallback parity remains open. |
| `packages/ai/src/utils/headers.ts` | `src/Tau.Ai/Utilities/AiHeaderUtilities.cs`, helper tests | partial | Tau now exposes `AiHeaderUtilities.ToDictionary(...)` for `HttpHeaders` / `HttpResponseMessage`, covering the .NET equivalent of `headersToRecord`; provider-local header application remains separate. |
| `packages/ai/src/utils/hash.ts` | `src/Tau.Ai/Utilities/ShortHash.cs`, helper tests | partial | Tau now exposes upstream-compatible `ShortHash.Compute(...)` with known output parity; existing provider-specific internal hashes remain behavior-specific until separately reviewed. |
| `packages/ai/src/utils/oauth/types.ts` | `src/Tau.Ai/Auth/OAuth/OAuthCredentials.cs`, `IOAuthProvider.cs`, `OAuthLoginCallbacks.cs` | partial | Core credential/callback/provider contracts exist, but are .NET-native and not export-shape equivalent. |
| `packages/ai/src/utils/oauth/pkce.ts` | `src/Tau.Ai/Auth/OAuth/OAuthPkce.cs`, `OAuthProviderTests.cs` | ported | PKCE verifier/challenge generation has unit coverage. |
| `packages/ai/src/utils/oauth/oauth-page.ts` | `src/Tau.Ai/Auth/OAuth/OAuthCallbackServer.cs` and provider callback responses | partial | Local callback flow exists, but exact upstream success/error HTML template parity is not frozen. |
| `packages/ai/src/utils/oauth/index.ts` | `src/Tau.Ai/Auth/OAuth/OAuthProviderRegistry.cs`, `BuiltInOAuthProviders.cs` | partial | Built-in registry covers Anthropic, GitHub Copilot, Gemini CLI, Antigravity and OpenAI Codex. Custom register/unregister/reset API shape is .NET-native; real e2e remains open. |
| `packages/ai/src/utils/oauth/anthropic.ts` | `src/Tau.Ai/Auth/OAuth/Providers/AnthropicOAuthProvider.cs`, tests | external-e2e-needed | Local authorization-code/PKCE and refresh baseline exists in code/tests; real Claude OAuth e2e and browser callback UX are not verified. |
| `packages/ai/src/utils/oauth/github-copilot.ts` | `src/Tau.Ai/Auth/OAuth/Providers/GitHubCopilotOAuthProvider.cs`, `src/Tau.Ai/Providers/GitHubCopilotHeaders.cs` | external-e2e-needed | Device-flow/token/header/model modification seams exist; enterprise/domain and real Copilot e2e need validation. |
| `packages/ai/src/utils/oauth/google-gemini-cli.ts` | `src/Tau.Ai/Auth/OAuth/Providers/GeminiCliOAuthProvider.cs`, `GoogleProjectDiscovery.cs` | external-e2e-needed | OAuth + project discovery baseline exists; real Google Cloud Code Assist login/refresh e2e remains open. |
| `packages/ai/src/utils/oauth/google-antigravity.ts` | `src/Tau.Ai/Auth/OAuth/Providers/AntigravityOAuthProvider.cs`, `GoogleProjectDiscovery.cs` | external-e2e-needed | Separate Antigravity credential flow exists; real Antigravity auth/model e2e remains open. |
| `packages/ai/src/utils/oauth/openai-codex.ts` | `src/Tau.Ai/Auth/OAuth/Providers/OpenAICodexOAuthProvider.cs`, `OpenAiCodexResponsesProvider.cs` | external-e2e-needed | OAuth/token parsing/refresh baseline exists and Codex Responses consumes account headers. Real ChatGPT/Codex OAuth e2e remains open. |
| `packages/ai/src/providers/register-builtins.ts` | `src/Tau.Ai/Providers/BuiltInProviders.cs` | partial | Lazy registration exists for Tau's built-ins plus models.json dynamic OpenAI-compatible providers. Exact upstream built-in API names and `setBedrockProviderModule` public hook are not fully equivalent. |
| `packages/ai/src/providers/simple-options.ts` | `src/Tau.Ai/Abstractions/Options.cs`, provider-specific option mapping | partial | Simple reasoning/max-token mapping exists across providers, but no single shared helper equivalent or complete `thinkingBudgets` surface is frozen. |
| `packages/ai/src/providers/transform-messages.ts` | provider-specific converters under `src/Tau.Ai/Providers/*/*MessageConverter.cs` and `OpenAiResponsesShared.cs` | partial | Conversion behavior is implemented per provider. Need dedicated cross-provider transform contract tests before marking equivalent. |
| `packages/ai/src/providers/openai-completions.ts` | `src/Tau.Ai/Providers/OpenAi/OpenAiProvider.cs`, `OpenAiCompat/OpenAiCompatibleProvider.cs`, OpenAI tests | partial | Chat-completions/OpenAI-compatible behavior has serialization and provider tests. Upstream API string is `openai-completions`; Tau currently registers `openai-chat-completions`, so exact config/model compatibility remains a closure item. |
| `packages/ai/src/providers/openai-responses.ts`, `openai-responses-shared.ts` | `src/Tau.Ai/Providers/OpenAiResponses/OpenAiResponsesProvider.cs`, `OpenAiResponsesShared.cs`, tests | external-e2e-needed | Responses SSE, tool calls, service tier, Copilot dynamic headers and image tool-result behavior have stub tests. Real OpenAI/Copilot Responses e2e remains open. |
| `packages/ai/src/providers/openai-codex-responses.ts` | `src/Tau.Ai/Providers/OpenAiResponses/OpenAiCodexResponsesProvider.cs`, `CodexWebSocketTransport.cs`, tests | external-e2e-needed | SSE/WebSocket/auto transport, account headers and fallback paths have fake transport tests. Real Codex Responses e2e remains open. |
| `packages/ai/src/providers/azure-openai-responses.ts` | `src/Tau.Ai/Providers/OpenAiResponses/AzureOpenAiResponsesProvider.cs`, tests | external-e2e-needed | Azure base URL/resource/deployment/api-version/api-key behavior has tests. Real Azure OpenAI e2e remains open. |
| `packages/ai/src/providers/anthropic.ts` | `src/Tau.Ai/Providers/Anthropic/*`, Anthropic/provider sanitizer tests | external-e2e-needed | Messages API stream parser, OAuth/API-key headers, cache/thinking/tool conversion are partly covered by tests. Real Anthropic e2e and exact cache/Claude Code identity semantics remain open. |
| `packages/ai/src/providers/google.ts`, `google-shared.ts` | `src/Tau.Ai/Providers/Google/GoogleProvider.cs`, `GoogleMessageConverter.cs`, `GoogleStreamParser.cs` | external-e2e-needed | Gemini API conversion/parser behavior has tests, but upstream helper-level behavior and exact `google-generative-ai` naming parity remain open. |
| `packages/ai/src/providers/google-gemini-cli.ts` | `src/Tau.Ai/Providers/Google/GoogleGeminiCliProvider.cs`, tests | external-e2e-needed | Cloud Code Assist headers, Antigravity fallback, retry delay extraction and empty SSE retry have tests. Real Google/Gemini CLI provider e2e remains open. |
| `packages/ai/src/providers/google-vertex.ts` | `src/Tau.Ai/Providers/Google/GoogleVertexProvider.cs`, `GoogleVertexAccessTokenResolver.cs`, tests | external-e2e-needed | API key, service-account JWT bearer and authorized-user refresh token paths have tests. Real Vertex e2e remains open. |
| `packages/ai/src/providers/mistral.ts` | `src/Tau.Ai/Providers/Mistral/MistralProvider.cs`, tests | external-e2e-needed | x-affinity, tool-call id normalization, usage and request serialization have tests. Real Mistral e2e remains open. |
| `packages/ai/src/providers/amazon-bedrock.ts` | `src/Tau.Ai/Providers/Bedrock/*`, Bedrock tests | external-e2e-needed | Tau covers bearer/SigV4, shared profile, credential process, ECS/IMDS/web identity, AssumeRole, SSO cache refresh/registration and eventstream parsing with tests. Real AWS Bedrock e2e and cache concurrency/edge behavior remain open. |
| `packages/ai/src/providers/github-copilot-headers.ts` | `src/Tau.Ai/Providers/GitHubCopilotHeaders.cs`, OpenAI Responses tests | ported | Static and dynamic Copilot headers plus vision request detection have tests. |
| `packages/ai/src/providers/faux.ts` | `src/Tau.Ai/Providers/Faux/FauxProvider.cs`, `tests/Tau.Ai.Tests/FauxProviderTests.cs` | ported | Tau now exposes a public .NET-native Faux provider helper with registration/unregistration, queued/factory responses, text/thinking/tool-call helpers, usage/cache estimates, streamed deltas, terminal error/aborted messages and public API sample coverage. Exact upstream option callbacks/signals remain tracked under shared stream options rather than this provider row. |

### AI Command / API / Env / Config / Log / Schema Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package exports and subpaths | `packages/ai/package.json` exports `.`, `./anthropic`, `./azure-openai-responses`, `./google`, `./google-gemini-cli`, `./google-vertex`, `./mistral`, `./openai-codex-responses`, `./openai-completions`, `./openai-responses`, `./oauth`, `./bedrock-provider`; `src/index.ts` re-exports TypeBox, registry, env, models, providers, stream, types, event stream, json parse, overflow, typebox helpers and validation. | `src/Tau.Ai` public namespaces and records; `src/Tau.Ai.Cli`; no subpath/package barrel concept in .NET; `AiPublicApiCompileSampleTests`; `Tau.Ai.Providers.Faux`; `StreamingJsonParser` | partial | Core public types/providers now have a .NET public API compile-sample covering messages/content/tools/models/options/usage, registry, stream functions/events, model helpers, auth/OAuth entry points, validation, helper utilities, streaming JSON parser and the public Faux provider. Standalone CLI baseline exists for help/list/login. Final closure still needs TypeBox re-export and exact TypeScript export/subpath shape decisions. |
| Standalone AI CLI | `packages/ai/package.json` bin `pi-ai`; `src/cli.ts` supports `login [provider]`, interactive provider selection and `list`, writing cwd `auth.json`. | `src/Tau.Ai.Cli` executable baseline; release artifact `bin/tau-ai.cmd` and `bin/pi-ai.cmd`; `OAuthCredentialStore` writes `TAU_AUTH_FILE`, `./.tau/auth.json`, or `~/.tau/auth.json`; `--auth-file auth.json` writes cwd-compatible auth explicitly. | partial | `tau-ai` help/list/login, provider selection, provider-list output, unknown-provider errors and credential save behavior are covered by tests and CLI smoke; release artifact smoke now verifies both `tau-ai list` and `pi-ai list`. Remaining gap is package/global install alias parity, exact upstream wording if required, and real OAuth e2e. |
| Built-in API names | `types.ts` `KnownApi` and `register-builtins.ts`: `anthropic-messages`, `openai-completions`, `mistral-conversations`, `openai-responses`, `azure-openai-responses`, `openai-codex-responses`, `google-generative-ai`, `google-gemini-cli`, `google-vertex`, `bedrock-converse-stream`. | `BuiltInProviders.cs`: `anthropic-messages`, `openai-chat-completions`, `mistral-conversations`, `openai-responses`, `azure-openai-responses`, `openai-codex-responses`, `google-generative-language`, `google-gemini-cli`, `google-vertex`, `bedrock-converse-stream`; `ModelConfigurationStore` maps `openai-completions` / `openai-compatible` to `openai-chat-completions`. | partial | All families have Tau providers, but exact API string parity diverges for OpenAI chat-completions and Google Gemini; config/model compatibility needs a final adapter or documented non-goal. |
| Known provider ids and env keys | `types.ts` `KnownProvider`; `env-api-keys.ts` maps OpenAI, Azure, Google/Gemini, GitHub Copilot aliases, Anthropic OAuth/API key precedence, Vertex ADC, Bedrock ambient AWS, Groq, Cerebras, xAI, OpenRouter, Vercel AI Gateway, z.ai, Mistral, Minimax, HF, opencode, kimi. | `EnvironmentApiKeyResolver.cs` maps the same core provider ids and env vars, plus `GOOGLE_API_KEY` fallback for `google`; Bedrock/Vertex return `<authenticated>` markers for ambient credentials. | partial | Main env matrix is covered, but final parity still needs focused env contract tests covering every provider id, precedence, authenticated marker behavior and the OpenAI/Google API-name divergence. |
| Shared stream options | `types.ts` `StreamOptions`: `temperature`, `maxTokens`, `signal`, `apiKey`, `transport`, `cacheRetention`, `sessionId`, `onPayload`, `onResponse`, `headers`, `maxRetryDelayMs`, `metadata`; `SimpleStreamOptions` adds `reasoning` and `thinkingBudgets`. | `Options.cs`: `Temperature`, `MaxTokens`, `TopP`, `ApiKey`, `Transport`, `CacheRetention`, `SessionId`, `Headers`, `MaxRetryDelay`, `Metadata`; cancellation is passed separately and `SimpleStreamOptions` has `Reasoning`. | partial | Transport/cache/session/header/metadata basics exist. Missing or intentionally different surfaces: `onPayload`, `onResponse`, `thinkingBudgets`, option-level `signal`, and Tau-only `TopP`; provider-level tests do not yet freeze a single shared options parity contract. |
| Provider-specific options | Provider files export `BedrockOptions`, `AnthropicOptions`, `AzureOpenAIResponsesOptions`, `GoogleOptions`, `GoogleGeminiCliOptions`, `GoogleVertexOptions`, `MistralOptions`, `OpenAICodexResponsesOptions`, `OpenAICompletionsOptions`, `OpenAIResponsesOptions`. | Provider-specific C# option records/classes exist where needed: `BedrockOptions`, `OpenAiResponsesOptions`, `OpenAiCodexResponsesOptions`, `AzureOpenAiResponsesOptions`, plus model compatibility metadata and per-provider request builders. | partial | Important behavior is implemented in provider request builders/tests, but exact public option type parity, thinking budget mapping and option names are not frozen. |
| Auth file and credential schema | `src/cli.ts` reads/writes cwd `auth.json`; OAuth entries use `{ type: "oauth", refresh, access, expires/expiresAt, ...metadata }`; `utils/oauth/index.ts` has registry and refresh/get-api-key helpers. | `OAuthCredentialStore.cs` supports `TAU_AUTH_FILE`, `./.tau/auth.json`, `~/.tau/auth.json`, OAuth and `api_key` entries, metadata filtering, Unix `0600`; `ProviderAuthResolver.cs` resolves explicit/env/auth.json/models.json. | partial | Tau is safer and more configurable, but path semantics differ from upstream `pi-ai` cwd auth file; expired OAuth status text still says UX refresh/login parity is incomplete and real OAuth e2e remains open. |
| Custom model/provider config | Upstream `types.ts` exposes OpenAI-compatible compat/routing metadata through `Model.compat`; generated model registry is source of provider/model data. | `ModelConfigurationStore.cs` reads `TAU_MODELS_FILE`, `./.tau/models.json`, `~/.tau/models.json`, merges provider/model overrides, resolves env/literal/`!command` credentials and dynamically registers explicit OpenAI-compatible providers. | partial | Tau has a concrete models.json runtime config path, but upstream package-level config UX is not equivalently frozen; status checks intentionally do not execute `!command` secrets. |
| Runtime log and inspection hooks | Upstream AI package has `onPayload` / `onResponse` callbacks and provider/OAuth `console.*` diagnostics, but no central JSONL runtime log surface. | `ITauLogSink`, `JsonlTauLogSink`, `TauRuntimeLogContext`, `TauSecretRedactor`; `ProviderAuthResolver.GetStatus` emits `auth/status.checked`; downstream CodingAgent/WebUi/Mom/Pods reuse the sink. | partial | Tau has stronger structured logging and default JSONL string-value redaction. It still lacks upstream-style payload/response callback parity and real provider e2e correlation proof. |
| Tool schema validation | `utils/validation.ts` exports `validateToolCall` / `validateToolArguments` using AJV + TypeBox, with CSP fallback behavior. | `Tool` stores `JsonElement ParameterSchema`; `Tau.Ai.ToolArgumentValidator` exposes public `ValidateToolCall` / `ValidateToolArguments`, and `Tau.Agent` consumes the same helper. | partial | AI-level public helper baseline is now present and tested. Remaining parity gap is full AJV/TypeBox keyword coverage and CSP/runtime-codegen fallback semantics, which do not map 1:1 to .NET. |
| Helper utilities | `utils/json-parse.ts`, `headers.ts`, `hash.ts`, `overflow.ts`, `typebox-helpers.ts`, TypeBox re-export from `index.ts`. | `AiHeaderUtilities`, `ShortHash`, `ContextOverflowDetector`, `JsonSchemaHelpers`, `StreamingJsonParser`, provider-local parsers and `JsonElement` schemas. | partial | Tau now has public .NET-native helper baselines for headers, short hash, context overflow, string enum JSON schema and streaming incomplete JSON parsing, covered by `AiUtilityHelpersTests` and the public API sample. Primary streaming tool-argument parser adoption is now present for OpenAI-style, OpenAI Responses shared, Anthropic, Mistral and Bedrock. Still open: full TypeBox re-export parity, exact TypeScript helper naming/export shape decisions, edge audit across future providers and real provider e2e. |
| OAuth registry and built-ins | `utils/oauth/index.ts` exports built-ins, `registerOAuthProvider`, `unregisterOAuthProvider`, `resetOAuthProviders`, `getOAuthProviders`, `refreshOAuthToken`, `getOAuthApiKey`; built-ins cover Anthropic, GitHub Copilot, Gemini CLI, Antigravity and OpenAI Codex. | `OAuthProviderRegistry.cs`, `BuiltInOAuthProviders.cs`, concrete provider classes and `ProviderAuthResolver`; CodingAgent commands call the same resolver/store. | partial | Built-ins exist and tests cover local flows, but public registry API shape is .NET-native; GitHub enterprise/domain, Copilot policy enablement, browser callback UX and all real OAuth e2e remain open. |
| Generated model/provider coverage | `models.generated.ts` includes many known provider ids, including OpenAI-compatible families beyond the provider implementations; package build runs `generate-models`. | `generated-models.seed.json`, `GeneratedBuiltInModels.g.cs`, `generate-tau-ai-models.ps1`, `ModelCatalog.cs`. | partial | Tau generated seed covers currently supported families but is not proven equivalent to upstream generated provider/model set; this remains separate from provider implementation e2e. |
| Error, abort and terminal stream contract | `types.ts` documents that stream functions should encode request/model/runtime failures in returned streams; providers also throw for some `streamSimple` setup errors such as missing API keys. | Tau providers emit `ErrorEvent`, high-level `Agent` facade appends synthetic assistant failure/cancel messages for stream fault and cancellation, `StopReason.Aborted` is mapped through proxy/OpenAI Responses/Mom normalization, low-level assistant stream cancellation emits aborted terminal events instead of faulting `RunStream`, and tool cancellation now emits terminal error tool results with pending-call cleanup. | partial | Core event model, facade failure/cancel synthesis, low-level assistant stream cancellation terminal coverage and tool cancellation cleanup exist. Exact thrown-vs-streamed provider error text, terminal event consistency across provider families and real e2e remain unverified. |

### Agent File-Level Matrix

| Upstream file | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/agent/src/agent.ts` | `src/Tau.Agent/Agent.cs`, `AgentState.cs`, `AgentRuntime.cs`, `tests/Tau.Agent.Tests/AgentFacadeTests.cs` | partial | `Agent` baseline now covers `AgentOptions`, state exposure, `Subscribe`, `PromptAsync`, `ContinueAsync`, steering/follow-up queues, queue clear, `HasQueuedMessages`, `Abort`, `WaitForIdleAsync`, `Reset`, prompt message lifecycle, listener settlement, and synthetic assistant messages for stream fault / cancellation. Still missing full abort signal plumbing parity, session/transport/thinking budget pass-through at facade level and every upstream state mutator edge. |
| `packages/agent/src/agent-loop.ts` | `src/Tau.Agent/Runtime/AgentRuntime.cs`, `ContextTransformer.cs`, `ToolExecutor.cs`, `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, `AgentEvents.cs`, queue/tool trace/contract tests | ported | Tau now has double loop, context transform/convert, sequential/parallel tools, before/after interceptors, runtime log trace, tool-result message lifecycle, `agent_end.messages`, `turn_end.message/toolResults`, tool update partial result, schema validation, error-as-tool-result for prepare/before/execute/after exceptions, high-level facade failure/cancel assistant synthesis, low-level `AgentRuntime.RunStream(...)` / `EventStream<AgentEvent, ChatMessage[]>` wrapper shape, exact parallel start/update timing, low-level assistant-stream cancellation terminal behavior, tool cancellation cleanup, parallel sibling cancellation result preservation and `TransformContext` cancellation parity. Remaining Agent work is now outside the loop contract itself. |
| `packages/agent/src/types.ts` | `AgentLoopConfig.cs`, `AgentState.cs`, `IAgentTool.cs`, `IToolInterceptor.cs`, `AgentEvents.cs`, `AgentQueueMode.cs` | partial | Core .NET records/interfaces exist and `AgentState` now includes system prompt/model/tools plus runtime fields. TypeScript declaration-merge custom message semantics are intentionally .NET-native; the runtime event payload contract is now locally covered, while some export-shape and facade option decisions remain open. |
| `packages/agent/src/proxy.ts` | `src/Tau.Agent/Proxy/ProxyStreamProvider.cs`, `tests/Tau.Agent.Tests/ProxyStreamProviderTests.cs` | ported | Request envelope, bearer header, text/tool/thinking event reconstruction and token-safe error handling have tests. Real proxy-server e2e, abort/malformed SSE/incomplete terminal event coverage remain open. |
| `packages/agent/src/index.ts` | `src/Tau.Agent/Tau.Agent.csproj` public assembly surface | partial | Public assembly now exposes facade/runtime/types/proxy by namespace instead of a TypeScript barrel, with `AgentPublicApiCompileSampleTests` freezing facade/runtime/proxy/event consumption. Final closure still needs decisions for TypeScript exports without Tau-native equivalents. |

## CodingAgent + Tui Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| CLI package/bin/config surface | `packages/coding-agent/src/cli.ts`, `src/config.ts`, `package.json` bin `pi` and `piConfig` | `src/Tau.CodingAgent/Program.cs`, command router/runtime/settings stores | partial | Tau has real CLI/RPC/settings/session paths, but packaging/bin/config-dir parity and all flags still need a command-level map. |
| Coding tools | `src/core/tools/*` | `src/Tau.CodingAgent/Tools/*`, `ReadFileToolTests.cs`, `ListDirectoryToolTests.cs`, tool/export tests | ported | Read/ls/edit/bash/glob/grep/write baseline exists with many upstream details; remaining gaps are custom renderers, image resize/clipboard and full TUI details. |
| JSONL session tree and lifecycle | `src/core/agent-session*`, tree/session/fork/clone/compact modules | `CodingAgentTreeSessionStore.cs`, `CodingAgentSessionSwitchCoordinator.cs`, tree/resume/metadata tests | partial | Broad Tau-native tree/session baseline exists; full upstream TreeSelector, branch-switch hooks, metadata inspector layout, multi-select and exact persisted schema parity remain open. |
| RPC mode | `src/modes/rpc/*` | `CodingAgentRpcHost.cs`, `CodingAgentRpcHostTests.cs` | partial | Tau covers prompt/steer/follow-up/settings/session/tree/bash/export commands; upstream extension UI subprotocol, full settings selector, terminal/packages and richer streamed contracts remain open. |
| Slash commands, prompts, skills and extensions | `src/core/slash-commands.ts`, `prompt-templates.ts`, `skills.ts`, `extensions/*` | `CodingAgentCommandRouter`, `CodingAgentPromptTemplateStore`, `CodingAgentSkillStore`, `CodingAgentExtensionCommandStore`, event bus tests | partial | Declarative command/resource/event baseline exists; upstream TypeScript extension runtime, custom tools, lifecycle events, resource selector and diagnostics parity remain open. |
| Settings/model/theme/thinking/auth selectors | settings/keybindings/theme/auth modules and interactive components | `CodingAgentSettingsStore/Selector`, `ModelSelector`, `ThemeStore/Selector`, `ThinkingSelector`, `AuthSelector`, tests | partial | Many Tau selectors and RPC fields exist; full upstream SettingsList/submenus, package/install settings, terminal/image/markdown edit UI and OAuth dialog/session parity remain open. |
| OAuth login/logout UX | auth storage and OAuth flows under coding-agent + ai | `InteractiveOAuthLoginCallbacks.cs`, `/auth` `/login` `/logout`, `Tau.Ai/Auth/OAuth/*` | partial | Local provider selection/login/logout baseline exists; real OAuth e2e and credential refresh UX still belong to AI/CodingAgent closure. |
| Export/share/rich rendering | `src/core/export-html/*`, share/import helpers | `CodingAgentHtmlSessionExporter.cs`, `CodingAgentShareClient.cs`, HTML/export tests | partial | HTML transcript is rich but not upstream template/vendor parity; real `gh gist` share smoke and Tau share viewer remain unverified. |
| Package manager / install telemetry / changelog | `src/core/package-manager.ts`, `package-manager-cli.ts`, changelog startup behavior | `CodingAgentSettingsStore` has fields; `/changelog` reads Tau release notes | missing | Tau persists some upstream-shaped settings but does not implement package manager CLI, install telemetry flow or startup changelog parity. |
| TUI editor/input/autocomplete/history | `packages/tui/src/editor.ts`, `autocomplete.ts`, `keybindings.ts`, `kill-ring.ts`, `undo-stack.ts` | `InteractiveInputEditor.cs`, `InputHistoryStore.cs`, `TuiAutocompleteProvider.cs`, keybinding/editor tests | partial | Strong editor foundation exists; paste marker, redo/grouped undo, visual autocomplete popup and full editor box parity remain open. |
| TUI components/selectors/render diff | `components/*`, `select-list.ts`, `settings-list.ts`, `tui.ts` | `TuiSelectList`, `TuiMultiSelectList`, `TuiOverlayHost`, `TuiDiffRenderer`, `TuiTranscript*`, tests | partial | Component/render foundations exist and are wired into CodingAgent selectors; no complete upstream TUI app host/focus stack/settings-list parity yet. |
| Terminal host / images / markdown / loaders | `terminal.ts`, `terminal-image.ts`, `components/image.ts`, `markdown.ts`, `loader.ts`, `cancellable-loader.ts` | `SystemConsoleTerminal`, `TuiAnsiRenderSurface`, limited message/status/markdown helpers | missing | Full ProcessTerminal lifecycle, kitty/iTerm image rendering, marked/highlight Markdown component, loader/cancellable-loader and hardware cursor remain open. |

### CodingAgent File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/coding-agent/package.json` | `src/Tau.CodingAgent/Tau.CodingAgent.csproj`, `Tau.slnx`, `scripts/verify-dotnet.ps1` | partial | Tau has a .NET console project and solution build path. Missing final `pi`-compatible packaged command, package export shape, binary asset copy pipeline, theme/export-html/vendor asset packaging and release artifact closure. |
| `src/index.ts` | Public C# namespaces in `src/Tau.CodingAgent/Runtime/**`, `src/Tau.CodingAgent/Tools/**` | partial | Tau exposes concrete stores/runners/selectors/tools by namespace. Need public API compile samples or explicit SDK export mapping for upstream session/auth/model/extension/resource/settings exports. |
| `src/cli.ts`, `src/main.ts`, `src/cli/args.ts` | `Program.cs`, `CodingAgentHost.cs`, `CodingAgentPrintMode.cs`, `CodingAgentRpcHost.cs` | partial | Interactive, print and RPC baselines exist. Top-level parity gaps include `--continue`, `--resume`, `--session`, `--fork`, `--session-dir`, `--api-key`, `--system-prompt`, `--append-system-prompt`, `--tools`, `--models`, `--thinking`, extension/prompt/skill flags, `--export`, `--list-models`, `--offline`, `--verbose`, `--version` and broad help UX. |
| `src/config.ts` | `CodingAgentSettingsStore.cs`, `CodingAgentTreeSessionStore.cs`, `CodingAgentChangelogStore.cs`, `CodingAgentShareClient.cs`, `CodingAgentThemeStore.cs` | partial | Tau has `.tau` defaults and `TAU_CODING_AGENT_*` env vars. Upstream `PI_CODING_AGENT_DIR`, `PI_PACKAGE_DIR`, `PI_SHARE_VIEWER_URL`, `~/.pi/agent/**`, packaged asset paths and `piConfig` identity are not equivalent. |
| `src/bun/cli.ts`, `src/bun/register-bedrock.ts` | .NET host/release path; `Tau.Ai` provider registration | non-goal-proposed | Bun runtime wrapper/register hook should likely be replaced by .NET release artifact/startup behavior. Needs explicit Phase 5 decision before closure. |
| `src/package-manager-cli.ts`, `src/core/package-manager.ts`, `src/cli/config-selector.ts`, `src/utils/git.ts`, `src/utils/tools-manager.ts` | `CodingAgentExtensionCommandStore.cs`, `CodingAgentExtensionResourceState.cs`, `CodingAgentSettingsStore.cs` | missing | Tau can load local declarative extension resources, but has no install/remove/update/list/config package manager, hosted-git source parsing, package source persistence, download manager or package resource selector. |
| `src/migrations.ts`, `packages/coding-agent/scripts/migrate-sessions.sh` | `CodingAgentTreeSessionStore.cs`, `CodingAgentSettingsStore.cs` | partial | Tau reads current flat JSON and JSONL tree formats and compatibility fields. Missing upstream auth/settings/session/extensions migrations, legacy `oauth.json` migration, root JSONL relocation, commands-to-prompts migration, keybinding migration and hooks/tools deprecation warnings. |
| `src/modes/index.ts`, `src/modes/print-mode.ts` | `CodingAgentPrintMode.cs`, `Program.cs`, `CodingAgentPrintModeTests.cs` | ported | Tau has explicit print mode path through `--print` / `-p`. Remaining differences are CLI flag breadth and output mode schema. |
| `src/modes/rpc/*` | `CodingAgentRpcHost.cs`, `CodingAgentRpcHostTests.cs` | partial | Tau handles LF-delimited JSON, prompt/steer/follow-up/abort/new session/state/settings/model/thinking/retry/bash/session/messages/commands/export paths. Missing upstream RPC client helper, exact TypeScript response schema, extension UI request/response protocol and richer streamed bash/events contract. |
| `src/modes/interactive/interactive-mode.ts`, `src/modes/interactive/components/**` | `CodingAgentHost.cs`, `Tau.Tui` interactive/composition/session classes, CodingAgent selectors | partial | Tau has a real interactive loop, transcript composition, selector paths, hotkeys and host state. Missing exact upstream TUI component layout for message/tool/bash/diff/tree/session/settings/oauth/theme/footer/borders/loaders, extension input/editor UI, decorative announcement components and complete focus stack behavior. |
| `src/modes/interactive/theme/**`, `src/modes/interactive/assets/**` | `CodingAgentThemeStore.cs`, `CodingAgentThemeSelector.cs`, `CodingAgentSettingsStore.cs` | partial | Theme discovery/selection exists with tests. Exact upstream theme schema, live watcher, packaged theme/asset path semantics, all color tokens and bitmap asset packaging remain open. |
| `src/core/agent-session*.ts`, `session-manager.ts`, `session-cwd.ts` | `CodingAgentTreeSessionStore.cs`, `CodingAgentSessionStore.cs`, `CodingAgentSessionSwitchCoordinator.cs`, `RuntimeCodingAgentRunner.cs` | partial | Tau has flat snapshot plus JSONL tree, resume/fork/clone/metadata/parent session metadata/runtime sync. Missing upstream session directory encoding, lifecycle extension events, cwd override prompts and byte-equivalent JSONL schema. |
| `src/core/auth-storage.ts`, `model-registry.ts`, `model-resolver.ts`, `defaults.ts`, `cli/list-models.ts` | `Tau.Ai` auth/model catalog, `CodingAgentModelAvailability.cs`, `CodingAgentCommandRouter.cs` | partial | Runtime provider/model/auth selection is rich. Missing exact `~/.pi/agent/auth.json` path, settings API key migration, CLI `--api-key`, `--list-models`, `--models`, glob/fuzzy scope parsing and full upstream model coverage. |
| `src/core/tools/**` | `src/Tau.CodingAgent/Tools/**`, `tests/Tau.CodingAgent.Tests/*ToolTests.cs` | ported | Read/bash/edit/write/grep/find/glob/list and truncation/path-safe behavior are implemented with targeted tests. Remaining gaps live around custom extension tool wrapping/rendering, image/clipboard ingestion and exact mutation queue/display semantics. |
| `src/core/bash-executor.ts` | `CodingAgentShellRunner.cs`, `ShellTool.cs`, RPC bash tests | partial | Cancellable shell runner and RPC bash/abort exist. Missing upstream shell config/streaming/profiling and exact result schema. |
| `src/core/compaction/**` | `RuntimeCodingAgentRunner.CompactAsync`, `SummarizeBranchAsync`, `CodingAgentAutoCompactionOptions.cs`, tree store tests | partial | Manual compaction, auto threshold, context-overflow compact/retry and branch summary entries exist. Missing extension compaction hooks, exact split-turn summarization behavior, cancellation UI and full JSONL schema parity. |
| `src/core/slash-commands.ts`, `prompt-templates.ts`, `skills.ts`, `resource-loader.ts`, `source-info.ts` | `CodingAgentCommandCatalog.cs`, `CodingAgentCommandRouter.cs`, prompt/skill/context stores | partial | Tau has built-in slash catalog, prompt templates, skills and context files. Missing exact upstream command semantics, source metadata/collision model and interactive resource selector. |
| `src/core/extensions/**` | `CodingAgentExtensionCommandStore.cs`, `CodingAgentExtensionEventBus.cs`, `CodingAgentExtensionResourceState.cs` | partial | Declarative JSON extension commands/resources/diagnostics and an in-process event bus exist. Missing TypeScript extension runtime, custom tools, package-loaded extensions, lifecycle runner/wrapper, extension CLI flags and extension UI methods. |
| `src/core/settings-manager.ts`, `keybindings.ts`, `messages.ts`, `system-prompt.ts`, `footer-data-provider.ts`, `output-guard.ts`, `diagnostics.ts`, `timings.ts`, `telemetry.ts`, `exec.ts`, `sdk.ts` | `CodingAgentSettingsStore.cs`, `Tau.Tui` keybindings, `RuntimeCodingAgentRunner.cs`, `CodingAgentFileOperationTracker.cs`, runtime log sink | partial | Tau has key settings, context/system prompt construction, file tracking and runtime logs. Missing full user/project/package settings merge, image/editor/terminal/telemetry fields, footer git/extension data provider, output guard, startup diagnostics/timings, install telemetry and SDK programmatic facade parity. |
| `src/core/export-html/**` | `CodingAgentHtmlSessionExporter.cs`, export/share tests | partial | Tau exports standalone HTML with JSONL/branch outline/rich message rendering. Missing upstream template/vendor files, exact marked/highlight behavior, custom tool renderer contract and real share viewer parity. |
| `src/utils/clipboard*`, `image-*`, `exif-orientation.ts`, `mime.ts`, `photon.ts`, `paths.ts`, `shell.ts`, `child-process.ts`, `sleep.ts`, `frontmatter.ts`, `changelog.ts` | `SystemCodingAgentClipboard.cs`, `ReadFileTool.cs`, `CodingAgentHtmlSessionExporter.cs`, `CodingAgentShellRunner.cs`, prompt/skill parsers, `CodingAgentChangelogStore.cs` | partial | Text clipboard, shell/path/frontmatter-like helpers and `/changelog` exist. Missing native/OSC52/image clipboard matrix, image resize/convert/EXIF/photon pipeline, shared utility API parity and startup changelog/version behavior. |

### CodingAgent CLI / RPC / Env / Config / Log / Schema Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/bin identity | `package.json` `piConfig.name=pi`, `piConfig.configDir=.pi`, bin `pi=dist/cli.js` | `src/Tau.CodingAgent/Tau.CodingAgent.csproj`, `dotnet run --project src/Tau.CodingAgent` | partial | App runs as .NET project/executable. Missing final `pi` command, `.pi` config identity, package export shape and binary asset closure. |
| Top-level CLI modes and flags | `cli/args.ts`, `main.ts` | `Program.cs`, `CodingAgentPrintMode.cs`, `CodingAgentRpcHost.cs` | partial | Interactive/print/RPC exist. Missing `--mode json`, session/model/prompt/tool/resource/offline/verbose/version/help/package manager parity and exact usage text. |
| Session, settings, auth and model config schema | `config.ts`, `auth-storage.ts`, `settings-manager.ts`, `model-registry.ts`, `session-manager.ts` | `.tau` session/settings/auth/model stores plus `TAU_CODING_AGENT_*`, `TAU_AUTH_FILE`, `TAU_MODELS_FILE` | partial | Working Tau-native persistence exists. Upstream `~/.pi/agent/**`, encoded-cwd session dirs, exact settings fields, migrations and schema parity remain open. |
| Slash commands and local resources | `slash-commands.ts`, prompt/skill/extension modules | `CodingAgentCommandCatalog`, router, prompt/skill/extension stores | partial | Tau has broad built-in command baseline and declarative resources. Missing upstream dynamic extension runtime commands, package-installed resources, resource selector and exact built-in command semantics. |
| RPC command/state/bash schema | `modes/rpc/rpc-types.ts`, `rpc-mode.ts`, `bash-executor.ts` | `CodingAgentRpcHost.cs`, `CodingAgentShellRunner.cs`, RPC tests | partial | Core headless commands exist. Missing exact response/event schema, pending/compacting timing, streamed bash output and RPC client helper. |
| RPC extension UI protocol | `RpcExtensionUIRequest` select/confirm/input/editor/notify/status/widget/title/editor-text | No equivalent in `CodingAgentRpcHost.cs`; local extension event bus only | missing | Concrete headless extension parity blocker. |
| Image/attachment ingestion | `@file` processing, clipboard image, resize/convert/EXIF/mime pipeline, image settings | Tau can carry image content in runner/export paths but lacks CLI preprocessing | missing | File/clipboard image ingestion and resize/block/show settings are not ported. |
| Share/export/release assets | `--export`, `/share`, `PI_SHARE_VIEWER_URL`, export templates/vendor files, binary copy assets | `/export`, `/share`, `CodingAgentHtmlSessionExporter`, `CodingAgentShareClient`, `TAU_SHARE_VIEWER_URL` | partial | HTML/JSONL export exists. Missing CLI `--export`, upstream template/vendor parity, real `gh gist` smoke and packaged release assets. |
| Runtime logs and redaction | debug log path, console diagnostics, RPC JSONL stdout | `JsonlTauLogSink`, `TauRuntimeLogContext`, JSONL tree redaction, `TAU_CODING_AGENT_REDACT_SECRETS` | partial | Tau has stronger structured log/redaction foundation. Missing upstream debug.log semantics, exact process event schema and cross-module correlation e2e. |
| External provider/auth/share e2e | real provider env/auth/OAuth, `gh gist` share | `Tau.Ai` providers/auth, CodingAgent selectors/share client | external-e2e-needed | Local contracts exist; real providers/OAuth/Copilot/Codex and GitHub gist smoke remain final blockers. |

### Tui File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/tui/src/index.ts` | `src/Tau.Tui/Tau.Tui.csproj`, `src/Tau.CodingAgent/Program.cs` | partial | Tau exposes a .NET library and CodingAgent consumes it directly. No TS barrel/package export shape or standalone `pi-tui` entry. |
| `utils.ts`, `fuzzy.ts` | `TuiText.cs`, `TuiFuzzyMatcher.cs`, `TuiComponentTests.cs`, `TuiFuzzyMatcherTests.cs` | ported | Visible width, truncation, wrapping and fuzzy match/filter have direct implementations and tests. |
| `editor-component.ts` | `ITuiComponent.cs`, `ITuiInputComponent.cs`, `IInteractiveRenderer.cs`, `IConsoleKeyReader.cs` | ported | Renderable/input component and renderer/key-reader boundaries exist and are used by editor/overlay/session layers. |
| `keys.ts`, `keybindings.ts` | `EditorAction.cs`, `KeyBindingMap.cs`, `SystemConsoleInputDevices.cs`, keybinding tests | partial | Default and override keybindings exist. Missing full upstream key decode, Kitty protocol state and key release/repeat semantics. |
| `stdin-buffer.ts`, `undo-stack.ts`, `kill-ring.ts` | `InputBuffer.cs`, `InteractiveInputEditor.cs`, editor tests | partial | Draft/commit, undo and kill/yank behavior exist inside editor. No independent same-name stdin buffer/undo stack/kill ring public modules. |
| `autocomplete.ts` | `TuiAutocompleteProvider.cs`, `CodingAgentAutocompleteProviderFactory.cs`, `InteractiveInputEditor.cs` | partial | Slash/path completion is wired into editor. Missing full visual autocomplete popup component contract. |
| `components/text.ts`, `truncated-text.ts`, `box.ts`, `spacer.ts` | `TuiTextBlock.cs`, `TuiBox.cs`, `TuiContainer.cs`, `TuiText.cs` | partial | Text/wrap/truncate/padding/container behavior exists. No independent spacer component parity. |
| `components/editor.ts`, `components/input.ts` | `InteractiveInputEditor.cs`, `SystemConsoleInteractiveRenderer.cs`, `InputHistoryStore.cs` | partial | History, reverse search, kill/yank and multiline are implemented. Not a literal upstream editor/input widget port. |
| `components/select-list.ts` | `TuiSelectList.cs`, `TuiSelectorSession.cs`, `TuiOverlayHost.cs`, CodingAgent selectors | ported | Single-select filtering/navigation/overlay and selector wiring have tests. |
| `components/settings-list.ts` | `TuiMultiSelectList.cs`, `TuiMultiSelectSession.cs`, `CodingAgentSettingsSelector.cs`, `CodingAgentScopedModelsSelector.cs` | partial | Multi-select/settings baselines exist. Not full upstream generic settings-list surface. |
| `components/markdown.ts` | `TuiMessageArea.cs`, `TuiStatusBar.cs`, `CodingAgentCompositionMetadataViewer.cs` | partial | Message/status rendering and limited markdown-like text exist. Missing rich markdown component. |
| `terminal.ts` | `ITerminal.cs`, `SystemConsoleTerminal.cs`, `TuiPassiveTerminal.cs`, `SystemConsoleInteractiveRenderer.cs` | external-e2e-needed | Prompt/write/line editing paths exist. ProcessTerminal lifecycle, PTY behavior, hardware cursor and real terminal smoke remain open. |
| `terminal-image.ts`, `components/image.ts`, `components/loader.ts`, `components/cancellable-loader.ts` | no direct Tau.Tui equivalent | missing | No Kitty/iTerm image protocol, image component, spinner/loader or cancellable loader implementation. |
| `tui.ts` | `TuiCompositionHost.cs`, `TuiCompositionSession.cs`, `TuiTranscriptViewport*.cs`, `TuiOverlayHost.cs`, selector/multi-select sessions | partial | Composition/transcript/overlay runtime exists and is reused by CodingAgent. Missing complete generic TUI app host, focus stack, hardware cursor and terminal lifecycle closure. |

### Tui Terminal / Input / Render / Theme Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Terminal/input shell | `terminal.ts`, `keys.ts`, `stdin-buffer.ts` | `ITerminal`, `SystemConsoleTerminal`, `TuiPassiveTerminal`, key reader/renderer/input buffer | external-e2e-needed | Production path is wired, but live TTY/PTY/cursor behavior needs real smoke. |
| Editor/history/autocomplete | `components/editor.ts`, `components/input.ts`, `autocomplete.ts`, `undo-stack.ts`, `kill-ring.ts` | `InteractiveInputEditor`, `InputHistoryStore`, `TuiAutocompleteProvider`, `KeyBindingMap` | partial | Behavior is strong; missing visual popup and full widget semantics. |
| Render primitives/viewport | `utils.ts`, `fuzzy.ts`, `components/text.ts`, `box.ts`, `tui.ts` | `TuiText`, `TuiTextBlock`, `TuiBox`, `TuiContainer`, `TuiFuzzyMatcher`, `TuiDiffRenderer`, `TuiAnsiRenderSurface`, transcript viewport | ported | Text width/wrap/diff/status anchoring/viewport resize/overlay composition have tests. |
| Selectors/overlay composition | `select-list.ts`, `settings-list.ts`, `tui.ts` | `TuiSelectList`, `TuiMultiSelectList`, selector sessions, `TuiOverlayHost`, `TuiCompositionSession`, CodingAgent selectors | partial | Single/multi-select and overlay runtime work. Missing full generic app host/focus stack. |
| Theme surface | component theme props and settings-list theme | `TuiSelectListTheme`, `TuiMultiSelectListTheme`, CodingAgent theme store/selector | partial | Tau has local selector theme plus CodingAgent JSON theme discovery. No library-wide equivalent token system or all-component theme parity. |
| Markdown/image/loader extras | `markdown.ts`, `image.ts`, `loader.ts`, `cancellable-loader.ts`, `terminal-image.ts` | `TuiMessageArea`, `TuiStatusBar`, text renderer only | missing | Rich markdown, terminal image and loader/cancellable-loader remain absent. |

## WebUi Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/component export | `packages/web-ui/package.json` exports `.` and `./app.css`; `src/index.ts`, `ChatPanel.ts`, web components | `src/Tau.WebUi` ASP.NET Core app with embedded `WebUiPage` | partial | Tau delivers a host app, not a reusable Lit/mini-lit component package with CSS export. Need decide product parity shape or build .NET static/component equivalent. |
| Chat panel, messages, input, streaming | `ChatPanel.ts`, `AgentInterface.ts`, `Messages.ts`, `MessageList.ts`, `Input.ts`, `StreamingMessageContainer.ts` | `WebUiPage.cs`, `WebChatService.cs`, `WebUiApplication.cs`, `WebUiBrowserFlowTests.cs` | partial | Tau supports create/send/stream/rename with browser tests; upstream component-level behavior and every UI state remain unmapped. |
| Attachments | `AttachmentTile.ts`, `AttachmentOverlay.ts`, `utils/attachment-utils.ts`, sandbox attachment provider | `WebChatAttachmentDto`, `WebUiPage` attachment picker/preview, service tests | partial | Basic attachment preview/send exists; upstream overlay/runtime attachment provider and richer extraction behavior remain open. |
| Storage/settings/provider keys/custom providers | `storage/*`, IndexedDB backend, settings/provider/custom stores | `WebChatStore.cs`, session JSON under `output/`, auth status/catalog endpoints | partial | Tau persists WebChat DTO sessions and settings per session, but not upstream IndexedDB store model, provider key store or custom provider UI parity. |
| Dialogs | `dialogs/SettingsDialog.ts`, `SessionListDialog.ts`, `ProvidersModelsTab.ts`, `ApiKeyPromptDialog.ts`, `CustomProviderDialog.ts`, `PersistentStorageDialog.ts` | Inline controls in `WebUiPage.cs`, API endpoints/tests | partial | Tau has simpler inline controls and auth status; full dialog workflows, persistent storage UX and API key prompt parity remain open. |
| Tool renderers and artifacts | `tools/renderers/*`, `tools/artifacts/*`, docx/xlsx/pdf/svg/html/image/text artifacts | `WebUiPage` tool cards, `WebChatHtml/Markdown/JsonlExporter`, browser/page tests | partial | Basic tool timeline and markdown rendering exist; document/xlsx/pdf/svg/html/image artifact runtime/renderers and sandbox iframe parity remain open. |
| Sandbox runtime bridge | `components/sandbox/*`, `SandboxedIframe.ts`, runtime message router/bridge/providers | No equivalent sandbox runtime provider layer found in `src/Tau.WebUi` | missing | Needed for artifact/runtime parity unless explicitly declared non-goal. |
| CodingAgent JSONL import/preview | Upstream storage/session semantics plus CodingAgent session tree surface | `CodingAgentJsonlSessionPreviewer.cs`, import endpoints/tests | partial | Tau preview/import returns branch tree audit but persists conservative timeline DTO only; true branch/tree WebUi persistence remains open. |
| Auth/model discovery/proxy utilities | `utils/model-discovery.ts`, `auth-token.ts`, `proxy-utils.ts`, provider key dialog | `GET /api/catalog`, `GET /api/auth/{provider}`, `WebUiRunnerFactory` | partial | Catalog/auth status exist; upstream proxy/token/provider-key model and login UX remain open. |
| Browser/release/static smoke | package `build/check`, example app and root browser smoke | `tests/Tau.WebUi.Tests/WebUiBrowserFlowTests.cs`, `scripts/verify-dotnet.ps1 -RunSmoke` | external-e2e-needed | Local app browser flow exists; release/static output smoke and component package build parity remain open. |

### WebUi File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `package.json`, `src/index.ts`, `src/app.css` | `Tau.WebUi.csproj`, `WebUiPage.cs` | partial | Tau is a hosted ASP.NET Core app with embedded page. Missing reusable Lit/mini-lit component package, `.`, `./app.css` exports and CSS build artifact parity. |
| `ChatPanel.ts` | `WebUiPage.cs`, `WebUiApplication.cs` | partial | Tau has a single-page chat shell. Missing upstream component mounting API, artifact side panel, responsive artifact split/overlay and Lit lifecycle. |
| `components/AgentInterface.ts`, `MessageList.ts`, `StreamingMessageContainer.ts`, `Messages.ts`, `ThinkingBlock.ts` | `WebChatService.cs`, `WebUiPage.cs`, browser tests | partial | NDJSON stream covers text/thinking/tool/done and browser tests cover load/create/send/rename. Missing exact Agent subscription semantics, abort UI, cost display and full message union rendering. |
| `MessageEditor.ts`, `AttachmentTile.ts`, `AttachmentOverlay.ts`, `attachment-utils.ts` | `WebChatAttachmentDto`, `WebChatService.BuildUserContent`, page file picker/preview JS | partial | Basic file/image preview and prompt injection exist. Missing rich overlay and full PDF/DOCX/PPTX/XLSX extraction parity. |
| Basic components/dialogs | Inline controls/styles in `WebUiPage.cs`, `/api/catalog`, `/api/sessions*`, `/api/auth/{provider}` | partial | Session/provider/model controls exist. Missing reusable components, Settings/ProvidersModels/ModelSelector/SessionList/PersistentStorage/CustomProvider/API key dialog workflows. |
| `storage/**` | `WebChatStore.cs`, `WebUiOptions.SessionsPath`, `output/webui-sessions.json` | partial | Server-side JSON store supports CRUD/search/clone/clear. Missing IndexedDB backend, transaction/quota/persistence model, provider key/custom provider/settings stores and full session metadata/data schema. |
| `utils/proxy-utils.ts`, `auth-token.ts`, `model-discovery.ts`, `format.ts`, `i18n.ts`, `test-sessions.ts` | server-side runner/auth/catalog, inline JS helpers, WebUi tests | partial | Tau has catalog/auth status and simple formatting/test fixtures. Missing browser CORS proxy/auth token utility, Ollama/llama.cpp/vLLM/LM Studio discovery UI, exported i18n/format utilities and upstream demo corpus. |
| Message/tool renderer registries | inline renderers in `WebUiPage.cs`, `WebChatToolCallDto`, service event mapping | partial | Generic markdown/tool cards exist. Missing pluggable message/tool renderer registries and specialized Bash/Calculate/Default/GetCurrentTime renderers. |
| `tools/extract-document.ts`, `javascript-repl.ts` | attachment extraction only; no JS REPL | partial | Tau injects extracted text/image content. Missing exported extract-document agent tool and sandbox-backed JavaScript REPL. |
| `components/SandboxedIframe.ts`, `components/sandbox/**` | no Tau.WebUi equivalent | missing | Sandbox iframe, runtime message bridge/router and console/file download/attachment/artifact providers are absent. |
| `tools/artifacts/**` | session exporters and generic tool output | partial | HTML/Markdown/JSON/JSONL exports exist. Missing live artifact panel/tool, artifact pill/element/console, Docx/Excel/Pdf/Svg/Html/Image/Markdown/Text/Generic artifact viewers and sandboxed HTML runtime. |
| `prompts/prompts.ts` | no Tau.WebUi prompt constants | missing | Artifact/attachment runtime provider prompt descriptions are not ported. |
| CodingAgent JSONL tree interoperability | `CodingAgentJsonlSessionPreviewer.cs`, preview/import endpoints/tests | partial | Strong preview/audit/filter metadata exists; conservative import linearizes to WebChat DTO and does not persist active branch tree. |
| WebUi-local import/export | `WebChatJsonlExporter/Importer`, HTML/Markdown exporters, endpoint tests | ported | Tau-native linear JSONL/JSON/HTML/Markdown export-import surface exists with redaction and parser errors; not a direct upstream component package surface. |

### WebUi HTTP / Component / Env / Config / Log / Schema / Browser Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Component/package export | `package.json`, `src/index.ts`, `src/app.css` | ASP.NET Core app and public C# services/contracts | partial | Hosted app works; reusable web component/CSS export API parity missing. |
| App shell and message streaming | `ChatPanel`, `AgentInterface`, streaming/message components | `GET /`, `/api/sessions/{id}/messages/stream` NDJSON | partial | Tau streams assistant text/thinking/tool/error/done. Missing component event semantics, abort/cost/UI states. |
| Health/catalog/auth/session HTTP | browser package storage/model utilities | `/healthz`, `/api/status`, `/api/catalog`, `/api/auth/{provider}`, session CRUD/search/import/export endpoints | partial | Tau has useful host HTTP surface. It is not upstream package route parity; browser provider-key/custom-provider workflows remain open. |
| CodingAgent JSONL HTTP | branch/tree session import-preview need | preview and conservative import endpoints | partial | Preview/audit exists; true branch/tree WebUi persistence remains open. |
| Storage schema | IndexedDB stores and `SessionMetadata`/`SessionData` | `WebChatSessionDto`, `WebChatMessageDto`, `WebChatStoreDocument` | partial | Tau schema is simpler and server-side; usage/cost/thinking/full Agent messages/IndexedDB semantics missing. |
| Tool/artifact/runtime surface | renderer registries, artifacts, sandbox iframe/providers, JS REPL | generic tool cards and export surfaces | missing | Live artifact/sandbox runtime layer is absent. |
| Env/config/log/redaction | browser storage/proxy/key settings; package build config | `appsettings.json`, `TAU_PROVIDER`, `TAU_MODEL`, `TAU_WEBUI_REDACT_SECRETS`, Tau.Ai auth/model env, `JsonlTauLogSink` | partial | Server-side config/log/redaction exists. Missing upstream browser settings/proxy/key store and component diagnostics parity. |
| Problem/error schema | component/storage errors | `application/problem+json` for imports/preview, stream `error` events | ported | Stable problem codes/line details are host-specific and tested. |
| Browser/release/static smoke | root browser smoke, package build/check/dev | WebUi browser tests, `verify-dotnet.ps1 -RunSmoke` | external-e2e-needed | Local app browser flow exists; release/static/component package smoke remains unproven. |

## Mom Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/bin/build | `packages/mom/package.json`, `mom` bin, Slack/Web API deps | `src/Tau.Mom/Tau.Mom.csproj`, `Tau.slnx`, `verify-dotnet.ps1` | partial | Worker project exists; npm package/bin/publish artifact parity remains Phase 5 work. |
| Main CLI/config | `src/main.ts`: `--sandbox`, `--download`, `MOM_SLACK_APP_TOKEN`, `MOM_SLACK_BOT_TOKEN` | `Program.cs`, `MomCommandLine.cs`, `appsettings.json`, Mom command tests | partial | Tau supports `--once`, `--validate-sandbox`, `--validate-slack`, `--download`; env/config surface differs and real Slack long-run entry still needs e2e. |
| Delegation runtime | `src/agent.ts`: `AgentSession`, Slack thread updates, usage, compaction, `last_prompt.jsonl` | `RuntimeDelegationAgentRunner.cs`, `Delegation*.cs`, `ChannelPromptDebugStore.cs`, runtime tests | partial | Tau has structured delegation/tool/usage/prompt-debug baseline, not full upstream AgentSession + Slack UI/event rendering + compaction/session tree parity. |
| Slack receive/respond/update/delete/upload | `src/slack.ts`: Socket Mode, mentions/DM, ack, queue, respond/update/delete/upload/backfill/stop | `SlackSocketModeTransport.cs`, `SlackEventMapper.cs`, `SlackWebApiResponder.cs`, queue/processor tests | external-e2e-needed | Fake connector/HTTP contract coverage exists; real Slack token/workspace smoke is required before verified. |
| Slack session sync | Upstream `log.jsonl -> context.jsonl` sync and restart recovery | `ChannelSessionStore.cs`, `ChannelLogStore.cs`, runtime/message processor tests | partial | Tau uses `context.json` Tau-native snapshot and same-workdir carry-over; upstream log-to-context JSONL sync and real Slack thread/session recovery remain open. |
| Store/log/attachment/status | `src/store.ts`, Slack attachment/download/status log behavior | `ChannelLogStore.cs`, `ChannelAttachmentStore.cs`, `ChannelStatusStore.cs`, `SlackAttachmentDownloader.cs` | partial | Tau has log/status/attachment manifests and redaction; schemas are not fully upstream-equivalent. |
| Events | `src/events.ts`: fs watch, immediate/one-shot/periodic, queue limits | `MomEventProcessor.cs`, `MomLocalDelegationFlow.cs`, `Worker.cs`, event tests | partial | Tau scans due events into inbox; not fs.watch + SlackBot.enqueueEvent real-time parity. |
| Download | `src/download.ts`, `main.ts --download` | `SlackChannelHistoryDownloadService.cs`, download tests | external-e2e-needed | HTTP contract tests exist; real Slack history/replies smoke remains open. |
| Sandbox/Docker | `src/sandbox.ts`, `docker.sh`, `dev.sh` | `MomSandbox.cs`, `MomSandboxValidator.cs`, sandbox/tool tests | external-e2e-needed | Host and Docker command construction seams exist; no real Docker container smoke or lifecycle helper parity yet. |
| Tools | `tools/bash.ts`, `read.ts`, `write.ts`, `edit.ts`, `attach.ts`, `truncate.ts` | `MomTools.cs`, `MomToolOutputTruncator.cs`, `MomSandboxAndToolsTests.cs` | ported | Five same-name tools exist with schema/diff/truncation/attachment behavior; Docker execution still gated by sandbox e2e. |
| Operator docs/helpers | `README.md`, `docs/slack-bot-minimal-guide.md`, `docs/sandbox.md`, `docs/events.md`, `scripts/migrate-timestamps.ts` | Root docs/architecture/next/history | partial | Tau lacks Mom-specific operator docs and `migrate-timestamps.ts` equivalent; exploratory docs may become non-goals only after user confirmation. |
| Runtime trace/correlation | Upstream `log.ts` + AgentSession events | `TauRuntimeLogContext`, `JsonlTauLogSink`, runtime delegation tests | partial | Tau emits delegation/response/tool/usage/end and propagates context to runner; not yet a complete cross CodingAgent/Agent/Mom/Pods e2e trace protocol. |

### Mom File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/mom/src/main.ts` | `Program.cs`, `MomCommandLine.cs`, `Worker.cs`, `SlackSocketModeWorker.cs`, `appsettings.json` | partial | `--once`, `--download`, `--validate-sandbox`, `--validate-slack` and config binding exist. Missing exact Node CLI daemon/startup semantics and real Slack long-run e2e. |
| `agent.ts` | `RuntimeDelegationAgentRunner.cs`, `FileDelegationProcessor.cs`, `MomChannelMessageProcessor.cs`, `MomLocalDelegationFlow.cs`, session/log/runtime/model/tools classes | partial | Prompt/context/session/tool-events/outbox/status path is covered by tests. Missing exact upstream AgentSession + Slack UI/event rendering + compaction/session tree parity. |
| `context.ts` | `ChannelSessionStore.cs`, `ChannelLogStore.cs`, `MomRuntimeContext.cs`, `ChannelWorkspaceLayout.cs` | partial | Tau injects `context.json`, `log.jsonl`, workspace memory and skills. Upstream `SettingsManager` workspace settings layer is not mirrored. |
| `events.ts` | `MomEventProcessor.cs`, `MomEventFile.cs`, `MomLocalDelegationFlow.cs`, `Worker.cs` | partial | Immediate/one-shot/periodic/invalid archive/due-key behavior has tests. Missing upstream fs watcher + debounce real-time model. |
| `log.ts` | `ChannelLogStore.cs`, `ChannelStatusStore.cs`, `MomSecretRedaction.cs`, `MomToolOutputTruncator.cs`, `ChannelPromptDebugStore.cs` | partial | Structured `log.jsonl`, `status.json`, usage summary, prompt debug and redaction exist. Console log shape is Tau-native, not byte-equivalent. |
| `sandbox.ts` | `MomSandbox.cs`, `MomSandboxValidator.cs`, `MomTools.cs`, sandbox/tool tests | ported | Host/Docker sandbox parsing, path mapping, Docker validation command construction, timeout/cancel and execution are tested. Real Docker smoke remains external. |
| `slack.ts` | `SlackSocketModeTransport.cs`, `SlackWebApiResponder.cs`, `SlackEventMapper.cs`, `SlackAttachmentDownloader.cs`, `SlackBackfillService.cs`, `SlackSocketModeWorker.cs`, channel processor/queue dispatcher, Slack tests | external-e2e-needed | Socket Mode/Web API/event mapping/backfill/attachment/queue contracts have fake tests. Real Slack workspace/file/history/stop smoke is still required. |
| `store.ts` | `ChannelAttachmentStore.cs`, `ChannelLogStore.cs`, `ChannelStatusStore.cs`, `ChannelSessionStore.cs` | ported | Attachment manifest, channel log, status and session context stores are implemented and reused. Schema/layout are Tau-native. |
| `download.ts` | `SlackChannelHistoryDownloadService.cs`, download tests | external-e2e-needed | History download/thread expansion/Bearer behavior has fake HTTP tests. Real Slack API validation remains open. |
| `tools/*.ts` | `MomTools.cs`, `MomToolOutputTruncator.cs`, sandbox/tool tests | ported | `read/write/edit/bash/attach` and truncation/diff/attachment behavior are implemented and tested. Docker execution still needs real smoke. |
| `packages/mom/scripts/migrate-timestamps.ts` | no direct Tau equivalent | missing | No timestamp migration command/script exists. Needs separate migration boundary or explicit non-goal. |

### Mom Slack / Event / Session / Sandbox / Env / Config / Log / Schema Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Entry / CLI / config bootstrap | `main.ts`, `store.ts`, `sandbox.ts`, `slack.ts`, migration script | `Program.cs`, `MomCommandLine.cs`, `MomOptions.cs`, `Worker.cs`, `SlackSocketModeWorker.cs`, `appsettings.json` | partial | Tau has local once/run/download/validation and config defaults. Upstream Node CLI UX, daemon semantics and config loading details are not 1:1. |
| Slack transport / Web API / Socket Mode / backfill / history | `slack.ts`, `download.ts`, `store.ts` | Slack transport/responder/mapper/backfill/history/download/attachment/queue/processor classes and tests | external-e2e-needed | Fake HTTP/socket/mapper/upload tests exist. Real Slack workspace smoke, event stream, file/history and stop validation remain open. |
| Event file scheduling / queueing | `events.ts` | `MomEventProcessor`, local delegation flow, worker tests | partial | Scheduled event parsing and invalid archive are tested. Missing fs watcher/debounce real-time behavior. |
| Session / context / memory / model restore | `context.ts`, `agent.ts` | `ChannelSessionStore`, `ChannelLogStore`, runtime context, model resolver, runner tests | partial | Tau handles session inheritance, workspace memory, channel history and provider/model carry-over. Upstream workspace settings layer/schema parity remains open. |
| Sandbox / tool delegation / workspace safety | `sandbox.ts`, `tools/*` | `MomSandbox`, `MomSandboxValidator`, `MomTools`, `ChannelWorkspaceLayout`, attachment store | ported | Host/Docker command construction and tools are tested. Actual Docker/container smoke is still external. |
| Log / status / redaction / schema | `log.ts`, `store.ts`, `agent.ts`, `context.ts` | `ChannelLogStore`, `ChannelStatusStore`, `MomSecretRedaction`, request/result/execution/tool event records, prompt debug store | partial | Structured Tau schemas exist and are safer/redacted. Not upstream byte-equivalent layout/console logging. |
| Attachment staging / manifest / download | `store.ts`, `slack.ts`, `tools/attach.ts` | `ChannelAttachmentStore`, `SlackAttachmentDownloader`, Mom attach tool | partial | Local manifest/download/attach path exists; real Slack attachment runtime validation remains open. |

## Pods Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/bin/build artifact | `packages/pods/package.json`: bin `pi-pods`, build copies `models.json` and `scripts` | `src/Tau.Pods/Tau.Pods.csproj`, `Tau.Pods` CLI | partial | Runtime CLI exists, but distributable artifact/script packaging parity remains Phase 5 work. |
| Config, registration and active pod | `src/config.ts`, `commands/pods.ts` list/setup/active/remove | `PodsConfigStore.cs`, `PodsConfigValidator.cs`, `PodsCli.cs`, config/CLI tests | ported | Tau supports init/list/validate/status/active/remove and setup registration baseline with JSON output. |
| SSH/SCP transport | `src/ssh.ts`, use of `sshExec`, `sshExecStream`, `scpFile` | `PodExecService.cs`, `PodSetupService.cs`, lifecycle/model/vLLM services | external-e2e-needed | Process argv hardening/fake runners exist; real SSH/SCP smoke is not yet proven. |
| Setup run | `commands/pods.ts setupPod`: HF/PI token check, copy `pod_setup.sh`, run setup, detect GPUs, save config | `PodSetupPlanner.cs`, `PodSetupService.cs`, setup tests | external-e2e-needed | Tau has plan/run/SCP/GPU-detect contract and token redaction tests; real remote setup/GPU smoke remains open. |
| Known model configs and GPU allocation | `src/models.json`, `model-configs.ts`, `startModel` GPU selection, memory/context overrides | `PodVllmCommandPlanner.cs`, `PodDefinition.Gpus`, vLLM option parsing | partial | Tau plans vLLM commands and records GPUs, but upstream known-model compatibility matrix, round-robin GPU allocation, `--memory` and `--context` behavior are not fully ported. |
| Model lifecycle | `commands/models.ts`: start/stop/list/logs/show known models | `PodModelService.cs`, `PodLifecycleService.cs`, `PodVllmOrchestrationService.cs`, tests | partial | Tau splits HF cache model lifecycle and vLLM deployment lifecycle; upstream direct `start/stop/logs/list` behavior, log streaming and process verification need command-level mapping. |
| vLLM plan/preflight/deploy/status/health/stop | Upstream model run scripts and SSH commands | `PodVllmCommandPlanner.cs`, `PodVllmOrchestrationService.cs`, vLLM tests | external-e2e-needed | Tau has richer systemd/nohup/health/rollback baseline and many fake-runner tests; no real HF/GPU/vLLM startup smoke yet. |
| Prompt against pod model | `commands/prompt.ts` builds local coding-agent args but throws `Not implemented` | No Tau `pods prompt` equivalent found | missing | Upstream is itself placeholder, but final matrix still needs a decision: port usable prompt path, map to CodingAgent base URL, or mark user-confirmed non-goal. |
| Runtime logs/correlation | Upstream mostly console/process oriented | `ITauLogSink` integration in probe/lifecycle/model/vLLM services | partial | Tau emits structured operation summaries; correlation through real remote e2e and release smoke remains unverified. |
| Root release/scripts parity | `packages/pods/scripts`, root `build-binaries.sh` / release | `scripts/verify-dotnet.ps1`, no complete release scripts | missing | Final 100% requires real Tau executable artifact and install/release chain. |

### Pods File-Level Matrix

| Upstream file | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/pods/src/index.ts` | `src/Tau.Pods/Program.cs`, `src/Tau.Pods/Cli/PodsCli.cs` | partial | Tau has a .NET CLI entry and wider command set. Upstream `pi-pods` bin shape/package export remains Phase 5 release parity work. |
| `packages/pods/src/cli.ts` | `src/Tau.Pods/Cli/PodsCli.cs`, `tests/Tau.Pods.Tests/PodsCliTests.cs` | partial | Tau covers init/list/validate/status/probe/exec/ssh/health/deploy/stop/restart/logs/deployments/setup/model/vllm commands with JSON variants. Upstream top-level `pi pods`, `shell`, `start`, `stop`, `list`, `logs`, `agent` names are not fully command-compatible yet; `agent` maps to missing prompt path. |
| `packages/pods/src/types.ts` | `src/Tau.Pods/Models/PodDefinition.cs`, `PodsConfig.cs`, `PodModelResults.cs`, `PodLifecycleResults.cs`, `PodSetupResults.cs`, `PodVllmResults.cs` | partial | Tau has richer typed result records and persists `active`, pod SSH endpoint, `modelsPath`, `vllmVersion`, `gpus`. Upstream `pods: Record<string, Pod>` and `models: Record<string, Model>` schema is not byte-equivalent; migration/compat decision remains open. |
| `packages/pods/src/config.ts` | `src/Tau.Pods/Services/PodsConfigStore.cs`, `PodsConfigValidator.cs`, `src/Tau.Pods/Models/PodsConfig.cs` | partial | Tau uses explicit/default `tau.pods.json` and validates config; upstream uses `PI_CONFIG_DIR` and `~/.pi/pods.json`. Need final config path/env compatibility decision. |
| `packages/pods/src/ssh.ts` | `src/Tau.Pods/Services/PodExecService.cs`, `PodSetupService.cs`, lifecycle/model/vLLM services | external-e2e-needed | Tau hardens local argv construction, injected runner failure and cancellation handling; real SSH/SCP smoke remains required before verified. |
| `packages/pods/src/model-configs.ts` | `src/Tau.Pods/Services/PodVllmCommandPlanner.cs`, `src/Tau.Pods/Models/PodDefinition.cs`, no full known-model config port | partial | Tau records GPU detection and plans vLLM commands, but upstream known-model matrix, GPU matching and memory/context transforms are not fully ported. |
| `packages/pods/src/models.json` | no full Tau equivalent; partial behavior in `PodVllmCommandPlanner.cs` / setup metadata | partial | Upstream known-model hardware/env/arg matrix is not frozen into Tau data. This is a high-value Phase 2/3 Pods candidate. |
| `packages/pods/src/commands/pods.ts` | `PodsCli.cs`, `PodSetupPlanner.cs`, `PodSetupService.cs`, `PodsConfigStore.cs`, `PodsConfigValidator.cs` | partial | Tau supports registration (`setup`), setup plan/run, active/remove/list/status and token-redacted setup output. Upstream `pi pods setup` immediately validates `HF_TOKEN`/`PI_API_KEY`, copies `pod_setup.sh`, runs remote setup and saves config; Tau separates registration/plan/run and still needs real remote setup smoke. |
| `packages/pods/src/commands/models.ts` | `PodModelService.cs`, `PodLifecycleService.cs`, `PodVllmCommandPlanner.cs`, `PodVllmOrchestrationService.cs`, `PodsCli.cs` | partial | Tau has HF cache list/pull/remove/status, lifecycle logs/deployments and vLLM orchestration. Upstream `start/stop/list/logs` local config/PID/log tail flow, round-robin GPU allocation, `--memory`, `--context`, `--gpus`, known-model env injection and direct log streaming remain only partially mapped. |
| `packages/pods/src/commands/prompt.ts` | no Tau `pods agent`/`pods prompt` command; possible future mapping to `Tau.CodingAgent` with pod OpenAI-compatible base URL | missing | Upstream implementation currently throws `Not implemented` after building CodingAgent args, but the user-visible `pi agent <name>` command exists. Tau needs either a usable equivalent, explicit missing item, or user-confirmed non-goal. |

### Pods Command / Env / Config / Log Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Top-level command names | `cli.ts`: `pods`, `shell`, `ssh`, `start`, `stop`, `list`, `logs`, `agent` | `PodsCli`: `setup`, `active`, `remove`, `exec/ssh`, `deploy/stop/restart/logs/deployments`, `model`, `vllm` | partial | Tau has broader structured commands but not exact upstream top-level aliases. Need command-compat shim or documented Tau-native divergence. |
| Setup env vars | `commands/pods.ts`, `pod_setup.sh`: `HF_TOKEN`, `PI_API_KEY` | `PodSetupPlanner`, `PodSetupService`: same env names, redacted display/stdout/stderr, config does not store tokens | ported | Contract/fake tests cover token presence and non-persistence; real setup run remains `external-e2e-needed`. |
| Config path/env | `config.ts`: `PI_CONFIG_DIR` -> `~/.pi/pods.json` | `PodsCli`: default `tau.pods.json`, `--config path`, positional config path | partial | Tau favors repo/local explicit config; final parity needs `PI_CONFIG_DIR` compatibility or a recorded non-goal decision. |
| Active pod semantics | `Config.active`, `getActivePod()` | `PodsConfig.ActivePodId`, active fallback in model/vLLM/deployments/logs/exec | ported | CLI tests cover active fallback and JSON shape; still not byte-equivalent to upstream config layout. |
| Known model data | `models.json`, `model-configs.ts` | no generated data table; planner/service use generic model id + optional extra args | partial | Missing full model data and GPU selection policy. |
| GPU allocation options | `start --memory --context --gpus --vllm` | `vllm plan/deploy --vllm`, `PodDefinition.Gpus`; no full `--memory/--context/--gpus` mapping | partial | Tau can pass arbitrary vLLM args; upstream convenience flags and round-robin GPU selection remain open. |
| Remote process/log schema | `models.ts`: pid in config, `~/.vllm_logs/<name>.log`, tail streaming and startup detection | `PodLifecycleService`/`PodVllmOrchestrationService`: metadata/service/pid/log files under `~/.tau_pods`, `logs` tail fetch, health/status JSON | partial | Tau has stronger structured status, but upstream log streaming/startup-complete semantics are not fully command-compatible. |
| Runtime logs | upstream mainly console/process output | `ITauLogSink` events for probe/lifecycle/model/vLLM `*.start|end` | partial | Structured Tau summary events exist; real remote e2e correlation remains unverified. |
| Prompt/agent against pod | `prompt.ts` builds `--base-url`, `--model`, `--api-key`, `--api`, `--system-prompt`, then throws `Not implemented` | none | missing | Keep explicit so Phase 1 does not lose a user-visible command just because upstream body is incomplete. |

## Root Script / Manifest File Matrix

| Upstream file/script | Upstream behavior | Tau target | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| `package.json` | Workspace build/check/test/version/publish/release scripts and examples workspace graph. | `Tau.slnx`, project files, `scripts/verify-dotnet.ps1`, `scripts/verify-dotnet.sh` | partial | Build/test gates exist; version/publish/release artifact parity remains missing. |
| `test.sh` | Backs up `~/.pi/agent/auth.json`, unsets provider/AWS/HF env vars, sets `PI_NO_LOCAL_LLM=1`, runs `npm test`. | `scripts/verify-no-env.ps1`, `scripts/invoke-no-env.ps1`, `scripts/verify-dotnet.ps1 -SkipRestore`, tests | partial | Tau now has no-env child-process isolation and temp Tau auth/config/session paths, verified through `verify-no-env.ps1 -SkipRestore`; exact move/restore auth backup behavior remains open by design because Tau points auth files at temp state instead. |
| `pi-test.sh` | Runs `packages/coding-agent/src/cli.ts`; `--no-env` unsets provider/AWS/Azure env vars. | `scripts/pi-test.ps1`, `scripts/invoke-no-env.ps1`, `scripts/verify-no-env.ps1`, `dotnet run --project src/Tau.CodingAgent/Tau.CodingAgent.csproj` | partial | Tau now has a PowerShell-first direct CodingAgent wrapper with `--no-env`, isolated state, `--input-file` stdin redirection and base64 child argument support; `verify-no-env.ps1 -RunSmoke` routes CodingAgent RPC smoke through it. Exact Unix shell wrapper remains open. |
| `scripts/check-browser-smoke.mjs` / `scripts/browser-smoke-entry.ts` | Browser smoke for upstream web UI/example bundle. | `tests/Tau.WebUi.Tests/WebUiBrowserFlowTests.cs`, `verify-dotnet.ps1 -RunSmoke` | partial | Tau has local browser/service smoke; release/static artifact smoke still open. |
| `scripts/build-binaries.sh` | Builds cross-platform `pi` archives using Bun compile and copies theme/assets/export-html/docs/examples. | `scripts/build-release-artifacts.ps1`, `scripts/package-release-artifacts.ps1`, `scripts/smoke-release-artifacts.ps1`, `.github/workflows/tau-ci.yml` | partial | Tau now publishes RID-specific executable artifacts for CodingAgent, AI CLI, Mom, Pods and WebUi with wrapper commands and manifest; host RID smoke verifies core commands in `artifacts/tau-win-x64`; archive packaging now supports zip for Windows and tar.gz for Linux/macOS, verifies clean extraction, and the matrix scripts cover `osx-arm64`, `osx-x64`, `linux-x64`, `linux-arm64`, and `win-x64`. The artifact now copies `README.md`, `LICENSE` and complete current `docs/`, and `releasePayload` explicitly audits `changelog`, `package-json`, `theme`, `export-html`, `examples`, Photon wasm, interactive assets and koffi native module status. Windows CI uploads the verified Windows zip and also runs tar.gz format structure smoke against the same host artifact. Non-host runner executable smoke, examples/Photon/interactive raster assets/external export-html payload parity and release automation remain open. |
| `scripts/release.mjs` | Clean worktree check, version bump/sync, changelog release sections, commit/tag, npm publish, push. | `Directory.Build.props`, `scripts/plan-release.ps1`, `scripts/prepare-release.ps1`, `scripts/update-release-version.ps1`, `scripts/update-release-notes.ps1`, release artifact/archive scripts | partial | Tau has executable artifact build/smoke, a repo-owned `VersionPrefix`, a dry-run release plan script, a guarded local preparation script, a version writeback helper, and a release notes helper. Preparation can write only version + release notes under explicit `-Apply`; it intentionally does not commit, tag, publish or push, so full release execution automation remains open. |
| `scripts/sync-versions.js` | Synchronizes workspace package versions. | `Directory.Build.props`, `scripts/update-release-version.ps1` | partial | Tau has one solution-wide MSBuild version source instead of package-workspace lockstep. The helper can update that source, but there is not yet a full release execution flow or NuGet/package publish sync decision. |
| `scripts/session-transcripts.ts` | Utility over upstream session transcripts. | CodingAgent/WebUi export/import commands | partial | Tau has JSON/JSONL/HTML exports, but script invocation and transcript utility parity are not frozen. |
| `scripts/profile-coding-agent-node.mjs` | TUI/RPC startup profiling, `PI_CODING_AGENT_DIR`, `PI_STARTUP_BENCHMARK`, offline env. | no Tau profiler/smoke script | missing | Candidate for Tau startup benchmark or non-goal decision. |
| `scripts/edit-tool-stats.mjs` | Edit tool statistics utility. | no Tau equivalent | missing | Need decide whether developer utility is in-scope or non-goal. |
| `scripts/cost.ts` | Reads session cost data under `~/.pi/agent/sessions`. | `ModelCatalog.CalculateCost`, CodingAgent session stats | partial | Library/session stats exist; standalone cost utility parity missing. |
| `packages/ai/scripts/generate-models.ts` | Generates upstream AI model catalog. | `src/Tau.Ai/Registry/generate-tau-ai-models.ps1`, `generated-models.seed.json`, `GeneratedBuiltInModels.g.cs` | partial | Tau has generation pipeline for supported families; full upstream generated model parity remains open. |
| `packages/ai/scripts/generate-test-image.ts` | Generates AI test image fixture. | Tau tests embed fixtures/stub content | missing | Needs decision whether script is test-only non-goal or Tau test utility. |
| `packages/pods/scripts/pod_setup.sh` | Remote setup script: validates HF/API tokens, installs vLLM variants, exports token/env, configures model path. | `PodSetupPlanner`/`PodSetupService` expects compatible remote script contract but does not vendor a Tau script file yet | partial | Tau builds command and can copy custom `--script`; real script packaging and remote smoke remain open. |
| `packages/pods/scripts/model_run.sh` | Remote vLLM wrapper with env and PID/log monitoring. | `PodVllmOrchestrationService` builds systemd/nohup service directly | partial | Tau intentionally uses service/nohup orchestration; command-level output and startup-monitor parity still need real smoke/decision. |
| `packages/mom/scripts/migrate-timestamps.ts` | Mom timestamp migration helper. | none | missing | Needs Mom schema migration decision. |
| `packages/coding-agent/scripts/migrate-sessions.sh` | CodingAgent session migration helper. | no direct script; JSONL/session stores handle current schema | missing | Needs session migration parity decision. |

## Phase 1 Normalization Result

- Current upstream package directory check at 2026-05-28 15:12 returned exactly `agent`, `ai`, `coding-agent`, `mom`, `pods`, `tui` and `web-ui`; no unknown upstream package directory remains outside this matrix.
- Capability rows now act as planning summaries only. File-level rows are the ownership/source mapping layer, and surface rows are the public command/API/env/config/log/schema/e2e contract layer.
- Duplicated gaps are intentionally kept only where a high-level row needs navigation context. Phase 2 workers should start from the granular file/surface row or the candidate queue below, not from a summary row alone.
- Status normalization for Phase 2 handoff: `ported` means implemented with local evidence but not final acceptance; `partial` means known behavior or schema gaps remain; `missing` means no meaningful Tau equivalent exists; `external-e2e-needed` remains open until real provider/service/runtime smoke passes; `non-goal-proposed` is not final until the user explicitly accepts it.
- Phase 1 is closed at grouped file-level granularity. Future workers may add finer rows while implementing, but broad package inventory should not be reopened unless upstream package directories or package manifests change.

## Phase 2 Candidate Queue

| Candidate | Owner | Upstream evidence | Tau target | Status source | Validation gate | External e2e? |
| --- | --- | --- | --- | --- | --- | --- |
| Agent loop event payload and schema closure | AgentWorker | `packages/agent/src/agent-loop.ts`, `types.ts`; AI `utils/validation.ts` | `src/Tau.Agent/Runtime/**`, `src/Tau.Agent/Abstractions/**`, `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, targeted `Tau.Agent.Tests` / `Tau.Ai.Tests` | Agent file-level rows: `agent-loop.ts` / `types.ts`; AI schema surface `partial`; Tau Agent contract now has payload/schema/error-result, public API compile-sample, high-level failure/cancel synthesis, low-level EventStream wrapper, parallel timing, low-level assistant stream cancellation, tool cancellation cleanup, parallel sibling result preservation and `TransformContext` cancellation baselines. Agent loop contract itself is now locally closed; AI validation helper baseline is public, with full AJV/TypeBox keyword parity still open. | `dotnet test tests\Tau.Agent.Tests\Tau.Agent.Tests.csproj --filter Agent --no-restore --verbosity minimal` plus full `Tau.Agent.Tests`; `dotnet test tests\Tau.Ai.Tests\Tau.Ai.Tests.csproj --filter ToolArgumentValidatorTests --no-restore --verbosity minimal` | No |
| AI public API/bin and helper export closure | AiWorker | `packages/ai/package.json`, `src/index.ts`, `src/cli.ts`, `utils/*`, `providers/faux.ts` | `src/Tau.Ai/**`, `src/Tau.Ai.Cli/**`, public API compile sample and CLI runner tests | AI file-level and AI surface rows: package exports, `pi-ai`, helper utilities; `AiPublicApiCompileSampleTests` now covers current public surface including public helper baselines, streaming JSON parser and the public Faux provider. Primary provider parser adoption for OpenAI-style, OpenAI Responses shared, Anthropic, Mistral and Bedrock is present. Standalone `tau-ai` CLI baseline is now present. Release/bin alias `pi-ai`, TypeBox re-export, exact TypeScript export/subpath decisions, provider edge audit and real provider e2e remain open. | `dotnet build src\Tau.Ai\Tau.Ai.csproj --no-restore --verbosity minimal`; `dotnet build src\Tau.Ai.Cli\Tau.Ai.Cli.csproj --no-restore --verbosity minimal`; public API compile-sample/helper/Faux/CLI tests; full `Tau.Ai.Tests`; CLI `list` smoke | No |
| AI provider/OAuth real e2e matrix | AiWorker | `packages/ai/src/providers/**`, `utils/oauth/**` | `src/Tau.Ai/Providers/**`, `src/Tau.Ai/Auth/OAuth/**`, e2e notes/history | AI provider rows marked `external-e2e-needed`; External E2E list | Targeted provider tests plus real provider/OAuth smoke commands with redacted env evidence | Yes |
| CodingAgent top-level CLI/session/config parity | CodingAgentWorker | `src/cli.ts`, `src/main.ts`, `src/cli/args.ts`, `src/config.ts`, migrations | `src/Tau.CodingAgent/Program.cs`, settings/session stores, CLI tests | CodingAgent file-level rows and CLI/config surface `partial` | `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter "Cli|Session|Settings" --no-restore --verbosity minimal` | No |
| CodingAgent RPC extension UI and streamed bash parity | CodingAgentWorker | `src/modes/rpc/*`, `src/core/bash-executor.ts`, extension UI request types | `CodingAgentRpcHost.cs`, `CodingAgentShellRunner.cs`, RPC tests | RPC rows and `RPC extension UI protocol` `missing` | `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter Rpc --no-restore --verbosity minimal` | No |
| CodingAgent image and clipboard ingestion | CodingAgentWorker | `src/utils/clipboard*`, `image-*`, `mime.ts`, `photon.ts`, `@file` handling | `src/Tau.CodingAgent/**`, possible `Tau.Tui` editor attachment seam | CodingAgent file-level utils row and image ingestion surface `missing` | Targeted CodingAgent tests for file/clipboard/image content plus guarded desktop clipboard smoke when available | Local desktop smoke |
| Tui terminal host, terminal image, loader and rich markdown | TuiWorker | `packages/tui/src/terminal.ts`, `terminal-image.ts`, `components/image.ts`, `markdown.ts`, `loader.ts`, `cancellable-loader.ts`, `tui.ts` | `src/Tau.Tui/**`, `tests/Tau.Tui.Tests/**` | Tui file-level rows and terminal/input/render surface | `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal`; live TTY/PTY smoke transcript | Local runtime smoke |
| WebUi CodingAgent branch/tree true persistence | WebUiWorker with CodingAgent contract review | `packages/web-ui/src/storage/**`, CodingAgent session semantics | `src/Tau.WebUi/**`, `tests/Tau.WebUi.Tests/**`; consume `CodingAgentTreeSessionStore` contract without changing it unless delegated | WebUi JSONL rows `partial`; WebUi storage schema surface | `dotnet test tests\Tau.WebUi.Tests\Tau.WebUi.Tests.csproj --filter "Jsonl|Session|Browser" --no-restore --verbosity minimal` | Browser smoke |
| WebUi sandbox/artifact runtime bridge | WebUiWorker | `components/SandboxedIframe.ts`, `components/sandbox/**`, `tools/artifacts/**`, `javascript-repl.ts` | `src/Tau.WebUi/**`, browser/API tests | WebUi sandbox/artifact rows `missing`/`partial` | `dotnet test tests\Tau.WebUi.Tests\Tau.WebUi.Tests.csproj --no-restore --verbosity minimal` plus browser artifact smoke | Browser smoke |
| Mom real Slack receive/respond/file/stop smoke | MomWorker | `packages/mom/src/slack.ts`, `download.ts`, `store.ts` | `src/Tau.Mom/Slack/**`, Mom-related `tests/Tau.Agent.Tests/**`, e2e record | Mom Slack rows `external-e2e-needed` | Fake Slack tests plus real Socket Mode/Web API smoke with redacted token/source evidence | Yes |
| Mom Docker sandbox smoke and session schema parity | MomWorker | `sandbox.ts`, `tools/*`, `context.ts`, `store.ts`, `log.ts` | `src/Tau.Mom/**`, Mom sandbox/session/log tests | Mom sandbox/session/log rows `partial`/`external-e2e-needed` | Mom targeted tests plus real Docker `--validate-sandbox` and tool execution smoke | Yes |
| Pods command/config/known-model compatibility | PodsWorker | `packages/pods/src/cli.ts`, `config.ts`, `model-configs.ts`, `models.json`, `commands/models.ts` | `src/Tau.Pods/Cli/**`, `Models/**`, services/tests | Pods command/config/model rows `partial` | `dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --filter "PodsCli|Model|Config" --no-restore --verbosity minimal` | No |
| Pods real SSH/HF/setup/GPU/vLLM e2e | PodsWorker | `ssh.ts`, `commands/pods.ts`, `commands/models.ts`, scripts `pod_setup.sh` / `model_run.sh` | `src/Tau.Pods/Services/**`, e2e record/history | Pods SSH/setup/vLLM rows `external-e2e-needed` | Fake-runner tests plus real SSH/SCP/HF/setup/vLLM smoke with redacted env evidence | Yes |
| Release/no-env/build artifact parity | Main Integrator | root `package.json`, `test.sh`, `pi-test.sh`, `scripts/build-binaries.sh`, `scripts/release.mjs` | `scripts/**`, `.github/workflows/tau-ci.yml`, project packaging, CI/release docs | RID-specific release artifact baseline is present via `scripts/build-release-artifacts.ps1` and `scripts/smoke-release-artifacts.ps1`; archive packaging now supports `auto|zip|tar.gz`, host-RID executable smoke, non-host extraction-only structure validation, and `build-release-matrix.ps1` / `package-release-matrix.ps1` for the upstream five-platform matrix. PowerShell no-env wrapper baseline is present via `scripts/invoke-no-env.ps1` / `scripts/verify-no-env.ps1`, and `scripts/pi-test.ps1` covers the direct CodingAgent `pi-test.sh --no-env` role for PowerShell validation. Windows GitHub Actions CI now reuses the same no-env smoke, Release artifact build, archive package/extract smoke and zip upload path plus tar.gz format structure smoke. Release payload baseline now copies current Tau docs payload and audits upstream payload items in `manifest.json.releasePayload`. `Directory.Build.props` now owns Tau product `VersionPrefix`; `scripts/plan-release.ps1` uses it for dry-run planning of upstream `release.mjs` steps; `scripts/prepare-release.ps1` composes local version + release notes writes under explicit `-Apply` with clean-worktree and helper-preview guards; `scripts/update-release-version.ps1` can update that MSBuild source when `-Apply` is explicit; `scripts/update-release-notes.ps1` can update `docs/releases/feature-release-notes.md` when `-Apply` is explicit; release manifest/smoke audit the same version source. Rows remain `partial` because no-env/release matrix execution, non-host runner executable smoke, exact Unix wrapper/auth-backup parity, examples/Photon/interactive raster assets/external export-html payload parity, release commit/tag/publish/push execution automation and real external e2e release smoke are not complete. | `git diff --check`, `plan-release.ps1` dry-run, `update-release-version.ps1` temp props writeback smoke, `update-release-notes.ps1` temp notes writeback smoke, `verify-no-env.ps1 -SkipRestore`, `verify-no-env.ps1 -SkipRestore -RunSmoke`, artifact build/start smoke, archive package/extract smoke in clean output dir, GitHub Actions Windows workflow | Release smoke |

## External E2E Needed

Initial known list:

- AI provider family real e2e: OpenAI/Responses/Codex/Azure/Anthropic/Google/Vertex/Gemini CLI/Antigravity/Mistral/Bedrock/OpenAI-compatible.
- OAuth real e2e: OpenAI Codex, Anthropic, Gemini CLI/Antigravity, GitHub Copilot device flow.
- AWS/Bedrock e2e: SigV4, SSO refresh, OIDC registration renewal, AssumeRole/profile/credential_process.
- Slack e2e: Socket Mode receive/ack/respond/update/delete/download/stop.
- Docker e2e: Mom sandbox validate + tool execution.
- Pods e2e: SSH/HF/GPU/vLLM full path.
- WebUi e2e: browser smoke against release/static output.

## Next Inventory Freeze Steps

- Phase 1 final normalization is complete at grouped file-level granularity. The upstream package directory set is fully represented by this matrix.
- Use `Phase 2 Candidate Queue` as the first worker backlog. Each row includes owner, upstream evidence, Tau target, status source, validation gate and external-e2e flag.
- Do not reopen broad inventory unless upstream package directories or package manifests change, or an implementation worker finds a concrete missing path while working a candidate.
- Keep `external-e2e-needed` entries open until real provider/service/runtime smoke exists or the user explicitly accepts a non-goal.
