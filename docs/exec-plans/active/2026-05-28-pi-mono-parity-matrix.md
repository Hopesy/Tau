# pi-mono -> Tau parity matrix

Status values:

- `verified`: Tau implementation has direct behavior evidence from tests/smoke/e2e and docs are current.
- `ported`: Tau implementation exists, but evidence is not yet strong enough for final 100% acceptance.
- `partial`: Tau covers part of the upstream behavior, with known behavior gaps.
- `missing`: No meaningful Tau equivalent yet.
- `external-e2e-needed`: Contract/fake coverage may exist, but real provider/service/runtime validation is still required.
- `non-goal-proposed`: Candidate non-goal; only valid after explicit user confirmation.

This matrix is the Phase 1 inventory surface for `docs/exec-plans/active/2026-05-28-tau-100-percent-pi-mono-parity.md`. It is intentionally evidence-first: every status must point to upstream source, Tau target, and validation evidence or remaining proof needed.

Current handoff at 2026-06-07: Phase 1 inventory freeze remains closed. `GOAL.md` has restored 100% pi-mono parity as the active objective, so Phase 2 workers should use the `Phase 2 Candidate Queue` below as the first implementation/e2e backlog. Do not reopen broad inventory unless upstream package directories or package manifests actually change, or a worker finds a concrete missing upstream path while closing a candidate.

## Inventory Progress

| Area | Upstream scope | Tau scope | Status | Notes |
| --- | --- | --- | --- | --- |
| AI + Agent | `packages/ai`, `packages/agent` | `src/Tau.Ai`, `src/Tau.Agent`, tests | partial | Capability inventory plus `packages/ai/src/**`, AI command/API/env/config/log/schema surface, and `packages/agent/src/**` file-level mapping are now frozen. Local NuGet-style consumer smoke now proves an external project can reference `Tau.Ai` directly, including explicit `ModelConfigurationStore` / `ProviderAuthResolver` injection, `models.json` dynamic OpenAI-compatible provider resolution, auth status reporting, header precedence, upstream API alias dispatch, provider/model/default request options injection, OpenAI Responses family provider-specific options typed dispatch, Mistral string/function `toolChoice` provider-specific options typed dispatch, Anthropic thinking/tool-choice provider-specific options typed dispatch, Google tool-choice/thinking provider-specific options typed dispatch, and Bedrock region/profile/bearer/tool-choice/thinking/request-metadata provider-specific options typed dispatch; external projects can also reference only `Tau.Agent` while transitively consuming `Tau.Ai`. `Tau.Ai.Cli` now has local dotnet tool install rehearsal for `pi-ai` / `tau-ai`; Agent stream proxy now has local loopback HTTP/SSE server e2e evidence; public export shape is now documented as Tau-native .NET assembly/package surface and covered by `verify-ai-agent-export-shape.ps1`; AI provider/OAuth matrix now has a repo-owned inspect/isolation executable contract in `verify-ai-provider-e2e-matrix.ps1`. Known final blockers still include real provider/OAuth e2e, remaining provider-specific config UX, real registry/global install promotion and signing/provenance rehearsal. |
| CodingAgent + Tui | `packages/coding-agent`, `packages/tui` | `src/Tau.CodingAgent`, `src/Tau.Tui`, tests | partial | Capability inventory plus grouped `packages/coding-agent/src/**`, CodingAgent CLI/RPC/env/config/log/schema surface, `packages/tui/src/**`, and Tui terminal/input/render/theme surface are now frozen. Known final blockers include package/bin/config parity, full TreeSelector/session schema, full jiti/custom tool extension runtime, clipboard image paste, image resize/EXIF/provider vision e2e, terminal host, real terminal image smoke, full Markdown/theme integration and live TTY smoke. |
| WebUi | `packages/web-ui` | `src/Tau.WebUi`, tests | partial | Capability inventory plus grouped `packages/web-ui/src/**` and WebUi HTTP/component/env/config/log/schema/browser/release surface are now frozen. Known final blockers include reusable component package parity, IndexedDB/provider-key/custom-provider UI, CodingAgent branch/tree true persistence, richer artifact renderers beyond the current lightweight viewer baseline, full sandbox/runtime providers, browser/release packaging parity. |
| Mom | `packages/mom` | `src/Tau.Mom`, Mom-related `tests/Tau.Agent.Tests` | partial | Capability inventory plus grouped `packages/mom/src/**`, `packages/mom/scripts/**`, and Mom Slack/event/session/sandbox/env/config/log/schema surface are now frozen. Known final blockers include real Slack smoke, Slack session sync/schema parity, Docker smoke, fs-watch event runtime, and timestamp migration helper parity. |
| Pods + root scripts | `packages/pods`, root scripts/manifests | `src/Tau.Pods`, `tests/Tau.Pods.Tests`, `scripts/*`, CI/release | partial | Capability inventory plus `packages/pods/src/**` file-level mapping and root script matrix are now frozen. Known final blockers include real SSH/HF/GPU/vLLM smoke, upstream GPU allocation/model prompt parity, rollout/rollback state, transport hardening and release artifact parity. |

## Upstream Package Surface

Initial file counts from `C:\Users\zhouh\Desktop\pi-mono-main`:

| Package | Total tracked source/doc/config files counted | `src/**` files counted | Tau target |
| --- | ---: | ---: | --- |
| `packages/ai` | 109 | 44 | `src/Tau.Ai`, `tests/Tau.Ai.Tests` |
| `packages/agent` | 15 | 5 | `src/Tau.Agent`, `tests/Tau.Agent.Tests` |
| `packages/coding-agent` | 411 | 137 | `src/Tau.CodingAgent`, `tests/Tau.CodingAgent.Tests` |
| `packages/tui` | 57 | 25 | `src/Tau.Tui`, `tests/Tau.Tui.Tests` |
| `packages/web-ui` | 85 | 75 | `src/Tau.WebUi`, `tests/Tau.WebUi.Tests` |
| `packages/mom` | 29 | 16 | `src/Tau.Mom`, Mom-related `tests/Tau.Agent.Tests` |
| `packages/pods` | 22 | 10 | `src/Tau.Pods`, `tests/Tau.Pods.Tests` |

Count method:

```powershell
$root='C:\Users\zhouh\Desktop\pi-mono-main'
$pkgs='ai','agent','coding-agent','tui','web-ui','mom','pods'
foreach($p in $pkgs){
  $src=Join-Path $root "packages\$p"
  $files=rg --files $src | Where-Object { $_ -match '\.(ts|tsx|js|mjs|json|md|sh|css)$' }
  $srcFiles=$files | Where-Object { $_ -match '\\src\\' }
  "${p}: total=$($files.Count) src=$($srcFiles.Count)"
}
```

## Root Build/Test/Release Surface

| Upstream item | Upstream behavior | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| `package.json` workspaces | Builds all packages in order: tui, ai, agent, coding-agent, mom, web-ui, pods. Runs root check, browser smoke, tests, version and publish scripts. | `Tau.slnx`, `scripts/verify-dotnet.ps1`, `scripts/verify-dotnet.sh`, `.github/workflows/tau-ci.yml` | partial | Tau has project-level build/test/smoke and Windows CI now runs release contract smoke, no-env smoke plus release artifact/archive build. `verify-dotnet.ps1 -SkipWebUiBrowserTests` is an explicit sandbox mode for non-browser WebUi validation when Chromium launch is blocked; default `verify-dotnet.ps1` remains the full gate. Local version/notes/commit/tag execution now exists under explicit `execute-release.ps1 -Apply`, but publish/push/version parity is not complete. |
| `test.sh` | Temporarily backs up `~/.pi/agent/auth.json`, unsets provider env vars, sets `PI_NO_LOCAL_LLM=1`, runs `npm test`. | `scripts/verify-no-env.ps1 -SkipRestore`, `scripts/invoke-no-env.ps1`, `scripts/verify-dotnet.ps1` and module tests | partial | Tau now has PowerShell child-process no-env isolation for provider/AWS/Azure/HF/Slack/Tau auth/config env vars, plus isolated `TAU_AUTH_FILE` / `TAU_MODELS_FILE` / CodingAgent state paths. It does not move or back up user auth files, so exact auth-backup shell parity remains open. |
| `pi-test.sh` | Runs `packages/coding-agent/src/cli.ts`, supports `--no-env` to unset provider env vars before CLI execution. | `scripts/pi-test.ps1`, `scripts/invoke-no-env.ps1`, `scripts/verify-no-env.ps1`, `dotnet run --project src/Tau.CodingAgent/Tau.CodingAgent.csproj` | partial | Tau now has a PowerShell-first CodingAgent direct wrapper: `pi-test.ps1` runs `Tau.CodingAgent`, supports `--no-env`, isolated Tau state, stdin file redirection and base64 child argument arrays; `verify-no-env.ps1 -RunSmoke` uses it for CodingAgent RPC `get_state`. It is still not an exact Unix shell wrapper. |
| `scripts/check-browser-smoke.mjs` + `scripts/browser-smoke-entry.ts` | Browser smoke for web UI/example. | `scripts/verify-dotnet.ps1 -RunSmoke`, `tests/Tau.WebUi.Tests/WebUiBrowserFlowTests.cs` | partial | Tau has WebUi smoke and browser tests; release/static package browser smoke still needs closure. |
| `scripts/build-binaries.sh` | Builds distributable upstream binaries. | `scripts/build-release-artifacts.ps1`, `scripts/package-release-artifacts.ps1`, `scripts/smoke-release-artifacts.ps1`, `.github/workflows/tau-ci.yml` | partial | Tau now has a release artifact baseline under `artifacts/tau-<rid>/` with `pi`, `tau-ai` / `pi-ai`, `mom`, `pi-pods` and `tau-web-ui` wrappers plus manifest and host-RID smoke. Archive packaging supports `auto|zip|tar.gz`: Windows RIDs produce zip, Linux/macOS RIDs produce tar.gz, archives are extracted into a clean temp directory for structure validation, and host RID archives run executable smoke. `build-release-matrix.ps1` / `package-release-matrix.ps1` now encode the upstream five-platform matrix. `build-release-artifacts.ps1` also copies current `README.md`, `LICENSE`, full `docs/` and `examples/`, while `manifest.json.releasePayload` audits upstream payload items as included, tau-native inline/docs/manifest, missing or not-applicable. Remaining gap is non-host runner executable smoke, Photon/interactive raster assets/external export-html payload parity and release automation. |
| `scripts/release.mjs` | Release/version/publish flow. | `Directory.Build.props`, `scripts/plan-release.ps1`, `scripts/execute-release.ps1`, `scripts/prepare-release.ps1`, `scripts/validate-release.ps1`, `scripts/verify-release-contracts.ps1`, `scripts/update-release-version.ps1`, `scripts/update-release-notes.ps1`, `scripts/build-release-artifacts.ps1`, release matrix/package scripts | partial | Tau now has a repo-owned MSBuild `VersionPrefix`, a dry-run release planner, guarded local preparation and validation scripts, a guarded local release execution script, a short dry-run JSON contract smoke, a narrowly scoped version writeback helper, and a narrowly scoped release notes helper. `execute-release.ps1 -Apply` requires a clean worktree, runs contract smoke, applies version+notes, optionally runs local validation, stages only `Directory.Build.props` and `docs/releases/feature-release-notes.md`, commits `Release v<version>` and tags `v<version>`. Release artifacts record the same version source in `manifest.json` and smoke validates it. Tau still does not implement publish or push execution automation, and it intentionally does not create an upstream-style fresh `[Unreleased]` commit because Tau release notes are a dated table. |
| `scripts/session-transcripts.ts` | Session transcript utility. | `scripts/export-session-transcripts.ps1`, CodingAgent/WebUi export paths | partial | Tau now has a PowerShell-first transcript utility that scans `.tau/coding-agent-session.jsonl` and `.tau/coding-agent-sessions/*.jsonl` or explicit `-SessionPath` / `-SessionsDirectory`, exports user/assistant text to `session-transcripts-000.txt` style chunks, and has fixture smoke coverage through `verify-session-audit-scripts.ps1`. Upstream `--analyze` subagent flow remains open because Tau does not yet have the same `pi --mode json --tools read,write` analysis contract. |
| `scripts/profile-coding-agent-node.mjs` | TUI/RPC profiling entry. | `scripts/profile-coding-agent-startup.ps1`, `scripts/verify-coding-agent-startup-profile.ps1` | partial | Tau now has an RPC startup profiler that runs `Tau.CodingAgent.dll --mode rpc --no-context-files --no-themes`, sends `get_state`, reports median startup time and is covered by `coding-agent-startup-profile-smoke`. TUI first-frame timing, CPU profile capture and Node/Bun runtime comparison remain open. |
| `scripts/edit-tool-stats.mjs` | Edit tool statistics utility. | `scripts/report-edit-tool-stats.ps1`, `scripts/verify-edit-tool-stats.ps1` | partial | Tau now has a PowerShell-first edit stats audit over CodingAgent JSONL sessions. It recognizes Tau `edit_file` and upstream `edit` tool calls/results, reports success/failure, single vs multi-edit, argument style, provider/model, extension, same-file clusters, context inflation and worst examples, with fixture smoke coverage. Runtime edit behavior and external provider/tool e2e remain separate gaps. |
| `scripts/cost.ts` | Cost utility. | `scripts/report-session-costs.ps1`, `Tau.Ai.Registry.ModelCatalog.CalculateCost`, CodingAgent JSONL/flat session usage persistence, CodingAgent session stats | partial | Tau now has a PowerShell-first session cost report script with fixture smoke coverage, CodingAgent persists assistant `message.usage.cost` when the active model has pricing, and `/session` plus RPC `get_session_stats` expose persisted token/cost totals. JSONL tree sessions aggregate usage/cost from current branch message entries so compaction snapshots do not hide prior cost records. The script still does not infer historical dollar cost from older sessions or models without persisted cost data; real provider/e2e cost samples, exact upstream session path semantics and richer upstream footer/TUI display remain open. |
| `scripts/sync-versions.js` | Workspace package version sync. | `Directory.Build.props`, `scripts/sync-release-versions.ps1`, `scripts/verify-release-version-sync.ps1` | partial | Tau now has a .NET-native version sync audit: `Directory.Build.props` is the single MSBuild version source, `src/**/*.csproj` inherit it by default, and the script detects or applies fixes for explicit project version drift. NuGet/package publish synchronization remains open. |

## AI + Agent Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| AI stream/event abstractions | `packages/ai/src/types.ts`, `stream.ts`, `utils/event-stream.ts` | `src/Tau.Ai/Abstractions/*`, `src/Tau.Ai/Streaming/*`, provider parser tests | ported | Tau has 13 stream-event records and channel-backed `EventStream`; final verification still depends on provider-family e2e. |
| Provider registry and built-ins | `api-registry.ts`, `providers/register-builtins.ts`, provider subpath exports in `packages/ai/package.json` | `src/Tau.Ai/Providers/ProviderRegistry.cs`, `BuiltInProviders.cs`, `StreamFunctions.cs`, `tests/Tau.Ai.Tests/BuiltInProvidersTests.cs`, `ProviderRegistryTests.cs` | partial | Tau has lazy registry and major built-ins. Registry/config now accepts upstream public API aliases `openai-completions`, `openai-compatible` and `google-generative-ai` while retaining Tau canonical keys; exact exported provider hook surface such as `setBedrockProviderModule` and real provider e2e remain open. |
| Model catalog / generated models | `models.ts`, `models.generated.ts`, `scripts/generate-models` implied by package build | `src/Tau.Ai/Registry/ModelCatalog.cs`, `GeneratedBuiltInModels.g.cs`, `generated-models.seed.json`, `ModelCatalogTests.cs` | partial | Generated seed exists, but current docs only prove Tau-supported model families, not full upstream generated model coverage. |
| OpenAI / Responses / Codex / Azure / Copilot providers | `providers/openai-*.ts`, `azure-openai-responses.ts`, `github-copilot-headers.ts` | `src/Tau.Ai/Providers/OpenAi*`, `tests/Tau.Ai.Tests/OpenAi*`, `AzureOpenAiResponsesProviderTests.cs` | external-e2e-needed | Stub/serialization tests exist; real cloud Responses/Codex/Azure/Copilot behavior remains unverified. |
| Anthropic / Google / Vertex / Gemini CLI / Antigravity / Mistral | `providers/anthropic.ts`, `google*.ts`, `google-vertex.ts`, `mistral.ts` | `src/Tau.Ai/Providers/Anthropic`, `Google`, `Mistral`, corresponding tests | external-e2e-needed | Contract tests cover request and parser behavior; real provider e2e and auth refresh/error semantics remain final blockers. |
| Bedrock / AWS auth chain | `bedrock-provider.ts`, `providers/amazon-bedrock.ts` | `src/Tau.Ai/Providers/Bedrock/*`, `Bedrock*Tests.cs`, `ModelConfigurationStoreTests.cs` | partial | Tau covers SigV4, bearer, shared profile, credential process, ECS/IMDS/web identity, AssumeRole and SSO refresh baseline. `models.json options` now project Bedrock `region`, `profile`, `bearerToken`, string/tool-object `toolChoice`, `reasoning`, `thinkingBudgetTokens`, `thinkingDisplay`, `interleavedThinking` and `requestMetadata` into typed `BedrockOptions`; request-body tests cover tool choice, request metadata, thinking display and non-adaptive Claude `anthropic_beta`. OIDC client registration renewal, multi-cache/concurrency and real AWS e2e remain open. |
| OAuth providers and credential store | `oauth.ts`, `utils/oauth/*` | `src/Tau.Ai/Auth/OAuth/*`, `OAuthProviderTests.cs`, `OAuthCredentialStoreTests.cs`, `ProviderAuthResolverTests.cs` | partial | Built-in OAuth providers exist for Anthropic, Copilot, Gemini CLI, Antigravity and OpenAI Codex; the local registry/store/helper contract is now directly tested and usable by external .NET consumers, but real OAuth e2e and UX-level refresh/login parity remain open. |
| Env/api key resolution and custom provider config | `env-api-keys.ts`, `api-registry.ts`, provider options | `EnvironmentApiKeyResolver.cs`, `ModelConfigurationStore.cs`, `ModelConfigurationStoreTests.cs`, `ProviderAuthResolverTests.cs` | partial | Tau supports env/literal/command-backed models.json subset, dynamic OpenAI-compatible registration, provider-level models.json auth status without executing command-backed secrets or leaking values, upstream API-name aliases in custom models/config, provider/model/default `options` for common request settings (`temperature`, `maxTokens`, `topP`, `transport`, `cacheRetention`, `sessionId`, `maxRetryDelayMs`, `metadata`, `reasoning`, `thinkingBudgets`), Responses family provider-specific `models.json` options (`serviceTier`, `reasoningEffort`, `reasoningSummary`, Codex `textVerbosity`, Azure `azureApiVersion` / `azureResourceName` / `azureBaseUrl` / `azureDeploymentName`), Mistral provider-specific options (`toolChoice` string/function object, `promptMode`, `reasoningEffort`), Anthropic provider-specific options (`thinkingEnabled`, `thinkingBudgetTokens`, adaptive `effort`, `thinkingDisplay`, `interleavedThinking`, `toolChoice` string/tool object), Google family provider-specific options (`toolChoice`, `thinkingEnabled`, `thinkingBudgetTokens`, `thinkingLevel`, Vertex `project/location`, Gemini CLI `projectId`), and Bedrock provider-specific options (`region`, `profile`, `bearerToken`, string/tool-object `toolChoice`, `thinkingBudgetTokens`, `thinkingDisplay`, `interleavedThinking`, `requestMetadata`). Provider-only status intentionally checks only provider-level credentials so model-only credential headers are not misreported as provider-wide configuration. Full upstream runtime config UX, remaining provider-specific option fields and real provider e2e still need mapping. |
| Secret redaction and runtime log foundation | Upstream package has auth/config surfaces; redaction is Tau hardening surface | `TauSecretRedactor.cs`, `JsonlSecretRedactor.cs`, `JsonlTauLogSink.cs`, redaction tests | ported | Tau has stronger string-value JSONL redaction foundation; remaining work is expanding patterns from real leaked samples and proving cross-module adoption. |
| Agent runtime loop, facade and tool events | `packages/agent/src/agent.ts`, `agent-loop.ts`, `types.ts` | `src/Tau.Agent/Agent.cs`, `Runtime/AgentRuntime.cs`, `Runtime/ToolExecutor.cs`, `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, `Abstractions/AgentEvents.cs`, `tests/Tau.Agent.Tests/AgentFacadeTests.cs`, `AgentRuntimeContractTests.cs`, `AgentPublicApiCompileSampleTests.cs`, queue/trace tests | partial | Tau now has a high-level `Agent` facade baseline plus `agent_end.messages`, `turn_end.message/toolResults`, tool update partial result, schema validation, error-as-tool-result closure, facade/runtime/proxy/event public API compile-sample coverage, high-level facade failure/cancel synthetic assistant message coverage, low-level `AgentRuntime.RunStream(...)` / `EventStream<AgentEvent, ChatMessage[]>` result wrapper coverage, parallel start/update timing parity, low-level assistant stream cancellation terminal coverage, tool cancellation cleanup, parallel sibling cancellation result preservation and `TransformContext` cancellation. `AgentApplicationBuilder.AddTool(..., prepareArguments:)` now forwards raw-arg preparation into `DelegateAgentTool`, and the external package consumer smoke observes `toolResult=prepared package consumer`; package/index export-shape decision is now covered by the dedicated export-shape smoke. |
| Agent stream proxy | `packages/agent/src/proxy.ts` reconstructs partial assistant messages from stripped proxy events via `/api/stream` | `src/Tau.Agent/Proxy/ProxyStreamProvider.cs`, `tests/Tau.Agent.Tests/ProxyStreamProviderTests.cs`, `scripts/verify-agent-proxy-server-e2e.ps1` | verified | Tau posts the upstream request envelope to a real local loopback HTTP/SSE `/api/stream` server, sends bearer auth, verifies the request body, rebuilds stripped proxy events into Tau stream events, and covers HTTP error, missing terminal event and malformed SSE JSON terminal-error behavior. This row has no external provider dependency and is covered by `ProxyStreamProviderTests` 5/5 plus the dedicated PowerShell smoke, `verify-dotnet.ps1 -RunSmoke`, `plan-release.ps1` and `verify-release-contracts.ps1`. |
| Package/index export shape | `packages/agent/src/index.ts`, `packages/ai/package.json` subpath exports and bins | `docs/AI_AGENT_EXPORT_SHAPE.md`, `Tau.Ai` / `Tau.Agent` assembly public API, public API compile samples, `scripts/verify-agent-package-consumer.ps1`, `scripts/verify-ai-cli-tool-install.ps1`, `scripts/verify-release-package-publish.ps1`, `scripts/verify-ai-agent-export-shape.ps1` | verified | Tau public API is intentionally .NET-native rather than TypeScript package-export equivalent: provider/auth/registry/streaming surfaces live under `Tau.Ai` namespaces, facade/runtime/proxy/platform surfaces live under `Tau.Agent` namespaces, `pi-ai` maps to release wrapper and dotnet tool package, and `@sinclair/typebox` re-exports map to Tau-native `JsonElement` schema helpers / validation instead of same-name exports. `verify-ai-agent-export-shape.ps1` fixes 12 AI package exports, 13 AI index export groups, 4 Agent index export groups and 37 evidence files, and is wired into release plan/contracts. Real registry/global install, signing/provenance and provider/OAuth e2e remain separate rows. |
| Local NuGet-style package consumer boundary | `packages/agent/package.json` depends on `@mariozechner/pi-ai`; `packages/ai/package.json` and `packages/agent/package.json` publish `README.md` | `Directory.Build.props`, `src/Tau.Ai/Tau.Ai.csproj`, `src/Tau.Agent/Tau.Agent.csproj`, `scripts/verify-agent-package-consumer.ps1`, `scripts/verify-release-contracts.ps1` | verified | Tau now packs `Tau.Ai` and `Tau.Agent` with README/description metadata, verifies `Tau.Agent.nuspec` declares the `Tau.Ai` dependency, restores/builds/runs a temp external `net10.0` console app that references only `Tau.Ai` and completes a Faux provider call through `StreamFunctions`, then restores/builds/runs a second temp external app that references only `Tau.Agent` while using `Tau.Ai.Providers.Faux` transitively through `AgentApplication`, delegate tool, session store and runtime log. The Tau.Ai consumer also asserts injected runtime config, model-level and provider-level `models.json` auth status, dynamic OpenAI-compatible provider registration, auth header precedence, upstream API alias dispatch (`google-generative-ai` -> Tau canonical `google-generative-language`), configured request options (`temperature`, `maxTokens`, `topP`, `transport`, `cacheRetention`, `sessionId`, `maxRetryDelayMs`, `reasoning`, `thinkingBudgets`, `metadata`), Responses provider-specific options typed dispatch into `OpenAiResponsesOptions`, Mistral provider-specific options typed dispatch into `MistralOptions` including function `toolChoice`, Anthropic provider-specific options typed dispatch into `AnthropicOptions` including thinking/display/interleaved-thinking/tool-choice fields, Google provider-specific options typed dispatch into `GoogleOptions` including tool-choice/thinking/max-token fields, and Bedrock provider-specific options typed dispatch into `BedrockOptions` including region/profile/bearer/tool-choice/reasoning/request-metadata fields; the Agent consumer asserts the prepared delegate tool result (`toolResult=prepared package consumer`), proving builder-level `prepareArguments` survives package consumption. This pure local package-consumer boundary has no external-e2e dependency and is covered by 85 script assertions plus release contract checks; real NuGet registry publish, signing/provenance and package/global install alias remain separate Phase 5 gaps. |
| Local dotnet tool install alias | `packages/ai/package.json` declares bin `pi-ai` and publishable CLI package behavior | `src/Tau.Ai.Cli/Tau.Ai.Cli.csproj`, `src/Tau.Ai.Cli/AiCliRunner.cs`, `tests/Tau.Ai.Tests/AiCliRunnerTests.cs`, `scripts/verify-ai-cli-tool-install.ps1`, `scripts/verify-release-contracts.ps1` | verified | Tau now packs the same `Tau.Ai.Cli` project as two temporary dotnet tool packages with `ToolCommandName=pi-ai` and `ToolCommandName=tau-ai`, installs them from a temp local package source into temp tool paths, verifies installed shims exist, and asserts `pi-ai --help`, `pi-ai list` and `tau-ai --help` render the launched command name and provider list. This is a local install rehearsal with no external registry dependency and is covered by the dedicated script plus `verify-dotnet.ps1 -RunSmoke`, `plan-release.ps1` and `verify-release-contracts.ps1`; real NuGet registry publish, source credentials, global install from a real feed, signing and provenance remain Phase 5 gaps. |

### AI File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/ai/src/index.ts` | `src/Tau.Ai/Tau.Ai.csproj`, public `Tau.Ai` namespaces, `tests/Tau.Ai.Tests/AiPublicApiCompileSampleTests.cs`, `docs/AI_AGENT_EXPORT_SHAPE.md`, `scripts/verify-ai-agent-export-shape.ps1` | verified | Core types/providers/registry are public by namespace rather than a TypeScript barrel, and `AiPublicApiCompileSampleTests` freezes the current .NET consumer surface. `AI_AGENT_EXPORT_SHAPE.md` now explicitly maps upstream index re-exports, including the TypeBox same-name export divergence, to Tau-native public namespaces/helpers; the dedicated smoke verifies the mapping and evidence files. Full AJV/TypeBox runtime keyword/codegen parity remains tracked by the schema validation row, not by package export shape. |
| `packages/ai/package.json` exports / bin `pi-ai` | `Tau.Ai` class library, `src/Tau.Ai.Cli/Tau.Ai.Cli.csproj`, release artifact `bin/pi-ai.cmd`, `scripts/verify-ai-cli-tool-install.ps1`, plus `Tau.CodingAgent` auth commands | partial | `Tau.Ai.Cli` now provides a standalone `tau-ai` baseline for help/list/login, interactive provider selection, Tau default auth store writes, and explicit `--auth-file auth.json` cwd auth compatibility; current release artifact now injects its wrapper alias into help/error usage so `pi-ai` prints `pi-ai` and the smoke also covers `pi-ai --help`; local dotnet tool install rehearsal now installs temp `pi-ai` / `tau-ai` tool packages and verifies `--help` / `list` command names. Remaining gap is real package feed/global install promotion, signing/provenance, source credential boundary and exact package export/subpath shape. |
| `packages/ai/src/types.ts` | `src/Tau.Ai/Abstractions/Messages.cs`, `ContentBlocks.cs`, `StreamEvents.cs`, `Models.cs`, `Options.cs`, `Tools.cs`, `Registry/ModelApiNames.cs` | partial | Tau ports message/content/tool/model/usage and 13 stream events as C# records. `StreamOptions.Signal`, `StreamOptions.OnPayload`, `StreamOptions.OnResponse`, `TopP`, `Transport`, `CacheRetention`, `SessionId`, `MaxRetryDelay`, `Metadata` and `SimpleStreamOptions.ThinkingBudgets` now exist in the .NET public surface and are locally exercised by provider tests plus the public API sample. `Signal` is now wired through the major provider-family I/O paths and emits a local terminal aborted assistant on pre-cancel/user cancellation. Upstream `KnownApi` names are not byte-equivalent internally, but `ModelApiNames` now adapts `openai-completions` / `openai-compatible` / `google-generative-ai` at registry/config boundaries; common request options, OpenAI Responses family provider-specific options, Mistral string/function provider-specific options, Anthropic thinking/tool-choice provider-specific options, Google tool-choice/thinking provider-specific options and Bedrock region/profile/bearer/tool-choice/thinking/request-metadata provider-specific options can also come from `models.json` when code does not explicitly set them. Real cloud callback/cancellation timing correlation and remaining provider-specific option fields remain open. |
| `packages/ai/src/api-registry.ts` | `src/Tau.Ai/Providers/ProviderRegistry.cs`, `BuiltInProviders.cs`, `ProviderRegistryTests.cs` | verified | `ProviderRegistry` is a pure, no-network, Tau-native registry whose own contract is now directly unit-covered: lazy factory init (factory not invoked until `Get`/`TryGet`), single materialization per entry, register-replace by api (factory and instance overloads), upstream API alias normalization on register/get/try-get/unregister, `Get` hit/`KeyNotFoundException` miss, `TryGet` hit/null miss, `RegisteredApis` membership, `Unregister`, source-scoped `UnregisterBySource` selectivity (only matching sourceId removed, null sourceId untouched) and `Clear`. Design difference vs upstream `registerApiProvider`: Tau does not wrap stream functions with a "Mismatched api" guard because Tau providers self-identify via `IStreamProvider.Api`; the exact TypeScript `ApiProvider` generic/`getApiProviders` surface stays .NET-native, so `verified` reflects complete local evidence for the registry behavior that exists rather than byte-for-byte export-shape parity. |
| `packages/ai/src/stream.ts` | `src/Tau.Ai/Providers/StreamFunctions.cs`, provider tests | partial | `Stream`, `StreamSimple`, `CompleteAsync`, and `CompleteSimpleAsync` exist and resolve auth/models.json before dispatch. They now merge provider/model/default `models.json options` into request options while preserving explicit code options as highest precedence, and project Responses family provider-specific config into typed `OpenAiResponsesOptions`, `OpenAiCodexResponsesOptions` or `AzureOpenAiResponsesOptions`, Mistral string/function provider-specific config into typed `MistralOptions`, Anthropic thinking/tool-choice provider-specific config into typed `AnthropicOptions`, Google family provider-specific config into typed `GoogleOptions` / `GoogleVertexOptions` / `GoogleGeminiCliOptions`, and Bedrock provider-specific config into typed `BedrockOptions` before dispatch. `StreamSimple` uses typed `Stream(...)` when provider-specific config is present so provider option rebuilding does not drop fields. Upstream import-side-effect registration, exact thrown error text/API names, remaining provider-specific option maps and real provider e2e remain open. |
| `packages/ai/src/models.ts` | `src/Tau.Ai/Registry/ModelCatalog.cs`, `ModelCatalogTests.cs` | partial | Tau supports provider/model lookup, canonical references, default model selection, cost calculation, service-tier multipliers, `SupportsXhigh`, and model equality. Full upstream model set and default parity remain unproven. |
| `packages/ai/src/models.generated.ts` | `src/Tau.Ai/Registry/GeneratedBuiltInModels.g.cs`, `generated-models.seed.json`, `scripts/generate-tau-ai-models.ps1` | partial | Tau has a generated catalog pipeline for currently supported families. It is not yet proven equivalent to upstream generated model coverage. |
| `packages/ai/src/env-api-keys.ts` | `src/Tau.Ai/Auth/EnvironmentApiKeyResolver.cs`, `ProviderAuthResolver.cs` | verified | Tau now has focused contract evidence for the full implemented env/auth matrix: Anthropic OAuth/API key precedence, Copilot token fallback order, Google `GEMINI_API_KEY` / `GOOGLE_API_KEY` precedence, Vertex explicit API key vs ADC marker, Windows `APPDATA\\gcloud\\application_default_credentials.json` and Unix `~/.config/gcloud/application_default_credentials.json` lookup, Bedrock ambient credential marker, and the implemented OpenAI/Azure/Groq/Cerebras/xAI/OpenRouter/Vercel AI Gateway/z.ai/Mistral/Minimax/HF/opencode/kimi env aliases. `ProviderAuthResolver` status text is now aligned with available OAuth login/refresh semantics, and `StreamFunctions` accepts injected `ModelConfigurationStore` / `ProviderAuthResolver` so external consumers can use the same contract. |
| `packages/ai/src/cli.ts` | `src/Tau.Ai.Cli/AiCliRunner.cs`, `src/Tau.Ai.Cli/Program.cs`, `tests/Tau.Ai.Tests/AiCliRunnerTests.cs`, `scripts/verify-ai-cli-tool-install.ps1` | partial | Tau now has a standalone AI CLI baseline: help/list/login, interactive provider selection, unknown-provider errors, OAuth credential save, default Tau auth store, and explicit `--auth-file auth.json` for upstream cwd auth compatibility. It also detects the real dotnet tool shim name for local `pi-ai` / `tau-ai` installs while preserving `TAU_AI_CLI_COMMAND_NAME` for release wrappers. Remaining gap is real OAuth e2e and real-feed/global install parity for installed binaries. |
| `packages/ai/src/oauth.ts` | `src/Tau.Ai/Auth/OAuth/*`, `ProviderAuthResolver.cs`, `OAuthCredentialStore.cs` | verified | Tau now exposes a complete local OAuth foundation surface for .NET consumers: registry, built-ins, credential store, auth resolution, provider info listing, register/unregister/reset semantics, and async refresh/get-api-key helpers. Direct tests plus the public API compile sample freeze the current surface; real provider login/refresh e2e remains tracked separately. |
| `packages/ai/src/bedrock-provider.ts` | `src/Tau.Ai/Providers/Bedrock/*`, `BuiltInProviders.cs` | partial | Tau loads Bedrock directly as a built-in provider and has injectable seams/tests. Upstream node-only subpath export wrapper is not package-shape equivalent. |
| `packages/ai/src/utils/event-stream.ts` | `src/Tau.Ai/Streaming/EventStream.cs`, `AssistantMessageStream.cs`, `EventStreamTests.cs` | verified | The channel-backed push/pull primitive's own contract is directly unit-covered: push/iterate ordering, completion on the terminal event, `ResultAsync` extraction, write-after-complete rejection, fault propagation to both the reader and `ResultAsync`, `End` short-circuit, and `AssistantMessageStream` Done/Error result mapping (including the synthesized error `AssistantMessage` from an `ErrorEvent` partial). This is a pure, deterministic in-memory primitive with no external-e2e dependency. Cross-provider terminal-event emission parity (every provider ending on the same `done`/`error` contract) remains tracked by the AI stream/event abstractions row and the provider `external-e2e-needed` rows. |
| `packages/ai/src/utils/json-parse.ts` | `src/Tau.Ai/Utilities/StreamingJsonParser.cs`, helper tests, public API sample; provider-specific parser/accumulator code under `src/Tau.Ai/Providers/**` | partial | Tau now exposes `StreamingJsonParser.ParseStreamingJson(...)`, calibrated against upstream `partial-json@0.1.7` for complete JSON, incomplete object/array/string/literal recovery, invalid/empty fallback `{}`, incomplete decimal drop and incomplete exponent base recovery. OpenAI-style, OpenAI Responses shared, Anthropic, Mistral and Bedrock streaming tool-call arguments now use the helper baseline for object raw-text partial/final arguments. Remaining gaps are exact TypeScript export naming, edge audit across future providers and real provider e2e. |
| `packages/ai/src/utils/sanitize-unicode.ts` | `src/Tau.Ai/Security/UnicodeTextSanitizer.cs`, `UnicodeTextSanitizerTests.cs`, request sanitizer tests | verified | Pure, deterministic, no-network surrogate sanitizer with direct unit coverage (15 tests) of the upstream contract: paired surrogates preserved, lone high/low removed (at start/end/middle), consecutive/reversed surrogates removed, valid pair preserved adjacent to a stripped surrogate, null/empty passthrough, and a post-condition invariant that no unpaired surrogate survives. Direct coverage uncovered and fixed a real production bug: `builder?.Append(text[++i])` short-circuited the `++i` advance when no unpaired surrogate preceded a valid pair, dropping the pair's low surrogate; the 8 provider integration tests missed it because they always place an unpaired surrogate before the emoji. Provider-level adoption stays covered by `ProviderRequestTextSanitizerTests`. |
| `packages/ai/src/utils/overflow.ts` | `src/Tau.Ai/Utilities/ContextOverflowDetector.cs`, `src/Tau.CodingAgent/Runtime/CodingAgentRetryOptions.cs`, `AiUtilityHelpersTests.cs`, public API sample | verified | Pure local overflow helper contract is directly covered: upstream provider error patterns, non-overflow exclusions, error-stopReason gate, silent usage overflow detection and pattern-source copy. CodingAgent retry/overflow classification reuses the same helper. Provider e2e remains tracked by provider rows, not by this deterministic utility row. |
| `packages/ai/src/utils/typebox-helpers.ts` | `src/Tau.Ai/Utilities/JsonSchemaHelpers.cs`, `AiUtilityHelpersTests.cs`, public API sample | verified | `JsonSchemaHelpers.StringEnum(...)` now matches the local .NET-native equivalent of upstream `StringEnum`: emits `{ type: "string", enum: [...] }`, optional description/default when truthy, omits empty optional fields, and keeps whitespace descriptions. Full TypeBox package re-export/runtime parity remains tracked by package/export/schema rows, not by this helper row. |
| `packages/ai/src/utils/validation.ts` | `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, `tests/Tau.Ai.Tests/ToolArgumentValidatorTests.cs`, Agent tool tests | partial | Tau now exposes `ToolArgumentValidator.ValidateToolCall(...)` / `ValidateToolArguments(...)` as an AI-level public helper and Agent reuses it for schema/error-result behavior. It covers object/array/required/properties/items/type coercion/enum/anyOf plus common AJV/TypeBox schema keywords: const, oneOf, allOf, additionalProperties, patternProperties, string length/pattern/format, numeric bounds/multipleOf, array size/uniqueItems and property count constraints. Full AJV/TypeBox keyword, runtime codegen and CSP fallback parity remains open. |
| `packages/ai/src/utils/headers.ts` | `src/Tau.Ai/Utilities/AiHeaderUtilities.cs`, `AiUtilityHelpersTests.cs`, public API sample | verified | Pure header conversion helper is directly covered for direct `HttpHeaders`, response headers, optional content-header inclusion and case-insensitive lookup. This is the .NET-native equivalent of upstream `headersToRecord`; provider-local request header precedence remains covered by provider/runtime config rows. |
| `packages/ai/src/utils/hash.ts` | `src/Tau.Ai/Utilities/ShortHash.cs`, `AiUtilityHelpersTests.cs`, public API sample | verified | `ShortHash.Compute(...)` matches upstream `shortHash` outputs for empty, ASCII, provider-style and Unicode inputs. Existing provider-specific private hash helpers remain behavior-specific implementation details outside this public utility row. |
| `packages/ai/src/utils/oauth/types.ts` | `src/Tau.Ai/Auth/OAuth/OAuthCredentials.cs`, `IOAuthProvider.cs`, `OAuthLoginCallbacks.cs` | verified | Core credential/callback/provider contracts are now directly exercised by provider tests, registry/helper tests and the public API compile sample. The shape is intentionally .NET-native rather than TypeScript-export equivalent, but the local contract is complete and stable. |
| `packages/ai/src/utils/oauth/pkce.ts` | `src/Tau.Ai/Auth/OAuth/OAuthPkce.cs`, `OAuthProviderTests.cs` | verified | RFC 7636 PKCE verifier/challenge generation is fully unit-covered with no external-e2e dependency: verifier decodes to 32 random bytes, emits 43-char base64url with only `[A-Za-z0-9-_]` (no `+`/`/`/`=`), challenge equals `BASE64URL(SHA-256(verifier))` (32 raw bytes → 43 base64url chars), and repeated generations stay unique and shape-stable. This pure, deterministic crypto helper has no upstream PKCE test, so `verified` reflects complete local RFC-conformance evidence for the surface. |
| `packages/ai/src/utils/oauth/oauth-page.ts` | `src/Tau.Ai/Auth/OAuth/OAuthCallbackServer.cs` and provider callback responses | partial | Local callback flow exists, but exact upstream success/error HTML template parity is not frozen. |
| `packages/ai/src/utils/oauth/index.ts` | `src/Tau.Ai/Auth/OAuth/OAuthProviderRegistry.cs`, `BuiltInOAuthProviders.cs` | verified | Built-in registry now has direct evidence for register/unregister/reset semantics, provider info listing, built-in restoration, custom provider registration, and async refresh/get-api-key helpers. `ProviderAuthResolver.GetStatus` still participates in the explicit consumer seam, so local package consumers can probe auth status against injected `auth.json` / `models.json` state while real OAuth e2e remains separate. |
| AI provider/OAuth e2e matrix | `packages/ai/src/providers/**`, `utils/oauth/**` | `scripts/verify-ai-provider-e2e-matrix.ps1`, `ProviderAuthResolver`, `ModelConfigurationStore`, provider tests | partial | Tau now has a repo-owned executable matrix contract for 11 default providers. `inspect` only reads status, `-Isolated` points auth/models at temporary empty files and blanks common provider env, and `-RunConfigured` is reserved for future minimal live calls against real credentials. `-RunConfigured -RequireConfigured` is the final-evidence guard: it fails when no provider is configured, no provider run is attempted, or the result is not a non-isolated all-success live run. The contract is wired into `plan-release.ps1` and `verify-release-contracts.ps1`; release contracts now also assert the required guard blocks an isolated/no-credential no-op. |
| `packages/ai/src/utils/oauth/anthropic.ts` | `src/Tau.Ai/Auth/OAuth/Providers/AnthropicOAuthProvider.cs`, tests | external-e2e-needed | Local authorization-code/PKCE and refresh baseline exists in code/tests; real Claude OAuth e2e and browser callback UX are not verified. |
| `packages/ai/src/utils/oauth/github-copilot.ts` | `src/Tau.Ai/Auth/OAuth/Providers/GitHubCopilotOAuthProvider.cs`, `src/Tau.Ai/Providers/GitHubCopilotHeaders.cs` | external-e2e-needed | Device-flow/token/header/model modification seams exist; enterprise/domain and real Copilot e2e need validation. |
| `packages/ai/src/utils/oauth/google-gemini-cli.ts` | `src/Tau.Ai/Auth/OAuth/Providers/GeminiCliOAuthProvider.cs`, `GoogleProjectDiscovery.cs` | external-e2e-needed | OAuth + project discovery baseline exists; real Google Cloud Code Assist login/refresh e2e remains open. |
| `packages/ai/src/utils/oauth/google-antigravity.ts` | `src/Tau.Ai/Auth/OAuth/Providers/AntigravityOAuthProvider.cs`, `GoogleProjectDiscovery.cs` | external-e2e-needed | Separate Antigravity credential flow exists; real Antigravity auth/model e2e remains open. |
| `packages/ai/src/utils/oauth/openai-codex.ts` | `src/Tau.Ai/Auth/OAuth/Providers/OpenAICodexOAuthProvider.cs`, `OpenAiCodexResponsesProvider.cs` | external-e2e-needed | OAuth/token parsing/refresh baseline exists and Codex Responses consumes account headers. Real ChatGPT/Codex OAuth e2e remains open. |
| `packages/ai/src/providers/register-builtins.ts` | `src/Tau.Ai/Providers/BuiltInProviders.cs`, `ModelApiNames.cs`, `ProviderRegistryTests.cs` | partial | Lazy registration exists for Tau's built-ins plus models.json dynamic OpenAI-compatible providers. Upstream API aliases are accepted at registry/config boundaries, so external callers can use `openai-completions`, `openai-compatible` and `google-generative-ai`; `setBedrockProviderModule` public hook and real built-in provider e2e are still not fully equivalent. |
| `packages/ai/src/providers/simple-options.ts` | `src/Tau.Ai/Abstractions/Options.cs`, provider-specific option mapping | verified | Tau now exposes the shared simple-options helper contract through `StreamOptionHelpers`: payload callback passthrough, response callback passthrough, reasoning clamp, shared thinking-budget selection, and terminal aborted assistant creation for shared cancellation handling. `SimpleStreamOptions.ThinkingBudgets` is public, `StreamFunctions` preserves the derived simple options through record cloning, and the major provider families consume the same helper. Tau keeps an extra `TopP` field, but the upstream `simple-options.ts` local contract is otherwise closed. |
| `packages/ai/src/providers/transform-messages.ts` | provider-specific converters under `src/Tau.Ai/Providers/*/*MessageConverter.cs` and `OpenAiResponsesShared.cs` | partial | Conversion behavior is implemented per provider. Need dedicated cross-provider transform contract tests before marking equivalent. |
| `packages/ai/src/providers/openai-completions.ts` | `src/Tau.Ai/Providers/OpenAi/OpenAiProvider.cs`, `OpenAiCompat/OpenAiCompatibleProvider.cs`, `ModelApiNames.cs`, OpenAI tests | partial | Chat-completions/OpenAI-compatible behavior has serialization and provider tests. Upstream API string `openai-completions` is now accepted by registry/config and normalized to Tau canonical `openai-chat-completions`; `OpenAiCompatibleProvider` removes an existing header before appending request headers, so explicit request headers can override provider/model-configured headers in the local consumer seam. Real provider e2e and exact TypeScript option surface remain open. |
| `packages/ai/src/providers/openai-responses.ts`, `openai-responses-shared.ts` | `src/Tau.Ai/Providers/OpenAiResponses/OpenAiResponsesProvider.cs`, `OpenAiResponsesShared.cs`, tests | external-e2e-needed | Responses SSE, tool calls, service tier, Copilot dynamic headers and image tool-result behavior have stub tests. `models.json options` can now supply `serviceTier`, `reasoningEffort` and `reasoningSummary` locally, with explicit typed options taking precedence. Real OpenAI/Copilot Responses e2e remains open. |
| `packages/ai/src/providers/openai-codex-responses.ts` | `src/Tau.Ai/Providers/OpenAiResponses/OpenAiCodexResponsesProvider.cs`, `CodexWebSocketTransport.cs`, tests | external-e2e-needed | SSE/WebSocket/auto transport, account headers and fallback paths have fake transport tests. `models.json options` can now supply `serviceTier`, `reasoningEffort`, `reasoningSummary` and `textVerbosity` locally. Real Codex Responses e2e remains open. |
| `packages/ai/src/providers/azure-openai-responses.ts` | `src/Tau.Ai/Providers/OpenAiResponses/AzureOpenAiResponsesProvider.cs`, tests | external-e2e-needed | Azure base URL/resource/deployment/api-version/api-key behavior has tests. `models.json options` can now supply `reasoningEffort`, `reasoningSummary`, `azureApiVersion`, `azureResourceName`, `azureBaseUrl` and `azureDeploymentName` locally. Real Azure OpenAI e2e remains open. |
| `packages/ai/src/providers/anthropic.ts` | `src/Tau.Ai/Providers/Anthropic/*`, Anthropic/provider sanitizer tests | external-e2e-needed | Messages API stream parser, OAuth/API-key headers, cache/thinking/tool conversion are partly covered by tests. Real Anthropic e2e and exact cache/Claude Code identity semantics remain open. |
| `packages/ai/src/providers/google.ts`, `google-shared.ts` | `src/Tau.Ai/Providers/Google/GoogleProvider.cs`, `GoogleMessageConverter.cs`, `GoogleStreamParser.cs`, `ModelApiNames.cs` | external-e2e-needed | Gemini API conversion/parser behavior has tests, and upstream API string `google-generative-ai` is now accepted by registry/config and normalized to Tau canonical `google-generative-language`. Upstream helper-level behavior and real Google provider e2e remain open. |
| `packages/ai/src/providers/google-gemini-cli.ts` | `src/Tau.Ai/Providers/Google/GoogleGeminiCliProvider.cs`, tests | external-e2e-needed | Cloud Code Assist headers, Antigravity fallback, retry delay extraction and empty SSE retry have tests. Real Google/Gemini CLI provider e2e remains open. |
| `packages/ai/src/providers/google-vertex.ts` | `src/Tau.Ai/Providers/Google/GoogleVertexProvider.cs`, `GoogleVertexAccessTokenResolver.cs`, tests | external-e2e-needed | API key, service-account JWT bearer and authorized-user refresh token paths have tests. Real Vertex e2e remains open. |
| `packages/ai/src/providers/mistral.ts` | `src/Tau.Ai/Providers/Mistral/MistralProvider.cs`, tests | external-e2e-needed | x-affinity, tool-call id normalization, usage, request serialization, string `toolChoice` and function `toolChoice` request payload shape have local tests. Real Mistral e2e remains open. |
| `packages/ai/src/providers/amazon-bedrock.ts` | `src/Tau.Ai/Providers/Bedrock/*`, Bedrock tests | external-e2e-needed | Tau covers bearer/SigV4, shared profile, credential process, ECS/IMDS/web identity, AssumeRole, SSO cache refresh/registration and eventstream parsing with tests. Real AWS Bedrock e2e and cache concurrency/edge behavior remain open. |
| `packages/ai/src/providers/github-copilot-headers.ts` | `src/Tau.Ai/Providers/GitHubCopilotHeaders.cs`, `GitHubCopilotHeadersTests.cs`, OpenAI Responses tests | verified | Pure, deterministic, no-network header helper with full direct unit coverage: static header values (`User-Agent`/`Editor-Version`/`Editor-Plugin-Version`/`Copilot-Integration-Id`), `InferInitiator` (empty list → user, last user → user, last assistant/toolResult → agent), `HasVisionInput` (user image, toolResult image, no image, and the upstream-faithful detail that an assistant-message image does NOT count), and `BuildDynamicHeaders` shape (`X-Initiator`/`Openai-Intent`, conditional `Copilot-Vision-Request`). Request-level wiring is additionally covered by `OpenAiResponsesProviderTests`. The exact upstream function-name surface is .NET-native; `verified` reflects complete local evidence with no external-e2e dependency (real Copilot auth/endpoint e2e is managed by other rows). |
| `packages/ai/src/providers/faux.ts` | `src/Tau.Ai/Providers/Faux/FauxProvider.cs`, `tests/Tau.Ai.Tests/FauxProviderTests.cs`, `AiPublicApiCompileSampleTests.cs` | verified | Public .NET-native Faux provider helper is fully local-covered with no external-e2e dependency: registration/unregistration, default/custom models, queued/factory responses, text/thinking/tool-call helpers, usage/cache estimates, streamed text/thinking/tool-call deltas, terminal error/exhaustion behavior, explicit terminal error/aborted assistant messages, `StreamOptions.OnResponse` callback with 200 response metadata, and `StreamOptions.Signal` pre-cancel / mid-stream cancel producing an aborted terminal assistant. The public API compile sample keeps the surface consumable by external .NET projects. Real provider/OAuth e2e and real cloud cancellation timing remain tracked by the shared options/provider rows. |

### AI Command / API / Env / Config / Log / Schema Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package exports and subpaths | `packages/ai/package.json` exports `.`, `./anthropic`, `./azure-openai-responses`, `./google`, `./google-gemini-cli`, `./google-vertex`, `./mistral`, `./openai-codex-responses`, `./openai-completions`, `./openai-responses`, `./oauth`, `./bedrock-provider`; `src/index.ts` re-exports TypeBox, registry, env, models, providers, stream, types, event stream, json parse, overflow, typebox helpers and validation. | `src/Tau.Ai` public namespaces and records; `src/Tau.Ai.Cli`; no subpath/package barrel concept in .NET; `AiPublicApiCompileSampleTests`; `Tau.Ai.Providers.Faux`; `StreamingJsonParser`; `docs/AI_AGENT_EXPORT_SHAPE.md`; `scripts/verify-ai-agent-export-shape.ps1` | verified | Core public types/providers have a .NET public API compile-sample covering messages/content/tools/models/options/usage, registry, stream functions/events, model helpers, auth/OAuth entry points, validation, helper utilities, streaming JSON parser and the public Faux provider. Standalone CLI baseline exists for help/list/login. `AI_AGENT_EXPORT_SHAPE.md` now records the Tau-native mapping for every upstream package export and index export group, including the explicit no-shim decision for TypeScript/npm and TypeBox same-name exports; `verify-ai-agent-export-shape.ps1` enforces that mapping locally. Real feed/global install and provider/OAuth e2e remain separate rows. |
| Standalone AI CLI | `packages/ai/package.json` bin `pi-ai`; `src/cli.ts` supports `login [provider]`, interactive provider selection and `list`, writing cwd `auth.json`. | `src/Tau.Ai.Cli` executable baseline; release artifact `bin/tau-ai.cmd` and `bin/pi-ai.cmd`; `scripts/verify-ai-cli-tool-install.ps1`; `OAuthCredentialStore` writes `TAU_AUTH_FILE`, `./.tau/auth.json`, or `~/.tau/auth.json`; `--auth-file auth.json` writes cwd-compatible auth explicitly. | partial | `tau-ai` help/list/login, provider selection, provider-list output, unknown-provider errors and credential save behavior are covered by tests and CLI smoke; release artifact smoke now verifies `tau-ai list`, `pi-ai list` and `pi-ai --help`, with wrapper alias usage text injected from the launched command name; local tool install smoke now verifies `pi-ai` / `tau-ai` dotnet tool shims from a temp package source. Remaining gap is real registry/global install parity, exact upstream wording if required, signing/provenance and real OAuth e2e. |
| Built-in API names | `types.ts` `KnownApi` and `register-builtins.ts`: `anthropic-messages`, `openai-completions`, `mistral-conversations`, `openai-responses`, `azure-openai-responses`, `openai-codex-responses`, `google-generative-ai`, `google-gemini-cli`, `google-vertex`, `bedrock-converse-stream`. | `BuiltInProviders.cs`: `anthropic-messages`, `openai-chat-completions`, `mistral-conversations`, `openai-responses`, `azure-openai-responses`, `openai-codex-responses`, `google-generative-language`, `google-gemini-cli`, `google-vertex`, `bedrock-converse-stream`; `ModelApiNames` maps `openai-completions` / `openai-compatible` / `google-generative-ai` to canonical Tau API names for registry/config dispatch. | partial | All families have Tau providers and upstream OpenAI/Google API aliases now work for local config/consumer dispatch. Internal canonical strings still differ from upstream, and real provider/OAuth e2e plus any future byte-equivalent TypeScript surface remain separate gaps. |
| Known provider ids and env keys | `types.ts` `KnownProvider`; `env-api-keys.ts` maps OpenAI, Azure, Google/Gemini, GitHub Copilot aliases, Anthropic OAuth/API key precedence, Vertex ADC, Bedrock ambient AWS, Groq, Cerebras, xAI, OpenRouter, Vercel AI Gateway, z.ai, Mistral, Minimax, HF, opencode, kimi. | `EnvironmentApiKeyResolver.cs` maps the same core provider ids and env vars, plus `GOOGLE_API_KEY` fallback for `google`; Bedrock/Vertex return `<authenticated>` markers for ambient credentials; Windows ADC lookup now includes `APPDATA\\gcloud\\application_default_credentials.json` before the Unix-style default path. | verified | The full implemented provider/env contract is now fixed by focused tests: all provider ids in the resolver have precedence or alias coverage, Vertex and Bedrock ambient markers are asserted, and the remaining browser/Vite-specific dynamic-import behavior does not apply to the .NET surface. |
| Shared stream options | `types.ts` `StreamOptions`: `temperature`, `maxTokens`, `signal`, `apiKey`, `transport`, `cacheRetention`, `sessionId`, `onPayload`, `onResponse`, `headers`, `maxRetryDelayMs`, `metadata`; `SimpleStreamOptions` adds `reasoning` and `thinkingBudgets`. | `Options.cs`: `Temperature`, `MaxTokens`, `TopP`, `ApiKey`, `Signal`, `OnPayload`, `OnResponse`, `Transport`, `CacheRetention`, `SessionId`, `Headers`, `MaxRetryDelay`, `Metadata`; `SimpleStreamOptions` has `Reasoning` and `ThinkingBudgets`. | verified | `StreamOptions` shared surface is now public in Tau as `Temperature`, `MaxTokens`, `TopP`, `ApiKey`, `Signal`, `OnPayload`, `OnResponse`, `Transport`, `CacheRetention`, `SessionId`, `Headers`, `MaxRetryDelay`, `Metadata`, and `SimpleStreamOptions` now carries `ThinkingBudgets` alongside `Reasoning`. `StreamFunctions` preserves the derived simple options through record cloning; provider tests plus public API sample exercise callback/budget behavior, and representative cancellation tests now prove pre-cancel/in-flight user cancellation emits an aborted terminal assistant across main provider families. The only deliberate divergence is Tau's additive `TopP` field, not a missing upstream member; real cloud callback/cancellation timing correlation remains external-e2e work. |
| Provider-specific options | Provider files export `BedrockOptions`, `AnthropicOptions`, `AzureOpenAIResponsesOptions`, `GoogleOptions`, `GoogleGeminiCliOptions`, `GoogleVertexOptions`, `MistralOptions`, `OpenAICodexResponsesOptions`, `OpenAICompletionsOptions`, `OpenAIResponsesOptions`. | Provider-specific C# option records/classes exist where needed: `BedrockOptions`, `AnthropicOptions`, `MistralOptions`, `OpenAiResponsesOptions`, `OpenAiCodexResponsesOptions`, `AzureOpenAiResponsesOptions`, plus model compatibility metadata and per-provider request builders. | partial | Important behavior is implemented in provider request builders/tests, and `ThinkingBudgets` now flows through the shared helper mapping. Anthropic and Mistral have .NET-native public option records for current mapped fields, but exact public option type parity and option names across every provider are still Tau-native rather than byte-for-byte upstream equivalents. |
| Auth file and credential schema | `src/cli.ts` reads/writes cwd `auth.json`; OAuth entries use `{ type: "oauth", refresh, access, expires/expiresAt, ...metadata }`; `utils/oauth/index.ts` has registry and refresh/get-api-key helpers. | `OAuthCredentialStore.cs` supports `TAU_AUTH_FILE`, `./.tau/auth.json`, `~/.tau/auth.json`, OAuth and `api_key` entries, metadata filtering, Unix `0600`; `ProviderAuthResolver.cs` resolves explicit/env/auth.json/models.json. | verified | Auth file load/save/remove, typed OAuth vs API-key parsing, reserved metadata filtering, Unix `0600` writeback, remove idempotence, expired-token refreshability and auth resolution are directly test-covered. Path semantics still differ from upstream `pi-ai` cwd auth file, but the local credential schema contract is complete. |
| Custom model/provider config | Upstream `types.ts` exposes OpenAI-compatible compat/routing metadata through `Model.compat`; generated model registry is source of provider/model data. | `ModelConfigurationStore.cs` reads `TAU_MODELS_FILE`, `./.tau/models.json`, `~/.tau/models.json`, merges provider/model overrides, resolves env/literal/`!command` credentials, normalizes upstream API aliases and dynamically registers explicit OpenAI-compatible providers. | partial | Tau has a concrete models.json runtime config path, including provider-only auth status for provider-level credentials and model-aware status for model-level credentials; status checks intentionally do not execute `!command` secrets or reveal configured values. Upstream package-level config UX is still not equivalently frozen. |
| Runtime log and inspection hooks | Upstream AI package has `onPayload` / `onResponse` callbacks and provider/OAuth `console.*` diagnostics, but no central JSONL runtime log surface. | `ITauLogSink`, `JsonlTauLogSink`, `TauRuntimeLogContext`, `TauSecretRedactor`; `ProviderAuthResolver.GetStatus` emits `auth/status.checked`; downstream CodingAgent/WebUi/Mom/Pods reuse the sink; `StreamOptions.OnPayload` / `OnResponse` exist for .NET consumers. | partial | Tau has stronger structured logging and default JSONL string-value redaction. It now exposes `onPayload` / `onResponse` on the public surface, exercises callback behavior across the main provider families, and locally proves `StreamOptions.Signal` cancellation produces an aborted terminal assistant instead of ordinary provider errors. It still lacks real provider/OAuth e2e correlation proof and real cloud callback/cancellation timing validation. |
| Tool schema validation | `utils/validation.ts` exports `validateToolCall` / `validateToolArguments` using AJV + TypeBox, with CSP fallback behavior. | `Tool` stores `JsonElement ParameterSchema`; `Tau.Ai.ToolArgumentValidator` exposes public `ValidateToolCall` / `ValidateToolArguments`, and `Tau.Agent` consumes the same helper. | partial | AI-level public helper baseline is now present and tested. Remaining parity gap is full AJV/TypeBox keyword coverage and CSP/runtime-codegen fallback semantics, which do not map 1:1 to .NET. |
| Helper utilities | `utils/json-parse.ts`, `headers.ts`, `hash.ts`, `overflow.ts`, `typebox-helpers.ts`, TypeBox re-export from `index.ts`. | `AiHeaderUtilities`, `ShortHash`, `ContextOverflowDetector`, `JsonSchemaHelpers`, `StreamingJsonParser`, provider-local parsers and `JsonElement` schemas. | partial | Tau now has public .NET-native helper baselines for headers, short hash, context overflow, string enum JSON schema and streaming incomplete JSON parsing, covered by `AiUtilityHelpersTests` and the public API sample. Primary streaming tool-argument parser adoption is now present for OpenAI-style, OpenAI Responses shared, Anthropic, Mistral and Bedrock. Still open: full TypeBox re-export parity, exact TypeScript helper naming/export shape decisions, edge audit across future providers and real provider e2e. |
| OAuth registry and built-ins | `utils/oauth/index.ts` exports built-ins, `registerOAuthProvider`, `unregisterOAuthProvider`, `resetOAuthProviders`, `getOAuthProviders`, `refreshOAuthToken`, `getOAuthApiKey`; built-ins cover Anthropic, GitHub Copilot, Gemini CLI, Antigravity and OpenAI Codex. | `OAuthProviderRegistry.cs`, `BuiltInOAuthProviders.cs`, concrete provider classes and `ProviderAuthResolver`; CodingAgent commands call the same resolver/store. | verified | The built-in registry and high-level helper contract is now locally complete: built-ins load by default, custom providers can register/unregister, built-ins are restored on reset/unregister, provider info listing is exposed, and refresh/get-api-key helpers are directly covered by tests and the public API compile sample. Real OAuth provider/browser/cloud e2e remains open. |
| Generated model/provider coverage | `models.generated.ts` includes many known provider ids, including OpenAI-compatible families beyond the provider implementations; package build runs `generate-models`. | `generated-models.seed.json`, `GeneratedBuiltInModels.g.cs`, `generate-tau-ai-models.ps1`, `ModelCatalog.cs`. | partial | Tau generated seed covers currently supported families but is not proven equivalent to upstream generated provider/model set; this remains separate from provider implementation e2e. |
| Error, abort and terminal stream contract | `types.ts` documents that stream functions should encode request/model/runtime failures in returned streams; providers also throw for some `streamSimple` setup errors such as missing API keys. | Tau providers emit `ErrorEvent`, high-level `Agent` facade appends synthetic assistant failure/cancel messages for stream fault and cancellation, `StopReason.Aborted` is mapped through proxy/OpenAI Responses/Mom normalization, low-level assistant stream cancellation emits aborted terminal events instead of faulting `RunStream`, and tool cancellation now emits terminal error tool results with pending-call cleanup. | partial | Core event model, facade failure/cancel synthesis, low-level assistant stream cancellation terminal coverage and tool cancellation cleanup exist. Exact thrown-vs-streamed provider error text, terminal event consistency across provider families and real e2e remain unverified. |

### Agent File-Level Matrix

| Upstream file | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/agent/src/agent.ts` | `src/Tau.Agent/Agent.cs`, `AgentState.cs`, `AgentRuntime.cs`, `tests/Tau.Agent.Tests/AgentFacadeTests.cs` | partial | `Agent` baseline now covers `AgentOptions`, state exposure, `Subscribe`, `PromptAsync`, `ContinueAsync`, steering/follow-up queues, queue clear, `HasQueuedMessages`, `Abort`, `WaitForIdleAsync`, `Reset`, prompt message lifecycle, listener settlement, and synthetic assistant messages for stream fault / cancellation. Still missing full abort signal plumbing parity, session/transport/thinking budget pass-through at facade level and every upstream state mutator edge. |
| `packages/agent/src/agent-loop.ts` | `src/Tau.Agent/Runtime/AgentRuntime.cs`, `ContextTransformer.cs`, `ToolExecutor.cs`, `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, `AgentEvents.cs`, queue/tool trace/contract tests | ported | Tau now has double loop, context transform/convert, sequential/parallel tools, before/after interceptors, runtime log trace, tool-result message lifecycle, `agent_end.messages`, `turn_end.message/toolResults`, tool update partial result, schema validation, error-as-tool-result for prepare/before/execute/after exceptions, high-level facade failure/cancel assistant synthesis, low-level `AgentRuntime.RunStream(...)` / `EventStream<AgentEvent, ChatMessage[]>` wrapper shape, exact parallel start/update timing, low-level assistant-stream cancellation terminal behavior, tool cancellation cleanup, parallel sibling cancellation result preservation and `TransformContext` cancellation parity. Remaining Agent work is now outside the loop contract itself. |
| `packages/agent/src/types.ts` | `AgentLoopConfig.cs`, `AgentState.cs`, `IAgentTool.cs`, `IToolInterceptor.cs`, `AgentEvents.cs`, `AgentQueueMode.cs` | partial | Core .NET records/interfaces exist and `AgentState` now includes system prompt/model/tools plus runtime fields. TypeScript declaration-merge custom message semantics are intentionally .NET-native; the runtime event payload contract is now locally covered, while some export-shape and facade option decisions remain open. |
| `packages/agent/src/proxy.ts` | `src/Tau.Agent/Proxy/ProxyStreamProvider.cs`, `tests/Tau.Agent.Tests/ProxyStreamProviderTests.cs`, `scripts/verify-agent-proxy-server-e2e.ps1` | verified | Request envelope, bearer header, text/tool/thinking event reconstruction, token-safe error handling, loopback HTTP/SSE server path, malformed SSE JSON and incomplete terminal event behavior have tests. Abort/cancellation remains covered by the AgentRuntime cancellation rows rather than this provider-options row. |
| `packages/agent/src/index.ts` | `src/Tau.Agent/Tau.Agent.csproj` public assembly surface; `scripts/verify-agent-package-consumer.ps1`; `docs/AI_AGENT_EXPORT_SHAPE.md`; `scripts/verify-ai-agent-export-shape.ps1` | verified | Public assembly exposes facade/runtime/types/proxy/platform by namespace instead of a TypeScript barrel. `AgentPublicApiCompileSampleTests` freezes facade/runtime/proxy/event consumption, the package consumer smoke freezes external `PackageReference Include="Tau.Agent"` consumption plus transitive `Tau.Ai` use, and the export-shape smoke fixes upstream `agent.js` / `agent-loop.js` / `proxy.js` / `types.js` mapping to Tau namespaces. Real registry/signing/provenance evidence remains a separate Phase 5 gate. |

## CodingAgent + Tui Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| CLI package/bin/config surface | `packages/coding-agent/src/cli.ts`, `src/config.ts`, `package.json` bin `pi` and `piConfig` | `src/Tau.CodingAgent/Program.cs`, command router/runtime/settings stores | partial | Tau has real CLI/RPC/settings/session paths. `--session <path>` explicit flat/jsonl file path selection, `--continue` / `--session-dir` local JSONL startup selection, `--fork <path>` direct JSONL tree session branch copy, `--session <id-prefix>` / `--fork <id-prefix>` local/session-dir JSONL id-prefix lookup, `--resume` local interactive selector, and `--models` / `--list-models` / `--verbose` startup model scope/listing wiring are locally covered with parser/runtime tests and CLI smokes. Packaging/bin/config-dir parity, session id / partial id global `listAll` lookup, cross-project fork prompt, and remaining session manager surfaces still need command-level closure. |
| Coding tools | `src/core/tools/*` | `src/Tau.CodingAgent/Tools/*`, `ReadFileToolTests.cs`, `ListDirectoryToolTests.cs`, tool/export tests | ported | Read/ls/edit/bash/glob/grep/write baseline exists with many upstream details; remaining gaps are custom renderers, image resize/clipboard and full TUI details. |
| JSONL session tree and lifecycle | `src/core/agent-session*`, tree/session/fork/clone/compact modules | `CodingAgentTreeSessionStore.cs`, `CodingAgentSessionSwitchCoordinator.cs`, tree/resume/metadata tests | partial | Broad Tau-native tree/session baseline exists; full upstream TreeSelector, branch-switch hooks, metadata inspector layout, multi-select and exact persisted schema parity remain open. |
| RPC mode | `src/modes/rpc/*` | `CodingAgentRpcHost.cs`, `CodingAgentRpcClient.cs`, `CodingAgentRpcExtensionUiBridge.cs`, RPC tests | partial | Tau covers prompt/steer/follow-up/settings/session/tree/bash/export commands, streamed bash output/lifecycle events, prompt response preflight timing, `get_state` pending queue count / compaction state timing, an upstream-shaped extension UI request/response bridge, real JS/TS extension fire-and-forget UI request emission, and a .NET RPC client helper with request/response correlation, event subscription, timeout and process-start contracts. Waiting extension UI dialogs from JS/TS handlers, full jiti/extension runner integration, full settings selector, terminal/packages and exact schema parity remain open. |
| Slash commands, prompts, skills and extensions | `src/core/slash-commands.ts`, `prompt-templates.ts`, `skills.ts`, `extensions/*` | `CodingAgentCommandRouter`, `CodingAgentPromptTemplateStore`, `CodingAgentSkillStore`, `CodingAgentExtensionCommandStore`, event bus tests | partial | Declarative command/resource/event baseline, limited `.js/.ts` `registerCommand` execution, limited `.js/.ts` `registerTool` schema/metadata + basic execute + prepareArguments baseline, `.js/.ts` `tool_call` block/input mutation / `tool_result` rewrite hook baseline, agent/message/turn/tool_execution lifecycle event broadcast non-blocking baseline, and a limited virtual package import stub subset for `@sinclair/typebox` plus selected `@mariozechner/*` packages exist; full jiti import/alias/virtualModules, rich tool content/renderers, session/provider/context/input/user_bash/model/resource/before hooks, resource selector and diagnostics parity remain open. |
| Settings/model/theme/thinking/auth selectors | settings/keybindings/theme/auth modules and interactive components | `CodingAgentSettingsStore/Selector`, `ModelSelector`, `ThemeStore/Selector`, `ThinkingSelector`, `AuthSelector`, tests | partial | Many Tau selectors and RPC fields exist; product-level `/settings select` now uses `TuiSettingsList` for the main settings surface and can write terminal/image/editor/changelog/telemetry fields. Full upstream submenus, package/install settings, transport/shell path/npm settings, runtime wiring for some persisted fields, terminal/image/markdown edit UI and OAuth dialog/session parity remain open. |
| OAuth login/logout UX | auth storage and OAuth flows under coding-agent + ai | `InteractiveOAuthLoginCallbacks.cs`, `/auth` `/login` `/logout`, `Tau.Ai/Auth/OAuth/*` | partial | Local provider selection/login/logout baseline exists; real OAuth e2e and credential refresh UX still belong to AI/CodingAgent closure. |
| Export/share/rich rendering | `src/core/export-html/*`, share/import helpers | `CodingAgentHtmlSessionExporter.cs`, `CodingAgentShareClient.cs`, HTML/export tests | partial | HTML transcript is rich but not upstream template/vendor parity; real `gh gist` share smoke and Tau share viewer remain unverified. |
| Package manager / install telemetry / changelog | `src/core/package-manager.ts`, `package-manager-cli.ts`, changelog startup behavior | `CodingAgentPackageManager`, `CodingAgentPackageCli`, `CodingAgentSettingsStore`, `CodingAgentStartupNoticeService`, `/changelog` | partial | Tau now has top-level package source `install/remove/uninstall/update/list/config`, user/project settings writes, upstream string/object `packages` persistence, local package resource discovery/filtering through package manifest, convention directories, glob includes, negation excludes and force include/exclude patterns, npm/git install/update/remove execution through an injectable command runner, startup `lastChangelogVersion` state, best-effort install telemetry, and local JavaScript/TypeScript extension `registerCommand` plus `registerTool` limited runtime for package/local `.js/.ts` modules including prepareArguments, `tool_call` input mutation / `tool_result` hook baseline, `registerFlag` / `registerShortcut` metadata/default/status baseline, CLI `--flag[=value]` value injection through `applyExtensionFlagValues`, interactive extension shortcut dispatch with reserved built-in keybinding skip, non-reserved built-in override diagnostics and local duplicate resolution, agent/message/turn/tool_execution lifecycle event broadcast with non-blocking handler errors logged as `extension/event.error`, and limited virtual package import stubs for `@sinclair/typebox`, `@mariozechner/pi-ai`, `@mariozechner/pi-ai/oauth`, `@mariozechner/pi-agent-core`, `@mariozechner/pi-tui` and `@mariozechner/pi-coding-agent`. Remaining gaps are interactive config selector, full jiti import/alias/virtualModules parity, rich tool content/renderers, session/provider/context/input/user_bash/model/resource/before hooks, final `pi` package/bin identity, real npm/git network smoke, package consumer smoke and real telemetry backend e2e. |
| TUI editor/input/autocomplete/history | `packages/tui/src/editor.ts`, `autocomplete.ts`, `keybindings.ts`, `kill-ring.ts`, `undo-stack.ts` | `InteractiveInputEditor.cs`, `InputHistoryStore.cs`, `TuiAutocompleteProvider.cs`, keybinding/editor tests | partial | Strong editor foundation exists; paste marker, redo/grouped undo, visual autocomplete popup and full editor box parity remain open. |
| TUI components/selectors/render diff | `components/*`, `select-list.ts`, `settings-list.ts`, `tui.ts` | `TuiSelectList`, `TuiMultiSelectList`, `TuiOverlayHost`, `TuiDiffRenderer`, `TuiTranscript*`, tests | partial | Component/render foundations exist and are wired into CodingAgent selectors; no complete upstream TUI app host/focus stack/settings-list parity yet. |
| Terminal host / images / markdown / loaders | `terminal.ts`, `stdin-buffer.ts`, `terminal-image.ts`, `components/image.ts`, `components/markdown.ts`, `components/loader.ts`, `components/cancellable-loader.ts` | `SystemConsoleTerminal`, `TuiProcessTerminal`, `TuiTerminalWriteLog`, `TuiInputSequenceBuffer`, `TuiAnsiRenderSurface`, `TuiTerminalImage`, `TuiImage`, `TuiLoader`, `TuiCancellableLoader`, `TuiMarkdown`, limited message/status helpers | partial | Stdin-buffer, ProcessTerminal lifecycle seam, loader/cancellable-loader, Markdown, terminal image and image component foundations now exist with targeted tests. Real TTY/PTY smoke, real kitty/iTerm/Windows terminal behavior, exact Marked/highlight integration, hardware cursor and live terminal image smoke remain open. |

### CodingAgent File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/coding-agent/package.json` | `src/Tau.CodingAgent/Tau.CodingAgent.csproj`, `Tau.slnx`, `scripts/verify-dotnet.ps1` | partial | Tau has a .NET console project and solution build path. Missing final `pi`-compatible packaged command, package export shape, binary asset copy pipeline, theme/export-html/vendor asset packaging and release artifact closure. |
| `src/index.ts` | Public C# namespaces in `src/Tau.CodingAgent/Runtime/**`, `src/Tau.CodingAgent/Tools/**` | partial | Tau exposes concrete stores/runners/selectors/tools by namespace. Need public API compile samples or explicit SDK export mapping for upstream session/auth/model/extension/resource/settings exports. |
| `src/cli.ts`, `src/main.ts`, `src/cli/args.ts` | `Program.cs`, `CodingAgentHost.cs`, `CodingAgentPrintMode.cs`, `CodingAgentRpcHost.cs`, `CodingAgentCliHelp.cs` | partial | Interactive, print and RPC baselines exist. `--help`/`-h` and `--version`/`-v` now print help (with extension-registered CLI flags listed) or the resolved assembly version and exit 0, mirroring upstream `main.ts`/`printHelp`; command name resolves to `pi` (overridable via `TAU_CODING_AGENT_COMMAND_NAME`). `--system-prompt` and repeatable `--append-system-prompt` now resolve each value as an existing file path (read contents) or literal text and append `\n\n`-joined text to the generated/custom system prompt (preserved across `/reload`), mirroring upstream `resolvePromptInput`/`buildSystemPrompt`. `--no-tools` / `--tools <list>` now gate the built-in tool set (CLI names `read,bash,edit,write,grep,find,ls` map to Tau `read_file,shell,edit_file,write_file,grep,glob,ls`; extension tools always load) and `parseArgs`-style diagnostics now surface: unknown `--tools` names warn, invalid `--thinking` levels warn, unknown single-dash options error and exit 1, mirroring upstream `main.ts` diagnostic handling. `--thinking <off|minimal|low|medium|high|xhigh>` now sets the initial reasoning level with precedence over the persisted `defaultThinkingLevel`, clamped to the model's capability. `--offline` now sets `PI_OFFLINE=1` early so the package manager and startup notice skip network operations, mirroring upstream `main.ts`. `--export <file.jsonl> [output.html]` now reads a JSONL session file, renders the standalone HTML transcript and exits (default output `pi-session-<input>.html`), mirroring upstream `exportFromFile`. `--api-key <key>` now injects a runtime API key for the resolved model's provider via `StreamOptions.ApiKey` (highest precedence over env/auth.json), mirroring upstream `authStorage.setRuntimeApiKey`. `--session <path>` explicit file paths now select flat or JSONL tree sessions; `--continue` and `--session-dir <dir>` now select/create local JSONL tree sessions at startup; `--fork <path>` now rejects conflicting session flags and copies a direct JSONL tree session current branch into a new session under explicit `--session-dir` or default `coding-agent-sessions`; `--session <id-prefix>` and `--fork <id-prefix>` now resolve local/session-dir JSONL session id prefixes before opening or forking; `--resume` now opens a local interactive session selector and rejects print/RPC mode with a clear diagnostic. `--models <patterns>` now provides per-process scoped model cycling with exact ids, `:<thinking>` suffixes and `*` / `?` wildcard expansion; `--list-models [search]` prints a model table and exits before opening a session; `--verbose` forces model-scope startup notice despite `quietStartup`. Remaining top-level parity gaps include session id / partial id global `listAll` lookup, cross-project fork prompts, complete SessionManager schema/selection, complete fuzzy model scope parity, plus the upstream default-tool-set divergence (Tau enables all built-ins by default vs upstream `read,bash,edit,write`). |
| `src/config.ts` | `CodingAgentSettingsStore.cs`, `CodingAgentTreeSessionStore.cs`, `CodingAgentChangelogStore.cs`, `CodingAgentShareClient.cs`, `CodingAgentThemeStore.cs` | partial | Tau has `.tau` defaults and `TAU_CODING_AGENT_*` env vars. Upstream `PI_CODING_AGENT_DIR`, `PI_PACKAGE_DIR`, `PI_SHARE_VIEWER_URL`, `~/.pi/agent/**`, packaged asset paths and `piConfig` identity are not equivalent. |
| `src/bun/cli.ts`, `src/bun/register-bedrock.ts` | .NET host/release path; `Tau.Ai` provider registration | non-goal-proposed | Bun runtime wrapper/register hook should likely be replaced by .NET release artifact/startup behavior. Needs explicit Phase 5 decision before closure. |
| `src/package-manager-cli.ts`, `src/core/package-manager.ts`, `src/cli/config-selector.ts`, `src/utils/git.ts`, `src/utils/tools-manager.ts` | `CodingAgentPackageManager.cs`, `CodingAgentExtensionCommandStore.cs`, `CodingAgentExtensionResourceState.cs`, `CodingAgentSettingsStore.cs` | partial | Tau can persist package sources, list user/project packages, print config/resource summaries, resolve and filter local package extension/skill/prompt/theme resources from `package.json` `pi` manifest, convention directories, glob includes, negation excludes and exact force include/exclude patterns into the existing stores, execute npm/git install/update/remove with fake-runner contract tests, and load package/local `.js/.ts` extension modules far enough to register slash commands via `pi.registerCommand`, tools via limited `pi.registerTool` metadata/basic execution plus prepareArguments, and tool call/result hook handlers via `pi.on("tool_call" | "tool_result")` including `event.input` mutation; `.ts` uses Node type-stripping hooks and is covered for package-loaded `node_modules` paths with erasable TypeScript; current virtual package import stubs cover `@sinclair/typebox` and selected `@mariozechner/*` extension helper packages. Remaining gaps are hosted-git edge parity beyond common `git:`/HTTPS/SSH forms, download manager, interactive package resource selector, real network smoke, full jiti import/alias/virtualModules parity and full non-command extension APIs beyond the basic tool hook baseline. |
| `src/migrations.ts`, `packages/coding-agent/scripts/migrate-sessions.sh` | `CodingAgentTreeSessionStore.cs`, `CodingAgentSettingsStore.cs`, `scripts/migrate-coding-agent-auth.ps1`, `scripts/verify-coding-agent-auth-migration.ps1`, `scripts/migrate-coding-agent-sessions.ps1`, `scripts/verify-coding-agent-session-migration.ps1`, `scripts/migrate-coding-agent-commands.ps1`, `scripts/verify-coding-agent-commands-migration.ps1`, `scripts/migrate-coding-agent-tools-to-bin.ps1`, `scripts/verify-coding-agent-tools-to-bin-migration.ps1`, `scripts/audit-coding-agent-deprecated-extension-dirs.ps1`, `scripts/verify-coding-agent-deprecated-extension-dirs-audit.ps1`, `scripts/migrate-coding-agent-keybindings.ps1`, `scripts/verify-coding-agent-keybindings-migration.ps1` | partial | Tau reads current flat JSON and JSONL tree formats and compatibility fields, has a dry-run/apply auth migration helper for legacy `oauth.json` and `settings.json.apiKeys` into `auth.json`, has a Tau-native helper for root misplaced JSONL relocation into discoverable `coding-agent-sessions/<encoded-cwd>/` directories, has a dry-run/apply helper for legacy `commands/` -> `prompts/` base directory migration, has a dry-run/apply helper for managed `fd`/`rg` binaries from `tools/` to `bin/`, including target-exists old-source removal, has a no-mutation deprecated `hooks/` plus custom `tools/` warning audit helper that ignores managed fd/rg binaries and hidden entries, and now has a dry-run/apply helper for legacy object-style keybinding id migration to canonical `tui.*` / `app.*` ids. Missing full settings/runtime auth parity, credential refresh UX, exact session schema parity, actual hooks/custom tools migration to `extensions/`, full jiti/custom tool extension runtime, package/extension shortcut migration and general custom tool migration parity. |
| `src/modes/index.ts`, `src/modes/print-mode.ts` | `CodingAgentPrintMode.cs`, `Program.cs`, `CodingAgentPrintModeTests.cs` | ported | Tau has explicit print mode path through `--print` / `-p`. Remaining differences are CLI flag breadth and output mode schema. |
| `src/modes/rpc/*` | `CodingAgentRpcHost.cs`, `CodingAgentRpcClient.cs`, `CodingAgentRpcExtensionUiBridge.cs`, RPC tests | partial | Tau handles LF-delimited JSON, prompt/steer/follow-up/abort/new session/state/settings/model/thinking/retry/bash/session/messages/commands/export paths, prompt accepted/error preflight response timing, `get_state.pendingMessageCount` / `isCompacting` state timing, streamed bash output/lifecycle events, `get_session_stats` token/cost totals, extension UI request/response bridge methods for select/confirm/input/editor/notify/status/widget/title/editor-text, and a .NET RPC client helper aligned with upstream `rpc-client.ts` for spawning, generated `req_N` ids, pending response correlation, event dispatch, stderr-backed timeouts and typed command helpers. Missing exact TypeScript response schema, full extension runtime integration and richer command/settings/package/terminal contracts. |
| `src/modes/interactive/interactive-mode.ts`, `src/modes/interactive/components/**` | `CodingAgentHost.cs`, `Tau.Tui` interactive/composition/session classes, CodingAgent selectors | partial | Tau has a real interactive loop, transcript composition, selector paths, hotkeys and host state. Missing exact upstream TUI component layout for message/tool/bash/diff/tree/session/settings/oauth/theme/footer/borders/loaders, extension input/editor UI, decorative announcement components and complete focus stack behavior. |
| `src/modes/interactive/theme/**`, `src/modes/interactive/assets/**` | `CodingAgentThemeStore.cs`, `CodingAgentThemeSelector.cs`, `CodingAgentSettingsStore.cs` | partial | Theme discovery/selection exists with tests. Exact upstream theme schema, live watcher, packaged theme/asset path semantics, all color tokens and bitmap asset packaging remain open. |
| `src/core/agent-session*.ts`, `session-manager.ts`, `session-cwd.ts` | `CodingAgentTreeSessionStore.cs`, `CodingAgentSessionStore.cs`, `CodingAgentSessionSwitchCoordinator.cs`, `RuntimeCodingAgentRunner.cs` | partial | Tau has flat snapshot plus JSONL tree, resume/fork/clone/metadata/parent session metadata/runtime sync and a local agent/message/turn/tool_execution extension lifecycle broadcast non-blocking baseline. Missing upstream session directory encoding, session switch/compact/tree/provider/context/input/user_bash/before hooks, cwd override prompts and byte-equivalent JSONL schema. |
| `src/core/auth-storage.ts`, `model-registry.ts`, `model-resolver.ts`, `defaults.ts`, `cli/list-models.ts` | `Tau.Ai` auth/model catalog, `CodingAgentModelAvailability.cs`, `CodingAgentCommandRouter.cs`, `CodingAgentModelListFormatter.cs` | partial | Runtime provider/model/auth selection is rich. CLI `--api-key <key>` now injects a runtime key for the resolved model's provider via `StreamOptions.ApiKey` (precedence over env/auth.json). CLI `--list-models [search]` now lists ModelCatalog rows with provider/model/context/max-out/thinking/images before opening a session, and `--models` now supports exact ids, `:<thinking>` suffixes and `*` / `?` wildcard expansion for startup/cycle scope. Missing exact `~/.pi/agent/auth.json` path, settings API key migration, full upstream fuzzy model scope behavior and full upstream model coverage. |
| `src/core/tools/**` | `src/Tau.CodingAgent/Tools/**`, `tests/Tau.CodingAgent.Tests/*ToolTests.cs` | ported | Read/bash/edit/write/grep/find/glob/list and truncation/path-safe behavior are implemented with targeted tests. Remaining gaps live around custom extension tool wrapping/rendering, image/clipboard ingestion and exact mutation queue/display semantics. |
| `src/core/bash-executor.ts` | `CodingAgentShellRunner.cs`, `ShellTool.cs`, RPC bash tests | partial | Cancellable shell runner and RPC bash/abort exist. Missing upstream shell config/streaming/profiling and exact result schema. |
| `src/core/compaction/**` | `RuntimeCodingAgentRunner.CompactAsync`, `SummarizeBranchAsync`, `CodingAgentAutoCompactionOptions.cs`, tree store tests | partial | Manual compaction, auto threshold, context-overflow compact/retry and branch summary entries exist. Missing extension compaction hooks, exact split-turn summarization behavior, cancellation UI and full JSONL schema parity. |
| `src/core/slash-commands.ts`, `prompt-templates.ts`, `skills.ts`, `resource-loader.ts`, `source-info.ts` | `CodingAgentCommandCatalog.cs`, `CodingAgentCommandRouter.cs`, prompt/skill/context stores | partial | Tau has built-in slash catalog, prompt templates, skills and context files. Missing exact upstream command semantics, source metadata/collision model and interactive resource selector. |
| `src/core/extensions/**` | `CodingAgentExtensionCommandStore.cs`, `CodingAgentExtensionToolAdapter.cs`, `CodingAgentExtensionEventBus.cs`, `CodingAgentExtensionResourceState.cs`, `CodingAgentJavaScriptExtensionRuntime.cs`, `CodingAgentExtensionLifecycleEventSink.cs`, `CodingAgentRpcExtensionUiBridge.cs` | partial | Declarative JSON extension commands/resources/diagnostics, TypeScript/JavaScript module entry discovery/status reporting, local `.js/.ts` `registerCommand` execution, limited `.js/.ts` `registerTool` schema/metadata + basic execute + prepareArguments adapter, `.js/.ts` `tool_call` block/input mutation / `tool_result` rewrite hooks through `IToolInterceptor`, and an in-process event bus exist. `.ts` modules use Node type-stripping hooks and are covered for relative `.ts` import plus package-loaded `node_modules` paths; extension tools are merged into runner startup tools, can override same-name built-ins, can prepare raw tool call args before schema validation, can mutate tool input after validation without revalidation, can block or rewrite tool execution results, can register flag metadata/defaults with local `getFlag` plus CLI `--flag[=value]` value injection through `applyExtensionFlagValues`, can register shortcut metadata/status that is resolved against interactive keybindings and dispatched to JS/TS handlers, can receive read-only `agent_start/end`, `turn_start/end`, `message_start/update/end` and `tool_execution_start/update/end` events from the production runner, can emit fire-and-forget RPC UI requests for notify/status/string-array widget/title/editor text from real JS/TS handlers, and can import the current limited virtual package stub subset for `@sinclair/typebox` plus selected `@mariozechner/*` helpers. Handler errors are logged as `extension/event.error` without failing the run. Missing full jiti import/alias/virtualModules parity, rich content/renderers/details semantics, session/provider/context/input/user_bash/model/resource/before hooks, lifecycle runner/wrapper, waiting extension UI dialogs, custom UI, interactive widget factories and live `/reload` tool hot-swap. |
| `src/core/settings-manager.ts`, `keybindings.ts`, `messages.ts`, `system-prompt.ts`, `footer-data-provider.ts`, `output-guard.ts`, `diagnostics.ts`, `timings.ts`, `telemetry.ts`, `exec.ts`, `sdk.ts` | `CodingAgentSettingsStore.cs`, `CodingAgentPackageManager.cs`, `CodingAgentStartupNoticeService.cs`, `Tau.Tui` keybindings, `RuntimeCodingAgentRunner.cs`, `CodingAgentFileOperationTracker.cs`, runtime log sink | partial | Tau has key settings, context/system prompt construction, file tracking, runtime logs, package source persistence/resource merge baseline, startup version state and best-effort install telemetry. Missing full user/project settings deep merge outside package resources, image/editor/terminal runtime wiring, footer git/extension data provider, output guard, startup diagnostics/timings edge parity, real telemetry backend e2e and SDK programmatic facade parity. |
| `src/core/export-html/**` | `CodingAgentHtmlSessionExporter.cs`, export/share tests | partial | Tau exports standalone HTML with JSONL/branch outline/rich message rendering. Missing upstream template/vendor files, exact marked/highlight behavior, custom tool renderer contract and real share viewer parity. |
| `src/utils/clipboard*`, `image-*`, `exif-orientation.ts`, `mime.ts`, `photon.ts`, `paths.ts`, `shell.ts`, `child-process.ts`, `sleep.ts`, `frontmatter.ts`, `changelog.ts` | `SystemCodingAgentClipboard.cs`, `ReadFileTool.cs`, `CodingAgentInitialMessageBuilder.cs`, `CodingAgentImagePreprocessor.cs`, `CodingAgentHtmlSessionExporter.cs`, `CodingAgentShellRunner.cs`, prompt/skill parsers, `CodingAgentChangelogStore.cs`, `CodingAgentStartupNoticeService.cs` | partial | Text clipboard, shell/path/frontmatter-like helpers, `/changelog`, startup changelog/version notice baseline, ReadFileTool image attachment baseline and CLI `@file` initial prompt baseline now exist. CLI preprocessing handles text files plus PNG/JPEG/GIF/WebP header-sniffed images, prepends redirected stdin, honors image blocking and rejects `@file` in RPC mode; shared image preprocessing now covers local PNG resize to upstream default 2000x2000 / 4.5MiB headroom, dimension notes, read_file auto-resize wiring and JPEG/WebP EXIF orientation dimension metadata. Missing native/OSC52/image clipboard matrix, full JPEG/GIF/WebP Photon resize/convert quality parity, terminal PNG conversion, shared utility API parity and exact upstream changelog entry slicing. |

### CodingAgent CLI / RPC / Env / Config / Log / Schema Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/bin identity | `package.json` `piConfig.name=pi`, `piConfig.configDir=.pi`, bin `pi=dist/cli.js` | `src/Tau.CodingAgent/Tau.CodingAgent.csproj`, `dotnet run --project src/Tau.CodingAgent` | partial | App runs as .NET project/executable. Missing final `pi` command, `.pi` config identity, package export shape and binary asset closure. |
| Top-level CLI modes and flags | `cli/args.ts`, `main.ts`, `package-manager-cli.ts` | `Program.cs`, `CodingAgentPrintMode.cs`, `CodingAgentRpcHost.cs`, `CodingAgentPackageManager.cs`, `CodingAgentCliHelp.cs` | partial | Interactive/print/RPC exist. Top-level package `install/remove/uninstall/update/list/config` now handles package source settings before runner startup, with user/project scope, local resource resolution baseline and npm/git command execution baseline. `--help`/`-h` and `--version`/`-v` now print help (with extension CLI flags) or version and exit 0. `--system-prompt` and repeatable `--append-system-prompt` now resolve each value as a file path (read contents) or literal text and append to the generated/custom system prompt, surviving `/reload`. `--tools`/`--no-tools` now gate which built-in tools load (CLI names mapped to Tau tool names; extension tools always load), and unknown `--tools` names plus unknown single-dash options now surface upstream-style warning/error diagnostics with error exit 1. Missing `--mode json`, session/model/resource/offline/verbose runtime wiring, default-tool-set divergence (Tau enables all built-ins by default vs upstream read/bash/edit/write), interactive config selector parity and real package/network smoke. |
| Session, settings, auth and model config schema | `config.ts`, `auth-storage.ts`, `settings-manager.ts`, `model-registry.ts`, `session-manager.ts` | `.tau` session/settings/auth/model stores plus `TAU_CODING_AGENT_*`, `TAU_AUTH_FILE`, `TAU_MODELS_FILE` | partial | Working Tau-native persistence exists. Settings now reads/writes upstream string/object `packages` source shape, preserves it across settings saves and persists `lastChangelogVersion` for startup notices. Upstream `~/.pi/agent/**`, encoded-cwd session dirs, remaining exact settings fields, migrations and broader schema parity remain open. |
| Slash commands and local resources | `slash-commands.ts`, prompt/skill/extension modules, package resources | `CodingAgentCommandCatalog`, router, prompt/skill/extension stores, `CodingAgentPackageManager` | partial | Tau has broad built-in command baseline and declarative resources. Local package sources now feed extension/prompt/skill/theme paths through package manifest or convention directories; extension paths are resolved as upstream-style `.ts/.js` module entries, directory `index.ts/js`, `package.json pi.extensions` entries, plus Tau JSON command files, and `/reload` refreshes package resources before extensions. JavaScript/TypeScript `pi.registerCommand` slash commands are now visible in `/extensions` and invokable through the local store; JavaScript/TypeScript `pi.registerTool` tools are visible in extension status, merged into runner startup tools for basic execution, can run `prepareArguments` before schema validation, can use `tool_call` input mutation / `tool_result` rewrite hooks, can report `registerFlag` / `registerShortcut` metadata/default/status through `/extensions` plus CLI `--flag[=value]` value injection, interactive sessions can dispatch resolved extension shortcuts to JS/TS handlers after applying reserved built-in skip and non-reserved built-in override diagnostics, and production runner emits read-only agent/message/turn/tool_execution lifecycle events to JS/TS `pi.on(...)` handlers. Missing full jiti/custom tool semantics, rich tool content/renderers, session/provider/context/input/user_bash/model/resource/before hooks, real npm/git package-installed resource smoke, full resource selector and exact built-in command semantics. |
| RPC command/state/bash schema | `modes/rpc/rpc-types.ts`, `rpc-mode.ts`, `rpc-client.ts`, `bash-executor.ts` | `CodingAgentRpcHost.cs`, `CodingAgentRpcClient.cs`, `CodingAgentShellRunner.cs`, RPC tests | partial | Core headless commands, streamed bash output/lifecycle events, a client-side request/response/event helper, upstream `message_update.assistantMessageEvent` stream-event shape baseline, RPC `ToolCall.arguments` object-shape baseline, and `get_state.pendingMessageCount` / `isCompacting` timing baseline exist. Remaining gaps include full response/message schema parity outside the covered tool call argument shape, full extension runner integration and richer command/settings/package/terminal contracts. |
| RPC extension UI protocol | `RpcExtensionUIRequest` select/confirm/input/editor/notify/status/widget/title/editor-text; `RpcExtensionUIResponse` value/confirmed/cancelled | `CodingAgentRpcExtensionUiBridge.cs`, `CodingAgentRpcHost.cs`, `CodingAgentJavaScriptExtensionRuntime.cs`, RPC tests | partial | Tau now has an upstream-shaped JSONL bridge: bridge callers can emit `extension_ui_request` for select/confirm/input/editor/notify/setStatus/setWidget/setTitle/set_editor_text, and RPC stdin `extension_ui_response` resolves pending value/confirmed/cancelled requests without producing command responses. Real JS/TS extension handlers now also emit fire-and-forget `notify/setStatus/setWidget/setTitle/set_editor_text` requests through the bridge. Waiting `select/confirm/input/editor` from JS/TS handlers, custom UI, interactive widget factories and full extension runner integration remain open. |
| Image/attachment ingestion | `@file` processing, clipboard image, resize/convert/EXIF/mime pipeline, image settings | `CodingAgentInitialMessageBuilder.cs`, `CodingAgentImagePreprocessor.cs`, `Program.cs`, `CodingAgentPrintMode.cs`, `CodingAgentHost.cs`, `ReadFileTool.cs` | partial | Tau now processes CLI `@file` text and PNG/JPEG/GIF/WebP image attachments into the initial prompt, passes image initial prompts through `ContentBlock` in print and interactive startup paths, prepends redirected stdin, rejects `@file` in RPC mode, honors `images.blockImages`, and routes both CLI `@file` and `read_file` through a shared image preprocessor. The local preprocessor covers PNG resize to upstream 2000x2000 / 4.5MiB headroom, dimension notes, JPEG/WebP EXIF orientation dimension metadata and startup settings `images.autoResize` wiring into `ReadFileTool`. Remaining gaps: clipboard image paste, full Photon resize/convert quality parity for JPEG/GIF/WebP and terminal PNG conversion, provider vision e2e, full TUI attachment/editor path and exact image setting/runtime terminal behavior. |
| Share/export/release assets | `--export`, `/share`, `PI_SHARE_VIEWER_URL`, export templates/vendor files, binary copy assets | `/export`, `/share`, CLI `--export`, `CodingAgentSessionFileExporter`, `CodingAgentHtmlSessionExporter`, `CodingAgentShareClient`, `TAU_SHARE_VIEWER_URL` | partial | HTML/JSONL export exists for both `/export` and CLI `--export <file.jsonl> [output.html]` (reads a JSONL session, renders standalone HTML, exits; default output `pi-session-<input>.html`), mirroring upstream `exportFromFile`. Missing upstream template/vendor parity, real `gh gist` smoke and packaged release assets. |
| Runtime logs and redaction | debug log path, console diagnostics, RPC JSONL stdout | `JsonlTauLogSink`, `TauRuntimeLogContext`, JSONL tree redaction, `TAU_CODING_AGENT_REDACT_SECRETS` | partial | Tau has stronger structured log/redaction foundation. Missing upstream debug.log semantics, exact process event schema and cross-module correlation e2e. |
| External provider/auth/share e2e | real provider env/auth/OAuth, `gh gist` share | `Tau.Ai` providers/auth, CodingAgent selectors/share client | external-e2e-needed | Local contracts exist; real providers/OAuth/Copilot/Codex and GitHub gist smoke remain final blockers. |

### Tui File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/tui/src/index.ts` | `src/Tau.Tui/Tau.Tui.csproj`, `src/Tau.CodingAgent/Program.cs` | partial | Tau exposes a .NET library and CodingAgent consumes it directly. No TS barrel/package export shape or standalone `pi-tui` entry. |
| `utils.ts`, `fuzzy.ts` | `TuiText.cs`, `TuiFuzzyMatcher.cs`, `TuiComponentTests.cs`, `TuiFuzzyMatcherTests.cs` | ported | Visible width, truncation, wrapping and fuzzy match/filter have direct implementations and tests. |
| `editor-component.ts` | `ITuiComponent.cs`, `ITuiInputComponent.cs`, `IInteractiveRenderer.cs`, `IConsoleKeyReader.cs` | ported | Renderable/input component and renderer/key-reader boundaries exist and are used by editor/overlay/session layers. |
| `keys.ts`, `keybindings.ts` | `EditorAction.cs`, `KeyBindingMap.cs`, `SystemConsoleInputDevices.cs`, keybinding tests | partial | Default and override keybindings exist. Missing full upstream key decode, Kitty protocol state and key release/repeat semantics. |
| `stdin-buffer.ts`, `undo-stack.ts`, `kill-ring.ts` | `TuiInputSequenceBuffer.cs`, `InputBuffer.cs`, `InteractiveInputEditor.cs`, editor tests | partial | Independent stdin buffer foundation now covers complete/incomplete CSI/OSC/DCS/APC/SS3/meta sequences, SGR mouse, bracketed paste content, high-byte meta conversion, flush/clear/timeout/destroy behavior. Draft/commit, undo and kill/yank behavior exist inside editor, but there are still no independent undo stack / kill ring public modules. |
| `autocomplete.ts` | `TuiAutocompleteProvider.cs`, `CodingAgentAutocompleteProviderFactory.cs`, `InteractiveInputEditor.cs` | partial | Slash/path completion is wired into editor. Missing full visual autocomplete popup component contract. |
| `components/text.ts`, `truncated-text.ts`, `box.ts`, `spacer.ts` | `TuiTextBlock.cs`, `TuiTruncatedText.cs`, `TuiBox.cs`, `TuiContainer.cs`, `TuiSpacer.cs`, `TuiText.cs` | ported | Local component baselines now cover the named upstream files: `TuiTextBlock` covers Text empty-text skip, tab-normalized wrap, padding, full-line background formatter and text/width cache invalidation; `TuiTruncatedText` covers first-line-only truncation, horizontal/vertical padding, empty-text padded output and width-aware ellipsis; `TuiBox` covers child rendering, padding, optional background formatter, `SetBackgroundFormatter(...)` and bg-sample/child-line cache invalidation; `TuiSpacer` covers default one-line render, dynamic line count updates, negative-count empty output and container composition. Complete TUI host/focus stack, theme rendering and terminal runtime gaps remain tracked in separate rows. |
| `components/editor.ts`, `components/input.ts` | `InteractiveInputEditor.cs`, `SystemConsoleInteractiveRenderer.cs`, `InputHistoryStore.cs` | partial | History, reverse search, kill/yank and multiline are implemented. Not a literal upstream editor/input widget port. |
| `components/select-list.ts` | `TuiSelectList.cs`, `TuiSelectorSession.cs`, `TuiOverlayHost.cs`, CodingAgent selectors | ported | Single-select filtering/navigation/overlay and selector wiring have tests. |
| `components/settings-list.ts` | `TuiSettingsList.cs`, `TuiSettingsListSession.cs`, `TuiMultiSelectList.cs`, `TuiMultiSelectSession.cs`, `CodingAgentSettingsSelector.cs`, `CodingAgentScopedModelsSelector.cs` | partial | `TuiSettingsList` now covers the upstream generic settings-list library baseline: label/value alignment, description wrapping, optional search, Enter/Space value cycle, submenu delegate/done callback, cancel/change callbacks, scroll indicator and width truncation. Product-level CodingAgent `/settings select` now uses it for the main settings-list surface and writes upstream-shaped terminal/image/editor/changelog/telemetry fields. Full upstream submenus, package/transport/shell path settings, runtime wiring for some persisted fields and complete settings UI parity remain open. |
| `components/markdown.ts` | `TuiMarkdown.cs`, `TuiMessageArea.cs`, `TuiStatusBar.cs`, `CodingAgentCompositionMetadataViewer.cs` | partial | `TuiMarkdown` now covers heading/paragraph/inline code-bold-italic-strike-link/fenced code/list/table/blockquote/hr/padding/background/cache and optional OSC 8 hyperlink output. Exact Marked tokenizer behavior, syntax highlight/theme integration and full CodingAgent TUI integration remain open. |
| `terminal.ts` | `ITerminal.cs`, `SystemConsoleTerminal.cs`, `TuiProcessTerminal.cs`, `TuiTerminalWriteLog`, `TuiInputSequenceBuffer.cs`, `TuiPassiveTerminal.cs`, `SystemConsoleInteractiveRenderer.cs` | external-e2e-needed | Prompt/write/line editing paths, stdin-buffer foundation and ProcessTerminal lifecycle seam exist. Targeted tests cover raw mode save/restore, UTF-8 input, bracketed paste, resize subscription, Unix dimension refresh seam, Windows VT input seam, Kitty protocol query/enable/disable, modifyOtherKeys fallback/disable, input drain, ANSI operations, title and `TAU_TUI_WRITE_LOG` / `PI_TUI_WRITE_LOG` diagnostic logging. PTY behavior, hardware cursor, real Kitty/iTerm/Windows terminal behavior and live terminal smoke remain open. |
| `terminal-image.ts`, `components/image.ts`, `components/loader.ts`, `components/cancellable-loader.ts` | `TuiTerminalImage.cs`, `TuiImage.cs`, `TuiLoader.cs`, `TuiCancellableLoader.cs`, terminal-image/loader tests | partial | Terminal image foundation covers capability detection, kitty/iTerm2 escape encoding, PNG/JPEG/GIF/WebP dimensions, row calculation, fallback text, hyperlink helper and image component protocol/fallback cache tests; loader/cancellable-loader foundation is also tested. Real terminal protocol smoke and full host cleanup lifecycle remain open. |
| `tui.ts` | `TuiCompositionHost.cs`, `TuiCompositionSession.cs`, `TuiTranscriptViewport*.cs`, `TuiOverlayHost.cs`, selector/multi-select sessions | partial | Composition/transcript/overlay runtime exists and is reused by CodingAgent. Missing complete generic TUI app host, focus stack, hardware cursor and terminal lifecycle closure. |

### Tui Terminal / Input / Render / Theme Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Terminal/input shell | `terminal.ts`, `keys.ts`, `stdin-buffer.ts` | `ITerminal`, `SystemConsoleTerminal`, `TuiProcessTerminal`, `TuiPassiveTerminal`, `TuiInputSequenceBuffer`, key reader/renderer/input buffer | external-e2e-needed | Stdin-buffer and ProcessTerminal lifecycle seams are locally tested, but production live TTY/PTY/cursor behavior and real terminal protocol behavior still need smoke. |
| Editor/history/autocomplete | `components/editor.ts`, `components/input.ts`, `autocomplete.ts`, `undo-stack.ts`, `kill-ring.ts` | `InteractiveInputEditor`, `InputHistoryStore`, `TuiAutocompleteProvider`, `KeyBindingMap` | partial | Behavior is strong; missing visual popup and full widget semantics. |
| Render primitives/viewport | `utils.ts`, `fuzzy.ts`, `components/text.ts`, `box.ts`, `tui.ts` | `TuiText`, `TuiTextBlock`, `TuiBox`, `TuiContainer`, `TuiFuzzyMatcher`, `TuiDiffRenderer`, `TuiAnsiRenderSurface`, transcript viewport | ported | Text width/wrap/diff/status anchoring/viewport resize/overlay composition have tests. |
| Selectors/overlay composition | `select-list.ts`, `settings-list.ts`, `tui.ts` | `TuiSelectList`, `TuiMultiSelectList`, `TuiSettingsList`, selector sessions, `TuiOverlayHost`, `TuiCompositionSession`, CodingAgent selectors | partial | Single/multi-select, settings-list and overlay runtime work, and product-level `/settings select` now consumes the settings-list main surface. Missing full generic app host/focus stack and product-level adoption of every upstream settings submenu. |
| Theme surface | component theme props and settings-list theme | `TuiSelectListTheme`, `TuiMultiSelectListTheme`, `TuiSettingsListTheme`, CodingAgent theme store/selector | partial | Tau has local selector/settings-list theme hooks plus CodingAgent JSON theme discovery. No library-wide equivalent token system or all-component theme parity. |
| Markdown/image/loader extras | `components/markdown.ts`, `components/image.ts`, `components/loader.ts`, `components/cancellable-loader.ts`, `terminal-image.ts` | `TuiMarkdown`, `TuiImage`, `TuiTerminalImage`, `TuiMessageArea`, `TuiStatusBar`, `TuiLoader`, `TuiCancellableLoader`, text renderer | partial | Loader/cancellable-loader, Markdown, terminal image and image component foundations exist with targeted tests. Exact Marked/highlight integration and real terminal image smoke remain absent. |

## WebUi Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/component export | `packages/web-ui/package.json` exports `.` and `./app.css`; `src/index.ts`, `ChatPanel.ts`, web components | `src/Tau.WebUi` ASP.NET Core app with embedded `WebUiPage` | partial | Tau delivers a host app, not a reusable Lit/mini-lit component package with CSS export. Need decide product parity shape or build .NET static/component equivalent. |
| Chat panel, messages, input, streaming | `ChatPanel.ts`, `AgentInterface.ts`, `Messages.ts`, `MessageList.ts`, `Input.ts`, `StreamingMessageContainer.ts` | `WebUiPage.cs`, `WebChatService.cs`, `WebUiApplication.cs`, `WebUiBrowserFlowTests.cs` | partial | Tau supports create/send/stream/rename with browser tests; upstream component-level behavior and every UI state remain unmapped. |
| Attachments | `AttachmentTile.ts`, `AttachmentOverlay.ts`, `utils/attachment-utils.ts`, sandbox attachment provider | `WebChatAttachmentDto`, `WebUiPage` attachment picker/preview, service/browser tests | partial | Basic attachment preview/send exists; HTML artifact sandbox now gets a read-only session attachment snapshot with `listAttachments` / `readTextAttachment` / `readBinaryAttachment`. Upstream overlay and richer PDF/DOCX/PPTX/XLSX extraction behavior remain open. |
| Storage/settings/provider keys/custom providers | `storage/*`, IndexedDB backend, settings/provider/custom stores | `WebChatStore.cs`, session JSON under `output/`, auth status/catalog endpoints | partial | Tau persists WebChat DTO sessions and settings per session, but not upstream IndexedDB store model, provider key store or custom provider UI parity. |
| Dialogs | `dialogs/SettingsDialog.ts`, `SessionListDialog.ts`, `ProvidersModelsTab.ts`, `ApiKeyPromptDialog.ts`, `CustomProviderDialog.ts`, `PersistentStorageDialog.ts` | Inline controls in `WebUiPage.cs`, API endpoints/tests | partial | Tau has simpler inline controls and auth status; full dialog workflows, persistent storage UX and API key prompt parity remain open. |
| Tool renderers and artifacts | `tools/renderers/*`, `tools/artifacts/*`, docx/xlsx/pdf/svg/html/image/text artifacts | `WebUiPage` tool cards, `WebChatHtml/Markdown/JsonlExporter`, `WebUiTools`, browser/page tests | partial | Basic tool timeline and markdown rendering exist; local artifact operation semantics cover create/update/rewrite/delete/get/logs, WebUi runner registers session-bound LLM-facing `artifacts` and `javascript_repl` AgentTools, and browser-side JS REPL execution can create/read artifacts through the same runtime bridge. The `javascript_repl` tool now waits for an active WebUi browser page, returns text output, and carries `details.files[]` with `fileName/mimeType/size/contentBase64`. Specialized Docx/Excel rich viewers, upstream pdfjs renderer/toggles, artifact message reconstruction and full renderer registry remain open. |
| Sandbox runtime bridge | `components/sandbox/*`, `SandboxedIframe.ts`, runtime message router/bridge/providers | `WebUiPage.cs`, `WebArtifactService.cs`, `WebUiTools`, runtime endpoints/browser tests | partial | Tau has a local HTML iframe runtime bridge for artifact list/get/create/update/rewrite/createOrUpdate/delete/logs, console/execution messages, read-only attachment snapshots, one-time `file-returned` metadata, browser-side `executeJavaScriptRepl` hidden sandbox execution host, a real session-bound `artifacts` AgentTool, and a session-bound active-browser bridge for LLM-facing `javascript_repl` execution through `/api/sessions/{id}/javascript-repl/next` and `/result`. Reusable `SandboxIframe`/router, extension sandbox URL mode and full component package parity remain open. |
| CodingAgent JSONL import/preview | Upstream storage/session semantics plus CodingAgent session tree surface | `CodingAgentJsonlSessionPreviewer.cs`, import endpoints/tests | partial | Tau preview/import returns branch tree audit but persists conservative timeline DTO only; true branch/tree WebUi persistence remains open. |
| Auth/model discovery/proxy utilities | `utils/model-discovery.ts`, `auth-token.ts`, `proxy-utils.ts`, provider key dialog | `GET /api/catalog`, `GET /api/auth/{provider}`, `WebUiRunnerFactory` | partial | Catalog/auth status exist; upstream proxy/token/provider-key model and login UX remain open. |
| Browser/release/static smoke | package `build/check`, example app and root browser smoke | `tests/Tau.WebUi.Tests/WebUiBrowserFlowTests.cs`, `scripts/verify-dotnet.ps1 -RunSmoke` | external-e2e-needed | Local app browser flow exists; release/static output smoke and component package build parity remain open. |

### WebUi File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `package.json`, `src/index.ts`, `src/app.css` | `Tau.WebUi.csproj`, `WebUiPage.cs` | partial | Tau is a hosted ASP.NET Core app with embedded page. Missing reusable Lit/mini-lit component package, `.`, `./app.css` exports and CSS build artifact parity. |
| `ChatPanel.ts` | `WebUiPage.cs`, `WebUiApplication.cs` | partial | Tau has a single-page chat shell plus a server-backed artifact split panel. Missing upstream component mounting API, responsive overlay parity and Lit lifecycle. |
| `components/AgentInterface.ts`, `MessageList.ts`, `StreamingMessageContainer.ts`, `Messages.ts`, `ThinkingBlock.ts` | `WebChatService.cs`, `WebUiPage.cs`, browser tests | partial | NDJSON stream covers text/thinking/tool/done and browser tests cover load/create/send/rename. Missing exact Agent subscription semantics, abort UI, cost display and full message union rendering. |
| `MessageEditor.ts`, `AttachmentTile.ts`, `AttachmentOverlay.ts`, `attachment-utils.ts` | `WebChatAttachmentDto`, `WebChatService.BuildUserContent`, page file picker/preview JS | partial | Basic file/image preview and prompt injection exist. Missing rich overlay and full PDF/DOCX/PPTX/XLSX extraction parity. |
| Basic components/dialogs | Inline controls/styles in `WebUiPage.cs`, `/api/catalog`, `/api/sessions*`, `/api/auth/{provider}` | partial | Session/provider/model controls exist. Missing reusable components, Settings/ProvidersModels/ModelSelector/SessionList/PersistentStorage/CustomProvider/API key dialog workflows. |
| `storage/**` | `WebChatStore.cs`, `WebUiOptions.SessionsPath`, `output/webui-sessions.json` | partial | Server-side JSON store supports CRUD/search/clone/clear. Missing IndexedDB backend, transaction/quota/persistence model, provider key/custom provider/settings stores and full session metadata/data schema. |
| `utils/proxy-utils.ts`, `auth-token.ts`, `model-discovery.ts`, `format.ts`, `i18n.ts`, `test-sessions.ts` | server-side runner/auth/catalog, inline JS helpers, WebUi tests | partial | Tau has catalog/auth status and simple formatting/test fixtures. Missing browser CORS proxy/auth token utility, Ollama/llama.cpp/vLLM/LM Studio discovery UI, exported i18n/format utilities and upstream demo corpus. |
| Message/tool renderer registries | inline renderers in `WebUiPage.cs`, `WebChatToolCallDto`, service event mapping | partial | Generic markdown/tool cards exist. Missing pluggable message/tool renderer registries and specialized Bash/Calculate/Default/GetCurrentTime renderers. |
| `tools/extract-document.ts`, `javascript-repl.ts` | `WebUiPage.cs` browser-side REPL host plus existing attachment extraction; `WebUiJavaScriptReplTool`; `WebUiJavaScriptReplBridge` | partial | Tau injects extracted text/image content, exposes HTML artifact attachment snapshot helpers, has `window.executeJavaScriptRepl(code)` hidden iframe execution that captures console/return value/error/files while reusing artifact/attachment/file-return runtime providers, and registers usable session-bound `javascript_repl` AgentTool execution through an active WebUi browser bridge. Tool results include output text and upstream-shaped `details.files[]` with base64 payloads. Missing exported extract-document agent tool, richer renderer integration and full prompt/runtime parity. |
| `components/SandboxedIframe.ts`, `components/sandbox/**` | `WebUiPage.cs`, `WebArtifactService.cs`, `WebUiJavaScriptReplBridge`, artifact/runtime/repl endpoints/tests | partial | Tau now has a server-backed HTML iframe preview and runtime message bridge for artifact list/get/create/update/rewrite/createOrUpdate/delete/logs, console/execution response baseline, read-only attachment helpers, one-time `returnDownloadableFile` / `file-returned` metadata, browser-side JS REPL execution host, and active-page polling for pending LLM-facing JS REPL requests. Missing reusable `SandboxIframe` / router component, extension sandbox URL mode and full component package parity. |
| `tools/artifacts/**` | `WebArtifactService.cs`, `WebArtifactStore.cs`, `WebUiTools`, artifact APIs, artifact pane/browser tests | partial | Tau now persists per-session artifacts to `output/webui-artifacts.json`, exposes CRUD/runtime endpoints, renders text/image/html/Markdown/SVG/PDF/image/generic previews, injects attachment/file-return runtime helpers for HTML artifacts, supports REPL-created artifacts, locally matches create/no-overwrite, update old_str/new_str first replacement, rewrite, delete/get and htmlArtifactLogs/logs semantics, and registers a session-bound LLM-facing `artifacts` AgentTool in the default WebUi runner. Missing full artifact message reconstruction, Docx/Excel rich viewers, upstream pdfjs page renderer/view-code toggles and renderer registry parity. |
| `prompts/prompts.ts` | `WebUiToolPrompts` in `WebUiTools.cs` | partial | Tau now ports baseline `artifacts`, `javascript_repl`, attachments, artifacts runtime and file-download provider descriptions for WebUi tool contracts, and the LLM-facing JavaScript REPL uses those descriptions with active-browser execution. Remaining gap is exact upstream prompt text/renderer integration. |
| CodingAgent JSONL tree interoperability | `CodingAgentJsonlSessionPreviewer.cs`, preview/import endpoints/tests | partial | Strong preview/audit/filter metadata exists; conservative import linearizes to WebChat DTO and does not persist active branch tree. |
| WebUi-local import/export | `WebChatJsonlExporter/Importer`, `WebChatHtmlExporter`, `WebChatMarkdownExporter`, `WebChatJsonlExporterTests.cs`, `WebChatExportImportRoundTripTests.cs` | verified | Tau-native linear JSONL/JSON/HTML/Markdown export-import surface is fully unit-covered with no external-e2e dependency: JSONL round-trip + source metadata, redaction on/off, HTML encoding/role-classed articles/redaction footer, Markdown structure/thinking/tool-call/attachment/error rendering, and importer error codes (`missing_session_header`, `empty_line`, `missing_type`, `invalid_entry_type`, `invalid_json`, `unsupported_version`, `missing_field`, `duplicate_message_id`, `invalid_parent_chain`). This is a Tau-native surface, not a direct upstream component package surface, so `verified` reflects complete local evidence for the surface that exists rather than upstream component-package parity. |

### WebUi HTTP / Component / Env / Config / Log / Schema / Browser Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Component/package export | `package.json`, `src/index.ts`, `src/app.css` | ASP.NET Core app and public C# services/contracts | partial | Hosted app works; reusable web component/CSS export API parity missing. |
| App shell and message streaming | `ChatPanel`, `AgentInterface`, streaming/message components | `GET /`, `/api/sessions/{id}/messages/stream` NDJSON | partial | Tau streams assistant text/thinking/tool/error/done. Missing component event semantics, abort/cost/UI states. |
| Health/catalog/auth/session HTTP | browser package storage/model utilities | `/healthz`, `/api/status`, `/api/catalog`, `/api/auth/{provider}`, session CRUD/search/import/export endpoints | partial | Tau has useful host HTTP surface. It is not upstream package route parity; browser provider-key/custom-provider workflows remain open. |
| CodingAgent JSONL HTTP | branch/tree session import-preview need | preview and conservative import endpoints | partial | Preview/audit exists; true branch/tree WebUi persistence remains open. |
| Storage schema | IndexedDB stores and `SessionMetadata`/`SessionData` | `WebChatSessionDto`, `WebChatMessageDto`, `WebChatStoreDocument` | partial | Tau schema is simpler and server-side; usage/cost/thinking/full Agent messages/IndexedDB semantics missing. |
| Tool/artifact/runtime surface | renderer registries, artifacts, sandbox iframe/providers, JS REPL | generic tool cards, artifact CRUD/runtime endpoints, artifact pane, iframe runtime bridge, browser-side JS REPL host, session-bound artifacts and javascript_repl AgentTools | partial | Local artifact store/panel/runtime bridge is tested at API and browser level, including HTML artifact attachment reads, one-time file-return metadata, upstream-style create/update/rewrite/delete/get, htmlArtifactLogs/logs local semantics, and REPL execution that captures console/return values/files while creating artifacts. The default WebUi runner exposes session-bound `artifacts` and `javascript_repl` AgentTools; `javascript_repl` queues a server-side tool request, the active browser polls/executes `window.executeJavaScriptRepl(code)`, posts output/error/files back, and returns base64 file details. Full renderer registry, artifact message reconstruction and release/static browser smoke remain open. |
| Env/config/log/redaction | browser storage/proxy/key settings; package build config | `appsettings.json`, `TAU_PROVIDER`, `TAU_MODEL`, `TAU_WEBUI_REDACT_SECRETS`, Tau.Ai auth/model env, `JsonlTauLogSink` | partial | Server-side config/log/redaction exists. Missing upstream browser settings/proxy/key store and component diagnostics parity. |
| Problem/error schema | component/storage errors | `application/problem+json` for imports/preview, stream `error` events | ported | Stable problem codes/line details are host-specific and tested. |
| Browser/release/static smoke | root browser smoke, package build/check/dev | WebUi browser tests, `verify-dotnet.ps1 -RunSmoke` | external-e2e-needed | Local app browser flow exists; release/static/component package smoke remains unproven. |

## Mom Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/bin/build | `packages/mom/package.json`, `mom` bin, Slack/Web API deps | `src/Tau.Mom/Tau.Mom.csproj`, `Tau.slnx`, `verify-dotnet.ps1` | partial | Worker project exists; npm package/bin/publish artifact parity remains Phase 5 work. |
| Main CLI/config | `src/main.ts`: `--sandbox`, `--download`, `MOM_SLACK_APP_TOKEN`, `MOM_SLACK_BOT_TOKEN` | `Program.cs`, `MomCommandLine.cs`, `appsettings.json`, Mom command tests | partial | Tau supports `--once`, `--validate-sandbox`, `--validate-slack`, `--download`; env/config surface differs and real Slack long-run entry still needs e2e. |
| Delegation runtime | `src/agent.ts`: `AgentSession`, Slack thread updates, usage, compaction, `last_prompt.jsonl` | `RuntimeDelegationAgentRunner.cs`, `Delegation*.cs`, `ChannelPromptDebugStore.cs`, runtime tests | partial | Tau has structured delegation/tool/usage/prompt-debug baseline, not full upstream AgentSession + Slack UI/event rendering + compaction/session tree parity. |
| Slack receive/respond/update/delete/upload | `src/slack.ts`: Socket Mode, mentions/DM, ack, queue, respond/update/delete/upload/backfill/stop | `SlackSocketModeTransport.cs`, `SlackEventMapper.cs`, `SlackWebApiResponder.cs`, queue/processor tests | external-e2e-needed | Fake connector/HTTP contract coverage exists; real Slack token/workspace smoke is required before verified. |
| Slack session sync | Upstream `log.jsonl -> context.jsonl` sync and restart recovery | `ChannelSessionStore.cs`, `ChannelLogStore.cs`, runtime/message processor tests | partial | Tau uses `context.json` Tau-native snapshot and same-workdir carry-over; upstream log-to-context JSONL sync and real Slack thread/session recovery remain open. |
| Store/log/attachment/status | `src/store.ts`, Slack attachment/download/status log behavior | `ChannelLogStore.cs`, `ChannelAttachmentStore.cs`, `ChannelStatusStore.cs`, `SlackAttachmentDownloader.cs` | partial | Tau has log/status/attachment manifests and redaction; schemas are not fully upstream-equivalent. |
| Events | `src/events.ts`: fs watch, immediate/one-shot/periodic, queue limits | `MomEventProcessor.cs`, `MomLocalDelegationFlow.cs`, `Worker.cs`, event tests | partial | Tau scans due events into inbox; not fs.watch + SlackBot.enqueueEvent real-time parity. |
| Download | `src/download.ts`, `main.ts --download` | `SlackChannelHistoryDownloadService.cs`, download tests | external-e2e-needed | HTTP contract tests exist; real Slack history/replies smoke remains open. |
| Sandbox/Docker | `src/sandbox.ts`, `docker.sh`, `dev.sh` | `MomSandbox.cs`, `MomSandboxValidator.cs`, sandbox/tool tests | external-e2e-needed | Host and Docker command construction seams exist; no real Docker container smoke or lifecycle helper parity yet. |
| Tools | `tools/bash.ts`, `read.ts`, `write.ts`, `edit.ts`, `attach.ts`, `truncate.ts` | `MomTools.cs`, `MomToolOutputTruncator.cs`, `MomSandboxAndToolsTests.cs` | ported | Five same-name tools exist with schema/diff/truncation/attachment behavior; Docker execution still gated by sandbox e2e. |
| Operator docs/helpers | `README.md`, `docs/slack-bot-minimal-guide.md`, `docs/sandbox.md`, `docs/events.md`, `scripts/migrate-timestamps.ts` | Root docs/architecture/next/history, `scripts/migrate-mom-timestamps.ps1`, `scripts/verify-mom-timestamp-migration.ps1` | partial | Tau now has a PowerShell-first timestamp migration helper for channel `log.jsonl` files with fixture smoke coverage. Mom-specific operator docs are still partial, and real Slack/Docker e2e remains open. |
| Runtime trace/correlation | Upstream `log.ts` + AgentSession events | `TauRuntimeLogContext`, `JsonlTauLogSink`, runtime delegation tests | partial | Tau emits delegation/response/tool/usage/end and propagates context to runner; not yet a complete cross CodingAgent/Agent/Mom/Pods e2e trace protocol. |

### Mom File-Level Matrix

| Upstream file(s) | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/mom/src/main.ts` | `Program.cs`, `MomCommandLine.cs`, `Worker.cs`, `SlackSocketModeWorker.cs`, `appsettings.json` | partial | `--once`, `--download`, `--validate-sandbox`, `--validate-slack` and config binding exist. Missing exact Node CLI daemon/startup semantics and real Slack long-run e2e. |
| `agent.ts` | `RuntimeDelegationAgentRunner.cs`, `FileDelegationProcessor.cs`, `MomChannelMessageProcessor.cs`, `MomLocalDelegationFlow.cs`, session/log/runtime/model/tools classes | partial | Prompt/context/session/tool-events/outbox/status path is covered by tests. Missing exact upstream AgentSession + Slack UI/event rendering + compaction/session tree parity. |
| `context.ts` | `ChannelSessionStore.cs`, `ChannelLogStore.cs`, `MomRuntimeContext.cs`, `ChannelWorkspaceLayout.cs` | partial | Tau injects `context.json`, `log.jsonl`, workspace memory and skills. Upstream `SettingsManager` workspace settings layer is not mirrored. |
| `events.ts` | `MomEventProcessor.cs`, `MomEventFile.cs`, `MomLocalDelegationFlow.cs`, `Worker.cs` | partial | Immediate/one-shot/periodic/invalid archive/due-key behavior has tests. Missing upstream fs watcher + debounce real-time model. |
| `log.ts` | `ChannelLogStore.cs`, `ChannelStatusStore.cs`, `MomSecretRedaction.cs`, `MomToolOutputTruncator.cs`, `ChannelPromptDebugStore.cs` | partial | Structured `log.jsonl`, `status.json`, usage summary, prompt debug and redaction exist. Console log shape is Tau-native, not byte-equivalent. |
| `sandbox.ts` | `MomSandbox.cs`, `MomSandboxValidator.cs`, `MomTools.cs`, sandbox/tool tests | ported | Host/Docker sandbox parsing, path mapping, Docker validation command construction, timeout/cancel and execution are tested. Real Docker smoke remains external. |
| `slack.ts` | `SlackSocketModeTransport.cs`, `SlackWebApiResponder.cs`, `SlackEventMapper.cs`, `SlackAttachmentDownloader.cs`, `SlackBackfillService.cs`, `SlackSocketModeWorker.cs`, channel processor/queue dispatcher, Slack tests | external-e2e-needed | Socket Mode/Web API/event mapping/backfill/attachment/queue contracts have fake tests. Real Slack workspace/file/history/stop smoke is still required. |
| `store.ts` | `ChannelAttachmentStore.cs`, `ChannelLogStore.cs`, `ChannelStatusStore.cs`, `ChannelSessionStore.cs` | ported | Attachment manifest, channel log, status and session context stores are implemented and reused. Schema/layout are Tau-native. |
| `download.ts` | `SlackChannelHistoryDownloadService.cs`, download tests | external-e2e-needed | History download/thread expansion/Bearer behavior has fake HTTP tests. Real Slack API validation remains open. |
| `tools/*.ts` | `MomTools.cs`, `MomToolOutputTruncator.cs`, sandbox/tool tests | ported | `read/write/edit/bash/attach` and truncation/diff/attachment behavior are implemented and tested. Docker execution still needs real smoke. |
| `packages/mom/scripts/migrate-timestamps.ts` | `scripts/migrate-mom-timestamps.ps1`, `scripts/verify-mom-timestamp-migration.ps1` | partial | Tau has a dry-run-by-default PowerShell migration helper for `<data-dir>/<channel>/log.jsonl`, converting millisecond Unix `ts` values to Slack `seconds.microseconds`, preserving malformed lines and Tau-native `*-bot` timestamps. Fixture smoke covers dry-run, apply and idempotency. |

### Mom Slack / Event / Session / Sandbox / Env / Config / Log / Schema Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Entry / CLI / config bootstrap | `main.ts`, `store.ts`, `sandbox.ts`, `slack.ts`, migration script | `Program.cs`, `MomCommandLine.cs`, `MomOptions.cs`, `Worker.cs`, `SlackSocketModeWorker.cs`, `appsettings.json` | partial | Tau has local once/run/download/validation and config defaults. Upstream Node CLI UX, daemon semantics and config loading details are not 1:1. |
| Slack transport / Web API / Socket Mode / backfill / history | `slack.ts`, `download.ts`, `store.ts` | Slack transport/responder/mapper/backfill/history/download/attachment/queue/processor classes and tests | external-e2e-needed | Fake HTTP/socket/mapper/upload tests exist. Real Slack workspace smoke, event stream, file/history and stop validation remain open. |
| Event file scheduling / queueing | `events.ts` | `MomEventProcessor`, local delegation flow, worker tests | partial | Scheduled event parsing and invalid archive are tested. Missing fs watcher/debounce real-time behavior. |
| Session / context / memory / model restore | `context.ts`, `agent.ts` | `ChannelSessionStore`, `ChannelLogStore`, runtime context, model resolver, runner tests | partial | Tau handles session inheritance, workspace memory, channel history and provider/model carry-over. Upstream workspace settings layer/schema parity remains open. |
| Sandbox / tool delegation / workspace safety | `sandbox.ts`, `tools/*` | `MomSandbox`, `MomSandboxValidator`, `MomTools`, `ChannelWorkspaceLayout`, attachment store | ported | Host/Docker command construction and tools are tested. Actual Docker/container smoke is still external. |
| Log / status / redaction / schema | `log.ts`, `store.ts`, `agent.ts`, `context.ts` | `ChannelLogStore`, `ChannelStatusStore`, `MomSecretRedaction`, request/result/execution/tool event records, prompt debug store | partial | Structured Tau schemas exist and are safer/redacted. Not upstream byte-equivalent layout/console logging. |
| Attachment staging / manifest / download | `store.ts`, `slack.ts`, `tools/attach.ts` | `ChannelAttachmentStore`, `SlackAttachmentDownloader`, Mom attach tool | partial | Local manifest/download/attach path exists; real Slack attachment runtime validation remains open. |

## Pods Matrix

| Capability | Upstream evidence | Tau target / evidence | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Package/bin/build artifact | `packages/pods/package.json`: bin `pi-pods`, build copies `models.json` and `scripts` | `src/Tau.Pods/Tau.Pods.csproj`, `Tau.Pods` CLI | partial | Runtime CLI exists, but distributable artifact/script packaging parity remains Phase 5 work. |
| Config, registration and active pod | `src/config.ts`, `commands/pods.ts` list/setup/active/remove | `PodsConfigStore.cs`, `PodsConfigValidator.cs`, `PodsCli.cs`, config/CLI tests | ported | Tau supports init, upstream-style `pods` config list, validate/status/active/remove and setup registration baseline with JSON output. |
| SSH/SCP transport | `src/ssh.ts`, use of `sshExec`, `sshExecStream`, `scpFile` | `PodExecService.cs`, `PodSetupService.cs`, lifecycle/model/vLLM services | external-e2e-needed | Process argv hardening/fake runners exist; real SSH/SCP smoke is not yet proven. |
| Setup run | `commands/pods.ts setupPod`: HF/PI token check, copy `pod_setup.sh`, run setup, detect GPUs, save config | `PodSetupPlanner.cs`, `PodSetupService.cs`, setup tests | external-e2e-needed | Tau has plan/run/SCP/GPU-detect contract and token redaction tests; real remote setup/GPU smoke remains open. |
| Known model configs and GPU allocation | `src/models.json`, `model-configs.ts`, `startModel` GPU selection, memory/context overrides | `src/Tau.Pods/Models/models.json`, `PodKnownModelRegistry.cs`, `PodVllmCommandPlanner.cs`, `PodDefinition.Gpus`, vLLM option parsing | partial | Tau now bundles the upstream `models.json`, ports `isKnownModel` / `getKnownModels` / `getModelName` / GPU count/type config matching, injects known-model args/env into `vllm plan/deploy` / top-level `start` unless explicit `--vllm` args are supplied, supports local `--gpus` / `--memory` / `--context` planning, and selects least-used GPU ids from configured `pod.models[*].gpu` state. Remaining gaps: upstream local start log UX, exact PID process state and real GPU/vLLM smoke. |
| Model lifecycle | `commands/models.ts`: start/stop/list/logs/show known models | `PodModelService.cs`, `PodLifecycleService.cs`, `PodVllmOrchestrationService.cs`, tests | partial | Tau splits HF cache model lifecycle and vLLM deployment lifecycle; top-level `start <model> --name <name>`, `list`, `stop <name>`, no-arg `stop` stop-all, `logs <name>` snapshot and `logs --follow` realtime stream now map to Tau vLLM/deployment services with active/explicit pod support. Successful vLLM deploy/top-level start writes configured model/model/port/GPU state and persists parsed remote wrapper `pid=<n>` when orchestration exposes it; successful vLLM/top-level stop removes the corresponding state. vLLM plan/deploy/logs now expose or prefer upstream `~/.vllm_logs/<name>.log`; `logs --follow` inherits stdio and runs `tail -f ~/.vllm_logs/<name>.log` after a configured-model guard; deploy now uploads a Tau `model_run_<name>.sh` plus `model_wrapper_<name>.sh`, uses pseudo-TTY `script` to write the upstream log path, exposes a structured startup watcher before `/health`, and now saves configured model state immediately after remote start before startup/health checks, deleting it again on startup/health failure. The generated runner now performs upstream-style `hf download "$MODEL_ID"` with `HF_HUB_ENABLE_HF_TRANSFER=1` and starts vLLM with `--api-key "$PI_API_KEY"`. Real process verification and real remote HF/GPU/vLLM smoke remain open. |
| vLLM plan/preflight/deploy/status/health/stop | Upstream model run scripts and SSH commands | `PodVllmCommandPlanner.cs`, `PodVllmOrchestrationService.cs`, vLLM tests | external-e2e-needed | Tau has richer systemd/nohup/health/rollback baseline and many fake-runner tests; no real HF/GPU/vLLM startup smoke yet. |
| Prompt against pod model | `commands/prompt.ts` builds local coding-agent args but throws `Not implemented` | `PodsCli` top-level `agent` command + `PodAgentRunner` + CodingAgent CLI provider/model/system prompt args | partial | Tau now resolves active/explicit pod, looks up `pod.models[modelName]`, builds upstream-style `--base-url`, `--model`, redacted `--api-key`, `--api`, `--system-prompt` and passthrough user args, then starts Tau.CodingAgent with a temporary `TAU_MODELS_FILE` provider bound to the pod vLLM endpoint plus `TAU_PROVIDER` / `TAU_MODEL`; the temp models config stores `PI_API_KEY` as an env var reference or `dummy`, not a literal secret. Local fake-runner tests cover argv/env/config generation, redaction and exit-code propagation; real pod prompt e2e over SSH/HF/GPU/vLLM remains open. |
| Runtime logs/correlation | Upstream mostly console/process oriented | `ITauLogSink` integration in probe/lifecycle/model/vLLM services | partial | Tau emits structured operation summaries; correlation through real remote e2e and release smoke remains unverified. |
| Root release/scripts parity | `packages/pods/scripts`, root `build-binaries.sh` / release | `scripts/verify-dotnet.ps1`, no complete release scripts | missing | Final 100% requires real Tau executable artifact and install/release chain. |

### Pods File-Level Matrix

| Upstream file | Tau target | Status | Proof / remaining gap |
| --- | --- | --- | --- |
| `packages/pods/src/index.ts` | `src/Tau.Pods/Program.cs`, `src/Tau.Pods/Cli/PodsCli.cs` | partial | Tau has a .NET CLI entry and wider command set. Upstream `pi-pods` bin shape/package export remains Phase 5 release parity work. |
| `packages/pods/src/cli.ts` | `src/Tau.Pods/Cli/PodsCli.cs`, `tests/Tau.Pods.Tests/PodsCliTests.cs` | partial | Tau now covers upstream-style `pods` group (`pods`, `pods setup`, `pods active`, `pods remove`), top-level `start <model> --name <name>` -> `vllm deploy`, top-level `list` -> active/explicit-pod deployments, top-level `stop <name>` -> `vllm stop`, no-arg `stop` -> list deployments then stop all, `shell` -> no-remote-command SSH process, existing `ssh`/`logs`, `logs --follow` -> inherited-stdio SSH `tail -f ~/.vllm_logs/<name>.log`, and `agent` -> Tau.CodingAgent process runner with pod endpoint models config. `logs` snapshot now prefers upstream `~/.vllm_logs/<name>.log` before older Tau journal/`.tau_pods` fallbacks; deploy JSON/text now includes `startupWatch` before health and exposes runner/wrapper script path plus `usesPseudoTtyWrapper`; the uploaded runner now includes upstream-style HF download and PI API key vLLM startup args; CLI deploy writes configured model state after remote start and before startup/health checks, then deletes it on startup/health failure. Remaining command gaps are real pod prompt e2e and broader real remote e2e. |
| `packages/pods/src/types.ts` | `src/Tau.Pods/Models/PodDefinition.cs`, `PodsConfig.cs`, `PodModelResults.cs`, `PodLifecycleResults.cs`, `PodSetupResults.cs`, `PodVllmResults.cs` | partial | Tau has richer typed result records and persists `active`, pod SSH endpoint/command, `modelsPath`, `vllmVersion`, `gpus` and per-pod configured model state. Config load/save now bridges upstream `pods: Record<string, Pod>` and `models: Record<string, Model>` objects while keeping internal typed records. |
| `packages/pods/src/config.ts` | `src/Tau.Pods/Services/PodsConfigStore.cs`, `PodsConfigValidator.cs`, `src/Tau.Pods/Models/PodsConfig.cs`, `PodsCli.cs` | ported | Tau now uses `PI_CONFIG_DIR` or no-env `~/.pi` as the upstream-compatible config directory and defaults to `<config-dir>/pods.json`; explicit `--config path` / positional config path still wins. `PodsConfigStore.Load` returns an empty config for missing files, reads upstream record-shaped `pods` / `models` config, still accepts legacy list-shaped Tau config, and `Save` writes the upstream record shape. |
| `packages/pods/src/ssh.ts` | `src/Tau.Pods/Services/PodExecService.cs`, `PodSetupService.cs`, lifecycle/model/vLLM services | external-e2e-needed | Tau hardens local argv construction, injected runner failure and cancellation handling; real SSH/SCP smoke remains required before verified. |
| `packages/pods/src/model-configs.ts` | `src/Tau.Pods/Services/PodKnownModelRegistry.cs`, `src/Tau.Pods/Services/PodVllmCommandPlanner.cs`, `src/Tau.Pods/Models/PodDefinition.cs`, registry/planner tests | partial | Tau ports the known-model lookup surface and upstream GPU type/count fallback into a registry consumed by the vLLM planner, applies `--gpus` / `--memory` / `--context` transforms in plan/deploy planning, and now owns the local usage-aware least-used GPU selection baseline from configured model state. Real hardware smoke remains open. |
| `packages/pods/src/models.json` | `src/Tau.Pods/Models/models.json`, `src/Tau.Pods/Tau.Pods.csproj`, `PodKnownModelRegistryTests.cs` | partial | Tau now vendors the exact upstream known-model hardware/env/arg matrix and copies it to build output. Final verified status still waits on command-level GPU allocation behavior and real vLLM/GPU smoke. |
| `packages/pods/src/commands/pods.ts` | `PodsCli.cs`, `PodSetupPlanner.cs`, `PodSetupService.cs`, `PodsConfigStore.cs`, `PodsConfigValidator.cs` | partial | Tau supports upstream `pods` group for config listing, local registration (`pods setup` / `setup`), active/remove/status, setup plan/run and token-redacted setup output. Upstream `pi pods setup` immediately validates `HF_TOKEN`/`PI_API_KEY`, copies `pod_setup.sh`, runs remote setup and saves config; Tau separates registration/plan/run and still needs real remote setup smoke. |
| `packages/pods/src/commands/models.ts` | `PodModelService.cs`, `PodLifecycleService.cs`, `PodKnownModelRegistry.cs`, `PodVllmCommandPlanner.cs`, `PodVllmOrchestrationService.cs`, `PodsCli.cs` | partial | Tau has HF cache list/pull/remove/status, lifecycle logs/deployments, vLLM orchestration, known-model args/env injection, local selected GPU ids, single-GPU `CUDA_VISIBLE_DEVICES`, `--memory` / `--context` vLLM arg transforms, usage-aware least-used GPU selection from configured model state, deploy/stop configured model state write/delete for `vllm plan/deploy` and top-level `start/stop`, best-effort wrapper process id persistence from deploy stdout, upstream `~/.vllm_logs/<name>.log` path compatibility, `logs --follow` realtime tail over the same path, deploy-time startup marker watcher with failure rollback, save-before-watch/delete-on-failure configured model ordering, a local `model_run_<name>.sh` + `model_wrapper_<name>.sh` pseudo-TTY `script` baseline, runner-side `HF_HUB_ENABLE_HF_TRANSFER=1 hf download "$MODEL_ID"` and `vllm serve ... --api-key "$PI_API_KEY"` command contract. Real remote runtime, real HF/GPU/vLLM smoke and multi-version rollback remain open. |
| `packages/pods/src/commands/prompt.ts` | `PodsCli` top-level `agent` command, `PodAgentRunner`, `PodConfiguredModel` state, `PI_API_KEY` redaction, CodingAgent CLI provider/model/system prompt args | partial | Tau now ports and executes the local prompt command contract: active/explicit pod resolution, configured model lookup, OpenAI-compatible base URL from pod host + configured port, GPT-OSS responses API selection, redacted API key source, passthrough message/options, temporary `models.json` provider config for Tau.Ai, and Tau.CodingAgent process startup with provider/model/system prompt wired. Upstream `prompt.ts` still throws, so this is a Tau-native runtime closure, not upstream proof; real pod endpoint prompt e2e remains required. |

### Pods Command / Env / Config / Log Surface

| Surface | Upstream evidence | Tau equivalent | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| Top-level command names | `cli.ts`: `pods`, `shell`, `ssh`, `start`, `stop`, `list`, `logs`, `agent` | `PodsCli`: upstream-style `pods`, `shell`, `ssh`, `start`, `stop`, `list`, `logs`, `agent` plus Tau-native `setup`, `deployments`, `model`, `vllm` | partial | Local command-compat shim now covers `pods` / `pods setup` / `pods active` / `pods remove`, top-level `start <model> --name <name>`, `list`, `stop <name>`, no-arg `stop` stop-all, `shell`, existing `ssh`/`logs`, and `agent` process startup through `PodAgentRunner`; `logs` now reads upstream `~/.vllm_logs/<name>.log` first. Tests cover these rewrites, shell SSH argv, redacted agent args, temporary pod model config, exit-code propagation and log path preference. Remaining gaps are exact upstream realtime local PID/log-tail/startup streaming flow and real pod prompt e2e. |
| Setup env vars | `commands/pods.ts`, `pod_setup.sh`: `HF_TOKEN`, `PI_API_KEY` | `PodSetupPlanner`, `PodSetupService`: same env names, redacted display/stdout/stderr, config does not store tokens | ported | Contract/fake tests cover token presence and non-persistence; real setup run remains `external-e2e-needed`. |
| Config path/env | `config.ts`: `PI_CONFIG_DIR` or `~/.pi`, config file `pods.json` | `PodsCli`: `PI_CONFIG_DIR` or no-env `~/.pi` -> `<config-dir>/pods.json`, `--config path`, positional config path | ported | Path/env and config shape compatibility are locally covered for `init` / `pods` / `model` / `vllm` / target command parsing, explicit config override precedence, no-env `~/.pi/pods.json` default, missing default config returning an empty config, upstream record-shaped read/write, and legacy list-shaped load migration. |
| Active pod semantics | `Config.active`, `getActivePod()` | `PodsConfig.ActivePodId`, active fallback in model/vLLM/deployments/logs/exec | ported | CLI tests cover active fallback and JSON shape; record-shaped save keeps `active` at the upstream top level. |
| Known model data | `models.json`, `model-configs.ts` | `src/Tau.Pods/Models/models.json`, `PodKnownModelRegistry`, `PodVllmCommandPlanner` | partial | Tau now ships the upstream model data, uses GPU type/count matching to add known args/env to vLLM plans, applies local `--gpus` / `--memory` / `--context` planning for `vllm plan/deploy` and top-level `start`, and uses configured model GPU state for least-used GPU selection. Still missing exact startup/log UX compatibility and real hardware smoke. |
| GPU allocation options | `start --memory --context --gpus --vllm` | top-level `start` shim plus `vllm plan/deploy --gpus/--memory/--context/--vllm`, `PodDefinition.Gpus`, planner/CLI tests | partial | Tau supports local convenience planning for known models through both `vllm plan/deploy` and top-level `start`: requested GPU count, selected GPU ids, single-GPU `CUDA_VISIBLE_DEVICES`, `--gpu-memory-utilization`, `--max-model-len` transforms, and least-used GPU allocation from `pod.models[*].gpu`. Remaining gaps are exact upstream startup/log/PID behavior and real GPU/vLLM smoke. |
| Remote process/log schema | `models.ts`: pid in config, `~/.vllm_logs/<name>.log`, tail streaming and startup detection | `PodLifecycleService`/`PodVllmOrchestrationService`: metadata/service/pid files under `~/.tau_pods`, vLLM stdout/stderr under `~/.vllm_logs`, `logs` tail fetch, health/status JSON | partial | Tau now parses remote `pid=<n>` from deploy stdout and carries it through CLI JSON/config writeback when available, while keeping `0` when systemd/fallback PID cannot be proven. vLLM plan/metadata JSON exposes `logPath`; systemd output and fallback nohup write `~/.vllm_logs/<name>.log`; `logs` tails that path before older Tau fallbacks; `vllm health/status` scans the same log path for startup complete/failure markers; CLI deploy persists configured model state before startup/health checks and removes it after watcher/health failure. Upstream real process verification remains open. |
| Runtime logs | upstream mainly console/process output | `ITauLogSink` events for probe/lifecycle/model/vLLM `*.start|end` | partial | Structured Tau summary events exist; real remote e2e correlation remains unverified. |
| Prompt/agent against pod | `prompt.ts` builds `--base-url`, `--model`, `--api-key`, `--api`, `--system-prompt`, then throws `Not implemented` | `agent [--config path] [--pod id] <name> [message/options...]` prompt mapping + Tau.CodingAgent process runner baseline | partial | Tau resolves pod/model config, prints the redacted upstream-style argument plan to stderr, creates a temporary Tau.Ai `models.json` provider targeting the pod vLLM base URL, starts Tau.CodingAgent with `--provider` / `--model` / `--system-prompt`, adds `--print` for prompt text, preserves user args such as `--json`, and propagates child exit codes. Temp config stores `PI_API_KEY` by env var name or `dummy`; real pod endpoint prompt e2e remains open. |

## Root Script / Manifest File Matrix

| Upstream file/script | Upstream behavior | Tau target | Status | Proof / gap |
| --- | --- | --- | --- | --- |
| `package.json` | Workspace build/check/test/version/publish/release scripts and examples workspace graph. | `Tau.slnx`, project files, `scripts/verify-dotnet.ps1`, `scripts/verify-dotnet.sh` | partial | Build/test gates exist, and local release version/notes/commit/tag execution now exists under explicit `execute-release.ps1 -Apply`. Publish/push and full release artifact parity remain open. |
| `test.sh` | Backs up `~/.pi/agent/auth.json`, unsets provider/AWS/HF env vars, sets `PI_NO_LOCAL_LLM=1`, runs `npm test`. | `scripts/verify-no-env.ps1`, `scripts/invoke-no-env.ps1`, `scripts/verify-dotnet.ps1 -SkipRestore`, tests | partial | Tau now has no-env child-process isolation and temp Tau auth/config/session paths, verified through `verify-no-env.ps1 -SkipRestore`; exact move/restore auth backup behavior remains open by design because Tau points auth files at temp state instead. |
| `pi-test.sh` | Runs `packages/coding-agent/src/cli.ts`; `--no-env` unsets provider/AWS/Azure env vars. | `scripts/pi-test.ps1`, `scripts/invoke-no-env.ps1`, `scripts/verify-no-env.ps1`, `dotnet run --project src/Tau.CodingAgent/Tau.CodingAgent.csproj` | partial | Tau now has a PowerShell-first direct CodingAgent wrapper with `--no-env`, isolated state, `--input-file` stdin redirection and base64 child argument support; `verify-no-env.ps1 -RunSmoke` routes CodingAgent RPC smoke through it. Exact Unix shell wrapper remains open. |
| `scripts/check-browser-smoke.mjs` / `scripts/browser-smoke-entry.ts` | Browser smoke for upstream web UI/example bundle. | `tests/Tau.WebUi.Tests/WebUiBrowserFlowTests.cs`, `verify-dotnet.ps1 -RunSmoke` | partial | Tau has local browser/service smoke; release/static artifact smoke still open. |
| `scripts/build-binaries.sh` | Builds cross-platform `pi` archives using Bun compile and copies theme/assets/export-html/docs/examples. | `scripts/build-release-artifacts.ps1`, `scripts/package-release-artifacts.ps1`, `scripts/smoke-release-artifacts.ps1`, `.github/workflows/tau-ci.yml` | partial | Tau now publishes RID-specific executable artifacts for CodingAgent, AI CLI, Mom, Pods and WebUi with wrapper commands and manifest; host RID smoke verifies core commands in `artifacts/tau-win-x64`; archive packaging now supports zip for Windows and tar.gz for Linux/macOS, verifies clean extraction, and the matrix scripts cover `osx-arm64`, `osx-x64`, `linux-x64`, `linux-arm64`, and `win-x64`. The artifact now copies `README.md`, `LICENSE`, complete current `docs/` and `examples/`, and `releasePayload` explicitly audits `changelog`, `package-json`, `theme`, `export-html`, `examples`, Photon wasm, interactive assets and koffi native module status. Windows CI uploads the verified Windows zip and also runs tar.gz format structure smoke against the same host artifact. Non-host runner executable smoke, Photon/interactive raster assets/external export-html payload parity and release automation remain open. |
| `scripts/release.mjs` | Clean worktree check, version bump/sync, changelog release sections, commit/tag, npm publish, push. | `Directory.Build.props`, `scripts/plan-release.ps1`, `scripts/execute-release.ps1`, `scripts/prepare-release.ps1`, `scripts/validate-release.ps1`, `scripts/verify-release-contracts.ps1`, `scripts/update-release-version.ps1`, `scripts/update-release-notes.ps1`, release artifact/archive scripts | partial | Tau has executable artifact build/smoke, a repo-owned `VersionPrefix`, a dry-run release plan script, guarded local preparation and validation scripts, a guarded local execution script, a dry-run JSON contract smoke, a version writeback helper, and a release notes helper. `execute-release.ps1 -Apply` covers local version+notes mutation, optional validation, narrow staging, `Release v<version>` commit and `v<version>` tag. Publish and push remain deliberately non-executed, and the upstream second `[Unreleased]` changelog commit is not mapped because Tau release notes use a dated table. |
| `scripts/sync-versions.js` | Synchronizes workspace package versions. | `Directory.Build.props`, `scripts/sync-release-versions.ps1`, `scripts/verify-release-version-sync.ps1`, `scripts/update-release-version.ps1`, `scripts/execute-release.ps1` | partial | Tau has one solution-wide MSBuild version source instead of package-workspace lockstep. `sync-release-versions.ps1` audits explicit project version drift and can apply center-version fixes; `verify-release-version-sync.ps1` fixes the repo/fixture contract. NuGet/package publish sync remains undecided. |
| `scripts/session-transcripts.ts` | Utility over upstream session transcripts. | `scripts/export-session-transcripts.ps1`, CodingAgent/WebUi export/import commands | partial | Tau has a root script invocation surface for transcript export: it reads Tau JSONL session files, extracts user/assistant text, chunks output files, tolerates malformed trailing JSONL lines, and is covered by `verify-session-audit-scripts.ps1`. Upstream transcript `--analyze` subagent mode remains unported. |
| `scripts/profile-coding-agent-node.mjs` | TUI/RPC startup profiling, `PI_CODING_AGENT_DIR`, `PI_STARTUP_BENCHMARK`, offline env. | `scripts/profile-coding-agent-startup.ps1`, `scripts/verify-coding-agent-startup-profile.ps1` | partial | Tau has a PowerShell-first RPC startup benchmark with isolated Tau state, offline/no-local-LLM env, BOM-free JSONL input redirection and release/CI smoke coverage. It does not yet provide TUI first-frame startup profiling, CPU profiles or Node/Bun runtime comparison. |
| `scripts/edit-tool-stats.mjs` | Edit tool statistics utility. | `scripts/report-edit-tool-stats.ps1`, `scripts/verify-edit-tool-stats.ps1` | partial | Tau has a root script invocation surface for edit tool statistics: it scans Tau JSONL sessions, supports Tau `edit_file` and upstream `edit`, filters by model/extension/failed/since, and is covered by `verify-edit-tool-stats.ps1`. This is a local audit utility; it does not alter edit tool runtime behavior or prove real provider e2e. |
| `scripts/cost.ts` | Reads session cost data under `~/.pi/agent/sessions`. | `scripts/report-session-costs.ps1`, `ModelCatalog.CalculateCost`, CodingAgent session stats/session store | partial | Standalone cost utility baseline exists and is covered by `verify-session-audit-scripts.ps1`; it aggregates persisted `usage.cost` and token records from Tau JSONL sessions without recalculating missing historical cost. CodingAgent flat/tree session persistence now writes assistant usage tokens, provider/model/api/timestamp metadata and computed `usage.cost` when model pricing is known, and `/session` plus RPC `get_session_stats` expose persisted token/cost totals, including current-branch JSONL tree totals after compaction. Real provider/e2e samples, older-session backfill, exact upstream session root/path semantics and richer upstream footer/TUI rendering remain open. |
| `packages/ai/scripts/generate-models.ts` | Generates upstream AI model catalog. | `src/Tau.Ai/Registry/generate-tau-ai-models.ps1`, `generated-models.seed.json`, `GeneratedBuiltInModels.g.cs` | partial | Tau has generation pipeline for supported families; full upstream generated model parity remains open. |
| `packages/ai/scripts/generate-test-image.ts` | Generates AI test image fixture. | `scripts/generate-ai-test-image.ps1`, `scripts/verify-ai-test-image.ps1`, optional `tests/Tau.Ai.Tests/Data/red-circle.png` output | ported | Tau has a PowerShell-first generator for a 200x200 white-background red-circle PNG fixture, using pure PNG/zlib writing rather than node-canvas. `verify-ai-test-image.ps1` generates into a temp directory and validates PNG chunks, IHDR, zlib/adler32, scanline filters, key pixels and SHA256 output. Broader CodingAgent image/clipboard ingestion and provider vision e2e remain separate gaps. |
| `packages/pods/scripts/pod_setup.sh` | Remote setup script: validates HF/API tokens, installs vLLM variants, exports token/env, configures model path. | `PodSetupPlanner`/`PodSetupService` expects compatible remote script contract but does not vendor a Tau script file yet | partial | Tau builds command and can copy custom `--script`; real script packaging and remote smoke remain open. |
| `packages/pods/scripts/model_run.sh` | Remote vLLM wrapper with env and PID/log monitoring. | `PodVllmCommandPlanner` / `PodVllmOrchestrationService` generated runner and wrapper scripts | partial | Tau now generates `~/.tau_pods/model_run_<name>.sh` with upstream-style runner markers, vLLM environment, optional `/root/venv` activation, runner-side `HF_HUB_ENABLE_HF_TRANSFER=1 hf download "$MODEL_ID"` with a stable failure marker, and Tau-planned `vllm serve ... --api-key "$PI_API_KEY"`, plus `~/.tau_pods/model_wrapper_<name>.sh` that runs `script -q -f -c ... ~/.vllm_logs/<name>.log` and appends `Script exited with code`. Systemd and fallback both launch that wrapper, plan/deploy text/JSON/metadata expose runner/wrapper paths and `usesPseudoTtyWrapper`, health/status/startup watch consume the same log markers, stop/rollback cleanup removes runner/wrapper files, and CLI config write/delete order now matches the local `startModel()` save-before-watch/delete-on-failure contract. Remaining gaps are real SSH/HF/GPU/vLLM smoke and multi-version rollback. |
| `packages/mom/scripts/migrate-timestamps.ts` | Mom timestamp migration helper. | `scripts/migrate-mom-timestamps.ps1`, `scripts/verify-mom-timestamp-migration.ps1` | partial | Tau has a local migration helper and smoke coverage; this does not prove real Slack runtime/session sync. |
| `packages/coding-agent/scripts/migrate-sessions.sh`, `packages/coding-agent/src/migrations.ts` auth/session/commands/tools/keybindings migrations | CodingAgent auth, session, extension-system and keybindings migration helpers. | `scripts/migrate-coding-agent-auth.ps1`, `scripts/verify-coding-agent-auth-migration.ps1`, `scripts/migrate-coding-agent-sessions.ps1`, `scripts/verify-coding-agent-session-migration.ps1`, `scripts/migrate-coding-agent-commands.ps1`, `scripts/verify-coding-agent-commands-migration.ps1`, `scripts/migrate-coding-agent-tools-to-bin.ps1`, `scripts/verify-coding-agent-tools-to-bin-migration.ps1`, `scripts/audit-coding-agent-deprecated-extension-dirs.ps1`, `scripts/verify-coding-agent-deprecated-extension-dirs-audit.ps1`, `scripts/migrate-coding-agent-keybindings.ps1`, `scripts/verify-coding-agent-keybindings-migration.ps1` | partial | Tau now has PowerShell-first helpers for legacy `oauth.json` + `settings.json.apiKeys` -> `auth.json`, root JSONL relocation, `commands/` -> `prompts/`, managed `fd`/`rg` `tools/` -> `bin/` relocation, deprecated `hooks/` plus custom `tools/` warning audit, and legacy object-style keybinding id migration. Mutating helpers default to dry-run and require explicit `-Apply`; auth migration skips when `auth.json` exists, preserves OAuth-over-api-key conflicts, renames `oauth.json` to `.migrated`, removes migrated `settings.json.apiKeys`, and redacts secrets from JSON output; session relocation validates direct root `*.jsonl` first-line `type=session` + `cwd`, commands migration renames only when `commands/` exists and `prompts/` does not, tools migration moves only `fd`, `rg`, `fd.exe`, `rg.exe` or removes old `tools/<name>` when `bin/<name>` already exists, the audit helper only reports `hooks/` or custom non-managed/non-hidden `tools/` entries without mutation, and keybindings migration preserves canonical winners/custom keys while skipping invalid/missing files and Tau array-style runtime keybinding files. Fixture smoke coverage now includes auth dry-run/apply/idempotent/invalid-json/secret-redaction, duplicate removal, custom tool preservation, managed/hidden ignore, file tools path skip, legacy key id rewrite/conflict handling, invalid/missing skip and idempotence. Broader full settings/runtime auth parity, credential refresh UX, actual hooks/custom tools migration to `extensions/`, full jiti/custom tool extension runtime, package/extension shortcut migration and general custom tool migration parity remain open. |

## Phase 1 Normalization Result

- Current upstream package directory check at 2026-05-28 15:12 returned exactly `agent`, `ai`, `coding-agent`, `mom`, `pods`, `tui` and `web-ui`; no unknown upstream package directory remains outside this matrix.
- Capability rows now act as planning summaries only. File-level rows are the ownership/source mapping layer, and surface rows are the public command/API/env/config/log/schema/e2e contract layer.
- Duplicated gaps are intentionally kept only where a high-level row needs navigation context. Phase 2 workers should start from the granular file/surface row or the candidate queue below, not from a summary row alone.
- Status normalization for Phase 2 handoff: `ported` means implemented with local evidence but not final acceptance; `partial` means known behavior or schema gaps remain; `missing` means no meaningful Tau equivalent exists; `external-e2e-needed` remains open until real provider/service/runtime smoke passes; `non-goal-proposed` is not final until the user explicitly accepts it.
- Phase 1 is closed at grouped file-level granularity. Future workers may add finer rows while implementing, but broad package inventory should not be reopened unless upstream package directories or package manifests change.

2026-06-17 Agent facade pass-through evidence: `AgentApplicationBuilder.AddTool(..., prepareArguments:)` now forwards raw-argument preparation into `DelegateAgentTool`; `AgentPlatformTests.PromptAsync_UsesDelegateToolPrepareArgumentsBeforeExecution` proves execution sees prepared args, `AgentPublicApiCompileSampleTests` keeps the public builder sample compiling, and `scripts/verify-agent-package-consumer.ps1` prints/asserts `toolResult=prepared package consumer` from a temp app that references only the `Tau.Agent` package. The `Agent runtime loop, facade and tool events` row remains `partial` for broader provider/OAuth, registry/signing/provenance and runtime config gates, not for package/index export-shape decision.

2026-06-18 AI provider/OAuth matrix evidence: `scripts/verify-ai-provider-e2e-matrix.ps1` records the Tau-native inspect/isolation contract for 11 default providers and is wired into `plan-release.ps1` plus `verify-release-contracts.ps1` in `-Isolated` mode. This closes the repo-owned matrix harness baseline, not any real provider/OAuth cloud e2e.

2026-06-19 AI provider/OAuth required-run guard evidence: `scripts/verify-ai-provider-e2e-matrix.ps1` now exposes `-RequireConfigured`, `realE2eSatisfied`, `completionStatus` and `gateFailure`. The required guard makes `-RunConfigured` fail when there are no configured providers, no real run attempts, or the result is not a non-isolated all-success provider run. Local validation fixed the deterministic no-credential boundary: `-Isolated -Json` passes with `completionStatus=inspect-only`, while `-RunConfigured -RequireConfigured -Isolated -Json` exits non-zero with `completionStatus=no-configured-providers`; `verify-release-contracts.ps1` asserts that guard behavior. This still leaves real provider/OAuth e2e `external-e2e-needed` until live credentials are configured and a non-isolated required run succeeds.

2026-06-18 AI/Agent export shape evidence: `docs/AI_AGENT_EXPORT_SHAPE.md` records the Tau-native decision for upstream AI package exports, AI index exports and Agent index exports; `scripts/verify-ai-agent-export-shape.ps1` verifies 12 AI package exports, 13 AI index export groups, 4 Agent index export groups and 37 evidence files. The script is wired into `plan-release.ps1` and `verify-release-contracts.ps1`.

2026-06-18 AI provider cancellation evidence: `StreamOptions.Signal` is now wired through OpenAI/OpenAI-compatible, OpenAI Responses/Codex/Azure, Anthropic, Google/Gemini CLI/Vertex, Mistral, Bedrock, Vertex ADC token exchange, SSE/WebSocket/EventStream parser and retry-delay paths. Representative OpenAI, OpenAI Responses, Bedrock and Gemini CLI cancellation tests prove pre-cancel or user-triggered cancellation emits `Request was aborted` with `StopReason.Aborted`; full `Tau.Ai.Tests` is 410/410. This closes local provider cancellation plumbing only; real provider/OAuth cloud e2e remains in the external matrix.

2026-06-18 AI models.json request options evidence: `ModelConfigurationStore` now parses provider-level, modelOverride-level and model-level `options`; `StreamFunctions` merges them into `StreamOptions` / `SimpleStreamOptions` before provider dispatch, while explicit code options override configured values. Focused config/public API tests passed 11/11, full `Tau.Ai.Tests` passed 424/424, and the package consumer smoke had 47 assertions including configured options output before the Responses-specific extension below. This closes common request option config locally; provider-specific option fields are tracked separately and real cloud e2e remains open.

2026-06-18 AI Responses provider-specific options evidence: `ModelConfigurationStore` now parses the first Responses-family provider-specific `models.json options` slice; `StreamFunctions` projects that config into `OpenAiResponsesOptions`, `OpenAiCodexResponsesOptions` or `AzureOpenAiResponsesOptions` before provider dispatch while keeping explicit typed options as highest precedence. Focused Responses/config tests passed 33/33, full `Tau.Ai.Tests` passed 429/429, and `scripts/verify-agent-package-consumer.ps1 -SkipRestore -Json` passed 52 assertions including `configuredResponsesOptionsType=OpenAiResponsesOptions`, `configuredResponsesOptionsServiceTier=flex`, `configuredResponsesOptionsReasoningEffort=low`, `configuredResponsesOptionsReasoningSummary=concise` and `configuredResponsesOptionsMaxTokens=111`. This closes only the OpenAI Responses family provider-specific option local contract; non-Responses provider-specific maps and real cloud e2e remain open.

2026-06-19 AI Mistral provider-specific options evidence: `ModelConfigurationStore` now parses the first Mistral provider-specific `models.json options` slice, including string `toolChoice` and object function `toolChoice`; `StreamFunctions` projects that config into `MistralOptions` before provider dispatch while keeping explicit typed options as highest precedence. Focused `ModelConfigurationStoreTests|MistralProviderTests` passed 22/22, full `Tau.Ai.Tests` passed 434/434, and `scripts/verify-agent-package-consumer.ps1 -SkipRestore -Json` passed 59 assertions including `configuredMistralOptionsType=MistralOptions`, `configuredMistralOptionsToolChoice=function:consumer_tool`, `configuredMistralOptionsToolChoiceKind=function`, `configuredMistralOptionsToolChoiceFunction=consumer_tool`, `configuredMistralOptionsPromptMode=reasoning`, `configuredMistralOptionsReasoningEffort=high` and `configuredMistralOptionsMaxTokens=321`. This closes the local Mistral string/function provider-specific option contract; remaining provider-specific maps and real cloud e2e remain open.

2026-06-19 AI Anthropic provider-specific options evidence: `ModelConfigurationStore` now parses the first Anthropic provider-specific `models.json options` slice, including `thinkingEnabled`, `thinkingBudgetTokens`, adaptive `effort`, `thinkingDisplay`, `interleavedThinking` and string/tool-object `toolChoice`; `StreamFunctions` projects that config into `AnthropicOptions` before provider dispatch while keeping explicit typed options as highest precedence and preserving explicit `SimpleStreamOptions.Reasoning` when typed dispatch is required. `AnthropicProvider` request-body tests cover budget/adaptive/disabled thinking, `output_config.effort`, `tool_choice`, `metadata.user_id` and beta header behavior. Focused `AnthropicProviderTests|ModelConfigurationStoreTests|AiPublicApiCompileSampleTests` passed 24/24, full `Tau.Ai.Tests` passed 441/441, and `scripts/verify-agent-package-consumer.ps1 -SkipRestore -Json` passed 69 assertions including `configuredAnthropicOptionsType=AnthropicOptions`, `configuredAnthropicOptionsThinkingEnabled=True`, `configuredAnthropicOptionsThinkingBudgetTokens=2345`, `configuredAnthropicOptionsEffort=high`, `configuredAnthropicOptionsThinkingDisplay=omitted`, `configuredAnthropicOptionsInterleavedThinking=True`, `configuredAnthropicOptionsToolChoice=tool:read_file` and `configuredAnthropicOptionsMaxTokens=654`. This closes the local Anthropic provider-specific option contract; real Anthropic cloud/OAuth e2e and remaining provider-specific maps remain open.

2026-06-19 AI Google provider-specific options evidence: `ModelConfigurationStore` now parses the first Google-family provider-specific `models.json options` slice, including `toolChoice`, `thinkingEnabled`, `thinkingBudgetTokens`, `thinkingLevel`, Vertex `project/location` and Gemini CLI `projectId`; `StreamFunctions` projects that config into `GoogleOptions`, `GoogleVertexOptions` or `GoogleGeminiCliOptions` before provider dispatch while keeping explicit typed options as highest precedence. Google/Vertex/Gemini CLI provider request-body tests cover tool choice, explicit thinking config and Gemini CLI project override behavior. Focused `ModelConfigurationStoreTests|GoogleGeminiCliProviderTests|GoogleVertexProviderTests|AiPublicApiCompileSampleTests` passed 39/39, full `Tau.Ai.Tests` passed 444/444, and `scripts/verify-agent-package-consumer.ps1 -SkipRestore -Json` passed 74 assertions including `configuredGoogleOptionsType=GoogleOptions`, `configuredGoogleOptionsToolChoice=any`, `configuredGoogleOptionsThinkingEnabled=True`, `configuredGoogleOptionsThinkingBudgetTokens=8765` and `configuredGoogleOptionsMaxTokens=765`. This closes the local Google-family provider-specific option contract; real Google/Vertex/Gemini CLI/Antigravity cloud/OAuth e2e and remaining provider-specific maps remain open.

2026-06-19 AI Bedrock provider-specific options evidence: `ModelConfigurationStore` now parses the first Bedrock provider-specific `models.json options` slice, including `region`, `profile`, `bearerToken`, string/tool-object `toolChoice`, `reasoning`, `thinkingBudgetTokens`, `thinkingDisplay`, `interleavedThinking` and `requestMetadata`; `StreamFunctions` projects that config into `BedrockOptions` before provider dispatch while keeping explicit typed options as highest precedence. `BedrockProviderTests` cover tool choice, request metadata, thinking display and non-adaptive Claude `anthropic_beta` request-body behavior. Focused `ModelConfigurationStoreTests|BedrockProviderTests` passed 39/39, full `Tau.Ai.Tests` passed 447/447, and `scripts/verify-agent-package-consumer.ps1 -SkipRestore -Json` passed 85 assertions including `configuredBedrockOptionsType=BedrockOptions`, `configuredBedrockOptionsRegion=us-west-2`, `configuredBedrockOptionsProfile=consumer-profile`, `configuredBedrockOptionsBearerToken=consumer-bedrock-token`, `configuredBedrockOptionsToolChoice=tool:read_file`, `configuredBedrockOptionsReasoning=High`, `configuredBedrockOptionsThinkingBudgetTokens=3456`, `configuredBedrockOptionsInterleavedThinking=True`, `configuredBedrockOptionsRequestMetadataApp=consumer` and `configuredBedrockOptionsMaxTokens=876`. This closes the local Bedrock provider-specific option contract; real AWS Bedrock cloud e2e, real credential chain behavior and remaining provider-specific maps remain open.

## Phase 2 Candidate Queue

| Candidate | Owner | Upstream evidence | Tau target | Status source | Validation gate | External e2e? |
| --- | --- | --- | --- | --- | --- | --- |
| Agent loop event payload and schema closure | AgentWorker | `packages/agent/src/agent-loop.ts`, `types.ts`; AI `utils/validation.ts` | `src/Tau.Agent/Runtime/**`, `src/Tau.Agent/Abstractions/**`, `src/Tau.Ai/Validation/ToolArgumentValidator.cs`, targeted `Tau.Agent.Tests` / `Tau.Ai.Tests` | Agent file-level rows: `agent-loop.ts` / `types.ts`; AI schema surface `partial`; Tau Agent contract now has payload/schema/error-result, public API compile-sample, high-level failure/cancel synthesis, low-level EventStream wrapper, parallel timing, low-level assistant stream cancellation, tool cancellation cleanup, parallel sibling result preservation and `TransformContext` cancellation baselines. Agent loop contract itself is now locally closed; AI validation helper baseline is public and now covers common AJV/TypeBox object/composition/string/number/array/property-count keywords, with full AJV/TypeBox runtime-codegen/CSP fallback parity still open. | `dotnet test tests\Tau.Agent.Tests\Tau.Agent.Tests.csproj --no-restore --verbosity minimal`; `dotnet test tests\Tau.Ai.Tests\Tau.Ai.Tests.csproj --filter ToolArgumentValidatorTests --no-restore --verbosity minimal` | No |
| AI public API/bin and helper export closure | AiWorker | `packages/ai/package.json`, `src/index.ts`, `src/cli.ts`, `utils/*`, `providers/faux.ts`, `providers/simple-options.ts` | `src/Tau.Ai/**`, `src/Tau.Ai.Cli/**`, public API compile sample, CLI runner tests, package consumer smoke, tool install smoke, package publish smoke, export-shape smoke | AI file-level and AI surface rows: package exports, `pi-ai`, helper utilities, shared stream options; `AiPublicApiCompileSampleTests` now covers current public surface including public helper baselines, streaming JSON parser, the public Faux provider, and callback/cancellation-capable shared stream options. Primary provider parser adoption for OpenAI-style, OpenAI Responses shared, Anthropic, Mistral and Bedrock is present. Standalone `tau-ai` CLI baseline is now present. ToolArgumentValidator now covers common AJV/TypeBox validation keywords used by Agent/tool schemas. Release/bin alias `pi-ai` command-name/help/error contract is locally closed through wrapper env injection and smoke; local dotnet tool install rehearsal now proves temp `pi-ai` / `tau-ai` tool packages can be installed and run from a local source; package publish smoke now also includes `Tau.Ai.Cli.PiAiTool` / `ToolCommandName=pi-ai` and `Tau.Ai.Cli.TauAiTool` / `ToolCommandName=tau-ai` in the default 3 library + 2 tool package publish command shape; the package consumer smoke now proves external `Tau.Ai` package consumers can directly use `Tau.Ai.Providers.Faux`, and external `Tau.Agent` package consumers can transitively use `Tau.Ai`; export-shape smoke now documents/verifies the Tau-native no-TypeScript-shim decision. `StreamOptionHelpers` now closes the local `onPayload` / `onResponse` / `thinkingBudgets` helper contract and local provider-wide cancellation plumbing across the main provider families. Full AJV/TypeBox runtime parity, real registry/global install, provider edge audit and real provider/OAuth cloud e2e remain open. | `dotnet build src\Tau.Ai\Tau.Ai.csproj --no-restore --verbosity minimal`; `dotnet build src\Tau.Ai.Cli\Tau.Ai.Cli.csproj --no-restore --verbosity minimal`; public API compile-sample/helper/Faux/CLI tests; full `Tau.Ai.Tests`; CLI `list` smoke; `scripts/verify-ai-cli-tool-install.ps1`; `scripts/verify-release-package-publish.ps1`; `scripts/verify-agent-package-consumer.ps1`; `scripts/verify-ai-agent-export-shape.ps1` | No |
| AI provider/OAuth real e2e matrix | AiWorker | `packages/ai/src/providers/**`, `utils/oauth/**` | `src/Tau.Ai/Providers/**`, `src/Tau.Ai/Auth/OAuth/**`, `scripts/verify-ai-provider-e2e-matrix.ps1`, e2e notes/history | AI provider rows marked `external-e2e-needed`; repo-owned inspect/isolation matrix harness and required-run no-op guard now exist | Targeted provider tests plus `verify-ai-provider-e2e-matrix.ps1 -RunConfigured -RequireConfigured` subset runs and real provider/OAuth smoke commands with redacted env evidence | Yes |
| CodingAgent top-level CLI/session/config parity | CodingAgentWorker | `src/cli.ts`, `src/main.ts`, `src/cli/args.ts`, `src/config.ts`, `package-manager-cli.ts`, `core/package-manager.ts`, `core/extensions/loader.ts`, migrations | `src/Tau.CodingAgent/Program.cs`, settings/session stores, `CodingAgentPackageManager.cs`, `CodingAgentExtensionCommandStore.cs`, `CodingAgentExtensionToolAdapter.cs`, `CodingAgentJavaScriptExtensionRuntime.cs`, `CodingAgentExtensionLifecycleEventSink.cs`, `CodingAgentStartupNoticeService.cs`, CLI/package/startup tests | CodingAgent file-level rows and CLI/config surface `partial`; top-level package source/list/config, local package resource discovery/filtering, upstream-style extension entry discovery/status, JavaScript/TypeScript `registerCommand` limited runtime, JavaScript/TypeScript `registerTool` limited runtime, prepareArguments baseline, `tool_call` block/input mutation / `tool_result` rewrite hook baseline, `registerFlag` / `registerShortcut` metadata/default/status baseline, CLI `--flag[=value]` value injection through `applyExtensionFlagValues`, interactive extension shortcut dispatch/conflict diagnostics baseline, agent/message/turn/tool_execution lifecycle event broadcast baseline, limited virtual package import stub baseline, npm/git install/update/remove execution, startup changelog/version state, install telemetry runtime baselines, CLI `--session <path>` explicit flat/jsonl file path selection baseline, CLI `--continue` / `--session-dir` local JSONL startup selection baseline, CLI `--fork <path>` direct JSONL tree session branch-copy baseline, CLI `--session <id-prefix>` / `--fork <id-prefix>` local/session-dir JSONL id-prefix lookup baseline, CLI `--resume` local interactive selector baseline, and CLI `--models` / `--list-models` / `--verbose` startup model scope/listing baseline are locally covered. Remaining CLI/session/package gaps are session id / partial id global `listAll` lookup, cross-project fork prompt, interactive config selector, full jiti import/alias/virtualModules parity, rich tool content/renderers, session/provider/context/input/user_bash/model/resource/before hooks, custom tool wrappers/migration, live `/reload` tool hot-swap, final `pi` package/bin identity, real npm/git network smoke, package consumer smoke and real telemetry backend e2e | `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter "CodingAgentInitialMessageBuilderTests|CodingAgentSessionTargetTests|CodingAgentStartupResumeResolverTests|CodingAgentModelListFormatterTests|CodingAgentCliModelScopeTests" --no-restore --verbosity minimal -m:1`; `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --no-restore --verbosity minimal -m:1` | No |
| CodingAgent RPC extension UI and streamed bash parity | CodingAgentWorker | `src/modes/rpc/*`, `src/core/bash-executor.ts`, extension UI request types | `CodingAgentRpcHost.cs`, `CodingAgentRpcClient.cs`, `CodingAgentRpcExtensionUiBridge.cs`, `CodingAgentJavaScriptExtensionRuntime.cs`, `CodingAgentShellRunner.cs`, RPC tests | RPC rows and `RPC extension UI protocol` now `partial`; streamed bash, extension UI bridge, real JS/TS extension fire-and-forget UI request emission, RPC client helper, `assistantMessageEvent` stream-event shape, `ToolCall.arguments` object-shape and prompt accepted/error preflight timing baselines are locally covered. Waiting extension UI dialogs from JS/TS handlers, full jiti/custom tool semantics, all remaining response/message schema details and exact command parity remain open | `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter "ExtensionToolUiFireAndForgetActionsEmitRpcRequests|ExtensionUiBridge|CodingAgentRpcHost" --no-restore --verbosity minimal`; `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter Rpc --no-restore --verbosity minimal` | No |
| CodingAgent image and clipboard ingestion | CodingAgentWorker | `src/utils/clipboard*`, `image-*`, `mime.ts`, `photon.ts`, `@file` handling | `src/Tau.CodingAgent/**`, possible `Tau.Tui` editor attachment seam | CodingAgent file-level utils row and image ingestion surface `partial`; CLI `@file` text/image initial prompt baseline plus local PNG resize / EXIF dimension metadata / read_file auto-resize wiring are locally covered, desktop clipboard/full Photon/provider vision remain open | Targeted CodingAgent tests for file/clipboard/image content plus guarded desktop clipboard smoke when available | Local desktop smoke |
| Tui terminal host, settings-list, terminal image, loader and rich markdown | TuiWorker | `packages/tui/src/terminal.ts`, `stdin-buffer.ts`, `terminal-image.ts`, `components/settings-list.ts`, `components/image.ts`, `components/markdown.ts`, `components/loader.ts`, `components/cancellable-loader.ts`, `tui.ts` | `src/Tau.Tui/**`, `tests/Tau.Tui.Tests/**` | Tui file-level rows and terminal/input/render surface; stdin-buffer / ProcessTerminal lifecycle / settings-list / loader / markdown / terminal-image foundations now have local tests, and CodingAgent `/settings select` consumes `TuiSettingsList` for its main settings surface. Live TTY/PTY smoke, hardware cursor, full settings submenu adoption/runtime wiring and complete TUI app host remain open | `dotnet test tests\Tau.Tui.Tests\Tau.Tui.Tests.csproj --no-restore --verbosity minimal`; live TTY/PTY smoke transcript | Local runtime smoke |
| WebUi CodingAgent branch/tree true persistence | WebUiWorker with CodingAgent contract review | `packages/web-ui/src/storage/**`, CodingAgent session semantics | `src/Tau.WebUi/**`, `tests/Tau.WebUi.Tests/**`; consume `CodingAgentTreeSessionStore` contract without changing it unless delegated | WebUi JSONL rows `partial`; WebUi storage schema surface | `dotnet test tests\Tau.WebUi.Tests\Tau.WebUi.Tests.csproj --filter "Jsonl|Session|Browser" --no-restore --verbosity minimal` | Browser smoke |
| WebUi sandbox/artifact runtime bridge | WebUiWorker | `components/SandboxedIframe.ts`, `components/sandbox/**`, `tools/artifacts/**`, `javascript-repl.ts` | `src/Tau.WebUi/**`, browser/API tests | WebUi sandbox/artifact rows now `partial`; per-session artifact store, CRUD/runtime endpoints, browser artifact pane, HTML artifact attachment helpers, one-time file-return metadata, artifact operation semantics, lightweight Markdown/SVG/PDF/image/text/generic viewer baseline, browser-side JavaScript REPL execution host, prompt/runtime provider descriptions, session-bound LLM-facing `artifacts` AgentTool wiring, and usable LLM-facing `javascript_repl` execution through an active browser bridge with file details/base64 are locally covered. Full artifacts tool/message reconstruction, Docx/Excel rich viewers, upstream pdfjs page renderer/toggles, reusable Lit component package, extension sandbox URL mode and release/static smoke remain open | `dotnet test tests\Tau.WebUi.Tests\Tau.WebUi.Tests.csproj --no-restore --verbosity minimal` plus browser artifact/repl smoke | Browser smoke |
| Mom real Slack receive/respond/file/stop smoke | MomWorker | `packages/mom/src/slack.ts`, `download.ts`, `store.ts` | `src/Tau.Mom/Slack/**`, Mom-related `tests/Tau.Agent.Tests/**`, e2e record | Mom Slack rows `external-e2e-needed` | Fake Slack tests plus real Socket Mode/Web API smoke with redacted token/source evidence | Yes |
| Mom Docker sandbox smoke and session schema parity | MomWorker | `sandbox.ts`, `tools/*`, `context.ts`, `store.ts`, `log.ts` | `src/Tau.Mom/**`, Mom sandbox/session/log tests | Mom sandbox/session/log rows `partial`/`external-e2e-needed` | Mom targeted tests plus real Docker `--validate-sandbox` and tool execution smoke | Yes |
| Pods command/config/known-model compatibility | PodsWorker | `packages/pods/src/cli.ts`, `config.ts`, `model-configs.ts`, `models.json`, `commands/models.ts`, `commands/prompt.ts`, `scripts/model_run.sh` | `src/Tau.Pods/Cli/**`, `Models/**`, `Services/**`, CodingAgent CLI arg parser/tests | Local known-model data/config planning baseline is now covered by `PodKnownModelRegistry`, bundled upstream `models.json`, planner/CLI JSON contract tests, top-level command shims, configured model state writeback/removal, duplicate configured deployment guard with top-level operation `failureKind`, realtime `logs --follow` over inherited-stdio SSH `tail -f ~/.vllm_logs/<name>.log`, deploy-time `startupWatch` marker wait / failure rollback, save-before-watch/delete-on-failure configured model ordering, local model runner wrapper / pseudo-TTY `script` contract, runner HF download / PI API key command contract, and pod-agent process runner config/argv/redaction contract. The row remains `partial` for broader command/runtime/e2e gaps. | `dotnet test tests\Tau.Pods.Tests\Tau.Pods.Tests.csproj --filter "PodsCli|Model|Config|DeployAsync|VllmDeploy|PodVllmCommandPlanner|Agent" --no-restore --verbosity minimal` | No |
| Pods real SSH/HF/setup/GPU/vLLM e2e | PodsWorker | `ssh.ts`, `commands/pods.ts`, `commands/models.ts`, scripts `pod_setup.sh` / `model_run.sh` | `src/Tau.Pods/Services/**`, e2e record/history | Pods SSH/setup/vLLM rows `external-e2e-needed` | Fake-runner tests plus real SSH/SCP/HF/setup/vLLM smoke with redacted env evidence | Yes |
| Release/no-env/build artifact parity | Main Integrator | root `package.json`, `test.sh`, `pi-test.sh`, `scripts/build-binaries.sh`, `scripts/release.mjs` | `scripts/**`, `.github/workflows/tau-ci.yml`, project packaging, CI/release docs | RID-specific release artifact baseline is present via `scripts/build-release-artifacts.ps1` and `scripts/smoke-release-artifacts.ps1`; archive packaging now supports `auto|zip|tar.gz`, host-RID executable smoke, non-host extraction-only structure validation, and `build-release-matrix.ps1` / `package-release-matrix.ps1` for the upstream five-platform matrix. PowerShell no-env wrapper baseline is present via `scripts/invoke-no-env.ps1` / `scripts/verify-no-env.ps1`, and `scripts/pi-test.ps1` covers the direct CodingAgent `pi-test.sh --no-env` role for PowerShell validation. Windows GitHub Actions CI now reuses the same release contract smoke, session audit smoke, CodingAgent auth/session/commands/tools migration smoke, CodingAgent deprecated extension dirs audit smoke, CodingAgent keybindings migration smoke, edit stats smoke, Mom timestamp smoke, startup profile smoke, version sync smoke, no-env smoke, Release artifact build, archive package/extract smoke and zip upload path plus tar.gz format structure smoke. Release payload baseline now copies current Tau docs and examples payload and audits upstream payload items in `manifest.json.releasePayload`. `Directory.Build.props` now owns Tau product `VersionPrefix`; `scripts/plan-release.ps1` uses it for dry-run planning of upstream `release.mjs` steps and now lists local release execution plus CodingAgent auth migration smoke, managed tools-to-bin migration smoke, deprecated extension dirs audit smoke and keybindings migration smoke; `scripts/execute-release.ps1` covers explicit clean-worktree local version+notes mutation, contract smoke, optional local validation, narrow staging, release commit and tag; `scripts/prepare-release.ps1` composes local version + release notes writes under explicit `-Apply` with clean-worktree and helper-preview guards; `scripts/validate-release.ps1` composes diff/no-env/matrix validation under explicit `-Run` and exposes `validationLevel` plus enabled/skipped validation metadata; `scripts/verify-release-contracts.ps1` fixes the dry-run JSON contract including execute dry-run mutation names without build/test/artifact mutation; `scripts/update-release-version.ps1` can update that MSBuild source when `-Apply` is explicit; `scripts/update-release-notes.ps1` can update `docs/releases/feature-release-notes.md` when `-Apply` is explicit; release manifest/smoke audit the same version source. Rows remain `partial` because publish/push execution automation, non-host runner executable smoke, exact Unix wrapper/auth-backup parity, Photon/interactive raster assets/external export-html payload parity and real external e2e release smoke are not complete. | `git diff --check`, `plan-release.ps1` dry-run, `execute-release.ps1` dry-run, `execute-release.ps1 -Apply` temp-repo smoke, `verify-release-contracts.ps1`, `verify-coding-agent-auth-migration.ps1`, `verify-coding-agent-tools-to-bin-migration.ps1`, `verify-coding-agent-deprecated-extension-dirs-audit.ps1`, `verify-coding-agent-keybindings-migration.ps1`, `validate-release.ps1` dry-run, `validate-release.ps1 -Run` local/small runtime validation, `update-release-version.ps1` temp props writeback smoke, `update-release-notes.ps1` temp notes writeback smoke, `verify-no-env.ps1 -SkipRestore`, `verify-no-env.ps1 -SkipRestore -RunSmoke`, artifact build/start smoke, archive package/extract smoke in clean output dir, GitHub Actions Windows workflow | Release smoke |

## External E2E Needed

Initial known list:

- AI provider family real e2e: OpenAI/Responses/Codex/Azure/Anthropic/Google/Vertex/Gemini CLI/Antigravity/Mistral/Bedrock/OpenAI-compatible.
- OAuth real e2e: OpenAI Codex, Anthropic, Gemini CLI/Antigravity, GitHub Copilot device flow.
- AWS/Bedrock e2e: SigV4, SSO refresh, OIDC registration renewal, AssumeRole/profile/credential_process.
- Slack e2e: Socket Mode receive/ack/respond/update/delete/download/stop.
- Docker e2e: Mom sandbox validate + tool execution.
- Pods e2e: SSH/HF/GPU/vLLM full path.
- WebUi e2e: browser smoke against release/static output.

## Next Inventory Freeze Steps

- Phase 1 final normalization is complete at grouped file-level granularity. The upstream package directory set is fully represented by this matrix.
- Use `Phase 2 Candidate Queue` as the first worker backlog. Each row includes owner, upstream evidence, Tau target, status source, validation gate and external-e2e flag.
- Do not reopen broad inventory unless upstream package directories or package manifests change, or an implementation worker finds a concrete missing path while working a candidate.
- Keep `external-e2e-needed` entries open until real provider/service/runtime smoke exists or the user explicitly accepts a non-goal.
