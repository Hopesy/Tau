# Tau 迁移对比与计划

本文件记录 Tau(.NET 10)对照上游参考项目 **pi**(TypeScript,本机 `C:\Users\zhouh\Desktop\pi-main`)的逐模块迁移状态与后续计划。

> 目标:与 pi 主线 `packages/` 下四个模块达到功能对齐(parity)。
> 状态图例:**PORTED**(已移植) / **PARTIAL**(部分移植) / **MISSING**(缺失) / **N/A**(平台差异,无需移植)。

---

## 一、总览

| 模块 | pi package | Tau project | 迁移度 | 状态 |
|---|---|---|---|---|
| Agent 运行时 | `agent` | `src/Tau.AgentCore` | **~98%** | 基本收口 |
| AI 底座 | `ai` | `src/Tau.Ai` | **~89%** | 接近收口 |
| 终端 UI | `tui` | `src/Tau.Tui` | **~77%** | 主体完成,底层输入待补 |
| Coding Agent 产品 | `coding-agent` | `src/Tau.CodingAgent` | **~81%** | 核心完整,长尾缺口集中 |
| **加权综合** | | | **~82%** | |

代码规模:Tau 源码约 8.2 万行 / 测试约 5.1 万行。最近全量验证基线为 `dotnet test Tau.slnx --no-restore`:1978 个测试全部通过(Tau.Ai 581 / Tau.AgentCore 152 / Tau.Tui 358 / Tau.CodingAgent 887)。

当前架构边界:已按本机参考项目 `C:\Users\zhouh\Desktop\pi-main` 简化为 `Tau.Ai`、`Tau.AgentCore`、`Tau.CodingAgent`、`Tau.Tui` 四个产品/基础模块及对应 tests/examples。`Tau.WebUi`、`Tau.Mom`、`Tau.Pods`、`Tau.Ai.Cli` 属于旧复杂架构残留,已清理,不纳入当前 parity 口径。

测量口径说明:迁移度按**功能区文件覆盖**估算,而非逐行 diff。TS→C# 行数膨胀使行数不可直接比较;pi 中大量 `.models.ts`(纯模型目录)与极薄 provider 配置被 Tau 的**代码生成模型目录 + OpenAiCompat 兼容层**统一吸收,计为已覆盖。

### 最近完成的可验证切片

| 提交 | pi 参考能力 | Tau 落点 | 状态/验证 |
|---|---|---|---|
| `fc42fa7` | `countdown-timer.ts` | `CodingAgentCountdownTimer` + auto-retry 倒计时状态 | DONE;全量 1795 tests passed |
| `373e0b0` | `show-images-selector.ts` | `CodingAgentShowImagesSelector` + settings show-images 子菜单 | DONE;全量 1792 tests passed |
| `4b792ce` | `image-resize-worker.ts` | `CodingAgentImageResizeWorker` 线程卸载边界 | DONE;全量 1783 tests passed |
| `8191f5e` | `clipboard-image.ts` + Ctrl+V 图片输入 | `SystemCodingAgentClipboard` + direct `ImageContent` 输入 | DONE;全量 1780 tests passed |
| `ed733b9` / `6dac3aa` / `7dcde8a` | `image-convert.ts` / `image-resize*.ts` | ImageSharp PNG/JPEG/WebP decode、EXIF auto-orient、Lanczos3 resize、PNG/JPEG candidate/fallback | DONE;后续切片全量验证覆盖 |
| `037d0dc` | `sdk.ts` | `CodingAgentSdk.CreateSessionAsync` 编程式 session 入口 | DONE;后续切片全量验证覆盖 |
| `65c7764` | `skill-invocation-message.ts` / `custom-message.ts` 基础展示语义 | `CodingAgentMessageDisplayFormatter` + `TuiMessageRole.Skill/Custom` + skill 展示接线 | DONE;全量 1801 tests passed |
| `e3e81f8` | `compaction-summary-message.ts` / `branch-summary-message.ts` 折叠展示语义 | `CodingAgentMessageDisplayFormatter` + command `DisplayMessages` + `TuiMessageRole.CompactionSummary/BranchSummary` | DONE;全量 1806 tests passed |
| `9276d40` | `assistant-message.ts` / `user-message.ts` OSC 133 prompt zone 基线 | `InteractiveConsoleSession` 对 user/assistant 直接终端输出写入 OSC 133 start/end/final 边界 | DONE;全量 1807 tests passed |
| `6d2f248` | `visual-truncate.ts` 宽度感知尾部预览截断 | `TuiText.TruncateToVisualLines` + `TuiVisualTruncateResult` | DONE;全量 1811 tests passed |
| `1393649` | `bash-execution.ts` 组件渲染基线 | `TuiBashExecution` + 宽度感知输出 preview / expanded / status / truncation tests | DONE;全量 1816 tests passed |
| `60ed0e9` | `tool-execution.ts` fallback 渲染基线 | `TuiToolExecution` + call args / text output / image fallback / pending-success-error background tests | DONE;全量 1820 tests passed |
| `2d43ff6` | bash/tool execution Host transcript 接线 | `CodingAgentHost` start/update/end → `TuiBashExecution` / `TuiToolExecution` + `InteractiveConsoleSession.WriteToolComponent` | DONE;全量 1822 tests passed |
| `eb219b0` | bash/tool execution transcript keyed update | `TranscriptEntry.Key` + keyed `WriteToolComponent`,同一 toolCallId 更新同一条 transcript | DONE;全量 1823 tests passed |
| `4852060` | `tool-execution.ts` image block 渲染 | `TuiToolExecution` 在 Kitty/iTerm2 能力下接入 `TuiImage` 真实终端图片输出,关闭图片或无协议时保持 fallback | DONE;全量 1826 tests passed |
| `96514ee` | `tool-execution.ts` Kitty 非 PNG 转换 | `CodingAgentHost` 在 Kitty 能力下复用 `CodingAgentImageConverter` 将 JPEG/WebP/GIF tool image block 转 PNG 后交给 `TuiToolExecution` 渲染 | DONE;全量 1827 tests passed |
| `b857a6d` | `footer.ts` stats line 基线 | `CodingAgentFooterFormatter` + Host composition status 显示 usage in/out/cache、cost、context percent/window 和 model/provider | DONE;全量 1828 tests passed |
| `826e846` | `footer-data-provider.ts` watcher parity | `CodingAgentFooterDataProvider` HEAD 目录监听、atomic HEAD replace、reftable path/tables.list 监听接线、WSL `/mnt/<drive>` polling 判定 | DONE;全量 1831 tests passed |
| `b20c555` | `footer.ts` cwd/session line 语义 | `CodingAgentFooterFormatter.FormatCwdForFooter` + Host 默认 composition status 显示 cwd(home→`~`)、branch、session name 和 extension status | DONE;全量 1835 tests passed |
| `f245002` | `utils/git.ts` hosted provider 矩阵 | `CodingAgentGitUrlParser` 补 GitHub shorthand、GitHub tree ref、GitLab nested group、Bitbucket、Gist、Sourcehut、`www.` domain 归一化 | DONE;全量 1844 tests passed |
| `e80ab4f` | `syntax-highlight.ts` theme 色表 | `TuiSyntaxHighlightTheme` + `TuiMarkdownTheme.SyntaxHighlightTheme` + `CodingAgentTheme.ToSyntaxHighlightTheme()` 接入 `syntax*` 颜色 | DONE;全量 1849 tests passed |
| 上轮切片 | `footer.ts` 多行 FooterComponent 布局 | `TuiStatusBar`/`TuiTranscriptViewport` 支持多行状态区;Host 默认 footer 拆为位置行、stats/model 行和扩展状态行,临时状态保持单行覆盖 | DONE;全量 1852 tests passed |
| 本次切片 | extension `ctx.ui.setFooter()` custom footer factory | `CodingAgentJavaScriptExtensionRuntime` 支持 function/component/string/array footer 转文本行;`CodingAgentRpcExtensionUiBridge` + `CodingAgentFooterDataProvider` + formatter 将自定义 footer 替换默认 footer,清除后回退默认 | DONE;全量 1854 tests passed |
| 本次切片 | extension `ctx.ui.setWorkingMessage()` / `setWorkingIndicator()` | JS extension UI action + RPC bridge + `CodingAgentFooterDataProvider` working 状态;Host running 状态显示自定义 indicator/message;Node extension runtime 改为临时 `.cjs` 脚本启动,避开 Windows `node -e` 命令行长度上限 | DONE;全量 1858 tests passed |
| 本次切片 | extension `ctx.ui.setHeader()` + `session_start` header 示例链路 | JS runtime 支持 `session_start` 事件、`ctx.mode`、function/component/string/array header 转文本行;`CodingAgentRpcExtensionUiBridge` + `CodingAgentFooterDataProvider` 存储/清除自定义 header;Host 启动欢迎区可由扩展替换内置 header | DONE;全量 1860 tests passed |
| 本次切片 | extension `ctx.ui.setHiddenThinkingLabel()` | JS extension UI action + RPC bridge + `CodingAgentFooterDataProvider` hidden thinking label 状态;Host thinking 输出前缀使用扩展 label,清除后回退默认 `thinking` | DONE;全量 1862 tests passed |
| 本次切片 | `assistant-message.ts` hidden thinking 历史/树预览语义 | `CodingAgentMessageDisplayFormatter.FormatAssistantMessage` 支持隐藏 thinking block 的默认/自定义 label;`/find` 可命中 thinking 内容;tree view/metadata 对 thinking-only assistant 显示 `Thinking...` 而不是空 preview,且不在导航列表泄露内部 reasoning 全文 | DONE;全量 1866 tests passed |
| 本次切片 | settings `hideThinkingBlock` + `assistant-message.ts` hidden thinking streaming | `CodingAgentSettingsStore`/`CodingAgentSettingsSelector`/`CodingAgentCommandRouter` 支持 `hideThinkingBlock` 设置持久化与 settings 选择器;`CodingAgentHost` 在隐藏开启时把流式 thinking 折叠为一次静态 label,不向终端 transcript 输出 reasoning delta | DONE;全量 1867 tests passed |
| 本次切片 | `app.thinking.toggle` thinking block 快捷切换 | `EditorAction.ToggleThinkingBlock` + 默认 `Ctrl+T` keybinding + `CodingAgentHost` action handler;交互中可即时翻转 `hideThinkingBlock`、持久化 settings,并影响下一轮流式 thinking 展示 | DONE;全量 1870 tests passed |
| 本次切片 | `app.tools.expand` tool output 快捷展开 | `EditorAction.ToggleToolOutputExpansion` + 默认 `Ctrl+O` keybinding + `CodingAgentHost` action handler;active bash/tool execution 组件可统一切换 expanded/collapsed,普通 tool 文本输出补尾部 preview | DONE;全量 1875 tests passed |
| 本次切片 | `/hotkeys` 快捷键发现面收口 | `CodingAgentHotkeysFormatter` 对 `ToggleThinkingBlock` / `ToggleToolOutputExpansion` / `OpenExternalEditor` / `QueueFollowUpMessage` / `RestoreQueuedMessages` 输出稳定 action 名、默认键位和上游描述;`/hotkeys` 可发现 `Ctrl+T` thinking block、`Ctrl+O` tool output、`Ctrl+G` external editor、`Alt+Enter` follow-up 与 `Alt+Up` dequeue | DONE;全量 1887 tests passed |
| 本次切片 | `app.editor.external` 外部编辑器基线 | `EditorAction.OpenExternalEditor` + 默认 `Ctrl+G` keybinding + `ICodingAgentExternalEditor`;Host 按上游优先 `$VISUAL`/`$EDITOR`,编辑期间停止 composition TUI,0 退出后读取临时文件回填草稿,并在 `/hotkeys` 暴露 | DONE;全量 1887 tests passed |
| 本次切片 | `tui.input.newLine` + `app.message.followUp`/`app.message.dequeue` 快捷键 | `EditorAction.NewLine` 对齐上游 `Shift+Enter`/`Ctrl+J` 换行;`Alt+Enter` 从旧换行语义改为 queue follow-up action;`Alt+Up` 恢复 queued steering/follow-up messages 到草稿;TUI editor action 保留草稿并由 Host 分发 | DONE;全量 1887 tests passed |
| 本次切片 | extension `ctx.ui.editor()` RPC editor 请求链路 | `CodingAgentJavaScriptExtensionRuntime` 增加运行中 UI request/response 行协议;JS/TS extension 可 `await ctx.ui.editor(title,prefill)`,Tau 通过 `CodingAgentRpcExtensionUiBridge.EditorAsync` 发出 upstream `extension_ui_request method=editor`,收到 `extension_ui_response` 后把编辑结果返回给 extension command/tool/shortcut handler;普通 notify/setEditorText 等 fire-and-forget action 保持批量 actions 路径 | DONE;全量 1888 tests passed |
| 本次切片 | `provider-attribution.ts` 产品归因 / opencode session header | `CodingAgentProviderAttribution` 镜像上游 OpenRouter/NVIDIA NIM/Cloudflare/Vercel AI Gateway 归因 header 与 `x-opencode-session`/`x-opencode-client` session header(按 provider 或 baseUrl host 判定),telemetry 关闭时不发归因;`RuntimeCodingAgentRunner` 在每次 `RunAsync` 按当前 model 动态计算并把归因作为最低优先级基底合并进 stream headers(model 切换后自动重算),`Program.cs`/`CodingAgentSdk` 用 `CodingAgentTelemetry` + `PI_TELEMETRY` 接线 gate | DONE;全量 1912 tests passed |
| 本次切片 | `provider-display-names.ts` 内置 provider 显示名 | `CodingAgentProviderDisplayNames` 镜像上游 35 条内置 provider id→显示名映射,`Resolve` 回退 provider id 本身、`IsBuiltInProvider` 对应 `isApiKeyLoginProvider` 首段判定;`CodingAgentAuthSelector` 登录/登出 provider 列表用显示名作 label、保留 provider id 作 value | DONE;全量 1932 tests passed |
| 本次切片 | `utils/mime.ts` 图片 MIME 嗅探 parity | `CodingAgentImageMimeDetector` 镜像上游 magic-byte 嗅探:progressive JPEG marker `0xf7` 拒绝、PNG 要求合法 `IHDR`(length 13)、animated PNG(`IDAT` 前出现 `acTL`)拒绝、4100 字节嗅探窗口;`ReadFileTool` 与 `CodingAgentInitialMessageBuilder` 两处内联探测改为复用该 helper。收紧检测暴露并修正了此前把 APNG / progressive JPEG 误判为可内联 PNG/JPEG 的潜在生产 bug | DONE;全量 1945 tests passed |
| 本次切片 | hidden easter egg components 文本基线 | `CodingAgentEasterEggFormatter` 对齐上游 `armin.ts` XBM 最终帧、`earendil-announcement.ts` 公告文本和 `daxnuts.ts` OpenCode Kimi K2.5 提示;`/arminsayshi`、`/dementedelves` 隐藏命令返回 custom display message,模型切换到 `opencode/*kimi-k2.5*` 时附加 daxnuts custom display message | DONE;全量 1955 tests passed |
| 本次切片 | extension `MessageRenderer` custom-message renderer 文本基线 | `CodingAgentJavaScriptExtensionRuntime` 支持 `registerMessageRenderer` registry 与 `renderMessage` 调用;`CodingAgentExtensionCommandStore` 暴露 `LoadMessageRenderers` / `TryRenderCustomMessage`,按扩展加载顺序第一个 renderer 生效,renderer 失败或空输出回退默认 custom-message formatter | DONE;全量 1957 tests passed |
| 本次切片 | extension `pi.sendMessage({ customType, content, display, details })` command/shortcut 展示链路 | JS extension command/shortcut 中的 custom message action 会保存 `AgentCustomMessage`,优先使用同扩展注册的 `MessageRenderer` 预渲染为 `custom>` transcript;无 renderer 时回退默认 `[customType]` 文本,`display:false` 不展示;slash extension command 与 extension shortcut 均不会误触发 runner 或 completed status | DONE;全量 1963 tests passed |
| 本次切片 | extension custom message 非 streaming/no-trigger session 持久化 | extension command/shortcut 产生的 `AgentCustomMessage` 会追加到 runner 当前会话状态,并通过 `CodingAgentSessionStore` flat JSON 与 `CodingAgentTreeSessionStore` tree JSONL round-trip 保存 `customType`、content、display、details、timestamp;`display:false` 消息不渲染但仍可持久化 | DONE;全量 1965 tests passed |
| 本次切片 | extension custom message `sendMessage(message, options)` delivery options | `CodingAgentJavaScriptExtensionRuntime` 捕获 `triggerTurn` 与合法 `deliverAs:"steer"|"followUp"|"nextTurn"`;`CodingAgentExtensionCommandStore` 保留每条 custom message 的投递选项;`ICodingAgentRunner`/`RuntimeCodingAgentRunner` 支持 typed `ChatMessage` steer/followUp/run 与批量 prompt,并把 `AgentHarnessMessages.ConvertToLlm` 接入 CodingAgent provider context,避免 custom/summary 等 harness 消息在真实 LLM 请求中丢失;Host 非 streaming 时按上游优先级处理 `nextTurn` pending、`triggerTurn` custom turn、显式 steer/followUp 队列,streaming 时按 followUp/steer 排队 | DONE;全量 1971 tests passed |
| 本次切片 | extension custom message append-only `message_start`/`message_end` lifecycle | 非 streaming/no-trigger 的 append-only custom message 在追加 runner state、写 session store 后,会按上游 `sendCustomMessage()` 发布 `message_start` 与 `message_end`;JS lifecycle handler 可读取 `role:"custom"`、`customType`、content、display、details、timestamp,且 `display:false` 只影响 transcript 展示,不影响持久化或 lifecycle 事件 | DONE;全量 1973 tests passed |
| 本次切片 | queued custom/user message streaming lifecycle | `AgentRuntime` 在 drain steering/followUp queued messages 后不再静默追加 state,而是在每个 turn 的 assistant response 前按上游顺序发布 `message_start`/`message_end` 再加入上下文;`RuntimeCodingAgentRunner` 覆盖 queued `AgentCustomMessage` 经 JS lifecycle handler 暴露 `customType`、content、display、details、timestamp,并让 typed custom queued message 可被 `DrainQueuedMessages()` 恢复为草稿文本 | DONE;全量 1976 tests passed |
| 本次切片 | extension `message_end` replacement semantics | `AgentLoopConfig.MessageEndTransformAsync` + `CodingAgentExtensionLifecycleEventSink.TransformMessageEndAsync` + JS runtime `emitEvent` 返回 `replacementMessage`;extension `pi.on("message_end")` 可按上游返回同 role 消息替换 finalized message,替换结果会进入 `MessageEndEvent`、runner state 和下一轮 provider context;不同 role/type 的替换被拒绝并记录 extension error | DONE;全量 1978 tests passed |

本轮文档更新只同步迁移跟踪状态,不把整体迁移度上调为 100%。剩余工作仍集中在交互 UI 专用渲染、footer/header/working/thinking 动态 width-aware 组件保真与动画、真实运行时 smoke、TUI 真实终端输入 smoke、overlay/focus 引擎和高亮/分词长尾。

---

## 二、Tau.AgentCore(~98%)

23 个有效 TS 文件,22 个完整移植。双循环执行模型直接对标 `agent-loop.ts`。

### 文件对照

| pi 文件 | Tau 对应 | 状态 |
|---|---|---|
| `agent.ts` | `Agent.cs` | PORTED |
| `agent-loop.ts` | `Runtime/AgentRuntime.cs`(+ `AgentLoopConfig`/`AgentQueueMode`/`ToolExecutor`/`ContextTransformer`) | PORTED |
| `types.ts` | `Abstractions/*` + `Runtime/AgentLoopConfig.cs` | PORTED |
| `proxy.ts` | `Proxy/ProxyStreamProvider.cs` | PORTED |
| `harness/agent-harness.ts` | `Harness/AgentHarness.cs` | PORTED |
| `harness/messages.ts` | `Harness/AgentHarnessMessages.cs` | PORTED |
| `harness/prompt-templates.ts` | `Harness/AgentHarnessPromptTemplates.cs` | PORTED |
| `harness/skills.ts` | `Harness/AgentHarnessSkills.cs` | PORTED |
| `harness/system-prompt.ts` | 合并入 `AgentHarnessSkills.cs` | PORTED |
| `harness/types.ts` | `Harness/Session/SessionTypes.cs` + 分散记录 | PARTIAL |
| `harness/utils/shell-output.ts` | `Harness/ShellOutputCapture.cs` | PORTED |
| `harness/utils/truncate.ts` | `Harness/ToolOutputTruncator.cs` | PORTED |
| `harness/compaction/compaction.ts` | `Harness/Compaction/AgentCompaction.cs` | PORTED |
| `harness/compaction/branch-summarization.ts` | `Harness/Compaction/AgentBranchSummaries.cs` | PORTED |
| `harness/compaction/utils.ts` | `Harness/Compaction/AgentCompactionSummaries.cs` | PORTED |
| `harness/session/session.ts` | `Harness/Session/AgentHarnessSession.cs` | PORTED |
| `harness/session/jsonl-repo.ts` | `Harness/Session/JsonlSessionRepo.cs` | PORTED |
| `harness/session/jsonl-storage.ts` | `Harness/Session/JsonlSessionStorage.cs`(+ `JsonlSessionSerialization`) | PORTED |
| `harness/session/memory-repo.ts` | `Harness/Session/InMemorySessionRepo.cs` | PORTED |
| `harness/session/memory-storage.ts` | `Harness/Session/InMemorySessionStorage.cs` | PORTED |
| `harness/session/repo-utils.ts` | `Harness/Session/SessionRepoUtilities.cs` | PORTED |
| `harness/session/uuid.ts` | `Harness/Session/UuidV7.cs` | PORTED |
| `harness/env/nodejs.ts` | `Harness/Env/AgentExecutionEnv.cs` | PORTED |
| `node.ts` | — | N/A(barrel 重导出) |

Tau 额外结构(无 pi 对应):`Platform/*` 宿主层(`AgentApplication`、`AgentApplicationBuilder` 等)、`Extensions/IAgentExtension.cs`。

### 缺口

- `Result<T,E>` 错误模式被 C# 异常替代(刻意的范式转换,非功能缺失)。

---

## 三、Tau.Ai(~89%)

所有真实 API 实现全部到位。

### 文件对照(摘要)

| pi 区域 | Tau 对应 | 状态 |
|---|---|---|
| `api/anthropic-messages.ts` | `Providers/Anthropic/*`(Provider/Converter/StreamParser) | PORTED |
| `api/openai-completions.ts` | `Providers/OpenAi/*` | PORTED |
| `api/openai-responses.ts` | `Providers/OpenAiResponses/OpenAiResponsesProvider.cs` | PORTED |
| `api/openai-codex-responses.ts` | `Providers/OpenAiResponses/{OpenAiCodexResponsesProvider,CodexWebSocketTransport}.cs` | PORTED |
| `api/azure-openai-responses.ts` | `Providers/OpenAiResponses/AzureOpenAiResponsesProvider.cs` | PORTED |
| `api/google-generative-ai.ts` + `google-shared.ts` | `Providers/Google/{GoogleProvider,GoogleMessageConverter,GoogleStreamParser}.cs` | PORTED |
| `api/google-vertex.ts` | `Providers/Google/{GoogleVertexProvider,GoogleVertexAccessTokenResolver}.cs` | PORTED |
| `api/bedrock-converse-stream.ts` | `Providers/Bedrock/*`(含完整 SigV4 + 凭证链:SSO/ECS/IMDS/Profile/WebIdentity/AssumeRole) | PORTED |
| `api/mistral-conversations.ts` | `Providers/Mistral/MistralProvider.cs` | PORTED |
| `api/openrouter-images.ts` | `Providers/OpenRouter/OpenRouterImagesProvider.cs` | PORTED |
| `api/simple-options.ts` | `Providers/StreamOptionHelpers.cs` + `OpenAiResponsesOptions.cs` | PORTED |
| `api/*.lazy.ts` | — | N/A(lazy 流式基础设施 C# 不需要) |
| `api/cloudflare.ts`(base-URL 常量) | `Providers/Cloudflare/CloudflareAuthResolver.cs` | PORTED |
| `api/openai-prompt-cache.ts`(64 字符截断) | `OpenAiResponsesShared.ClampOpenAiPromptCacheKey` | PORTED |
| `api/transform-messages.ts`(跨 provider 视觉剥离) | `Utilities/MessageTransformer.cs` + provider 接入 | PORTED |
| ~35 个薄 provider(deepseek/groq/cerebras/xai/together/...) | `OpenAiCompatibleProvider` + `ModelConfigurationStore` 动态注册 + env key | PORTED-via-compat |
| `providers/cloudflare-*` + `cloudflare-auth.ts` | `Providers/Cloudflare/CloudflareAuthResolver.cs` + `StreamFunctions` request dispatch | PORTED |
| `auth/*` | `Auth/{ProviderAuthResolver,EnvironmentApiKeyResolver,OAuthCredentialStore,...}.cs` | PORTED |
| `utils/oauth/*` | `Auth/OAuth/*`(Anthropic/GitHubCopilot/OpenAICodex/PKCE/CallbackServer) | PORTED |
| `utils/*`(hash/headers/json-parse/overflow/sanitize/validation/event-stream) | `Utilities/*` + `Streaming/*` + `Security/*` + `Validation/*` | PORTED |
| `utils/node-http-proxy.ts` | `Utilities/NodeHttpProxyResolver.cs` + `TauHttpClientFactory` | PORTED |
| `cli.ts`(交互式 OAuth 登录 CLI) | `Tau.CodingAgent` 顶层 `auth list` / `auth login [provider]` / `login [provider]` | PORTED-via-host |
| `models.generated.ts` | `Registry/GeneratedBuiltInModels.g.cs` 嵌入式 seed loader + `generated-models.seed.json` | PARTIAL |
| 各厂商 `*.models.ts` 目录数据 | 已 seed OpenAI-compatible 与混合 API 目录(deepseek/groq/cerebras/xai/together/openrouter/moonshot/nvidia/huggingface/xiaomi/zai/ant-ling/cloudflare-workers-ai/opencode/fireworks/cloudflare-ai-gateway 等) | PARTIAL(数据) |

Tau 额外:`Auth/OAuth/Providers/{GeminiCliOAuthProvider,AntigravityOAuthProvider,GoogleProjectDiscovery}.cs`。

### 缺口

- **Cloudflare**(`cloudflare-auth.ts` + `cloudflare.ts`)已补齐:支持 Workers AI / AI Gateway 的账户/网关 ID 解析、`{CLOUDFLARE_ACCOUNT_ID}`/`{CLOUDFLARE_GATEWAY_ID}` base-URL 模板替换、AI Gateway `cf-aig-authorization` 认证头与 `auth.json` api_key `env`。
- `utils/node-http-proxy.ts`已补齐:默认 provider/OAuth HTTP 路径通过 `TauHttpClientFactory` 尊重 `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` 与 `NO_PROXY`,支持大小写 env、端口/后缀绕过、无 scheme 代理补齐、HTTP/HTTPS 协议校验与 proxy userinfo credentials。
- `cli.ts`:已在现有 `Tau.CodingAgent` 可执行入口补齐 OAuth provider list/login 子命令,复用 Tau auth store 写入 `auth.json`。
- `openai-prompt-cache`:已补 `prompt_cache_key` 64 Unicode code point 截断助手,并接入 OpenAI Responses / Azure Responses / Codex Responses 请求体。
- `transform-messages`:跨 provider 非视觉模型图片占位已提取到 `Utilities/MessageTransformer.cs`,并接入 OpenAI / OpenAI Responses / Anthropic / Google / Bedrock / Mistral 请求转换入口。上游同文件里的 thinking/tool-call 规范化语义在 Tau 中仍分散在各 provider converter 内实现。
- **数据缺口**:OpenAI-compatible 与混合 API 厂商内置模型目录已批量补齐。剩余不是目录条目本身,而是上游 `compat` 的深层行为 parity;Anthropic `cache_control`/`supportsCacheControlOnTools`/`supportsEagerToolInputStreaming` 已接入,禁用 eager tool input 且当前有 tools 时会按上游发送 `fine-grained-tool-streaming-2025-05-14` beta header,`forceAdaptiveThinking: false` 已可覆盖内置 adaptive 模型名规则并回退 legacy thinking,SimpleStreamOptions reasoning 在 adaptive thinking 下已按上游发送 `output_config.effort`,`thinkingLevelMap.off: null` 已保留为 `SupportsDisabledThinking=false` 并在 thinking off 时省略 `thinking.type=disabled`,Anthropic empty thinking signature replay 已按 `allowEmptySignature` 兼容开关对齐,OpenAI-compatible `cacheControlFormat: "anthropic"` 的 message/tool `cache_control` 已接入,chat-completions `prompt_cache_key`/`prompt_cache_retention` 已接入;OpenAI Chat Completions provider-specific options 路径已对齐上游,不再把模型目录 `MaxOutputTokens` 作为默认请求 `max_tokens`/`max_completion_tokens`,仅调用方或 models.json 显式 `maxTokens` 时发送;已有 tool call/tool result 历史但当前无 tools 时,Chat Completions 请求已按上游发送 `tools: []`,兼容 Anthropic/LiteLLM 代理。

---

## 四、Tau.Tui(~75%)

### 文件对照

| pi 文件 | Tau 对应 | 状态 |
|---|---|---|
| `components/box.ts` | `Components/TuiBox.cs`(+ `TuiContainer`) | PORTED |
| `components/{loader,cancellable-loader}.ts` | `Components/Tui{Loader,CancellableLoader}.cs` | PORTED |
| `components/{spacer,text,truncated-text}.ts` | `Components/Tui{Spacer,TextBlock,TruncatedText}.cs` | PORTED |
| `components/markdown.ts` | `Components/TuiMarkdown.cs` | PORTED |
| `components/{select-list,settings-list}.ts` | `Components/Tui{SelectList,MultiSelectList,SettingsList}.cs` | PORTED |
| `components/image.ts` | `Components/TuiImage.cs` | PORTED |
| `autocomplete.ts` | `Runtime/TuiAutocompleteProvider.cs` | PORTED |
| `fuzzy.ts` | `Components/TuiFuzzyMatcher.cs` | PORTED |
| `kill-ring.ts` / `undo-stack.ts` / `word-navigation.ts` | 合并入 `Runtime/InteractiveInputEditor.cs` | PORTED |
| `stdin-buffer.ts` | `Runtime/TuiInputSequenceBuffer.cs` | PORTED |
| `terminal.ts` | `Runtime/TuiProcessTerminal.cs`(+ `SystemConsoleTerminal`) | PORTED |
| `terminal-image.ts` | `Rendering/TuiTerminalImage.cs` | PORTED |
| `components/input.ts` | `Runtime/InteractiveInputEditor.cs`(单行子集) | PARTIAL |
| `components/editor.ts`(2307 行) | `Runtime/InteractiveInputEditor.cs`(1150 行) | PARTIAL |
| `keybindings.ts` | `Runtime/KeyBindingMap.cs`(+ `Abstractions/EditorAction.cs`) | PARTIAL;已补 `tui.input.newLine`、`app.tools.expand`、`app.thinking.toggle`、`app.editor.external`、`app.message.followUp`、`app.message.dequeue` |
| `keys.ts`(1400 行) | `Runtime/TuiKeyDecoder.cs` + `Runtime/TuiInputSequenceBuffer.cs` + `Runtime/TuiDecodedKeyReader.cs` + `Runtime/TuiConsoleRawMode.cs` | PARTIAL |
| `tui.ts`(1714 行) | `Runtime/{TuiCompositionHost,TuiTranscriptViewportHost,TuiOverlayHost,...}.cs` | PARTIAL |
| `utils.ts`(text/width/wrap) + `coding-agent/components/visual-truncate.ts` | `Rendering/TuiText.cs` | PARTIAL |
| `terminal-colors.ts` | `Runtime/TuiTerminalColors.cs` + `Runtime/TuiProcessTerminal.cs` | PORTED |
| `native-modifiers.ts` | — | MISSING(macOS 原生插件) |

### 缺口

- **`keys.ts`(1400 行)按键解码器** — `Runtime/TuiKeyDecoder.cs` 已覆盖 VT/kitty/CSI-u/modifyOtherKeys 解析、`parseKey`/`matchesKey`/printable decode/key release/repeat,并同步 `TuiProcessTerminal` 的 Kitty protocol 状态。`TuiProcessTerminal` 已按上游启动序列 push Kitty flags、query flags、追加 DA sentinel,并在 Kitty 不可用或 DA 先返回时启用 modifyOtherKeys fallback。已新增 `Runtime/TuiDecodedKeyReader.cs`/`TuiConsoleKeyInfoMapper`/`TuiStreamRawInputReader`/`TuiConsoleRawMode.cs`,把 raw sequence decoder 桥接到现有 `IConsoleKeyReader`/交互编辑器路径;`SystemConsoleKeyReader` 已在交互式终端默认启用 raw stdin 路径,管道/重定向场景回退 `.NET Console.ReadKey`,并支持 `TAU_TUI_RAW_INPUT=1/0` 强制开关,raw 路径进入/恢复终端 raw mode。剩余缺口是真实终端交互 smoke 与逐步替代 `.NET ConsoleKeyInfo` 直读。
- **`terminal-colors.ts`** — 已新增 `Runtime/TuiTerminalColors.cs`,并把 OSC 11 背景色查询、迟到响应消费、`?997` 明暗报告查询/通知接入 `TuiProcessTerminal`。
- **`editor.ts` paste-marker 系统** — 普通 backspace/delete/左右光标已按 text element 边界处理,避免拆开 surrogate pair 与组合音标;Ctrl+word 移动/删除已补 text element、ASCII 标点边界与 CJK 原子边界;raw bracketed paste 路径已保留 Paste 事件,大粘贴会插入 `[paste #N +M lines]` / `[paste #N M chars]` 摘要并在提交/取消保存时展开真实内容;有效 paste marker 已在左右移动、Backspace/Delete、Ctrl+word 移动/删除中作为原子段处理;`GetExpandedDraft()` 已可在提交前为扩展/宿主读取展开后的真实草稿。剩余缺口是视觉换行地图和完整 `Intl.Segmenter` 词典级 word segmentation。
- `utils.ts` 高级文本 — 已补 `WrapTextWithAnsi`、`SliceByColumn`/`SliceWithWidth`、`ExtractSegments`、`NormalizeTerminalOutput` 的 ANSI 状态继承、软换行样式恢复、underline/OSC8 行尾关闭、宽字符 strict 边界、Thai/Lao AM vowel 终端输出规范化,并补齐 `visual-truncate.ts` 等价的宽度感知尾部视觉行截断(`TuiText.TruncateToVisualLines`)。剩余缺口是完整 `Intl.Segmenter` 词典级 word 分词 parity 与编辑器 paste-marker-aware segmentation 更深接入。
- `tui.ts` overlay/focus 引擎 — `TuiStatusBar`/`TuiTranscriptViewport` 已可为 CodingAgent footer 预留多行状态区;仍缺富 overlay 定位模型(百分比尺寸)、`CURSOR_MARKER` 硬件光标、`Focusable` 模型。
- `native-modifiers.ts` — macOS 原生插件,合理跳过。

---

## 五、Tau.CodingAgent(~81%)

core 层移植扎实(常合并进大的 `CodingAgent*` 类);图像处理链已基本收口,剩余长尾集中在交互 UI 专用渲染、footer/status 细节、语法高亮主题与少量运维兼容矩阵。

### 已移植良好

- **工具**(`core/tools/`):bash/edit/edit-diff/find/grep/ls/read/write + truncate/output-accumulator → `Tools/*.cs` + `TuiDiffRenderer` + `ToolOutputTruncator`,全部 PORTED。
- **扩展系统**(`core/extensions/`):loader/runner/types/wrapper → `CodingAgent{ExtensionCommandStore,JavaScriptExtensionRuntime,ExtensionToolAdapter,...}.cs`,PORTED。
- **会话/compaction/RPC/print/settings/theme/slash-commands/skills/model 解析**:基本 PORTED。

### 缺口(按主题)

**交互 UI 组件(`modes/interactive/components/`,38 个里缺口最多):**
- 彩蛋 armin / daxnuts / earendil-announcement — 已补文本/终端基线:隐藏命令 `/arminsayshi`、`/dementedelves` 可输出对应 custom display message,opencode Kimi K2.5 模型选择可触发 daxnuts 提示。剩余是动画、图片和动态 TUI 组件保真。
- 专用消息渲染器:`skill-invocation-message` 已补基础折叠展示与 user message 分离接线;`compaction-summary-message` / `branch-summary-message` 已补折叠展示语义、命令结果接线和 TUI/transcript 专用前缀;`assistant-message` / `user-message` 已补直接终端输出的 OSC 133 prompt zone 基线;`custom-message` 已补默认 formatter 基线(标签 + 文本/图片占位)、extension `MessageRenderer` registry/文本调用基线、extension command/shortcut 中 `pi.sendMessage({ customType, content, display, details })` 到 Host `custom>` transcript 的即时展示链路、非 streaming/no-trigger custom message 的 runner + flat/tree session 持久化、`triggerTurn`/`deliverAs` typed delivery options、LLM context 转换、append-only lifecycle event、queued steering/followUp lifecycle event 和 `message_end` replacement 语义;`bash-execution` 已补独立 `TuiBashExecution` 文本渲染基线(命令头、ANSI/CRLF 清理、宽度感知 collapsed preview、expanded 输出、running/cancelled/error/truncation 状态);`tool-execution` 已补无扩展 renderer 场景的 `TuiToolExecution` fallback 渲染基线(工具名、JSON args、文本输出、尾部 preview / expanded 切换、图片 fallback、pending/success/error 背景状态)、PNG image block 真实终端渲染基线和 Kitty 非 PNG 转 PNG 渲染接线;`CodingAgentHost` 已把 tool start/update/end 事件接到 direct terminal transcript 的 bash/tool 组件,并用 toolCallId keyed transcript 更新避免同一工具调用在 composition transcript 中重复追加;`app.tools.expand` / `Ctrl+O` 已补 active bash/tool execution 快捷展开/折叠。剩余是 tool renderer 自定义组件接线、assistant/user 的完整 Markdown/背景盒/composition 组件保真、RPC stream 与真实终端原地重绘接线(PARTIAL)。
- 小部件:`show-images-selector` 已补独立 Yes/No selector 并接入 settings show-images 子菜单;`countdown-timer` 已补通用倒计时 helper 并接入自动重试状态;extension `ctx.ui.editor()` 的 RPC editor 请求/响应链路已补,但完整 TUI `ExtensionEditorComponent` 视觉组件仍未保真;trust-selector / first-time-setup / login-dialog / config-selector 仅部分。

**图像处理链:**
- `photon.ts`(WASM 后端)缺失,当前由 managed ImageSharp + 手写 PNG encoder 替代:已支持 PNG/JPEG/WebP 解码、EXIF 自动朝向、Lanczos3 缩放、PNG/JPEG 候选编码、多档 JPEG quality fallback、按 alpha/grayscale 自动选择 grayscale/RGB/RGBA PNG 编码、终端 PNG 转换(`image-convert.ts`)、剪贴板图片读取(`clipboard-image.ts`)、Ctrl+V 图片粘贴和 `image-resize-worker` 等价线程卸载。

**语法高亮:**
- `syntax-highlight.ts` — PARTIAL。已新增 `Tau.Tui.Rendering.TuiSyntaxHighlighter`,并把 `TuiMarkdownTheme` 默认接到该实现;覆盖 diff/json/C#/JS/TS/Python/PowerShell/shell/XML/HTML 的确定性 ANSI 高亮。已新增 `TuiSyntaxHighlightTheme` scope formatter、ANSI truecolor/256 色解析、Markdown fenced code `SyntaxHighlightTheme` 接线和 `CodingAgentTheme.ToSyntaxHighlightTheme()` 的 `syntax*` 色表映射;diff/meta/default token 对缺失的 `syntax*` 专用键会回退到现有 `toolDiff*`/`dim`/`text` 颜色。剩余差距:未引入 highlight.js 全语言库。

**其他 utils / 运维:**
- `core/footer-data-provider.ts` / `components/footer.ts` — PARTIAL。已补 `CodingAgentFooterDataProvider`: 普通 repo/worktree `.git` 解析、HEAD branch/detached、cwd 切换、branch-change callback、extension status map、available provider count、HEAD 目录 watcher 基线、atomic HEAD replace 监听、reftable path/tables.list watcher 接线和 WSL `/mnt/<drive>` polling 判定;并已接入 `CodingAgentHost`/`CodingAgentRpcExtensionUiBridge`,JS extension `ctx.ui.setStatus()` 可写入同一 footer data provider。当前 TUI composition status bar 已支持多行 footer:默认状态按上游 `FooterComponent.render()` 拆为 cwd(home→`~`)/branch/session 位置行、usage in/out/cache/cost/context stats + provider/model/thinking 行、可选 extension status 行;临时状态(`Goodbye!`/error/cancelled 等)继续保持单行覆盖并带 branch/extension 状态。extension `ctx.ui.setFooter()` 已支持 function/component/string/array 形式的文本 footer factory,并按上游替换默认 footer;`setFooter(undefined/null)` 清除后回退默认 footer。extension `ctx.ui.setWorkingMessage()` / `setWorkingIndicator()` 已接入 Host running 状态,支持自定义运行中文案、静态首帧 indicator、隐藏 indicator 和清除回退。extension `ctx.ui.setHeader()` 已补文本 header factory 基线,并通过 `session_start` + `ctx.mode === "tui"` 在 Host 启动欢迎区替换内置 header;`setHeader(undefined/null)` 清除后回退默认 header。extension `ctx.ui.setHiddenThinkingLabel()` 已补文本 label 基线,Host 的 thinking 输出前缀会使用扩展 label,清除后回退默认 `thinking`;assistant-message 历史/tree preview 已支持隐藏 thinking block label,thinking-only assistant 不再显示空预览;settings `hideThinkingBlock` 已补持久化、settings selector、命令路由回调和流式 thinking 静态 label 折叠,隐藏时不向终端 transcript 输出内部 reasoning delta;`app.thinking.toggle` / `Ctrl+T` 已补主输入交互 action,可即时翻转并保存 hidden thinking 设置。剩余:动态 footer/header/working/thinking 组件的 width-aware 重渲染与动画保真、真实 WSL/reftable 运行时 smoke。
- `core/extensions` JS runtime — PORTED/PARTIAL。Node extension runtime 已从 `node -e <内嵌脚本>` 改为临时 `.cjs` 脚本启动,避免 Windows 命令行长度上限在扩展 API 增长后导致 `文件名或扩展名太长`;并补运行中 UI request/response 行协议,让 `ctx.ui.editor()` 可通过现有 RPC extension UI bridge 等待宿主返回编辑结果;已支持 `registerMessageRenderer` 注册表、load status 暴露、`renderMessage` 文本调用基线,以及 extension command/shortcut 里 `pi.sendMessage({ customType, content, display, details })` 的即时 Host display 接线、非 streaming/no-trigger session 持久化、append-only/queued `message_start`/`message_end` lifecycle、`message_end` replacement semantics 和 `sendMessage(message, { triggerTurn, deliverAs })` typed delivery options。剩余:长期可维护的独立 runtime asset/打包策略。
- `utils/git.ts` — PORTED。已新增 `CodingAgentGitUrlParser`: 独立 `GitSource` 解析形态、`git:` historical shorthand、显式协议 URL、scp-like、GitHub/GitLab/Bitbucket/Gist/Sourcehut hosted shortcut、GitHub `user/repo` shorthand、GitHub tree ref、GitLab nested group、`www.` domain 归一化、`@`/`#` ref、pinned 语义、安装路径 unsafe/percent-decode 校验;`CodingAgentPackageManager` 已复用该 helper,并补齐 git URL identity 去重与漏写 `git:` 的 update 建议。兼容矩阵参考 `hosted-git-info@9.0.3`;Tau 保留比上游更保守的 dotted-owner GitHub shorthand 拒绝规则,避免把 `github.com/example` 误判成 `github.com` 这个 owner 下的仓库。
- `version-check.ts` — PORTED。已新增 `CodingAgentVersionCheck`:SemVer/prerelease 比较、`pi.dev/api/latest-version` 获取、`PI_SKIP_VERSION_CHECK`/`PI_OFFLINE` 跳过、package/note 解析、失败静默回退;并接入 `CodingAgentHost` 启动异步新版提醒。
- `core/diagnostics.ts` — PORTED。已新增 `CodingAgentResourceDiagnostic` / `CodingAgentResourceCollision` / 资源类型常量,并把扩展、包、主题诊断暴露为统一 `ResourceDiagnostics`;主题重名冲突已输出结构化 collision(winner/loser path/source)。
- `fs-watch.ts` — PORTED。已新增 `CodingAgentFsWatch`:统一 `WatchWithErrorHandler`、`CloseWatcher` 和 5000ms retry delay 常量;现有 footer git HEAD watcher 已复用该 helper,保持错误回调与 debounce 刷新语义。
- `windows-self-update.ts` — PORTED。已新增 `CodingAgentWindowsSelfUpdate`:定位 `node_modules/.pi-native-quarantine`、清理旧 quarantine、枚举/注入 loaded native 文件、移动到 quarantine 后复制回原位;Windows 下 `tau update` 的 npm 包更新路径已在重新安装前执行 cleanup/quarantine。
- `telemetry.ts` — PORTED。已新增 `CodingAgentTelemetry`:对齐 `PI_TELEMETRY` 的 `1`/`true`/`yes` truthy 解析、环境变量覆盖设置、未设置时回退 `EnableInstallTelemetry`;启动通知服务保留 `PI_OFFLINE` 禁用网络副作用的现有保护。
- `provider-attribution.ts` — PORTED。已新增 `CodingAgentProviderAttribution`:对齐 OpenRouter/NVIDIA NIM/Cloudflare/Vercel AI Gateway 的 provider/baseUrl host 判定与归因 header(`HTTP-Referer`/`X-OpenRouter-*`、`X-BILLING-INVOKE-ORIGIN`、`User-Agent: pi-coding-agent`、`http-referer`/`x-title`)、opencode `x-opencode-session`/`x-opencode-client` session header、install-telemetry gating 与上游一致的 header 合并顺序(归因/session 作基底,显式 headers 覆盖);`RuntimeCodingAgentRunner` 在每轮 `RunAsync` 按当前 model 重算并注入 stream options(model 切换会重新归因),`Program.cs`/`CodingAgentSdk` 调用点用 `IsInstallTelemetryEnabled` + `PI_TELEMETRY` gate。归因品牌字符串保留 `pi`(向第三方网关标识来源产品,与既有 share/version-check 约定一致)。session-affinity header(`x-session-affinity`)属于 Ai 层 `model.Compat` 既有能力,不在此切片范围。
- `provider-display-names.ts` — PORTED。已新增 `CodingAgentProviderDisplayNames`:镜像上游 `BUILT_IN_PROVIDER_DISPLAY_NAMES` 全表(anthropic/openrouter/nvidia/cloudflare-*/google*/zai*/xiaomi* 等),提供 `TryGetBuiltIn`/`Resolve`(回退 provider id)/`IsBuiltInProvider`;`CodingAgentAuthSelector` 列表 Label 改用 display name(Value 仍保留原始 provider id 用于选择回传)。剩余:`getProviderDisplayName` 完整 fallback 链中 registered provider/oauth provider 自定义 name 优先级依赖 model-registry 注册 name 概念,Tau 的 `ProviderRegistry` 按 API name 索引、暂无 per-provider display name,故当前只接 built-in 表 → identity 尾部链。
- `migrations.ts` — PORTED。已新增 `CodingAgentMigrations`:启动时静默执行 legacy `oauth.json`/`settings.json apiKeys` → `auth.json`、根目录 JSONL session → `cwd/.tau/coding-agent-sessions`、`commands/` → `prompts/`、managed `tools/{fd,rg}` → `bin/`、legacy keybinding 名称迁移,并收集 hooks/tools deprecated extension warnings;`Program.cs` 已在加载包/扩展/会话前运行迁移并输出 warnings。
- `sdk.ts` — PORTED。已新增 `CodingAgentSdk.CreateSessionAsync`:支持外部代码以 `cwd`/`agentDir`/settings/session/model/thinking/tools/custom tools 创建可运行 `CodingAgentSdkSession`,加载 package/extension/prompt/skill/context resources,恢复 tree session,接入 extension tools/interceptors/lifecycle,并提供 `RunAsync`/`Save` 编程式入口。
- `bun/restore-sandbox-env.ts`(Bun 专用)— N/A。

---

## 六、迁移计划

按「影响面 × 用户可见度」排序。每项标注预估工作量(S/M/L)与所属模块。

### 优先级 P0(parity 关键,功能性破损)

1. **[Ai · M] Cloudflare auth + base-URL 模板 — DONE**
   已补 `cloudflare-auth.ts` + `cloudflare.ts` 对应能力:账户/网关 ID 解析、`{CLOUDFLARE_ACCOUNT_ID}`/`{CLOUDFLARE_GATEWAY_ID}` 替换、AI Gateway 专用认证头清理与 `auth.json` api_key `env` 支持。
   - 新增 `Providers/Cloudflare/CloudflareAuthResolver.cs`,接入 `StreamFunctions` request dispatch 和 `OpenAiCompatibleProvider` header 清理。

2. **[Tui · L] `keys.ts` 按键解码器 — PARTIAL**
   已新增 `Runtime/TuiKeyDecoder.cs`,覆盖 VT/kitty/CSI-u/modifyOtherKeys 解析、`parseKey`/`matchesKey`/printable decode/key release/repeat,并把 `TuiProcessTerminal` 的 Kitty protocol 状态同步给 decoder。
   - `TuiProcessTerminal` 已补齐上游 `\x1b[>7u\x1b[?u\x1b[c` 启动握手语义:先 push Kitty flags,再 query flags,最后用 DA sentinel 快速进入 modifyOtherKeys fallback;停止/Drain 时会恢复 keyboard protocol 状态。
   - 已新增 `Runtime/TuiDecodedKeyReader.cs`/`TuiConsoleKeyInfoMapper`/`TuiStreamRawInputReader`/`TuiConsoleRawMode.cs`,raw CSI-u/modifyOtherKeys 序列可通过 `IConsoleKeyReader` 驱动 `InteractiveInputEditor`;`SystemConsoleKeyReader` 在交互式终端默认启用 raw stdin 路径,管道/重定向场景回退,并支持 `TAU_TUI_RAW_INPUT=1/0` 强制开关与 raw mode 进入/恢复。
   - 剩余:真实终端交互 smoke,逐步替代 `.NET ConsoleKeyInfo` 直读。

### 优先级 P1(高性价比 / 明显观感差距)

3. **[CodingAgent · S] 语法高亮接线 — PARTIAL**
   已新增 `Tau.Tui.Rendering.TuiSyntaxHighlighter`,并把 `TuiMarkdownTheme` 默认接入;常见代码块和 diff 已恢复 ANSI 高亮,`syntax*` theme 色表已可驱动 fenced code token 颜色。
   - 剩余:按 pi 的 highlight.js 方案扩展全语言支持。

4. **[CodingAgent · L] 逐类型消息渲染组件**
   assistant-message / user-message / bash-execution / tool-execution / compaction-summary / branch-summary 的专用渲染,替换当前通用 `TuiMarkdown`/`TuiMessageArea` 的低保真覆盖。`visual-truncate.ts` 的宽度感知尾部预览截断基础设施已移入 `TuiText.TruncateToVisualLines`;`bash-execution.ts` 的文本渲染基线已落到 `TuiBashExecution`;`tool-execution.ts` 的无扩展 renderer fallback 渲染基线、尾部 preview / expanded 切换、PNG image block 真实终端渲染基线与 Kitty 非 PNG 转 PNG 渲染接线已落到 `TuiToolExecution`/`CodingAgentHost`;`custom-message` 的 extension `MessageRenderer` registry/文本调用基线、`pi.sendMessage` command/shortcut 即时 Host display、非 streaming/no-trigger session 持久化、append-only/queued `message_start`/`message_end` lifecycle、`message_end` replacement semantics、`triggerTurn` 与 `deliverAs:"steer"|"followUp"|"nextTurn"` 投递选项已补;Host direct terminal transcript 已开始用组件渲染 tool start/update/end,composition transcript 已按 toolCallId 做 keyed update,并补 `Ctrl+O` / `app.tools.expand` active tool output 展开/折叠。剩余是动态边框/loader/theme/key hint 保真、内置/扩展 tool renderer 接线、RPC stream 接线以及真实终端原地重绘。

5. **[Ai · S] `node-http-proxy` 代理支持 — DONE**
   已新增 `Utilities/NodeHttpProxyResolver.cs` + `TauHttpClientFactory`;默认 provider/OAuth HTTP 路径尊重 `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` 与 `NO_PROXY`,并支持 proxy userinfo credentials。

6. **[Tui · M] `terminal-colors.ts` 明暗检测 — DONE**
   已新增 `Runtime/TuiTerminalColors.cs`,并把 OSC 11 背景色查询、迟到响应消费、`?997` 明暗报告查询/通知接入 `TuiProcessTerminal`。

### 优先级 P2(长尾 / 按需)

7. **[CodingAgent · M] 图像处理链增强 — DONE** — PNG/JPEG/WebP 解码、EXIF 自动朝向、Lanczos3 缩放、PNG/JPEG candidate、多档 JPEG quality fallback、PNG grayscale/RGB/RGBA 内容感知编码、`image-convert.ts` 终端 PNG 转换、`clipboard-image.ts` 剪贴板图片读取、Ctrl+V 图片粘贴和 `image-resize-worker` 等价线程卸载已补。
8. **[CodingAgent · S] footer data provider / git helpers** — `core/footer-data-provider.ts` git branch/footer 数据基线、extension status 接线、composition status bar 渲染、HEAD 目录 watcher、atomic HEAD replace、reftable watcher path 接线和 WSL `/mnt/<drive>` polling 判定已补;`components/footer.ts` 的 usage in/out/cache、cost、context percent/window stats line 基线、cwd(home→`~`)/branch/session name 位置行和可选 extension status 行已接入多行 status bar;extension `ctx.ui.setFooter()` 文本 footer factory 已补并支持清除回退;extension `ctx.ui.setWorkingMessage()` / `setWorkingIndicator()` 已接入 running 状态;extension `ctx.ui.setHeader()` 文本 header factory 已补并接入 `session_start` 启动替换;extension `ctx.ui.setHiddenThinkingLabel()` 文本 label 基线与 assistant-message 历史/tree preview 已补;settings `hideThinkingBlock` 持久化、settings selector、`Ctrl+T` / `app.thinking.toggle` 快捷切换和流式 thinking 静态 label 折叠已补;`utils/git.ts` 独立 URL helper、identity 去重、漏写 `git:` 建议和 hosted provider 兼容矩阵已补。继续补动态 footer/header/working/thinking width-aware 重渲染与动画保真、真实 WSL/reftable smoke。
9. **[CodingAgent · S] 运维/诊断基础设施** — `version-check.ts` 已补齐并接入交互启动新版提醒;`core/diagnostics.ts` 资源诊断模型已补齐并接入扩展/包/主题状态;`fs-watch.ts` 已补齐并接入 footer git watcher;`windows-self-update.ts` 已补齐并接入 Windows npm package update 路径;`telemetry.ts` 设置开关 helper 已补齐并接入 startup notice telemetry 判断;`migrations.ts` 启动迁移器已补齐并覆盖 auth/session/resource/keybinding/deprecation 迁移。
10. **[Tui · M] `utils.ts`/`editor.ts` 文本边界增强** — `utils.ts` 已补 ANSI 保真 wrap/slice/extractSegments 与 Thai/Lao normalize;editor 普通字符删除/左右移动已按 text element 边界处理,Ctrl+word 移动/删除已补 ASCII 标点与 CJK 原子边界,bracketed paste 大粘贴摘要/提交展开、paste-marker 原子移动/删除和提交前展开草稿读取已补。后续继续补视觉换行地图与完整 `Intl.Segmenter` 词典级分词。
11. **[CodingAgent · S] 缺失小部件** — `show-images-selector` 已补独立选择器与 settings 接线;`countdown-timer` 已补通用倒计时 helper并接入自动重试状态;`skill-invocation-message` 已补基础折叠展示和 user message 分离;`compaction-summary-message` / `branch-summary-message` 已补折叠展示语义、命令结果接线和 TUI/transcript 专用前缀;`assistant-message` / `user-message` 已补直接终端输出的 OSC 133 prompt zone 基线;`custom-message` 已补默认 formatter 基线、extension `MessageRenderer` registry/文本调用基线、`pi.sendMessage` command/shortcut 即时 Host display、非 streaming/no-trigger session 持久化、append-only/queued message lifecycle event、`message_end` replacement semantics、typed runner delivery options(`triggerTurn`/`deliverAs`)和 LLM context 转换;`bash-execution` 已补文本渲染组件基线;`tool-execution` 已补 fallback 渲染组件基线、尾部 preview / expanded 切换;bash/tool 已接入 Host direct terminal transcript,composition transcript 对同一 toolCallId 做 keyed update,`app.tools.expand` active tool output 展开/折叠已补;extension `ctx.ui.editor()` 的 RPC 请求/响应链路已补;armin/daxnuts/earendil-announcement 彩蛋已补文本/终端基线。剩余完整 TUI extension-editor 视觉组件、assistant/user composition 组件保真、内置/扩展 tool renderer 接线、RPC stream 接线、彩蛋动画/图片保真和真实终端原地重绘。
12. **[AgentCore · M] `ExecutionEnv` 抽象 — DONE** — 已新增 `Harness/Env/AgentExecutionEnv.cs`,提供 core 层可注入的文件/shell 后端接口与系统实现。
13. **[Ai · S] 内置模型目录补 seed — DONE(目录数据)**
    已把 OpenAI-compatible 与混合 API 目录批量加入 `generated-models.seed.json`,并将 `GeneratedBuiltInModels.g.cs` 改为嵌入式 seed loader。后续剩余是 provider 请求兼容行为 parity,不是模型目录条目缺失。
14. **[Ai · S] `openai-prompt-cache` 64 字符截断 — DONE**
    已新增 `OpenAiResponsesShared.ClampOpenAiPromptCacheKey`,并接入 OpenAI Responses / Azure Responses / Codex Responses 请求体。
15. **[Ai · S] `transform-messages` 非视觉图片降级共享 — DONE**
    已新增 `Utilities/MessageTransformer.cs`,把非视觉模型中的 user/tool image 转为上游一致的占位文本,并接入 OpenAI / OpenAI Responses / Anthropic / Google / Bedrock / Mistral 请求转换入口。

### 明确不移植(平台差异,N/A)

- `native-modifiers.ts`(macOS 原生 `.node` 插件)
- `bun/*`、`compat.ts`、`legacy-api-aliases.ts`、`api/*.lazy.ts`、`node.ts`(JS/Bun 专用或重导出)
- `Result<T,E>` 模式(已用 C# 异常替代)

---

## 七、维护约定

- 每完成一项,把对应行状态改为 PORTED 并在计划表勾除。
- 新增缺口随时追加到对应模块表。
- 迁移度百分比在每个模块大块收口后重新评估。
