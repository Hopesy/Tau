# Tau next

这份文件只记录 **当前还没做完、后续需要继续推进的缺口**，方便快速检索。

当前主线：`docs/exec-plans/active/2026-05-28-tau-100-percent-pi-mono-parity.md`。

当前目标：所有能力 100% pi-mono parity。Tau 必须对 `C:\Users\zhouh\Desktop\pi-mono-main` 的用户可见功能、协议、命令、配置、env、日志、持久化 schema、release/CI 和真实 e2e 建立 Tau 对应实现、验证证据或用户确认的非目标说明。

这份文件和 `GOAL.md` 一起构成持续运行的 next 队列；每次收到 `/goal` 的继续指令，都从这里和当前 active plan 继续下一刀，直到 Final audit 关闭。

最近完成前置能力：`docs/exec-plans/completed/2026-06-07-tau-agent-platform-baseline.md`。

Tau Agent 应用底座的首版已完成本地验收：API、tool/session/observability、provider run + tool execution runtime log、examples、全仓 gate、`-RunSmoke`、package/release 消费边界说明、质量/架构/history 收口和 completion audit 均已有当前证据。该能力现在作为 100% parity 的 shared Agent foundation，不再作为当前主线目标，也不代表完整 product parity 完成。

当前 Phase 1 parity matrix：`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md`。

当前 Phase 1 parity matrix 状态：grouped inventory freeze 已完成。matrix 已合并 capability-level inventory；`packages/ai`、`packages/agent`、`packages/coding-agent`、`packages/tui`、`packages/web-ui`、`packages/mom`、`packages/pods` 的 grouped file-level mapping 已冻结；AI、CodingAgent、Tui、WebUi、Mom、Pods 的 command/API/env/config/log/schema 或对应 surface 子矩阵已冻结；root script/manifest matrix 已冻结；final normalization 已明确 capability/file/surface 三层职责、统一 status 语义，并确认当前上游 package 目录集合没有未知目录。后续从 matrix 的 `Phase 2 Candidate Queue` 分派 implementation/e2e worker 切片，不重开 broad inventory。

当前完整移植总路线图：`docs/exec-plans/active/2026-05-10-tau-complete-pi-mono-port.md`。后续所有 `pi-mono-main` parity 工作默认以 100% parity 多 Agent 收口计划为最终验收入口，旧 `2026-04-23-tau-port-baseline.md` 保留为已完成 P1 基线和决策历史。

当前直接执行入口：

1. 先执行 `GOAL.md` Pass 0 状态校准：清理 matrix、active plan、`next.md`、`docs/QUALITY_SCORE.md` 中与最新 HEAD 证据冲突的 stale 描述，尤其不能把 fake-only、本地 planning baseline 或已关闭旧 WIP 误写成 final parity。
2. 再从 `docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 的 `Phase 2 Candidate Queue` 选择下一条可验证切片。
3. 优先选择 critical contract 或真实 e2e blocker：AI provider/OAuth、CodingAgent CLI/RPC/session/config、Tui live terminal/settings runtime、WebUi branch/tree/artifact runtime、Mom Slack/Docker、Pods SSH/HF/GPU/vLLM、release/package final parity。

## 已完成前置能力：Agent 应用底座 baseline

当前状态：

- Agent platform baseline 阶段的 Phase 0 目标切换已完成：`GOAL.md`、completed plan、`next.md`、`docs/QUALITY_SCORE.md` 和 history 当时把短期主线切到 Agent Platform Baseline；当前主线已经恢复为 100% pi-mono parity，本节只记录已完成前置能力。
- `Tau.Agent` 已从已有 `AgentOptions` / `Agent` facade、`AgentRuntime.RunStream(...)`、`IAgentTool`、tool events、usage/stop reason、cancellation 和 runtime log context，继续推进到 `src/Tau.Agent/Platform/**` 的首版平台 public surface：`AgentApplication`、`AgentApplicationBuilder`、`AgentRunResult`、`DelegateAgentTool`、`AgentToolContext`、`IAgentSessionStore`、`AgentSessionSnapshot`、`InMemoryAgentSessionStore`。
- 已确认 `Tau.Ai` 具备平台测试基础：`ProviderRegistry`、model/auth/config、`ToolArgumentValidator`、`ITauLogSink` / `JsonlTauLogSink`、`TauRuntimeLogContext` 和 `Tau.Ai.Providers.Faux` scripted provider 已存在。
- `tests/Tau.Agent.Tests/AgentPlatformTests.cs` 和 public API compile sample 已覆盖 fake provider、delegate tool、session save/restore、provider run + tool execution runtime log trace、usage/stop reason、取消回合不保存 session 且回滚内存消息。
- `examples/Tau.Agent.ConsoleExample` 和 `examples/Tau.Agent.HttpExample` 已提供两个最小 Agent 应用示例；`scripts/verify-agent-platform-examples.ps1` 已能 build/smoke 两个示例，并已接入 `scripts/verify-dotnet.ps1 -RunSmoke`。
- 当时本地验收已经完成：targeted Agent platform tests 5/5、example smoke、`Tau.Agent.Tests` 123/123、`Tau.Ai.Tests` 284/284、仓库级 `verify-dotnet.ps1 -SkipRestore` 和 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 均已通过；当前全仓计数以本文件后续“当前新增验证基线”和 `docs/QUALITY_SCORE.md` 为准。
- 真实 provider/OAuth e2e、真实 NuGet/package registry 发布演练、真实 signing/provenance rehearsal 和后续 product parity 接回仍是后续边界；它们不被 fake provider/platform smoke 误标为完成。
- `/settings select` UI parity 已作为独立切片关闭，不再作为当前未提交 WIP；当前如继续 UI/TUI parity，应按 100% parity plan 与 matrix 独立分派剩余 settings submenus、runtime wiring、theme/terminal host 和真实 TTY/PTY smoke。

后续默认路线：

1. Agent platform 作为 shared foundation 被后续 parity 切片复用，不重新实现。
2. 真实 provider/OAuth e2e、真实 package registry 发布演练、真实 signing/provenance rehearsal 或 NuGet 消费者项目 smoke 继续作为 100% parity 的 release/e2e 缺口管理。
3. `/settings select` UI parity 已保持独立提交边界；后续 UI/TUI parity 不混入 Agent SDK/API 或 docs-only goal pivot。

### Platform API / SDK boundary

- [x] 冻结第一版 Tau Agent platform public surface checklist：`Tau.Agent` 创建/运行/取消/streaming event，`Tau.Ai` provider/model/auth/config 消费方式，tool registration/schema validation，session/state/log/audit，observability/correlation，examples/package 边界已落在 completed plan 和 `src/Tau.Agent/Platform/**`。
- [x] 审视 `Tau.CodingAgent` 中可复用能力，明确哪些应封装或下沉到 platform 层：当前 platform 只下沉 UI-free session/log/tool/run result 合同，不暴露 command router、TUI selector/session、TreeSelector/metadata UI、interactive settings UI。
- [x] 建立 `AgentApplication` / `AgentApplicationBuilder` / `AgentRunResult` / `DelegateAgentTool` 或等价简洁入口，让外部 .NET 应用能用最小代码创建 Agent、注册 tools、绑定 fake/real provider、订阅 event stream、保存 session/log。
- [x] 建立 `IAgentSessionStore` / `AgentSessionSnapshot` / `InMemoryAgentSessionStore` 或等价 UI-free session adapter，固定普通 conversation 保存/恢复合同。
- [x] 新增 public API compile sample / targeted tests：fake provider + tool call + cancellation + session/log + runtime event trace。

### Examples / templates

- [x] 新增 Console Agent app 示例：fake provider、一个函数工具、streaming 输出、session/log 写出、README 命令。
- [x] 新增 ASP.NET Core 或 Worker Agent app 示例：HTTP/后台入口、tool call、structured response 或 streaming、runtime log sink。
- [x] 把示例 smoke 加入本地 PowerShell 验证链或新增 dedicated platform smoke。

### Reliability / security / packaging

- [x] 固定平台 redaction 默认策略：provider/auth/tool/runtime log/session 输出不得泄漏 secret。当前 provider run runtime log 只写 provider/model/api/message/tool count/usage/cost/failureKind 等摘要，tool runtime log 只写摘要 bytes/failureKind，JSONL sink 默认 string-value redaction；真实 provider/auth e2e 和非标准 secret pattern 仍保留为后续安全 backlog。
- [x] 固定 tool failure/cancellation 行为：错误和取消不污染 session，runtime log 只写摘要和 failure kind。
- [x] 明确 NuGet/library package boundary：默认 `Tau.Ai` / `Tau.Agent` 是 library boundary，examples 是源码模板；release/package 计划、README、quality 和本地验证证据已同步，真实 registry 发布演练保留为后续外部验证。

## P0：100% pi-mono parity backlog（当前主线）

### Tau.Ai

#### Provider / API fidelity

- [x] `Amazon Bedrock` 真实实现 SigV4 / bearer token / shared credentials profile 调用，不再返回 placeholder
- [x] `openai-responses` 与 `openai-codex-responses` 提高协议保真度，补齐和上游更一致的 payload / stream 语义（SSE 路径）
- [x] `Mistral` 从 OpenAI-compatible 过渡到更接近原生 conversations 行为
- [x] `Google Vertex` 从 API key 模式扩到真正 ADC token exchange
- [x] `Google Gemini CLI / Antigravity` 从当前简化版凭证载荷扩到更完整的请求/响应细节
- [x] Codex WebSocket transport / 会话级 socket 缓存
- [x] OpenAI Responses service-tier cost / pricing multiplier
- [x] GitHub Copilot dynamic headers / vision behavior 的完整 Responses 路径
- [x] Azure dedicated `azure-openai-responses` provider，不再继续走 OpenAI-compatible 兜底
- [x] provider 请求文本 unpaired surrogate sanitize（`openai-responses` / Codex Responses / Azure Responses 共用 converter、OpenAI chat-completions / OpenAI-compatible 共用 converter，以及 Anthropic / Google / Google Vertex / Gemini CLI / Mistral / Bedrock provider，会在 system/user/assistant/thinking/tool-result 文本进入 request body 前删除孤立 UTF-16 surrogate，并保留合法 emoji surrogate pair；后续只剩真实 provider e2e 和非标准 payload 样本扩展）
- [~] Tau.Ai public API compile-sample / standalone CLI baseline（`AiPublicApiCompileSampleTests` 已用外部消费者样例固定 message/content/tool/model/options/usage、`ProviderRegistry`、`StreamFunctions`、stream event/result、`ModelCatalog`、`ProviderAuthResolver`、OAuth registry、`ToolArgumentValidator`、当前 public helper 入口和 `Tau.Ai.Providers.Faux` public scripted provider；本轮新增 `AiHeaderUtilities`、`ShortHash`、`ContextOverflowDetector`、`JsonSchemaHelpers.StringEnum` 作为 headers/hash/overflow/string-enum helper baseline，并补 Faux provider registration/streaming/usage/cache/error helper baseline；本轮继续新增 `StreamingJsonParser.ParseStreamingJson(...)`，对照上游 `partial-json@0.1.7` 固定 streaming incomplete JSON parser public helper baseline，并把 OpenAI-style / OpenAI Responses / Anthropic / Mistral / Bedrock streaming tool-call arguments 接入 helper baseline；本轮已补 `Tau.Ai.Cli` standalone `tau-ai` help/list/login baseline、交互式 provider selection、Tau 默认 auth store 写入和 `--auth-file auth.json` cwd auth 兼容入口；release artifact 已补 `pi-ai` wrapper alias 并通过 `pi-ai list` / `pi-ai --help` smoke；仍缺 package/global install alias、完整 TypeBox re-export、exact TypeScript helper/export/subpath shape 的实现或非目标决策，以及真实 provider/OAuth e2e / edge audit）
- [~] AI-level tool schema validation helper（`Tau.Ai.ToolArgumentValidator.ValidateToolCall` / `ValidateToolArguments` 已公开并被 `Tau.Agent` 复用，当前除 object/array/required/properties/items/type coercion/enum/anyOf 外，继续覆盖 const、oneOf、allOf、additionalProperties、patternProperties、minLength/maxLength、pattern、format、minimum/maximum、exclusiveMinimum/exclusiveMaximum、multipleOf、minItems/maxItems、uniqueItems、minProperties/maxProperties；仍缺完整 AJV/TypeBox keyword parity 和 CSP/runtime-codegen fallback 语义决策）

#### Model registry / generated models

- [x] 建立 generated models 管线，不再只靠手写 `BuiltInModels`
- [~] 引入更完整的 provider 列表和模型全集（已把当前已支持 API 家族扩到 66 个 generated seed，仍未覆盖全部上游 provider）
- [x] 支持 typed/default model 解析与更接近上游的 default model 策略
- [x] 把当前 Tau 可实际消费的 compatibility / capability / routing 元数据补到 `Model` / generator / OpenAI-compatible provider（OpenRouter / Vercel routing、reasoning/max-tokens/tool-stream/strict/stream-usage 兼容字段）

#### OAuth / auth

- [x] Anthropic OAuth 真实 login / refresh 流程
- [x] GitHub Copilot device flow
- [x] OpenAI Codex OAuth flow
- [x] Gemini CLI / Antigravity login flow
- [x] auth.json 的迁移、写回、刷新后持久化策略

#### 配置 / 安全

- [x] auth.json schema 明文化
- [~] secret 持久化边界和脱敏规则（已补默认 `./.tau/auth.json` / `./.tau/models.json` / JSONL session 本地状态忽略、`auth.json` Unix 0600 写入、OAuth metadata 保留字段过滤、auth status 不回显密钥、models.json credential header 状态识别、command-backed `apiKey` 状态检查不执行 `!command` 的回归、HTML transcript 导出（`/export` / `/share`）默认对常见 AWS / GitHub / Slack / Anthropic / OpenAI / Bearer / JWT secret 模式做 `[redacted]` 替换（`TAU_CODING_AGENT_REDACT_SECRETS=0` 可关闭），以及 `TauSecretRedactor` 提升到 `Tau.Ai` 并被 `Tau.WebUi GET /api/sessions/{id}/export.html` 使用（`TAU_WEBUI_REDACT_SECRETS=0` 可关闭）；已补 `JsonlTauLogSink` runtime event log 默认 JSONL string-value 脱敏 baseline（`TAU_LOG_REDACT_SECRETS=0` 可在 sink 创建时关闭，完整 JSONL 写出后复用 `JsonlSecretRedactor`，覆盖 category/event/field value 并保留 field key/number/bool/null）；已补 `JsonlSecretRedactor` 通用 JSONL string value redaction foundation，并让 CodingAgent JSONL tree session 写出默认复用该能力（`TAU_CODING_AGENT_REDACT_SECRETS=0` 可关闭），WebUi-local JSONL export/import 与 CodingAgent JSONL preview/import 默认复用该能力（`TAU_WEBUI_REDACT_SECRETS=0` 可关闭），Mom channel `log.jsonl` / `last_prompt.jsonl` 也默认复用该能力（`TAU_MOM_REDACT_SECRETS=0` 可关闭）；field key 保持不改，仍需继续梳理非标准 secret pattern 与真实云端凭证链边界）
- [x] provider-specific headers 支持（models.json 已能合并静态 provider/model headers，并在 StreamFunctions 层解析 provider/model request headers）
- [x] Bedrock AWS SSO / AssumeRole / credential_process / IMDS / ECS / web identity credential chain（已覆盖全部六个源 + AssumeRole 的 source_profile 与 credential_source 两种触发方式（Environment / EcsContainer / Ec2InstanceMetadata）；SigV4 签名器泛化到任意 service，STS XML 解析共享，token cache 路径 / portal endpoint / sts endpoint 均可在 BedrockOptions 中显式覆盖；已补 SSO cache token 自动刷新 baseline：过期 access token + cache 中 `clientId` / `clientSecret` / `refreshToken` / 未过期 `registrationExpiresAt` 足够时调用 OIDC `CreateToken` 并 best-effort 写回 cache；剩余增量：真实云端 e2e、OIDC client registration 自动重新注册、多 cache/并发刷新边界）
- [x] 自定义 provider / custom model 配置入口（`TAU_MODELS_FILE`、`./.tau/models.json`、`~/.tau/models.json`，支持 Tau 已注册 API 的 `providers/baseUrl/api/apiKey/authHeader/headers/compat/models/modelOverrides` 子集）
- [x] models.json dynamic OpenAI-compatible provider registration（未预注册 provider 只在 `apiKind` / `apiType` 显式标记为 `openai-compatible`、`openai-completions` 或 `openai-chat-completions` 时运行时注册；拼错或未知 api 不静默兜底）
- [x] models.json 的 `apiKey/authHeader`、shell/env value resolution、运行时 request auth 合并

### Tau.Agent

- [x] high-level Agent facade baseline（`AgentOptions` / `Agent`，覆盖 model/provider registry/system prompt/tools/interceptors/options 持有，`PromptAsync`、`ContinueAsync`、`Subscribe`、`Steer`、`FollowUp`、queue clear、`HasQueuedMessages`、`Abort`、`WaitForIdleAsync`、`Reset`；prompt user message 和 tool result message lifecycle 已有测试）
- [x] agent loop/event contract（已具备双层循环、tool execution、context transform/convert、tool-result message lifecycle 和 runtime trace；这轮已补上 `agent_end.messages`、`turn_end.message/toolResults`、rich tool update partial result、schema validation、tool exception -> error tool result baseline、facade/runtime/proxy/event public API compile-sample、high-level `Agent` facade 的 stream failure / cancellation synthetic assistant message baseline、low-level `AgentRuntime.RunStream(...)` / `EventStream<AgentEvent, ChatMessage[]>` wrapper、parallel tool start/update timing、low-level assistant stream cancellation -> aborted terminal event / `RunStream.ResultAsync` completion、tool cancellation cleanup、parallel sibling cancellation result preservation 和 `TransformContext` cancellation；`StopReason.Aborted` 已接入 facade、proxy、OpenAI Responses cancelled 映射、Mom stop reason normalization 和 aborted `ErrorEvent` preservation。）

### Tau.CodingAgent

- [x] flat session 持久化（`TAU_CODING_AGENT_SESSION_FILE` 或 `./.tau/coding-agent-session.json`，启动自动 rehydrate，回合后保存）
- [x] JSONL session tree baseline（默认 `./.tau/coding-agent-session.jsonl`，`TAU_CODING_AGENT_TREE_SESSION_FILE` 或 `.jsonl` 形式的 `TAU_CODING_AGENT_SESSION_FILE` 可覆盖；已支持 header、append-only message/model/session-info/label/branch_summary entries、entry id/parentId/timestamp、runner diff 同步、current branch restore；写出时默认按 `TAU_CODING_AGENT_REDACT_SECRETS` 递归脱敏 JSON string value，保留 object key/number/bool/null）
- [x] session usage/cost stats display baseline（`CodingAgentSessionStats` 暴露 `tokens.input/output/cacheRead/cacheWrite/total`、`cost` 和 `costRecords`；CLI `/session` 显示 persisted usage/cost；RPC `get_session_stats` 返回同一 JSON stats；JSONL tree session 模式从当前 branch 持久化 assistant entries 汇总 usage/cost，覆盖 compaction 后 runtime snapshot 不再携带旧 assistant message 的场景。真实 provider/e2e cost samples、older-session backfill、exact upstream session root/path semantics 和 richer upstream footer/TUI rendering 仍保持 open）
- [x] auth.json migration helper（`scripts/migrate-coding-agent-auth.ps1` 对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateAuthToAuthJson()`，把 legacy `oauth.json` provider credentials 和 `settings.json.apiKeys` 迁移到 `auth.json`；默认 dry-run，显式 `-Apply` 才写 `auth.json`、把 `oauth.json` rename 为 `oauth.json.migrated` 并从 `settings.json` 删除 `apiKeys`；`auth.json` 已存在时完全跳过 legacy 文件；OAuth 与 api key 同 provider 冲突时 OAuth 优先；JSON 输出只报告 provider 与 credential kind，不回显 secret；`verify-coding-agent-auth-migration.ps1` 用 fixture 固定 dry-run/apply/idempotent、existing auth skip、invalid oauth/settings skip、OAuth winner、secret redaction 和 remaining gap audit，当前 25 assertions 通过。本项只关闭 legacy `oauth.json` + `settings.json.apiKeys` -> `auth.json` helper baseline，不迁移 Tau runtime `coding-agent-settings.json`，也不关闭 full settings/runtime auth parity、credential refresh UX 或真实 OAuth e2e）
- [x] root JSONL session relocation helper（`scripts/migrate-coding-agent-sessions.ps1` 对照上游 `packages/coding-agent/scripts/migrate-sessions.sh`，扫描 agent 根目录直接子 `.jsonl`，只迁移第一行 `type=session` 且带 `cwd` 的 misplaced session；cwd 编码沿用上游 slash/backslash/colon 替换规则，目标落在 Tau 当前 `/clone` / `/resume` 可发现的 `coding-agent-sessions/<encoded-cwd>/`；默认 dry-run，显式 `-Apply` 才移动；`verify-coding-agent-session-migration.ps1` 用 fixture 固定 dry-run/apply/idempotent、target conflict、坏 header、nested 不扫描和 Windows cwd 编码。本项不关闭 settings/session/keybindings/tools-to-bin migrations、actual hooks/custom tools migration to extensions、full settings/runtime auth parity 或 exact session schema parity）
- [x] commands-to-prompts migration helper（`scripts/migrate-coding-agent-commands.ps1` 对照上游 `packages/coding-agent/src/migrations.ts` 的 `commands/` -> `prompts/` migration，默认检查 `~/.tau` 与 `./.tau`，也支持显式 `-BaseDirectory`；当 `commands/` 存在且 `prompts/` 不存在时 rename，默认 dry-run，显式 `-Apply` 才执行；`verify-coding-agent-commands-migration.ps1` 用 fixture 固定 dry-run/apply/content preserved、target-exists、no-commands、file source、missing base 和二次 apply 幂等。本项不关闭 settings/session/keybindings/tools-to-bin migrations、actual hooks/custom tools migration to extensions、full settings/runtime auth parity 或 general custom tool migration parity）
- [x] tools-to-bin managed binary migration helper（`scripts/migrate-coding-agent-tools-to-bin.ps1` 对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateToolsToBin()`，扫描 agent `tools/` 下 `fd`、`rg`、`fd.exe`、`rg.exe`，目标 `bin/<name>` 不存在时 move，目标已存在时删除旧 `tools/<name>`，custom tools 保持不动；默认 dry-run，显式 `-Apply` 才执行；`verify-coding-agent-tools-to-bin-migration.ps1` 用 fixture 固定 dry-run/apply/duplicate removal/content preservation/custom tool preserved/directory source skip/missing tools dir 和二次 apply 幂等。本项不关闭 settings/session/keybindings migrations、actual hooks/custom tools migration to extensions、full settings/runtime auth parity 或 general custom tool migration parity）
- [x] deprecated extension dirs audit helper（`scripts/audit-coding-agent-deprecated-extension-dirs.ps1` 对照上游 `packages/coding-agent/src/migrations.ts` 的 `checkDeprecatedExtensionDirs()`，默认检查 `~/.tau` 与 `./.tau`，也支持显式 `-BaseDirectory` / `-Label`；发现 `hooks/` 时报告 renamed warning，发现 `tools/` 中除 `fd`、`rg`、`fd.exe`、`rg.exe` 和隐藏 entry 以外的 custom entries 时报告 custom tools warning；只审计不改文件；`verify-coding-agent-deprecated-extension-dirs-audit.ps1` 用 fixture 固定 hooks/custom warning、managed/hidden ignore、file tools path skip、missing base 和 no-mutation boundary。本项不关闭 actual hooks/custom tools migration to extensions、settings/session/keybindings migrations、full settings/runtime auth parity、general custom tool migration parity 或完整 jiti/custom tool extension runtime）
- [x] keybindings id migration helper（`scripts/migrate-coding-agent-keybindings.ps1` 对照上游 `packages/coding-agent/src/migrations.ts` 的 `migrateKeybindingsConfigFile()` 与 `core/keybindings.ts` 的 legacy key name map，把 upstream object-style `keybindings.json` 旧键名迁移为 canonical `tui.*` / `app.*` keybinding ids；默认检查 `~/.tau/keybindings.json`，也支持显式 `-AgentDirectory` / `-KeybindingsPath`；默认 dry-run，显式 `-Apply` 才写回；canonical 键已存在时保留 canonical 值，未知 custom 键保留；`verify-coding-agent-keybindings-migration.ps1` 用 fixture 固定 dry-run/apply、冲突 canonical winner、custom key preserved、Tau array-style `coding-agent-keybindings.json` no-op、invalid/missing skip 和二次 apply 幂等。本项不关闭 full settings/runtime auth parity、package/extension shortcut migration、actual hooks/custom tools migration to extensions 或完整 keybinding runtime parity）
- [~] session lifecycle（已补 `/new`、`/session` tree stats、`/metadata [entry-id]` session/entry inspector、composition `/metadata` structured inspector baseline（固定 session summary + latest metadata list + entry detail + relation drill-down/backstack + Tab/Shift+Tab / h/l entry browse、detail summary block、relation target summary、relation jump/list selection 同步），以及 composition `/tree --interactive` 下 `i` 直接打开当前选中 entry 的 structured metadata inspector、退出后返回 tree 选择上下文；如果在 metadata 中跳到另一个 tree 可见 entry，关闭后 tree 选中态会跟着同步，目标仅因 fold 隐藏时会先展开再选中。interactive tree 现在也已经从“选择器”推进到真正的 in-place navigation baseline：选择当前 leaf 会 no-op 并返回 `Already at this point`；非-user entry 选择后直接切 leaf 并恢复 session；user entry 选择后回到 parent/root 并把原 user 文本预填回 draft；当离开当前 branch 且存在可摘要内容时，还会给出 `no summary / summarize / summarize with custom prompt / cancel` 的 tree summary prompt baseline；summary choice 取消后会重新回到 tree，并尽量保持原选中 entry；如果 custom summary prompt 输入被取消，则会回到 summary 选择而不是直接退出 tree。tree navigation decision 也已补可选 `label` foundation：内部调用现在能把 label 落到 `branch_summary` entry，或在不 summarizing 时落到目标 entry；当前还没有新的用户可见 label prompt，后续主要给 extension/runtime surface 复用。同一条 summary contract 现在也已补 `replaceInstructions` foundation：interactive tree decision 会把它透传给 runner，`replaceInstructions=true` 时 custom prompt 替换默认 branch summary 模板，而不是继续走 `Additional focus` 追加；当前仍无新的用户可见 replace prompt，先作为后续 extension/runtime surface 的树导航语义基础。`Shift+L` 也已补成 tree 内 label edit baseline，编辑完成后会回到同一条 tree 路径并尽量保持原选中 entry。interactive bare `/resume` 现在在 selector 可用时会打开 session selector baseline：预选当前 session，显示 session name/首条 user 文本、model、message 数、mtime 和路径；切换前会先把当前 runner `SyncFromRunner()` 到 tree session，避免未落盘消息或 session name 被覆盖；session 发现也已覆盖当前自定义 tree file 旁边的 sibling `coding-agent-sessions/`；selector 现已支持按 session name、provider/model、cwd 和路径做 case-insensitive 搜索过滤，支持 `Ctrl+R` 进入 rename mode、`Ctrl+D` 删除非当前 session、`Ctrl+N` 只看显式命名 session、`Ctrl+P` 切换路径显示、`Tab` 在 current/all scope 间切换，以及 `Ctrl+S` 在 threaded/recent/relevance 排序间切换；threaded 模式会在无搜索时按 JSONL header `parentSession` 把当前列表中可见父子 session 渲染为树状浏览；当前 session 在 selector 内改名后，若用户取消返回，router 也会同步更新 runner `SessionName`，避免内存态和 JSONL 文件分叉；Windows 下删除会优先走 Recycle Bin，再回退直接删除；当这次 `/resume` 真正切到别的 session 且当前 branch 有可摘要内容时，router 现在还会复用同一条 tree summary prompt，把当前 branch 汇总成 root-level `branch_summary` 后再切走，并保留当前 session 的 provider/model/name 供后续 resume 恢复。`/new` 现在也会在 reset 前复用同一条 summary/cancel 决策；如果用户选择 summarize，会先把当前 branch 收成 root summary，再开启新 branch。`.jsonl` 的 `/import` 现在也会在真正替换当前 tree session 前复用同一条 summary/cancel 决策；flat JSON snapshot import 仍保持原来“直接恢复 snapshot 并重写当前 tree”的边界。当前这条 switch summary prompt 还新增了 richer context：内部 state 已携带 `reason`（tree/new/resume/import）和可选 `targetSessionPath`，host prompt 会据此区分 `tree switch`、`new session`、`resume <file>`、`import <file>`。同时，session replacement 已不再复用 `treeNavigationPrompt` 这条命名和调用链，而是单独走 `sessionSwitchPrompt` seam；现在还进一步补了 `sessionSwitchHook` seam，允许内部 hook 对 `/new`、`/resume`、`.jsonl` `/import` 和 RPC `new_session` / `switch_session` 做 cancel 或 decision override。hook state 也已经带上 current 侧 `sessionName/provider/model`，以及目标 session 的 `name/provider/model/messageCount` metadata，方便后续 runtime/extension 基于“要切去哪个 session”做更像上游的判断。RPC cancel 语义也已向上游靠拢：hook 取消时返回 success response 且 `cancelled=true`，而不是 error。现在这条决策链又进一步收口成独立的 `CodingAgentSessionSwitchCoordinator`，不再让 `CommandRouter` 和 `RpcHost` 各自维护一套 summary/hook 链。interactive tree 也已改成消费完整 filtered/search 结果，不再沿用文本 `/tree` 默认 24 条窗口；估算 token/context usage、auto-compaction threshold budget、retry policy display、JSONL `cwd/parentSession` metadata、`/name`、`/tree` 过滤/搜索模式与 label timestamp、`/label`、`/fork`、`/fork <entry-id> --summarize [instructions]`、`/clone`、`/resume`、`.jsonl` export/import；`/fork --summarize` 会写 JSONL `branch_summary`，恢复 branch 时作为 context message 注入 runtime；仍缺跨平台 trash parity 的统一抽象、真实 extension/runtime 驱动的 richer session switch hooks，以及上游 TreeSelector parity）
- [x] session switch result contract（`CodingAgentSessionSwitchCoordinator` 现在返回统一 audited outcome：`reason`、previous/target session path、hook/prompt 使用情况、decision、summary entry count/token estimate；CLI 与 RPC 统一从该 result 派生输出。RPC 主合同继续保持上游兼容的 `cancelled`，`summarizedCurrentBranch` / `branchSummary` / `alreadyCurrent` 仍是 Tau 扩展字段，不把内部审计字段扩进公共协议）
- [x] settings / model selection / provider selection / thinking level / scoped models / theme setting（`/settings`、`/theme`、`/model`、`/provider`、`/models`、`/providers`、`/thinking`、`/scoped-models`，默认写入 `TAU_CODING_AGENT_SETTINGS_FILE` 或 `./.tau/coding-agent-settings.json`；`/settings [current|path|select]` 可查看 settings 路径/摘要，并在真实交互式 editor 会话中打开 `TuiSettingsList` 主设置面；selector 当前可直接切换 auto-compaction、terminal show images / clear on shrink、image auto-resize / block images、show hardware cursor、editor padding、autocomplete max visible、steering/follow-up mode、tree filter、thinking level、quiet startup、collapse changelog、install telemetry，并可从同一列表进入 scoped models 和 theme 既有子 selector；`/thinking [current|select|cycle|off|minimal|low|medium|high|xhigh]` 可查看、用 TUI selector 选择、循环、设置或关闭 reasoning level，并写回 settings `defaultThinkingLevel`；`/theme [current|list|select|set|clear] [name]` 可查看、列出、用 TUI selector 选择、校验保存或清空当前主题；`/scoped-models [current|select|set|add|remove|clear|all] [provider/model ...]` 可查看、用 TUI multi-select 选择、命令式维护或清空模型 scope；`/model select [search]`、交互式裸 `/model` 或 Ctrl+L 会按当前 `enabledModels` scope 或全部模型打开 TUI 单选模型 selector，选择后保存默认 provider/model；空闲输入 prompt 的 Ctrl+P / Ctrl+Shift+P 会按 settings `enabledModels` scope 或全部可用模型切换下一个/上一个模型，并保存默认模型；同一 settings 文件支持上游兼容 `treeFilterMode` 作为 `/tree` 默认过滤模式，支持 retry attempts/base delay 字段供 `/retry` 和生产入口使用，支持 default thinking level、steering/follow-up queue mode 启动恢复，支持 `theme` 当前主题名，支持 terminal/image/editor/changelog/telemetry settings-list 字段写回，并支持 `enabledModels` 有序数组作为模型切换 scope；`autoCompactionEnabled` 保存 boolean 开关，旧 `queueMode` 读入时迁移为 `steeringMode`；`enabledModels` 缺失/null 表示 all enabled / no filter。package source/list/local resource baseline 已由独立 package manager 切片补上；完整 transport/shell path/npm runtime wiring、部分 persisted terminal/editor/image/changelog 字段的真实 runtime wiring 和完整上游 settings submenus 仍属后续）
- [x] settings store / RPC upstream field surface（settings snapshot/document 与 RPC `get_settings` / `update_settings` 已补上游兼容的 `shellPath`、`shellCommandPrefix`、`npmCommand`、`quietStartup`、`collapseChangelog`、`enableInstallTelemetry`、`lastChangelogVersion`、`terminal.showImages`、`terminal.clearOnShrink`、`images.autoResize`、`images.blockImages`、`markdown.codeBlockIndent`、`showHardwareCursor`、`editorPaddingX`、`autocompleteMaxVisible`；settings store 现在也能读写上游 string/object `packages` source 形状，并在后续保存 settings 时保留 package sources；`npmCommand` 保留 argv 顺序和重复项，`markdown.codeBlockIndent` 保留空格；npm/git package install execution 和 startup changelog/install telemetry runtime 已由独立切片补上，本项本身仍不关闭完整 TUI settings editor）
- [~] package manager / package resources / startup changelog telemetry baseline（对照上游 `package-manager-cli.ts`、`core/package-manager.ts`、`core/settings-manager.ts`、`core/extensions/loader.ts`、`core/extensions/runner.ts`、`core/agent-session.ts`、`core/extensions/wrapper.ts`、`core/tools/tool-definition-wrapper.ts`、`utils/git.ts` 和 interactive startup behavior，Tau 顶层 `install/remove/uninstall/update/list/config` 在 runner 初始化前处理 package command；默认写 user settings，`-l/--local` 写 project settings；`list` 按 User/Project 分组；`config` 输出 settings path 与 package resource count；npm package install/remove/update 现在通过可注入 command runner 执行全局 `npm install -g`、project `.tau/npm --prefix` 和 settings `npmCommand`，git source 支持常见 `git:` / HTTPS / SSH source 的 clone/checkout/pull/remove 到 user/project `.tau/git` install root，`PI_OFFLINE` 会跳过 update，命令失败不会写入 settings；本地 package source 会解析 `package.json` 的 `pi.extensions/skills/prompts/themes` manifest、约定 `extensions/skills/prompts/themes` 目录和 package object filters，并支持 plain include、glob include、`!` exclude、`+` exact force-include、`-` exact force-exclude 资源过滤；extension resources 现在按上游式 direct `.ts/.js` file、目录 `index.ts/js` 和 `package.json pi.extensions` entry 发现，同时保留 Tau JSON extension command file 兼容，再把 package resources 与 extension-contributed resources 合并给 extension/prompt/skill/theme stores；`/extensions` 会报告 TypeScript/JavaScript module entry 的加载状态，`.js` 与 `.ts` module 都能通过 limited local Node subprocess runtime 执行 default export factory 并注册 `pi.registerCommand(...)` slash command，`.ts` 路径使用 Node 24 type-stripping hooks，覆盖 package-loaded `node_modules` 下的 erasable TypeScript entry；同一 limited runtime 现在也能注册 `pi.registerTool(...)` 的 schema/metadata，把 extension tools 合并到 runner startup tools，支持 `prepareArguments` 在 schema validation 前准备参数，并通过 Node 子进程执行 `execute(toolCallId, params, signal, onUpdate, ctx)` 的文本型 `ToolResult` baseline；`tool_call` hook 可在 schema validation 后、实际执行前 block 或 mutation `event.input`，mutation 后参数会传给后续 hook、tool update event、实际 tool execution 与 `tool_result` hook，且不重新 schema validation；`tool_result` hook 可改写 content/details/isError；同一 limited runtime 现在也支持 `pi.registerFlag(...)` metadata/default 注册、`pi.getFlag(...)` 默认值读取和 `pi.registerShortcut(...)` metadata/status 注册，`/extensions` 可列出 flags/shortcuts，module status 会报告非零 flags/shortcuts；CLI 现在会收集未知 `--flag[=value]` token，按上游 `applyExtensionFlagValues` 对照已注册 flag 校验后注入 runtime，让 `pi.getFlag(...)` 返回 CLI 值并覆盖 `registerFlag` 默认值，未知 flag / 缺值 string flag 产出 error 诊断；interactive input 现在可解析并 dispatch resolved extension shortcuts，reserved 内置 keybinding 冲突会 warning 并跳过，非 reserved 内置绑定 warning 后允许 extension 覆盖，同一 key 的 extension 重复注册会 warning 且后者胜出，handler 可用 `ctx.sendMessage(...)` 进入 runner；同一 limited runtime 现在也支持 `pi.on("agent_start"|"agent_end"|"turn_start"|"turn_end"|"message_start"|"message_update"|"message_end"|"tool_execution_start"|"tool_execution_update"|"tool_execution_end")` 的只读生命周期广播 baseline，production runner 在产出对应 `AgentEvent` 前向 JS/TS handler 发送上游形状 payload；同一 limited runtime 现在还固定了 `@sinclair/typebox`、`@mariozechner/pi-ai`、`@mariozechner/pi-ai/oauth`、`@mariozechner/pi-agent-core`、`@mariozechner/pi-tui` 和 `@mariozechner/pi-coding-agent` 的 virtual package import stub 子集，可参与真实 JS extension command/tool 注册与执行；`/reload` 会重新解析 package resources 后再刷新 extensions 并报告 tools/flags/shortcuts（有内容时），并刷新当前进程 shortcut cache，但不热替换当前 runner tool registry；interactive startup 现在持久化 `lastChangelogVersion`，fresh install 记录版本并 best-effort 上报 telemetry，更新启动按 `collapseChangelog` 显示折叠或完整 notice，恢复已有 messages 的 session 不弹 notice，`PI_OFFLINE` / `PI_TELEMETRY` 控制 telemetry。仍缺 interactive config selector、完整 `@mariozechner/jiti` import/alias/virtualModules parity、rich tool content/renderers/details 语义、session/provider/context/input/user_bash/model/resource/before hooks、最终 `pi` package/bin identity、真实 npm/git network smoke、真实 package consumer smoke 和真实 telemetry backend e2e）
- [~] CodingAgent JS/TS extension UI fire-and-forget RPC bridge（已对照上游 RPC mode 的 fire-and-forget UI request 语义，把真实 JS/TS extension handler 内的 `notify`、`setStatus`、string-array `setWidget`、`setTitle`、`setEditorText` 和 `pasteToEditor` 接到 `CodingAgentRpcExtensionUiBridge`，输出 `extension_ui_request` JSONL；同轮 fixed limited virtual package import stub 子集，覆盖 `@sinclair/typebox` 与 `@mariozechner/pi-ai` / `pi-ai/oauth` / `pi-agent-core` / `pi-tui` / `pi-coding-agent` 在真实 JS extension command/tool 中的注册执行路径；仍缺等待型 `select/confirm/input/editor` 从真实 JS/TS handler 等待 RPC response、custom UI、interactive TUI widget factory、完整 jiti import/alias/virtualModules runtime 和 package consumer/network smoke）
- [x] steering / follow-up CLI baseline（`ICodingAgentRunner.Steer/FollowUp` 暴露 `AgentRuntime` 现有队列；`CodingAgentHost` 在 active runner turn 期间消费可注入 `ICodingAgentTurnInputSource`；交互式 console 下 Enter 提交 steering、Alt+Enter 提交 follow-up；composition input overlay 已显示默认快捷键提示行；完整动态 keybinding hints / 完整 TUI overlay 仍属后续）
- [~] CLI `@file` / image initial prompt baseline（对照上游 `cli/file-processor.ts`、`cli/initial-message.ts`、`main.ts`、`utils/mime.ts`、`utils/image-resize.ts`、`utils/exif-orientation.ts` 和 `core/tools/path-utils.ts`，Tau 现在会把 redirected stdin、`@file` 文本、PNG/JPEG/GIF/WebP 图片和第一条 CLI message 合并为 initial prompt；print mode 与 interactive startup 在含图片时走 `ContentBlock` runner 路径，后续 CLI messages 在 interactive host 中依序执行；RPC 模式拒绝 `@file`，空白 stdin 不触发 print mode；CLI `@file` 和 `read_file` 现在共用 `CodingAgentImagePreprocessor`，在 `images.autoResize=true` 时覆盖本地 PNG resize 到上游默认 2000x2000 / 4.5MiB base64 headroom、尺寸说明和 JPEG/WebP EXIF orientation 尺寸解析。该项仍不是完整 image ingestion final parity：仍缺 clipboard image paste、完整 Photon resize/convert quality parity for JPEG/GIF/WebP、terminal PNG conversion、provider vision e2e、完整 TUI attachment/editor path 和 terminal image runtime wiring）
- [x] RPC mode baseline（`--mode rpc` 走 LF-delimited JSONL stdin/stdout，`CodingAgentRpcHost` 覆盖 `prompt`、`steer`、`follow_up`、`abort`、`new_session`（含可选 `parentSession` 写入 JSONL header metadata，以及 `summarizeCurrentBranch` / `customInstructions` / `replaceInstructions` current-branch summary hook）、`get_state`、`get_settings`、`update_settings`、`set_model`、`cycle_model`、`get_available_models`、`set_thinking_level`、`cycle_thinking_level`、`set_auto_retry`、`abort_retry`、`bash`、`abort_bash`、`set_steering_mode`、`set_follow_up_mode`、`set_auto_compaction`、`switch_session`、`get_fork_messages`、`fork`（上游兼容 `text/cancelled` response，Tau 扩展保留 leaf/message/model 元数据）、`compact`、`clone`、`get_session_stats`、`get_messages`、`get_commands`、`export_html`、`get_last_assistant_text`、`set_session_name`；`prompt`、`steer`、`follow_up` 现在会读取上游同名 `images` 数组并把文本 + 图片内容块传给 runner，其中 active prompt 下的 `streamingBehavior=steer|followUp` 也走同一内容路径；`message_update` 现在输出上游 `assistantMessageEvent` 字段，不再输出 Tau 私有 `streamEvent`，并补齐 assistant stream event 的 `partial`、end content、`toolCall` 和 terminal reason baseline；RPC message/content 投影现在把 tool call 的 JSON object 参数输出为上游对象形 `arguments`，覆盖 `toolcall_end.toolCall`、`assistantMessageEvent.partial/message`、`message_update.message`、`agent_end.messages` 和 `get_messages`；`get_state` 现在返回真实 steering/follow-up `pendingMessageCount`，并在 manual `compact` 后台运行期间返回 `isCompacting=true`；`bash` 在保留最终 response contract 的同时会输出 `bash_output` stdout/stderr chunk 和终态 truncation/fullOutputPath metadata，并用 `bash_event` 标记 started/completed/cancelled/failed；`get_commands` 已按上游收口为 extension / prompt / skill 动态命令，不返回本地 slash catalog；`CodingAgentRpcExtensionUiBridge` 现在提供上游同形 extension UI request/response bridge，覆盖 select/confirm/input/editor/notify/status/widget/title/editor-text，并让 stdin `extension_ui_response` resolve pending requests 而不输出普通 command response；`CodingAgentRpcClient` 现在对齐上游 `rpc-client.ts` 的 .NET-native helper baseline，支持启动 RPC 进程、生成 `req_N`、pending response correlation、event subscription、stderr-backed timeout、`promptAndWait` / `collectEvents` / `waitForIdle`、typed bash result 和 stop-time pending rejection；完整 jiti/extension runner integration、custom tools、上游 settings selector / theme selector / terminal / packages 等全量配置面、全量 response/message schema 其它细节和 richer command set 仍属后续）
- [x] CLI `--mode json` 单次输出 baseline（对照上游 `cli/args.ts` `Mode`、`main.ts` `resolveAppMode`/`toPrintOutputMode` 和 `modes/print-mode.ts`，`CodingAgentCliArguments` 现在把 `--mode json` / `--mode=json` 解析为 `JsonMode`；`Program.cs` 让 json mode 蕴含 print mode，在写出第一条 agent event 前先输出上游形 session header JSON 行（`type/version/id/timestamp/cwd/parentSession`，无 tree session 时跳过），随后 `CodingAgentPrintMode` 在 json 模式下把每个 `AgentEvent` 复用 `CodingAgentRpcHost.SerializeEventLine(...)` 的同一上游 schema 序列化为一行 JSON；error/aborted 仍按上游语义退出码 1 但不向 stderr 复写，文本模式行为不变。`CodingAgentTreeSessionStore.GetSessionHeader()` / controller `GetSessionHeader()` 暴露上游兼容 header info。该项只关闭本地 json 单次输出 baseline；完整 upstream stdout takeover、benchmark timings、真实 provider e2e 仍属后续）
- [x] CLI `--help` / `--version` baseline（对照上游 `cli/args.ts` `printHelp(...)` 与 `main.ts` `VERSION` 输出，`--help`/`-h` 打印帮助并以 exit 0 退出，帮助体在加载 extension store 后列出 extension 注册的 CLI flags；帮助体现在还渲染上游同序的 `Examples:`、`Environment Variables:` 和 `Available Tools (default: read, bash, edit, write):` 三段，其中 Examples/Available Tools 与 Tau 实际 flag/工具行为一致，Environment Variables 只列 Tau 真正消费的变量（以 `EnvironmentApiKeyResolver` 为权威 provider-key 列表加 `PI_OFFLINE` / `PI_TELEMETRY`），上游中无 Tau 消费者的 env var 有意不列；`--version`/`-v` 打印解析出的 assembly 版本并 exit 0；命令名默认 `pi`，可由 `TAU_CODING_AGENT_COMMAND_NAME` 覆盖；`CodingAgentCliHelp` 承载帮助/版本渲染，`CodingAgentCliArguments` 解析 `Help`/`Version`，`Program.cs` 在 parse 后立即处理 version、在 extension store 构造后处理 help。剩余 top-level session flag/runtime wiring（session id / partial id 全局 lookup、cross-project fork prompt）和上游 help 中暂无 Tau 消费者的 env var/精确 Azure deployment map 说明仍属后续）
- [x] CLI session startup flags baseline（对照上游 `cli/args.ts`、`main.ts` 和 `core/session-manager.ts`，`CodingAgentCliArguments` 现在捕获 `--session <path>` / `--session=<path>`、`--continue` / `-c` 与 `--session-dir <dir>` / `--session-dir=<dir>`；`Program.cs` 通过 `CodingAgentSessionTarget.Resolve(...)` 在启动时选择 session 目标：显式 `.jsonl` 走 `CodingAgentTreeSessionController.OpenOrCreate(path)` 并优先 tree snapshot，显式非 `.jsonl` 走 `CodingAgentSessionStore(path)` flat snapshot；`--session-dir` 不带 continue 时在指定目录创建新的 per-run JSONL tree session，带 `--continue` 时选择该目录中最近有效 JSONL session，目录为空则新建；无显式 session/session-dir/continue 时保留既有 `TAU_CODING_AGENT_SESSION_FILE` / 默认 flat + tree fallback。该项不关闭 session id / partial id 全局 lookup、cross-project fork prompt 或完整 upstream `SessionManager` schema parity）
- [x] CLI `--fork <path>` path-only baseline（对照上游 `cli/args.ts`、`main.ts validateForkFlags/createSessionManager(...)` 和 `core/session-manager.ts forkFrom(...)`，`CodingAgentCliArguments` 现在捕获 `--fork <path>` / `--fork=<path>`，并拒绝缺值、空 inline 值和把下一个 option 当作 fork path；`Program.cs` 拒绝 `--fork` 与 `--session` / `--continue` / `--resume` 组合；`CodingAgentSessionTarget` 要求源 session 文件存在，并用 `CodingAgentTreeSessionStore.ExportCurrentBranch(...)` 把源 JSONL tree session 当前 branch 复制到新 JSONL 目标。目标目录优先用显式 `--session-dir`，否则使用默认 tree session 目录旁的 `coding-agent-sessions`。该项不关闭 session id / partial id 全局 lookup、cross-project fork prompt 或完整 upstream `SessionManager` schema parity）
- [x] CLI session id-prefix lookup baseline（对照上游 `main.ts resolveSessionPath(...)` 和 `core/session-manager.ts list(...)`，`CodingAgentSessionTarget` 现在把带路径分隔符或扩展名的 `--session` / `--fork` 参数继续视作文件路径；无扩展、非路径参数会按 JSONL session id 前缀解析，先查显式 `--session-dir` 中的 session，再查当前可发现的默认/clone session 列表。`--session <prefix>` 匹配后打开 JSONL tree session，`--fork <prefix>` 匹配后复制 current branch 到同一目标目录并记录 `parentSession`。该项不关闭全局 `SessionManager.listAll()` 等价 lookup、cross-project fork prompt 或完整 upstream `SessionManager` schema/selection parity）
- [x] CLI `--resume` 本地交互 selector baseline（对照上游 `main.ts createSessionManager(...)` 的 `parsed.resume` 分支和 `core/session-manager.ts list(...)`，`CodingAgentStartupResumeResolver` 现在在交互式终端中用现有 console/composition resume selector 列出显式 `--session-dir` 与当前默认/clone 可发现 JSONL sessions，选择后打开对应 tree session；print/RPC 或无 selector 场景明确报错，空列表或取消选择以 0 退出且不创建 session。该项不关闭全局 `SessionManager.listAll()`、cross-project fork prompt 或完整 upstream `SessionManager` schema/selection parity）
- [x] CLI `--models` / `--list-models` / `--verbose` baseline（对照上游 `cli/args.ts`、`main.ts` 和 `cli/list-models.ts`，`CodingAgentCliArguments` 现在捕获 `--models <patterns>` / `--models=<patterns>`、`--list-models [search]` / `--list-models=<search>` 和 `--verbose`；`Program.cs` 在打开 session 前处理 `--list-models`，输出 provider/model/context/max-out/thinking/images 表并退出；`--models` 解析逗号列表，支持 exact `provider/model`、唯一 model id、`:thinking` 后缀和 `*` / `?` 通配展开，展开后的 per-process scope 优先于 settings `enabledModels` 参与启动模型选择、interactive Ctrl+P 和 RPC `cycle_model`，但不写回 settings；`--verbose` 会覆盖 `quietStartup` 并在交互启动时显示 model scope notice；`--list-models [search]` 现在通过新公开的 `Tau.Tui.Components.TuiFuzzyMatcher`（上游 `packages/tui/src/fuzzy.ts` `fuzzyFilter` 的 .NET 端口，含 word-boundary/consecutive 评分与 alphanumeric-swap fallback）判定 search 命中，再按 provider/id 字母序排序，与上游 `cli/list-models.ts` filter-then-sort 行为一致，替换了旧的纯 subsequence 匹配。该项不关闭完整 fuzzy model scope parity（`--models` glob/scope 仍走既有 expander）、真实 provider/auth e2e、full upstream model coverage 或最终 package/bin identity）
- [x] CLI `--system-prompt` / `--append-system-prompt` baseline（对照上游 `core/resource-loader.ts` `resolvePromptInput(...)` 与 `core/system-prompt.ts` `buildSystemPrompt(...)` 的 `appendSection`，`--system-prompt` 和可重复的 `--append-system-prompt` 现在把每个值按“存在文件路径则读取内容，否则按字面文本”解析，多个 append 值按出现顺序以 `\n\n` 合并；append 文本以 `\n\n` 接到生成或自定义 system prompt 之后、context files / skills 之前，并在 `/reload` 重建 prompt 时保留（`RuntimeCodingAgentRunner` 持有 `_appendSystemPrompt`）。`CodingAgentCliArguments` 解析 `AppendSystemPrompt`，`Program.cs` 用本地 `ResolvePromptInput` / `CombineAppendSystemPrompt` 解析后传入 `RuntimeCodingAgentRunner.Create(appendSystemPrompt: ...)`）
- [x] CLI `--tools` / `--no-tools` / 未知 option 诊断 baseline（对照上游 `cli/args.ts` `parseArgs` 的 `allTools` / diagnostics 与 `main.ts` 的 tool 选择，`--tools <csv>` / `--tools=<csv>` 把上游 CLI 工具名（`read,bash,edit,write,grep,find,ls`）映射到 Tau 工具名（`read_file,shell,edit_file,write_file,grep,glob,ls`）后只启用对应内置工具；`--no-tools` 不带 `--tools` 时清空内置工具，带 `--tools` 时只保留命名工具；未指定时保持 Tau 全量默认工具集；extension tools 始终合并，不受该选择影响。未知工具名产出 `warning` 诊断，未知单横线 option（如 `-x`）产出 `error` 诊断；`CodingAgentCliArguments` 收集 `NoTools`/`Tools`/`Diagnostics`，`Program.cs` 在 parse 后打印诊断、有 error 时 exit 1，并通过 `ResolveSelectedBuiltInToolNames` 把选择传给 `RuntimeCodingAgentRunner.CreateDefaultTools(..., selectedBuiltInToolNames)`。当前未改 Tau 默认全量工具集与上游 `read,bash,edit,write` 默认子集的差异，`--tools`/`--no-tools` 仅过滤内置工具不影响 system prompt 工具列表展示）
- [x] CLI `--extension`/`-e` / `--skill` / `--prompt-template` / `--no-extensions`/`-ne` / `--no-skills`/`-ns` / `--no-prompt-templates`/`-np` resource discovery baseline（对照上游 `cli/args.ts` parseArgs 的 repeatable resource path flags / discovery toggles 与 `main.ts` 的 `resolveCliPaths(...)` + resourceLoader `additional*Paths` / `no*` 透传，`CodingAgentCliArguments` 现在把可重复 `--extension`/`-e`、`--skill`、`--prompt-template` 解析为 `ExtensionPaths` / `SkillPaths` / `PromptTemplatePaths`（支持空格与 `=` 两种形式），把 `--no-extensions`/`-ne`、`--no-skills`/`-ns`、`--no-prompt-templates`/`-np` 解析为 `NoExtensions` / `NoSkills` / `NoPromptTemplates`；这些 flag 之前被 parser swallow 但从未存储/生效。`Program.cs` 现在像既有 `--theme` 一样把显式 CLI 路径作为 `explicitPaths`、把 `no*` 作为 `includeDefaults: !no*` 传给 `CodingAgentExtensionCommandStore` / `CodingAgentSkillStore` / `CodingAgentPromptTemplateStore`，CLI 路径优先于 env-configured paths，`no*` 关闭 user/project 默认目录发现但仍保留显式 CLI 路径；path resolution 复用各 store 既有 `ResolvePath(path, cwd)` 相对路径解析。该项只关闭本地 resource discovery flag wiring baseline；完整 jiti/custom tool extension runtime、interactive resource selector 和真实 package/network smoke 仍 open）
- [x] CLI `--thinking <level>` baseline（对照上游 `cli/args.ts` `VALID_THINKING_LEVELS` 与 `main.ts` 的 `parsed.thinking` 覆盖，`--thinking <level>` / `--thinking=<level>` 校验 `off/minimal/low/medium/high/xhigh`，合法值在 `Program.cs` 中按当前模型能力 clamp 后设置 runner `ThinkingLevel`，优先级高于 settings `DefaultThinkingLevel`；无效值产出 `warning` 诊断并回落 settings 默认。`CodingAgentCliArguments` 解析 `Thinking`，不写回 settings，仅作用于当前进程 runner 初始 reasoning level）
- [x] CLI `--offline` baseline（对照上游 `main.ts` 的 `offlineMode = args.includes("--offline") || isTruthyEnvFlag(PI_OFFLINE)` 与 `process.env.PI_OFFLINE = "1"`，`--offline` 在 `Program.cs` parse 后立即把进程内 `PI_OFFLINE` 设为 `1`，使现有 `CodingAgentPackageManager`（跳过 update）和 `CodingAgentStartupNoticeService`（禁用 telemetry）等 `PI_OFFLINE` 消费者统一进入离线模式；已设非空 `PI_OFFLINE` 时不覆盖。`CodingAgentCliArguments` 解析 `Offline`，与 env flag 二者其一为真即生效）
- [x] CLI `--export <file.jsonl> [output.html]` baseline（对照上游 `core/export-html/index.ts` `exportFromFile`，`--export` 读取指定 JSONL session 文件，通过 `CodingAgentTreeSessionController.OpenOrCreate(path)` 加载 current branch snapshot / summary / JSONL，复用 `CodingAgentHtmlSessionExporter.Export` 渲染 standalone HTML 后退出，不进入 runner；输出路径缺省为 `pi-session-<input>.html`，可由首条 message 显式覆盖。逻辑收口到可测试的 `CodingAgentSessionFileExporter`，对缺失文件、非 `.jsonl` 路径和无效 session header 返回友好错误并 exit 1。`CodingAgentCliArguments` 解析 `Export`；当前只支持 `.jsonl` 输入，不做上游 flat snapshot 导出或主题选择）
- [x] CLI `--api-key` baseline（对照上游 `main.ts` 的 `authStorage.setRuntimeApiKey(model.provider, parsed.apiKey)`，`--api-key <key>` / `--api-key=<key>` 把显式 API key 注入当前 runner 的 `SimpleStreamOptions.ApiKey`，`StreamFunctions` 通过 `ProviderAuthResolver.ResolveApiKey(provider, options.ApiKey)` 让显式 key 优先于 env / auth.json / models.json 凭证；`CodingAgentCliArguments` 解析 `ApiKey`，`RuntimeCodingAgentRunner.Create(apiKey: ...)` 把它写入 config StreamOptions，`Program.cs` 把 `cli.ApiKey` 传入 Create。Tau 始终解析出 provider/model，因此不复刻上游"无 model 时报错"的分支；不写回 auth.json，仅作用于当前进程请求）
- [x] CodingAgent CLI/RPC per-run runtime log context baseline（`ICodingAgentRunner.RunAsync(...)` 增加可选 `TauRuntimeLogContext` per-run 入口；`RuntimeCodingAgentRunner` 优先使用本次 context，再回退构造期 context；CLI 普通 turn 生成稳定 turn correlation/message id，并在 retry attempts 间复用同一 context；RPC prompt 用 command id 作为 `correlationId/messageId`，并从 JSONL tree summary 注入 `sessionId`。当前不提前生成 JSONL message entry id，不改变 retry/rollback 持久化时序）
- [~] auth 管理入口（已补 `/auth [current|select|provider]` provider auth 状态查看与交互式 TUI status selector baseline、`/login [select|provider]` OAuth provider selector baseline 和 `/logout [select|provider]` OAuth provider selector / 本地 `auth.json` credential 清理；`/auth select` 只检查 provider credential 状态，不写凭证、不执行 OAuth login；交互式裸 `/login` 或 `/login select` 会筛选当前注册且有 OAuth provider 的 provider，选择后调用现有 OAuth login flow 并保存到 `auth.json`；交互式裸 `/logout` 或 `/logout select` 会筛选当前有本地 OAuth credential 且注册了 OAuth provider 的 provider，选择后只删除对应 `auth.json` entry；`/logout` 不修改环境变量或 `models.json` credential 配置；完整上游 OAuth login dialog/session parity、credential refresh UX 和真实 OAuth e2e 仍在 Tau.Ai OAuth backlog）
- [x] slash command router 抽离（`CodingAgentCommandRouter`；当前命令行为不变，为 `/compact` / login flow 等后续命令留 seam）
- [x] local quit command（`/quit` 结束当前 CLI loop，不调用 runner，不进入 LLM conversation）
- [x] local help command（`/help` 列出当前 Tau 已支持命令，已纳入 `/prompts`、`/skills` 与 `/extensions`）
- [x] local hotkeys command（`/hotkeys` 列出当前交互式 editor 注入的 `IKeyBindingMap` 绑定；默认包含 Ctrl+P / Ctrl+Shift+P model cycle 和 Ctrl+L model selector；自定义 keybindings 和 `action: "None"` 禁用默认绑定会反映到输出；无 editor 的 print/RPC/redirected 模式返回未启用错误；完整上游 app/session/tree/extension shortcut registry 仍属后续）
- [x] local reload command（`/reload` 重读 settings 并同步 retry policy / default thinking level，重读 JSON extension resources 并刷新 extension-contributed prompt/skill/theme paths，重载 prompts、skills、context files、theme status 和交互式 editor keybindings；runner 使用生成 system prompt 时会刷新 skill/context inventory；完整 theme selector、TUI theme rendering、theme file watcher、完整 jiti/custom tool extension runtime 和 full resource selector 仍属后续）
- [x] local changelog command（`/changelog [count|all]` 读取 Tau 本地 release notes 表，默认从当前目录向上查找 `docs/releases/feature-release-notes.md`，也可由 `TAU_CODING_AGENT_CHANGELOG_FILE` 覆盖；这是 Tau-native CLI baseline，启动 changelog 渲染、`collapseChangelog` 设置和 install/update telemetry 仍属后续）
- [x] local settings command（`/settings [current|path|select]` 展示 settings 文件路径/摘要，或在真实交互式 editor 会话中打开 `TuiSettingsList` 主设置面；selector 可写回 auto-compaction、terminal/image/editor/changelog/telemetry 相关字段、steering/follow-up mode、tree filter、thinking level，并保留 scoped models 和 theme 子 selector 入口；完整上游 submenus、package/transport/shell path/npm settings、部分设置字段的真实 runtime wiring 和完整 TUI edit parity 仍属后续）
- [x] local thinking command selector（`/thinking [current|select|cycle|off|minimal|low|medium|high|xhigh]` 保留查询/显式设置/循环语义，并在真实交互式 editor 会话中通过 `CodingAgentThinkingSelector` 打开上游同序 `off/minimal/low/medium/high/xhigh` 列表；选择后立即同步 runner `ThinkingLevel` 和 settings `defaultThinkingLevel`；模型能力 clamp 已接入：非 reasoning 模型只允许 off，不支持 xhigh 的 reasoning 模型把 xhigh 降到 high；完整上游 settings 子菜单 parity 仍属后续）
- [x] local theme command（`/theme [current|list|select|set|clear] [name]` 复用 theme loader/status，列出 built-in、用户、项目、env/CLI 和 extension-contributed themes，`select` 通过 `TuiSelectorSession` 交互式选择并写入 settings `theme`，`set` 校验存在后写入 settings `theme`，`clear` 回到默认 `dark`；完整上游 theme selector parity、TUI theme rendering 和 theme file watcher 仍属后续）
- [x] local model selector / scoped models / model cycle auth filtering + footer/scope/detail/search chrome + per-entry thinking + 模型能力 clamp baseline（`/scoped-models [current|select|set|add|remove|clear|all] [provider/model[:thinking] ...]` 查看或维护 settings `enabledModels`；该配置入口仍基于全部注册模型，允许提前配置尚未登录的 provider；`enabledModels` 条目可用 `:off|minimal|low|medium|high|xhigh` 为单个 scoped model 指定 thinking override；真实交互式 editor 会话中裸 `/scoped-models` 或 `/scoped-models select` 会打开 TUI multi-select selector，支持 filter、toggle、provider toggle、enable all、clear、reorder、save/cancel，并保留既有 per-entry thinking suffix 但暂不提供逐项编辑；`clear` / `all` 回到 all enabled / no filter；`/model select [search]`、交互式裸 `/model` 或 Ctrl+L 会按当前 scope 或全部已配置凭证模型打开 TUI 单选 selector，有 scoped 候选时默认 scoped，可用 Tab 在 `scoped` / `all` 候选间切换，顶部显示 `Model Selector` / `Search:` 轻量 chrome，普通字符输入会更新过滤，Backspace 会回退过滤，列表下方显示 `Model Name: ...`，并在底部提示 `Only showing models with configured auth`，选择后保存默认 provider/model 并保留 draft；空闲输入 prompt 的 Ctrl+P / Ctrl+Shift+P 和 RPC `cycle_model` 会按当前 scope 中已配置凭证的模型或全部已配置凭证模型循环切换，遇到 per-entry thinking override 时同步 runner thinking level 和默认 thinking setting，并按目标模型能力 clamp；显式 `/model`、`/provider` 和 RPC `get_available_models` / `set_model` / `cycle_model` / `update_settings.settings.model` 同样拒绝未配置凭证模型；完整上游 theme/dynamic-border/terminal-host parity 和 per-entry thinking UI editor 仍属后续）
- [x] slash command catalog（`CodingAgentCommandCatalog` 统一当前本地命令 name/usage/description，`/help` 和 usage 错误共用）
- [x] coding tool `read_file` text/image parity（`offset` 已对齐上游 read tool 的 1-based 语义，`offset=0` 宽松归一到第 1 行；兼容上游 `file_path` 参数别名；limit 截断时追加 `[N more lines in file. Use offset=M to continue.]` 续读提示；offset 越界返回 error；文本输出已对齐上游 2000 行 / 50KB `truncateHead`，截断时给出 `Showing lines ... Use offset=...` 续读提示并写入 `ToolResult.Details`；首行超过 50KB 时返回 bash fallback 提示；`ReadFileToolDetails` 现在携带 path、kind、language、line range、total lines、hasMore、image mime/size/omit/resized/dimensions metadata，并已被 HTML transcript tool result renderer 消费为 read_file metadata block；PNG/JPEG/GIF/WebP 现在按文件头识别并返回 `Read image file [mime]` + `ImageContent` base64 附件；`read_file` 现在通过 `CodingAgentImagePreprocessor` 覆盖本地 PNG resize、尺寸说明和 JPEG/WebP EXIF orientation 尺寸解析，并由启动 settings `images.autoResize` 控制默认工具行为；完整 Photon JPEG/GIF/WebP resize/convert、TUI syntax-highlight render component 和 custom tool renderer 仍属后续）
- [x] coding tool `ls` entry-format/limit/byte truncation/HTML details parity（schema 已补 `limit`，默认 500；输出按 case-insensitive 名称排序，目录只追加 `/`，包含 dotfiles，不再输出 `[DIR]` 前缀和文件大小；空目录返回 `(empty directory)`，缺失路径 / 文件路径 / limit 截断提示对齐上游；输出已接入上游同款 50KB `truncateHead`，entry limit 和 byte truncation 都会写入 `ToolResult.Details`；HTML transcript 已把 `ls` 结果渲染为 directory-aware list，显示 `limit` tool summary，并消费 `ListDirectoryToolDetails` 输出 entry limit/truncation metadata；仍未移植上游 TUI render details）
- [x] local session name command（`/name [display name | clear]` 查看、设置或清空当前 session display name，并写入 session store）
- [x] local copy command（`/copy` 复制最后一条 assistant 文本到系统剪贴板，clipboard 写入通过 `ICodingAgentClipboard` 隔离）
- [x] local export command（`/export` 默认导出 standalone HTML transcript；`/export <path>` 对 `.html/.htm` 路径导出 HTML，对 `.jsonl` 路径导出当前 branch JSONL，其他路径导出 Tau 平面 session snapshot JSON；HTML 提供 branch outline、filter/search、current-branch JSONL timeline、branch_summary timeline/read files/modified files、文本 code fence rendering、inline code span rendering、plaintext link rendering、Markdown heading/list/blockquote block rendering、Markdown strong/emphasis span rendering、Markdown pipe table rendering、Markdown task list rendering、image metadata caption、长 tool result folding、tool call JSON argument rendering、长 tool call arguments folding、read_file tool result metadata block 并内嵌可下载 JSONL）
- [x] local history command（`/history [count|all]` 在 editor 启用时列出最近输入；non-interactive 模式返回未启用错误；持久化结合 `FileInputHistoryStore`）
- [x] local clear command（`/clear` 通过 InteractiveConsoleSession.ClearScreen 发送 ANSI clear-screen + cursor-home 序列；session/runtime state 保持不变；router 在 non-interactive 模式返回未启用错误）
- [x] local find command（`/find <pattern>` 在 _runner.Messages 上 case-insensitive 搜索 TextContent 与 ToolCallContent.Arguments，输出 role + 序号 + 上下文截断的匹配行；缺 pattern 报 usage；无匹配返回友好提示）
- [x] local import command（`/import <path>` 严格导入 Tau snapshot JSON 或 resume JSONL session，并恢复 messages/provider/model/display name；仍缺上游 share/import richer metadata）
- [~] manual / auto compaction（已补 `/compact [instructions]`，当前使用当前模型生成摘要并把 flat session 压成 summary message；JSONL tree 会追加 `compaction` entry，记录 `summary`、`firstKeptEntryId`、估算 `tokensBefore`、`fromHook`、`isSplitTurn` 和 `turnPrefixSummary` baseline；manual / auto compaction 默认优先按 `TAU_CODING_AGENT_COMPACT_KEEP_RECENT_TOKENS` 的 token budget 保留 recent messages，再回落到最近 4 条 message，可用 `TAU_CODING_AGENT_COMPACT_KEEP_RECENT_MESSAGES` 调整 fallback，branch restore / resume / clone export 会重建 summary + retained messages + post-compaction messages；如果 retained cut point 落在一个 user turn 中间，恢复 runtime 时会把 split-turn prefix context 拼入 summary message，HTML transcript timeline 也会展示并可搜索该字段；已补 `TAU_CODING_AGENT_AUTO_COMPACT_TOKENS` / `TAU_CODING_AGENT_AUTO_COMPACT_INSTRUCTIONS`，普通消息执行前超过阈值会自动 compact 并写 `fromHook=true`，context overflow 会恢复回合前 snapshot、compact、记录 `fromHook=true` boundary 并重试同一输入；settings/RPC 已补 `autoCompactionEnabled` boolean control，但实际自动触发仍需要 threshold budget；`/session` 会显示当前估算 token、模型 context window 和 auto threshold 剩余量；已升级为上游结构化 summarization prompt（Goal/Progress/Decisions/Next Steps）和 iterative update summarization（前一次 compaction summary 作为 previous-summary 合并新信息）；仍缺上游 LLM-generated split-turn summarization 的独立 LLM 调用、compaction extension events 和 cancellation UI 语义）
- [~] retry / rollback（已补普通回合失败/取消 rollback baseline：runner exception、取消或错误型 `AgentEndEvent` 会恢复回合前 snapshot，并避免 flat JSON / JSONL tree 持久化失败输入；已补 host-level retryable error auto-retry baseline：settings 优先、`TAU_CODING_AGENT_AUTO_RETRY_ATTEMPTS` / `TAU_CODING_AGENT_AUTO_RETRY_BASE_DELAY_MS` env 兜底控制 429/5xx/rate limit/timeout/network 等 transient error 的有限重试，重试前恢复 snapshot；已补 `/retry [current|default|off|<max attempts> [base delay ms]]` settings command baseline，可持久化并同进程热更新；已补 RPC `set_auto_retry` / `abort_retry` baseline，headless client 可开关 retry 并取消 pending retry delay；已补 Tau-native JSONL `auto_retry_start` / `auto_retry_end` audit entries，成功时先持久化成功 attempt messages 再写 retry end，失败或耗尽时只保留 retry audit；已补 retry delay cancellation visibility baseline，取消 delay 时显示明确状态、写 `Retry cancelled` end audit，并保持失败输入不落盘；context overflow 已单独走 compact-and-retry baseline，且分类现在复用 `Tau.Ai.ContextOverflowDetector` 的上游 provider pattern / non-overflow exclusion baseline；仍缺完整 settings UI 控制和完整 retry cancellation UI）
- [~] JSONL tree navigator / richer session metadata（已补命令行 tree filter/search：`default/no-tools/user-only/labeled-only/all`、settings `treeFilterMode`、`--search query` 与 `--label-time`；已补 `/tree --interactive`（`-i`）j/k/↑/↓/g/G/Home/End/Enter/q/Esc 的交互式 navigator baseline，复用 `IConsoleKeyReader`，输出 `selected entry <id>`/`tree navigator cancelled` 状态，并在 interactive 路径下消费完整 filtered/search 结果，不再沿用文本树摘要的 `MaxEntries` 截断窗口；本地 overlay `/` 搜索现在优先吃 tree view item 的 richer `SearchText`，不再只匹配 `DisplayLine`；interactive tree 还新增了上游同名 `Shift+L` label edit、`Shift+T` label timestamp toggle，以及直接 filter hotkeys：`Ctrl+D` default、`Ctrl+T` no-tools、`Ctrl+U` user-only、`Ctrl+L` labeled-only、`Ctrl+A` all、`Ctrl+O/Shift+Ctrl+O` cycle；raw/composition 两条路径都会按当前 view item 元数据和 filter state 原地切换，其中 labeled-only 已改为读取真实 label 元数据，而不是从渲染文本里猜方括号；label edit 会经由 host prompt 写回 `AppendLabelChange(...)`，随后重开 tree 并尽量保持原选中 entry；已补 `f` filter cycling（default→no-tools→user-only→labeled-only→all→default）、`n`/`N` next/prev match、普通 Left/Right 分页、Ctrl/Alt+Left 折叠或跳到上一个分支段、Ctrl/Alt+Right 展开或跳到下一个分支段、filter/search 切换时保持 selected entry id 稳定、Space 折叠/展开当前 entry descendants、selected entry type/depth/branch/leaf metadata、无匹配时 Enter 不踩空、append-only `tree_state` session metadata fold snapshot、settings-backed `treeCollapsedEntryIds` 兼容 fallback、composition `/metadata` structured inspector baseline（overview/relations/sections + detail summary + relation target summary + numeric relation drill-down/backstack + Tab/Shift+Tab / h/l entry browse），以及 composition tree navigator `i -> metadata inspector -> back to tree` 主屏整合、metadata relation jump 关闭后同步 tree 选中态、必要时自动展开 folded ancestors，和 `/metadata [entry-id]` CLI inspector；仍缺多选和更完整的 TUI metadata inspector 版式）
- [~] dynamic slash command registry / prompt registry / skills/extensions discovery（已补 prompt template discovery/expansion baseline：`~/.tau/prompts`、`./.tau/prompts`、`TAU_CODING_AGENT_PROMPT_PATHS`、`/prompts`、`$1/$@/$ARGUMENTS/${@:N[:L]}` 参数替换；已补 skill command discovery/expansion baseline：`~/.tau/skills`、`./.tau/skills`、`TAU_CODING_AGENT_SKILL_PATHS`、`/skills`、`/skill:<name>`、`disable-model-invocation` 和默认 system prompt inventory；已补 context files baseline：`~/.tau/AGENTS.md|CLAUDE.md` + parent-to-cwd `AGENTS.md|CLAUDE.md` + `--no-context-files` / `-nc` + `/reload` refresh；已补 theme loader/status + CLI/TUI selection baseline：built-in `dark/light`、`~/.tau/themes`、`./.tau/themes`、`TAU_CODING_AGENT_THEME_PATHS`、repeatable `--theme`、`--no-themes/-nt`、上游 required color tokens、`vars` 递归引用、`export.pageBg/cardBg/infoBg`、load diagnostics 和 `/theme` settings 持久化；已补 JSON extension command/resource/load diagnostics baseline：`~/.tau/extensions`、`./.tau/extensions`、`TAU_CODING_AGENT_EXTENSION_PATHS`、`/extensions`、`response/prompt` 参数替换、`sendToRunner`、重复命令 `name:1/name:2`、`promptPaths/skillPaths/themePaths` 资源贡献、extension 文件/resource 明细、坏 JSON 和缺失显式路径诊断；已补上游式 TypeScript/JavaScript extension entry discovery/status baseline：direct `.ts/.js`、目录 `index.ts/js`、`package.json pi.extensions` entry 和 package-loaded extension paths 现在能进入 `/extensions` module 列表；`.js` 和 `.ts` module 现在会通过 limited local Node subprocess runtime 执行 default export factory，支持 `pi.registerCommand(...)` 生成 `/extensions` 可见 slash command，`TryInvoke` 支持 handler return status text，并支持 `pi.sendMessage(...)` / `ctx.sendMessage(...)` 把输入发送到 runner；同一 runtime 也支持 `pi.registerTool(...)` 的 schema/metadata/status 发现、basic execute adapter 和 `prepareArguments` 参数准备，runner 启动时会合并 extension tools，extension tool 可覆盖同名 built-in；同一 runtime 已补 `pi.on("tool_call")` / `pi.on("tool_result")` hook baseline，生产 runner 会通过 `IToolInterceptor` 允许 extension 阻断工具调用、按顺序 mutation `event.input` 并把 mutation 后参数传给实际工具执行，或改写文本 content / JSON details / isError；同一 runtime 还支持 `registerFlag` / `getFlag` / `registerShortcut` metadata/status、CLI flag value injection 和交互式 shortcut handler dispatch；同一 runtime 现在也支持 `agent_start/end`、`turn_start/end`、`message_start/update/end` 与 `tool_execution_start/update/end` 的 `pi.on(...)` 只读生命周期广播，生产 runner 会在对应 AgentEvent 产出前调用 JS/TS handlers；`.ts` 使用 Node 24 type-stripping hooks，覆盖 relative `.ts` import 和 package-loaded `node_modules` 下 erasable TypeScript entry；已补 `CodingAgentExtensionEventBus` 纯内存 event bus foundation，支持 string/typed event subscribe/publish、按订阅顺序 dispatch、dispose unsubscribe、handler exception 聚合结果和 cancellation 透传；已补 `CodingAgentAutocompleteProviderFactory`，把本地 slash command、prompt、skill 和 extension command 转成 Tui slash suggestions，并在启动与 `/reload` 后注入交互式 editor；已补 `/reload` 对 settings / extension resources / prompts / skills / context files / theme status / keybindings / extension shortcut cache 的当前进程重载 baseline；仍缺完整 `@mariozechner/jiti` import/alias/virtualModules parity、rich tool content/renderers/details 语义、session/provider/context/input/user_bash/model/resource/before hooks、真实 extension UI 调用、运行中 runner tool hot-swap、package consumer/network smoke、完整 theme selector parity、TUI theme rendering、theme file watcher、interactive resource selector、full resource selector 和 richer runtime diagnostics）
- [x] standalone HTML transcript export（`/export` 默认 HTML，`/export <path.html|path.htm>` 显式 HTML，覆盖 text/thinking/tool call/tool result/image 内容，并提供 branch outline、filter/search、cwd/parent metadata、current-branch JSONL timeline、label/model/compaction/branch-summary events、本地 Download JSONL、branch summary read/modified file 列表、文本 fenced code block 的独立 code rendering baseline、inline code span rendering baseline、plaintext link rendering baseline、Markdown heading/list/blockquote block rendering baseline、Markdown strong/emphasis span rendering baseline、Markdown pipe table rendering baseline、Markdown task list rendering baseline、image metadata caption baseline、长 tool result 默认折叠 baseline、tool call JSON arguments 格式化 baseline、长 tool call arguments 默认折叠 baseline，以及 read_file result details 的 path/language/line/image/truncation metadata block）
- [~] share/Gist export parity 和上游 richer HTML template（已补 `/share` secret Gist baseline：复用 HTML transcript export、检查 `gh auth status`、执行 `gh gist create --public=false`，并支持 `TAU_SHARE_VIEWER_URL` 覆盖预览 URL；HTML transcript 已支持 message deep-link/copy-link、branch outline filter/search、label badge、model/label/compaction/branch-summary timeline entries 和 `targetId` 定位；仍缺真实 `gh` smoke、Tau 专属 share viewer 和完整上游 richer HTML template）
- [~] richer rendering（已补 HTML transcript 文本 fenced code block -> `code-block` / `<code>` baseline、普通文本 backtick inline code span -> `<code class="inline-code">` baseline、普通文本 Markdown-style `[label](http/https...)` 与裸 `http(s)` URL 外链 baseline、普通文本 angle-bracket autolink `<http(s)://...>` baseline、普通文本 heading/list/blockquote block rendering baseline、嵌套列表会保留子 `<ul>/<ol>` 位于父 `<li>` 内的 HTML 结构、普通文本 horizontal rule (`---`/`***`/`___`) -> `<hr>` baseline、普通文本 strong/emphasis span rendering baseline、普通文本 strikethrough (`~~text~~`) -> `<del>` baseline、普通文本 Markdown pipe table -> 可横向滚动 `<table>` baseline、普通文本 task list -> disabled checkbox baseline、image mime type / byte count caption baseline、长 tool result -> `<details class="tool-result-fold">` 默认折叠 baseline、tool call JSON arguments -> `code-block` / `<code data-language="json">` 格式化 baseline、长 tool call arguments -> `<details class="tool-call-arguments-fold">` 默认折叠 baseline，以及 read_file result details metadata block；仍缺完整 Markdown/highlight renderer、custom tool renderer 和上游 richer HTML template）
- [x] 显式 `Create(provider, model, history)` runner 工厂
- [x] 与 `ModelCatalog` 对齐的默认模型解析层继续收口
- [x] 把当前 `Tau.CodingAgent` / `Tau.WebUi` / `Tau.Mom` / `Tau.CodingAgent.Tests` / `Tau.Agent.Tests` 的 DLL `HintPath` workaround 收回到更正常的 `ProjectReference` 结构
- [x] 解决当前本机上 `Tau.slnx` / metaproj / workload resolver 的 build 异常（`dotnet build Tau.slnx --verbosity minimal` 已通过）

### Tau.Tui

- [~] 真正的输入编辑器（`InteractiveInputEditor` baseline 已落：key-by-key 读取、char append、backspace/delete、左/右光标、Home/End、Ctrl+Left/Right 词级跳转、Ctrl+Backspace 删除前一个词、Ctrl+Delete 删除下一个词、Ctrl-A/E 行首行尾跳转、Ctrl-K/U kill-to-end/start、Ctrl-R 反向 history 搜索（再按 R 切到更旧匹配，Esc/Ctrl-G 取消，Enter 提交）、Enter 提交、Ctrl-C 取消、`InputHistory` Up/Down 历史回放（去重 + capacity）；`Tau.CodingAgent` 在交互式 console（无 redirected stdin/stdout、未设 `TAU_CODING_AGENT_DISABLE_INPUT_EDITOR=1`）下默认使用 editor，非交互回退 `Console.ReadLine`；history 通过 `FileInputHistoryStore` 持久化到 `~/.tau/coding-agent-history`（可被 `TAU_CODING_AGENT_HISTORY_FILE` 覆盖），启动加载 / 提交追加 / 超 capacity 截断；通过 `IConsoleKeyReader`/`IInteractiveRenderer` seam 测试；已补多行输入 baseline：Shift/Alt+Enter 插入换行、反斜杠+Enter fallback 插入换行、Home/End/Ctrl-A/E/K/U 按当前行工作、Backspace/Delete/Ctrl-word 在换行边界合并行、Up/Down 在多行 buffer 内优先纵向移动，`SystemConsoleInteractiveRenderer` 可按多行 buffer 重绘并把提交位置落到输入块末尾；已补会话内 kill ring / yank baseline：Ctrl+K / Ctrl+U / Ctrl+Backspace / Ctrl+Delete 删除文本进入 kill ring，Ctrl+Y yank 最近 kill，Alt+Y 在刚 yank 后轮换；已补会话内最小 undo stack，Ctrl+Z / Ctrl+_ 恢复上一份 text+cursor 快照；已补 `TuiCombinedAutocompleteProvider` slash command、command argument、path 和 `@file` attachment completion foundation，并把 Tab 接到 `InteractiveInputEditor`，当前会在同一 autocomplete session 内直接应用候选、Tab 向前轮换/回绕、Shift+Tab 向后轮换/回绕，Ctrl+Z 回到触发补全前文本/cursor；`Tau.CodingAgent` 启动和 `/reload` 会把本地 command、prompt、skill 和 extension command 注入 autocomplete provider；composition overlay 继续复用 `TuiText.Wrap` 做输入窗口 wrap；仍缺完整上游 editor box、paste marker、redo/复杂分组 undo、autocomplete popup、可视候选选择和 completion hint rendering parity）
- [~] 组件系统（已补 `ITuiComponent` / `ITuiInputComponent`、`TuiContainer`、`TuiBox` 上游 Box background/cache baseline、`TuiTextBlock` 上游 Text background/cache baseline、`TuiTruncatedText` 首行截断组件、`TuiSpacer`、`TuiText` 宽度/截断/wrap/background helper，`TuiLoader` / `TuiCancellableLoader` loader foundation，`TuiMarkdown` markdown foundation，`TuiTerminalImage` / `TuiImage` terminal image foundation，`TuiSettingsList` settings-list foundation，以及 `TuiInputSequenceBuffer` stdin-buffer foundation：settings-list 现覆盖 label/value 对齐、description wrapping、可选搜索、Enter/Space value cycle、submenu delegate/done callback、Esc/Ctrl-C cancel、滚动提示和宽度截断；terminal/image/input foundation 覆盖 capability detection、kitty/iTerm2 escape encoding、PNG/JPEG/GIF/WebP dimension sniffing、row calculation、fallback text、hyperlink helper、image component cache 和 protocol/fallback render、跨 chunk escape sequence buffering、bracketed paste content event、高字节 meta 转换、flush/clear/destroy；当前是库内基础组件树，不等于完整上游 focus stack、real terminal image smoke、Marked/highlight Markdown integration、theme rendering 或 CodingAgent 产品级完整 settings UI）
- [~] terminal host lifecycle（已补 `TuiProcessTerminal` / `ITuiProcessTerminalTransport` / `ITuiTerminalTimer` / `TuiTerminalWriteLog`：raw mode 保存/恢复、UTF-8 input、stdin resume/pause、bracketed paste enable/disable、resize subscription、Unix dimension refresh seam、Windows VT input seam、`TuiInputSequenceBuffer` 输入拆包和 paste re-wrap、Kitty protocol query/enable/disable、modifyOtherKeys fallback/disable、exit drain、ANSI move/cursor/clear/title 操作、`TAU_TUI_WRITE_LOG` / `PI_TUI_WRITE_LOG` 诊断写日志；`Tau.Tui.Tests` 已覆盖 12 个 targeted tests；仍缺真实 TTY/PTY smoke、真实 Kitty/iTerm/Windows terminal 行为、硬件 cursor、完整 generic TUI app host 和 CodingAgent 主屏接管）
- [~] selector foundation / session host（已补 `TuiSelectList`：fuzzy filter/sort、选中态、j/k/方向键/Home/End/PageUp/PageDown/Enter/Esc/Ctrl-C、描述列对齐、滚动提示和 footer hint 行；已补 `TuiMultiSelectList` / `TuiMultiSelectSession`：fuzzy filter/sort、toggle、provider toggle、enable all、clear、Alt+Up/Down reorder、save/cancel；已补 `TuiSettingsList` / `TuiSettingsListSession`：settings item 搜索、value cycle、submenu 委派、cancel/change callback 和 session change/cancel result；fuzzy 规则按上游字符顺序匹配、连续/词边界加权、空 query 保持原显示顺序、space-separated tokens 全部匹配，并支持 alpha/numeric token 交换场景；已补 `ITuiRenderSurface`、`TuiOverlayHost` 和 `TuiSelectorSession`，可测试初始渲染、按键分发、diff apply、Enter 选择和 Esc/Ctrl-C 取消；已接入 `Tau.CodingAgent` `/theme select`、交互式 `/settings`、`/scoped-models`、`/model select`、`/auth select` status selector、`/login` OAuth provider selector、`/logout` OAuth provider selector 和 `/thinking select` thinking level selector baseline；`TuiSettingsList` 现已替换产品级 `/settings select` 主设置面，但完整 upstream submenus、focus stack、OAuth login dialog/session 或 resource selector UI 仍属后续）
- [~] 消息区 / 状态区（已补库层 `TuiMessageArea` 和 `TuiStatusBar`：role prefix、wrap、bottom-anchored visible transcript、left/right status segment 与窄宽截断；已补 `TuiScrollbackBuffer` 纯内存 viewport/scrollback foundation，覆盖 append/replace/clear、`maxLines` oldest-line trim、height resize、line/page scroll、bottom-follow 和 visible lines；已补 `TuiTranscriptViewport` 组合 message/status/scrollback 的固定高度 viewport foundation；已补 `TuiTranscriptViewportHost` 把 viewport、`ITuiRenderSurface` 和 `TuiDiffRenderer` 包成可复用 render/apply wrapper，支持 force/reset frame、resize full redraw、status/message diff、apply 失败不推进 previous frame，并补上 overlay `Anchor` / `Margin` / `OffsetRow` / `OffsetColumn` / `MinWidth` / `MaxHeight` 布局 baseline；已补 `TuiTranscriptSession` start/stop、auto-render mutation、PageUp/PageDown/Home/End 滚动键输入和 key reader seam，其中 Home 固定 scroll-top、End 固定 scroll-bottom；已补 `TuiInputSequenceBuffer` 作为后续真实 terminal raw input 的拆包/paste foundation；仍未接入完整 ProcessTerminal host、非捕获 overlay、硬件 cursor、selector overlay 或 CodingAgent 主屏）
- [x] 键盘体系（`SystemConsoleKeyReader` + `InteractiveInputEditor` 已支持主要导航/编辑键 + 词级跳转/删除 + Ctrl-A/E/K/U readline 习惯 + Ctrl-R 反向搜索；自定义绑定层已落：`IKeyBindingMap` + `EditorAction` 枚举驱动 dispatch，`KeyBindingMap.Default` 保留既有行为并新增 Ctrl+P / Ctrl+Shift+P model cycle action 与 Ctrl+L `SelectModel` action，`KeyBindingFileStore` 从 `TAU_CODING_AGENT_KEYBINDINGS_FILE` 或 `~/.tau/coding-agent-keybindings.json` 加载 `{ key, modifiers, action }` JSON，`action: "None"` 用来 disable 默认绑定）
- [~] 更稳定的差分渲染层（已补 `TuiDiffRenderer` / `TuiRenderFrame` / `TuiRenderOperation` 纯函数式 diff 计划器：首帧/强制/宽高变化 full redraw，稳定尺寸下只返回 changed/cleared lines；`TuiOverlayHost` 已能把 diff 交给可注入 render surface；`TuiAnsiRenderSurface` 已能输出 synchronized ANSI full redraw 和 line diff；`TuiScrollbackBuffer` / `TuiTranscriptViewport` 已固定纯内存 viewport/scrollback 语义；`TuiTranscriptViewportHost` 已固定 runtime host 的 diff apply 顺序和失败重绘语义；`TuiTranscriptSession` 已固定 transcript session 层 state mutation 后的 render 触发语义；仍未实现完整 terminal host、overlay compositing 和硬件 cursor）

## P1：后续应用面

### Tau.WebUi

- [x] 最小聊天 UI
- [x] session 持久化（`output/webui-sessions.json`）
- [x] provider/model 选择入口（`/api/catalog` + 会话设置）
- [x] 流式消息绑定（NDJSON streaming endpoint + 前端 ReadableStream 增量渲染）
- [x] richer rendering / thinking / tool timeline 展示（fenced code blocks、inline code、links、headings/lists/blockquotes、strong/emphasis、tables、task lists、thinking details、tool call cards with status/input/output）
- [x] auth/settings UX（`/api/auth/{provider}` 状态查询 + 前端 provider/model 切换时刷新 auth 状态；真实 login flow 仍在 Tau.Ai OAuth backlog）
- [x] 附件体系（`WebChatAttachmentDto` + 前端 file picker/preview/remove + 发送时 base64 content + text extraction）
- [x] session lifecycle（session delete / export JSON download / export.html / export.md / export.jsonl 线性 transcript / import.jsonl 线性 transcript roundtrip / `.jsonl` 前端文件上传识别 / JSONL import content-type 校验 / `application/problem+json` 稳定错误 code+line / WebUi JSONL export/import 默认 string-value secret redaction / CodingAgent JSONL session 只读 preview endpoint `/api/sessions/import.coding-agent-jsonl/preview` / preview tree metadata（leaf/root/branch/label counts、entry type histogram、current branch entry ids、per-entry depth/children/current-branch/current-leaf/label metadata，id/parent/label resolution 按 case-insensitive tree-store 语义处理）/ `search` 与 `currentBranchOnly` preview filter / filter audit / preview-derived conservative import endpoint `/api/sessions/import.coding-agent-jsonl`（默认全量 timeline，`?currentBranchOnly=true` 只持久化当前 branch messages，并稳定返回/持久化 `importStrategy`：`conservative-timeline-linearized` 或 `conservative-current-branch-linearized`，明确 timeline-only、branch tree 不持久化和 warning codes）/ 前端 import flow 会先做 CodingAgent JSONL preview，并在 preview、import 完成后和重新打开已导入 session 时显示 strategy/currentBranchOnly/timelineOnly/branchTreePersisted=false/leaf/warningCodes 审计摘要 / conservative import source tree/source audit/warnings / CodingAgent JSONL preview/import 默认 string-value secret redaction / clone / search by title / import file upload / title rename / last-opened session restore baseline / clear messages 端点；CodingAgent import 仍按 timeline-derived `preview.Messages` 持久化保守 WebChat DTO，不消费 `preview.Tree`，不替换 WebChatStore，也不是完整 branch/tree parity）
- [~] artifact / sandbox runtime bridge baseline（`WebArtifactStore` 默认持久化到 `output/webui-artifacts.json`；新增 per-session artifact CRUD API、runtime message endpoint、前端 artifact pane、text/image/html/Markdown/SVG/PDF/generic preview 和 HTML sandbox helper `listArtifacts/getArtifact/createArtifact/updateArtifact/rewriteArtifact/createOrUpdateArtifact/deleteArtifact/htmlArtifactLogs`；session 删除会同步清理 artifact state。当前已补 HTML artifact sandbox 的只读 attachment snapshot helper `listAttachments/readTextAttachment/readBinaryAttachment`，一次性 `returnDownloadableFile(fileName, content, mimeType?) -> file-returned` metadata bridge，对照上游 `tools/artifacts/artifacts.ts` 的 create no-overwrite、update `old_str/new_str` 首个匹配替换、rewrite 整文件替换、delete/get 和 `htmlArtifactLogs`/`logs` 本地语义，轻量 Markdown/SVG/PDF/image/text/generic viewer routing/render/download baseline，以及 browser-side `window.executeJavaScriptRepl(code)` execution host：隐藏 sandbox iframe 会复用 attachment/artifact/file-return/console runtime bridge，执行 async browser JS 后返回 console、return value、error 和 returned files，并刷新 artifact pane；`file-returned` 不写 artifact store，保持上游 one-time download 语义。当前又补 session-bound LLM-facing `artifacts` AgentTool 与 WebUi runner 默认注册：工具使用同一 `WebArtifactService` 执行 create/update/rewrite/get/delete/logs，生产聊天流在 `tool_end: artifacts` 后刷新 artifact pane；`WebUiToolPrompts` 也移植了 artifacts、javascript_repl、attachments、artifacts runtime 和 file-download provider description baseline。本轮继续补上 session-bound LLM-facing `javascript_repl` active browser bridge：工具请求进入 `WebUiJavaScriptReplBridge`，活动 WebUi 页面通过 `/api/sessions/{id}/javascript-repl/next` 长轮询领取，调用现有 `window.executeJavaScriptRepl(code)` sandbox host，并通过 `/result` 回传 output/error 与 `fileName/mimeType/size/contentBase64` details；无活动页面时返回明确 timeout error。当前已关闭 server-backed artifact store + runtime bridge + attachments/file-return provider + artifact operation semantics + lightweight viewer baseline + browser JS REPL execution host + LLM-facing artifacts tool wiring + LLM-facing javascript_repl active-browser bridge/file-details 本地合同；仍缺 full artifact message reconstruction、Docx/Excel rich viewers、pdfjs page renderer/view-code toggles、reusable Lit component package、extension sandbox URL mode 和 release/static browser smoke）
- [x] 更高层的 WebUi 行为测试（已补 `WebChatService` fake-runner 流式消息、附件 prompt 和持久化行为测试、Minimal API endpoint 的 NDJSON streaming/session 持久化/export/import/delete/rename/restore/错误状态回归、默认 runner message stream 的 `ITauLogSink -> RuntimeCodingAgentRunner` runtime log 透传回归、artifact CRUD/runtime endpoint、file-return metadata、artifact create/update/rewrite/delete/get/htmlArtifactLogs 回归，以及 `tests/Tau.WebUi.Tests` 用 Microsoft.Playwright 真实 headless Chromium 跑 create session/streaming send/rename/artifact pane refresh、Markdown/SVG/PDF/generic artifact viewer baseline、HTML artifact 读取 session attachment 并调用 `returnDownloadableFile` 的 browser-级 flow 测试）

### Tau.Mom

- [x] 本地文件委派 worker
- [x] `--once`
- [x] inbox/outbox/archive
- [x] 结构化 `.json` 请求（`prompt/provider/model/workingDirectory/title/metadata/attachments`，title/metadata/attachments 已进入 runtime/outbox）
- [x] local attachment staging（本地存在的 request/event attachment 会复制到 `workingDirectory/attachments/`，并通过 `attachments/attachments.jsonl` 与 `log.jsonl` 保留 `original/local` 元数据）
- [x] runner / result schema seam（结构化 `DelegationToolEvent` + stop reason + `DelegationUsage` + 可注入 `ICodingAgentRunner` 工厂，留给 Slack/workspace/sandbox 适配层接线）
- [x] runtime delegation log events（`RuntimeDelegationAgentRunner` 会向 `ITauLogSink` 写 `delegation.start` / `response.start` / `tool.start` / `tool.end` / `response.end` / `usage` / `delegation.end`；生产 `Tau.Mom` 已接 `JsonlTauLogSink.FromEnvironment()`，取消路径会先写 `delegation.end stopReason=cancelled` 再 rethrow；Mom delegation 已用 `requestId` / Slack `ts` 生成 per-delegation `TauRuntimeLogContext`，把 `correlationId/sessionId/messageId` 同步到 Mom 自身 runtime events 和内部 CodingAgent runner `agent/run.*` / tool trace；当前仍是 response/tool/usage 文本摘要 baseline，不是完整 trace/correlation/auth/tool/pod 统一协议）
- [x] workspace memory context（`workingDirectory/MEMORY.md` 与父目录 `MEMORY.md` 注入 delegation prompt）
- [x] channel history context（`workingDirectory/log.jsonl` 最近非 bot 文本消息注入 delegation prompt，跳过 malformed/空文本/current ts）
- [x] local channel log writeback（本地 file delegation 完成后把用户请求和 bot 结果追加到 `workingDirectory/log.jsonl`）
- [x] local runtime status（本地 file delegation 执行前后写 `workingDirectory/status.json`，记录 `running/completed/failed`、请求文件、provider/model、时间、错误与响应摘要）
- [x] local busy-state guard（同一 workdir 已有未过期 `running` 状态时保留 inbox 请求并跳过处理，默认 60 分钟后视为 stale）
- [x] local events wake-up（`events/*.json` 的 `immediate` / `one-shot` / `periodic` 转换为 inbox 委派请求，channelId 映射到本地 channel workdir）
- [x] mom runtime context seam（`<mom_runtime_context>` 注入 workspace/channel layout、events 文件格式、attachment manifest、memory/log/status 路径与 `[SILENT]` 约定）
- [x] local channel session context（`workingDirectory/context.json` 使用 Tau-native session snapshot 恢复/保存同一 workdir 的 runtime messages；同一 workdir 后续 request 未显式传 provider/model 时会沿用上一轮保存的 provider/model/session name，完成后再写回当前 runner messages/provider/model/session name）
- [x] prompt debug snapshot（调用 runner 前写 `workingDirectory/last_prompt.jsonl`，记录 mom runtime context、delegation context、实际 runner input、恢复的 session messages、当前 prompt 和 attachment/image attachment count；默认对常见 secret pattern 做 JSON string value redaction，`TAU_MOM_REDACT_SECRETS=0` 可关闭）
- [x] workspace layout bootstrap（统一创建 `scratch/`、workspace/channel `skills/`、`attachments/`、`events/`，并把 `SYSTEM.md` 与 Agent Skills prompt inventory 注入 prompt context）
- [x] Slack-compatible channel message envelope（`MomChannelMessage` / `MomChannelAttachment` 统一 file/events/未来 Slack adapter 的 channel/user/ts/thread/attachment/request metadata 映射）
- [x] fake Slack transport / responder seam（`IMomChannelTransport` / `IMomChannelResponder` / `MomChannelMessageProcessor` 先固定 Slack adapter 输入输出契约，不直接接真实 SDK）
- [x] Slack event mapper seam（`SlackEventMapper` 固定 Socket Mode `app_mention` / DM / skip / mention stripping / file metadata 到 `MomChannelMessage` 的 receive-side 规则）
- [x] Slack Web API responder seam（`SlackWebApiResponder` 用 `HttpClient` 固定 `chat.postMessage`、thread response、`chat.update`、`chat.delete`、`files.uploadV2` 与 token 脱敏错误边界）
- [x] Slack runtime response update/delete seam（`MomChannelMessageProcessor` 会在 responder 支持 runtime response 时先发 `_Thinking_ ...` 占位，完成后更新最终响应；`[SILENT]` 响应会删除占位且不额外回复）
- [x] Slack Socket Mode transport seam（`auth.test` / `apps.connections.open` / WebSocket text frame / envelope ack / `SlackSocketModeEnabled` worker 开关）
- [x] Slack startup backfill seam（`SlackBackfillService` 对已有 `log.jsonl` 的 channel 调 `conversations.history`，oldest/cursor 分页、去重过滤、log-only old message writeback）
- [x] Slack channel history download/export mode（`mom --download <channel-id>` 使用 bot token 调 `conversations.info` / 分页 `conversations.history` / thread replies `conversations.replies`，按时间顺序把 transcript 写 stdout、进度写 stderr；这是手动导出入口，不等于真实 Slack smoke 或 session sync）
- [x] Mom timestamp migration helper（`scripts/migrate-mom-timestamps.ps1` 对照上游 `packages/mom/scripts/migrate-timestamps.ts`，扫描 `<data-dir>/<channel>/log.jsonl`，默认 dry-run，显式 `-Apply` 时把历史毫秒 Unix `ts` 转成 Slack `seconds.microseconds`，保留坏 JSONL 行、已有 Slack timestamp 和 Tau-native `*-bot` timestamp；`verify-mom-timestamp-migration.ps1` 用 fixture 固定 dry-run/apply/idempotent 行为）
- [x] Slack private file download seam（`SlackAttachmentDownloader` 使用 bot token 下载 `url_private_download/url_private`，并复用 `ChannelAttachmentStore` 写 `original/local/source` manifest）
- [x] Slack per-channel queue seam（`MomChannelQueueDispatcher` 固定同频道顺序处理、不同频道独立推进、pending queue limit 和 stop bypass）
- [x] Slack true cancellable stop seam（`MomChannelRunRegistry` 跟踪当前 in-process channel run，stop bypass queue 后取消 linked runner token，写 `cancelled` status 并回复 `_Stopped_`）
- [x] Agent Skills prompt inventory parity（workspace/channel `skills/**/SKILL.md` -> XML `<available_skills>`，sandbox path mapping，channel override，`disable-model-invocation`）
- [x] Mom sandbox/tool delegation seam（`MomSandboxConfig` / `IMomSandboxExecutor` / `MomToolSet`，默认 host sandbox，配置层支持 `docker:<container>`，新增 `--validate-sandbox` 显式 sandbox validation 入口，Docker validate/exec 路径已可通过注入 `IMomSandboxProcessRunner` 本地验证，runner 默认工具切到 `bash/read/write/edit/attach`，attach 产物进入 execution attachments；`bash` 截断时会把完整 stdout/stderr 保存到 temp `mom-bash-*.log` 并在 result details 暴露 `fullOutputPath`，`edit` 成功结果包含字符数变化和 line-numbered diff details）
- [ ] real Slack smoke
- [~] workspace / sandbox / tool delegation（已补 workspace memory context、本地 attachment staging、scratch 目录、SYSTEM.md、Agent Skills prompt inventory、host sandbox executor、docker path/command construction seam、`--validate-sandbox` 显式 validation 入口、Docker validate/exec 可测试 seam 和 `bash/read/write/edit/attach` tools；Mom `bash` / `edit` 结果 details 已补 full-output path 和 diff fidelity，`attach` 现在按 title 优先返回可见名称并在 `MomAttachToolDetails` 暴露原始 path、sandbox/workspace path、filename、title、sizeBytes；仍缺真实 Docker sandbox smoke）
- [~] message / runtime flow（已补最小 `log.jsonl` channel history 注入、本地 request/result 写回、`context.json` runtime messages、same-workdir provider/model/session name carry-over、`last_prompt.jsonl` prompt debug snapshot、`log.jsonl` / `last_prompt.jsonl` 默认 secret redaction baseline、runtime delegation response/tool/usage log events、delegation -> CodingAgent runner per-run correlation context、`status.json` runtime 状态、本地 busy-state guard、Slack-compatible envelope、Slack event mapper、Slack Socket Mode transport seam、Slack startup backfill seam、channel processor busy/stop/typing/thread response seam、runtime response 占位/更新/`[SILENT]` 删除 seam、true cancellable stop seam、Slack Web API responder seam、Slack private file download seam 与 per-channel queue seam，并已有 fake runner 覆盖本地连续消息路径；仍缺真实 Slack session sync、真实 Slack smoke 下的多消息 flow、完整 trace/correlation 协议和更高层 delegation e2e）
- [~] 更高层 delegation flow 与端到端测试（已补 `MomLocalDelegationFlow`，统一 `Worker` 和 `--once` 的 event -> inbox request -> runner -> outbox/status/log/archive 本地流程，并用 fake runner e2e 固定 provider/model normalize、attachment staging、inbox cleanup、model-only request 解析和本地 same-workdir carry-over；真实 Slack smoke、真实 Docker smoke 和 Slack session sync 仍缺）

### Tau.Pods

- [x] config init / `pods` list / validate / status
- [x] probe（HTTP endpoint / TCP ssh target）
- [x] exec（SSH pod remote command execution）
- [x] deploy / stop / restart / health / logs / deployments（`PodLifecycleService` + CLI commands，SSH-based deploy/stop/restart/logs/deployments 和 HTTP/SSH health check；logs 快照优先读取上游 `~/.vllm_logs/<name>.log`，再回退 journal/旧 `.tau_pods` 日志，可配置 tail，并已支持 `--config` / `--pod` option、active pod fallback 与 JSON `failureKind`；`logs --follow` / `-f` 现在对齐上游 `pi logs <name>` 的 configured model guard，并通过继承 stdout/stderr 的 SSH remote command 执行 `tail -f ~/.vllm_logs/<name>.log`；`vllm deploy` / top-level `start` 现在在远端启动命令成功后先写 configured model state，再运行 deploy-time `startupWatch` 扫描 `~/.vllm_logs/<name>.log` 的 startup complete/failure marker，ready 后再跑 `/health`，failure 时 rollback 并删除 configured model state；deployments 通过 SSH 列出并解析 `~/.tau_pods/*.json` metadata，输出 name/model/status/ts）
- [x] setup plan/run baseline（`PodSetupPlanner` + `setup plan [--json] [--mount <command>] [--models-path <path>] [--vllm release|nightly|gpt-oss] [path] <id>` 已接入 CLI；`PodSetupService` + `setup run [--json] [--script <path>] [--mount <command>] [--models-path <path>] [--vllm release|nightly|gpt-oss] [path] <id>` 已固定 SSH test、SCP `/tmp/pod_setup.sh`、远端 setup 执行和 `nvidia-smi` GPU detect 执行链；`PodDefinition.VllmVersion` 与 `Gpus` 支持持久化 setup 结果，`setup run` 成功后会写回当前 pod 的 `modelsPath`、`vllmVersion` 和 detected `gpus`，失败不写 config，JSON/text 输出显式暴露 `configUpdated` 与 `configPath`；sample config 与 validator 已同步；plan/run 输出只暴露 `HF_TOKEN` / `PI_API_KEY` 配置状态或 placeholder，真实 token 通过 stdin 传给远端 shell，stdout/stderr 会做 token redaction，config 写回也不会保存 token；当前由 fake process runner 覆盖，不宣称真实 SSH/HF/GPU pod smoke 已完成）
- [x] pod registration / active config baseline（`PodsConfig` 已持久化上游兼容 JSON 字段 `active`；`pods --json` / `status --json` 输出 `activePodId` 和 per-pod `active`；新增 `active [--json] [path] <id>` 与 `remove [--json] [path] <id>`，并通过上游式 `pods active` / `pods remove` 暴露；新增上游同形的本地注册入口 `setup [--json] [--mount <command>] [--models-path <path>] [--vllm release|nightly|gpt-oss] [path] <id> "ssh [-p port] <host>"` 和 `pods setup ...` shim，只创建/更新本地 pod config、不执行 SSH/SCP/setup run；无 active 时自动设为新 pod；复杂 SSH options 当前显式拒绝，真实远端执行仍走 `setup run`）
- [x] config path/env/schema compatibility baseline（对照上游 `config.ts`，`PodsCli` 已在未显式传 config path 时支持 `PI_CONFIG_DIR/pods.json`，无 env 时默认 `~/.pi/pods.json`；显式 `--config path` 和旧 positional config path 继续优先；missing config 现在返回空 `PodsConfig`，`pods --json` / `validate --json` 不再报 `Config not found`；`PodsConfigStore` 现在读取/写入上游 record-shaped `pods: Record<string, Pod>` / `models: Record<string, Model>` config，并继续兼容旧 list-shaped Tau config load，save 会迁移到上游形状）
- [x] vLLM command planner baseline（`PodVllmCommandPlanner` 本地生成 serve/systemd/metadata/remote command plan，固定 model cache path convention、deployment name、port、served model name、env、extra args、revision-aware snapshot discovery 和 shell quoting；`vllm plan [--json] [--config path] [--pod id] <model> [--name name] [--revision rev] [--snapshot rev] [--gpus n] [--memory percent] [--context size] [--vllm <args...>]` 已接入 CLI 只打印计划，省略 `--pod` 时默认使用 config `active`，旧 `[path] <id> <model> [name]` positional 形式仍兼容；默认文本输出，`--json` 输出 machine-readable plan；`--vllm` 后剩余参数原样作为 vLLM serve extra args，不被 Tau `--json` 等选项吞掉，且会覆盖 known-model / GPU / memory / context 自动规划；不执行 SSH，不启动 systemd unit，也不是 rollout/orchestration）
- [x] known-model config planning baseline（此前对照上游 `packages/pods/src/models.json` 与 `model-configs.ts`，把上游 known-model matrix 以 `src/Tau.Pods/Models/models.json` 形式随 `Tau.Pods` 输出复制，并新增 `PodKnownModelRegistry` 固定 `isKnownModel` / `getKnownModels` / `getModelName` / GPU type + GPU count config fallback；`PodVllmCommandPlanner` 在没有显式 `--vllm` extra args时会自动把匹配配置的 args/env 注入 `vllm plan/deploy` serve command、metadata 和 JSON/text 输出，显式 `--vllm` 仍覆盖 known-model config；当前关闭的是本地 known-model 参数/env 规划 baseline，usage-aware GPU allocation state 已由后续独立子切片关闭本地合同；仍不关闭直接 PID/log-tail/startup streaming 流程或真实 SSH/HF/GPU/vLLM smoke）
- [x] vLLM GPU/memory/context option planning baseline（本轮对照上游 `commands/models.ts` 的 `--gpus` / `--memory` / `--context` 处理，把 `vllm plan/deploy` 和 top-level `start` 扩展为可在非 `--vllm` 路径请求 known-model GPU count、选择本地 `PodDefinition.Gpus` 中的 GPU id、为单 GPU plan 注入 `CUDA_VISIBLE_DEVICES`、把 `--memory 50%` 归一成 `--gpu-memory-utilization 0.5`、把 `--context 32k` 映射到 `--max-model-len 32768`，并在 JSON/text plan 与 metadata 中暴露 `requestedGpuCount`、`selectedGpus`、`memoryUtilization` 和 `contextTokens`；unknown model 使用 `--gpus` 会报错，显式 `--vllm` 会忽略这些 convenience options。usage-aware GPU allocation state 已由后续独立子切片关闭本地合同；仍不关闭直接 PID/log-tail/startup streaming 流程或真实 SSH/HF/GPU/vLLM smoke）
- [x] usage-aware GPU allocation state baseline（本轮对照上游 `commands/models.ts` 的 `selectGPUs` / `startModel` / `stopModel`，`PodVllmCommandPlanner` 现在从 configured `pod.Models[*].Gpu` 统计 usage，按最少使用 GPU 选择 `selectedGpus`，请求全部 GPU 时保持 inventory 顺序；成功 `vllm deploy` / top-level `start` 写回 configured model 的 model/port/gpu state，成功 `vllm stop` / top-level `stop` / no-arg stop-all 删除对应 state，stop-all 只删除成功停止的 deployment。当前关闭的是本地 allocation state 合同；`Pid=0` 仍是临时边界，不关闭 exact PID/log-tail/startup streaming、真实 SSH/HF/GPU/vLLM smoke 或多版本 rollback）
- [x] deploy PID state writeback baseline（本轮对照上游 `commands/models.ts` 成功启动后把 `pid` 写入 `pod.models[name]` 的本地配置合同，让 `PodVllmOrchestrationService` 在 systemd 路径 best-effort 查询 `MainPID`，在 fallback 路径回显 `.pid` 文件中的 wrapper 进程号，并把 deploy stdout 的 `pid=<n>` 解析到 `PodVllmOperationResult.ProcessId`、CLI JSON `processId` 和 configured model `pid`。当远端没有给出可审计正整数 PID 时仍写 `0`；model runner wrapper baseline 已由后续独立切片关闭本地合同，真实 SSH/HF/GPU/vLLM smoke 或多版本 rollback 仍保持 open）
- [x] vLLM log path compatibility baseline（本轮对照上游 `commands/models.ts` / `model_run.sh` 的 `~/.vllm_logs/<name>.log` 路径合同，`PodVllmCommandPlanner` 现在在 plan/metadata JSON 暴露 `logPath`，top-level `logs` 快照先 tail 该路径再回退 journal/旧 `.tau_pods` 日志；后续 model runner wrapper baseline 已把 deploy 输出改为 wrapper 通过 `script` 写入同一路径，systemd 不再重复 append wrapper stdout/stderr。当前只关闭本地日志路径 baseline；startup watcher、startup complete/failure line parsing、runner wrapper 已由后续独立 baseline 收口，真实 SSH/HF/GPU/vLLM smoke 或多版本 rollback 仍保持 open）
- [x] vLLM startup log marker health/status baseline（本轮对照上游 `startModel` 的 log watcher marker，`vllm health/status` 现在在 curl `/health` 不 ready 后优先扫描 `~/.vllm_logs/<name>.log` 最近日志，识别 `Application startup complete` 为 ready，识别 `Model runner exiting with code`、`Script exited with code`、`torch.OutOfMemoryError`、`CUDA out of memory` 和 engine initialization failure 为 unhealthy，并保留旧 `.tau_pods` 日志 fallback；`ParseState` 也能从 raw startup marker 映射状态。当前只关闭本地 startup marker 判定 baseline；deploy-time startup watcher 与 model runner wrapper 已由后续独立 baseline 收口，真实 SSH/HF/GPU/vLLM smoke 或多版本 rollback 仍保持 open）
- [x] duplicate configured deployment guard baseline（本轮对照上游 `startModel` 在 `pod.models[name]` 已存在时拒绝启动的本地合同，`vllm deploy` 和 top-level `start` 现在在远端 preflight/deploy 前先用 planner 规范化 deployment name，并在 configured model state 已存在时返回 `failureKind=model-already-exists`，不执行 SSH、不覆盖原 configured state；`PodVllmOperationResult`、JSON 和 text 输出新增 top-level `failureKind`，现有 deploy/stop failure 也会给出稳定分类。当前只关闭 duplicate-start guard 与 operation failure kind baseline；deploy-time startup watcher、model runner wrapper 和 save-before-watch/delete-config ordering 已由后续独立 baseline 收口，真实 SSH/HF/GPU/vLLM smoke 或多版本 rollback 仍保持 open）
- [x] realtime log follow baseline（本轮对照上游 `viewLogs()` 的 `tail -f ~/.vllm_logs/<name>.log` 行为，`PodExecService.OpenCommandAsync` 新增继承 stdout/stderr 的 SSH remote command 路径并设置 `FORCE_COLOR=1`；`PodLifecycleService.FollowLogsAsync` 与 `logs --follow` / `logs -f` 会先确认 configured model 存在，missing model 返回 `failureKind=model-not-found` 且不执行 SSH，成功时流式 tail 上游日志路径；`--json --follow` 明确拒绝，普通 `logs --json` 快照合同保持不变。当前只关闭 top-level realtime log viewing baseline；deploy-time startup watcher 与 model runner wrapper 已由后续独立 baseline 收口，真实 SSH/HF/GPU/vLLM smoke 或多版本 rollback 仍保持 open）
- [x] deploy-time startup watcher baseline（本轮对照上游 `startModel()` 启动后 tail `~/.vllm_logs/<name>.log` 并等待 `Application startup complete` / failure marker 的合同，`PodVllmOrchestrationService.DeployAsync` 现在在 remote deploy command 成功后运行 `WatchStartupAsync`，结构化输出 `startupWatch` JSON/text；watcher 识别 `Model runner exiting with code`、`Script exited with code`、OOM 和 engine initialization failure 为 `failureKind=startup-failed`，失败会触发 rollback，ready 后再继续现有 `/health` readiness；configured model 先写后删的上游时序由后续独立 baseline 收口。当前只关闭 deploy-time startup marker wait / failure rollback baseline；model runner wrapper 已由后续独立 baseline 收口，真实 SSH/HF/GPU/vLLM smoke、多版本 rollback 或 Pods final `verified` 状态仍保持 open）
- [x] model runner wrapper / pseudo-TTY script baseline（本轮对照上游 `packages/pods/scripts/model_run.sh` 和 `commands/models.ts` 的 `/tmp/model_run_<name>.sh` + `script -q -f -c ... ~/.vllm_logs/<name>.log` 启动合同，`PodVllmCommandPlanner` / `PodVllmOrchestrationService` 现在生成并上传 `~/.tau_pods/model_run_<name>.sh` 与 `~/.tau_pods/model_wrapper_<name>.sh`，systemd 与 fallback 都启动同一 wrapper；runner 保留 `Model runner exiting with code`，wrapper 追加 `Script exited with code`，plan/deploy text/JSON 与 metadata 暴露 runner/wrapper script path 和 `usesPseudoTtyWrapper`，stop/rollback cleanup 会删除 runner/wrapper 脚本。当前只关闭本地 runner/wrapper/pseudo-TTY 合同；HF download / PI API key command baseline 和 save-before-watch/delete-config ordering baseline 已由后续独立切片收口，真实 SSH/HF/GPU/vLLM smoke、多版本 rollback 和 Pods final `verified` 状态仍保持 open）
- [x] model runner HF download / PI API key command baseline（本轮对照上游 `packages/pods/scripts/model_run.sh`，`PodVllmCommandPlanner` 生成的 `model_run_<name>.sh` 现在在 runner header 后执行 `HF_HUB_ENABLE_HF_TRANSFER=1 hf download "$MODEL_ID"`，失败时输出稳定 `ERROR: Failed to download model` 并退出，成功时输出 `Model download complete`；Tau-planned `vllm serve` 命令固定携带 `--api-key "$PI_API_KEY"`，再追加 served model name 与额外 vLLM args。当前只关闭本地上传脚本/远端命令文本合同；save-before-watch/delete-config ordering baseline 已由后续独立切片收口，真实远端 HF download、PI API key 环境、SSH/GPU/vLLM startup smoke、多版本 rollback 和 Pods final `verified` 状态仍保持 open）
- [x] startModel save-before-watch / delete-config ordering baseline（本轮对照上游 `commands/models.ts` 在 remote start 返回 PID 后先保存 `config.pods[podName].models[name]`，再等待 startup log，启动失败后删除配置的合同，`PodVllmOrchestrationService.DeployAsync` 新增 `onRemoteStarted` seam；`PodsCli` 在远端启动成功后、`startupWatch` / `/health` 之前写 configured model state，并在 startup watcher 或 health 失败 rollback 后删除该 state。测试直接在 watcher/health fake SSH 命令执行时读取 config，证明 save-before-watch 中间状态可观察。当前只关闭本地 config ordering contract；真实 SSH/HF/GPU/vLLM startup smoke、多版本 rollback、transport hardening 和 Pods final `verified` 状态仍保持 open）
- [~] vLLM SSH command/service orchestration baseline（`vllm preflight/deploy/status/health/stop [--json]` 已通过 `PodVllmOrchestrationService` 接入 CLI：preflight/deploy 支持 `--config path` / `--pod id` / `--name name` / `--revision rev` / `--snapshot rev` 显式消歧，status/health/stop 支持 `--config path` / `--pod id` 显式 override，省略 `--pod` 时使用 config `active`；preflight 会 SSH 检查 `command -v vllm`、远端 HF cache、`snapshots/`、默认 `refs/main`、指定 `refs/<revision>` / `snapshots/<revision>` 和单 snapshot fallback，输出 `requestedRevision`、`resolvedModelPath`、`snapshotCount`、`vllmAvailable` 与稳定 `failureKind`，并对 cache 缺失、snapshot 缺失、ref 指向缺失、指定 revision 缺失、多 snapshot ambiguous、SSH failure 给出可操作 summary；deploy 会先跑 preflight，失败时不写 service、不启动 vLLM；带 `--prefetch` 且 failureKind 属于模型 cache/snapshot 缺口时会复用 `model pull`（含 revision）后重新 preflight，prefetch 失败仍不写 service、不触发 rollback；成功后把 concrete snapshot path 注入 runner 中的 `vllm serve`；deploy 会 SSH 写 `~/.tau_pods/model_run_<deployment>.sh`、`~/.tau_pods/model_wrapper_<deployment>.sh`、`~/.tau_pods/<deployment>.service` 和 `.json`，runner 先执行 upstream-style HF download 并用 `--api-key "$PI_API_KEY"` 启动 vLLM，优先 `systemctl --user enable --now` 启动 wrapper，systemd user command 不可用或失败时 fallback 到 `setsid` / `nohup` 启动同一 wrapper并写 `.pid`；remote start 成功后 CLI 会先写 configured model state，再按 12 次、5 秒 backoff 先扫 upstream log marker 再做远端 `/health` readiness 窗口，可用 `--no-health` 跳过，也可用 `--health-attempts` / `--health-backoff-ms` 调整；startup watcher 或 health 失败会 rollback cleanup 并删除 configured model state；`vllm deploy ... [--vllm <args...>]` 已把 sentinel 后剩余参数透传进远端 `vllm serve`；部署命令失败或 health 最终非 ready 时尝试 rollback cleanup；status 拉取 systemd/fallback pid/metadata 文本；health 返回 `ready/unhealthy/dead/starting/unknown`、`failureKind` 和 attempts；stop/rollback disable/remove unit、kill fallback pid并清理 metadata/service/pid/runner/wrapper 脚本；生产入口会把同一个 `TAU_LOG_FILE`/默认 `.tau/log.jsonl` sink 注入 vLLM service，写出 `vllm.preflight/deploy/status/health/stop/rollback.start|end` 摘要事件，但不把完整 remote command/stdout/stderr 放进 runtime log。成功只代表 SSH command 和配置的 readiness 窗口通过，不代表真实 GPU/HF/vLLM 环境长期健康；metadata 仍是 `planned-vllm`，rollback 是 cleanup，不是多版本 rollout）
- [~] 真正的 CLI 运维命令体系（已补 `health/deploy/stop/restart/logs/deployments`、上游式 `pods` group、top-level `start <model> --name <name>` / `list` / `stop <name>` shim、no-arg `stop` stop-all 聚合、top-level `shell` SSH 入口、top-level `agent` prompt 参数映射 + Tau.CodingAgent process runner baseline、vLLM deploy/stop configured model state 写回/删除和 usage-aware GPU allocation baseline、`model list/pull/remove/status` model lifecycle baseline、`model pull --revision` / `--snapshot` revision-aware HF download、`vllm deploy --prefetch --revision` preflight/pull/re-preflight/deploy 闭环、`model` 子命令可省略 pod id 并默认使用 config `active`（`pull/remove/status` 单尾随参数按 model id 解释，两个尾随参数保留旧 `<pod-id> <model-id>` 用法）、`model list/pull/remove/status` 与 `deployments` 现在也支持 `--config path` / `--pod id` 显式入口、`deployments` / top-level `list` 以及 `vllm status/health/stop` / top-level `stop <name>` 默认使用 config `active`（有 pod-id 时仍可显式 override，且支持 `--config path` / `--pod id`；no-arg `stop` 会列出 deployments 后逐个 stop）、`vllm plan/preflight/deploy` / top-level `start` 通过 `--config path` / `--pod id` / `--name name` / `--revision rev` / `--snapshot rev` 支持 active pod fallback、revision 选择并保留旧 `[path] <pod-id> <model-id> [deployment-name]` positional 用法、`model list/status --json` 远端 HF snapshot/ref 可见性（`snapshotCount`、`resolvedModelPath`、`snapshotFailureKind`）、`setup plan/run` setup command 与 SSH/SCP/script/GPU-detect execution chain baseline、setup result config writeback、setup run `configUpdated/configPath` 输出、vLLM version metadata、token stdin transport 与输出/config redaction、`vllm plan/preflight/deploy/status/health/stop` CLI baseline 与 `--json` 输出、`vllm plan/deploy ... --vllm <args...>` 自定义 vLLM 参数透传、pod-level `modelsPath`、长耗时 `pull` 的 SSH keepalive、可省略 path 的 target-command 参数解析、SSH lifecycle metadata 命令转义、SSH exec/interactive shell `ArgumentList` argv hardening、本地 ssh 启动失败/runner 异常/cancellation 结构化返回、vLLM command/service orchestration baseline、远端模型 snapshot preflight、health retry/backoff、failure classification 和 CLI 参数回归测试；仍缺真实 pod prompt e2e、真实 HF download/SSH smoke、真实远端 setup run/SCP/GPU smoke、真实 vLLM startup/health smoke、多版本回滚、更完整远端 transport hardening和真实运维 smoke）

## P2：工程化

- [~] release 产物改为真实 Tau 可执行产物（当前已覆盖 Windows current-RID artifact、zip/tar.gz archive structure smoke、PowerShell no-env wrapper、`pi-test.ps1`、release dry-run plan/preparation/validation/execution scripts、release finalization script、package publish script、release contract smoke、release finalize smoke、release package publish smoke、session transcript/cost audit smoke、CodingAgent auth migration smoke、root JSONL relocation smoke、commands-to-prompts smoke、managed tools-to-bin smoke、deprecated extension dirs audit smoke、keybindings id migration smoke、edit stats smoke、Mom timestamp migration smoke、startup profile smoke、MSBuild version sync smoke，以及 GitHub Actions Windows workflow artifact upload。`scripts/plan-release.ps1` 现在列出 release finalization preview、guarded package publish preview、`release-finalize-smoke`、`release-package-publish-smoke`、CodingAgent auth/session/commands/tools/keybindings migration smoke、deprecated extension dirs audit smoke、edit stats smoke、Mom timestamp smoke、startup profile smoke 和 release version sync smoke；`scripts/verify-release-contracts.ps1` 和 CI 也固定同一 smoke。新增 `scripts/finalize-release.ps1` / `scripts/verify-release-finalize.ps1` 对照上游 `release.mjs` 后段，把 Tau 本地 release commit/tag 后的 branch/tag push 与可选 GitHub Release archive upload 做成 guarded finalization：默认 dry-run，显式 `-Apply` 才在 clean worktree、remote、branch、tag 指向 branch tip、archive 等预检通过后 push，`-CreateGitHubRelease` 通过 fake-gh smoke 固定 archive upload、draft/prerelease flags 和 dirty worktree 阻断。新增 `scripts/publish-release-packages.ps1` / `scripts/verify-release-package-publish.ps1` 对照上游 `npm run publish`，把 package registry publish 做成 guarded dry-run/apply 层：默认只覆盖 `Tau.Ai`、`Tau.Agent`、`Tau.Tui` 库包，应用项目默认仍走 release archives；API key 只从指定 env 读取且 JSON/command output/子进程 output preview 脱敏；fake-dotnet smoke 固定默认库包边界、pack/push 参数、API key output preview redaction、应用项目 warning 和 dirty worktree 阻断。CodingAgent assistant `usage.cost` 默认持久化和本地 `/session` / RPC stats display 已有 baseline。仍缺非宿主平台 executable smoke、真实 NuGet/package registry 发布演练、应用项目包策略、真实 package signing/provenance rehearsal、真实远端发布演练、session transcript analyze 子 agent 模式、真实 provider usage-cost 样本和 richer footer/TUI 展示、full settings/runtime auth parity、credential refresh UX、actual hooks/custom tools migration to extensions、general custom tool migration/runtime parity、edit tool runtime/e2e 继续收口、TUI first-frame startup profiling/CPU profile、Photon/interactive raster assets/external export-html payload parity、exact Unix wrapper/auth-backup parity 和真实外部 e2e release smoke；`pi-ai` wrapper command-name/help/error contract 已在 release artifact smoke 里闭合，但 package/global install alias 仍 open）
- [~] release provenance / package signing 本地 baseline（已新增 `scripts/generate-release-provenance.ps1`、`scripts/sign-release-packages.ps1` 与 `scripts/verify-release-provenance.ps1`；`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `release-provenance-smoke`。当前关闭的是本地 manifest SHA256 审计、NuGet signing dry-run/apply 命令合同、证书路径/指纹入口、timestamp 参数、dirty apply 阻断和证书密码脱敏；仍缺真实 code-signing certificate rehearsal、真实 NuGet/package registry 发布演练、release archive signing、外部供应链 attestation、非宿主 executable smoke 和真实外部 e2e release smoke）
- [x] AI test image fixture generator（`scripts/generate-ai-test-image.ps1` 对照上游 `packages/ai/scripts/generate-test-image.ts` 生成 200x200 白底红圆 PNG fixture，默认输出 `tests/Tau.Ai.Tests/Data/red-circle.png`，也支持 `-OutputPath` 与 `-Json`；实现用纯 PowerShell 写 PNG chunks 与 zlib stored blocks，不依赖 node-canvas 或 System.Drawing；`verify-ai-test-image.ps1` 用临时目录固定 PNG signature/chunks、IHDR、zlib/adler32、scanline filter、关键像素和 SHA256，当前 30 assertions 通过；`plan-release.ps1`、`verify-release-contracts.ps1` 和 CI 已纳入 `ai-test-image-smoke`。本项不关闭 CodingAgent image/clipboard ingestion、provider vision e2e 或 upstream canvas antialias byte-for-byte parity）
- [ ] solution build 的环境异常诊断文档化
- [ ] provider e2e 测试（当前 Bedrock 已有 StubHandler 级 bearer/SigV4/shared profile/eventstream 回归，Vertex 已有 ADC token/SSE 回归，Gemini CLI/Antigravity 已有 headers/fallback/retry/empty-stream 回归，仍缺真实云端 e2e）
- [ ] coding-agent 默认路径的更高层回归测试
- [x] 当前新增验证基线（本轮 CodingAgent CLI `--session <path>` explicit file path baseline 已通过 CodingAgent build 0 warning / 0 error、focused `CodingAgentInitialMessageBuilderTests|CodingAgentSessionTargetTests` gate 39/39 和完整 `Tau.CodingAgent.Tests` 577/577。该验证覆盖 CLI parser 对 `--session <path>` / `--session=<path>` 的捕获，显式 flat session path 只用 flat store 并加载 snapshot，显式 `.jsonl` session path 只用 tree controller 并优先 tree snapshot，默认 target 在 tree empty 时仍保留 flat session fallback。验证过程中还修复了 `WebChatStore` / `WebArtifactStore` 在 Windows 临时目录下的 overwrite 写入兼容问题，并通过 WebChatStore 单点回归 1/1、WebUi artifact focused gate 5/5、WebUi 非浏览器回归 53/53 与完整 WebUi browser flow 61/61；`WebUiBrowserFixture` 也已移除测试进程内 `playwright install chromium --with-deps`，避免受限 Windows 环境下 Playwright OS dependency install script 的 `Get-WmiObject : Access denied` / 下载阻塞。后续最终复跑 `verify-dotnet.ps1 -SkipRestore` 已通过，计数为 Ai 287、Agent 123、Tui 251、CodingAgent 615、WebUi 61、Pods 216；`-SkipWebUiBrowserTests` 仅作为浏览器启动受限环境的显式兜底，不能替代 WebUi release/static smoke。）
- [x] 当前新增验证基线（本轮 CodingAgent CLI `--models` / `--list-models` / `--verbose` baseline 已通过 CodingAgent build 0 warning / 0 error、focused `CodingAgentInitialMessageBuilderTests|CodingAgentModelListFormatterTests|CodingAgentCliModelScopeTests` gate 46/46、完整 `Tau.CodingAgent.Tests` 587/587 和 `dotnet run --project src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-build -- --list-models gemini` smoke。该验证覆盖 parser 对 `--models` / inline `--models=`、`--list-models` optional search 和 `--verbose` 的捕获，model table formatter 的 token/image/reasoning 输出与 fuzzy search，`--models` per-process override 优先于 settings `enabledModels` 参与 model cycle，以及 `google/*:low` 通配 scope 展开为规范 `google/gemini-2.5-pro:low`。完整 upstream fuzzy scope、真实 provider/auth e2e 和 package/bin identity 仍保持 open。）
- [x] 当前新增验证基线（本轮 CodingAgent CLI `--continue` / `--session-dir` baseline 已通过 focused `CodingAgentInitialMessageBuilderTests|CodingAgentSessionTargetTests` gate 50/50、完整 `Tau.CodingAgent.Tests` 594/594，以及沙箱内 `verify-dotnet.ps1 -SkipRestore -SkipWebUiBrowserTests`（Ai 287、Agent 123、Tui 251、CodingAgent 594、WebUi 非浏览器 53、Pods 216）。该验证覆盖 parser 对 `--continue`、`-c`、`--session-dir <dir>` 与 inline `--session-dir=<dir>` 的捕获，显式 session-dir 创建新的 per-run JSONL tree session，`--continue --session-dir <dir>` 选择该目录最近有效 JSONL session，目录为空时新建，以及默认无 session flags 时仍保留 flat + tree fallback。direct `--fork <path>` 和本地 `--resume` selector 已由后续验证基线关闭；session id / partial id 全局 lookup、cross-project fork prompt 和完整 upstream SessionManager schema 仍保持 open。）
- [x] 当前新增验证基线（本轮 CodingAgent CLI `--fork <path>` path-only baseline 已通过 focused `CodingAgentInitialMessageBuilderTests|CodingAgentSessionTargetTests` gate 60/60、完整 `Tau.CodingAgent.Tests` 604/604、CLI fork smoke（sourceCount=1 / forkCount=1）以及沙箱内 `verify-dotnet.ps1 -SkipRestore -SkipWebUiBrowserTests`（Ai 287、Agent 123、Tui 251、CodingAgent 604、WebUi 非浏览器 53、Pods 216）。该验证覆盖 parser 对 `--fork <path>` / inline `--fork=<path>` 的捕获，缺值 / 空 inline 值 / 下一个 token 为 option 时的硬错误，fork 源文件不存在时的明确 IOException，以及源 JSONL tree session 当前 branch 复制到默认 `.tau/coding-agent-sessions` 或显式 `--session-dir` 目录的新 JSONL session，并记录 `parentSession`。本地 `--resume` selector 已由后续验证基线关闭；session id / partial id 全局 lookup、cross-project fork prompt 和完整 upstream SessionManager schema 仍保持 open。）
- [x] 当前新增验证基线（本轮 CodingAgent CLI `--session <id-prefix>` / `--fork <id-prefix>` 本地 lookup baseline 已通过 focused `CodingAgentInitialMessageBuilderTests|CodingAgentSessionTargetTests` gate 63/63、完整 `Tau.CodingAgent.Tests` 607/607、CLI id-prefix smoke（`--session <prefix>` 打开已有 JSONL 不新增文件，`--fork <prefix>` 后 JSONL 数从 1 增到 2）以及沙箱内 `verify-dotnet.ps1 -SkipRestore -SkipWebUiBrowserTests`（Ai 287、Agent 123、Tui 251、CodingAgent 607、WebUi 非浏览器 53、Pods 216）。该验证覆盖显式 `--session-dir` 优先的 JSONL session id-prefix lookup、missing prefix 明确 `No session found matching '<prefix>'`、matched tree session startup、matched fork source current-branch export 与 `parentSession` metadata。本地 `--resume` selector 已由后续验证基线关闭；全局 lookup、cross-project prompt 和完整 upstream SessionManager schema/selection 仍保持 open。）
- [x] 当前新增验证基线（本轮 CodingAgent CLI `--resume` 本地交互 selector baseline 已通过 focused `CodingAgentInitialMessageBuilderTests|CodingAgentSessionTargetTests|CodingAgentStartupResumeResolverTests|CodingAgentModelListFormatterTests|CodingAgentCliModelScopeTests` gate 77/77、完整 `Tau.CodingAgent.Tests` 615/615、完整 `Tau.WebUi.Tests` 61/61、`dotnet run --project src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-build -- --list-models gemini` smoke、`dotnet run --project src\Tau.CodingAgent\Tau.CodingAgent.csproj --no-build -- --resume --print` 非交互诊断 smoke，以及项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 287、Agent 123、Tui 251、CodingAgent 615、WebUi 61、Pods 216）。该验证覆盖 parser 对 `--resume` / `-r` 的捕获、print/RPC 非交互拒绝、空 session 列表 0 退出、显式 `--session-dir` session 列表选择、取消选择以及选择后打开 tree session。全局 `listAll` lookup、cross-project prompt 和完整 upstream SessionManager schema/selection 仍保持 open。）
- [x] 当前新增验证基线（本轮 WebUi-local import/export integer row-closure 已把 parity matrix `WebUi-local import/export` 行从 `ported` 提升到首个 `verified`，machine 计数 verified 从 0 -> 1（其余 ported 31、partial 197、missing 1、external-e2e-needed 31、non-goal-proposed 1，total 262）。新增 `tests/Tau.WebUi.Tests/WebChatExportImportRoundTripTests.cs` 9 个测试，把 `Tau.WebUi.Tests` 从 61 提到 70：覆盖 JSONL round-trip + source metadata、redaction on/off、HTML encoding/role-classed `<article>`/redaction footer、Markdown structure/thinking/tool-call/attachment/error rendering，以及此前未测的 importer error code（`empty_line`、`missing_type`、`invalid_entry_type`、`missing_field`），与既有 `WebChatJsonlExporterTests.cs` 一起把 `missing_session_header`、`invalid_json`、`unsupported_version`、`duplicate_message_id`、`invalid_parent_chain` 也纳入证据链。已通过 `dotnet build tests\Tau.WebUi.Tests\Tau.WebUi.Tests.csproj --no-restore --verbosity minimal` 0 warning / 0 error 和完整 `Tau.WebUi.Tests` 70/70；最终项目级 `verify-dotnet.ps1 -SkipRestore` 复跑见下。该行是 Tau-native export-import surface，`verified` 反映其本地证据已对所存在 surface 完整覆盖、且无 external-e2e 依赖，不代表 upstream component-package 形状 parity；reusable Lit component package / CSS export 仍由其它 partial 行管理。）
- [x] 当前新增验证基线（本轮 AI OAuth PKCE integer row-closure 已把 parity matrix `packages/ai/src/utils/oauth/pkce.ts` 行从 `ported` 提升到 `verified`，machine 计数 verified 从 1 -> 2（其余 ported 30、partial 197、missing 1、external-e2e-needed 31、non-goal-proposed 1，total 262）。在既有 2 个 PKCE 测试（43 字符 base64url verifier、RFC 7636 S256 challenge formula、每次不同）基础上，`OAuthProviderTests.cs` 新增 5 个测试把 `Tau.Ai.Tests` 从 287 提到 292：verifier 还原为 32 字节随机熵（RFC 7636 §4.1 推荐长度）、verifier/challenge 严格符合 base64url alphabet 且无 padding、challenge 还原为 32 字节（SHA-256 输出长度）、多次迭代结构稳定且 verifier 唯一、challenge 对同一 verifier 确定性可复算。PKCE 是纯确定性 / 无网络的 RFC 7636 verifier/challenge 生成；上游 `pkce.ts` 无对应测试，故 `verified` 反映 Tau 对该 surface 的本地证据已完整且无 external-e2e 依赖。已通过 focused 复跑 `Tau.Ai.Tests` 292/292；最终项目级 `verify-dotnet.ps1 -SkipRestore` 复跑见下。真实 provider OAuth login/refresh e2e 仍由其它 external-e2e-needed 行管理，与本 PKCE 生成 surface 无关。）
- [x] 当前新增验证基线（本轮 AI unicode sanitizer integer row-closure 已把 parity matrix `packages/ai/src/utils/sanitize-unicode.ts` 行从 `ported` 提升到 `verified`，machine 计数 verified 从 2 -> 3（其余 ported 29、partial 197、missing 1、external-e2e-needed 31、non-goal-proposed 1，total 262），并在此过程中发现并修复一个真实生产 bug。新增 `tests/Tau.Ai.Tests/UnicodeTextSanitizerTests.cs` 15 个直接单元测试把 `Tau.Ai.Tests` 从 292 提到 307：覆盖 paired surrogate 保留、unpaired high/low 删除、首尾边界、连续 high、反转 low+high、valid pair 紧邻 unpaired surrogate、多个 distinct pair、null/empty passthrough、no-surrogate/only-paired 短路返回原引用，以及 strip 后无残留 unpaired surrogate 的端到端不变量。所有 surrogate 内容用 raw `char` cast（`(char)0xD83D` 等）构造、源文件纯 ASCII，避免 literal emoji 经编辑器/文件编码/console round-trip 损坏；断言比较 raw code unit 而非 rendered 字符串。该测试暴露出 `UnicodeTextSanitizer.RemoveUnpairedSurrogates` 在 valid pair 之前没有任何 unpaired surrogate 时会丢掉 pair 的 low surrogate：原实现用 `builder?.Append(text[++i])`，当 builder 仍为 null（尚未分配）时 null-conditional 短路掉了 `++i` 副作用，循环自增落到 low surrogate 上被当成 lone low surrogate 删除。既有 8 个 provider 集成测试都把 unpaired surrogate 放在 emoji 之前、提前分配 builder，因此一直漏掉该路径。修复把 advance 改为无条件 `i++`。已通过 focused `UnicodeTextSanitizerTests` 15/15 与完整 `Tau.Ai.Tests` 307/307；最终项目级 `verify-dotnet.ps1 -SkipRestore` 复跑见下。该 surface 为纯确定性 / 无网络的 UTF-16 surrogate 清洗，`verified` 反映本地证据完整且无 external-e2e 依赖。）
- [x] 当前新增验证基线（本轮 AI provider registry integer row-closure 已把 parity matrix `packages/ai/src/api-registry.ts` 行从 `ported` 提升到 `verified`，machine 计数 verified 从 3 -> 4（其余 ported 28、partial 197、missing 1、external-e2e-needed 31、non-goal-proposed 1，total 262）。新增 `tests/Tau.Ai.Tests/ProviderRegistryTests.cs` 13 个直接单元测试把 `Tau.Ai.Tests` 从 307 提到 320：覆盖 factory 注册的 lazy 求值（注册不创建实例、首次 Get 才创建、后续 Get 复用同一实例）、instance 注册、同 api 重复注册替换、Get 未注册抛 `KeyNotFoundException`、TryGet 命中/未命中、RegisteredApis 成员、Unregister、UnregisterBySource 只删匹配 source 而保留其它、无匹配 source 时 no-op、Clear 清空，以及 source 维度的批量注销选择性。对照上游 `api-registry.ts` 的 `registerApiProvider/getApiProvider/getApiProviders/unregisterApiProviders/clearApiProviders` 与 `sourceId` 语义；Tau 的注册表不复制上游 `wrapStream` 的 "Mismatched api" 守卫（Tau provider 通过 `IStreamProvider.Api` 自描述），该设计差异已在 matrix 行注明，不作 parity 夸大。已通过 focused 复跑 `Tau.Ai.Tests` 320/320；最终项目级 `verify-dotnet.ps1 -SkipRestore` 复跑见下。该 surface 为纯确定性 / 无网络的注册表，`verified` 反映本地证据完整且无 external-e2e 依赖；exact TypeScript `ApiProvider` 泛型/wrap 形状仍是 .NET-native 差异。）
- [x] 当前新增验证基线（本轮 CodingAgent extension UI fire-and-forget RPC bridge baseline 已通过 focused `ExtensionToolUiFireAndForgetActionsEmitRpcRequests|ExtensionUiBridge|CodingAgentRpcHost` gate 64/64、focused `LoadStatus_LoadsJavascriptExtensionWithVirtualPackageImports` 1/1、完整 `Tau.CodingAgent.Tests` 533/533。该验证覆盖真实 JavaScript extension tool handler 中 `ctx.hasUI`、`notify`、`setStatus`、string-array `setWidget`、`setTitle`、`setEditorText`、`pasteToEditor` 到 RPC `extension_ui_request` 的 JSONL 输出，以及 `@sinclair/typebox`、`@mariozechner/pi-ai`、`pi-ai/oauth`、`pi-agent-core`、`pi-tui`、`pi-coding-agent` virtual import stub 子集在真实 JS extension command/tool 中参与注册执行；等待型 dialog/custom UI/full jiti import/alias/virtualModules 仍未关闭。）
- [x] 当前新增验证基线（本轮 CodingAgent extension lifecycle broadcast baseline 已通过 focused `RuntimeCodingAgentRunnerTests|CodingAgentExtensionCommandStoreTests` gate 51/51、完整 `Tau.CodingAgent.Tests` 531/531 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 287、Agent 123、Tui 251、CodingAgent 531、WebUi 61、Pods 216）。该验证覆盖 JS/TS `pi.on("agent_start"|"agent_end"|"turn_start"|"turn_end"|"message_start"|"message_update"|"message_end"|"tool_execution_start"|"tool_execution_update"|"tool_execution_end")` 的 production runner 只读 payload 广播，以及 handler exception 不失败 run、只写 `extension/event.error` runtime log 的非阻断语义。）
- [x] 当前新增验证基线（本轮 CodingAgent extension shortcut interactive dispatch baseline 已通过 focused `CodingAgentExtensionCommandStoreTests|CodingAgentHostTests` gate 82/82、完整 `Tau.CodingAgent.Tests` 528/528、完整 `Tau.Tui.Tests` 251/251、`git diff --check`（仅 CRLF normalization warning）和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 123、Tui 251、CodingAgent 528、WebUi 61、Pods 216）。该验证覆盖 JS/TS `pi.registerShortcut` handler dispatch、`ctx.sendMessage(...)` runner message mapping、interactive editor shortcut hook、host resolved shortcut cache、reserved built-in keybinding conflict warning/skip、non-reserved built-in override warning、duplicate extension shortcut warning/later-wins 和 `/reload` 后 shortcut cache refresh。）
- [x] 当前新增验证基线（本轮 CodingAgent extension `registerFlag` / `registerShortcut` metadata/default/getFlag/status baseline 已通过 CodingAgent build 0 warning / 0 error、focused flags/shortcuts gate 4/4、focused `ExtensionCommandStore|ExtensionsCommand` gate 30/30、完整 `Tau.CodingAgent.Tests` 521/521、WebUi bridge timeout 目标复验 1/1、`git diff --check` 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 123、Tui 251、CodingAgent 521、WebUi 61、Pods 216）。该验证覆盖 JS/TS module load、`pi.registerFlag` boolean/string default metadata、`pi.getFlag` 同 extension 默认值读取、`CodingAgentExtensionStatus.Flags`、`/extensions` flags 输出、`pi.registerShortcut` metadata/status 和非零 module status `flags N` / `shortcuts N`。）
- [x] 当前新增验证基线（本轮 CodingAgent extension CLI flag value injection baseline 已通过 CodingAgent build 0 warning / 0 error、focused `ExtensionCommandStore|ExtensionsCommand|CliArguments` gate 32/32、缺值 string flag CLI smoke（输出 `Extension flag "--mode-name" requires a value` 且 `exit=1`）、完整 `Tau.CodingAgent.Tests` 524/524 和项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 123、Tui 251、CodingAgent 524、WebUi 61、Pods 216）。该验证覆盖未知 `--flag` / `--flag=value` / `--flag value` 解析、extension flag 注册表校验、boolean flag -> `true`、string flag value 注入、unknown/value-less string flag diagnostics、CLI 值覆盖 `registerFlag` default 后的 `pi.getFlag(...)` 读取，以及 error diagnostics 返回非零退出码。）
- [x] 当前新增验证基线（本轮 CodingAgent extension `tool_call` input mutation baseline 已通过 CodingAgent build 0 warning / 0 error、focused Agent mutation gate 2/2、focused CodingAgent hook gate 3/3、focused `ExtensionCommandStore|RuntimeCodingAgentRunner` gate 40/40、完整 `Tau.Agent.Tests` 123/123、完整 `Tau.CodingAgent.Tests` 517/517 和项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 123、Tui 251、CodingAgent 517、WebUi 61、Pods 216）。此前 CodingAgent extension tool `prepareArguments` baseline 已通过 focused `ExtensionCommandStore|RuntimeCodingAgentRunner` gate 37/37、完整 `Tau.CodingAgent.Tests` 514/514 和项目级 gate；CodingAgent extension `registerTool` limited runtime 已通过 focused `ExtensionCommandStore|RuntimeCodingAgentRunner` gate 35/35、完整 `Tau.CodingAgent.Tests` 512/512 和项目级 gate；CodingAgent TypeScript extension command runtime 已通过 focused `ExtensionCommandStore|ExtensionsCommand` gate 17/17、完整 `Tau.CodingAgent.Tests` 507/507 和项目级 gate；CodingAgent JavaScript extension command runtime 已通过 focused `ExtensionCommandStore|ExtensionsCommand` gate 13/13、完整 `Tau.CodingAgent.Tests` 503/503 和项目级 gate；extension entry discovery/status 已通过 focused `PackageManager|ExtensionCommandStore` gate 21/21、完整 `Tau.CodingAgent.Tests` 498/498 和项目级 gate。当前 TS/JS extension 验证覆盖 package manifest `pi.extensions` 的 `.js/.ts` module load、TypeScript interface/type annotation、relative `.ts` import、package-loaded `node_modules` entry、`pi.registerCommand` command metadata、`pi.registerTool` tool metadata/schema/executionMode/basic execute/details、`prepareArguments` 参数准备、`tool_call` handler block/reason、input mutation/no-revalidation/cross-module chaining、`tool_result` handler content/details/isError rewrite、built-in tool override、extension duplicate first-wins、`/extensions` runtime/status display、`pi.sendMessage` / `ctx.sendMessage` runner message mapping、handler return status text 和 load failure diagnostic；entry discovery 验证覆盖 direct `.ts`、目录 `index.js`、package entry `src/main.ts`、JSON command 兼容和 `/extensions` status module metadata。）
- [x] 当前测试基线（本轮 CodingAgent usage-cost stats/display 已通过 focused CodingAgent gate 248/248、完整 `Tau.CodingAgent.Tests` 484/484 和项目级 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 120、Tui 251、CodingAgent 484、WebUi 61、Pods 216）。上一轮 CodingAgent assistant usage cost persistence 已通过 focused Agent/Proxy gate 18/18、focused CodingAgent session/tree gate 10/10、session audit script smoke 15 assertions、完整 `Tau.Agent.Tests` 120/120、完整 `Tau.CodingAgent.Tests` 480/480 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 120、Tui 251、CodingAgent 480、WebUi 61、Pods 216）。上一轮 pod-agent process runner baseline 已通过 focused Pods `Agent|PodsCli` gate 96/96、CodingAgent `CodingAgentInitialMessageBuilderTests` 6/6、完整 `Tau.Pods.Tests` 216/216、完整 `Tau.CodingAgent.Tests` 456/456 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 61、Pods 216）。上一轮 WebUi LLM-facing JavaScript REPL active-browser bridge baseline 已通过 focused `WebUiTools|JavaScriptReplBridgeEndpoints|WebUiPageTests` gate 10/10、真实 Chromium `JavaScriptReplBridge` browser flow 2/2、完整 `dotnet test tests\Tau.WebUi.Tests\Tau.WebUi.Tests.csproj --no-restore --verbosity minimal` 61/61 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 61、Pods 215）。上一轮 WebUi LLM-facing artifacts tool baseline 已通过 focused `WebUiTools|WebUiPageTests` gate 7/7、完整 WebUi 57/57 和项目级 `verify-dotnet.ps1 -SkipRestore`（Ai 280、Agent 119、Tui 251、CodingAgent 456、WebUi 57、Pods 215）。上一轮 WebUi JavaScript REPL execution host baseline 已通过 focused `JavaScriptRepl|WebUiPageTests` gate 4/4 和完整 WebUi 53/53；上一轮 WebUi artifact specialized viewer baseline 已通过跨项目 `dotnet test tests\Tau.CodingAgent.Tests\Tau.CodingAgent.Tests.csproj --filter WebUiEndpointTests --no-restore --verbosity minimal` 11/11。上一轮 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 已通过 tau-ai、Agent platform examples、WebUi 和 Mom smoke。此前 release validation 在 `dotnet restore Tau.slnx -r win-x64 --verbosity minimal` 后通过 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\build-release-artifacts.ps1 -Configuration Release -SkipRestore`，该命令 publish 5 个 executable app 并执行 artifact smoke；archive validation 已覆盖 `package-release-artifacts.ps1 -ArtifactRoot .\artifacts\tau-win-x64 -ArchiveFormat zip`、`package-release-artifacts.ps1 -ArtifactRoot .\artifacts\tau-win-x64 -ArchiveFormat tar.gz`，并用 `-ArchiveFormat tar.gz -ForceSmoke` 验证 tar.gz 解压后的宿主 artifact smoke；artifact smoke 覆盖 `tau-ai` / `pi-ai`、CodingAgent RPC、Pods help、WebUi 和 Mom `--once`。）
- [~] CodingAgent RPC mode（已复用 runner `Steer/FollowUp` seam 落地 `--mode rpc` JSONL baseline，覆盖 prompt、steer、follow_up、abort、new_session（含上游同名 `parentSession` metadata，以及 `summarizeCurrentBranch` / `customInstructions` / `replaceInstructions` summary hook）、get_state、get_settings、update_settings、set_model、cycle_model、get_available_models、set_thinking_level、cycle_thinking_level、set_auto_retry、abort_retry、bash、abort_bash、set_steering_mode、set_follow_up_mode、set_auto_compaction、switch_session、get_fork_messages、fork（上游兼容 `text/cancelled` response，Tau 扩展保留 leaf/message/model 元数据）、compact、clone、get_session_stats、get_messages、get_commands、export_html、get_last_assistant_text、set_session_name；`bash` 已补 `bash_output` stdout/stderr chunk、终态 truncation/fullOutputPath metadata 和 `bash_event` lifecycle baseline，最终 `bash` response contract 保持不变；`CodingAgentRpcExtensionUiBridge` 已补 extension UI request/response bridge baseline；JS/TS extension runtime 现在能在真实 handler 内把 fire-and-forget `notify/setStatus/setWidget/setTitle/setEditorText/pasteToEditor` 发到同一 JSONL bridge；`CodingAgentRpcClient` 已补上游 `rpc-client.ts` 的 start/stop/send/event/timeout/typed helper baseline；RPC assistant event 与 message/content 投影已覆盖 `assistantMessageEvent` 和 toolCall `arguments` 对象形 baseline；`prompt` response timing 现在对齐上游 preflight hook 的本地可测边界：首条 agent event 写出前才返回 success，构造/首事件前失败返回 command error，accepted response 不被后续 abort cancellation 吃掉；`get_state.pendingMessageCount` 现在来自真实 steering/follow-up queue count，`get_state.isCompacting` 可观察 manual compact 后台运行状态；`get_commands` 当前只返回可通过 prompt 调用的 extension/prompt/skill 动态命令，local JS/TS `registerCommand` 执行基线在 slash store/host 边界完成，不代表 RPC 已具备完整 extension runner integration；仍缺等待型 extension UI dialog 从 JS/TS handler 等待 response、完整 jiti/extension runner integration、exact response schema 其它细节、上游 settings selector / 全量配置面和 full command parity）
- [~] 可观测性：provider 调用、auth、tool execution、session / delegation / pod probe 的最小日志（已补 `Tau.Ai.Observability.ITauLogSink`/`JsonlTauLogSink`/`NullTauLogSink` baseline，默认写 `./.tau/log.jsonl`，可被 `TAU_LOG_FILE` 覆盖或 `TAU_LOG_DISABLED=1` 关闭；`JsonlTauLogSink` 当前默认对完整 runtime event JSONL 做 string-value redaction，覆盖 category/event/field value 并保留 field key/number/bool/null；`TauRuntimeLogContext` 已提供最小 cross-module correlation 字段 baseline，`RuntimeCodingAgentRunner.RunAsync` 每次 run 生成/继承 `correlationId` 并让 `agent/run.start|run.end|run.cancel|run.error` 带 provider/model/inputBytes/elapsedMs/error 及 correlation 字段；JS/TS lifecycle handler error 以 `extension/event.error` 摘要记录且不失败 run；WebUi message stream 默认 runner每次发送注入 `sessionId` 和 transient `messageId`；`AgentRuntime` 现在会在每次 provider stream 边界写 `provider/run.start|run.end`，字段限于 provider/model/api、message/tool count、transport、usage/cost、stopReason、failureKind、duration 和 correlation/session/message id，不写 prompt、headers、api key、metadata、完整 messages、tool arguments 或 tool result；`AgentLoopConfig.LogContext -> ToolExecutor` 让 `tool/execution.start|end` 继承同一 `correlationId/sessionId/messageId`，覆盖 toolCallId/toolName/executionMode/argumentBytes/success/isError/failureKind/durationMs/contentBlockCount/textBytes/detailType，并对 not-found、blocked、invalid-arguments、cancelled、exception 和 tool-result-error 做轻量分类且不写完整 arguments/result；`ProviderAuthResolver.GetStatus` 已写 `auth/status.checked` 摘要事件，字段限于 provider/configured/source/usesOAuth/canLogin，CodingAgent 和 WebUi auth status 生产路径都会透传 runtime sink；`RuntimeDelegationAgentRunner` 已 emit Mom delegation response/tool/usage/end events 且生产入口接入 env log sink，并把同一 `TauRuntimeLogContext` 传给内部 CodingAgent runner；CodingAgent JSONL tree session 写出也默认脱敏 string value；`PodProbeService` 已写 `probe.start/end`，`probe.end` 已包含 summary/statusCode/endpoint/host/port/failureKind 摘要字段；`PodVllmOrchestrationService` 已写 vLLM `preflight/deploy/status/health/stop/rollback.start|end` 摘要事件；`PodLifecycleService` 已写普通 pod lifecycle `health/deploy/stop/restart/logs/deployments.start|end` 摘要事件；`PodModelService` 已写 `model.list/pull/remove/status.start|end` 摘要事件；仍缺 Pods 与真实 e2e trace 的统一 correlation 贯通）
- [ ] `scripts/verify-dotnet.sh` 对运行态 smoke 的进一步自动化
- [x] `scripts/verify-dotnet.ps1` 对运行态 smoke 的进一步自动化（`-RunSmoke` 已覆盖 `WebUi` 与 `Mom --once`）
- [~] 把 `verify-dotnet.ps1 -RunSmoke` 接到 CI 或补 bash 等价 smoke（`.github/workflows/tau-ci.yml` 已接入 Windows PowerShell no-env smoke、Release artifact build、archive package/extract smoke 和 zip artifact upload；本地仍以 PowerShell 链验证，尚未补 bash 等价 smoke，也未证明真实外部 e2e release smoke）

## 当前已知环境现实

- [ ] 当前 Windows 环境下 `bash scripts/verify-dotnet.sh --skip-restore` 会落到 WSL 并失败于缺少 `/bin/bash`
- [x] Windows 本机已补 `scripts/verify-dotnet.ps1` 作为等价项目级验证入口
- [ ] 本地标准命令继续保持 bash 形式，但现场执行要接受 PowerShell 脚本或等价顺序 `dotnet` 验证作为兜底
- [x] `Tau.Mom` 也已收回 `ProjectReference`
