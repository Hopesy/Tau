# Tau 100% pi-mono parity /goal

## /goal prompt

当前目标（foundation-first override，2026-06-15）：先把 `Tau.Ai` / `Tau.Agent` 作为可被外部 .NET 项目引用的 Agent 基座收口到本地 100% 完成，然后才恢复 `Tau.CodingAgent`、`Tau.Tui`、`Tau.WebUi`、`Tau.Mom`、`Tau.Pods` 等工具/产品项目迁移。全量 `C:\Users\zhouh\Desktop\pi-mono-main` 100% 可审计移植仍是最终目标，但当前 `/goal` 调度必须先服务 AI/Agent foundation。

你是 Main Integrator，不是单模块 worker。当前主线恢复为 `docs/exec-plans/active/2026-05-28-tau-100-percent-pi-mono-parity.md`；Phase 1 inventory freeze 的权威矩阵是 `docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md`。后续实现必须从 matrix 的 `Phase 2 Candidate Queue` 或当前 active plan 明确列出的 Phase 2/3/4/5 缺口领取，不重开 broad inventory。

`docs/exec-plans/completed/2026-06-07-tau-agent-platform-baseline.md` 是已完成的前置能力：它证明 Tau 已具备第一版可复用 Agent 应用底座、examples、provider run / tool execution runtime log、platform smoke 和全仓 gate 证据。它不是 100% product parity 完成证据，也不能关闭真实 provider/OAuth、Slack、Docker、SSH/HF/GPU/vLLM、WebUi artifact/runtime、Tui live terminal、release/package registry 等最终缺口。

每一轮都按当前 repo 事实推进：先区分 committed baseline、dirty WIP、已验证结果、文档声称和真实上游行为；再选择一个可审计、可验证、可单独评审的 parity 切片。当前优先级下，如果 `Tau.Ai` / `Tau.Agent` foundation completion gate 没有完成并形成提交边界，不领取其它工具/产品模块的新切片。不要问“要不要继续”。只有存在真正歧义且继续会产出与用户意图相反的成果时才停下来问。

## /goal 执行协议

- `/goal` 是 Tau 迁移的持续运行主控入口。收到 `继续`、`继续继续`、`下一步` 时，不重新询问是否继续，也不重开 inventory，而是直接读取当前工作树、`GOAL.md`、`next.md`、matrix、active plan、history 和验证证据，进入下一轮。
- 每一轮都必须按 `审视 -> 执行 -> 提升` 运行，只领取一个互斥切片；切片完成后立刻同步 `next.md`、`docs/QUALITY_SCORE.md`、active plan、matrix 和 history，再进入下一轮。
- 切片的默认退出条件只有三个：当前切片的验收门槛全部满足、选择的缺口被用户明确确认是 `non-goal`、或者同一阻塞条件连续三轮复现且确实无法前进，需要向用户说明阻塞。
- 这份文件写的是“怎么持续推进直到完成”，不是“一次性任务清单”；中间任何通过都只是 checkpoint，最终目标仍然是把所有迁移推进到 100% parity 并关闭 Final audit。

## Foundation-first completion gate

当前用户指定 `Tau.Ai` / `Tau.Agent` 先于其它工具项目完全收口。这里的 “100%” 是 **Agent 基座可被其它 .NET 项目引用使用** 的本地完成标准，不等同于全量 pi-mono product parity 或真实外部云服务验收。只有以下条件全部满足，才能恢复其它工具/产品模块迁移：

- `Tau.Ai` package 可独立被外部项目引用：本地 pack 后，临时外部 app 只 `PackageReference Include="Tau.Ai"`，能 restore/build/run，并通过 `Tau.Ai.Providers.Faux`、`ProviderRegistry`、`StreamFunctions` 完成一次 LLM 调用。
- `Tau.Agent` package 可被外部项目引用：本地 pack 后，临时外部 app 只 `PackageReference Include="Tau.Agent"`，能通过 `Tau.Agent.nuspec` 传递消费 `Tau.Ai`，并运行 `AgentApplication`、delegate tool、session store 和 runtime log 回合。
- `Tau.Ai` / `Tau.Agent` NuGet metadata 不再是占位：package description、README、license、repository、tags 和 `Tau.Agent -> Tau.Ai` dependency 均被脚本断言。
- public API compile samples、Agent platform examples、`verify-agent-package-consumer.ps1`、`verify-release-contracts.ps1` 和 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 全部通过。
- `GOAL.md`、active plan、parity matrix、`next.md`、`docs/QUALITY_SCORE.md`、README 和 history 同步记录该 foundation 边界。

真实 provider/OAuth e2e、真实 NuGet registry 发布、真实 signing/provenance 和 global install alias 仍是后续全量 parity / external-e2e / release gates；TypeScript/npm compatibility package 不属于当前 .NET foundation gate，若未来要求真实 npm shim，需要作为单独 product surface 处理。没有真实凭证或 registry 环境时不得伪造成完成，但也不得阻塞本地 Agent 基座包引用能力的完成判定。

## Current checkpoint

- [x] Agent platform baseline completed：`src/Tau.Agent/Platform/**`、Console/HTTP examples、provider run + tool execution runtime log、platform smoke 和全仓 gate 已经作为 shared Agent foundation 归档到 `docs/exec-plans/completed/2026-06-07-tau-agent-platform-baseline.md`。
- [x] Phase 1 inventory freeze completed：`docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 已冻结 capability、file-level、surface 和 root scripts/manifests 三层 inventory；后续不重开 broad package scan。
- [x] Foundation-first local package consumer boundary：`scripts/verify-agent-package-consumer.ps1` 覆盖 `Tau.Ai` direct package consumer 和 `Tau.Agent` transitive `Tau.Ai` package consumer；其中 `Tau.Ai` consumer 还显式证明 `ModelConfigurationStore` / `ProviderAuthResolver` 注入、`models.json` 动态 provider、`auth.json`、model-level 与 provider-level auth status、OpenAI-compatible header 覆盖链路、上游 `google-generative-ai` API 名 alias 调度、provider/model/default `models.json options` 注入、OpenAI chat-completions provider-specific options typed dispatch、OpenAI Responses provider-specific options typed dispatch、Mistral provider-specific options typed dispatch（含 function `toolChoice` 对象）、Anthropic provider-specific options typed dispatch（thinking、display、interleaved thinking 和 tool `toolChoice`）、Google provider-specific options typed dispatch（`toolChoice`、thinking budget 和 max tokens），以及 Bedrock provider-specific options typed dispatch（`region`、`profile`、`bearerToken`、tool `toolChoice`、reasoning/thinking、`requestMetadata`）。2026-06-19 已复跑 dedicated smoke 91 assertions；`verify-release-contracts.ps1 -Json` 和此前 `verify-dotnet.ps1 -SkipRestore -RunSmoke` 已通过，本地 Agent 基座 package consumer gate 已验证完成。
- [x] Tau.Ai env/api key resolution and auth-status file-level contract：`packages/ai/src/env-api-keys.ts` / `Known provider ids and env keys` 已用 52 项 env/auth 定向断言和完整 `Tau.Ai.Tests` 393/393 收口为 `verified`，覆盖 Anthropic、Copilot、Google/Gemini、Vertex ADC、Bedrock ambient marker 和已实现的 OpenAI/Azure/Groq/Cerebras/xAI/OpenRouter/Vercel AI Gateway/z.ai/Mistral/Minimax/HF/opencode/kimi env aliases；后续 `Tau.Ai` 工作应优先转向剩余 provider-specific e2e，而不是重复补同类 env 映射。
- [x] Tau.Ai OAuth registry/helper local contract：`OAuthProviderRegistry` 现在固定 built-in restore、custom register/unregister/reset、provider info listing 和 helper-level refresh/get-api-key 合同；`OAuthProviderRegistryTests`、`ProviderAuthResolverTests`、`EnvironmentApiKeyResolverTests` 和 `AiPublicApiCompileSampleTests` 的 focused gate 68/68 通过，真实 provider/browser/cloud OAuth e2e 仍保持 `external-e2e-needed`。
- [x] Tau.Ai helper utilities local contract：`headers.ts`、`hash.ts`、`overflow.ts`、`typebox-helpers.ts` 对应的 .NET-native helper rows 已验证为纯本地 `verified`，focused helper/public API gate 36/36、完整 `Tau.Ai.Tests` 399/399 通过；aggregate helper utilities 仍保留 provider edge audit / TypeBox export/runtime / real e2e 缺口。
- [x] Tau.Ai public Faux provider local contract：`packages/ai/src/providers/faux.ts` 对应的 .NET-native public test provider 已收口为 `verified`；`StreamOptions.Signal` / `OnResponse` 进入 public surface，Faux provider 已固定 response callback、pre-cancel/mid-stream aborted terminal assistant、queued/factory responses、usage/cache、streamed deltas 和 unregister 行为。focused `FauxProviderTests|AiPublicApiCompileSampleTests` 21/21、完整 `Tau.Ai.Tests` 402/402 通过；真实 provider/OAuth e2e 不因此关闭。
- [x] Tau.Ai shared stream options local contract：`packages/ai/src/types.ts` 与 `packages/ai/src/providers/simple-options.ts` 中的 `onPayload`、`onResponse` 和 `thinkingBudgets` 已落到 `StreamOptions.OnPayload`、`StreamOptions.OnResponse`、`ProviderResponse`、`SimpleStreamOptions.ThinkingBudgets` 和 `StreamOptionHelpers`。OpenAI、OpenAI-compatible、OpenAI Responses/Codex/Azure、Anthropic、Google/Gemini CLI/Vertex、Mistral 与 Bedrock 请求路径都接入 payload/response callback；Google/Gemini CLI/Vertex、Anthropic 和 Bedrock 复用 shared thinking-budget helper。focused shared-options gate 39/39、当时完整 `Tau.Ai.Tests` 406/406 通过；provider-wide cancellation local contract 已由下一条单独收口，真实 provider/OAuth e2e 和真实云端 callback/cancellation timing correlation 仍保持 open。
- [x] Tau.Ai provider cancellation local contract：`StreamOptions.Signal` 已贯穿 OpenAI、OpenAI-compatible、OpenAI Responses/Codex/Azure、Anthropic、Google/Gemini CLI/Vertex、Mistral、Bedrock 和 Vertex ADC token exchange 的真实 I/O、retry delay、WebSocket、SSE 与 Bedrock eventstream 解析路径；pre-cancel 与用户触发的 `OperationCanceledException` 均收束为 terminal `ErrorEvent("Request was aborted")` 和 `StopReason.Aborted` assistant。focused cancellation/provider gate 44/44、完整 `Tau.Ai.Tests` 410/410 通过；真实 provider/OAuth e2e 与真实云端 cancellation timing correlation 仍保持 open。
- [x] Tau.Ai KnownApi alias adapter：`ModelApiNames` 现在把上游公开 API 名 `openai-completions` / `openai-compatible` / `google-generative-ai` 规范化到 Tau 内部 canonical API `openai-chat-completions` / `google-generative-language`；`ProviderRegistry.Register/Get/TryGet/Unregister` 和 `ModelConfigurationStore` 共同消费该映射。focused alias/config/registry gate 52/52 通过，外部 package consumer smoke 36 assertions 通过；这关闭本地 API 名称兼容层，不关闭真实 provider/OAuth e2e 或字节级 TypeScript API 名 parity。
- [x] Tau.Ai models.json request options local contract：`ModelConfigurationStore` 现在解析 provider-level、`modelOverrides.<model>.options` 和 `models[].options`，并由 `StreamFunctions` 按 provider -> modelOverride -> model -> explicit code options 合并到 `StreamOptions` / `SimpleStreamOptions`。本地合同覆盖 `temperature`、`maxTokens`、`topP`、`transport`、`cacheRetention`、`sessionId`、`maxRetryDelayMs`、`metadata`、`reasoning` 和 `thinkingBudgets`；`Signal`、`OnPayload`、`OnResponse` 仍只能来自代码。focused config/public API gate 11/11、当时完整 `Tau.Ai.Tests` 424/424、外部 package consumer smoke 47 assertions 通过；剩余非 Responses provider-specific option map 和真实 provider/OAuth e2e 仍保持 open。
- [x] Tau.Ai OpenAI chat-completions models.json provider-specific options local contract：对照上游 `providers/openai-completions.ts`，`OpenAiOptions` 现在公开 `ToolChoice` 与 `ReasoningEffort`，`OpenAiToolChoice` 支持字符串选择和 `{ type: "function", function: { name } }` function choice。`models.json options` 对 `openai-chat-completions` 支持 `toolChoice` 和 `reasoningEffort`，`StreamFunctions` 会投影到 typed `OpenAiOptions`，显式代码 `OpenAiOptions` 继续优先；`OpenAiProvider` 会输出 `tool_choice` 和按 compatibility 映射后的 `reasoning_effort` / thinking 字段。focused `OpenAiProviderSerializationTests|ModelConfigurationStoreTests|AiPublicApiCompileSampleTests` 33/33、完整 `Tau.Ai.Tests` 450/450、外部 package consumer smoke 91 assertions 通过；真实 OpenAI/OpenAI-compatible 云端字段验收、OAuth/provider e2e、registry/signing/provenance 仍保持 open。
- [x] Tau.Ai OpenAI Responses models.json provider-specific options local contract：`models.json options` 现在额外支持 Responses 家族第一批 provider-specific 字段，并由 `StreamFunctions` 在 provider dispatch 前投影到 typed options：`openai-responses` 支持 `serviceTier`、`reasoningEffort`、`reasoningSummary`；`openai-codex-responses` 额外支持 `textVerbosity`；`azure-openai-responses` 额外支持 `azureApiVersion`、`azureResourceName`、`azureBaseUrl`、`azureDeploymentName`。代码显式 typed options 仍优先，`StreamSimple` 在有 provider-specific config 时会走 typed `Stream(...)` 分发，避免 provider 重建 options 时丢字段。focused Responses/config gate 33/33、完整 `Tau.Ai.Tests` 429/429、外部 package consumer smoke 52 assertions 通过；其它 provider-specific option map、真实 provider/OAuth e2e 和真实云端字段验收仍保持 open。
- [x] Tau.Ai Mistral models.json provider-specific options local contract：对照上游 `providers/mistral.ts`，`models.json options` 现在支持 `mistral-conversations` 第一批 provider-specific 字段：字符串型 `toolChoice`、对象型 function `toolChoice`、`promptMode` 和 `reasoningEffort`。`StreamFunctions` 会把配置投影到 `MistralOptions`，`StreamSimple` 在存在这些配置时走 typed `Stream(...)`，显式 `MistralOptions` 仍优先；代码侧也可用 `MistralToolChoice.Function(name)` 直接输出对象型 `tool_choice`。focused `ModelConfigurationStoreTests|MistralProviderTests` 22/22、完整 `Tau.Ai.Tests` 434/434、外部 package consumer smoke 59 assertions 通过；其它非 Responses provider-specific option map、真实 Mistral 云端字段验收和真实 provider/OAuth e2e 仍保持 open。
- [x] Tau.Ai Anthropic models.json provider-specific options local contract：对照上游 `providers/anthropic.ts`，`AnthropicOptions` 现在公开 `thinkingEnabled`、`thinkingBudgetTokens`、adaptive `effort`、`thinkingDisplay`、`interleavedThinking` 和字符串/tool 对象型 `toolChoice` 的 .NET-native surface；`ModelConfigurationStore` 可从 `models.json options` 解析这些字段，`StreamFunctions` 会投影到 typed `AnthropicOptions` 并保持显式代码 options 优先，`AnthropicProvider` 会输出 `thinking`、`output_config.effort`、`tool_choice`、`metadata.user_id` 和 non-adaptive interleaved-thinking beta header。focused `AnthropicProviderTests|ModelConfigurationStoreTests|AiPublicApiCompileSampleTests` 24/24、完整 `Tau.Ai.Tests` 441/441、外部 package consumer smoke 69 assertions 通过；真实 Anthropic 云端字段验收、OAuth/browser refresh e2e 和其它 provider-specific option map 仍保持 open。
- [x] Tau.Ai Google models.json provider-specific options local contract：对照上游 `providers/google.ts`、`google-vertex.ts` 和 `google-gemini-cli.ts`，Google 系 typed options 现在公开 `GoogleThinkingOptions`、`GoogleOptions`、`GoogleVertexOptions.ToolChoice/Thinking` 和 `GoogleGeminiCliOptions`；`models.json options` 支持 `toolChoice`、`thinkingEnabled`、`thinkingBudgetTokens`、`thinkingLevel`、Vertex `project/location` 和 Gemini CLI `projectId`，并由 `StreamFunctions` 投影到对应 typed provider options。Google/Vertex/Gemini CLI provider 会把 tool choice、thinking config 和 project override 写入本地 request body / URL 构造路径。focused `ModelConfigurationStoreTests|GoogleGeminiCliProviderTests|GoogleVertexProviderTests|AiPublicApiCompileSampleTests` 39/39、完整 `Tau.Ai.Tests` 444/444、外部 package consumer smoke 74 assertions 通过；真实 Google/Vertex/Gemini CLI/Antigravity 云端调用、OAuth/browser refresh e2e 和真实云端字段验收仍保持 open。
- [x] Tau.Ai Bedrock models.json provider-specific options local contract：对照上游 `providers/amazon-bedrock.ts`，`BedrockOptions` 现在公开并消费 `InterleavedThinking`，`models.json options` 支持 Bedrock 第一批 provider-specific 字段：`region`、`profile`、`bearerToken`、字符串/tool 对象型 `toolChoice`、`reasoning`、`thinkingBudgetTokens`、`thinkingDisplay`、`interleavedThinking` 和 `requestMetadata`；`StreamFunctions` 会把配置投影到 typed `BedrockOptions` 并保持显式代码 options 优先，`BedrockMessageConverter` 会输出 tool choice、request metadata、thinking display 和 non-adaptive Claude `anthropic_beta` request field。focused `ModelConfigurationStoreTests|BedrockProviderTests` 39/39、完整 `Tau.Ai.Tests` 447/447、外部 package consumer smoke 85 assertions 通过；真实 AWS Bedrock credential chain/cloud request、OIDC client registration renewal、AWS profile/cache concurrency 和真实 provider/OAuth e2e 仍保持 open。
- [x] Agent platform builder facade option pass-through：`AgentApplicationBuilder.AddTool(..., prepareArguments:)` 现在会透传给 `DelegateAgentTool`；`AgentPlatformTests.PromptAsync_UsesDelegateToolPrepareArgumentsBeforeExecution`、`AgentPublicApiCompileSampleTests` 和外部 package consumer smoke 已证明该参数在执行前生效且可被外部消费者编译使用。
- [x] Agent stream proxy local server path：`tests/Tau.Agent.Tests/ProxyStreamProviderTests.cs` 已覆盖真实 loopback HTTP/SSE `/api/stream` server path、Authorization/body contract、缺 terminal event 和 malformed SSE JSON；`scripts/verify-agent-proxy-server-e2e.ps1` 已接入 `verify-dotnet.ps1 -RunSmoke`、`plan-release.ps1` 和 `verify-release-contracts.ps1`。
- [x] Tau.Ai.Cli local dotnet tool install alias：`scripts/verify-ai-cli-tool-install.ps1` 通过临时本地 package source 安装 `pi-ai` / `tau-ai` dotnet tool 包并验证 `--help` / `list` 行为；`Tau.Ai.Cli` 现优先识别真实 tool shim 命令名，同时保留 `TAU_AI_CLI_COMMAND_NAME` 供 release wrapper 注入。
- [x] AI/Agent export-shape decision：`docs/AI_AGENT_EXPORT_SHAPE.md` 固定上游 AI package/index exports 与 Agent index exports 到 Tau-native .NET assembly/package surface 的映射；`scripts/verify-ai-agent-export-shape.ps1` 已接入 `plan-release.ps1` 和 `verify-release-contracts.ps1`，本地 smoke 固定 75 assertions。
- [x] AI provider/OAuth matrix harness baseline：`scripts/verify-ai-provider-e2e-matrix.ps1` 已固定 11 个 foundation-first provider 的 inspect/isolation contract，并接入 `verify-release-contracts.ps1`；`-Isolated` 只证明本地 deterministic contract，不关闭真实 provider/OAuth e2e。2026-06-19 继续补上 `-RunConfigured -RequireConfigured` required-live-e2e guard：无已配置 provider、无真实 run attempt 或非隔离全成功结果会非零退出并输出 `completionStatus` / `gateFailure`，防止把无凭证 no-op 误记成真实 provider/OAuth 通过。
- [ ] Phase 2 critical contract closure：foundation-first gate 当前优先于其它工具/产品切片；通过后继续从 matrix 的 `Phase 2 Candidate Queue` 领取真实 provider/OAuth、package/global install alias、runtime contract 或 release/package 缺口。
- [ ] Phase 3 product runtime parity：CodingAgent + Tui、WebUi、Mom、Pods 的用户可见 runtime 行为继续补齐；fake-only runner、stub provider、fixture smoke 只能作为合同证据，不能标成产品完成。
- [ ] Phase 4 external e2e closure：真实 provider/OAuth/AWS/Slack/Docker/Pods/WebUi/browser/release 等 e2e 要通过；没有环境时保持 `external-e2e-needed`，不能降级成完成。
- [ ] Phase 5 release/package/CI final parity：Tau release/CI 必须产出真实 executable/package artifacts，并完成 registry/signing/provenance/non-host smoke 或取得用户明确非目标确认。
- [ ] Final audit：matrix 全部 `verified` 或用户确认 `non-goal`，`next.md` 无未完成 parity backlog，active plan 可归档为 completed。

## Strict completion criteria

全量 `Tau 100% pi-mono parity /goal` 只有在以下条件全部满足后才能标记 complete：

- 上游 `packages/ai`、`packages/agent`、`packages/coding-agent`、`packages/tui`、`packages/web-ui`、`packages/mom`、`packages/pods` 以及 root scripts/manifests 的用户可见功能、协议、命令、配置、环境变量、错误语义、日志、持久化 schema 和 release/CI 行为都有 Tau 对应实现、验证证据，或有用户明确确认的 `non-goal`。
- `docs/exec-plans/active/2026-05-28-pi-mono-parity-matrix.md` 中所有条目最终为 `verified` 或用户确认的 `non-goal`；`partial`、`missing`、`ported`、`external-e2e-needed` 不能作为最终完成状态。
- `next.md` 不再保留未完成 product parity backlog；无法完成的项必须写成用户确认的非目标，而不是“后续优化”。
- Windows PowerShell 本地权威链通过：
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore`
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-dotnet.ps1 -SkipRestore -RunSmoke`
- 真实外部 e2e 通过，或用户明确确认非目标：AI providers、OAuth、AWS/Bedrock、Slack、Docker、Pods SSH/HF/GPU/vLLM、WebUi browser/release/static smoke、package registry/signing/provenance/release smoke。
- release/CI 产出真实 Tau executable/package artifacts；不能只保留 dry-run、fake runner 或占位 wrapper。
- 每个实质变更都有 `docs/histories/YYYY-MM/**` 记录；active plan、matrix、`next.md`、`docs/QUALITY_SCORE.md` 和必要架构/安全/可靠性文档同步。
- 最终报告必须列出每个上游 package 和 root script 的 Tau 证据链：对应实现、targeted tests/smoke、外部 e2e 或用户确认的 `non-goal`。没有证据链的行不能被标为 `verified`。
